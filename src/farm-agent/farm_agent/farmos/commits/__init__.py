"""farm_agent/farmos/commits -- commit handlers package.

Provides the per-type commit modules and the normalize() shape transformer.

Phase 62-08: normalize.py + commit_activity, commit_input, commit_observation,
commit_harvest.

Phase 62-09: commit_seeding, commit_seeding_session (seeding family + group primitives).

Phase 62-10: commit_router (dispatch table).
"""
