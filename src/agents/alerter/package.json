{
  "name": "mushy-alerter",
  "version": "0.1.0",
  "description": "Signal alerter agent for FC-1 chamber (Phase 17)",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "test": "jest",
    "test:watch": "jest --watch",
    "eval:extraction": "jest --config test/eval/extraction/jest.config.js --runInBand",
    "test:eval-ingestion": "jest --config test/eval/ingestion/jest.config.js --runInBand",
    "test:eval": "EVAL_RUN_LIVE=1 jest --config test/eval/ingestion/jest.config.js --runInBand test/eval/ingestion/notebook-2025.test.js test/eval/ingestion/sessions.test.js"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.91.1",
    "jimp": "^0.22.12",
    "node-cron": "^3.0.3",
    "pg": "^8.20.0",
    "ulid": "^3.0.2",
    "ws": "^8.16.0",
    "yaml": "^2.9.0",
    "zod": "^3.23.0",
    "zod-to-json-schema": "^3.23.0"
  },
  "devDependencies": {
    "jest": "^29.7.0"
  }
}
