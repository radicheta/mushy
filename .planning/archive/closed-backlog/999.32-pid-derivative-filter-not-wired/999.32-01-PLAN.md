---
phase: 999.32-pid-derivative-filter-not-wired
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/chambers/fc-core/fc_core/fc_controller.py
  - src/chambers/fc-core/fc_core/test/test_pid_kernel.py
  - src/chambers/fc-core/fc_core/test/test_controller.py
  - src/chambers/fc-core/config/fc_config.yaml
autonomous: true
requirements: [FILT-01, FILT-02, FILT-03, FILT-04, FILT-05, FILT-06]

must_haves:
  truths:
    - "pid_derivative_filter_tau YAML value actually filters the D term at runtime (FILT-01)"
    - "On a synthetic noisy steady-state input, filtered D contribution RMS is at least 3x lower than unfiltered (FILT-02)"
    - "On a unit step in dRH/dt, filtered D term reaches ~63% of step amplitude in tau seconds (FILT-03)"
    - "Filter state resets cleanly on bumpless engage, Mode C entry, and sensor-staleness recovery (FILT-04)"
    - "ros2 param set /fc_controller pid_derivative_filter_tau <v> takes effect on the next tick (FILT-05)"
    - "vendor/simple_pid/pid.py is byte-identical to its pre-phase content (FILT-06)"
  artifacts:
    - path: "src/chambers/fc-core/fc_core/fc_controller.py"
      provides: "Filter state init, _reset_d_filter helper, _filtered_derivative_contribution helper, integration in control_loop, reset call sites"
      contains: "_filtered_derivative_contribution"
    - path: "src/chambers/fc-core/fc_core/test/test_pid_kernel.py"
      provides: "Pure-Python kernel tests for the IIR LPF math"
      contains: "test_d_filter_noise_rejection"
    - path: "src/chambers/fc-core/fc_core/test/test_controller.py"
      provides: "Live-reload param test for pid_derivative_filter_tau"
      contains: "test_pid_derivative_filter_live_reload"
  key_links:
    - from: "fc_controller.__init__"
      to: "self._pid (vendored PID instance)"
      via: "self._pid.Kd = 0.0 after construction"
      pattern: "self\\._pid\\.Kd\\s*=\\s*0\\.0"
    - from: "fc_controller.control_loop"
      to: "_filtered_derivative_contribution"
      via: "raw_pid_output + d_contribution before final clamp"
      pattern: "_filtered_derivative_contribution"
    - from: "fc_controller transitions (_engage_pid_bumplessly, Mode C entry, staleness recovery)"
      to: "_reset_d_filter"
      via: "explicit call at each discontinuity site"
      pattern: "_reset_d_filter\\(\\)"
---

<objective>
Wire `pid_derivative_filter_tau` into actual derivative filtering in `fc_controller.py` via an external first-order IIR low-pass on dRH/dt, leaving the vendored `simple_pid/pid.py` untouched. Approach is RESEARCH.md option (a) — recommended, HIGH confidence.

Purpose: Since Phase 27 shipped, Kd=4.0 has been multiplying raw, single-tick sensor jitter directly into duty (~±5–8% jitter on `pid_output` observed live 2026-05-04). The param was declared but never used; this phase makes it actually do something. Fix is small (~20 LOC + ~30 LOC tests).

Output: `fc_controller.py` patched, four new kernel tests, one extended controller test, vendored `pid.py` unchanged. Ready to deploy to fc1 next weekend (NOT during a live tuning session — see farmer constraint).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/999.32-pid-derivative-filter-not-wired/999.32-RESEARCH.md
@src/chambers/fc-core/fc_core/fc_controller.py
@src/chambers/fc-core/fc_core/vendor/simple_pid/pid.py
@src/chambers/fc-core/fc_core/test/test_pid_kernel.py
@src/chambers/fc-core/fc_core/test/test_controller.py
@src/chambers/fc-core/config/fc_config.yaml

<interfaces>
<!-- Key contracts the executor needs. Anchored to fc_controller.py line numbers verified 2026-05-04. -->

Vendored PID (do NOT modify — `vendor/simple_pid/pid.py` line 24 forbids it):
```python
class PID:
    def __call__(self, input_, dt=None) -> float: ...      # returns clamped P+I+D output
    def set_auto_mode(self, enabled: bool, last_output: float | None = None) -> None: ...
    Kp: float; Ki: float; Kd: float                         # writable; live-reload pattern
    auto_mode: bool
```

Controller anchors (current line numbers, fc_controller.py):
- `:42`   — param `('pid_derivative_filter_tau', 10.0)` declared (read-only after this — wire it)
- `:136`  — `self._safe_state_active = False`
- `:151`  — `self._pid = PID(...)` constructor
- `:160`  — `differential_on_measurement=True` (preserved — see Open Q1 in RESEARCH)
- `:252`  — `def _engage_pid_bumplessly(self):` — RESET CALL SITE #1
- `:342`  — `def control_loop(self):`
- `:402-404` — staleness recovery branch (`self._safe_state_active = False`) — RESET CALL SITE #2
- `:416`  — `_engage_pid_bumplessly()` invocation (already covered by #1)
- `:428-433` — Mode C bypass branch (`if abs(error_pct) > bypass_pct:`) — RESET CALL SITE #3
- `:419-421` — live-reload of pid_kp/ki/kd (mirror this pattern for tau and kd_effective)

Reset call sites (4 total, per RESEARCH.md "Reset on discontinuity"):
1. `_engage_pid_bumplessly` (line 252-ish)            — implemented this phase
2. Mode C entry in control_loop (line 428-ish)        — implemented this phase
3. Sensor-staleness recovery (line 402-ish)           — implemented this phase
4. SHT30 heater-cycle recovery (post-blanking-window) — **wired by 999.34, no-op until then.** Document the hook so 999.34 only needs to add one line.

Filter math (RESEARCH.md Pattern 1):
```
alpha = dt / (tau + dt)            # tau<=0 -> alpha=1.0 (filter disabled / pass-through)
d_filtered = alpha * raw_rate + (1 - alpha) * d_filtered_prev
contribution = -kd_effective * d_filtered
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add IIR LPF kernel tests (RED) + extend controller param test</name>
  <files>
    src/chambers/fc-core/fc_core/test/test_pid_kernel.py,
    src/chambers/fc-core/fc_core/test/test_controller.py
  </files>
  <behavior>
    Four new kernel tests in `test_pid_kernel.py`, all pure-Python (no ROS, no fc_controller import) — they test the IIR LPF math against a reference implementation embedded in the test file. This means the tests pass standalone before the controller wiring exists; they verify the *math* the controller will implement. After Task 2 lands, an additional kernel test imports the controller helper and asserts byte-equivalence against the in-test reference.

    - `test_d_filter_noise_rejection` (FILT-02): seed=0, 60 ticks at dt=0.2s, constant input 94.0 + Gaussian sigma=0.05, Kd=4.0, tau=10.0. Compute both filtered and raw `-Kd · rate` series; assert `raw_rms / filt_rms >= 3.0`. Per RESEARCH §"Common Pitfalls 6", metric is RMS of tick-to-tick contribution.
    - `test_d_filter_step_response` (FILT-03): unit step `raw_rate=1.0` for n=tau/dt ticks; assert filtered value in [0.55, 0.70] (theoretical 1 - e^-1 ≈ 0.632).
    - `test_d_filter_reset_clears_state` (FILT-04): build up nonzero `d_filtered` with N=20 ticks of constant rate=1.0; reset (`d_filtered=0, last_input=None`); assert next call with any input returns contribution = 0.0 (first-tick-after-reset rule).
    - `test_d_filter_disabled_with_tau_zero` (FILT-05 corollary): tau=0.0 → alpha=1.0 → filtered output equals raw rate exactly. Tests the live-A/B-test affordance.

    In `test_controller.py`: extend with `test_pid_derivative_filter_live_reload` — uses the existing `ros_context` / node fixture pattern (see existing `test_pid_params_declared`). Builds the node, sets `pid_derivative_filter_tau` to 5.0 via `set_parameters([Parameter(...)])`, verifies `node.get_parameter('pid_derivative_filter_tau').value == 5.0`. (Does NOT spin a control loop; just verifies the live-reload contract — same depth as existing `test_pid_params_declared`.)

    All four kernel tests use a small reference function defined inside the test module:
    ```python
    def _ref_filter_step(prev_filt, raw_rate, dt, tau):
        alpha = 1.0 if tau <= 0.0 else dt / (tau + dt)
        return alpha * raw_rate + (1 - alpha) * prev_filt
    ```
    Task 2 will then import the controller's helper and assert it produces identical output for a fixed input sequence (added as `test_d_filter_matches_reference` at the end of Task 2).

    Why TDD: RESEARCH §FILT-05 explicitly flags Wave 0 test gaps. The math is small and deterministic; tests are cheaper to write first and pin behavior before patching the controller (which is harder to drive in a unit test due to ROS spin).
  </behavior>
  <action>
    1. Open `src/chambers/fc-core/fc_core/test/test_pid_kernel.py`. Append the four tests using the sketches in RESEARCH §"Test design sketches" (lines 530-588) verbatim where complete; fill in `test_d_filter_reset_clears_state` and `test_d_filter_disabled_with_tau_zero` per the "behavior" notes above. Use `random.seed(0)` for determinism. No ROS imports.
    2. Open `src/chambers/fc-core/fc_core/test/test_controller.py`. Locate the existing `test_pid_params_declared` test, add `test_pid_derivative_filter_live_reload` immediately after using the same fixture style (look at the surrounding test for the exact pattern — do not reinvent fixture machinery).
    3. Run kernel tests: `pytest src/chambers/fc-core/fc_core/test/test_pid_kernel.py -x`. The four new tests MUST pass (they reference an in-test math impl, not the controller). Existing kernel tests must still pass.
    4. Run controller tests: `pytest src/chambers/fc-core/fc_core/test/test_controller.py -x`. New live-reload test must pass.
    5. Commit. Message: `test(999.32): add IIR LPF kernel tests for pid_derivative_filter_tau`
  </action>
  <verify>
    <automated>cd /mnt/slime-kingdom/opt/mushy && pytest src/chambers/fc-core/fc_core/test/test_pid_kernel.py src/chambers/fc-core/fc_core/test/test_controller.py -x -q</automated>
  </verify>
  <done>
    Four new tests in `test_pid_kernel.py` pass against in-test reference math; one new test in `test_controller.py` passes; all pre-existing tests still pass; commit landed.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire IIR LPF into fc_controller.py + add reference-equivalence test (GREEN)</name>
  <files>
    src/chambers/fc-core/fc_core/fc_controller.py,
    src/chambers/fc-core/fc_core/test/test_pid_kernel.py,
    src/chambers/fc-core/config/fc_config.yaml
  </files>
  <behavior>
    Controller now uses the IIR LPF for the D contribution. Acceptance:

    - In `__init__`, after `self._pid = PID(...)`: set `self._pid.Kd = 0.0` (we own D now), initialize `self._kd_effective`, `self._d_filtered = 0.0`, `self._last_input_for_d = None`.
    - New method `_reset_d_filter(self)`: sets `self._d_filtered = 0.0`, `self._last_input_for_d = None`. Idempotent. No logging (called frequently).
    - New method `_filtered_derivative_contribution(self, current_input: float, dt: float) -> float`: implements the math in `<interfaces>` above. Live-reloads `pid_kd` (into `self._kd_effective`) and `pid_derivative_filter_tau` each call. Returns 0.0 when `_last_input_for_d is None` or `dt <= 0.0` (first-tick / degenerate-dt guard, per RESEARCH Pitfall 3).
    - In `control_loop` after `raw_pid_output = self._pid(error_pct, dt=dt)`: compute `d_contribution = self._filtered_derivative_contribution(error_pct, dt)`, then `combined = raw_pid_output + d_contribution`, then `duty = max(0.0, min(1.0, combined))`. Use `error_pct` (not `current_humidity`) — see RESEARCH Open Q1; this preserves the input the lib was previously receiving.
    - Reset hooks (3 sites in this phase, 4th deferred to 999.34):
      1. `_engage_pid_bumplessly`: append `self._reset_d_filter()` after `self._pid.set_auto_mode(...)`.
      2. Sensor-staleness recovery (after `self._safe_state_active = False` in the recovery branch ~line 402-404): add `self._reset_d_filter()` before the "Fresh sensor data received" log.
      3. Mode C entry (the `if abs(error_pct) > bypass_pct:` branch ~line 428-433): add `self._reset_d_filter()` immediately after the `set_auto_mode(False)` call. Mode C forces `duty = 1.0` directly so the contribution is never used; the reset prevents stale state from polluting the post-Mode-C tick when error drops back inside the bypass band.
      4. SHT30 heater-cycle recovery: add a 1-line code comment near `_reset_d_filter` definition: `# Phase 999.34 will add a 4th caller after the SHT30 heater-cycle blanking window. No-op stub until then.` No call site needed in this phase.
    - YAML: in `src/chambers/fc-core/config/fc_config.yaml`, update the comment beside `pid_derivative_filter_tau: 10.0` to read something like `# Time constant (s) for first-order IIR LPF on dRH/dt. tau<=0 disables. tau should be >> dt (control_interval). Live-reloadable. Wired into PID per Phase 999.32.` (Replace whatever placeholder/no comment is there — do NOT change the value 10.0.)
    - Add one final kernel test `test_d_filter_matches_reference` to `test_pid_kernel.py`: instantiates a tiny shim that calls the controller's filter math (extract the pure math into a module-level helper if cleanest, OR copy the same arithmetic and assert byte-equivalence against the in-test reference for a fixed deterministic input sequence of 30 ticks). Goal: lock the controller's filter to the math the kernel tests verified.

    Tests after this task: 5 new kernel tests + 1 new controller test all green; vendored `pid.py` git-diff empty; existing test suite intact.
  </behavior>
  <action>
    1. Read `src/chambers/fc-core/fc_core/fc_controller.py` once, end to end (it's small enough).
    2. Apply edits per "behavior" above, in this order:
       a. `__init__` filter state + `self._pid.Kd = 0.0`.
       b. New `_reset_d_filter` and `_filtered_derivative_contribution` methods (place them near `_engage_pid_bumplessly` for locality).
       c. `control_loop`: integrate the contribution after `self._pid(...)` call, before clamp.
       d. Three reset call sites (bumpless engage, staleness recovery, Mode C entry).
       e. YAML comment update.
    3. Sanity check: `git diff src/chambers/fc-core/fc_core/vendor/simple_pid/pid.py` MUST be empty (FILT-06).
    4. Add `test_d_filter_matches_reference` to `test_pid_kernel.py`. If extracting the filter math to a module-level helper in `fc_controller.py` is cleaner, do so (function `_iir_lpf_step(prev, raw, dt, tau) -> float` at module scope, used by both the method and the test). This is a clean factoring, not a refactor.
    5. Build the package: `cd /mnt/slime-kingdom/opt/mushy && colcon build --packages-select fc_core --symlink-install`. Source the install: `source install/setup.bash` if needed for tests.
    6. Run the full fc_core test suite: `colcon test --packages-select fc_core && colcon test-result --verbose --test-result-base build/fc_core` OR fall back to `pytest src/chambers/fc-core/fc_core/test/ -x`.
    7. Lint: `ament_flake8 src/chambers/fc-core/fc_core/fc_controller.py` — fix any new violations introduced by the patch. Do NOT touch unrelated lint debt.
    8. Commit. Message: `fix(999.32): wire pid_derivative_filter_tau via external IIR LPF on dRH/dt`. NO Co-Authored-By trailer.
  </action>
  <verify>
    <automated>cd /mnt/slime-kingdom/opt/mushy && pytest src/chambers/fc-core/fc_core/test/ -x -q && git diff --exit-code src/chambers/fc-core/fc_core/vendor/simple_pid/pid.py && ament_flake8 src/chambers/fc-core/fc_core/fc_controller.py</automated>
  </verify>
  <done>
    All fc_core tests green (kernel + controller + others). `pid.py` byte-identical to pre-phase. flake8 clean on the modified file. Three reset call sites in place; 4th documented as a 999.34 hook. YAML comment accurate. Commit landed on a feature branch (NOT pushed to fc1/prod yet — see plan-level rollout note).
  </done>
</task>

</tasks>

<verification>

## Phase-level checks (post-Task-2)

1. **Static contract:** `git diff src/chambers/fc-core/fc_core/vendor/simple_pid/pid.py` is empty (FILT-06).
2. **Math contract:** all 5 kernel tests + 1 controller test green (FILT-01..05).
3. **Wiring contract:** `grep -n "_filtered_derivative_contribution\|_reset_d_filter" src/chambers/fc-core/fc_core/fc_controller.py` shows: 1 method def, 1 call inside `control_loop`, 1 def for `_reset_d_filter`, ≥3 reset call sites.
4. **Composition with 999.34:** comment near `_reset_d_filter` references the 4th hook (SHT30 heater-cycle recovery) so 999.34's planner sees the seam.
5. **No-op for 999.27:** filter is a private controller concern; no new ROS topic published this phase. `pid_d_filtered` as a published telemetry field is **noted as a future enhancement** (see "Composition" below) but explicitly not in scope here — does not gate on 999.27.

## Live validation (deferred to next weekend tuning session)

Per farmer constraint "do not change prod code while live-tuning", **the FILT-02 ≥3× claim is verified in unit tests this phase.** Live confirmation against fc1 telemetry is a manual checkpoint owned by the next calibration session, **not by this phase's execution.**

When the live session happens (out-of-scope for this plan):
- Capture pre-deploy 30-min `fc.pid_output` baseline at quiet operating point.
- Deploy via fc1/prod branch + `deploy.sh`.
- Capture 30-min post-deploy at same conditions.
- Compare tick-to-tick RMS. Target: ≥3× reduction (RESEARCH FILT-02).
- If miss, diagnosis is wrong (not D-noise dominant). Do not blame the filter without checking residual cycle period (RESEARCH Pitfall 5).

</verification>

<success_criteria>
- [ ] `pid_derivative_filter_tau` actually filters the D term in `control_loop` (FILT-01)
- [ ] Kernel test asserts ≥3× noise reduction with seeded synthetic data (FILT-02)
- [ ] Kernel test asserts τ-second 63% step response (FILT-03)
- [ ] Three reset call sites in place: bumpless engage, staleness recovery, Mode C entry (FILT-04)
- [ ] Live-reload test confirms `ros2 param set` path works (FILT-05)
- [ ] `vendor/simple_pid/pid.py` git-diff empty (FILT-06)
- [ ] `colcon test --packages-select fc_core` green
- [ ] `ament_flake8` clean on modified file
- [ ] 4th reset call site (999.34 SHT30 heater cycle) documented as a no-op stub with comment
- [ ] Commit landed on feature branch only — NOT pushed to `fc1/prod`
</success_criteria>

<rollout_strategy>

## Deploy gate

This phase produces code only. Deploy to fc1 is a separate, deliberate operator action:

1. Land both commits on `main` after review.
2. **Do not deploy mid-tuning-session.** Wait for a quiet window (next weekend per phase brief).
3. Pre-deploy: `ssh fc1 -- 'cd ~/mushroom_farm_ws && git fetch && git log HEAD..origin/fc1/prod --oneline'` — confirm clean state.
4. Capture pre-fix baseline (see Verification §Live validation).
5. Push `main` → `fc1/prod`, `ssh fc1 ./deploy.sh`, `systemctl restart fc-core`.
6. Watch `journalctl -u fc-core -f` for first 5 min for any startup error.
7. Capture post-fix curve, compare to baseline.

## Rollback

The fix is contained in `fc_controller.py` + tests. To rollback:
- `git revert` both commits (test commit + fix commit) on a branch.
- Push to `fc1/prod`, `deploy.sh`, restart.
- Filter state on the live node disappears with the process restart — no persistent state to clean up.

**Live A/B affordance:** instead of a code rollback, set `ros2 param set /fc_controller pid_derivative_filter_tau 0.0`. With τ=0, α=1.0, filter is pass-through — equivalent to current (broken) behavior. This is the recommended first-line de-risk.

</rollout_strategy>

<risks>

| Risk | Likelihood | Impact | Mitigation |
|------|-----------|--------|------------|
| Double-D bug (Kd on lib AND external) | LOW | HIGH (oscillation worsens) | RESEARCH Pitfall 1; Task 2 explicitly sets `self._pid.Kd = 0.0`; kernel test compares against reference math |
| dt=0/None on first tick injects NaN | LOW | MED | Helper returns 0.0 when `_last_input_for_d is None` or `dt <= 0.0`; pre-existing line 409-412 guards dt fallback |
| Stale filter state across mode boundaries | MED | MED | Three reset call sites; reset test (FILT-04) covers contract |
| τ=10s too aggressive, D becomes ineffective | MED | LOW | Live-tunable via ROS param; sweep plan in RESEARCH §"τ Selection"; if real transients (door-open) get under-corrected, drop to τ=3.0 live |
| Setpoint-ramp injects D kick (we differentiate `error_pct`, not measurement) | LOW | LOW | RESEARCH Open Q1; if observed in calibration, switch to differentiating `current_humidity * 100` — one-line change |
| 4th reset hook (999.34) forgotten when heater-cycle ships | MED | MED | In-code comment near `_reset_d_filter` flags the seam; this plan's verification checklist explicitly mentions it |
| Live-reload of τ during oscillation creates transient | LOW | LOW | α recompute is per-tick (RESEARCH Anti-Pattern "Caching α"); transient is bounded by ~τ_new |

</risks>

<composition_notes>

## With 999.34 (SHT30 heater-cycle blanking)

- 999.34 will introduce a sensor-output discontinuity (~30s blanking window after each heater cycle).
- 999.34's planner: add **one line** to call `self._reset_d_filter()` at the end of the blanking window, alongside whatever filter-window-recovery logic 999.34 introduces. The `_reset_d_filter` helper is already correct for this purpose.
- This phase pre-comments the seam so 999.34's planner sees it.

## With 999.27 (derived telemetry)

- `self._d_filtered` (the filtered rate, in %RH/s) **could** be a useful published telemetry field for dashboard validation of this filter's behavior in production.
- **Not in scope for 999.32.** When 999.27 is planned, it can publish a `fc1/pid/d_filtered` topic by reading `self._d_filtered` at the same cadence as existing PID telemetry. Zero coupling: this phase ships without touching the bridge or telemetry pipeline.

## With next PID retune (separate backlog)

- Filing this fix **before** the next Kd-lowering experiment prevents conflating "Kd was too high" with "raw D was noisy". Per RESEARCH §"User Constraints", this is the explicit reason 999.32 is being broken out.

</composition_notes>

<output>
After completion, create `.planning/phases/999.32-pid-derivative-filter-not-wired/999.32-01-SUMMARY.md` with:
- Files modified (with LOC delta)
- Test count: tests added, total fc_core tests after
- `git diff --stat src/chambers/fc-core/fc_core/vendor/simple_pid/pid.py` (must be empty)
- Pre-deploy checklist for next weekend's live session (baseline capture, deploy steps, post-deploy comparison)
- Composition seams left for 999.34 and 999.27
</output>
