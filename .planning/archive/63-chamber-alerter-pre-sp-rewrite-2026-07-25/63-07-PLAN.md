---
phase: 63-chamber-alerter
plan: 07
type: execute
wave: 3
depends_on: ["63-04"]
files_modified:
  - src/farm-agent/farm_agent/chamber/state.py
  - src/farm-agent/tests/chamber/test_state.py
autonomous: true
requirements: [CHM-01]
must_haves:
  truths:
    - "resolve_effective_config reproduces Tier A (mode-anchored, fresh-only), Tier B (per-mode override), Tier C (global override, mode-freshness-independent), and the fresh/stale/cold freshness state (D-01)."
    - "The stale freshness state suspends the RH rule (feeds is_rh_oob); Tier C (pi_offline_min etc.) applies even when mode is stale/cold."
    - "drive_alert_type is ONE generic function; pi/sensor/sht30/scd41 use the fast-fire override (oob_n=1, oob_window_min=0) but rh/humidifier do NOT (Pitfall 3)."
    - "mode_update resets oob_count/first_oob_at/in_band_count for rh/humidifier but PRESERVES last_fired_at cooldown (Pitfall 6)."
    - "All 6 alert types are present; FSM state resets in-memory on restart (D-06/D-07)."
  artifacts:
    - path: "src/farm-agent/farm_agent/chamber/state.py"
      provides: "FSM: STATES, SEVERITY, initial_state, cooldown_ms, is_snoozed, drive_alert_type, resolve_effective_config, has_mode_context, transition/event handlers"
      contains: "resolve_effective_config"
  key_links:
    - from: "farm_agent/chamber/state.py"
      to: "farm_agent/chamber/rules.py"
      via: "detector calls in event handlers"
      pattern: "rules\\.(is_rh_oob|is_pi_offline|is_humidifier_stuck|is_sensor_silent|is_sensor_error)"
    - from: "state.py has_mode_context gate"
      to: "resolve_effective_config"
      via: "Pattern 4 cold-boot gate"
      pattern: "has_mode_context"
---

<objective>
Port the alert state machine (`state.js`) — the single highest-risk file in the port. It owns
`resolve_effective_config` (the Tier A/B/C + freshness resolver, D-01), the generic
`drive_alert_type` FSM transition (OK→PENDING→FIRING→SNOOZED, reused by all 6 alert types), cooldown,
severity, snooze evaluation, and the per-event handlers (humidity, mode_update, pi_liveness, tick,
sensor_health) that call the Plan-04 detectors.

Purpose: This is where Phase-64 parity is won or lost. Every dated bug-fix guard (Pitfall 3 fast-fire
override, Pitfall 6 cooldown-survives-mode-swap, the has_mode_context cold-boot gate) is load-bearing.
FSM state is in-memory (D-06) and covers all 6 types (D-07).
Output: `chamber/state.py` and a comprehensive FSM test suite.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/63-chamber-alerter/63-CONTEXT.md
@.planning/phases/63-chamber-alerter/63-RESEARCH.md
@.planning/phases/63-chamber-alerter/63-PATTERNS.md

<interfaces>
state.js → state.py mapping (RESEARCH § state.js table):
  STATES = OK/PENDING/FIRING/SNOOZED (exact strings — parity replay may assert on them).
  ALERT_TYPES = [rh, sensor, pi, humidifier, sht30, scd41] (D-07: all 6, this order).
  SEVERITY: rh/humidifier = WARN; sensor/pi/sht30/scd41 = CRITICAL.
  initial_state(now_ms): sht30_last_seen_ms/scd41_last_seen_ms init to booted_at_ms, NEVER None
    (so a never-seen sensor doesn't fire spuriously).
  cooldown_ms(alert_type, config): severity-conditional (critical_cooldown_min vs cooldown_min).
  is_snoozed(entry, now): snoozed_until is not None and now < snoozed_until.
  drive_alert_type(entry, alert_type, oob_now, fields, now, config): Pattern 3 — ONE generic fn.
  resolve_effective_config(state, env_config, now_ms): D-01 Tier A/B/C + freshness {state, source}.
    Tier A (mode-anchored): rh_target = current_mode.target_humidity*100, rh_band from live ROS mode;
      applies only when mode FRESH (mode_age <= mode_stale_min, ws connected).
    Tier B (per-mode): oob_n, oob_window_min, cooldown_min, critical_cooldown_min,
      humidifier_stuck_min from alerter_overrides[mode.name].
    Tier C (global): pi_offline_min, sensor_offline_min, heartbeat_hour, max_sends_per_hour from
      alerter_globals — INDEPENDENT of mode freshness.
    Freshness: fresh|stale|cold; stale/cold → env fallback; stale feeds is_rh_oob suspend (2026-05-07).
  has_mode_context(state): Pattern 4 gate — only route through resolve_effective_config once a
    mode_update/overrides_update/globals_update has EVER been received; else feed raw ChamberConfig.
  transition(prev, event, now, config): deep-clone-then-mutate SHAPE preserved. RESEARCH + PATTERNS
    recommend dataclasses + dataclasses.replace over dict+deepcopy (no shared mutable refs across ticks).

Pitfall 3 fast-fire: pi/sensor/sht30/scd41 handlers build {..effective, oob_n:1, oob_window_min:0}
  before drive_alert_type; rh/humidifier keep real effective.oob_n/oob_window_min. Extract ONE helper
  `_fast_fire_config(effective)` used at all ~6 call sites, NOT applied to rh/humidifier.
Pitfall 6 mode_update: reset oob_count=0, first_oob_at=None, ctx.in_band_count=0 for rh/humidifier
  ONLY; PRESERVE last_fired_at (cooldown survives mode swap); sensor/pi/sht30/scd41 untouched.

Detectors imported from chamber/rules.py (Plan 04). sensor_flap_min_sec flap floor gates only the
Pi-side xxx_fresh='false' FLAG path (Pitfall 4), separate from is_sensor_silent.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: FSM primitives + resolve_effective_config (Tier A/B/C + freshness)</name>
  <read_first>
    - src/agents/alerter/src/state.js (full, 718 lines — parity target; resolveEffectiveConfig is the highest-risk region)
    - .planning/phases/63-chamber-alerter/63-RESEARCH.md § state.js mapping table + Pitfall 1 (stale-suspend) + Pattern 4 (has_mode_context) + § D-01
    - .planning/phases/63-chamber-alerter/63-PATTERNS.md § "chamber/state.py" (dataclasses over dict+deepcopy)
    - src/farm-agent/farm_agent/chamber/config.py (ChamberConfig knob names — from Plan 03)
  </read_first>
  <behavior>
    - test_resolve_effective_config_tiers: Tier A applies mode target only when fresh; Tier C
      (pi_offline_min) applies even when mode is stale/cold; freshness state is fresh/stale/cold
      across the mode-present/absent × age matrix.
    - test_has_mode_context_cold_boot: before any mode/overrides/globals event, raw ChamberConfig is
      fed (no resolve_effective_config); after one such event, the resolver is used.
    - test_stale_suspends_rh: freshness.state=='stale' produces an effective config whose freshness
      makes is_rh_oob return False.
    - initial_state: sht30_last_seen_ms/scd41_last_seen_ms == booted_at_ms (not None).
  </behavior>
  <action>
    Create `farm_agent/chamber/state.py` with: STATES/ALERT_TYPES/SEVERITY constants (exact strings,
    6 types), an FSM entry dataclass, `initial_state(now_ms)` (sht30/scd41 last-seen init to
    booted_at_ms), `cooldown_ms(alert_type, config)` (severity-conditional), `is_snoozed(entry, now)`,
    `has_mode_context(state)` (Pattern 4 gate), and `resolve_effective_config(state, env_config,
    now_ms)` implementing the full Tier A/B/C + freshness resolver per the interface spec. Use
    dataclasses + dataclasses.replace for the effective-config and entry copies (no shared mutable
    refs). Docstrings cite state.js line ranges + the 2026-05-07 stale-suspend rationale. Create
    `tests/chamber/test_state.py` (part 1) covering the resolver tier matrix, the has_mode_context
    gate, stale-suspend, and initial_state.
  </action>
  <verify>
    <automated>cd src/farm-agent && uv run pytest tests/chamber/test_state.py -x -k "resolve or has_mode or stale or initial"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -Ec "def (resolve_effective_config|has_mode_context|initial_state|cooldown_ms|is_snoozed)" farm_agent/chamber/state.py` == 5.
    - ALERT_TYPES has all 6 (rh, sensor, pi, humidifier, sht30, scd41); SEVERITY maps rh/humidifier→WARN, others→CRITICAL.
    - `test_resolve_effective_config_tiers` proves Tier C applies under stale mode while Tier A does not.
    - `initial_state` sets sht30/scd41 last-seen to booted_at_ms (asserted not None).
  </acceptance_criteria>
  <done>The config resolver + FSM primitives reproduce the Tier A/B/C + freshness + cold-boot behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: drive_alert_type + per-event handlers (fast-fire override, mode-swap cooldown)</name>
  <read_first>
    - src/agents/alerter/src/state.js (full — driveAlertType + the humidity/mode_update/pi_liveness/tick/sensor_health handlers)
    - .planning/phases/63-chamber-alerter/63-RESEARCH.md § Pitfall 3 (fast-fire oob_n=1/oob_window_min=0) + Pitfall 4 (flap-floor scope) + Pitfall 6 (mode_update preserves last_fired_at)
    - src/farm-agent/farm_agent/chamber/rules.py (detector signatures — from Plan 04)
    - src/farm-agent/farm_agent/signal_io/receive_loop.py (sequential-never-gather per-item try/except discipline)
  </read_first>
  <behavior>
    - test_fast_fire_types: pi/sensor/sht30/scd41 fire on the FIRST oob occurrence (oob_n=1,
      oob_window_min=0); rh/humidifier require the real oob_n/oob_window_min debounce.
    - test_mode_update_preserves_cooldown: a mode_update resets oob_count/first_oob_at/in_band_count
      for rh/humidifier but a within-cooldown re-trigger is still suppressed (last_fired_at preserved);
      sensor/pi/sht30/scd41 entries are untouched by mode_update.
    - test_full_fsm_transitions: OK→PENDING→FIRING→SNOOZED via drive_alert_type across representative
      events; snooze application moves an entry to SNOOZED and suppresses sends until expiry.
  </behavior>
  <action>
    Extend `farm_agent/chamber/state.py` with the generic `drive_alert_type(entry, alert_type,
    oob_now, fields, now, config)` (Pattern 3 — one function for all 6 types) and the per-event
    handlers (humidity, mode_update, pi_liveness, tick, sensor_health) that call the chamber.rules
    detectors and route through drive_alert_type. Add one shared `_fast_fire_config(effective)`
    helper (copy with oob_n=1, oob_window_min=0) applied at the pi/sensor/sht30/scd41 call sites but
    NOT rh/humidifier (Pitfall 3). In mode_update, reset oob_count/first_oob_at/in_band_count for
    rh/humidifier only and PRESERVE last_fired_at (Pitfall 6); leave sensor/pi/sht30/scd41 untouched.
    Keep the sensor flap-floor (sensor_flap_min_sec) gating only the Pi-flag path, separate from
    is_sensor_silent (Pitfall 4). Iterate alert types sequentially with per-item try/except (no
    asyncio.gather). Extend `tests/chamber/test_state.py` (part 2) with the behaviors above.
  </action>
  <verify>
    <automated>cd src/farm-agent && uv run pytest tests/chamber/test_state.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "def drive_alert_type" farm_agent/chamber/state.py` == 1 (single generic fn, not 6).
    - `grep -c "_fast_fire_config" farm_agent/chamber/state.py` >= 1 and it is applied to pi/sensor/sht30/scd41, not rh/humidifier.
    - `test_mode_update_preserves_cooldown` passes (last_fired_at survives mode swap).
    - `uv run pytest tests/chamber/test_state.py -x` green (full FSM suite).
  </acceptance_criteria>
  <done>The generic FSM drives all 6 alert types with the fast-fire override and mode-swap cooldown continuity intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| WS/detector inputs → FSM | Telemetry-derived booleans/timestamps drive alert transitions (internal, semi-trusted). Correctness of suppression logic is a safety property (missed CRITICAL) more than a security one. |
| snooze application → FSM mutation | A parsed snooze (already whitelist-gated upstream) mutates in-memory FSM state (mute-abuse surface). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-63-12 | Denial of Service (missed alert) | inverted stale-suspend / wrong fast-fire config silently suppressing CRITICALs | mitigate | Pitfall-1/3 tests assert the exact suppression matrix; parity replay in Phase 64 is the backstop. |
| T-63-13 | Tampering | snooze mutating FSM to silence a real alert beyond intended scope | accept | Snooze is whitelist-gated (V4, upstream), time-bounded, in-memory (D-06); mode_update does not clear snooze incorrectly. Node parity behavior. |
</threat_model>

<verification>
`cd src/farm-agent && uv run pytest tests/chamber/test_state.py -x` green (Tier A/B/C matrix, fast-fire,
mode-swap cooldown, full FSM transitions).
</verification>

<success_criteria>
The FSM reproduces state.js behavior exactly — Tier A/B/C resolution, all 6 types, fast-fire override,
and cooldown-survives-mode-swap — as the foundation for Phase-64 parity.
</success_criteria>

<output>
Create `.planning/phases/63-chamber-alerter/63-07-SUMMARY.md` when done.
</output>
