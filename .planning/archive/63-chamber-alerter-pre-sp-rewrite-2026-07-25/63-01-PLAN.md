---
phase: 63-chamber-alerter
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/farm-agent/.lint-imports
  - src/farm-agent/tests/test_foray_seam.py
  - src/farm-agent/tests/test_import_linter_contract.py
autonomous: true
requirements: [CHM-01]
must_haves:
  truths:
    - "Foray packages (tenancy, persistence, extraction, signal_io, confirm, farmos, capture, gate) cannot import farm_agent.chamber — enforced by BOTH the grep gate and import-linter."
    - "`uv run lint-imports` exits 0 against the current tree (no hard-error on nonexistent source modules)."
    - "The import-linter contract runs inside the pytest suite for the first time."
  artifacts:
    - path: "src/farm-agent/.lint-imports"
      provides: "corrected source_modules matching real package set"
      contains: "farm_agent.farmos"
    - path: "src/farm-agent/tests/test_import_linter_contract.py"
      provides: "pytest wrapper asserting lint-imports exit 0"
    - path: "src/farm-agent/tests/test_foray_seam.py"
      provides: "FORAY_PACKAGES grep scope covering all 8 current Foray packages"
  key_links:
    - from: "tests/test_import_linter_contract.py"
      to: ".lint-imports"
      via: "subprocess uv run lint-imports"
      pattern: "lint-imports"
---

<objective>
Activate the Foray seam gate correctly (D-00). Fix the two pre-existing defects that block
activation: `.lint-imports` `source_modules` references a nonexistent package
(`farm_agent.farmos_client`) and hard-errors today, and `tests/test_foray_seam.py`'s
`FORAY_PACKAGES` grep scope only covers 3 of the 8 shipped Foray packages. Then wire
`lint-imports` into the pytest suite for the first time.

Purpose: The seam gate is the load-bearing guarantee that `chamber/` (mushy-private) stays a
consumer of the Foray island, never a dependency of it. Every subsequent chamber plan relies on
this gate being real and green. Per D-00 the enforced direction is `forbidden: Foray ↛ chamber`
(chamber → Foray is allowed) — NOT the inverted ROADMAP SC3 prose.
Output: A green seam gate enforced by both grep and import-linter, runnable via pytest.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/63-chamber-alerter/63-CONTEXT.md
@.planning/phases/63-chamber-alerter/63-RESEARCH.md
@.planning/phases/63-chamber-alerter/63-PATTERNS.md

<interfaces>
Current (broken) `.lint-imports` source_modules lists `farm_agent.farmos_client` (does not
exist; real package is `farm_agent.farmos`) and `farm_agent.llm` (does not exist), and omits
`signal_io`, `confirm`, `farmos`, `capture`. Real current package set:
tenancy, persistence, extraction, signal_io, confirm, farmos, capture, gate.
`forbidden_modules = farm_agent.chamber` (correct as-is; import-linter skips nonexistent
forbidden_modules, so the contract is valid even before chamber/ exists).
`tests/test_foray_seam.py` FORAY_PACKAGES currently = [tenancy, persistence, extraction].
Do NOT touch `test_no_chamber_imports_in_foray`, `test_seam_trips_on_violation`,
`test_seam_trips_on_bare_import_form` — they are correct and grep the FORAY_PACKAGES list.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix .lint-imports source_modules + wire import-linter into pytest</name>
  <read_first>
    - src/farm-agent/.lint-imports (the file being modified; Pitfall 7 target)
    - .planning/phases/63-chamber-alerter/63-PATTERNS.md § "`.lint-imports` (MODIFY — Pitfall 7 fix)" (exact before/after source_modules block)
    - .planning/phases/63-chamber-alerter/63-RESEARCH.md § Pitfall 7 (verified live: before-fix hard error, after-fix "Contracts: 1 kept, 0 broken" exit 0)
  </read_first>
  <action>
    In `.lint-imports`, replace the `source_modules` list under
    `[importlinter:contract:foray-seam]` with the real current package set:
    farm_agent.tenancy, farm_agent.persistence, farm_agent.extraction, farm_agent.signal_io,
    farm_agent.confirm, farm_agent.farmos, farm_agent.capture, farm_agent.gate. Drop
    `farm_agent.farmos_client` and `farm_agent.llm`. Leave `forbidden_modules = farm_agent.chamber`
    and `root_packages` untouched. Update the stale header comment block (the "chamber/ does not
    exist yet ... Do NOT add import-linter to the pytest run until Phase 63" note) to record that
    Phase 63 activates the contract. Then create `tests/test_import_linter_contract.py` (new file):
    a pytest test that invokes `uv run lint-imports --config .lint-imports` via subprocess from the
    `src/farm-agent` root and asserts exit code 0, capturing stdout/stderr into the assertion
    message on failure. Follow the doc-comment-as-spec header convention (cite D-00 + Pitfall 7).
  </action>
  <verify>
    <automated>cd src/farm-agent && uv run lint-imports --config .lint-imports; test $? -eq 0 && uv run pytest tests/test_import_linter_contract.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `uv run lint-imports --config .lint-imports` exits 0 and prints "Contracts: 1 kept, 0 broken".
    - `source_modules` in `.lint-imports` contains `farm_agent.farmos` and does NOT contain
      `farm_agent.farmos_client` or `farm_agent.llm`.
    - `tests/test_import_linter_contract.py` exists and passes; it shells out to `lint-imports` and
      asserts exit 0.
  </acceptance_criteria>
  <done>Import-linter runs clean against the real tree and is enforced inside pytest.</done>
</task>

<task type="auto">
  <name>Task 2: Correct test_foray_seam.py FORAY_PACKAGES grep scope</name>
  <read_first>
    - src/farm-agent/tests/test_foray_seam.py (the file being modified; Pitfall 8 target, full file)
    - .planning/phases/63-chamber-alerter/63-PATTERNS.md § "`tests/test_foray_seam.py` (MODIFY — Pitfall 8 fix)" (exact before/after list)
    - .planning/phases/63-chamber-alerter/63-RESEARCH.md § Pitfall 8
  </read_first>
  <action>
    In `tests/test_foray_seam.py`, extend `FORAY_PACKAGES` to the full current set:
    farm_agent/tenancy, farm_agent/persistence, farm_agent/extraction, farm_agent/signal_io,
    farm_agent/confirm, farm_agent/farmos, farm_agent/capture, farm_agent/gate. Update the stale
    comment on the line above the list (it currently claims signal_io/confirm/farmos_client/capture/llm
    "are not created this phase" — false since Phases 57-62). Do NOT modify
    `test_no_chamber_imports_in_foray`, `test_seam_trips_on_violation`, or
    `test_seam_trips_on_bare_import_form` — they read FORAY_PACKAGES and are correct as-is.
  </action>
  <verify>
    <automated>cd src/farm-agent && uv run pytest tests/test_foray_seam.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `FORAY_PACKAGES` contains all 8 directories including `farm_agent/signal_io`,
      `farm_agent/farmos`, `farm_agent/capture`, `farm_agent/gate`.
    - `test_no_chamber_imports_in_foray` passes (no Foray package currently imports chamber).
    - The two negative-control tests (`test_seam_trips_on_violation`,
      `test_seam_trips_on_bare_import_form`) still pass.
  </acceptance_criteria>
  <done>The primary grep gate scans all 8 Foray packages, not 3.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build/test tooling → source tree | Static import-graph enforcement; no runtime/untrusted input. The seam gate is a Tampering-prevention control (stops future code from coupling Foray to mushy-private chamber). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-63-01 | Tampering | Foray package accidentally imports farm_agent.chamber | mitigate | Dual enforcement: corrected `.lint-imports` forbidden contract + `FORAY_PACKAGES` grep gate, both run in pytest. |
| T-63-02 | Tampering | Silent gate rot (stale source_modules hides real coupling) | mitigate | `test_import_linter_contract.py` asserts exit 0 in CI, so a future package rename that breaks the contract file is caught immediately. |
</threat_model>

<verification>
`cd src/farm-agent && uv run lint-imports --config .lint-imports` exits 0;
`uv run pytest tests/test_foray_seam.py tests/test_import_linter_contract.py -x` green.
</verification>

<success_criteria>
Both seam-enforcement mechanisms (import-linter contract + grep gate) run inside pytest and pass
against the real current package tree; the enforced direction is Foray ↛ chamber (D-00).
</success_criteria>

<output>
Create `.planning/phases/63-chamber-alerter/63-01-SUMMARY.md` when done.
</output>
