---
phase: 63-chamber-alerter
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/farm-agent/pyproject.toml
autonomous: false
requirements: [CHM-01]
must_haves:
  truths:
    - "`websockets` (WS bridge client) and `tzdata` (ZoneInfo tz database) are project dependencies."
    - "`import websockets` and `from zoneinfo import ZoneInfo; ZoneInfo('America/Montevideo')` succeed in the project venv."
  artifacts:
    - path: "src/farm-agent/pyproject.toml"
      provides: "websockets + tzdata in [project] dependencies"
      contains: "websockets"
  key_links:
    - from: "pyproject.toml"
      to: "farm_agent.chamber.ws_client (Plan 06)"
      via: "websockets import"
      pattern: "websockets"
---

<objective>
Verify the legitimacy of the two new third-party packages and add them to the project. `websockets`
is the asyncio WS client for the ROS-bridge connection (Plan 06); `tzdata` bundles the IANA tz
database so `ZoneInfo('America/Montevideo')` works inside a slim Docker image lacking system tzdata
(Plans 04/05, CHM-02).

Purpose: Wave-2 chamber modules import these; they must land in Wave 1. Both passed live slopcheck
in RESEARCH but their package identity is sourced from training knowledge (`[ASSUMED]`), so a
blocking legitimacy checkpoint precedes the install per the package-legitimacy gate.
Output: `websockets` and `tzdata` pinned in `pyproject.toml`, import-verified.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/63-chamber-alerter/63-RESEARCH.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Package legitimacy gate — websockets + tzdata</name>
  <read_first>
    - .planning/phases/63-chamber-alerter/63-RESEARCH.md § "Package Legitimacy Audit" (both `[OK]` slopcheck, both `[ASSUMED]` names)
    - .planning/phases/63-chamber-alerter/63-RESEARCH.md § Standard Stack (websockets 16.1, tzdata 2026.3)
  </read_first>
  <what-built>Nothing installed yet — this gate authorizes the install in Task 2.</what-built>
  <how-to-verify>
    1. Visit https://pypi.org/project/websockets/ — confirm it is the actively-maintained asyncio
       WebSocket library (repo github.com/python-websockets/websockets), high download count,
       current major version ~16.x.
    2. Visit https://pypi.org/project/tzdata/ — confirm it is the CPython-maintained IANA tz
       database package (repo github.com/python/tzdata).
    3. Confirm neither is a typosquat of the intended name.
  </how-to-verify>
  <acceptance_criteria>
    - Operator confirms both packages are the legitimate, intended libraries on PyPI.
  </acceptance_criteria>
  <resume-signal>Type "approved" to authorize `uv add websockets tzdata`, or name concerns.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Add and import-verify websockets + tzdata</name>
  <read_first>
    - src/farm-agent/pyproject.toml (dependency section being modified)
    - .planning/phases/63-chamber-alerter/63-RESEARCH.md § Environment Availability (versions, uv path)
  </read_first>
  <action>
    From `src/farm-agent/`, run `uv add websockets tzdata` (adds to `[project] dependencies` and
    resolves the lockfile). Do NOT pin exotic versions — accept the resolver's current-major
    releases (websockets ~16.x, tzdata ~2026.x). Then smoke-verify the imports resolve in the venv.
  </action>
  <verify>
    <automated>cd src/farm-agent && uv run python -c "import websockets; from zoneinfo import ZoneInfo; assert str(ZoneInfo('America/Montevideo'))=='America/Montevideo'; print('ok', websockets.__version__)"</automated>
  </verify>
  <acceptance_criteria>
    - `pyproject.toml` `[project] dependencies` lists `websockets` and `tzdata`.
    - `import websockets` succeeds and `ZoneInfo('America/Montevideo')` constructs without error.
  </acceptance_criteria>
  <done>Both dependencies installed and importable in the project venv.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PyPI registry → build | New third-party packages enter the dependency graph (supply-chain surface). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-63-SC | Tampering | `uv add websockets tzdata` | mitigate | Blocking-human legitimacy checkpoint (Task 1) verifies both on pypi.org before install; both are established CPython-ecosystem packages that passed live slopcheck in RESEARCH. |
</threat_model>

<verification>
`cd src/farm-agent && uv run python -c "import websockets; from zoneinfo import ZoneInfo; ZoneInfo('America/Montevideo')"` succeeds.
</verification>

<success_criteria>
Both packages are legitimacy-verified, installed, pinned, and importable.
</success_criteria>

<output>
Create `.planning/phases/63-chamber-alerter/63-02-SUMMARY.md` when done.
</output>
