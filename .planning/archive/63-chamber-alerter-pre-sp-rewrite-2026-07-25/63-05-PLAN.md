---
phase: 63-chamber-alerter
plan: 05
type: execute
wave: 2
depends_on: ["63-02", "63-03"]
files_modified:
  - src/farm-agent/farm_agent/chamber/message.py
  - src/farm-agent/farm_agent/chamber/heartbeat.py
  - src/farm-agent/tests/chamber/test_message.py
  - src/farm-agent/tests/chamber/test_heartbeat.py
autonomous: true
requirements: [CHM-02, CHM-01]
must_haves:
  truths:
    - "Every farmer-facing timestamp renders in America/Montevideo (UYT, UTC-3), never UTC or Toronto (CHM-02 / SC2)."
    - "A snapshot test pins a formatted time to the Montevideo local value and asserts it differs from the UTC rendering."
    - "fmt_num renders '?' for null/NaN and strips trailing-zero decimals, matching message.js."
    - "The heartbeat only marks a day done when the summary has at least one non-None field (defer-and-retry, Pitfall 10)."
  artifacts:
    - path: "src/farm-agent/farm_agent/chamber/message.py"
      provides: "fmt_num/hhmm/fmt_duration/fmt_relative + alert formatters, TZ via ZoneInfo(config.timezone)"
      contains: "ZoneInfo"
    - path: "src/farm-agent/farm_agent/chamber/heartbeat.py"
      provides: "heartbeat_loop asyncio daily scheduler with defer-on-empty-summary"
      contains: "heartbeat_loop"
  key_links:
    - from: "farm_agent/chamber/message.py"
      to: "zoneinfo.ZoneInfo(ChamberConfig.timezone)"
      via: "hhmm / all time renders"
      pattern: "ZoneInfo"
    - from: "farm_agent/chamber/heartbeat.py"
      to: "farm_agent/chamber/message.py"
      via: "summary formatting via fmt_num"
      pattern: "message"
---

<objective>
Port farmer-facing formatting (`message.js`) with the D-04 TZ bug fixed — all time rendering routes
through `ZoneInfo(ChamberConfig.timezone)` (default America/Montevideo), closing the Toronto-since-
Phase-13 UTC bug (CHM-02) — and port the daily heartbeat scheduler (`heartbeat.js`) with its
defer-if-summary-empty retry semantics (Pitfall 10).

Purpose: CHM-02's literal proof is a snapshot test pinning a formatted alert to Montevideo/UYT
(UTC-3). The heartbeat depends on message formatting (fmt_num), so both land together. This TZ
change is a pre-declared intentional parity delta for Phase 64.
Output: `chamber/message.py`, `chamber/heartbeat.py`, and their unit tests including the SC2 snapshot.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/63-chamber-alerter/63-CONTEXT.md
@.planning/phases/63-chamber-alerter/63-RESEARCH.md
@.planning/phases/63-chamber-alerter/63-PATTERNS.md

<interfaces>
message.js → message.py: fmt_num, hhmm, fmt_duration, fmt_relative + the per-alert-type message
builders. D-04 fix: hhmm(ts_ms, tz_name) = datetime.fromtimestamp(ts_ms/1000, tz=ZoneInfo(tz_name))
.strftime("%H:%M") — NEVER .toISOString()/timezone.utc. tz_name comes from ChamberConfig.timezone.
Node's hhmm() (message.js:50-52) did new Date(tsMs).toISOString().slice(11,16) → always UTC (the bug).
fmt_num: null/NaN → '?'; 1-decimal with trailing-zero strip; round-number formatting per
feedback_round_farmer_numbers. No em-dashes in farmer-facing output (feedback_no_em_dashes_in_artifacts).

SC2 snapshot (RESEARCH Code Examples): 2026-07-13T23:30:00Z == 20:30 in America/Montevideo; assert
hhmm(ts_ms, "America/Montevideo") == "20:30" AND != "23:30" (negative control proving the fix).

heartbeat.js → heartbeat.py: run-once-then-sleep asyncio loop (analog: capture/retention.py
retention_loop). Pitfall 10: only set last_heartbeat_day = day when summary has >=1 of rh/temp/co2
non-None; else defer and retry (Node retries ~15 min). Do NOT copy retention's fixed 86_400 sleep —
parameterize the retry interval. Fail-open try/except per tick, never crash the loop. Fires at
ChamberConfig.heartbeat_hour in the configured TZ (ZoneInfo).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Port message.js → chamber/message.py (D-04 TZ fix + fmt_num)</name>
  <read_first>
    - src/agents/alerter/src/message.js (full, 155 lines — parity target; note hhmm() line 50-52 UTC bug)
    - .planning/phases/63-chamber-alerter/63-RESEARCH.md § "D-04 TZ formatting — the exact fix" + § "D-04 snapshot test shape" + Anti-Patterns (never timezone.utc)
    - .planning/phases/63-chamber-alerter/63-PATTERNS.md § "chamber/message.py" (mask_number formatter shape, exact hhmm code)
    - src/farm-agent/farm_agent/tenancy/tenant.py (mask_number lines 195-207: pure-formatter doc convention)
  </read_first>
  <behavior>
    - test_hhmm_renders_montevideo_not_utc: hhmm(ts for 2026-07-13T23:30:00Z, "America/Montevideo")
      == "20:30" and != "23:30".
    - test_fmt_num_parity: None/NaN → "?"; 92.0 → "92"; 92.5 → "92.5"; trailing-zero strip; no em-dash.
    - A formatted pi-offline alert body ("last RH XX% @ HH:MM") renders the HH:MM in Montevideo local.
  </behavior>
  <action>
    Create `farm_agent/chamber/message.py` porting fmt_num, hhmm, fmt_duration, fmt_relative and the
    per-alert-type message builders from message.js. Implement hhmm exactly as the RESEARCH Code
    Example: `datetime.fromtimestamp(ts_ms / 1000, tz=ZoneInfo(tz_name)).strftime("%H:%M")`. Route
    EVERY farmer-facing time render through hhmm/fmt_relative with tz_name = ChamberConfig.timezone —
    never construct a UTC datetime or use `timezone.utc`. Preserve fmt_num's '?' sentinel, 1-decimal
    trailing-zero strip, and em-dash-free output. Each function gets a mask_number-style docstring
    ("Port of message.js X()", bullet spec, worked example). Create `tests/chamber/test_message.py`
    including the Montevideo snapshot (SC2) with the UTC negative control.
  </action>
  <verify>
    <automated>cd src/farm-agent && uv run pytest tests/chamber/test_message.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "ZoneInfo" farm_agent/chamber/message.py` >= 1; `grep -c "timezone.utc" farm_agent/chamber/message.py` == 0; `grep -c "toISOString\|utcnow" farm_agent/chamber/message.py` == 0.
    - `test_hhmm_renders_montevideo_not_utc` passes (== "20:30", != "23:30").
    - `uv run pytest tests/chamber/test_message.py -x` green.
  </acceptance_criteria>
  <done>All farmer-facing time renders in Montevideo local; SC2 snapshot proves the Toronto/UTC bug is fixed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Port heartbeat.js → chamber/heartbeat.py (defer-on-empty scheduler)</name>
  <read_first>
    - src/agents/alerter/src/heartbeat.js (full, 88 lines — parity target; the day-mark conditional)
    - src/farm-agent/farm_agent/capture/retention.py (full, ~60 lines: run-once-then-sleep asyncio-loop analog)
    - .planning/phases/63-chamber-alerter/63-RESEARCH.md § Pitfall 10 (defer-and-retry, never mark day done on empty summary)
    - .planning/phases/63-chamber-alerter/63-PATTERNS.md § "chamber/heartbeat.py" (retry interval parameterized, not 86_400)
    - src/farm-agent/farm_agent/chamber/message.py (fmt_num for the summary line — from Task 1)
  </read_first>
  <behavior>
    - test_heartbeat_defers_on_empty_summary: at heartbeat_hour with summary all-None → day NOT
      marked done, no send; next tick with a non-None field → sends and marks day done.
    - Firing hour is evaluated in ChamberConfig.timezone (ZoneInfo), not UTC.
    - A raised exception inside a tick is caught and logged; the loop continues.
  </behavior>
  <action>
    Create `farm_agent/chamber/heartbeat.py` with an async `heartbeat_loop(...)` following the
    retention_loop while-True/try/except/sleep shape, but with a shorter retry interval when
    deferring (parameterize per Pitfall 10, ~15 min) rather than a fixed 86_400. Compute the current
    hour/day via ZoneInfo(config.timezone). Only set `last_heartbeat_day = day` when the summary has
    at least one of rh/temp/co2 non-None; otherwise defer. Format the summary line via message.fmt_num
    (route the summary send through the shared SignalClient — injected, not constructed here; the
    actual wiring happens in Plan 08). Fail-open per tick. Create `tests/chamber/test_heartbeat.py`
    with a fake clock / injected now + fake send capturing the defer-then-fire behavior.
  </action>
  <verify>
    <automated>cd src/farm-agent && uv run pytest tests/chamber/test_heartbeat.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "86_400\|86400" farm_agent/chamber/heartbeat.py` == 0 for the defer path (retry
      interval is parameterized/shorter).
    - `grep -c "ZoneInfo" farm_agent/chamber/heartbeat.py` >= 1 (hour computed in configured TZ).
    - `test_heartbeat_defers_on_empty_summary` passes: no send + day-not-marked on empty summary.
    - `uv run pytest tests/chamber/test_heartbeat.py -x` green.
  </acceptance_criteria>
  <done>The daily heartbeat defers on empty telemetry and never emits a '?'-filled summary.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| chamber → outbound Signal | message.py output is sent to farmers; risk is Information Disclosure (leaking secrets/phone numbers into a message body) and mis-rendered time (correctness, not security). |
| WS telemetry → heartbeat summary | Summary values originate from the ROS bridge (semi-trusted internal); no untrusted-input parsing here. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-63-08 | Information Disclosure | message.py rendering a phone number/secret into a farmer message | mitigate | Reuse tenancy.mask_number for any phone-number render; message builders format only telemetry values, never ChamberConfig secrets. |
| T-63-09 | Denial of Service | exception in heartbeat tick killing the loop | mitigate | Per-tick try/except log-and-continue (retention_loop pattern); loop never dies. |
</threat_model>

<verification>
`cd src/farm-agent && uv run pytest tests/chamber/test_message.py tests/chamber/test_heartbeat.py -x` green,
including the Montevideo snapshot (SC2).
</verification>

<success_criteria>
Farmer-facing time is Montevideo-local (CHM-02 proven by snapshot); the heartbeat defers on empty
summaries and fires correctly at the configured local hour.
</success_criteria>

<output>
Create `.planning/phases/63-chamber-alerter/63-05-SUMMARY.md` when done.
</output>
