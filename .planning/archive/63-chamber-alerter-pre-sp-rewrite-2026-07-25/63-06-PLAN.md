---
phase: 63-chamber-alerter
plan: 06
type: execute
wave: 2
depends_on: ["63-02", "63-03"]
files_modified:
  - src/farm-agent/farm_agent/chamber/ws_client.py
  - src/farm-agent/tests/chamber/test_ws_client.py
autonomous: true
requirements: [CHM-01]
must_haves:
  truths:
    - "The WS client reconnects with exponential backoff doubling 1s→30s, matching bridge-client.js exactly (parity for SC1 timing)."
    - "A failed /health poll logs-and-continues (feeds fc1_last_msg_ts=None downstream), never crashing the reconnect loop."
    - "The /health poll reuses the injected shared httpx.AsyncClient, not a second one."
  artifacts:
    - path: "src/farm-agent/farm_agent/chamber/ws_client.py"
      provides: "WsClient — websockets client + /health poll + hand-rolled backoff"
      contains: "class WsClient"
  key_links:
    - from: "farm_agent/chamber/ws_client.py"
      to: "websockets connection"
      via: "async recv loop + explicit backoff"
      pattern: "websockets"
---

<objective>
Port the ROS-bridge client (`bridge-client.js`): an asyncio `websockets` client that consumes WS
telemetry messages and polls `/health` for the `fc1LastMsgTs` chamber-dark signal, with a
hand-rolled exponential backoff (1s→30s doubling) mirroring Node's exact reconnect schedule.

Purpose: The reconnect timing is parity-critical — SC1 ("pi-offline fires within the configured
timeout window") depends on the ws-disconnect branch behaving like Node. RESEARCH recommends
hand-rolling the backoff rather than trusting the library's built-in reconnect iterator, because
library-default timing may differ.
Output: `chamber/ws_client.py` and reconnect/backoff + health-poll unit tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/63-chamber-alerter/63-CONTEXT.md
@.planning/phases/63-chamber-alerter/63-RESEARCH.md
@.planning/phases/63-chamber-alerter/63-PATTERNS.md

<interfaces>
bridge-client.js hand-rolls exponential backoff (backoffMs 1000→30000 doubling, reconnectTimer) and
exposes ws-liveness state (ws_connected, ws_last_connected_ms) + emits parsed WS messages (humidity,
mode_update, overrides_update, globals_update, pi_liveness, ...). /health poll (~10s timer) does an
httpx GET mirroring Node's fetch(healthUrl) and extracts fc1LastMsgTs.

Analog: signal_io/client.py (SignalClient) — constructor-injection discipline (config: ChamberConfig,
http: httpx.AsyncClient injected, log injected, NOT constructing its own httpx client), module
docstring convention, fail-open ensure_groups_loaded try/except (lines 145-172) → apply the same
log-and-continue shape to the /health poll leg. Task lifecycle: create_task → cancel →
await-CancelledError-swallow (boot.py / receive_loop.py pattern).

OPEN QUESTION (RESEARCH Q2, A1): the exact websockets v16.x top-level async client API (connect
context-manager vs async-iterator vs .recv() loop) was NOT verified against live docs. The executor
MUST inspect the installed package's own docstrings/type stubs before finalizing the connect idiom
(e.g. `uv run python -c "import websockets; help(websockets.connect)"`). Regardless of the library's
built-in reconnect, the BACKOFF TIMING is hand-rolled to match Node's doubling schedule.

bridge_ws_url / bridge_health_url come from ChamberConfig (Plan 03).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify websockets v16.x client API, then port bridge-client.js → chamber/ws_client.py</name>
  <read_first>
    - src/agents/alerter/src/bridge-client.js (full, 101 lines — parity target; backoffMs doubling)
    - .planning/phases/63-chamber-alerter/63-RESEARCH.md § Open Question 2 + § Alternatives Considered (hand-roll backoff, not library default) + Assumptions A1
    - .planning/phases/63-chamber-alerter/63-PATTERNS.md § "chamber/ws_client.py" (constructor-injection, fail-open /health poll, docstring convention)
    - src/farm-agent/farm_agent/signal_io/client.py (constructor-injection + fail-open lines 52-90, 145-172)
    - src/farm-agent/farm_agent/boot.py (create_task/cancel/await-CancelledError lifecycle to be wired in Plan 08)
  </read_first>
  <action>
    First, inspect the installed `websockets` API surface (`uv run python -c "import websockets;
    help(websockets)"` or read its type stubs) to pin the correct v16.x connect/recv idiom before
    writing code. Then create `farm_agent/chamber/ws_client.py` with a `WsClient` class:
    constructor-injected `config: ChamberConfig`, `http: httpx.AsyncClient` (the shared client — do
    NOT construct a second), and `log`. Implement a reconnecting recv loop with a HAND-ROLLED
    exponential backoff (`backoff_ms` starting 1000, doubling to a 30000 cap, reset on successful
    connect) mirroring bridge-client.js exactly — do not rely on the library's built-in reconnect
    timing. Parse incoming WS frames into typed events (humidity, mode_update, overrides_update,
    globals_update, pi_liveness) dispatched to an injected callback. Add a `/health` poll leg (httpx
    GET on bridge_health_url) extracting fc1_last_msg_ts, wrapped in fail-open try/except (a failed
    poll logs a warning and feeds None downstream — never raises, never kills the reconnect loop).
    Expose ws_connected / ws_last_connected_ms liveness state. Module docstring cites bridge-client.js
    + the hand-rolled-backoff decision.
  </action>
  <verify>
    <automated>cd src/farm-agent && uv run pytest tests/chamber/test_ws_client.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "import websockets\|from websockets" farm_agent/chamber/ws_client.py` >= 1.
    - WsClient.__init__ takes an injected `http: httpx.AsyncClient`; `grep -c "httpx.AsyncClient()" farm_agent/chamber/ws_client.py` == 0 (no self-constructed client).
    - The /health poll path has a try/except that logs and continues on failure.
  </acceptance_criteria>
  <done>WsClient connects, reconnects with Node-parity backoff, and polls /health fail-open.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Reconnect/backoff + health-poll unit tests (fake WS + mocked httpx)</name>
  <read_first>
    - src/farm-agent/farm_agent/chamber/ws_client.py (from Task 1)
    - src/agents/alerter/src/bridge-client.js (backoff schedule to assert against)
    - src/farm-agent/tests/conftest.py (signal_http respx mock fixture pattern for httpx mocking)
  </read_first>
  <behavior>
    - test_reconnect_backoff_doubles: successive failed connects yield backoff 1s,2s,4s,...capped 30s;
      a successful connect resets backoff to 1s.
    - test_health_poll_failure_is_fail_open: a mocked /health 500/timeout logs and yields
      fc1_last_msg_ts=None without raising or stopping the loop.
    - test_ws_liveness_state: ws_connected/ws_last_connected_ms update on connect/disconnect.
  </behavior>
  <action>
    Create `tests/chamber/test_ws_client.py` using a fake/mocked WS transport (monkeypatch the connect
    coroutine to raise/succeed on schedule) and a respx-mocked httpx client for the /health leg.
    Assert the backoff doubling schedule and cap, the reset-on-connect, and the fail-open /health
    behavior. Use injected fake clock/sleep to keep the test fast (no real waiting).
  </action>
  <verify>
    <automated>cd src/farm-agent && uv run pytest tests/chamber/test_ws_client.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `test_reconnect_backoff_doubles` asserts the 1s→2s→4s→…→30s cap sequence and reset-on-connect.
    - `test_health_poll_failure_is_fail_open` asserts no exception + None downstream.
    - Tests complete in <5s (no real sleeps).
  </acceptance_criteria>
  <done>Reconnect timing and fail-open health polling are unit-verified against the Node schedule.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ROS-bridge WS/HTTP → ws_client | Inbound telemetry frames + /health JSON from the mission_control_bridge (internal, semi-trusted). Malformed frames must not crash the loop. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-63-10 | Denial of Service | malformed WS frame / bridge outage stalling the client | mitigate | Fail-open per-message + per-health-poll try/except; hand-rolled backoff bounds reconnect storms at a 30s cap. |
| T-63-11 | Spoofing | rogue WS server feeding false telemetry | accept | Bridge endpoint is a fixed internal URL over wg0/tailscale (project network trust); no auth change in scope. Detectors treat absent/None inputs as no-trigger, limiting blast radius. |
</threat_model>

<verification>
`cd src/farm-agent && uv run pytest tests/chamber/test_ws_client.py -x` green; backoff schedule matches
bridge-client.js.
</verification>

<success_criteria>
WsClient reproduces Node's reconnect/backoff timing and fail-open health polling; SC1's ws-disconnect
timing path is parity-faithful.
</success_criteria>

<output>
Create `.planning/phases/63-chamber-alerter/63-06-SUMMARY.md` when done.
</output>
