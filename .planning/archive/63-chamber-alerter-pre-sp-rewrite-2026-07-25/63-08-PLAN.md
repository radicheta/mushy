---
phase: 63-chamber-alerter
plan: 08
type: execute
wave: 4
depends_on: ["63-01", "63-04", "63-05", "63-06", "63-07"]
files_modified:
  - src/farm-agent/farm_agent/chamber/service.py
  - src/farm-agent/farm_agent/boot.py
  - src/farm-agent/tests/chamber/test_service_wiring.py
autonomous: true
requirements: [CHM-01]
must_haves:
  truths:
    - "boot.py constructs exactly ONE SignalClient and ONE ReceiveLoop; chamber reuses both (D-05, A3 guard)."
    - "The composite dispatcher routes snooze/mute-shaped text to the chamber snooze handler and falls through to the capture pipeline for everything else."
    - "boot.py always wires get_max_sends_per_hour = ChamberConfig.max_sends_per_hour into the shared SignalClient."
    - "Inducing a bridge disconnect fires a pi-offline alert within the configured timeout window (SC1)."
    - "The chamber heartbeat + ws_client reconnect tasks are create_task'd at boot and cancelled on shutdown; FSM state is in-memory (D-06)."
  artifacts:
    - path: "src/farm-agent/farm_agent/chamber/service.py"
      provides: "ChamberService composing ws_client + state + rules + message + heartbeat + snooze; apply_snooze; start/stop"
      contains: "class ChamberService"
    - path: "src/farm-agent/farm_agent/boot.py"
      provides: "chamber wiring: ChamberConfig, WsClient, heartbeat task, composite dispatch, rate-cap hook"
      contains: "_composite_dispatch"
  key_links:
    - from: "farm_agent/boot.py"
      to: "farm_agent.signal_io.receive_loop.ReceiveLoop"
      via: "dispatch=_composite_dispatch (single loop, D-05)"
      pattern: "_composite_dispatch"
    - from: "boot.py SignalClient"
      to: "ChamberConfig.max_sends_per_hour"
      via: "get_max_sends_per_hour hook"
      pattern: "get_max_sends_per_hour"
    - from: "chamber/service.py"
      to: "chamber.ws_client / state / message"
      via: "event dispatch → detectors → FSM → format → send"
      pattern: "ws_client|state|message"
---

<objective>
Compose the chamber subsystem (`service.py`) and wire it into `boot.py`: the WS client feeds events
into the FSM (state.py) which calls detectors (rules.py) and, on a firing transition, formats
(message.py) and sends via the SHARED SignalClient; the heartbeat task runs; and inbound snooze/mute
commands are routed via a composite dispatcher on the single shared ReceiveLoop (D-05). This is the
integration seam that makes CHM-01 real end-to-end.

Purpose: D-05 mandates exactly one SignalClient and one ReceiveLoop — chamber's snooze handling is
composed INTO the existing dispatch, never a second loop. boot.py also finally wires the rate-cap
hook (Pitfall 9) from ChamberConfig. SC1 (bridge disconnect → pi-offline alert within timeout) is
proven here.
Output: `chamber/service.py`, the boot.py wiring edits, and `test_service_wiring.py` (SC1 + D-05).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/63-chamber-alerter/63-CONTEXT.md
@.planning/phases/63-chamber-alerter/63-RESEARCH.md
@.planning/phases/63-chamber-alerter/63-PATTERNS.md

<interfaces>
boot.py current wiring (PATTERNS § boot.py:75-118): constructs `http = httpx.AsyncClient()`,
`signal_client = SignalClient(config=config, http=http)` (NO hook today), builds the capture
`pipeline`, then `receive_loop = ReceiveLoop(signal_client, dispatch=pipeline["handle"],
config=config)` + `await receive_loop.start()`, and `asyncio.create_task(retention_loop(...))` /
`confirm_watchdog_loop(...)`. Shutdown (boot.py:134-151) cancels each task with
`.cancel()` / `await` / `except asyncio.CancelledError: pass`. T-58-03-05: exactly ONE ReceiveLoop.

D-05 composite dispatch (RESEARCH Pattern 5 / PATTERNS boot.py): REPLACE `dispatch=pipeline["handle"]`
with an async `_composite_dispatch(env)` that uses signal_io.router to extract text, calls
chamber.snooze.parse_snooze_command; if ok → chamber_service.apply_snooze(parsed) (+ optional ack
send) and return; else → await pipeline["handle"](env). The whitelist gate stays in ReceiveLoop.tick
UPSTREAM (V4) — the composite dispatcher must not add a second ungated path.

Pitfall 9 boot wiring: construct `chamber_config = ChamberConfig.load(env, tenant_config=config)` and
pass `get_max_sends_per_hour=lambda: chamber_config.max_sends_per_hour` to the SignalClient
constructor (client.py already has the hardcoded fallback from Plan 03).

service.py: ChamberService composes ws_client (Plan 06), state FSM (Plan 07), rules (Plan 04),
message (Plan 05), heartbeat (Plan 05), snooze (Plan 04). On a ws event → run the FSM tick →
on a firing/transition that should send → format via message → await signal_client.send(body).
apply_snooze(parsed) mutates in-memory FSM. start()/stop() mirror receive_loop.py lifecycle. FSM
state in-memory only (D-06 — no persistence).

Boot log discipline (T-56-06-01): lifecycle-only log lines, never ChamberConfig fields.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build chamber/service.py composer</name>
  <read_first>
    - src/farm-agent/farm_agent/signal_io/receive_loop.py (full — start/stop lifecycle to mirror)
    - src/farm-agent/farm_agent/chamber/{ws_client,state,rules,message,heartbeat,snooze}.py (Plans 04-07 outputs)
    - .planning/phases/63-chamber-alerter/63-PATTERNS.md § "chamber/service.py" + § Shared Patterns (async task lifecycle)
    - src/agents/alerter/src/state.js (how firing transitions map to a send in Node)
  </read_first>
  <action>
    Create `farm_agent/chamber/service.py` with a `ChamberService` class: constructor-injected
    `config: ChamberConfig`, shared `signal_client`, `http`, and `log`. It constructs/holds the
    in-memory FSM state (state.initial_state), the WsClient (Plan 06, reusing the injected http), and
    the heartbeat parameters. On each WS event, run the state.py handler for that event, then for any
    entry whose transition should emit, format the body via message.py (tz = config.timezone) and
    `await signal_client.send(body)`. Provide `apply_snooze(parsed)` mutating the in-memory FSM
    (SNOOZED entry), and `start()`/`stop()` that create/cancel the ws reconnect loop + heartbeat task,
    mirroring receive_loop.py's cancel/await/except-CancelledError shape. Keep the per-alert-type
    iteration sequential (no asyncio.gather). No persistence (D-06). Module docstring cites the Node
    files composed.
  </action>
  <verify>
    <automated>cd src/farm-agent && uv run python -c "from farm_agent.chamber import service; print('import ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "class ChamberService" farm_agent/chamber/service.py` == 1 with `apply_snooze`, `start`, `stop` methods.
    - ChamberService reuses an injected SignalClient (no `SignalClient(` construction inside service.py) and injected http (no `httpx.AsyncClient()`).
    - `grep -Ec "deepcopy|persist|pool\\.execute|INSERT" farm_agent/chamber/service.py` == 0 for FSM persistence (D-06 in-memory).
  </acceptance_criteria>
  <done>ChamberService composes the full alert pipeline over the shared Signal client with in-memory state.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire chamber into boot.py + composite dispatch + rate-cap hook, with SC1/D-05 tests</name>
  <read_first>
    - src/farm-agent/farm_agent/boot.py (full — construction 75-118, shutdown 134-151)
    - .planning/phases/63-chamber-alerter/63-RESEARCH.md § Pattern 5 (composite dispatch) + Pitfall 9 (boot hook wiring) + § Validation Architecture (SC1 integration test row)
    - .planning/phases/63-chamber-alerter/63-PATTERNS.md § "boot.py (MODIFY)" (subsystem construction + shutdown symmetry)
    - src/farm-agent/farm_agent/signal_io/router.py (extract_source/classify_envelope for the dispatcher)
    - src/farm-agent/tests/test_boot.py (existing boot test conventions)
  </read_first>
  <behavior>
    - test_composite_dispatch_routing: a snooze/mute-shaped envelope routes to apply_snooze (FSM
      mutates, capture pipeline NOT called); a non-command envelope falls through to pipeline["handle"].
    - test_exactly_one_receive_loop: boot constructs exactly one ReceiveLoop and one SignalClient.
    - test_bridge_disconnect_fires_pi_alert (SC1): a simulated bridge disconnect (ws_last_connected_ms
      aged past the threshold) drives the FSM to fire a pi-offline alert that reaches the injected
      fake SignalClient.send within the configured timeout window.
    - test_ratecap_hook_wired: the SignalClient is constructed with get_max_sends_per_hour returning
      ChamberConfig.max_sends_per_hour.
  </behavior>
  <action>
    Edit `boot.py`: after loading `config` (TenantConfig), construct `chamber_config =
    ChamberConfig.load(env, tenant_config=config)`. Pass `get_max_sends_per_hour=lambda:
    chamber_config.max_sends_per_hour` to the `SignalClient(...)` constructor. Construct the
    `ChamberService` (reusing http + signal_client). Define an async `_composite_dispatch(env)` per
    Pattern 5 (router extract → parse_snooze_command → apply_snooze+return on ok, else await
    pipeline["handle"](env)) and pass it as `dispatch=` to the single `ReceiveLoop` (do NOT create a
    second loop). `asyncio.create_task` the chamber service start (ws reconnect + heartbeat) and add
    matching `.cancel()`/`await`/`except asyncio.CancelledError` blocks in the shutdown sequence. Add
    a lifecycle-only boot log line (no ChamberConfig fields). Create `tests/chamber/test_service_wiring.py`
    covering the behaviors above (inject a fake SignalClient capturing sends; drive events directly
    for the SC1 timing assertion rather than requiring a live bridge).
  </action>
  <verify>
    <automated>cd src/farm-agent && uv run pytest tests/chamber/test_service_wiring.py tests/test_boot.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "_composite_dispatch" farm_agent/boot.py` >= 1 and `grep -c "ReceiveLoop(" farm_agent/boot.py` == 1 (single loop).
    - `grep -c "get_max_sends_per_hour" farm_agent/boot.py` >= 1 (hook wired to chamber_config).
    - `test_bridge_disconnect_fires_pi_alert` proves a pi-offline alert reaches the fake SignalClient within the configured window (SC1).
    - `test_composite_dispatch_routing` proves snooze→chamber, else→pipeline.
    - `uv run pytest tests/chamber/test_service_wiring.py tests/test_boot.py -x` green.
  </acceptance_criteria>
  <done>Chamber runs in the single-process asyncio boot with one Signal client / one receive loop, composite snooze routing, rate-cap hook, and SC1 proven.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| inbound Signal envelope → composite dispatcher | UNTRUSTED text; the whitelist gate runs in ReceiveLoop.tick UPSTREAM. The composite dispatcher must not add a second, ungated ingestion path for snooze commands (V4). |
| chamber → shared SignalClient | Outbound alert volume bounded by the rate cap (now sourced from ChamberConfig via the boot hook). |
| ROS-bridge WS → ChamberService | Telemetry drives real farmer alerts; a stalled/false bridge must not spuriously fire or silently miss. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-63-14 | Tampering | second SignalClient/ReceiveLoop double-polling signal-cli (dropped/duplicated receipts) | mitigate | D-05 composite-dispatch wiring; test_exactly_one_receive_loop asserts exactly one of each in boot. |
| T-63-15 | Elevation of Privilege | snooze command bypassing the sender whitelist via the composite path | mitigate | Composite dispatcher reuses ReceiveLoop's upstream whitelist gate; adds no ungated path (verified by wiring test). |
| T-63-16 | Denial of Service | outbound alert flood (rate cap unbounded after D-03 move) | mitigate | boot wires get_max_sends_per_hour=ChamberConfig.max_sends_per_hour; SignalClient hardcoded fallback (Plan 03) guarantees a cap regardless. |
| T-63-SC | Tampering | npm/pip installs | mitigate | No new installs in this plan; websockets/tzdata legitimacy gated in Plan 02. |
</threat_model>

<verification>
`cd src/farm-agent && uv run pytest tests/ && uv run lint-imports` — full suite green including the
new chamber tests and the activated seam contract; SC1 + D-05 wiring tests pass.
</verification>

<success_criteria>
Chamber is composed into the running process with one shared Signal client and receive loop, composite
snooze routing, the rate-cap hook wired, in-memory FSM (D-06), and a bridge-disconnect firing a
pi-offline alert within the configured window (SC1).
</success_criteria>

<output>
Create `.planning/phases/63-chamber-alerter/63-08-SUMMARY.md` when done.
</output>
