---
phase: 63-chamber-alerter
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/farm-agent/farm_agent/signal_io/client.py
  - src/farm-agent/tests/test_signal_ratecap.py
  - src/farm-agent/farm_agent/tenancy/tenant.py
  - src/farm-agent/tests/test_tenancy.py
  - src/farm-agent/farm_agent/chamber/__init__.py
  - src/farm-agent/farm_agent/chamber/config.py
  - src/farm-agent/tests/chamber/__init__.py
  - src/farm-agent/tests/chamber/conftest.py
  - src/farm-agent/tests/chamber/test_config.py
autonomous: true
requirements: [CHM-01, CHM-02]
must_haves:
  truths:
    - "The 7 alerter knobs (rh_target, rh_band, pi_offline_min, sensor_offline_min, heartbeat_hour, max_sends_per_hour, timezone) no longer live on TenantConfig."
    - "ChamberConfig owns the full alerter knob set and reads secrets/identity from an injected TenantConfig, never re-reading os.environ for them (D-02)."
    - "ChamberConfig.timezone defaults to America/Montevideo (D-04), overridable by TZ env."
    - "SignalClient rate cap works with a hardcoded default (20) when no hook is supplied — no AttributeError after the field move (Pitfall 9)."
  artifacts:
    - path: "src/farm-agent/farm_agent/chamber/config.py"
      provides: "ChamberConfig frozen dataclass + load(env, tenant_config)"
      contains: "America/Montevideo"
    - path: "src/farm-agent/tests/chamber/conftest.py"
      provides: "ChamberConfig test factory fixture"
  key_links:
    - from: "farm_agent/signal_io/client.py"
      to: "_DEFAULT_MAX_SENDS_PER_HOUR"
      via: "_current_cap fallback constant"
      pattern: "_DEFAULT_MAX_SENDS_PER_HOUR"
    - from: "farm_agent/chamber/config.py"
      to: "farm_agent.tenancy.TenantConfig"
      via: "composition-by-injection"
      pattern: "TenantConfig"
---

<objective>
Relocate the 7 alerter-only knobs out of the Foray `TenantConfig` into a new chamber-local
`ChamberConfig` (D-02/D-03), and decouple `SignalClient`'s rate-cap fallback from the field being
removed (Pitfall 9). `ChamberConfig` owns the FULL alerter knob set (the 7 relocated fields plus
the not-yet-ported ones) with `timezone` defaulting to `America/Montevideo` (D-04).

Purpose: Keeps the extractable Foray island free of mushy-private alerter concerns and unblocks the
D-04 TZ fix. This is an atomic move — removing the field from TenantConfig and fixing the one
orphaned Foray reader (`signal_io.client._current_cap`) must ship together or `test_signal_ratecap`
raises AttributeError.
Output: `chamber/config.py` (ChamberConfig), a slimmed TenantConfig, a decoupled SignalClient, and
chamber test scaffolding (conftest + test_config).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/63-chamber-alerter/63-CONTEXT.md
@.planning/phases/63-chamber-alerter/63-RESEARCH.md
@.planning/phases/63-chamber-alerter/63-PATTERNS.md

<interfaces>
Verified: the ONLY non-chamber read of the 7 fields is `signal_io/client.py:133`
(`return self._config.max_sends_per_hour` inside `_current_cap()`). No other Foray package reads
any of the 7. boot.py does not read them.

TenantConfig field lines to remove (tenant.py): 263-267 (rh_target, rh_band, pi_offline_min,
sensor_offline_min, heartbeat_hour), 271 (max_sends_per_hour — NOTE receive_poll_sec on the same
group STAYS), 288 (timezone); parse lines 367-371, 375 (max_sends), 392 (timezone default
"America/Toronto"); constructor args 417-421, 423, 432. Leave receive_poll_sec, draft_*,
commit_watchdog_*, fidelity_csv_path, log_level untouched.

ChamberConfig knob set (D-03): 7 relocated (rh_target, rh_band, pi_offline_min, sensor_offline_min,
heartbeat_hour, max_sends_per_hour, timezone) + not-yet-ported (oob_n, oob_window_min, cooldown_min,
critical_cooldown_min, humidifier_stuck_min, sht30_enabled, scd41_enabled, sensor_flap_min_sec,
mode_stale_min, mode_boot_grace_ms, bridge_ws_url, bridge_health_url, dashboard_url, receive_poll_sec).
Node env-var names + defaults are in src/agents/alerter/src/config.js.

Composition (D-02): ChamberConfig reads signal_sender, signal_recipient, signal_api_url, tenant_id,
timescale creds from an injected TenantConfig — NOT via _must_env again. Mirror
signal_io/client.py:65-74 injection.

test_signal_ratecap: `_make_client(max_sends=2, get_max_hook=None)` at line 19; the no-hook test at
line 114 currently asserts fallback to `config.max_sends_per_hour` — must change to assert the new
hardcoded default of 20 (or supply the hook explicitly).
test_tenancy: tests at 265 (test_int_field_from_env), 276-284 (test_float_field_from_env asserts
cfg.rh_target==92.5), 287 (test_int_default_used_when_absent) reference the moved fields.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Decouple SignalClient rate cap + remove 7 fields from TenantConfig</name>
  <read_first>
    - src/farm-agent/farm_agent/signal_io/client.py (lines 120-133 `_current_cap`; Pitfall 9)
    - src/farm-agent/farm_agent/tenancy/tenant.py (field decls 263-288, parse 367-392, ctor 417-432)
    - src/farm-agent/tests/test_signal_ratecap.py (full; `_make_client`, no-hook fallback test)
    - src/farm-agent/tests/test_tenancy.py (the 3 field tests at 265/276/287)
    - src/agents/alerter/src/config.js (line ~175: ALERT_MAX_SENDS_PER_HOUR default 20 — the A4 constant)
    - .planning/phases/63-chamber-alerter/63-PATTERNS.md § "signal_io/client.py (MODIFY)" + "tenancy/tenant.py (MODIFY)"
  </read_first>
  <behavior>
    - Test: SignalClient constructed with no get_max_sends_per_hour hook enforces cap = 20 (the new
      hardcoded default), never raising AttributeError.
    - Test: SignalClient with a hook returning a finite number honours the hook; a hook that raises
      falls back to 20.
    - Test: TenantConfig no longer exposes rh_target / max_sends_per_hour / timezone (getattr fails
      or the fields are absent), while receive_poll_sec and other retained fields still load.
  </behavior>
  <action>
    In `signal_io/client.py`, add a module-level constant `_DEFAULT_MAX_SENDS_PER_HOUR = 20`
    (cite config.js:175 / A4) and change `_current_cap()`'s final fallback line from
    `return self._config.max_sends_per_hour` to `return _DEFAULT_MAX_SENDS_PER_HOUR`. The
    dynamic-hook branch is unchanged. In `tenancy/tenant.py`, remove the 7 field declarations, their
    parse lines, and their constructor arguments per the interface line map (surgical removal only).
    Update `tests/test_signal_ratecap.py`: the no-hook fallback test asserts cap 20; adjust
    `_make_client` if it seeds `ALERT_MAX_SENDS_PER_HOUR` expecting the old plumbing. Update
    `tests/test_tenancy.py`: retarget or remove the 3 tests that assert the moved fields on
    TenantConfig (they will move to test_config.py in Task 2 for ChamberConfig).
  </action>
  <verify>
    <automated>cd src/farm-agent && uv run pytest tests/test_signal_ratecap.py tests/test_tenancy.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "_DEFAULT_MAX_SENDS_PER_HOUR" farm_agent/signal_io/client.py` shows the constant and
      the `_current_cap` fallback uses it; `grep -c "self._config.max_sends_per_hour" farm_agent/signal_io/client.py` == 0.
    - `grep -Ec "rh_target|max_sends_per_hour|timezone" farm_agent/tenancy/tenant.py` (excluding
      comments) == 0 for the removed fields.
    - `uv run pytest tests/test_signal_ratecap.py tests/test_tenancy.py -x` green.
  </acceptance_criteria>
  <done>The 7 fields are gone from TenantConfig; SignalClient rate cap is self-sufficient.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create ChamberConfig + chamber test scaffolding</name>
  <read_first>
    - src/farm-agent/farm_agent/tenancy/tenant.py (frozen-dataclass + _parse_int_env/_parse_float_env/_must_env pattern, load() shape)
    - src/agents/alerter/src/config.js (full; the Tier A/B/C/D knob classification + env names + defaults = the spec)
    - src/farm-agent/tests/conftest.py (TEST_ENV pattern to mirror for chamber conftest)
    - .planning/phases/63-chamber-alerter/63-PATTERNS.md § "chamber/config.py (config)" (composition, D-04 default flip)
    - .planning/phases/63-chamber-alerter/63-RESEARCH.md § Open Question 3 (env-only, no tenant-YAML layer)
  </read_first>
  <behavior>
    - Test: ChamberConfig.load(env={}, tenant_config=<factory>) yields defaults matching config.js
      (rh_target 90.0, rh_band 3.0, pi_offline_min 5, sensor_offline_min 5, heartbeat_hour 8,
      max_sends_per_hour 20, oob_n/oob_window_min/cooldown_min/etc. per config.js).
    - Test: ChamberConfig.timezone defaults to "America/Montevideo" with no TZ env (D-04); TZ env
      overrides it.
    - Test: ChamberConfig copies signal_sender/signal_recipient/signal_api_url/tenant_id from the
      injected TenantConfig and does NOT re-read them from env (D-02).
  </behavior>
  <action>
    Create `farm_agent/chamber/__init__.py` (empty package marker) and `farm_agent/chamber/config.py`:
    a `@dataclass(frozen=True) ChamberConfig` with a module docstring citing `config.js` as the port
    target. Copy `_parse_int_env`/`_parse_float_env`/`_parse_bool_env` env helpers (verbatim shape
    from tenant.py; Claude's Discretion whether to share via a `_env.py`). Declare the full knob set
    (7 relocated + not-yet-ported per the interface list) with defaults from config.js.
    `timezone = env.get("TZ") or "America/Montevideo"` (the D-04 flip — do NOT copy Toronto). Provide
    `load(env, tenant_config)` taking an already-loaded TenantConfig and copying secrets/identity
    fields off it (composition, mirror client.py:65-74). Env-only, no tenant-YAML layer. Create
    `tests/chamber/__init__.py`, `tests/chamber/conftest.py` (a `chamber_config` factory fixture
    mirroring conftest.TEST_ENV + a fake TenantConfig), and `tests/chamber/test_config.py` covering
    the behaviors above (including the 3 field-default tests moved from test_tenancy).
  </action>
  <verify>
    <automated>cd src/farm-agent && uv run pytest tests/chamber/test_config.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "America/Montevideo" farm_agent/chamber/config.py` >= 1 and `grep -c "America/Toronto" farm_agent/chamber/config.py` == 0.
    - ChamberConfig exposes all 7 relocated fields plus oob_n/oob_window_min/cooldown_min/
      critical_cooldown_min/humidifier_stuck_min/sht30_enabled/scd41_enabled/sensor_flap_min_sec/
      mode_stale_min/mode_boot_grace_ms/bridge_ws_url/bridge_health_url/dashboard_url/receive_poll_sec.
    - `uv run pytest tests/chamber/test_config.py -x` green; `tests/chamber/conftest.py` exposes a
      reusable ChamberConfig factory.
  </acceptance_criteria>
  <done>ChamberConfig owns the full alerter knob set with Montevideo default; chamber test tree bootstrapped.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| env/compose → ChamberConfig | Config values enter via os.environ (trusted operator input). Secrets flow from TenantConfig, never re-read or logged. |
| ChamberConfig → SignalClient rate cap | The max_sends_per_hour value bounds outbound send volume (Denial-of-Service / rate-abuse control). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-63-03 | Denial of Service | SignalClient outbound rate cap after field move | mitigate | Hardcoded `_DEFAULT_MAX_SENDS_PER_HOUR=20` guarantees a cap even with no hook; boot wires the ChamberConfig value in Plan 08. No path leaves the cap unbounded. |
| T-63-04 | Information Disclosure | ChamberConfig secrets (signal_sender, timescale creds) | accept/mitigate | Secrets injected from TenantConfig, never re-read from env in chamber; ChamberConfig object is never logged wholesale (mirror boot T-56-06-01). |
</threat_model>

<verification>
`cd src/farm-agent && uv run pytest tests/test_signal_ratecap.py tests/test_tenancy.py tests/chamber/test_config.py -x` green.
</verification>

<success_criteria>
The 7 alerter knobs live on ChamberConfig (Montevideo TZ default); TenantConfig and SignalClient are
decoupled from them with zero regressions.
</success_criteria>

<output>
Create `.planning/phases/63-chamber-alerter/63-03-SUMMARY.md` when done.
</output>
