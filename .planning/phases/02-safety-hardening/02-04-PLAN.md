---
phase: 02-safety-hardening
plan: 04
type: execute
wave: 2
depends_on:
  - 02-02
files_modified:
  - src/chambers/fc-core/fc_core/fc_controller.py
  - src/chambers/fc-core/fc_core/test/test_controller.py
  - src/chambers/fc-core/config/fc_config.yaml
autonomous: true
requirements:
  - ACTR-02
  - TEST-01
must_haves:
  truths:
    - "Humidifier GPIO pin is configurable from fc_config.yaml, not hardcoded"
    - "test_humidity_control asserts actuator state (True/False), not pin number"
    - "Changing humidifier_pin in config changes the GPIO pin used (in non-sim mode)"
  artifacts:
    - path: "src/chambers/fc-core/fc_core/fc_controller.py"
      provides: "Configurable humidifier_pin from params"
      contains: "humidifier_pin"
    - path: "src/chambers/fc-core/fc_core/test/test_controller.py"
      provides: "Fixed test assertions for actuator state"
      contains: "node.humidifier_state"
    - path: "src/chambers/fc-core/config/fc_config.yaml"
      provides: "humidifier_pin param"
      contains: "humidifier_pin: 17"
  key_links:
    - from: "src/chambers/fc-core/config/fc_config.yaml"
      to: "src/chambers/fc-core/fc_core/fc_controller.py"
      via: "humidifier_pin param declaration and get_parameter"
      pattern: "humidifier_pin"
---

<objective>
Make humidifier GPIO pin configurable from fc_config.yaml and fix broken test assertions in test_controller.py.

Purpose: ACTR-02 requires the humidifier pin to be configurable (currently hardcoded to 17 at line 49 of fc_controller.py). TEST-01 requires test_humidity_control to assert actuator state (on/off boolean), not the GPIO pin number (currently lines 66 and 73 assert `node.humidifier_pin == 1` and `node.humidifier_pin == 0`, which tests the pin number, not the state).

Output: Configurable humidifier_pin in config and controller, fixed test assertions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-safety-hardening/02-CONTEXT.md
@src/chambers/fc-core/fc_core/fc_controller.py
@src/chambers/fc-core/fc_core/test/test_controller.py
@src/chambers/fc-core/config/fc_config.yaml

@.planning/phases/02-safety-hardening/02-02-SUMMARY.md
</context>

<interfaces>
<!-- Current hardcoded pin in fc_controller.py (line 49) -->
From src/chambers/fc-core/fc_core/fc_controller.py:
```python
# Humidifier control (GPIO)
self.humidifier_pin = 17  # GPIO17
GPIO.setup(self.humidifier_pin, GPIO.OUT)
```

<!-- Existing pattern for reading pins from params (light_pin, line 54) -->
```python
self.light_pin = self.get_parameter('light_pin').value
```

<!-- Current broken test assertions (lines 66, 73) -->
From src/chambers/fc-core/fc_core/test/test_controller.py:
```python
# Humidifier should be ON
assert node.humidifier_pin == 1    # BUG: tests pin number, not state

# Humidifier should be OFF
assert node.humidifier_pin == 0    # BUG: tests pin number, not state
```

<!-- Correct attribute to assert (exists in simulation mode) -->
```python
self.humidifier_state = False  # set in __init__ sim branch
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Make humidifier_pin configurable and fix test assertions</name>
  <files>src/chambers/fc-core/fc_core/fc_controller.py, src/chambers/fc-core/fc_core/test/test_controller.py, src/chambers/fc-core/config/fc_config.yaml</files>
  <read_first>
    - src/chambers/fc-core/fc_core/fc_controller.py
    - src/chambers/fc-core/fc_core/test/test_controller.py
    - src/chambers/fc-core/config/fc_config.yaml
  </read_first>
  <action>
Per D-06, D-10 from CONTEXT.md:

**Step 1 — Add humidifier_pin to fc_config.yaml (D-06):**

Add `humidifier_pin: 17` to the config file, in the section near `light_pin`. After 02-02's cleanup the section header is "Sensor config" — add humidifier_pin under a new "Actuator pins" comment or alongside light_pin. The result should include:
```yaml
# Actuator pins
humidifier_pin: 17  # GPIO17 for humidifier SSR
light_pin: 18  # GPIO18 for light control
```

Note: `light_pin` was previously under "Sensor pins" (renamed to "Sensor config" by 02-02). Move it to "Actuator pins" where it logically belongs. Keep `sht30_i2c_address` under the "Sensor config" heading.

**Step 2 — Make fc_controller.py read humidifier_pin from params (D-06):**

In the `declare_parameters` block (starting line 13), add `('humidifier_pin', 17),` to the parameters list. Place it near the existing `('light_pin', 18)` entry.

In the non-simulation branch (around line 49), replace the hardcoded line:
```python
self.humidifier_pin = 17  # GPIO17
```
with:
```python
self.humidifier_pin = self.get_parameter('humidifier_pin').value
```
This follows the exact same pattern already used for `light_pin` at line 54.

**Step 3 — Fix test assertions (D-10):**

In test_controller.py, function `test_humidity_control`:

Replace line 66:
```python
assert node.humidifier_pin == 1
```
with:
```python
assert node.humidifier_state == True
```

Replace line 73 (after the "above target" control loop run):
```python
assert node.humidifier_pin == 0
```
with:
```python
assert node.humidifier_state == False
```

These assert the boolean actuator state (True = ON, False = OFF) instead of the GPIO pin number. The `humidifier_state` attribute exists in simulation mode (set in `__init__` at line 62) and is toggled by `set_humidifier()` at line 120.

**Step 4 — Run tests:**
Run `python3 -m pytest src/chambers/fc-core/fc_core/test/test_controller.py::test_humidity_control -x -v` to confirm the fixed assertions pass.
  </action>
  <verify>
    <automated>cd /mnt/slime-kingdom/opt/mushy && python3 -m pytest src/chambers/fc-core/fc_core/test/test_controller.py::test_humidity_control -x -v</automated>
  </verify>
  <acceptance_criteria>
    - fc_config.yaml contains `humidifier_pin: 17`
    - fc_controller.py declare_parameters block contains `('humidifier_pin', 17)`
    - fc_controller.py non-sim branch contains `self.humidifier_pin = self.get_parameter('humidifier_pin').value`
    - fc_controller.py contains zero occurrences of the hardcoded pattern `self.humidifier_pin = 17`
    - test_controller.py contains `assert node.humidifier_state == True`
    - test_controller.py contains `assert node.humidifier_state == False`
    - test_controller.py contains zero occurrences of `assert node.humidifier_pin == 1` or `assert node.humidifier_pin == 0`
    - test_humidity_control test passes (exit code 0)
  </acceptance_criteria>
  <done>humidifier_pin is configurable from fc_config.yaml (ACTR-02 satisfied). Test assertions check actuator state boolean, not pin number (TEST-01 satisfied).</done>
</task>

</tasks>

<verification>
- `grep "humidifier_pin: 17" src/chambers/fc-core/config/fc_config.yaml` returns a match
- `grep "get_parameter('humidifier_pin')" src/chambers/fc-core/fc_core/fc_controller.py` returns a match
- `grep "humidifier_state ==" src/chambers/fc-core/fc_core/test/test_controller.py` returns two matches (True and False)
- `grep "humidifier_pin ==" src/chambers/fc-core/fc_core/test/test_controller.py` returns no matches
- `python3 -m pytest src/chambers/fc-core/fc_core/test/test_controller.py::test_humidity_control -x -v` passes
</verification>

<success_criteria>
Humidifier pin reads from config param instead of hardcoded value. Default is still 17 (no behavior change). Tests assert actuator on/off state, not GPIO pin number. test_humidity_control passes.
</success_criteria>

<output>
After completion, create `.planning/phases/02-safety-hardening/02-04-SUMMARY.md`
</output>
