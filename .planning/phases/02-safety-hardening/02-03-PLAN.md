---
phase: 02-safety-hardening
plan: 03
type: execute
wave: 3
depends_on:
  - 02-04
files_modified:
  - src/chambers/fc-core/fc_core/fc_controller.py
autonomous: true
requirements:
  - SENS-05
must_haves:
  truths:
    - "Spike readings are filtered before reaching control logic"
    - "Rolling median uses 5-sample window as specified"
    - "Raw sensor values still published on fc/humidity (sensors publish truth)"
    - "Control loop acts on median-filtered humidity, not raw"
  artifacts:
    - path: "src/chambers/fc-core/fc_core/fc_controller.py"
      provides: "Rolling median spike rejection in humidity callback"
      contains: "_humidity_buffer"
  key_links:
    - from: "src/chambers/fc-core/fc_core/fc_controller.py"
      to: "self.current_humidity"
      via: "median of _humidity_buffer replaces raw assignment"
      pattern: "_humidity_buffer"
---

<objective>
Add rolling median spike rejection to the humidity reading pipeline in fc_controller.py.

Purpose: SENS-05 requires filtering outlier readings before they reach the control loop. Per D-01, rolling median over 5 samples is immune to single-point spikes (unlike rolling average). Per D-02, the filter lives in the controller (receive side), not the sensor node — sensors publish raw truth, controller decides what to act on.

Output: fc_controller.py with a 5-sample rolling median buffer in humidity_callback that feeds the control loop.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-safety-hardening/02-CONTEXT.md
@src/chambers/fc-core/fc_core/fc_controller.py
</context>

<interfaces>
<!-- Current humidity_callback in fc_controller.py (line 93-94) -->
From src/chambers/fc-core/fc_core/fc_controller.py:
```python
def humidity_callback(self, msg):
    self.current_humidity = msg.relative_humidity
```

<!-- Control loop reads self.current_humidity (line 144, 160-163) -->
```python
def control_loop(self):
    if self.current_temp is None or self.current_humidity is None:
        return
    # ...
    if self.current_humidity < (self.get_parameter('target_humidity').value - ...):
        self.set_humidifier(True)
    elif self.current_humidity > (self.get_parameter('target_humidity').value + ...):
        self.set_humidifier(False)
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add rolling median filter to humidity_callback</name>
  <files>src/chambers/fc-core/fc_core/fc_controller.py, src/chambers/fc-core/fc_core/test/test_controller.py</files>
  <read_first>
    - src/chambers/fc-core/fc_core/fc_controller.py
    - src/chambers/fc-core/fc_core/test/test_controller.py
  </read_first>
  <behavior>
    - Test 1: After receiving 5 humidity messages [0.80, 0.82, 0.81, 0.99, 0.83], current_humidity should be 0.82 (median), not 0.99 (the spike)
    - Test 2: After receiving fewer than 5 messages (e.g., 3 messages [0.80, 0.82, 0.81]), current_humidity should be 0.81 (median of available samples)
    - Test 3: After receiving 6+ messages, buffer should only keep last 5 (FIFO), and median computed from those 5
  </behavior>
  <action>
Per D-01, D-02, D-03 from CONTEXT.md:

**Step 1 — Add test cases to test_controller.py:**

Add a new test function `test_humidity_spike_rejection` that:
1. Creates a FruitingChamberController node (runs in simulation mode by default)
2. Sends 5 RelativeHumidity messages with values [0.80, 0.82, 0.81, 0.99, 0.83] via `node.humidity_callback(msg)`
3. Asserts `node.current_humidity == pytest.approx(0.82)` (median of sorted [0.80, 0.81, 0.82, 0.83, 0.99])

Add a second test `test_humidity_median_partial_buffer`:
1. Send only 3 messages: [0.80, 0.82, 0.81]
2. Assert `node.current_humidity == pytest.approx(0.81)` (median of sorted [0.80, 0.81, 0.82])

Add a third test `test_humidity_buffer_fifo`:
1. Send 7 messages: [0.80, 0.82, 0.81, 0.99, 0.83, 0.84, 0.85]
2. Assert buffer length is 5 (last 5: [0.99, 0.83, 0.84, 0.85, 0.81]... no wait, FIFO last 5 = [0.81, 0.99, 0.83, 0.84, 0.85])
   Actually with collections.deque(maxlen=5), after 7 pushes the buffer contains [0.99, 0.83, 0.84, 0.85, ... ] — let me be precise:
   Push order: 0.80, 0.82, 0.81, 0.99, 0.83, 0.84, 0.85
   deque(maxlen=5) after all: [0.99, 0.83, 0.84, 0.85, 0.85] — no.
   deque(maxlen=5): after 5 items = [0.80, 0.82, 0.81, 0.99, 0.83]. Push 0.84 -> drops 0.80 -> [0.82, 0.81, 0.99, 0.83, 0.84]. Push 0.85 -> drops 0.82 -> [0.81, 0.99, 0.83, 0.84, 0.85].
   Sorted: [0.81, 0.83, 0.84, 0.85, 0.99]. Median = 0.84.
3. Assert `node.current_humidity == pytest.approx(0.84)`

All test functions should follow existing test patterns: use `ros_context` fixture, create node, send messages, destroy node.

Helper to send a humidity message:
```python
def _send_humidity(node, value):
    msg = RelativeHumidity()
    msg.relative_humidity = value
    node.humidity_callback(msg)
```

**Step 2 — Implement rolling median in fc_controller.py:**

Add `from collections import deque` and `from statistics import median` at the top of the file.

In `__init__`, after the `self.current_humidity = None` line (around line 80), add:
```python
self._humidity_buffer = deque(maxlen=5)
```

Replace the `humidity_callback` method (currently line 93-94):
```python
def humidity_callback(self, msg):
    self._humidity_buffer.append(msg.relative_humidity)
    self.current_humidity = median(self._humidity_buffer)
```

This is the complete change. The control loop continues reading `self.current_humidity` unchanged — it now gets the median-filtered value instead of raw. The `statistics.median` function handles both odd and even-length sequences correctly.

**Step 3 — Run tests and verify:**
Run `cd /mnt/slime-kingdom/opt/mushy && python3 -m pytest src/chambers/fc-core/fc_core/test/test_controller.py -x -v` to confirm new tests pass.
  </action>
  <verify>
    <automated>cd /mnt/slime-kingdom/opt/mushy && python3 -m pytest src/chambers/fc-core/fc_core/test/test_controller.py::test_humidity_spike_rejection src/chambers/fc-core/fc_core/test/test_controller.py::test_humidity_median_partial_buffer src/chambers/fc-core/fc_core/test/test_controller.py::test_humidity_buffer_fifo -x -v</automated>
  </verify>
  <acceptance_criteria>
    - fc_controller.py contains `from collections import deque`
    - fc_controller.py contains `from statistics import median`
    - fc_controller.py contains `self._humidity_buffer = deque(maxlen=5)`
    - fc_controller.py humidity_callback contains `self._humidity_buffer.append(`
    - fc_controller.py humidity_callback contains `self.current_humidity = median(self._humidity_buffer)`
    - test_controller.py contains `def test_humidity_spike_rejection`
    - test_controller.py contains `def test_humidity_median_partial_buffer`
    - test_controller.py contains `def test_humidity_buffer_fifo`
    - All three new tests pass (exit code 0)
  </acceptance_criteria>
  <done>Rolling median filter (5-sample window) added to humidity_callback. Spike at 0.99 in [0.80, 0.82, 0.81, 0.99, 0.83] produces median 0.82, not 0.99. Three tests verify behavior. SENS-05 satisfied.</done>
</task>

</tasks>

<verification>
- `grep "_humidity_buffer" src/chambers/fc-core/fc_core/fc_controller.py` returns matches for deque init and append
- `grep "median" src/chambers/fc-core/fc_core/fc_controller.py` returns matches for import and usage
- `python3 -m pytest src/chambers/fc-core/fc_core/test/test_controller.py -x -v` — all tests pass
</verification>

<success_criteria>
Humidity spike rejection via 5-sample rolling median is implemented in fc_controller.py. Raw sensor values still published by fc_sensors.py (unchanged). Control loop receives median-filtered humidity. Three test cases verify spike rejection, partial buffer, and FIFO behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/02-safety-hardening/02-03-SUMMARY.md`
</output>
