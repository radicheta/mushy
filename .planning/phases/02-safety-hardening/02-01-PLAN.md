---
phase: 02-safety-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/chambers/fc-core/fc_core/fc_sensors.py
autonomous: true
requirements:
  - SENS-03
must_haves:
  truths:
    - "fc_sensors.py exception handler contains no time.sleep() or blocking calls"
    - "Sensor read errors are logged at ERROR level and the sample is skipped"
  artifacts:
    - path: "src/chambers/fc-core/fc_core/fc_sensors.py"
      provides: "Non-blocking sensor error handling"
      contains: "self.get_logger().error"
  key_links: []
---

<objective>
Verify and harden the sensor exception handler in fc_sensors.py to confirm it is non-blocking.

Purpose: SENS-03 requires non-blocking sensor error handling. Per D-05, the current code already has no time.sleep() in the exception handler — this is an audit-and-verify task. If the audit confirms D-05, document the verification. If any blocking call is found, remove it.

Output: Verified fc_sensors.py with confirmed non-blocking error handling.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-safety-hardening/02-CONTEXT.md
@src/chambers/fc-core/fc_core/fc_sensors.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit and verify non-blocking sensor error handling</name>
  <files>src/chambers/fc-core/fc_core/fc_sensors.py</files>
  <read_first>
    - src/chambers/fc-core/fc_core/fc_sensors.py
  </read_first>
  <action>
Per D-04 and D-05 from CONTEXT.md:

1. Read fc_sensors.py completely. Audit the `read_sensors` method exception handler (line 72-73 area).

2. Verify there is NO `time.sleep()` call anywhere in the file — not in the exception handler, not in any callback, nowhere. Also check for any other blocking calls: `input()`, blocking `socket` operations, or synchronous HTTP requests.

3. If the audit confirms no blocking calls exist (expected per D-05): the exception handler should log at ERROR level and skip the sample. The current code does exactly this:
   ```python
   except Exception as e:
       self.get_logger().error(f'Failed to read sensor: {e}')
   ```
   This is correct — on error, the timer callback returns without publishing, effectively skipping the sample. The next timer tick will retry automatically.

4. If (unexpectedly) a `time.sleep()` or blocking call IS found: remove it. Replace with a simple `self.get_logger().error(...)` and return (skip sample). Per D-04: no consecutive failure counters, no log rate limiting — keep it simple.

5. Regardless of findings, add a brief inline comment above the except block to document the design intent:
   ```python
   # Non-blocking: log error and skip sample. Next timer tick retries automatically.
   except Exception as e:
       self.get_logger().error(f'Failed to read sensor: {e}')
   ```
  </action>
  <verify>
    <automated>cd /mnt/slime-kingdom/opt/mushy && python3 -c "import ast; tree = ast.parse(open('src/chambers/fc-core/fc_core/fc_sensors.py').read()); calls = [n for n in ast.walk(tree) if isinstance(n, ast.Call) and isinstance(n.func, ast.Attribute) and n.func.attr == 'sleep']; assert len(calls) == 0, f'Found {len(calls)} sleep() calls'; print('PASS: no sleep() calls found')"</automated>
  </verify>
  <acceptance_criteria>
    - fc_sensors.py contains zero occurrences of `time.sleep` or `.sleep(`
    - fc_sensors.py line 72 area contains comment: "Non-blocking: log error and skip sample"
    - fc_sensors.py exception handler contains `self.get_logger().error(`
    - No `import time` statement exists in fc_sensors.py (unless used elsewhere — if not used, remove it)
  </acceptance_criteria>
  <done>fc_sensors.py exception handler verified non-blocking, documented with inline comment. SENS-03 satisfied.</done>
</task>

</tasks>

<verification>
- `grep -n "sleep" src/chambers/fc-core/fc_core/fc_sensors.py` returns no matches
- `grep -n "Non-blocking" src/chambers/fc-core/fc_core/fc_sensors.py` returns a match in the except block area
- `grep -n "get_logger().error" src/chambers/fc-core/fc_core/fc_sensors.py` returns a match
</verification>

<success_criteria>
fc_sensors.py has zero blocking calls in any callback or exception handler. Error handling logs at ERROR level and skips the sample. Design intent documented with inline comment.
</success_criteria>

<output>
After completion, create `.planning/phases/02-safety-hardening/02-01-SUMMARY.md`
</output>
