---
phase: 02-safety-hardening
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/chambers/fc-core/config/fc_config.yaml
autonomous: true
requirements:
  - SENS-04
must_haves:
  truths:
    - "Config file reflects actual hardware (SHT30, not DHT22)"
    - "sht30_i2c_address is tunable from config without editing code"
    - "Sensor normalization produces 0.0-1.0 in both real and simulation paths"
  artifacts:
    - path: "src/chambers/fc-core/config/fc_config.yaml"
      provides: "Cleaned config matching actual hardware"
      contains: "sht30_i2c_address"
  key_links:
    - from: "src/chambers/fc-core/config/fc_config.yaml"
      to: "src/chambers/fc-core/fc_core/fc_sensors.py"
      via: "sht30_i2c_address param"
      pattern: "sht30_i2c_address"
---

<objective>
Clean up fc_config.yaml to match actual hardware (SHT30, SSR-10A) and verify sensor normalization consistency.

Purpose: SENS-04 requires consistent normalization between real and sim paths. Per D-11, this is already correct — both paths produce 0-100 before /100.0 division, publishing 0.0-1.0. The real work here is config cleanup per D-07, D-08, D-09: remove stale DHT22 references, add missing sht30_i2c_address, update comments.

Output: Clean config file that matches the actual FC-1 hardware setup.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-safety-hardening/02-CONTEXT.md
@src/chambers/fc-core/config/fc_config.yaml
@src/chambers/fc-core/fc_core/fc_sensors.py
</context>

<interfaces>
<!-- From fc_sensors.py — params it reads from config -->
From src/chambers/fc-core/fc_core/fc_sensors.py:
```python
self.declare_parameters(
    namespace='',
    parameters=[
        ('sensor_simulation_mode', True),
        ('sht30_i2c_address', 0x44),
        ('sensor_read_interval', 2.0),
    ]
)
```

From src/chambers/fc-core/config/fc_config.yaml (current):
```yaml
dht_pin: 4  # GPIO4 for DHT22  <-- STALE, to be removed
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Verify sensor normalization and clean up config</name>
  <files>src/chambers/fc-core/config/fc_config.yaml</files>
  <read_first>
    - src/chambers/fc-core/config/fc_config.yaml
    - src/chambers/fc-core/fc_core/fc_sensors.py
  </read_first>
  <action>
Per D-07, D-08, D-09, D-11 from CONTEXT.md:

**Step 1 — Verify normalization (D-11):**
Read fc_sensors.py and confirm both paths produce consistent output:
- Real hardware path (line 48-49): `self.sht.temperature` and `self.sht.relative_humidity` — SHT30 returns humidity in 0-100 range
- Simulation path (line 53-56): `self.sim_humidity` is 0.5-1.0, then multiplied by 100.0 at line 56 to get 0-100
- Both paths then divide by 100.0 at line 67: `float(humidity) / 100.0`
- Result: both paths publish 0.0-1.0 to the topic. This is correct. No code change needed.

**Step 2 — Clean up fc_config.yaml:**

Remove the `dht_pin: 4` line entirely (D-07) — this param is not read by fc_sensors.py (which uses SHT30 over I2C, not GPIO).

Add `sht30_i2c_address: 0x44` under the sensor section (D-09) — fc_sensors.py already declares this as a node param with default 0x44, but it's missing from the yaml config file, making it untunable without editing code.

Update the comment on `sensor_simulation_mode` from referencing DHT22 to SHT30 (D-08). Change:
```yaml
sensor_simulation_mode: false   # false = read real DHT22 hardware
```
to:
```yaml
sensor_simulation_mode: false   # false = read real SHT30 hardware (I2C)
```

Update the "Sensor pins" comment section to "Sensor config" since it's I2C address now, not GPIO pins:
```yaml
# Sensor config
sht30_i2c_address: 0x44  # SHT30 I2C address (bus auto-detected by adafruit_blinka)
light_pin: 18  # GPIO18 for light control
```

The final config should have this section (replacing the old "Sensor pins" block):
```yaml
# Sensor config
sht30_i2c_address: 0x44  # SHT30 I2C address (bus auto-detected by adafruit_blinka)
light_pin: 18  # GPIO18 for light control
```
  </action>
  <verify>
    <automated>cd /mnt/slime-kingdom/opt/mushy && grep -c "dht_pin" src/chambers/fc-core/config/fc_config.yaml | grep -q "^0$" && echo "PASS: dht_pin removed" && grep -q "sht30_i2c_address" src/chambers/fc-core/config/fc_config.yaml && echo "PASS: sht30_i2c_address present" && grep -q "SHT30" src/chambers/fc-core/config/fc_config.yaml && echo "PASS: SHT30 referenced"</automated>
  </verify>
  <acceptance_criteria>
    - fc_config.yaml contains zero occurrences of "dht_pin"
    - fc_config.yaml contains zero occurrences of "DHT22"
    - fc_config.yaml contains `sht30_i2c_address: 0x44`
    - fc_config.yaml contains "SHT30" in a comment
    - fc_config.yaml still contains `sensor_simulation_mode: false`
    - fc_config.yaml still contains `light_pin: 18`
    - fc_config.yaml still contains `sensor_read_interval: 2.0`
  </acceptance_criteria>
  <done>Config cleaned: dht_pin removed, sht30_i2c_address added, DHT22 references replaced with SHT30. Normalization verified consistent (no code change needed). SENS-04 satisfied.</done>
</task>

</tasks>

<verification>
- `grep "dht_pin\|DHT22" src/chambers/fc-core/config/fc_config.yaml` returns no matches
- `grep "sht30_i2c_address: 0x44" src/chambers/fc-core/config/fc_config.yaml` returns a match
- `grep "SHT30" src/chambers/fc-core/config/fc_config.yaml` returns at least one match
</verification>

<success_criteria>
fc_config.yaml reflects actual FC-1 hardware (SHT30 sensor, not DHT22). The sht30_i2c_address param is configurable from yaml. Normalization verified consistent between real and sim paths (no code change was needed).
</success_criteria>

<output>
After completion, create `.planning/phases/02-safety-hardening/02-02-SUMMARY.md`
</output>
