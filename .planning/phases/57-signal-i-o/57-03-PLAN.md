---
phase: 57-signal-i-o
plan: 03
type: execute
wave: 2
depends_on: ["57-01"]
files_modified:
  - src/farm-agent/farm_agent/signal_io/router.py
  - src/farm-agent/farm_agent/signal_io/receive_loop.py
  - src/farm-agent/tests/test_signal_router.py
  - src/farm-agent/tests/test_signal_receive_loop.py
autonomous: true
requirements: [SIG-03]
user_setup: []

must_haves:
  truths:
    - "A whitelisted sender's envelope is routed; a sender not in the whitelist is rejected (logged + skipped) before any branch"
    - "An unknown-but-whitelisted sender resolves to '(unassigned)' and is NOT dropped (SC#5)"
    - "DM vs group context is distinguished; UPDATE/QUIT group envelopes are treated as non-group; group triggers (mention/command/quote) are collected"
    - "The receive loop processes envelopes sequentially (one await each) — never asyncio.gather — preserving send attribution"
    - "The loop never dies on a single tick error: it logs a warning and the next tick proceeds"
    - "D-03: the receive loop long-polls /v1/receive?timeout=1 (~1s worst-case latency), following the REST->httpx transport per D-01"
  artifacts:
    - path: "src/farm-agent/farm_agent/signal_io/router.py"
      provides: "extract_source, is_whitelisted, classify_envelope (DM/group), collect_group_triggers, resolve_farmer, mask via tenant.mask_number"
      exports: ["resolve_farmer", "collect_group_triggers"]
    - path: "src/farm-agent/farm_agent/signal_io/receive_loop.py"
      provides: "ReceiveLoop: start/stop async poll → sequential dispatch(envelope) seam"
      exports: ["ReceiveLoop"]
  key_links:
    - from: "router.resolve_farmer"
      to: "TenantConfig.signal_farmer_map"
      via: "dict.get(source) or '(unassigned)'"
      pattern: "signal_farmer_map"
    - from: "receive_loop tick"
      to: "router whitelist + dispatch seam"
      via: "sequential for-loop, one await per envelope"
      pattern: "for .* in envelopes"
---

<objective>
Port the attribution-sensitive envelope-routing skeleton from `receive-loop.js` into `farm_agent/signal_io/router.py` (pure functions: source extract, whitelist gate, DM-vs-group classification, group-trigger collection, `(unassigned)` farmer-map resolution) and `farm_agent/signal_io/receive_loop.py` (async poll loop with a `dispatch(envelope)` seam). Covers SIG-03 / SC#5.

Purpose: SC#5 ("unknown numbers tagged `(unassigned)`, never dropped") and SC#2-adjacent routing must land in Phase 57 at the router level even though the capture-row CONSUMER is Phase 58. Per RESEARCH Scope Boundary: `message.js` is the alert FORMATTER (Phase 63) — do NOT port it; routing lives in `receive-loop.js`. Port ONLY the source/whitelist/DM-group/trigger/`(unassigned)` skeleton + the dispatch seam; the confirm/snooze/experiment/capture dispatch branches are LATER phases.
Output: `router.py`, `receive_loop.py`, + two unit test files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/57-signal-i-o/57-RESEARCH.md
@.planning/phases/57-signal-i-o/57-PATTERNS.md
@.planning/phases/57-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 (depends_on). Inject TenantConfig; do not read os.environ. -->
TenantConfig: .signal_sender, .signal_recipient, .signal_additional_senders: list[str],
  .signal_farmer_map: dict[str,str] (e164→slug), .receive_poll_sec
mask_number(n) -> str   (module fn in tenancy/tenant.py)

<!-- SignalClient interface (Plan 02, NOT a hard dependency — inject by duck-type for tests).
     receive_loop only needs .receive(); it does not import client.py. -->
SignalClient.receive(timeout_sec=1, ignore_attachments=False) -> list[envelope dicts]

<!-- Envelope shape (from signal-cli /v1/receive). Defensive dual-shape read required. -->
env["envelope"]["source"] : str e164
env["envelope"]["dataMessage"] : {message, attachments, mentions, quote, groupInfo:{groupId,type}}
(fallback shape: env["dataMessage"] — handle both)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: router.py — whitelist, DM/group, triggers, (unassigned) resolution (SIG-03, SC#5)</name>
  <read_first>
    - src/agents/alerter/src/receive-loop.js:14-29 (collectGroupTriggers: dual-shape dm read, mention/command/quote triggers, the command regex incl. U+FFFC marker), :124-128 (allowedSenders set), :134-156 (source extract, whitelist gate BEFORE any branch, groupId/groupType, isGroup excludes UPDATE/QUIT, triggers = dm-set or group-trigger-set)
    - src/agents/alerter/src/capture.js:86 (`(unassigned)` resolution: signalFarmerMap.get(source) ?? '(unassigned)')
    - src/agents/alerter/src/config.js:258-261 (maskNumber — already ported in Plan 01 as tenant.mask_number; import it)
    - .planning/phases/57-signal-i-o/57-PATTERNS.md §router.py (exact Python forms for whitelist set, dual-shape dm read, resolve_farmer, the SCOPE BOUNDARY note)
    - src/farm-agent/farm_agent/tenancy/tenant.py (_resolve_farmer_map already builds signal_farmer_map; pure-function module style to mirror)
  </read_first>
  <behavior>
    - Test (whitelist): is_whitelisted(source, config) True for signal_sender / signal_recipient / an entry in signal_additional_senders; False otherwise.
    - Test (SC#5): resolve_farmer("+known", config) == the mapped slug; resolve_farmer("+unknown", config) == "(unassigned)" — never None, never raises.
    - Test (DM vs group): classify_envelope returns is_group False for a no-groupInfo envelope; True for groupId present with type not in {UPDATE,QUIT}; False when type is UPDATE or QUIT.
    - Test (triggers): collect_group_triggers detects 'mention' when a mention.number == bot phone; 'command' on "mute"/"@bot snooze"/"/force-x" (incl. a leading U+FFFC marker); 'quote' when quote.author (or authorNumber) == bot phone. DM context → {"dm"}.
    - Test (dual-shape): source/dm read works for both env["envelope"]["dataMessage"] and env["dataMessage"].
  </behavior>
  <action>
    Create signal_io/router.py as a pure-function module (mirror tenant.py style; inject TenantConfig, no os.environ). Implement: extract_source(env) (env.get("envelope",{}).get("source")); allowed_senders(config) → set([config.signal_sender, config.signal_recipient, *config.signal_additional_senders] filtered truthy); is_whitelisted(source, config); classify_envelope(env) → dict {source, dm, group_id, group_type, is_group} with is_group = bool(group_id) and group_type not in ("UPDATE","QUIT") and dual-shape dm read `env.get("envelope",{}).get("dataMessage") or env.get("dataMessage") or {}`; collect_group_triggers(env, bot_phone) → set porting receive-loop.js:14-29 verbatim (mention/command/quote; the command regex with optional `@mention ` and U+FFFC tolerance; accept quote.author or quote.authorNumber); resolve_farmer(source, config) → config.signal_farmer_map.get(source) or "(unassigned)" (SC#5 — unknown-but-whitelisted is tagged, NOT dropped). Use tenant.mask_number for any log line. Do NOT port the snooze/experiment/capture branches.
  </action>
  <verify>
    <automated>cd src/farm-agent && uv run pytest tests/test_signal_router.py -x -q</automated>
  </verify>
  <done>Whitelist gate, DM-vs-group classification (UPDATE/QUIT excluded), group-trigger collection, and (unassigned) resolution all ported and unit-tested; unknown sender resolves to "(unassigned)" and is not dropped. test_signal_router green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: receive_loop.py — sequential poll + dispatch seam</name>
  <read_first>
    - src/agents/alerter/src/receive-loop.js:47-70 (createReceiveLoop ctor: signalClient, dispatch, config, logger, clock), :130-156 (tick(): receive → `for (const env of envelopes)` SEQUENTIAL → source extract → whitelist gate → classify → dispatch)
    - .planning/phases/57-signal-i-o/57-RESEARCH.md §"Pattern: anti-patterns" (NEVER asyncio.gather over envelopes — attribution; PITFALLS #5/#6) + §receive_loop Open Question A3 (dual-poller contention — relevant to Plan 04 live-fire, not this unit)
    - .planning/phases/57-signal-i-o/57-PATTERNS.md §receive_loop.py (async lifecycle mirroring pool.py start/stop; loop-never-dies)
    - src/farm-agent/farm_agent/persistence/pool.py (build/open ↔ start/stop async lifecycle idiom)
  </read_first>
  <behavior>
    - Test: tick() with a mock client.receive returning 3 envelopes from whitelisted senders calls dispatch exactly 3 times, in order (assert call order — attribution).
    - Test: an envelope from a non-whitelisted source is NOT dispatched (rejected + logged).
    - Test: an envelope with no source is skipped.
    - Test (loop-never-dies): if dispatch raises on envelope #2, the loop logs a warning and still processes envelope #3 (or the next tick proceeds) — tick() does not propagate the exception.
    - Test: start()/stop() create and cancel the poll task cleanly (no warning about un-awaited task).
  </behavior>
  <action>
    Create signal_io/receive_loop.py with class ReceiveLoop(signal_client, dispatch: Callable[[dict], Awaitable], config: TenantConfig, logger=..., poll_sec=None). poll_sec defaults to config.receive_poll_sec. Implement async tick(): try: envelopes = await signal_client.receive(timeout_sec=1); for env in envelopes (SEQUENTIAL — one await per env, NEVER asyncio.gather): source = router.extract_source(env); if not source: continue; if not router.is_whitelisted(source, config): logger.warning("[receive] rejected sender (not in whitelist)"); continue; wrap the per-envelope dispatch in try/except so one bad envelope does not kill the tick (log warn, continue). The whole tick body is also wrapped so a receive() error logs a warning and the next tick proceeds (Pitfall 4). Implement start() (create asyncio task running `while True: await tick(); await asyncio.sleep(poll_sec)`) and stop() (cancel + await the task, swallow CancelledError). Mirror pool.py's async lifecycle. The dispatch seam is the Phase-58+ consumer entry point — do NOT implement capture/confirm here.
  </action>
  <verify>
    <automated>cd src/farm-agent && uv run pytest tests/test_signal_receive_loop.py -x -q</automated>
  </verify>
  <done>ReceiveLoop polls, gates on the whitelist, dispatches sequentially in order, never dies on a single envelope/tick error, and start/stop cleanly manage the asyncio task. test_signal_receive_loop green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| signal-cli → router | Inbound envelopes from arbitrary senders cross into the bot; the whitelist is the access-control gate |
| router → dispatch seam | Only whitelisted, attributed envelopes reach Phase-58+ consumers |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-57-03-01 | Spoofing | unknown sender driving the bot | mitigate | Whitelist gate BEFORE any branch (V4, T-17-02/R7); only signal_sender/recipient/additional senders processed; unknown-but-whitelisted → "(unassigned)", not dropped (SC#5) |
| T-57-03-02 | Tampering | quote-spoof (reply to another farmer's draft) | accept | Sender-equality quote-spoof guard lives in the confirm-path phase (receive-loop.js:263), NOT this primitive phase — noted out of scope per RESEARCH Security Domain |
| T-57-03-03 | Information Disclosure | rejected-sender log line | mitigate | Log via mask_number; never log the full e164 (V7) |
| T-57-03-04 | Denial of Service | one malformed envelope killing the loop | mitigate | Per-envelope + per-tick try/except → warn + continue (loop-never-dies, Pitfall 4) |
</threat_model>

<verification>
- `cd src/farm-agent && uv run pytest tests/test_signal_router.py tests/test_signal_receive_loop.py -x` green.
- `cd src/farm-agent && uv run pytest` full suite green.
- `grep -n 'gather' src/farm-agent/farm_agent/signal_io/receive_loop.py | grep -v '^#'` returns nothing (no asyncio.gather over envelopes).
</verification>

<success_criteria>
- SIG-03 routing skeleton ported: whitelist, DM-vs-group, group triggers, (unassigned) resolution.
- SC#5 proven: unknown sender → "(unassigned)", never dropped.
- Sequential dispatch preserves attribution (no asyncio.gather).
- message.js (alert formatter) NOT ported here — confirmed scope boundary honored.
</success_criteria>

<output>
Create `.planning/phases/57-signal-i-o/57-03-SUMMARY.md` when done. Note the dispatch seam is the Phase-58 capture-pipeline entry point.
</output>
