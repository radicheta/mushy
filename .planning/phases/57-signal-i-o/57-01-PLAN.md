---
phase: 57-signal-i-o
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/farm-agent/pyproject.toml
  - src/farm-agent/farm_agent/tenancy/tenant.py
  - src/farm-agent/farm_agent/persistence/outbound_repo.py
  - src/farm-agent/tests/test_tenancy.py
  - src/farm-agent/tests/test_signal_persist.py
  - src/farm-agent/tests/conftest.py
autonomous: false
requirements: [SIG-02]
user_setup: []

must_haves:
  truths:
    - "An outbound row persists to signal_outbound; an insert failure never throws and never blocks the caller (fail-open, D-02)"
    - "TenantConfig exposes signal_api_url, signal_additional_senders, and a mask_number helper that signal_io modules consume by injection (never reading os.environ)"
    - "An httpx mock fixture + a fake-raising outbound repo are available to the signal_io unit suite"
  artifacts:
    - path: "src/farm-agent/farm_agent/persistence/outbound_repo.py"
      provides: "insert_outbound(pool, row) -> {ok|reason}, never-throw psycopg3 INSERT"
      exports: ["insert_outbound"]
    - path: "src/farm-agent/farm_agent/tenancy/tenant.py"
      provides: "signal_api_url + signal_additional_senders fields + mask_number()"
      contains: "signal_api_url"
    - path: "src/farm-agent/pyproject.toml"
      provides: "httpx runtime dep + respx dev dep"
      contains: "httpx"
  key_links:
    - from: "outbound_repo.insert_outbound"
      to: "signal_outbound table"
      via: "psycopg3 async with pool.connection()"
      pattern: "INSERT INTO signal_outbound"
    - from: "signal_io callers (Plan 02)"
      to: "TenantConfig.signal_api_url / mask_number"
      via: "constructor injection"
      pattern: "signal_api_url"
---

<objective>
Land the Phase-57 foundation that the SignalClient (Plan 02) and router (Plan 03) build on: the new HTTP/mocking dependencies, the three missing `TenantConfig` surfaces (`signal_api_url`, `signal_additional_senders`, `mask_number`), and the never-throw `outbound_repo.insert_outbound` (SIG-02 durable persist, fail-open per D-02).

Purpose: RESEARCH assumed httpx was already a Phase-56 dependency and that the config exposed these fields — neither is true on disk. This plan closes those gaps so Wave 2 plans are pure ports with no scavenger hunt and no missing imports.
Output: `outbound_repo.py`, extended `tenant.py`, `pyproject.toml` deps, conftest fixtures, and their unit tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/57-signal-i-o/57-RESEARCH.md
@.planning/phases/57-signal-i-o/57-PATTERNS.md

<interfaces>
<!-- The signal_outbound DDL already exists (Phase 56). Column order is the contract. -->
From farm_agent/persistence/migrations.py:279-292 (DDL, DO NOT re-create):
  signal_outbound(id uuid PK, tenant_id text NOT NULL, sent_at timestamptz NOT NULL,
    recipient_e164 text NOT NULL, intent text NOT NULL, body text NOT NULL,
    attachments jsonb, source_module text NOT NULL, source_line integer,
    related_capture_id text, related_draft_id text, signal_msg_ts bigint)

From farm_agent/persistence/pool.py:44-52 (psycopg3 acquisition idiom to mirror):
  async with pool.connection() as conn: await conn.execute(sql, params)   # %s placeholders, NOT $1
  type hint: psycopg_pool.AsyncConnectionPool

From farm_agent/tenancy/tenant.py (frozen dataclass + load(env) — extend, do not rewrite):
  @dataclass(frozen=True) class TenantConfig: signal_sender, signal_recipient,
    signal_group_id, signal_farmer_map: dict[str,str], receive_poll_sec,
    max_sends_per_hour, ...
  helpers: _must_env, _pick, _parse_int_env, _parse_float_env, _parse_farmer_map_string
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Package legitimacy gate — httpx + respx</name>
  <read_first>
    - src/farm-agent/pyproject.toml (current deps; httpx is NOT present despite RESEARCH assuming it)
    - .planning/phases/57-signal-i-o/57-RESEARCH.md §"Package Legitimacy Audit" (states no new installs — that audit is stale vs the on-disk pyproject; httpx + respx ARE new installs here)
  </read_first>
  <what-built>Nothing yet — this is a pre-install legitimacy verification required by the package gate because httpx (runtime) and respx (dev) are net-new installs not in the Phase-56 pyproject.toml.</what-built>
  <how-to-verify>
    1. Confirm httpx on https://pypi.org/project/httpx/ — Encode project (same authors as Starlette/uvicorn), widely used, current ~0.28.x.
    2. Confirm respx on https://pypi.org/project/respx/ — maintainer 5monkeys (Jonas Lundberg), BSD-3-Clause, Production/Stable, ~0.23.x, the standard httpx mock library.
    3. Both are [ASSUMED]→verified legitimate. Approve to proceed with the pinned adds in Task 2.
  </how-to-verify>
  <acceptance_criteria>
    - Human confirms both packages are legitimate (not [SLOP]/[SUS]) before any install. This checkpoint is never auto-approvable (T-57-SC).
  </acceptance_criteria>
  <resume-signal>Type "approved" or name a package to replace.</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add httpx + respx deps; extend TenantConfig with signal_api_url, signal_additional_senders, mask_number</name>
  <read_first>
    - src/farm-agent/pyproject.toml (dependencies + dev extras blocks)
    - src/farm-agent/farm_agent/tenancy/tenant.py (full file — frozen dataclass + load(); _pick/_must_env helpers at :45-128, field block at :208-256, assignment block at :293-388)
    - src/agents/alerter/src/config.js:132,136-137,255-261 (Node port-targets: signalApiUrl default 'http://signal-cli:8080'; signalAdditionalSenders = SIGNAL_ADDITIONAL_SENDERS split/trim/filter; maskNumber)
    - .planning/phases/57-signal-i-o/57-PATTERNS.md §router.py "maskNumber" block (exact Python form + the <6 guard → "XXXX")
  </read_first>
  <behavior>
    - Test: TenantConfig.signal_api_url defaults to "http://signal-cli:8080" when SIGNAL_API_URL unset; honors env override.
    - Test: signal_additional_senders parses "+1,+2 , " into ["+1","+2"] (split on comma, strip, drop empties); empty/unset → [].
    - Test: mask_number("+15551234567") == "+1XXXXXX4567"; mask_number("short") == "XXXX"; mask_number(non-str) == "XXXX".
  </behavior>
  <action>
    In pyproject.toml: add `httpx>=0.28` to `[project].dependencies`; add `respx>=0.23` to `[project.optional-dependencies].dev`. Run `uv sync` to lock.
    In tenant.py: add frozen fields `signal_api_url: str` and `signal_additional_senders: list[str]` to TenantConfig; in `load()` resolve `signal_api_url = _pick(tenant_cfg, env, "SIGNAL_API_URL", "http://signal-cli:8080")` (Tier-D env-default, like Node config.js:132) and `signal_additional_senders` by splitting `env.get("SIGNAL_ADDITIONAL_SENDERS","")` on comma, stripping, filtering empties. Add a module-level `mask_number(n)` pure function porting config.js:258-261 verbatim (guard `not isinstance(n,str) or len(n)<6` → "XXXX"; else `n[:2] + "X"*(len(n)-6) + n[-4:]`). Export mask_number alongside load.
  </action>
  <verify>
    <automated>cd src/farm-agent && uv run pytest tests/test_tenancy.py -x -q</automated>
  </verify>
  <done>httpx in [project].dependencies, respx in dev extras, uv.lock updated; TenantConfig carries signal_api_url + signal_additional_senders; mask_number passes the three behavior assertions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: outbound_repo.insert_outbound (never-throw) + conftest fixtures</name>
  <read_first>
    - src/agents/alerter/src/outbound-db.js:54-82 (insertOutbound port-target: column order is the contract, returns {ok}/{ok:false,reason}, NEVER throws)
    - src/farm-agent/farm_agent/persistence/migrations.py:263-327 (DDL already shipped; column order; signal_outbound.source_module is NOT NULL)
    - src/farm-agent/farm_agent/persistence/pool.py:44-52 (psycopg3 `async with pool.connection()` idiom)
    - src/farm-agent/tests/conftest.py (TEST_ENV + session pool fixture + socket-skip pattern to mirror)
    - src/farm-agent/tests/test_persistence.py (skipif-DB gate style; DB-independent pure tests that always run)
  </read_first>
  <behavior>
    - Test (DB-independent, always runs): a FAKE repo whose insert raises is swallowed — direct unit on the try/except wrapper returns {"ok": False, "reason": ...}, never propagates. (The fail-open-from-send assertion lives in Plan 02's test_signal_persist via this fake.)
    - Test (DB-gated, @requires_db): insert_outbound writes a real row; signal_msg_ts stored as int round-trips as int (bigint, not float); recipient_e164 string and group:<id> forms both insert.
    - Test (DB-gated): omitting signal_msg_ts stores NULL; omitting source_module defaults to "signal_io" (column is NOT NULL).
  </behavior>
  <action>
    Create farm_agent/persistence/outbound_repo.py with `async def insert_outbound(pool: AsyncConnectionPool, row: dict) -> dict`. Use `async with pool.connection() as conn: await conn.execute(sql, params)` with `%s` placeholders in the exact column order tenant_id, sent_at, recipient_e164, intent, body, attachments, source_module, source_line, related_capture_id, related_draft_id, signal_msg_ts (attachments cast `%s::jsonb`, pass psycopg.types.json.Jsonb(row["attachments"]) or None). Default `source_module` to "signal_io" when row omits it (column is NOT NULL). Do NOT coerce signal_msg_ts here — store as-is (caller int()'d it). Wrap the whole body in try/except → return {"ok": False, "reason": str(e)} on any exception; {"ok": True} on success. NEVER raise. No DDL — migrations.py owns it.
    In conftest.py: add a `respx_mock`-based fixture (or a monkeypatched httpx.AsyncClient transport) named `signal_http` returning a configurable mock, and a `FakeOutboundRepo` helper class whose insert_outbound can be toggled to raise (for Plan 02's fail-open test). These need no DB → always available.
  </action>
  <verify>
    <automated>cd src/farm-agent && uv run pytest tests/test_signal_persist.py -x -q</automated>
  </verify>
  <done>outbound_repo.insert_outbound exists, is never-throw, writes the 11-column row in contract order with bigint signal_msg_ts; conftest exposes an httpx mock fixture + a raising FakeOutboundRepo; the DB-independent fail-open test passes (DB-gated tests skip cleanly when :5434 is down).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| signal_io → TimescaleDB | Outbound rows written to the shared prod DB; an insert failure must not back-pressure the farmer-facing send |
| config → env | SIGNAL_SENDER and other secrets cross from env into the process; must never be sourced from tenant YAML |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-57-01-01 | Denial of Service | outbound_repo INSERT | mitigate | Never-throw try/except → {ok:false} fail-open; a DB outage degrades to warn, send return unaffected (D-02) |
| T-57-01-02 | Information Disclosure | mask_number / logs | mitigate | mask_number ported verbatim; never log full e164 (V7) |
| T-57-01-03 | Information Disclosure | SIGNAL_SENDER in tenant YAML | mitigate | signal_sender stays env-only via _must_env (W9); this plan does not move it to YAML |
| T-57-SC | Tampering | pip/uv installs (httpx, respx) | mitigate | Task 1 blocking-human legitimacy checkpoint before any install |
</threat_model>

<verification>
- `cd src/farm-agent && uv run pytest tests/test_tenancy.py tests/test_signal_persist.py -x` green.
- `cd src/farm-agent && uv run pytest` (full suite) green — no regression in Phase-56 tests.
- `grep -n 'httpx' src/farm-agent/pyproject.toml` returns a dependency line.
</verification>

<success_criteria>
- httpx is a runtime dep, respx a dev dep, both pinned in uv.lock (after human legitimacy approval).
- TenantConfig exposes signal_api_url + signal_additional_senders; mask_number ported.
- outbound_repo.insert_outbound is never-throw and writes signal_msg_ts as bigint int.
- conftest exposes httpx-mock + raising-FakeOutboundRepo helpers for Wave 2.
</success_criteria>

<output>
Create `.planning/phases/57-signal-i-o/57-01-SUMMARY.md` when done.
</output>
