---
phase: 57-signal-i-o
plan: 02
type: execute
wave: 2
depends_on: ["57-01"]
files_modified:
  - src/farm-agent/farm_agent/signal_io/__init__.py
  - src/farm-agent/farm_agent/signal_io/client.py
  - src/farm-agent/tests/test_signal_client.py
  - src/farm-agent/tests/test_signal_quote.py
  - src/farm-agent/tests/test_signal_groups.py
  - src/farm-agent/tests/test_signal_ratecap.py
  - src/farm-agent/tests/test_signal_persist.py  # shared append-target declared in Plan 01 (Wave 1); Plan 02 (Wave 2) appends assertions, no write race
autonomous: true
requirements: [SIG-01, SIG-02, SIG-04]
user_setup: []

must_haves:
  truths:
    - "SignalClient.send/receive/fetch_attachment/accounts hit the REST container via httpx, mapping 1:1 onto signal.js"
    - "A valid quote with a string timestamp is coerced via int(str(ts)); an invalid quote shape fails open to an unquoted send + warn, never throws (SC#3)"
    - "A group target's internal_id is translated to id-b64 via the lazy /v1/groups cache; on cache miss it passes through (SC#2)"
    - "Two concurrent send coroutines never exceed maxSendsPerHour — guarded by asyncio.Lock (SC#4)"
    - "A successful send persists exactly one signal_outbound row via the injected repo, fail-open; signal_msg_ts stored as int (bigint, SC#1)"
    - "D-04: rate-cap is an in-memory sendHistory of ms-timestamps pruned to the last hour, guarded by an asyncio.Lock (NOT DB-derived), with cap read per-send via the dynamic getMaxSendsPerHour hook; resets on restart (1:1 with Node)"
    - "D-05: the intent-agnostic quote primitive coerces timestamp via int(str(ts)), validates shape with isValidQuote, and fails open to an unquoted send + warn on invalid shape (never throws, never silently drops)"
  artifacts:
    - path: "src/farm-agent/farm_agent/signal_io/client.py"
      provides: "SignalClient: send, receive, fetch_attachment, accounts, sends_this_hour, ensure_groups_loaded, is_valid_quote"
      exports: ["SignalClient"]
      min_lines: 120
  key_links:
    - from: "SignalClient.send"
      to: "/v2/send"
      via: "httpx.AsyncClient.post"
      pattern: "/v2/send"
    - from: "SignalClient.send"
      to: "outbound_repo.insert_outbound"
      via: "fail-open persist hook"
      pattern: "insert_outbound"
    - from: "SignalClient.send rate-cap"
      to: "asyncio.Lock"
      via: "check+reserve inside lock, POST outside lock"
      pattern: "asyncio.Lock|self._lock"
---

<objective>
Port `signal.js`'s `createSignalClient` factory to `farm_agent/signal_io/client.py` as a `SignalClient` class — the wire-level Signal I/O choke-point. Covers SIG-01 (send/receive/fetch_attachment/accounts via httpx against the REST container), SIG-02 (asyncio.Lock-guarded rate-cap + fail-open durable persist), and SIG-04 (intent-agnostic quote primitive). This is a near-mechanical 1:1 translation gated by the Phase-64 parity check — the ONE genuinely new mechanism is the asyncio.Lock (Node didn't need it).

Purpose: single send choke-point preserving Phase-37/44/50 behavior so Phase-64 parity holds.
Output: `signal_io/client.py` + four unit test files (client, quote, groups, ratecap).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/57-signal-i-o/57-RESEARCH.md
@.planning/phases/57-signal-i-o/57-PATTERNS.md
@.planning/phases/57-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 (depends_on). Inject these; do not read os.environ. -->
TenantConfig (farm_agent/tenancy/tenant.py): .signal_api_url, .signal_sender,
  .signal_recipient, .signal_group_id, .max_sends_per_hour; mask_number(n) module fn.
outbound_repo.insert_outbound(pool, row: dict) -> {"ok": True} | {"ok": False, "reason": str}  (never-throw)
persistence/pool.py build_pool(config) -> AsyncConnectionPool

<!-- Module-split decision (PATTERNS.md "Module-split note" + RESEARCH): keep close to
     signal.js's single factory. One SignalClient in client.py with is_valid_quote,
     ensure_groups_loaded, _prune_history, _current_cap as methods. router.py +
     receive_loop.py are SEPARATE (Plan 03). Do NOT split quote/groups/ratecap into
     their own modules — proximity to the port source minimizes parity diff. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SignalClient transport + quote primitive (SIG-01, SIG-04)</name>
  <read_first>
    - src/agents/alerter/src/signal.js:5-13 (ctor + effectiveDefault), :71-80 (isValidQuote), :82-151 (send choke-point, target resolution, payload + quote build, POST), :205-224 (receive, fetchAttachment, accounts)
    - .planning/phases/57-signal-i-o/57-RESEARCH.md §"Code Examples" (Send/Receive/Fetch attachment httpx forms) + Pitfall 1 (bigint) + Pitfall 3 (quote string-ts coercion + fail-open)
    - .planning/phases/57-signal-i-o/57-PATTERNS.md §client.py + §quote.py (the exact is_valid_quote Python; int(str(ts)) NOT float() NOT bare int())
    - src/farm-agent/farm_agent/persistence/pool.py (async client/injection idiom to mirror)
    - src/farm-agent/tests/conftest.py (signal_http httpx-mock fixture from Plan 01)
  </read_first>
  <behavior>
    - Test: send("hi") to a string target POSTs /v2/send with {message,number,recipients:[+to]}; returns {"ok":True,"timestamp":...} using response timestamp (string "1779562666675") coerced; int(str(ts)) is an int.
    - Test: receive() GETs /v1/receive/{sender}?timeout=1&ignore_attachments=false and returns parsed JSON list.
    - Test: fetch_attachment(id) returns bytes (r.content); accounts() returns parsed JSON.
    - Test (SC#3): quote={timestamp:"123",author:"+1",message:"x"} → payload.quote.timestamp == 123 (int). Invalid quote (missing author) → NO quote key in payload, a warn is logged, send still returns ok (fail-open, no raise).
    - Test: invalid send target (None, empty str, {} without groupId) raises ValueError("invalid send target").
  </behavior>
  <action>
    Create signal_io/client.py with class SignalClient. Constructor injects config: TenantConfig, http: httpx.AsyncClient, outbound_repo=None, pool=None, get_max_sends_per_hour: Callable[[],int]|None=None, logger=..., timeout_s=10.0, tenant_id="mossrock". Resolve effective_default = default_target if provided else config.signal_recipient; raise ValueError if empty (signal.js:8-11). Implement is_valid_quote(q) per PATTERNS quote.py (math.isfinite(float(str(q["timestamp"]))) in try/except; author non-empty str; message str — empty allowed). Implement send(body, *, bypass_cap=False, to=None, intent=None, related_capture_id=None, related_draft_id=None, source_module="signal_io", quote=None): resolve target (str phone | {"groupId":...}); build recipients; build payload {message,number=config.signal_sender,recipients}; if quote not None: if is_valid_quote → payload["quote"]={"timestamp":int(str(quote["timestamp"])),"author":...,"message":...} else logger.warning + send unquoted (NEVER raise). POST {config.signal_api_url}/v2/send with timeout=timeout_s; on status>=400 raise RuntimeError(f"signal-cli {status}: {text[:200]}"); parse json; log `[signal] sent -> {mask_number(target) or group:<id[:8]>...} ({len(body)} chars)`. Return {"ok":True,"timestamp":data.get("timestamp") or now}. Implement receive(timeout_sec=1, ignore_attachments=False), fetch_attachment(id)->bytes, accounts() per RESEARCH Code Examples (urllib.parse.quote_plus for path segments; request timeout = timeout_sec+5 on receive). Rate-cap + groups + persist land in Tasks 2-3 of this plan; stub the rate-cap reserve call and persist hook as methods they fill in.
  </action>
  <verify>
    <automated>cd src/farm-agent && uv run pytest tests/test_signal_client.py tests/test_signal_quote.py -x -q</automated>
  </verify>
  <done>SignalClient send/receive/fetch_attachment/accounts hit the REST endpoints via httpx; quote primitive coerces string-ts via int(str(ts)) and fails open on invalid shape; invalid target raises. test_signal_client + test_signal_quote green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rate-cap with asyncio.Lock + group-ID translation (SC#4, SC#2)</name>
  <read_first>
    - src/agents/alerter/src/signal.js:13,41-56 (sendHistory, pruneHistory, currentCap/getMaxSendsPerHour), :82-89,147 (cap check + push-on-success), :226-229 (sendsThisHour); :20-39 (groupIdMap + ensureGroupsLoaded), :98-112 (translate-on-send)
    - .planning/phases/57-signal-i-o/57-RESEARCH.md §"Pattern 2" (asyncio.Lock check+reserve, the IMPORTANT append-timing note: reserve-before-await option (a); do NOT hold the lock across the POST) + §"Pattern 3" (ensure_groups_loaded exact Python) + Pitfall 4 + Pitfall 2
    - .planning/phases/57-signal-i-o/57-PATTERNS.md §ratecap.py + §groups.py
  </read_first>
  <behavior>
    - Test (SC#4): with max_sends_per_hour=2, two concurrent `await asyncio.gather(send(...), send(...), send(...))` against a mock that returns ok — exactly 2 succeed, the 3rd returns {"ok":False,"reason":"rate-cap"}; the in-memory history length never exceeds the cap. (Reserve-before-await: a slot is consumed inside the lock before the POST.)
    - Test: get_max_sends_per_hour hook returning a finite number overrides config.max_sends_per_hour on the next send; a hook that raises falls back to config value.
    - Test: _prune_history drops timestamps older than now-3_600_000; sends_this_hour() reflects pruned count.
    - Test (SC#2): a {"groupId": "<internal_id>"} target with /v1/groups mock returning [{"id":"group.<idb64>","internal_id":"<internal_id>"}] sends recipients=["group.<idb64>"] (translated). A groupId not in the cache passes through as recipients=["group.<groupId>"].
  </behavior>
  <action>
    Add to SignalClient: self._lock = asyncio.Lock(); self._send_history: list[int] = []. Implement _prune_history(now) (cutoff = now-3_600_000; keep t>=cutoff), _current_cap() (try get_max_sends_per_hour() finite-number else config.max_sends_per_hour), sends_this_hour(). In send(): now=int(time.time()*1000); `async with self._lock:` prune + cap = _current_cap(); if not bypass_cap and len(history)>=cap: warn + return {"ok":False,"reason":"rate-cap"}; else history.append(now) (reserve BEFORE await — RESEARCH option (a)). POST happens OUTSIDE the lock. Add self._group_id_map: dict[str,str]={}, self._groups_loaded=False; implement async ensure_groups_loaded(force=False) porting signal.js:22-39 (GET /v1/groups/{quote_plus(sender)}; strip "group." prefix; map internal_id→id; on exception logger.warning, do NOT throw). In send target resolution for group targets: await ensure_groups_loaded(False); resolved_group_id = map.get(groupId, groupId); recipients=[f"group.{resolved_group_id}"]. Note for Phase 64: option (a) counts attempts not successes (micro-delta) — record in SUMMARY.
  </action>
  <verify>
    <automated>cd src/farm-agent && uv run pytest tests/test_signal_ratecap.py tests/test_signal_groups.py -x -q</automated>
  </verify>
  <done>Two concurrent sends never exceed the cap (asyncio.Lock, reserve-before-await, POST outside lock); dynamic cap hook honored; group internal_id→id-b64 translated via lazy /v1/groups cache with pass-through on miss. test_signal_ratecap + test_signal_groups green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Fail-open durable persist hook (SIG-02, SC#1)</name>
  <read_first>
    - src/agents/alerter/src/signal.js:153-199 (durable persist hook: recipient encoding group:<id> path-b; intent default 'unknown'+warn; signal_msg_ts=Number(json.timestamp) or null; try/except fail-open + defense-in-depth)
    - src/agents/alerter/src/outbound-db.js:54-82 (row shape / column contract)
    - .planning/phases/57-signal-i-o/57-RESEARCH.md §"Code Examples" Outbound persist (psycopg3 fail-open) + Pitfall 1 (int() not float()) + Pitfall 7 (tz-aware sent_at)
    - src/farm-agent/farm_agent/persistence/outbound_repo.py (Plan 01 insert_outbound signature)
    - src/farm-agent/tests/conftest.py (raising FakeOutboundRepo from Plan 01)
  </read_first>
  <behavior>
    - Test (SIG-02 fail-open): inject a FakeOutboundRepo whose insert_outbound raises; send() still returns {"ok":True} and logs a warn — the exception never propagates.
    - Test (SC#1): on a successful send, the persist hook is called once with signal_msg_ts == int(response timestamp) (a Python int, not float, not str), recipient_e164 == the +e164 for a string target, sent_at tz-aware UTC.
    - Test: a group send persists recipient_e164 == "group:<resolved_id-b64>" (path-b encoding).
    - Test: send() with intent=None defaults effective intent to "unknown" and logs a warn.
  </behavior>
  <action>
    After a successful POST in send(), if self._outbound_repo and self._pool: effective_intent = intent or (warn + "unknown"); recipient_col = target if string else f"group:{resolved_group_id or target['groupId']}"; build row {tenant_id, sent_at=datetime.now(timezone.utc), recipient_e164=recipient_col, intent=effective_intent, body, attachments=None, source_module, source_line=None, related_capture_id, related_draft_id, signal_msg_ts=int(data["timestamp"]) if data.get("timestamp") else None}. Call `await self._outbound_repo.insert_outbound(self._pool, row)` inside try/except → on exception logger.warning(f"[signal] outbound persist threw (fail-open): {e}") and CONTINUE; if result is {"ok":False} logger.warning the reason. The persist runs OUTSIDE the rate-cap lock and NEVER affects the {"ok":True,"timestamp":...} return (D-02 defense-in-depth — repo is already never-throw, this is the second layer).
  </action>
  <verify>
    <automated>cd src/farm-agent && uv run pytest tests/test_signal_persist.py tests/test_signal_client.py -x -q</automated>
  </verify>
  <done>Successful send persists one row with bigint int signal_msg_ts + path-b recipient encoding; a raising repo is swallowed (send still ok); missing intent → 'unknown'+warn. test_signal_persist + test_signal_client green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SignalClient → signal-cli REST | Outbound message bodies + targets cross to the container; rate-cap caps cost/flood |
| SignalClient → TimescaleDB | Outbound persistence; must not back-pressure the send |
| quote caller → wire | Untrusted-shape quote dict; validated before it reaches /v2/send |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-57-02-01 | Denial of Service | send rate-cap | mitigate | asyncio.Lock-guarded check+reserve so concurrent coroutines cannot exceed maxSendsPerHour (SC#4); dynamic cap hook |
| T-57-02-02 | Tampering | quote payload | mitigate | is_valid_quote shape check; invalid → unquoted send + warn (fail-open, never throw). Sender-equality spoof guard is OUT OF SCOPE (confirm-path phase, receive-loop.js:263) |
| T-57-02-03 | Information Disclosure | send/group log lines | mitigate | mask_number on the e164 target; group label truncated to 8 chars |
| T-57-02-04 | Information Disclosure | invalid-quote warn dump | mitigate | warn dumps the quote shape, not a secret; quote carries only ts/author/message (author is an e164 — acceptable in a warn; do not add full-body logging) |
</threat_model>

<verification>
- `cd src/farm-agent && uv run pytest tests/test_signal_client.py tests/test_signal_quote.py tests/test_signal_groups.py tests/test_signal_ratecap.py tests/test_signal_persist.py -x` green.
- `cd src/farm-agent && uv run pytest` full suite green.
- `grep -n 'asyncio.Lock' src/farm-agent/farm_agent/signal_io/client.py` present.
</verification>

<success_criteria>
- SignalClient ports signal.js 1:1: send/receive/fetch_attachment/accounts via httpx.
- SC#1 (bigint int signal_msg_ts), SC#2 (group translation), SC#3 (quote coercion + fail-open), SC#4 (Lock cap) all proven by unit tests.
- D-02 fail-open persist: repo failure never affects send.
- Module stays close to signal.js (single SignalClient class) for parity.
</success_criteria>

<output>
Create `.planning/phases/57-signal-i-o/57-02-SUMMARY.md` when done. Record the rate-cap append-timing micro-delta (attempts vs successes) for the Phase-64 parity author.
</output>
