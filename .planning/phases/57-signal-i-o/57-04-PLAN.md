---
phase: 57-signal-i-o
plan: 04
type: execute
wave: 3
depends_on: ["57-02", "57-03"]
files_modified:
  - src/farm-agent/scripts/live_fire_57.py
  - .planning/phases/57-signal-i-o/57-LIVE-FIRE.md
autonomous: false
requirements: [SIG-01, SIG-04]
user_setup: []

must_haves:
  truths:
    - "A live bot→bot round-trip send lands and the stored signal_outbound.signal_msg_ts is a non-null bigint (SC#1)"
    - "A live quote-threaded send renders a native quote bubble on the Signal client (SC#3)"
    - "The live-fire does not steal the farmer's messages from the live Node alerter (self-send-to-bot pattern OR Node receive loop stopped)"
  artifacts:
    - path: "src/farm-agent/scripts/live_fire_57.py"
      provides: "self-send bot→bot harness: send plain + quote-threaded, then SELECT signal_outbound"
      exports: ["main"]
    - path: ".planning/phases/57-signal-i-o/57-LIVE-FIRE.md"
      provides: "operator runbook for SC#1 + SC#3 live verification"
  key_links:
    - from: "live_fire_57.py"
      to: "SignalClient + outbound_repo"
      via: "real httpx.AsyncClient + real pool to the shared DB"
      pattern: "SignalClient"
---

<objective>
Provide the live-fire verification for SC#1 (round-trip `signal_msg_ts` persisted as non-null bigint) and SC#3 (native quote bubble renders on the client) — the two acceptance shapes that unit tests with mocked httpx cannot prove. RESEARCH A2 flags that the quote payload was only spiked on signal-cli 0.14.2 and the live container is 0.200-dev, so the quote round-trip MUST be a live gate, not just a mock test.

Purpose: prove the ported SignalClient works against the real `signal-cli-rest-api` container + shared DB without stealing the live Node alerter's farmer traffic.
Output: a self-send harness + an operator runbook. This plan is operator-driven (autonomous: false).

SC#1 "receives a reply" interpretation (explicit, not silent): ROADMAP SC#1's "receives a reply" clause is satisfied by (a) the self-send bot→bot message LANDING on the bot device (a real inbound delivery the operator can see), PLUS (b) the Plan-02 unit-level `SignalClient.receive()` coverage proving the receive leg parses envelopes correctly. The live INBOUND drain (a running receive loop consuming farmer traffic) is DEFERRED to Phase-58 live-fire, consistent with RESEARCH A4 and the dual-poller `/v1/receive` drain hazard (A3) — Phase 57 must NOT start a second poller on the farmer-facing account. This is the loose-but-intended reading of SC#1, recorded here so the gate is explicit. The generated 57-LIVE-FIRE.md MUST restate this.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/57-signal-i-o/57-RESEARCH.md
@.planning/phases/57-02-SUMMARY.md
@.planning/phases/57-03-SUMMARY.md

<interfaces>
SignalClient (Plan 02): send(body, *, quote=None, intent=...), receive(...)
outbound_repo.insert_outbound (Plan 01), build_pool (Phase 56)
Phase-50 spike pattern: self-send bot→bot (+59891840205 → +59891840205) to avoid
  competing with the live Node alerter on the farmer-facing /v1/receive drain.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: live_fire_57.py self-send harness</name>
  <read_first>
    - .planning/phases/50-signal-native-quote-threading/50-CONTEXT.md (locked quote shape {timestamp,author,message}; bot→bot self-send spike pattern)
    - .planning/phases/57-signal-i-o/57-RESEARCH.md §"Open Questions" A3 (dual-poller contention — /v1/receive drains the queue; do NOT compete on the farmer account) + A2 (0.200-dev quote shape unverified — this is the gate) + §"Manual-Only Verifications"
    - src/farm-agent/farm_agent/signal_io/client.py (Plan 02 SignalClient)
    - src/farm-agent/farm_agent/persistence/pool.py + outbound_repo.py
  </read_first>
  <action>
    Create src/farm-agent/scripts/live_fire_57.py with async main(): build a real httpx.AsyncClient + a real pool (build_pool from TenantConfig.load()) + outbound_repo; construct a SignalClient with default_target = config.signal_sender (self-send bot→bot, NOT the farmer). Step 1: send a plain message with intent="live_fire_57" to itself; capture the returned timestamp. Step 2: send a SECOND message with quote={timestamp: <the first send's returned ts as a STRING>, author: config.signal_sender, message: "round-trip"} — exercises the int(str(ts)) coercion path against 0.200-dev. Step 3: SELECT id, signal_msg_ts, pg_typeof(signal_msg_ts), intent FROM signal_outbound WHERE intent='live_fire_57' ORDER BY sent_at DESC LIMIT 2 and print; assert signal_msg_ts IS NOT NULL and pg_typeof is bigint. Print a clear PASS/FAIL for SC#1. Do NOT start a receive loop (avoid the dual-poller drain hazard, A3). Guard: refuse to run if TenantConfig points at a non-self default_target (must self-send).
  </action>
  <verify>
    <automated>cd src/farm-agent && python -c "import ast; ast.parse(open('scripts/live_fire_57.py').read())" && echo "syntax ok"</automated>
  </verify>
  <done>live_fire_57.py exists, self-sends bot→bot, sends a quote-threaded second message, and SELECTs/asserts signal_msg_ts is a non-null bigint. Syntax-valid; no receive loop started.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: SC#1 + SC#3 live round-trip on the real container</name>
  <what-built>
    A self-send harness (live_fire_57.py) that sends a plain + a quote-threaded message from the bot to itself through the real signal-cli-rest-api 0.200-dev container, then asserts the persisted signal_msg_ts is a non-null bigint.
  </what-built>
  <how-to-verify>
    1. Ensure no dual-poller contention: either confirm the live Node alerter receive loop is idle for the test window, OR rely on the bot→bot self-send (the harness does not poll). Per RESEARCH A3, do NOT run a second poller on the farmer account.
    2. From the farm-agent context with real env (tenants/mossrock/secrets.env in-process; SIGNAL_SENDER=+59891840205), run: `cd src/farm-agent && uv run python scripts/live_fire_57.py`.
    3. Confirm the harness prints PASS for SC#1: signal_outbound.signal_msg_ts is non-null and pg_typeof == bigint for both rows.
    4. SC#3: on the bot's own Signal client (the +59891840205 device / linked viewer), confirm the SECOND message renders as a NATIVE QUOTE BUBBLE quoting the first message (compare to 50-LIVE-FIRE_ack-quote.jpg). Screenshot it.
    5. If the quote does NOT render natively on 0.200-dev (RESEARCH A2 risk), capture the /v2/send response + payload and flag a shape drift for follow-up before declaring SC#3.
  </how-to-verify>
  <acceptance_criteria>
    - SC#1: `SELECT signal_msg_ts, pg_typeof(signal_msg_ts) FROM signal_outbound WHERE intent='live_fire_57'` returns non-null bigint for both rows.
    - SC#3: a native quote bubble renders on the Signal client (screenshot attached to 57-LIVE-FIRE.md), OR a documented shape-drift finding if 0.200-dev rejects the 0.14.2 payload shape.
    - No farmer message was consumed by the test (self-send only).
  </acceptance_criteria>
  <resume-signal>Type "approved" with the SC#1 result + the SC#3 screenshot/finding, or describe the failure.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| live_fire harness → shared signal-cli container | Shared with the live Node alerter + bridge; the test must not drain the farmer queue |
| live_fire harness → shared prod TimescaleDB | Writes real signal_outbound rows tagged intent='live_fire_57' (cleanable) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-57-04-01 | Denial of Service | dual-poller /v1/receive drain | mitigate | Self-send bot→bot; harness does NOT poll /v1/receive (RESEARCH A3); no contention with the live Node alerter |
| T-57-04-02 | Information Disclosure | live-fire writes to prod DB | accept | Rows tagged intent='live_fire_57', low volume, cleanable; no PII beyond the bot's own number |
| T-57-04-03 | Tampering | wrong default_target sends to a farmer | mitigate | Harness refuses to run unless default_target == signal_sender (self-send guard) |
</threat_model>

<verification>
- live_fire_57.py is syntax-valid and self-send-guarded.
- Operator confirms SC#1 (bigint) + SC#3 (quote bubble) at the checkpoint.
</verification>

<success_criteria>
- SC#1 proven live: signal_msg_ts persisted as non-null bigint via a real round-trip.
- SC#3 proven live: native quote bubble renders on 0.200-dev (or a documented shape-drift finding).
- SC#1 "receives a reply": satisfied by the self-send landing on the bot device + Plan-02 `SignalClient.receive()` unit coverage; the live inbound drain is explicitly deferred to Phase-58 (no second poller on the farmer account here). 57-LIVE-FIRE.md states this interpretation.
- No farmer traffic disrupted.
</success_criteria>

<output>
Create `.planning/phases/57-signal-i-o/57-LIVE-FIRE.md` (operator runbook) and `.planning/phases/57-signal-i-o/57-04-SUMMARY.md` when done.
</output>
