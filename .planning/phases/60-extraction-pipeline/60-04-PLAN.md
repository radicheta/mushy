---
phase: 60-extraction-pipeline
plan: 04
type: execute
wave: 4
depends_on: ["60-03"]
files_modified:
  - src/farm-agent/tests/test_extraction_live_fire.py
  - src/farm-agent/pyproject.toml
autonomous: false
requirements: [XTR-01]
user_setup: []

must_haves:
  truths:
    - "A marker/env-gated real-Sonnet test exists that runs the real extractor against the live May-22 fixture"
    - "The test is skipped by default and NEVER runs in CI (requires ANTHROPIC_API_KEY + EXTRACTION_LIVE_FIRE=1)"
    - "When run, it asserts the real model produces 1 seeding_session / 5 groups / 11 children / correct block names and reports token+cache usage"
  artifacts:
    - path: "src/farm-agent/tests/test_extraction_live_fire.py"
      provides: "operator-run real-Sonnet accuracy validation harness"
      contains: "EXTRACTION_LIVE_FIRE"
    - path: "src/farm-agent/pyproject.toml"
      provides: "pytest marker doc updated for the extraction live-fire env gate"
      contains: "EXTRACTION_LIVE_FIRE"
  key_links:
    - from: "src/farm-agent/tests/test_extraction_live_fire.py"
      to: "farm_agent.extraction.extractor.create_extractor"
      via: "real AsyncAnthropic against the copied fixture"
      pattern: "create_extractor"
---

<objective>
Author the deferred real-Sonnet accuracy harness: a marker/env-gated, operator-run test that drives `create_extractor` against the REAL claude-sonnet-4-6 model on the live 2026-05-22 fixture (real audio transcript + real downscaled photo + text follow-up), asserts the model produces the correct seeding_session draft, and reports token/cache usage. It is skipped by default and never CI-blocking, exactly like the Phase 58/59 live-fires.

Purpose: The hermetic tests (plans 02/03) prove the WIRING (retry, schema, seq-minting, multimodal assembly). This harness proves the model EXTRACTION ACCURACY, which costs real API tokens and must stay operator-gated.
Output: `test_extraction_live_fire.py` and a pyproject marker doc update.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/60-extraction-pipeline/60-CONTEXT.md
@.planning/phases/60-extraction-pipeline/60-RESEARCH.md

<read_first>
<!-- The exact operator-run live-fire pattern to mirror (marker, skipif, real client, finally close) -->
@src/farm-agent/tests/test_gate_live_fire.py
<!-- The extractor under test -->
@src/farm-agent/farm_agent/extraction/extractor.py
<!-- multimodal path->block resolution used to feed the real image -->
@src/farm-agent/farm_agent/extraction/multimodal.py
<!-- pyproject markers block to update -->
@src/farm-agent/pyproject.toml
</read_first>

<interfaces>
From farm_agent.extraction.extractor:
  create_extractor(client, model="claude-sonnet-4-6", max_tokens=16384, ...) -> {"extract": extract}
From farm_agent.extraction.multimodal:
  async read_image_to_base64(image_path, log=None) -> dict   # {ok, data, media_type}
  build_content_blocks(text, transcript, images) -> list[dict]
Fixture dir: src/farm-agent/tests/fixtures/extraction/seeding-session-may22/
  transcript.txt, paper-log.jpg, text-followup.txt, expected-draft.json
Existing pytest marker (pyproject.toml [tool.pytest.ini_options] markers):
  live_fire: real-API operator-run validation; skipped unless ANTHROPIC_API_KEY + GATE_LIVE_FIRE=1
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the marker/env-gated real-Sonnet accuracy harness</name>
  <files>src/farm-agent/tests/test_extraction_live_fire.py, src/farm-agent/pyproject.toml</files>
  <action>Create tests/test_extraction_live_fire.py mirroring tests/test_gate_live_fire.py exactly in shape: module docstring stating OPERATOR-RUN ONLY, skipped by default, never CI; the env gate is ANTHROPIC_API_KEY + EXTRACTION_LIVE_FIRE=1 (NOT GATE_LIVE_FIRE — this is the extraction harness). Decorate with @pytest.mark.live_fire, @pytest.mark.skipif(not ANTHROPIC_API_KEY or not EXTRACTION_LIVE_FIRE, reason=...), and @pytest.mark.asyncio. In the test: build a real anthropic.AsyncAnthropic(api_key=..., max_retries=2); in a try/finally that awaits client.close(): load the fixture transcript.txt and text-followup.txt; call await read_image_to_base64 on paper-log.jpg and assert ok (confirms the real Pillow downscale path); assemble a captures list with one capture carrying text (text-followup content), transcript (transcript content), and images=[the resolved image dict]; call create_extractor(client)["extract"](captures). Assert result["ok"] is True; the resulting draft type is "seeding_session"; len(groups) == 5; total children == 11; the flattened child block names equal exactly 260522_SHI_1..3 + 260522_KOY_4..11. ASSERT CHILD NAMES, NOT KOY PARENT ATTRIBUTION. Then assert per-field provenance present on each group field. Pull usage from result["usage"] and assert cache liveness: cache_creation_input_tokens > 0 OR cache_read_input_tokens > 0 (Pitfall 4 — confirms the large system prompt cleared the Sonnet cache threshold); on failure, fail with a message pointing at prompts.py CACHEABLE_SYSTEM_BLOCKS. print() a report line with the block names, group/child counts, and usage (visible under pytest -v). Use pytest.fail with actionable messages for accuracy mismatches and note "real model-accuracy signal — do NOT relax the assertion; record mismatched names as a finding." Then update src/farm-agent/pyproject.toml: change the existing live_fire marker description to read "real-API operator-run validation; skipped unless ANTHROPIC_API_KEY + (GATE_LIVE_FIRE or EXTRACTION_LIVE_FIRE)=1" (keep the single live_fire marker; the per-test skipif selects the env var). No em-dashes.</action>
  <verify>
    <automated>cd src/farm-agent && uv run pytest -q tests/test_extraction_live_fire.py -m live_fire 2>&1 | grep -Eiq "skip|deselect|no tests ran" && uv run python -c "import ast; ast.parse(open('tests/test_extraction_live_fire.py').read()); print('parses + skipped-by-default ok')"</automated>
  </verify>
  <done>test_extraction_live_fire.py parses, is marked live_fire, and is SKIPPED by default (no API key / EXTRACTION_LIVE_FIRE unset); pyproject marker doc references EXTRACTION_LIVE_FIRE; the harness asserts the 5-group/11-child/block-name/provenance shape + cache liveness when run by the operator.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Operator acknowledgement that the deferred live-fire is operator-run, not CI-blocking</name>
  <files>(none — acknowledgement checkpoint)</files>
  <what-built>The real-Sonnet accuracy harness exists but is intentionally skipped in CI and in this autonomous run. Running it spends real Anthropic tokens (one Sonnet call against a ~21KB+ cached system prompt + a downscaled photo) and proves model accuracy, which the hermetic tests cannot.</what-built>
  <action>This is an acknowledgement checkpoint, not an implementation step. No files change. The executor pauses for the operator to acknowledge the deferral and optionally trigger the run.</action>
  <how-to-verify>
    Confirm you understand the deferral:
    1. The Phase 60 hermetic suite (plans 01-03) is the CI ship-gate and is fully green.
    2. The real-Sonnet run is DEFERRED and operator-run, exactly like the Phase 58/59 live-fires.
    3. To run it later: `export ANTHROPIC_API_KEY=<key>; export EXTRACTION_LIVE_FIRE=1; cd src/farm-agent && uv run pytest -q tests/test_extraction_live_fire.py -m live_fire -v` and record token/cache usage + any name mismatch as a finding.
    Type "acknowledged" to close the plan, or "run now" if you want to spend the tokens this session.
  </how-to-verify>
  <resume-signal>Type "acknowledged" (defer) or "run now".</resume-signal>
  <done>Operator has acknowledged the deferred-live-fire status; CI gate is the hermetic suite only.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator-supplied ANTHROPIC_API_KEY to real Anthropic API | Real key + real tokens cross only under the explicit env gate |
| Real fixture photo/transcript to Anthropic | Real farmer content sent only when the operator opts in |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-60-04-01 | Information Disclosure | real API key in test | mitigate | Key read from env only; never hard-coded, never logged; client closed in finally |
| T-60-04-02 | Denial of Service | unintended real API spend | mitigate | Double-gated: @pytest.mark.live_fire AND skipif on ANTHROPIC_API_KEY + EXTRACTION_LIVE_FIRE; skipped in CI and in this run |
| T-44-04-01 | Tampering | real farmer content to LLM | accept | Same prompt-injection posture as plan 03 (content in messages[], not system); operator-run only |
</threat_model>

<verification>
`cd src/farm-agent && uv run pytest -q` — the live-fire test is collected but SKIPPED (no env gate set); the rest of the suite stays green. The harness only executes under the operator's explicit env vars.
</verification>

<success_criteria>
- test_extraction_live_fire.py exists, parses, and is skipped by default (never CI-blocking).
- When run with ANTHROPIC_API_KEY + EXTRACTION_LIVE_FIRE=1, it asserts the real-Sonnet draft shape (5 groups / 11 children / correct block names / provenance) and cache liveness, and reports usage.
- pyproject marker doc references EXTRACTION_LIVE_FIRE.
- Operator has acknowledged the deferral.
</success_criteria>

<output>
Create `.planning/phases/60-extraction-pipeline/60-04-SUMMARY.md` when done.
</output>
