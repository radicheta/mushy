---
phase: 60-extraction-pipeline
plan: 03
type: execute
wave: 3
depends_on: ["60-01", "60-02"]
files_modified:
  - src/farm-agent/farm_agent/extraction/extractor.py
  - src/farm-agent/farm_agent/boot.py
  - src/farm-agent/tests/extraction/test_extractor.py
  - src/farm-agent/tests/extraction/test_extraction_fixture.py
autonomous: true
requirements: [XTR-01, XTR-02, XTR-03]
user_setup: []

must_haves:
  truths:
    - "create_extractor returns {extract} and never throws — every failure path returns {ok:False, reason}"
    - "The first user turn opens with a tool_result closing tu_fewshot_6 (Anthropic 400s without it)"
    - "A schema-invalid first response triggers ONE retry with a tool_result is_error=True + matching tool_use_id; resolves on retry"
    - "A second schema-invalid response returns {ok:False, reason:schema_invalid, raw_first, raw_retry} — needs_review, not an exception (exactly 2 LLM calls)"
    - "Replaying the May-22 fixture yields one seeding_session, 5 groups, 11 children, block names 260522_SHI_1..3 + 260522_KOY_4..11, with per-field provenance"
    - "timeout is passed via client.with_options(timeout=...), never as a messages.create body kwarg"
    - "The extractor's Submission JSON schema still passes the FND-04 structural-parity gate"
    - "boot.py injects the shared AsyncAnthropic singleton into create_extractor (no second client)"
  artifacts:
    - path: "src/farm-agent/farm_agent/extraction/extractor.py"
      provides: "create_extractor factory, build_initial_user_content, find_tool_use_block, 2-call retry, pack_result"
      contains: "submit_extraction"
    - path: "src/farm-agent/tests/extraction/test_extractor.py"
      provides: "happy-path / retry-resolves / terminal-failure / SDK-error / no-tool-use / call-shape tests"
    - path: "src/farm-agent/tests/extraction/test_extraction_fixture.py"
      provides: "May-22 fixture replay: 5 groups / 11 children / block names / provenance"
    - path: "src/farm-agent/farm_agent/boot.py"
      provides: "create_extractor wired with the shared anthropic_client singleton"
      contains: "create_extractor"
  key_links:
    - from: "src/farm-agent/farm_agent/extraction/extractor.py"
      to: "src/farm-agent/farm_agent/extraction/schemas/submission.py"
      via: "Submission.model_validate(block.input) + SUBMISSION_JSON_SCHEMA as input_schema"
      pattern: "Submission\\.model_validate"
    - from: "src/farm-agent/farm_agent/extraction/extractor.py"
      to: "src/farm-agent/farm_agent/extraction/prompts.py"
      via: "CACHEABLE_SYSTEM_BLOCKS + cacheable_few_shot()"
      pattern: "cacheable_few_shot"
    - from: "src/farm-agent/farm_agent/boot.py"
      to: "create_extractor"
      via: "shared anthropic_client singleton injection"
      pattern: "create_extractor\\(client=anthropic_client"
---

<objective>
Build the extractor itself: `create_extractor` (a never-throws factory mirroring the Phase-59 classifier), assemble the multimodal user turn with the mandatory tu_fewshot_6 closer, implement Node's exact 2-call retry (initial + one retry on schema-invalid, terminal failure → needs_review never an exception), wire it into `boot.py` on the shared AsyncAnthropic singleton, and prove the May-22 fixture replay (5 groups / 11 children / block names / provenance) plus re-verify the FND-04 schema-parity gate against the real extractor's Submission schema.

Purpose: This closes XTR-01/02/03 end-to-end (using the prompts from plan 01 and the leaf helpers from plan 02). The fixture replay and parity re-verify are the ROADMAP success-criteria gates (SC-1, SC-2, SC-4).
Output: `extractor.py`, the boot wiring, and two test files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/60-extraction-pipeline/60-CONTEXT.md
@.planning/phases/60-extraction-pipeline/60-RESEARCH.md
@.planning/phases/60-extraction-pipeline/60-PATTERNS.md

<read_first>
<!-- Node source of truth for the call flow + 2-call retry + pack_result + buildInitialUserContent -->
@src/agents/alerter/src/extraction/extractor.js
@src/agents/alerter/src/extraction/validator.js
<!-- Python factory blueprint to mirror (never-throws, with_options, find_tool_use_block) -->
@src/farm-agent/farm_agent/gate/classifier.py
<!-- Existing schemas to import (do NOT modify) -->
@src/farm-agent/farm_agent/extraction/schemas/submission.py
<!-- Boot wiring site (gate injection is the analog) -->
@src/farm-agent/farm_agent/boot.py
<!-- FND-04 parity test to re-verify -->
@src/farm-agent/tests/test_schema_parity.py
</read_first>

<interfaces>
From farm_agent/extraction/schemas/submission.py (already ported — do NOT modify):
  Submission                 # model_validate(block.input) target; raises ValidationError
  SUBMISSION_JSON_SCHEMA     # Submission.model_json_schema(); pass DIRECTLY as tool input_schema (pydantic v2 emits type:object at root — no inlineTopLevelRef)
  DraftSubmission            # each element of Submission.drafts (list, min_length=1)
  # Submission.continuity, Submission.continuity_reason, Submission.capture_kind exist
  # DraftSubmission.draft is a discriminated union; DraftSubmission.per_field_confidence exists

From farm_agent/extraction/prompts.py (plan 01):
  CACHEABLE_SYSTEM_BLOCKS    # system=... with ephemeral cache_control
  cacheable_few_shot()       # list of few-shot turns; last assistant turn's tool_use id is "tu_fewshot_6"

From farm_agent/extraction/multimodal.py (plan 02):
  build_content_blocks(text, transcript, images) -> list[dict]   # images = resolved {data, media_type} dicts

Extractor public surface (port of extractor.js):
  TOOL_NAME = "submit_extraction"
  create_extractor(client, model="claude-sonnet-4-6", max_tokens=16384, on_llm_call=None, log=None) -> {"extract": extract}
  async extract(captures, in_flight_draft=None, corpus_context=None, farmer_correction=None) -> dict
    # returns {ok:True, drafts, continuity_decision, continuity_reason, draft, per_field_confidence, capture_kind, usage}
    #      or {ok:False, reason:...}  (reason in: api error str | "no_tool_use_in_response" | "schema_invalid")

From tests.conftest (plan 01):
  FakeAnthropicClientForExtractor(responses=[{"tool_input":{...}} | {"raise": exc}, ...])
    # .calls records each create() kwargs; block.id = f"tu_call_{N}" per call
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement extractor.py (factory, tu_fewshot_6 closer, 2-call retry, pack_result)</name>
  <files>src/farm-agent/farm_agent/extraction/extractor.py, src/farm-agent/tests/extraction/test_extractor.py</files>
  <behavior>
    - SC-1 happy path: one valid tool_use → {ok:True}, len(drafts)==1, continuity_decision present, exactly 1 call
    - SC-2 retry resolves: first call invalid (missing required field), second valid → {ok:True}, len(client.calls)==2, the second call's messages include a tool_result block with is_error True and tool_use_id == "tu_call_0" (the first call's block.id)
    - SC-3 terminal failure: both calls invalid → {ok:False, reason "schema_invalid"}, "raw_first" and "raw_retry" present, NO exception, exactly 2 calls
    - SC-4 SDK error: create() raises → {ok:False}, reason contains the error text, no exception propagated
    - SC-5 no tool_use: response content has no submit_extraction block → {ok:False, reason "no_tool_use_in_response"}
    - call shape: tool_choice == {"type":"tool","name":"submit_extraction"}; "timeout" NOT in any recorded create() kwargs; system[0]["cache_control"] == {"type":"ephemeral"}; the FIRST block of the user-turn content is a tool_result with tool_use_id "tu_fewshot_6"
  </behavior>
  <action>Create farm_agent/extraction/extractor.py mirroring farm_agent/gate/classifier.py structure and src/agents/alerter/src/extraction/extractor.js behavior. Imports: anthropic; pydantic ValidationError; CACHEABLE_SYSTEM_BLOCKS and cacheable_few_shot from farm_agent.extraction.prompts; build_content_blocks from farm_agent.extraction.multimodal; Submission and SUBMISSION_JSON_SCHEMA from farm_agent.extraction.schemas.submission. Constants: TOOL_NAME = "submit_extraction", a TOOL_DESCRIPTION string, and build_tool_spec() returning {name:TOOL_NAME, description:..., input_schema:SUBMISSION_JSON_SCHEMA} (pass the schema DIRECTLY — pydantic v2 already emits type:object at root, no inlineTopLevelRef). Implement find_tool_use_block(resp) via attribute access (block.type=="tool_use" and block.name==TOOL_NAME) exactly like classifier.py (Pitfall 7). Implement build_initial_user_content(captures, in_flight_draft, corpus_context, farmer_correction): FIRST block MUST be {"type":"tool_result","tool_use_id":"tu_fewshot_6","content":[{"type":"text","text":"accepted"}]} (Pitfall 8 — Anthropic 400s without it); then a corpus_context text block if corpus_context is non-null; then an in-flight draft text block ("In-flight draft: none" when None); then a farmer_correction text block if non-empty/trimmed; then for each capture extend with build_content_blocks(text=cap text, transcript=cap transcript, images=cap images). Implement sum_usage(list) summing input_tokens/output_tokens/cache_creation_input_tokens/cache_read_input_tokens (attribute access, default 0). Implement pack_result(submission, usage) → {ok:True, drafts:submission.drafts, continuity_decision:submission.continuity, continuity_reason:submission.continuity_reason, draft:(first draft or None), per_field_confidence:(first per_field_confidence or None), capture_kind:submission.capture_kind, usage}. Implement call_with_observer(req, capture_id) that calls client.with_options(timeout=_timeout_s).messages.create(**req) and, if on_llm_call is set, invokes it — accept BOTH sync and coroutine callables (if inspect.iscoroutine(result) or asyncio.iscoroutinefunction(on_llm_call), await it) — wrapping the observer invocation in its own try/except that logs a WARNING and never propagates (mirror Node "observer errors caught + warn"). Factory create_extractor(client, model="claude-sonnet-4-6", max_tokens=16384, on_llm_call=None, log=None) -> {"extract": extract}. The inner async extract(captures, in_flight_draft=None, corpus_context=None, farmer_correction=None): wrap the WHOLE body in try/except Exception → WARNING + {ok:False, reason:str(e)} (never raise). Build messages = [*cacheable_few_shot(), {"role":"user","content":build_initial_user_content(...)}]; base_req = {model, max_tokens, system:CACHEABLE_SYSTEM_BLOCKS, tools:[build_tool_spec()], tool_choice:{"type":"tool","name":TOOL_NAME}, messages}. Call resp = await call_with_observer(base_req, capture_id) — on SDK exception the outer try returns {ok:False}. block = find_tool_use_block(resp); if None → {ok:False, reason:"no_tool_use_in_response"}. Try Submission.model_validate(block.input); on ValidationError as e: build assistant_turn = {"role":"assistant","content":resp.content} and retry_user_turn = {"role":"user","content":[{"type":"tool_result","tool_use_id":block.id,"is_error":True,"content":str(e)}]} (block.id NOT block.name — Pitfall 1); retry_req = {**base_req, "messages":[*messages, assistant_turn, retry_user_turn]}; resp2 = await call_with_observer(retry_req, capture_id); block2 = find_tool_use_block(resp2); if None → {ok:False, reason:"no_tool_use_in_response"}; try Submission.model_validate(block2.input) except ValidationError as e2: return {ok:False, reason:"schema_invalid", errors:e2.errors(), raw_first:block.input, raw_retry:block2.input} (MUST be caught — Pitfall 5, never propagate). On success pack_result with sum_usage of both calls' usage. The timeout default: use a module-level extractor timeout (Node uses a longer timeout than the gate — e.g. 60_000 ms; mirror the Node value from extractor.js) passed via with_options only (Pitfall 9 — NEVER a body kwarg). No api_key reference anywhere; no farmer text/transcript/image data in any log line. Then write tests/extraction/test_extractor.py covering SC-1..SC-5 + call-shape using FakeAnthropicClientForExtractor; build the valid tool_input by validating the fixture (load expected-draft.json wrapped in a Submission envelope) OR a minimal valid Submission dict, and an invalid one by dropping a required field.</action>
  <verify>
    <automated>cd src/farm-agent && uv run pytest -q tests/extraction/test_extractor.py -x</automated>
  </verify>
  <done>extractor.py exposes create_extractor; never raises; first user block is the tu_fewshot_6 closer; the 2-call retry pairs tool_use_id correctly and resolves on retry; terminal failure returns schema_invalid with raw_first/raw_retry; timeout never appears in create() kwargs; all test_extractor.py tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: May-22 fixture replay test + FND-04 parity re-verify</name>
  <files>src/farm-agent/tests/extraction/test_extraction_fixture.py</files>
  <action>Create tests/extraction/test_extraction_fixture.py. Load tests/fixtures/extraction/seeding-session-may22/expected-draft.json. Two checks. (a) Direct schema parity: validate the fixture's draft payload through Submission.model_validate (wrapping in the Submission envelope shape if expected-draft.json is a bare draft — inspect its top-level keys; the fixture is the locked anchor, adapt the wrapper, not the fixture) and assert it validates clean — this proves the expected draft conforms to the real ported schema. (b) Extractor replay: construct FakeAnthropicClientForExtractor with a single response whose tool_input is the valid Submission dict built from the fixture; call create_extractor(client)["extract"] with a captures list referencing the fixture's modalities; assert result["ok"] is True, the resulting draft type is "seeding_session", len(groups) == 5, the total child count across groups == 11, and the flattened child block names equal exactly ["260522_SHI_1","260522_SHI_2","260522_SHI_3","260522_KOY_4","260522_KOY_5","260522_KOY_6","260522_KOY_7","260522_KOY_8","260522_KOY_9","260522_KOY_10","260522_KOY_11"]. Assert per-field provenance is present on each group field (the Provenanced wrapper exposes value/confidence/sources). ASSERT CHILD BLOCK NAMES, NOT KOY PARENT ATTRIBUTION (the parent is intentionally ambiguous in the source audio — 260118_KOY_12 vs 260425_KOY_4; do not assert on it). Also re-run the FND-04 gate: import the parity-check from tests/test_schema_parity.py (or replicate its structural-diff assertion) against SUBMISSION_JSON_SCHEMA as exported and consumed by the extractor's build_tool_spec(), asserting the structural diff is clean (SC-4). If test_schema_parity.py already covers SUBMISSION_JSON_SCHEMA, a thin assertion that build_tool_spec()["input_schema"] is SUBMISSION_JSON_SCHEMA (identity) plus a call to the existing parity helper suffices — do not duplicate the full diff logic.</action>
  <verify>
    <automated>cd src/farm-agent && uv run pytest -q tests/extraction/test_extraction_fixture.py tests/test_schema_parity.py -x</automated>
  </verify>
  <done>The fixture replay asserts 1 seeding_session / 5 groups / 11 children / the exact 260522_SHI_1..3 + 260522_KOY_4..11 names / per-field provenance present; the FND-04 parity gate re-verifies clean against the extractor's Submission schema; both files pass.</done>
</task>

<task type="auto">
  <name>Task 3: Wire create_extractor into boot.py on the shared AsyncAnthropic singleton</name>
  <files>src/farm-agent/farm_agent/boot.py</files>
  <action>In src/farm-agent/farm_agent/boot.py, add `from farm_agent.extraction.extractor import create_extractor` to the imports near the gate import. After the existing `gate = create_event_gate(...)` block, add `extractor = create_extractor(client=anthropic_client)` — reuse the SAME anthropic_client singleton (do NOT construct a second AsyncAnthropic; T-56-06-01 keeps the api_key in the one constructor). Pass `extractor` into the capture pipeline constructor: update the `create_capture_pipeline(...)` call to also pass `extractor=extractor` (add the kwarg; the pipeline's consumption of the extractor and the full state-machine integration is Phase 61 scope — this plan only injects it so the singleton is shared and available). If create_capture_pipeline does not yet accept an extractor kwarg, add an optional `extractor=None` parameter to its signature without changing existing behavior (a minimal, additive wiring seam — no extraction is invoked from the pipeline yet). Surgical change only: do not refactor adjacent boot logic. No em-dashes in comments.</action>
  <verify>
    <automated>cd src/farm-agent && uv run python -c "import ast,sys; src=open('farm_agent/boot.py').read(); assert 'create_extractor' in src and 'extractor = create_extractor(client=anthropic_client)' in src.replace('  ',' ') or 'create_extractor(client=anthropic_client' in src; ast.parse(src); print('boot wiring ok')" && uv run pytest -q tests/test_boot.py -x</automated>
  </verify>
  <done>boot.py imports and calls create_extractor with the shared anthropic_client singleton, passes extractor into the capture pipeline, parses clean, and tests/test_boot.py stays green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| farmer text / transcript / image → Anthropic | Untrusted farmer content crosses into the LLM via messages[] only |
| LLM tool_use.input → pydantic | Untrusted model output validated before use |
| anthropic_api_key → process | Secret held only by the injected AsyncAnthropic constructor |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-44-04-01 | Tampering | build_initial_user_content | mitigate | Farmer text/transcript/image enter via the user turn in messages[], NEVER concatenated into CACHEABLE_SYSTEM_BLOCKS (system prompt stays static) |
| T-60-03-01 | Tampering | Submission.model_validate | mitigate | extra='forbid' on every nested model; invalid tool_use.input triggers retry then {ok:False}, never trusted raw |
| T-56-06-01 | Information Disclosure | anthropic api_key | mitigate | Injected client owns the key; extractor never references or logs it; one shared singleton in boot.py |
| T-59-02-01 | Information Disclosure | WARNING logs / observer errors | mitigate | Log only exception/reason strings; never farmer text, transcript, image data, or block.input contents |
| T-60-03-02 | Denial of Service | LLM call hang | mitigate | Per-request timeout via client.with_options(timeout=...); never-throws fail-open returns {ok:False} on any error |
</threat_model>

<verification>
Run `cd src/farm-agent && uv run pytest -q` — full suite green, including test_extractor.py, test_extraction_fixture.py, test_schema_parity.py (FND-04 re-verify), and test_boot.py.
</verification>

<success_criteria>
- create_extractor never throws; first user block is the tu_fewshot_6 closer; timeout is never a body kwarg.
- 2-call retry: schema-invalid first → one retry with tool_result is_error=True + matching tool_use_id → resolves; second failure → {ok:False, reason:schema_invalid, raw_first, raw_retry} (exactly 2 calls).
- May-22 fixture replay: 1 seeding_session, 5 groups, 11 children, names 260522_SHI_1..3 + 260522_KOY_4..11, per-field provenance present (child names asserted, not parent attribution).
- FND-04 parity gate re-verifies clean against the extractor's Submission schema (SC-4).
- boot.py shares the single AsyncAnthropic singleton with the extractor; full suite green.
</success_criteria>

<output>
Create `.planning/phases/60-extraction-pipeline/60-03-SUMMARY.md` when done.
</output>
