---
phase: 60-extraction-pipeline
plan: 02
type: execute
wave: 2
depends_on: ["60-01"]
files_modified:
  - src/farm-agent/farm_agent/extraction/multimodal.py
  - src/farm-agent/farm_agent/extraction/seq_helper.py
  - src/farm-agent/tests/extraction/test_multimodal.py
  - src/farm-agent/tests/extraction/test_seq_helper.py
autonomous: true
requirements: [XTR-02, XTR-03]
user_setup: []

must_haves:
  truths:
    - "A missing or unreadable image is skipped with a WARNING and never aborts extraction (fail-open)"
    - "An image over ~1.15MP or ~5MB is downscaled and re-encoded to JPEG; RGBA/LA/P modes convert to RGB first"
    - "The media_type returned reflects the post-downscale format (image/jpeg after re-encode), not the original"
    - "mint_child_block_names validates every name with re.fullmatch(BLOCK_NAME_RE); 260522_SHI_1_EXTRA is rejected"
    - "The per-session SEQ counter spans species (KOY starts at 4 in the May-22 fixture, not 1)"
    - "lookup_last_seq_for_date walks both legacy seeding.block_name and seeding_session.groups[].child_block_names, skip-on-error per row"
  artifacts:
    - path: "src/farm-agent/farm_agent/extraction/multimodal.py"
      provides: "mime_from_path, downscale_if_needed, read_image_to_base64, build_content_blocks"
      contains: "downscale_if_needed"
    - path: "src/farm-agent/farm_agent/extraction/seq_helper.py"
      provides: "yyyymmdd_to_yymmdd, mint_child_block_names, seq_of, extract_seqs_from_row, lookup_last_seq_for_date"
      contains: "re.fullmatch"
    - path: "src/farm-agent/tests/extraction/test_multimodal.py"
      provides: "downscale/RGBA/fail-open/media_type unit tests"
    - path: "src/farm-agent/tests/extraction/test_seq_helper.py"
      provides: "mint + fullmatch-reject + per-session lookup tests"
  key_links:
    - from: "src/farm-agent/farm_agent/extraction/seq_helper.py"
      to: "src/farm-agent/farm_agent/extraction/schemas/seeding.py"
      via: "import BLOCK_NAME_RE"
      pattern: "from farm_agent.extraction.schemas.seeding import BLOCK_NAME_RE"
    - from: "src/farm-agent/farm_agent/extraction/multimodal.py"
      to: "PIL.Image"
      via: "downscale + RGB convert + JPEG re-encode"
      pattern: "Image\\.(open|resize)"
---

<objective>
Port the two pure/leaf extraction modules from Node: `multimodal.py` (image path → base64 content blocks with Pillow downscale + fail-open) and `seq_helper.py` (B5 block-name minting + per-session SEQ lookup), with their hermetic unit tests. These have no dependency on the extractor or the LLM and can be built and tested in isolation.

Purpose: XTR-03 (B5 minting with anchored fullmatch) and the image-assembly half of XTR-02 live entirely in these two leaf modules. Building them in Wave 2 lets the extractor (Wave 3) import finished, tested helpers.
Output: `multimodal.py`, `seq_helper.py`, and their two test files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/60-extraction-pipeline/60-CONTEXT.md
@.planning/phases/60-extraction-pipeline/60-RESEARCH.md
@.planning/phases/60-extraction-pipeline/60-PATTERNS.md

<read_first>
<!-- Node sources of truth -->
@src/agents/alerter/src/extraction/multimodal.js
@src/agents/alerter/src/extraction/seq-helper.js
<!-- Existing schema providing BLOCK_NAME_RE (do NOT modify) -->
@src/farm-agent/farm_agent/extraction/schemas/seeding.py
<!-- Python test-structure analog -->
@src/farm-agent/tests/test_gate_classifier.py
</read_first>

<interfaces>
From farm_agent/extraction/schemas/seeding.py:
  BLOCK_NAME_RE = r"^[0-9]{6}_[A-Z]{2,4}_[0-9]+$"   # use re.fullmatch, NOT re.match/search

multimodal.py public surface (port of multimodal.js):
  MAX_BYTES = 5 * 1024 * 1024
  MAX_PIXELS = 1_150_000
  mime_from_path(p: str) -> str                      # .jpg/.jpeg→image/jpeg, .png→image/png, else application/octet-stream
  downscale_if_needed(buf: bytes, media_type: str) -> tuple[bytes, str]
  async read_image_to_base64(image_path: str, log=None) -> dict   # {ok, data, media_type} | {ok:False, reason}
  build_content_blocks(text, transcript, images) -> list[dict]     # images = already-resolved {data, media_type} dicts

seq_helper.py public surface (port of seq-helper.js):
  yyyymmdd_to_yymmdd(event_date: str) -> str         # "2026-05-22" -> "260522"; raises ValueError on bad input
  mint_child_block_names(event_date_yymmdd, species_code, start_seq, qty) -> list[str]
  seq_of(block_name: str) -> int | None              # None for "NEEDS_SEQ" / non-matching
  extract_seqs_from_row(draft_json: dict) -> list[int]
  async lookup_last_seq_for_date(pool, event_date, log=None) -> dict  # {ok, last_seq, source} | {ok:False, reason}
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Port multimodal.py (Pillow downscale + fail-open image assembly)</name>
  <files>src/farm-agent/farm_agent/extraction/multimodal.py, src/farm-agent/tests/extraction/test_multimodal.py</files>
  <behavior>
    - mime_from_path: "photo.jpg" and "x.JPEG" → image/jpeg; "y.PNG" → image/png; "z.tiff" → application/octet-stream
    - downscale_if_needed on the 900x1600 paper-log.jpg (1.44MP > 1.15MP) returns media_type image/jpeg and a strictly smaller byte buffer
    - downscale_if_needed on a small in-memory 100x100 RGBA PNG returns JPEG bytes with no exception (RGB convert before JPEG save)
    - downscale_if_needed on a small under-threshold JPEG returns the buffer unchanged with its original media_type
    - read_image_to_base64 on a nonexistent path returns {ok: False, ...} with no exception raised
    - build_content_blocks(text="hello", transcript=None, images=[]) → one text block; (text=None, transcript="words") → one "Transcript: words" text block; (images=[{data:"abc","media_type":"image/jpeg"}]) → one image block with source.type base64
  </behavior>
  <action>Create farm_agent/extraction/multimodal.py mirroring src/agents/alerter/src/extraction/multimodal.js (Foray island — only stdlib + PIL imports, no other farm_agent package). Define MAX_BYTES = 5*1024*1024 and MAX_PIXELS = 1_150_000. Implement mime_from_path using pathlib suffix lowercased. Implement downscale_if_needed(buf, media_type): open via Image.open(io.BytesIO(buf)); if len(buf) <= MAX_BYTES and w*h <= MAX_PIXELS return (buf, media_type) unchanged; else compute scale = sqrt(MAX_PIXELS / (w*h)), resize via img.resize((new_w, new_h), Image.LANCZOS) with max(1, ...) floors, convert to RGB if img.mode in (RGBA, LA, P) BEFORE saving, save to BytesIO as JPEG quality=85, and return (out.getvalue(), "image/jpeg"). Implement async read_image_to_base64(image_path, log=None): try read bytes via Path(image_path).read_bytes(); media_type = mime_from_path(path); if media_type in (image/jpeg, image/png) call downscale_if_needed and use the RETURNED media_type (Pitfall 2 — always the post-downscale type, never the original); base64.b64encode(buf).decode("ascii"); return {ok:True, data, media_type}; on ANY exception log a WARNING via (log or logger) with only the exception/reason (never image bytes) and return {ok:False, reason:str(e)} — fail-open, never raise (D-03/D-04). Implement build_content_blocks(text, transcript, images): text block if text.strip(); a text block "Transcript: {transcript}" if transcript.strip(); for each img in (images or []) with a truthy data, append {type:image, source:{type:base64, media_type:img.get("media_type") or "image/jpeg", data:img["data"]}}. Then write tests/extraction/test_multimodal.py covering the behaviors above; use the copied paper-log.jpg fixture for the real-downscale assertion and build a 100x100 RGBA PNG in-memory for the RGBA path.</action>
  <verify>
    <automated>cd src/farm-agent && uv run pytest -q tests/extraction/test_multimodal.py -x</automated>
  </verify>
  <done>multimodal.py exposes the four public functions; all test_multimodal.py tests pass including the real paper-log.jpg downscale (media_type image/jpeg, smaller buffer), the RGBA→JPEG path, and fail-open on a missing path.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Port seq_helper.py (B5 minting + per-session SEQ lookup)</name>
  <files>src/farm-agent/farm_agent/extraction/seq_helper.py, src/farm-agent/tests/extraction/test_seq_helper.py</files>
  <behavior>
    - yyyymmdd_to_yymmdd("2026-05-22") == "260522"; "bad-input" raises ValueError
    - mint_child_block_names("260522","SHI",1,3) == ["260522_SHI_1","260522_SHI_2","260522_SHI_3"]
    - mint_child_block_names("260522","KOY",4,4) == ["260522_KOY_4".."260522_KOY_7"] (per-session counter; KOY starts at 4)
    - mint_child_block_names("260522","shi",1,1) raises ValueError (lowercase fails BLOCK_NAME_RE fullmatch)
    - re.fullmatch(BLOCK_NAME_RE, "260522_SHI_1_EXTRA") is None; re.fullmatch(BLOCK_NAME_RE, "260522_SHI_1") is not None
    - seq_of("260522_KOY_4") == 4; seq_of("NEEDS_SEQ") is None; seq_of("garbage") is None
    - extract_seqs_from_row({"type":"seeding","block_name":"260522_SHI_1"}) == [1]
    - extract_seqs_from_row({"type":"seeding_session","groups":[{"child_block_names":{"value":["260522_KOY_4","260522_KOY_5"]}}]}) == [4,5]
    - extract_seqs_from_row on a malformed row returns a partial list, never raises
  </behavior>
  <action>Create farm_agent/extraction/seq_helper.py mirroring src/agents/alerter/src/extraction/seq-helper.js. Import BLOCK_NAME_RE from farm_agent.extraction.schemas.seeding (this single intra-package import is allowed; it stays inside the extraction Foray island). Define EVENT_DATE_RE = re.compile(r"^(\d{4})-(\d{2})-(\d{2})$"). Implement yyyymmdd_to_yymmdd(event_date): match via EVENT_DATE_RE; raise ValueError on no match; return f"{m.group(1)[2:]}{m.group(2)}{m.group(3)}". Implement mint_child_block_names(event_date_yymmdd, species_code, start_seq, qty): build qty consecutive names f"{event_date_yymmdd}_{species_code}_{start_seq+i}", validate EACH with re.fullmatch(BLOCK_NAME_RE, name) (NOT re.match — 260522_SHI_1_EXTRA must be rejected), raise ValueError(f"mint_invalid_block_name: {name}") on failure, return the list. Implement seq_of(block_name): return None for "NEEDS_SEQ"; return None if re.fullmatch(BLOCK_NAME_RE, block_name) is None; else parse the integer after the last underscore (return None if not a finite int). Implement extract_seqs_from_row(draft_json): if type == "seeding" parse draft_json["block_name"] via seq_of; if type == "seeding_session" walk groups[].child_block_names — handle both the Provenanced shape {"value": [...]} and a bare list — collecting seq_of of each; wrap per-row in try/except so a single malformed draft yields a partial list and never raises; "NEEDS_SEQ" sentinel is excluded by seq_of. Implement async lookup_last_seq_for_date(pool, event_date, log=None): query signal_draft WHERE status IN ('committed','awaiting_farmer','confirmed','pending') AND draft_json->>'event_date' = $1 using psycopg3 async syntax (mirror the existing repo query style under farm_agent/persistence or capture); for each row call extract_seqs_from_row, collect the MAX SEQ across all rows; return {ok:True, last_seq, source} (last_seq None when no rows); on ANY exception log a WARNING (reason only) and return {ok:False, reason:str(e)}. Then write tests/extraction/test_seq_helper.py covering all behaviors above as pure tests; mark lookup_last_seq_for_date DB tests with skipif when no test DB pool is available (mirror existing persistence-test skip pattern), OR cover extract_seqs_from_row directly without a pool (preferred — it is the pure core lookup walks).</action>
  <verify>
    <automated>cd src/farm-agent && uv run pytest -q tests/extraction/test_seq_helper.py -x</automated>
  </verify>
  <done>seq_helper.py exposes the public functions; re.fullmatch rejects 260522_SHI_1_EXTRA; KOY minting starts at 4; extract_seqs_from_row walks both legacy and session shapes skip-on-error; all test_seq_helper.py tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Capture-pipeline image paths → multimodal.py | Image paths originate from CAP-01 (already validated); multimodal.py constructs no paths |
| LLM-emitted block names → seq_helper.py | Block names from draft_json are untrusted; every minted name is fullmatch-validated |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-60-02-01 | Tampering | mint_child_block_names | mitigate | re.fullmatch(BLOCK_NAME_RE) anchors full string; 260522_SHI_1_EXTRA rejected (re.match would pass it) |
| T-60-02-02 | Denial of Service | read_image_to_base64 on bad/huge image | mitigate | Fail-open: skip the block + WARNING (reason only), never abort; downscale caps pixels/bytes |
| T-60-02-03 | Information Disclosure | WARNING logs on image failure | mitigate | Log only the path + reason string, never image bytes or farmer content (T-59-02-01 pattern) |
| T-60-02-04 | Tampering | extract_seqs_from_row on malformed draft | mitigate | skip-on-error per row; one bad draft cannot crash the SEQ lookup |
</threat_model>

<verification>
Run `cd src/farm-agent && uv run pytest -q tests/extraction/test_multimodal.py tests/extraction/test_seq_helper.py` — both green. Then `uv run pytest -q` to confirm no regression.
</verification>

<success_criteria>
- multimodal.py downscales the 900x1600 fixture to JPEG with a smaller buffer and the correct (post-downscale) media_type.
- A missing image path returns {ok:False} with a WARNING, never an exception.
- mint_child_block_names uses re.fullmatch; lowercase species and trailing _EXTRA are rejected; KOY minting starts at 4 (per-session counter).
- extract_seqs_from_row walks both legacy seeding.block_name and seeding_session.groups[].child_block_names, skip-on-error.
- Both test files pass; full suite stays green.
</success_criteria>

<output>
Create `.planning/phases/60-extraction-pipeline/60-02-SUMMARY.md` when done.
</output>
