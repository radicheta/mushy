---
phase: 60-extraction-pipeline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/farm-agent/pyproject.toml
  - src/farm-agent/farm_agent/extraction/prompts.py
  - src/farm-agent/farm_agent/extraction/__init__.py
  - src/farm-agent/tests/conftest.py
  - src/farm-agent/tests/extraction/__init__.py
  - src/farm-agent/tests/fixtures/extraction/seeding-session-may22/transcript.txt
  - src/farm-agent/tests/fixtures/extraction/seeding-session-may22/paper-log.jpg
  - src/farm-agent/tests/fixtures/extraction/seeding-session-may22/text-followup.txt
  - src/farm-agent/tests/fixtures/extraction/seeding-session-may22/expected-draft.json
autonomous: false
requirements: [XTR-01]
user_setup: []

must_haves:
  truths:
    - "Pillow is a declared runtime dependency, legitimacy-confirmed before install"
    - "The Node extraction system prompt + few-shot turns exist verbatim in Python with cache_control ephemeral"
    - "The 2026-05-22 fixture (transcript + paper-log.jpg + text-followup + expected-draft) is copied into the Python test tree"
    - "FakeAnthropicClientForExtractor can replay a sequence of tool_use responses and simulate a retry"
  artifacts:
    - path: "src/farm-agent/farm_agent/extraction/prompts.py"
      provides: "CACHEABLE_SYSTEM_BLOCKS + cacheable_few_shot() verbatim from Node system.js"
      contains: "cache_control"
    - path: "src/farm-agent/tests/fixtures/extraction/seeding-session-may22/expected-draft.json"
      provides: "Locked regression anchor (5 groups / 11 children / block names)"
    - path: "src/farm-agent/tests/conftest.py"
      provides: "FakeAnthropicClientForExtractor multi-call fake"
      contains: "FakeAnthropicClientForExtractor"
    - path: "src/farm-agent/pyproject.toml"
      provides: "Pillow runtime dependency"
      contains: "Pillow"
  key_links:
    - from: "src/farm-agent/farm_agent/extraction/prompts.py"
      to: "src/agents/alerter/src/extraction/prompts/system.js"
      via: "verbatim copy of SYSTEM_PROMPT array + FEW_SHOT turns"
      pattern: "tu_fewshot_6"
    - from: "src/farm-agent/tests/conftest.py"
      to: "anthropic Messages tool_use envelope"
      via: "MagicMock with block.type/name/id/input + usage"
      pattern: "submit_extraction"
---

<objective>
Lay the Phase 60 foundation: add the Pillow runtime dependency (behind a package-legitimacy checkpoint), port the Node extraction system prompt + few-shot turns verbatim into `prompts.py`, copy the 2026-05-22 seeding-session fixture into the Python test tree, and extend `conftest.py` with a multi-call Anthropic fake that can simulate the 2-call retry. These are the Wave 0 prerequisites every downstream plan depends on.

Purpose: Waves 2 and 3 cannot run without the prompt constants, the fixture, the fake client, and Pillow. Isolating them here keeps the leaf-unit and extractor waves focused on logic.
Output: `prompts.py`, the copied fixture dir, an extended `conftest.py`, and `Pillow` in `pyproject.toml`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/60-extraction-pipeline/60-CONTEXT.md
@.planning/phases/60-extraction-pipeline/60-RESEARCH.md
@.planning/phases/60-extraction-pipeline/60-PATTERNS.md

<read_first>
@src/agents/alerter/src/extraction/prompts/system.js
@src/farm-agent/farm_agent/gate/prompts.py
@src/farm-agent/tests/conftest.py
@src/farm-agent/farm_agent/extraction/schemas/submission.py
</read_first>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Package-legitimacy gate for Pillow, then add to pyproject.toml</name>
  <files>src/farm-agent/pyproject.toml</files>
  <what-built>Pillow is a net-new runtime dependency (replaces Node's jimp for image downscale). Per the Package Legitimacy Gate (T-60-SC), Pillow is tagged [ASSUMED] in 60-RESEARCH.md because slopcheck was unavailable at research time. It must be human-verified before install.</what-built>
  <how-to-verify>
    1. Open https://pypi.org/project/Pillow/ and confirm: maintained by the python-pillow org (github.com/python-pillow/Pillow), 12+ years history, very high weekly download count, latest version is a recent release (research saw 12.2.0).
    2. Confirm the source repo is github.com/python-pillow/Pillow (PyPA-recognized project).
    3. If legitimate, type "approved". If anything looks wrong (typosquat name, low downloads, no repo), type the concern and STOP.
  </how-to-verify>
  <action>After approval, add the line `"Pillow>=10.0",` to the `dependencies` list in src/farm-agent/pyproject.toml (the existing list ends with `"python-ulid>=3.1",`). Do NOT touch `[project.optional-dependencies]`. Then run `cd src/farm-agent && uv sync` to install it into the workspace venv.</action>
  <verify>
    <automated>cd src/farm-agent && uv run python -c "import PIL; from PIL import Image; print(PIL.__version__)"</automated>
  </verify>
  <resume-signal>Type "approved" to confirm Pillow legitimacy, or describe the concern.</resume-signal>
  <done>Pillow is in pyproject.toml dependencies and `import PIL` succeeds in the workspace venv.</done>
</task>

<task type="auto">
  <name>Task 2: Port the Node extraction system prompt + few-shot verbatim into prompts.py</name>
  <files>src/farm-agent/farm_agent/extraction/prompts.py, src/farm-agent/farm_agent/extraction/__init__.py</files>
  <action>Create src/farm-agent/farm_agent/extraction/__init__.py (empty or a short docstring; this is a Foray island — no cross-package imports). Create farm_agent/extraction/prompts.py mirroring the structure of farm_agent/gate/prompts.py. Copy the SYSTEM_PROMPT array from src/agents/alerter/src/extraction/prompts/system.js VERBATIM (join the array elements with newlines into a single SYSTEM_PROMPT str); the provenance source taxonomy (audio / paper_log_photo / bag_label_photo / text / model_inference) must survive byte-identical. Define CACHEABLE_SYSTEM_BLOCKS as a list with one dict: type text, the SYSTEM_PROMPT text, and cache_control set to type ephemeral. Copy the FEW_SHOT turns from system.js into a FEW_SHOT list — preserve every tool_use and tool_result block exactly, including the last assistant turn whose tool_use carries id tu_fewshot_6 (downstream code closes it). Add cacheable_few_shot() returning FEW_SHOT. Do NOT abbreviate or summarize the prompt — the cache threshold (~1024 tokens for Sonnet) requires the full text, and any drift breaks parity. No import of any other farm_agent package (Foray seam, FND-05). No em-dashes introduced in code/comments per project convention.</action>
  <verify>
    <automated>cd src/farm-agent && uv run python -c "from farm_agent.extraction.prompts import CACHEABLE_SYSTEM_BLOCKS, cacheable_few_shot, SYSTEM_PROMPT; assert CACHEABLE_SYSTEM_BLOCKS[0]['cache_control'] == {'type': 'ephemeral'}; assert len(SYSTEM_PROMPT) > 1000; fs = cacheable_few_shot(); assert any('tu_fewshot_6' in str(t) for t in fs); print('ok', len(SYSTEM_PROMPT), len(fs))"</automated>
  </verify>
  <done>prompts.py exports SYSTEM_PROMPT (>1000 chars), CACHEABLE_SYSTEM_BLOCKS with ephemeral cache_control, and cacheable_few_shot() containing a tu_fewshot_6 tool_use; no cross-package import.</done>
</task>

<task type="auto">
  <name>Task 3: Copy the May-22 fixture and extend conftest with FakeAnthropicClientForExtractor</name>
  <files>src/farm-agent/tests/conftest.py, src/farm-agent/tests/extraction/__init__.py, src/farm-agent/tests/fixtures/extraction/seeding-session-may22/transcript.txt, src/farm-agent/tests/fixtures/extraction/seeding-session-may22/paper-log.jpg, src/farm-agent/tests/fixtures/extraction/seeding-session-may22/text-followup.txt, src/farm-agent/tests/fixtures/extraction/seeding-session-may22/expected-draft.json</files>
  <action>Copy the four fixture files (transcript.txt, paper-log.jpg, text-followup.txt, expected-draft.json) from src/agents/alerter/test/fixtures/seeding-session-may22/ to src/farm-agent/tests/fixtures/extraction/seeding-session-may22/ — byte-for-byte (use cp, do NOT hand-retype expected-draft.json; it is the locked regression anchor). Create src/farm-agent/tests/extraction/__init__.py (empty). In src/farm-agent/tests/conftest.py, add a class FakeAnthropicClientForExtractor mirroring the existing FakeAnthropicClient shape but driven by a responses list-of-dicts sequence (one entry consumed per create call). Each response entry is either a dict with key tool_input (the dict tool input) or a dict with key raise (an exception to raise). Implement with_options(**kwargs) returning self, a messages property returning self, and async def create(**kwargs) that: appends kwargs to self.calls; pops the next response by self.call_index then increments it; raises the exception if a raise key is present; otherwise returns a MagicMock whose .content is a single-element list with block.type tool_use, block.name submit_extraction, block.id set to f"tu_call_{N}" using the PRE-increment index value (DISTINCT per call so retry turns can pair tool_use_id against the first call's block.id), block.input set to the entry's tool_input, and a .usage MagicMock with input_tokens/output_tokens/cache_creation_input_tokens/cache_read_input_tokens set to ints. Keep the class importable from tests.conftest (no fixture wrapper required; tests construct it directly).</action>
  <verify>
    <automated>cd src/farm-agent && uv run python -c "from PIL import Image; im=Image.open('tests/fixtures/extraction/seeding-session-may22/paper-log.jpg'); print(im.size); import json; json.load(open('tests/fixtures/extraction/seeding-session-may22/expected-draft.json')); from tests.conftest import FakeAnthropicClientForExtractor; print('ok')"</automated>
  </verify>
  <done>The four fixture files exist and load (paper-log.jpg opens at 900x1600); FakeAnthropicClientForExtractor imports from tests.conftest.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Node fixture files → Python test tree | Static fixture copy; expected-draft.json is the locked regression anchor and must not be hand-edited |
| pip/PyPI → workspace venv | Net-new Pillow dependency crosses the supply-chain boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-60-SC | Tampering | `uv sync` / Pillow install | mitigate | Blocking human-verify checkpoint (Task 1) confirms Pillow legitimacy on pypi.org before install; [ASSUMED] per 60-RESEARCH.md |
| T-60-01-01 | Tampering | expected-draft.json copy | mitigate | Byte-for-byte cp, never hand-retyped; downstream parity asserts against it |
| T-44-04-01 | Tampering | prompts.py SYSTEM_PROMPT | mitigate | System prompt is a static cacheable block; farmer text is NEVER concatenated here (it enters via messages[] in plan 03) |
</threat_model>

<verification>
Run `cd src/farm-agent && uv run pytest -q` — the existing suite must stay green (no test regressions from the conftest extension). The new prompts.py, fixture, and fake client are exercised by plans 02/03.
</verification>

<success_criteria>
- Pillow imports in the workspace venv (legitimacy-approved).
- prompts.py exports a >1000-char verbatim SYSTEM_PROMPT, CACHEABLE_SYSTEM_BLOCKS with ephemeral cache_control, and cacheable_few_shot() containing tu_fewshot_6.
- The four fixture files exist and load; paper-log.jpg is 900x1600 (downscale will fire downstream).
- FakeAnthropicClientForExtractor is importable and replays a response sequence with distinct block.id per call.
- Existing test suite stays green.
</success_criteria>

<output>
Create `.planning/phases/60-extraction-pipeline/60-01-SUMMARY.md` when done.
</output>
