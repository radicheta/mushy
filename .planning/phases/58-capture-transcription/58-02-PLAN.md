---
phase: 58-capture-transcription
plan: 02
type: execute
wave: 2
depends_on: ["58-01"]
files_modified:
  - src/farm-agent/farm_agent/capture/transcribe_client.py
  - src/farm-agent/farm_agent/capture/capture_repo.py
  - src/farm-agent/tests/test_transcribe_client.py
  - src/farm-agent/tests/test_capture_repo.py
autonomous: true
requirements: [CAP-01, CAP-02]
user_setup: []

must_haves:
  truths:
    - "transcribe_client.transcribe(path) returns {ok:True, text, duration_ms, language} on Whisper 200, and {ok:False, reason} on timeout / 5xx / missing audio_path — and NEVER raises (CAP-02, D-01)"
    - "transcribe() is async def awaiting the httpx call, so awaiting it yields the event loop (off-loop per D-02/D-03); it accepts a str path OR a {audio_path} dict (harness symmetry)"
    - "capture_repo.insert_capture(pool, row) writes a signal_capture row with attachment_paths as text[] (list[str], NOT Jsonb) and corpus_context hard-coded None, fail-open, NEVER raises (CAP-01, D-04 persist)"
    - "mark_expired_older_than(pool, age_seconds) soft-expires old rows and returns a count, fail-open"
  artifacts:
    - path: "src/farm-agent/farm_agent/capture/transcribe_client.py"
      provides: "create_transcribe_client(api_url, http, timeout_ms) -> {transcribe}; never-throws httpx POST to /transcribe"
      exports: ["create_transcribe_client"]
    - path: "src/farm-agent/farm_agent/capture/capture_repo.py"
      provides: "insert_capture(pool, row) + mark_expired_older_than(pool, age_seconds); never-throws psycopg3"
      exports: ["insert_capture", "mark_expired_older_than"]
  key_links:
    - from: "transcribe_client.transcribe"
      to: "whisper-transcribe /transcribe"
      via: "await httpx.AsyncClient.post json={audio_path}"
      pattern: "/transcribe"
    - from: "capture_repo.insert_capture"
      to: "signal_capture table"
      via: "psycopg3 async with pool.connection(), %s placeholders"
      pattern: "INSERT INTO signal_capture"
---

<objective>
Port the two separable leaf units the pipeline (Plan 03) composes: `transcribe_client.py` (the never-throws httpx client to the `whisper-transcribe` sibling, CAP-02 / D-01) and `capture_repo.py` (the never-throws `signal_capture` INSERT + soft-expiry, CAP-01 / D-04 persist). Both are pure mechanical ports with direct Node spec + Python idiom analogs and have NO file overlap, so they ship together in one Wave-2 plan.

Purpose: these are the Foray-seam separable units (D-08) — each is independently testable with no pipeline dependency. `transcribe_client` mirrors `SignalClient` (injected httpx, discriminated `{ok,...}` result); `capture_repo` mirrors `outbound_repo` exactly (psycopg3, fail-open, `%s` params). Landing them first means Plan 03's pipeline is pure wiring against known contracts.
Output: `transcribe_client.py`, `capture_repo.py`, and their unit tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/58-capture-transcription/58-RESEARCH.md
@.planning/phases/58-capture-transcription/58-PATTERNS.md
@.planning/phases/58-capture-transcription/58-VALIDATION.md

<interfaces>
<!-- The signal_capture DDL already exists (Phase 56). Column order is the contract. -->
From PATTERNS §capture_repo.py — INSERT column order (17 cols, %s x17):
  signal_capture(id, captured_at, sender, message_type, raw_text, attachment_paths,
    transcript, llm_session_tag, llm_reply, degraded, group_id, farmos_person,
    reply_target_kind, signal_msg_ts, quote_msg_ts, quote_author_e164, corpus_context)
  attachment_paths = text[] (pass list[str] directly, NEVER Jsonb)
  llm_session_tag/llm_reply = always None (Phase 59+); corpus_context = always None (backfill-only)

From signal_io/client.py:52-90 (injected-httpx constructor shape transcribe_client mirrors):
  SignalClient(*, config: TenantConfig, http: httpx.AsyncClient); self.http = http

From persistence/outbound_repo.py:79-85 (fail-open try/except → {ok}/{ok:False,reason} to copy verbatim)
From persistence/pool.py:44-52 (async with pool.connection() as conn: await conn.execute(sql, params))
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: transcribe_client.py — never-throws httpx client to whisper-transcribe</name>
  <read_first>
    - src/agents/alerter/src/transcribe-client.js (port-target: createTranscribeClient factory, AbortController timeout, never-throws, dual-arg str|{audio_path})
    - src/farm-agent/farm_agent/signal_io/client.py:52-90,256-262 (injected httpx.AsyncClient + timeout= + status>=400 idioms to mirror)
    - src/whisper-transcribe/main.py (/transcribe response contract: {text, duration_ms, language, language_probability}; A4 VERIFIED)
    - .planning/phases/58-capture-transcription/58-PATTERNS.md §transcribe_client.py (factory + never-throws + timeout shapes)
    - .planning/phases/58-capture-transcription/58-RESEARCH.md §Pattern 2 + §"Whisper Fake/Stub Strategy" Option A (respx test approach)
  </read_first>
  <behavior>
    - Test (respx 200): transcribe("/data/signal-capture/x.ogg") -> {ok:True, text:"hola", duration_ms:1500, language:"es"}.
    - Test (dual-arg): transcribe({"audio_path": "/p.ogg"}) behaves identically to transcribe("/p.ogg").
    - Test (missing): transcribe(None) and transcribe({}) -> {ok:False, reason:"missing audio_path"}, no HTTP call.
    - Test (5xx): Whisper 500 -> {ok:False, reason starts "whisper 500:"}; never raises.
    - Test (timeout): respx side_effect httpx.TimeoutException -> {ok:False, reason:"timeout"}; never raises.
    - Test (off-loop / D-02·D-03): transcribe is an `async def` and uses `await` on the httpx post (assert via inspect.iscoroutinefunction on the returned callable).
  </behavior>
  <action>
    Create farm_agent/capture/transcribe_client.py with `def create_transcribe_client(api_url: str, http: httpx.AsyncClient, timeout_ms: int = 200_000, logger=None) -> dict` returning `{"transcribe": transcribe}`. Hold the injected `http` in the closure (mirror SignalClient — do NOT create a client per call). Inner `async def transcribe(arg) -> dict`: resolve `audio_path = arg if isinstance(arg, str) else (arg or {}).get("audio_path")`; if falsy return `{"ok": False, "reason": "missing audio_path"}`. Else `await http.post(f"{api_url}/transcribe", json={"audio_path": audio_path}, timeout=timeout_ms/1000)`; on status>=400 return `{"ok": False, "reason": f"whisper {status}: {text[:200]}"}`; on success return `{"ok": True, "text": data.get("text") or "", "duration_ms": data.get("duration_ms", 0), "language": data.get("language") or "unknown"}`. Catch `httpx.TimeoutException` → `{"ok": False, "reason": "timeout"}` (WARNING) and bare `Exception` → `{"ok": False, "reason": str(e)}` (WARNING). NEVER raise. Per D-01 this is a faithful HTTP port — do NOT add an in-process Whisper or ProcessPoolExecutor.
  </action>
  <verify>
    <automated>cd src/farm-agent && uv run pytest tests/test_transcribe_client.py -x -q</automated>
  </verify>
  <done>transcribe_client.py exists; transcribe is async, never-throws, dual-arg, returns the discriminated result on 200/5xx/timeout/missing; respx unit tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: capture_repo.py — never-throws insert_capture + mark_expired_older_than</name>
  <read_first>
    - src/agents/alerter/src/capture-db.js (port-target: insertCapture row shape + column order + markExpiredOlderThan)
    - src/farm-agent/farm_agent/persistence/outbound_repo.py (mirror exactly: _INSERT_SQL constant, %s params, async with pool.connection(), fail-open try/except, Jsonb for jsonb columns only)
    - src/farm-agent/farm_agent/persistence/pool.py:44-52 (psycopg3 acquisition; UTC pool)
    - .planning/phases/58-capture-transcription/58-PATTERNS.md §capture_repo.py (exact INSERT/EXPIRE SQL + params tuple) + §"Critical Subtleties" items 1,2
    - src/farm-agent/tests/test_signal_persist.py (DB-independent fail-open test + @requires_db round-trip gate style)
  </read_first>
  <behavior>
    - Test (DB-independent, always runs): insert_capture against a pool whose connection() raises is swallowed → {"ok": False, "reason": ...}, never propagates.
    - Test (DB-gated, @requires_db): a row with attachment_paths=["a.mp3","b.jpg"] round-trips as a text[] == ["a.mp3","b.jpg"] (NOT a JSON string); transcript=None stores NULL; degraded round-trips bool.
    - Test (DB-gated): corpus_context is NULL in the written row even though the column is in the INSERT (hard-coded None).
    - Test (DB-gated): mark_expired_older_than(pool, age) sets expired=true only on rows older than age and returns the affected count; fail-open returns 0 on error.
  </behavior>
  <action>
    Create farm_agent/capture/capture_repo.py. Copy outbound_repo.py's structure verbatim. Define `_INSERT_SQL` for `signal_capture` with the 17-column order from the interfaces block and 17 `%s` placeholders. `async def insert_capture(pool: AsyncConnectionPool, row: dict) -> dict`: build the params tuple pulling `row["id"], row["captured_at"], row["sender"], row["message_type"], row.get("raw_text"), row.get("attachment_paths", []), row.get("transcript"), None (llm_session_tag), None (llm_reply), row.get("degraded", False), row.get("group_id"), row.get("farmos_person"), row.get("reply_target_kind"), row.get("signal_msg_ts"), row.get("quote_msg_ts"), row.get("quote_author_e164"), None (corpus_context)`. Pass `attachment_paths` as a plain list[str] — psycopg3 auto-adapts to text[]; do NOT wrap in Jsonb (subtlety 1). corpus_context is ALWAYS None (subtlety 2). Wrap in try/except → {"ok": False, "reason": str(e)} (WARNING); else {"ok": True}. NEVER raise. Also add `async def mark_expired_older_than(pool, age_seconds: int) -> int` with `UPDATE signal_capture SET expired=true WHERE captured_at < NOW() - (%s || ' seconds')::interval AND expired IS DISTINCT FROM true`, returning `result.rowcount or 0`, fail-open returns 0.
  </action>
  <verify>
    <automated>cd src/farm-agent && uv run pytest tests/test_capture_repo.py -x -q</automated>
  </verify>
  <done>capture_repo.py exists; insert_capture is never-throw, writes attachment_paths as text[] and corpus_context as None; mark_expired_older_than soft-expires + counts; DB-independent fail-open test passes (DB-gated tests skip cleanly when the test DB is down).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| transcribe_client → whisper-transcribe | A server-generated audio_path crosses to the GPU sibling; the path must be under whisper's ALLOWED_ROOT |
| capture_repo → TimescaleDB | Capture rows written to the shared prod DB; an insert failure must not back-pressure the pipeline (fail-open) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-58-02-01 | Tampering | whisper audio_path injection | mitigate | transcribe_client sends only server-generated paths (built in pipeline Plan 03); whisper-transcribe enforces ALLOWED_ROOT on its side (main.py:_resolve_safe). No client filename ever reaches it. |
| T-58-02-02 | Tampering | corpus_context injection from a live caller | mitigate | corpus_context hard-coded None in insert_capture params (subtlety 2); only the backfill harness ever sets it |
| T-58-02-03 | Denial of Service | capture_repo INSERT under DB outage | mitigate | Never-throw try/except → {ok:false}; a DB outage degrades to a WARNING, the pipeline continues (D-04 persist) |
| T-58-02-04 | Denial of Service | whisper timeout blocks the loop | mitigate | httpx `timeout=` + never-throws → {ok:False,reason:"timeout"}; await yields the loop (D-02) |
</threat_model>

<verification>
- `cd src/farm-agent && uv run pytest tests/test_transcribe_client.py tests/test_capture_repo.py -x` green.
- `cd src/farm-agent && uv run pytest` (full suite) green — no Phase 56/57 regression.
- `grep -n 'Jsonb' src/farm-agent/farm_agent/capture/capture_repo.py` shows Jsonb is NOT applied to attachment_paths (text[]); corpus_context param is the literal None.
</verification>

<success_criteria>
- transcribe_client is a faithful never-throws httpx port (D-01), async/off-loop (D-02/D-03), dual-arg.
- capture_repo.insert_capture is fail-open (D-04), writes attachment_paths as text[] and corpus_context as None.
- mark_expired_older_than soft-expires + counts, fail-open.
- Both units are independently unit-tested with no pipeline dependency (D-08 Foray seam).
</success_criteria>

<output>
Create `.planning/phases/58-capture-transcription/58-02-SUMMARY.md` when done.
</output>
</content>
</invoke>
