---
phase: 58-capture-transcription
plan: 04
type: execute
wave: 4
depends_on: ["58-03"]
files_modified:
  - src/farm-agent/scripts/live_fire_58.py
  - .planning/phases/58-capture-transcription/58-LIVE-FIRE.md
autonomous: false
requirements: [CAP-01, CAP-02]
user_setup:
  - service: whisper-transcribe
    why: "SC#1 live-fire needs the GPU transcription container healthy (D-07 blocker)"
    dashboard_config:
      - task: "Resolve mushy-whisper-transcribe-1 CUDA forward-compat hang (cuInit err 804 on GeForce) — purge cuda-compat per [[project_whisper_cuda_compat_geforce_804]]; confirm GET /health returns 200"
        location: "elder-plops docker host"
      - task: "Verify alerter-py and whisper-transcribe share the same /data/signal-capture bind-mount in docker-compose.override.yml (A5) — else Whisper 400s every request"
        location: "docker-compose.override.yml"

must_haves:
  truths:
    - "A real voice note sent via Signal produces a signal_capture row with a ULID id, the correct farmer slug, and a NON-NULL transcript (SC#1)"
    - "The image-path D-05 gate holds live: a real photo's attachment_paths entry points to a file that exists on disk (SC#3)"
    - "The live-fire is gated on the D-07 ops fix (Whisper healthy) + A5 mount alignment — it does NOT attempt to make those fixes in phase code"
  artifacts:
    - path: "src/farm-agent/scripts/live_fire_58.py"
      provides: "post-send SELECT/assert harness: latest signal_capture row has ULID id + correct slug + non-null transcript + on-disk attachment paths"
      exports: ["main"]
    - path: ".planning/phases/58-capture-transcription/58-LIVE-FIRE.md"
      provides: "operator runbook for SC#1 (non-null transcript) + the D-07/A5 prerequisites"
  key_links:
    - from: "live_fire_58.py"
      to: "signal_capture table"
      via: "real pool SELECT of the latest captured row"
      pattern: "signal_capture"
    - from: "live capture pipeline (Plan 03, running in boot)"
      to: "whisper-transcribe container"
      via: "transcribe_client HTTP over the shared /data/signal-capture mount"
      pattern: "/transcribe"
---

<objective>
Provide the live-fire verification for SC#1 — a real voice note through the live, boot-wired Plan-03 capture pipeline produces a `signal_capture` row with a ULID id, the correct farmer slug, and a NON-NULL `transcript` — plus the SC#3 live check that a real image's stored path exists on disk. These are the acceptance shapes that the mocked-Whisper unit suite cannot prove.

Purpose: prove the ported pipeline works against the real signal-cli container + the real (D-07-fixed) Whisper container + the shared DB. This plan is operator-driven (`autonomous: false`) and is BLOCKED until the D-07 ops fix (Whisper healthy) and the A5 mount alignment are confirmed — both are prerequisite ops fixes, NOT phase code. Per the Phase-57 deferral (57-04 SC#1 note), Phase 58 is where the live inbound drain on the farmer account is exercised; the operator must ensure the live Node alerter is idle for this account during the test so there is no dual-poller contention.
Output: a post-send assertion harness + an operator runbook.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/58-capture-transcription/58-RESEARCH.md
@.planning/phases/58-capture-transcription/58-VALIDATION.md
@.planning/phases/58-capture-transcription/58-03-SUMMARY.md

<interfaces>
Plan 03: boot.py runs the live capture pipeline (ReceiveLoop dispatch=pipeline["handle"]).
Phase 56: build_pool(config) for the SELECT; TenantConfig.load().
DB column shapes (Phase 56): signal_capture(id text ULID, farmos_person text, transcript text, attachment_paths text[], message_type text, captured_at timestamptz).
D-07 blocker: mushy-whisper-transcribe-1 unhealthy (cuInit err 804) — see [[project_whisper_cuda_compat_geforce_804]].
A5: whisper enforces ALLOWED_ROOT=/data/signal-capture; alerter-py must mount the same path.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: live_fire_58.py assertion harness</name>
  <read_first>
    - .planning/phases/57-signal-i-o/57-04-PLAN.md Task 1 (the SELECT/assert harness shape + self-guard pattern this mirrors)
    - .planning/phases/58-capture-transcription/58-VALIDATION.md §"Manual-Only Verifications" (the SELECT query + the D-07/A5 prereqs)
    - .planning/phases/58-capture-transcription/58-RESEARCH.md §"Open Questions" Q3 (ALLOWED_ROOT mount alignment) + §"Runtime State Inventory"
    - src/farm-agent/farm_agent/persistence/pool.py (build_pool) + tenancy/tenant.py (load)
  </read_first>
  <action>
    Create src/farm-agent/scripts/live_fire_58.py with async main(): build a real pool via build_pool(TenantConfig.load()). PREFLIGHT (print clear PASS/FAIL, do not fix): (a) GET {config.whisper_url}/health expects 200 — if 503/unreachable, print "BLOCKED D-07: whisper unhealthy" and exit non-zero; (b) confirm config.capture_base_dir == "/data/signal-capture" and note the A5 cross-container mount must be verified by the operator. MAIN ASSERT: `SELECT id, farmos_person, message_type, transcript, attachment_paths, captured_at FROM signal_capture ORDER BY captured_at DESC LIMIT 1` and print the row; assert the id is a 26-char ULID, farmos_person is not "(unassigned)" for a known farmer, and for an audio/mixed message_type the transcript IS NOT NULL (SC#1). For each path in attachment_paths assert `os.path.exists(path)` (SC#3 live). Print a clear PASS/FAIL for SC#1 + SC#3. This harness only READS (it does not send) — the operator sends the real voice note via Signal through the live boot-wired pipeline; the harness verifies the resulting row.
  </action>
  <verify>
    <automated>cd src/farm-agent && python -c "import ast; ast.parse(open('scripts/live_fire_58.py').read())" && echo "syntax ok"</automated>
  </verify>
  <done>live_fire_58.py exists, preflights Whisper /health + the base-dir, SELECTs the latest signal_capture row, and asserts ULID id + correct slug + non-null transcript (SC#1) + on-disk attachment paths (SC#3). Syntax-valid; read-only (does not send).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: SC#1 + SC#3 live voice-note round-trip (BLOCKED on D-07 + A5)</name>
  <what-built>
    The live, boot-wired capture pipeline (Plan 03) plus a read-only assertion harness (live_fire_58.py) that verifies the latest signal_capture row has a ULID id, the correct farmer slug, a non-null transcript, and on-disk attachment paths.
  </what-built>
  <how-to-verify>
    1. PREREQ D-07: confirm `docker exec mushy-whisper-transcribe-1` is healthy — `curl -fsS http://localhost:8090/health` (or the configured WHISPER_URL) returns 200. If it shows cuInit err 804, apply the cuda-compat purge per [[project_whisper_cuda_compat_geforce_804]] FIRST. This is an ops fix, NOT phase code.
    2. PREREQ A5: confirm `alerter-py` and `whisper-transcribe` share the same `/data/signal-capture` bind-mount in docker-compose.override.yml — `docker inspect` both. If they differ, Whisper 400s every /transcribe regardless of health.
    3. PREREQ dual-poller: ensure the live Node alerter is idle for the farmer account for the test window (Phase 58 is the live inbound-drain phase; do not run two pollers on the same account).
    4. With the farm-agent boot daemon running (the Plan-03 pipeline live), send a REAL VOICE NOTE from a known farmer's Signal to the bot.
    5. Run `cd src/farm-agent && uv run python scripts/live_fire_58.py`; confirm it prints PASS for SC#1 (ULID id + correct slug + non-null transcript) and SC#3 (each attachment path exists on disk).
    6. Send a REAL PHOTO (image or mixed message); re-run the harness; confirm the image path exists on disk (SC#3) and the row persisted even if transcript is null for a photo-only message.
    7. If transcription returns null on a voice note despite a healthy container, capture the alerter-py logs (the D-04 WARNING + reason) and the /transcribe response, and flag before declaring SC#1.
  </how-to-verify>
  <acceptance_criteria>
    - SC#1: `SELECT id, farmos_person, transcript FROM signal_capture ORDER BY captured_at DESC LIMIT 1` returns a 26-char ULID id, the correct (non-"(unassigned)") farmer slug, and a non-null transcript for the voice note.
    - SC#3: every path in the row's attachment_paths exists on disk (`os.path.exists` True).
    - D-04 verified: a photo-only or transcription-failure capture still persisted (row present, transcript null, degraded true) — no capture dropped.
    - No dual-poller contention; the live Node alerter did not also consume the test message.
  </acceptance_criteria>
  <resume-signal>Type "approved" with the SC#1 SELECT output + SC#3 path-exists result, or describe the failure (and whether D-07/A5 was the blocker).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| live-fire harness → shared prod TimescaleDB | Read-only SELECT of real signal_capture rows; no writes |
| live capture pipeline → shared signal-cli container | The boot daemon drains the farmer queue; must not contend with the live Node alerter |
| live capture pipeline → whisper-transcribe over shared mount | A real audio_path under /data/signal-capture crosses to the GPU sibling |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-58-04-01 | Spoofing | dual-poller drains the farmer queue | mitigate | Operator confirms the live Node alerter is idle for the account during the test (prereq 3); exactly one ReceiveLoop runs (Plan 03 boot) |
| T-58-04-02 | Information Disclosure | live-fire reads real farmer PII from DB | accept | Read-only; the operator runbook masks the e164 in any pasted output; low volume |
| T-58-04-03 | Tampering | Whisper 400 on mount mismatch silently nulls transcripts | mitigate | A5 preflight (prereq 2) + the harness /health probe surface the misconfig before declaring SC#1 |
</threat_model>

<verification>
- live_fire_58.py is syntax-valid and read-only (does not send).
- Operator confirms SC#1 (non-null transcript) + SC#3 (on-disk paths) at the checkpoint, after the D-07 + A5 prereqs are met.
</verification>

<success_criteria>
- SC#1 proven live: a real voice note yields a signal_capture row with ULID id, correct slug, and non-null transcript.
- SC#3 proven live: the stored attachment paths exist on disk (D-05 holds end-to-end).
- D-07 + A5 handled as prerequisite ops fixes (not phase code); the live-fire is correctly gated on them.
- No farmer traffic disrupted by dual-poller contention.
</success_criteria>

<output>
Create `.planning/phases/58-capture-transcription/58-LIVE-FIRE.md` (operator runbook, restating the D-07/A5 prereqs + the dual-poller caution) and `.planning/phases/58-capture-transcription/58-04-SUMMARY.md` when done.
</output>
</content>
</invoke>
