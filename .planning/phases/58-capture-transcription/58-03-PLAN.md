---
phase: 58-capture-transcription
plan: 03
type: execute
wave: 3
depends_on: ["58-02"]
files_modified:
  - src/farm-agent/farm_agent/capture/pipeline.py
  - src/farm-agent/farm_agent/capture/capture_history.py
  - src/farm-agent/farm_agent/capture/retention.py
  - src/farm-agent/farm_agent/boot.py
  - src/farm-agent/tests/test_capture_pipeline.py
  - src/farm-agent/tests/test_capture_history.py
autonomous: true
requirements: [CAP-01, CAP-02]
user_setup: []

must_haves:
  truths:
    - "handle(envelope) turns an inbound envelope into a signal_capture row: ULID id seeded from captured_at, farmer slug via resolve_farmer ((unassigned) for unknown), classify -> message_type, attachments downloaded to baseDir/<day>/<time>-<ulid>.<ext>, and it NEVER raises (CAP-01, D-03)"
    - "D-05: a downloaded path is added to attachment_paths ONLY after Path(p).exists() is True; a missing file is dropped + degraded=True + WARNING (extractor never gets a path to a missing file, SC#3)"
    - "D-04: on transcription {ok:False} the row still persists with transcript=NULL + degraded=True + WARNING; the capture is never dropped"
    - "SC#2/D-02/D-03: transcription is awaited (async HTTP), so the receive loop yields during a slow transcription — another coroutine runs while handle() is mid-transcribe"
    - "D-06: capture is PRE-confirmation, so the no-silent-failure-after-farmer-confirm rule does NOT bind here; handle() emits NO farmer-facing acks on any path — fail-open + WARNING is the only posture (no ack/reply task exists in this phase)"
    - "boot wires create_capture_pipeline as the receive_loop dispatch AND starts the retention task; the live inbound drain deferred from Phase 57 now runs (CAP-01/CAP-02 end-to-end)"
  artifacts:
    - path: "src/farm-agent/farm_agent/capture/pipeline.py"
      provides: "create_capture_pipeline(...) -> {handle, record_reply_capture}; classify/build_path/_coerce_ts/ULID helpers"
      exports: ["create_capture_pipeline"]
      min_lines: 120
    - path: "src/farm-agent/farm_agent/capture/capture_history.py"
      provides: "select_recent_by_sender + select_recent_outbound_by_recipient (fail-open SELECT for Phase 59+)"
      exports: ["select_recent_by_sender", "select_recent_outbound_by_recipient"]
    - path: "src/farm-agent/farm_agent/capture/retention.py"
      provides: "retention_loop(pool, config) asyncio periodic soft-expiry (run-once-then-sleep)"
      exports: ["retention_loop"]
    - path: "src/farm-agent/farm_agent/boot.py"
      provides: "receive loop wired to pipeline.handle + retention task started"
      contains: "create_capture_pipeline"
  key_links:
    - from: "receive_loop dispatch"
      to: "pipeline.handle"
      via: "boot wires dispatch=pipeline['handle']"
      pattern: "create_capture_pipeline"
    - from: "pipeline.handle"
      to: "capture_repo.insert_capture / transcribe_client.transcribe / signal_client.fetch_attachment"
      via: "injected deps (Foray seam D-08)"
      pattern: "insert_capture"
    - from: "pipeline.handle"
      to: "router.resolve_farmer / _read_dm / mask_number"
      via: "reuse Phase-57 primitives (no re-derivation)"
      pattern: "resolve_farmer"
---

<objective>
Port the capture pipeline integrator (`pipeline.py` — `handle` + `record_reply_capture` + `classify`/`build_path`/ULID/`_coerce_ts` helpers, CAP-01/CAP-02), the read-side history (`capture_history.py`), the retention task (`retention.py`), and wire all three into `boot.py` so the live inbound drain deferred from Phase 57 now runs end-to-end. `handle()` is the Phase-57 `dispatch(envelope)` seam consumer.

Purpose: this is the integration that makes the phase real — it composes the Plan-02 leaf units (transcribe_client, capture_repo) plus the Phase-57 primitives (resolve_farmer, _read_dm, fetch_attachment, mask_number) into the single never-raises `handle()` orchestrator (D-03), enforcing the D-04 transcription fail-open and the D-05 disk-existence gate. Wiring it into boot starts the second poller on the farmer account that Phase 57 explicitly deferred (57-04 SC#1 note).
Output: `pipeline.py`, `capture_history.py`, `retention.py`, boot wiring, and the pipeline/history unit tests. handle() terminates after insert_capture (Step 4) and returns a CaptureResult for Phase 59+ — it does NOT implement the event gate, extraction enqueue, or LLM reply (those are Phases 59/60).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/58-capture-transcription/58-RESEARCH.md
@.planning/phases/58-capture-transcription/58-PATTERNS.md
@.planning/phases/58-capture-transcription/58-VALIDATION.md
@.planning/phases/58-capture-transcription/58-02-SUMMARY.md
@.planning/phases/58-capture-transcription/58-01-SUMMARY.md

<interfaces>
<!-- Contracts this plan composes — all on disk after Plans 01/02 + Phase 57. -->
Plan 02: create_transcribe_client(api_url, http, timeout_ms) -> {"transcribe": async(arg)->{ok,...}}
Plan 02: capture_repo.insert_capture(pool, row) -> {ok}/{ok:False,reason}; mark_expired_older_than(pool, age_seconds) -> int
Plan 01: A1-RESOLVED ULID call form (import `from ulid import ULID`); fake_capture_repo/fake_transcribe_client/whisper_http fixtures

Phase 57 primitives (reuse, do NOT re-derive):
  signal_io/router.py: resolve_farmer(source, config) -> str ("(unassigned)" fallback); _read_dm(envelope) -> dict; mask_number(n)
  signal_io/client.py: SignalClient(*, config, http, default_target); async fetch_attachment(id) -> bytes
  signal_io/receive_loop.py: ReceiveLoop(signal_client, dispatch, config, poll_sec); .start()/.stop()

boot.py:35-71 (extend, do not rewrite): main() builds config -> pool -> migrations -> idle on SIGTERM.
  Phase 58 inserts between migrations and the idle-wait: build httpx.AsyncClient + SignalClient,
  create_transcribe_client, create_capture_pipeline, ReceiveLoop(dispatch=pipeline["handle"]).start(),
  asyncio.create_task(retention_loop(pool, config)); cancel both on shutdown before pool.close().

TenantConfig fields: whisper_url, capture_base_dir, capture_retention_days, receive_poll_sec, signal_api_url
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: pipeline.py — create_capture_pipeline (handle + record_reply_capture + helpers)</name>
  <read_first>
    - src/agents/alerter/src/capture.js (port-target: createCapturePipeline, classify, safeExt, buildPath, handle, recordReplyCapture, the per-step try/except, D-03 "errors never escape handle()", Step boundaries 1-4 — and the lines 177-313 sections this plan does NOT port: event gate, extraction enqueue, LLM compose/reply)
    - src/farm-agent/farm_agent/signal_io/receive_loop.py:30-108 (factory+injection+per-envelope try/except idiom to mirror)
    - src/farm-agent/farm_agent/signal_io/router.py (resolve_farmer, _read_dm, mask_number — import and reuse)
    - src/farm-agent/farm_agent/signal_io/client.py:fetch_attachment signature
    - .planning/phases/58-capture-transcription/58-PATTERNS.md §pipeline.py (factory shape, D-05 gate, D-04 fail-open, _coerce_ts, ULID+build_path, classify, imports block) + §"Critical Subtleties" items 3,4,5,6
    - .planning/phases/58-capture-transcription/58-01-SUMMARY.md (A1-RESOLVED: the exact ULID call form to use)
    - .planning/phases/58-capture-transcription/58-RESEARCH.md §Patterns 3-7 + §"Scope Boundary: What Phase 58 Does NOT Port" + §Pitfall 5 (envelope dual-read) + §Pattern 7 (recordReplyCapture)
  </read_first>
  <behavior>
    - test_handle_text_only: text-only envelope -> insert_capture called with message_type="text", a 26-char ULID id, farmos_person from resolve_farmer; handle returns a non-None CaptureResult dict.
    - test_handle_audio_attachment: one audio attachment -> fetch_attachment called, file written under baseDir/<YYYY-MM-DD>/<HH-MM-SS>-<ulid>-<att_id>.<safe_ext>, that path in attachment_paths; transcript populated from the fake transcribe_client.
    - test_d05_missing_file_dropped: when the written file does not exist (patch Path.exists->False) the path is NOT in attachment_paths, degraded=True, and a WARNING is logged.
    - test_d04_transcription_failure: transcribe_client returns {ok:False} -> insert_capture STILL called, transcript=None, degraded=True, handle does NOT raise, capture NOT dropped.
    - test_unassigned_farmer: unknown sender -> farmos_person == "(unassigned)", capture still inserted.
    - test_sc2_transcription_offloop: inject a transcribe_client whose transcribe awaits asyncio.sleep(0.1); a concurrently scheduled task's flag flips before handle() returns (loop yields); AND assert the injected transcribe is a coroutine function.
    - test_handle_never_raises: a dep that raises mid-step (e.g. fetch_attachment raises) is swallowed; handle returns None, the receive loop is not killed.
    - test_record_reply_capture: record_reply_capture persists a row with NO attachment download / transcription (persist-only stub) for confirm-thread replies.
  </behavior>
  <action>
    Create farm_agent/capture/pipeline.py. Port the module-level pure helpers verbatim from PATTERNS: `classify(text, attachments)`, `safe_ext(content_type)`, `build_path(base_dir, captured_at_ms, file_id, ext)` (server-controlled name only — NEVER use att.filename; V12 hardening), `_coerce_ts(v)`, and the ULID id generator using the A1-RESOLVED call form (`from ulid import ULID`). Then `def create_capture_pipeline(pool, signal_client, transcribe_client, config, dispatch_result=None, logger=None) -> dict` returning `{"handle": handle, "record_reply_capture": record_reply_capture}`. Inner `async def handle(envelope) -> dict | None` with an OUTER try/except that swallows everything and returns None (D-03), wrapping these steps each in their own try/except: (1) parse via `env = envelope.get("envelope") or envelope`, `dm = _read_dm(envelope)`, source/text/attachments/groupId/reply_target_kind; `captured_at_ms = _coerce_ts(dm.get("timestamp")) or int(time.time()*1000)`; `capture_id = generate_capture_id(captured_at_ms)`; `farmos_person = resolve_farmer(source, config)`; `message_type = classify(text, attachments)`; (2) for each attachment: `buf = await signal_client.fetch_attachment(att["id"])`, build path with `file_id=f"{capture_id}-{att['id']}"` + `safe_ext(att.get("contentType"))`, mkdir parents, write_bytes, then **D-05** `if not Path(path).exists(): WARNING (mask_number(source)); degraded=True; continue` else append; (3) first audio path only: `r = await transcribe_client["transcribe"](audio_path)`; `if r.get("ok"): transcript = r.get("text")` else WARNING + degraded=True + transcript=None (**D-04**); (4) `await capture_repo.insert_capture(pool, {...captured_at: datetime.now(timezone.utc) or from captured_at_ms, attachment_paths=list[str], corpus_context not passed...})`; (5) return a CaptureResult dict (capture_id, sender, farmos_person, raw_text, transcript, attachment_paths, reply_target_kind, group_id, captured_at_ms, signal_msg_ts, quote_msg_ts, quote_author_e164); if `dispatch_result` is set, `await dispatch_result(result)` (fire-forward seam, also try/except'd). Also port `record_reply_capture(envelope, ctx)` as a persist-only stub (insert_capture with no Step 2/3, mirrors capture.js recordReplyCapture). Use `mask_number(source)` on EVERY log line referencing the sender (V7). Do NOT port the event gate / extraction enqueue / LLM compose/reply sections (Phases 59/60).
  </action>
  <verify>
    <automated>cd src/farm-agent && uv run pytest tests/test_capture_pipeline.py -x -q</automated>
  </verify>
  <done>pipeline.py exists; handle() is never-raises (D-03), enforces the D-05 disk-existence gate and D-04 transcription fail-open, builds server-controlled ULID paths, resolves the farmer slug, and returns a CaptureResult; record_reply_capture persists confirm-thread replies; the SC#2 off-loop assertion passes; all 8 behavior tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: capture_history.py + retention.py</name>
  <read_first>
    - src/agents/alerter/src/capture-history.js (port-target: selectRecentBySender, selectRecentOutboundByRecipient)
    - src/agents/alerter/src/capture-retention.js (port-target: createRetentionJob daily cron, markExpiredOlderThan usage)
    - src/farm-agent/farm_agent/persistence/outbound_repo.py (fail-open SELECT/INSERT idiom; %s params)
    - src/farm-agent/farm_agent/signal_io/receive_loop.py:114-143 (asyncio.Task lifecycle — though retention is a module-level loop function per PATTERNS)
    - .planning/phases/58-capture-transcription/58-PATTERNS.md §capture_history.py + §retention.py (exact SELECT shape + retention_loop)
    - .planning/phases/58-capture-transcription/58-RESEARCH.md §"Retention Job: Port Decision" + §"Open Questions" Q2 (first-run timing — use run-once-then-sleep to catch up after an outage)
  </read_first>
  <behavior>
    - test_select_recent_by_sender (DB-gated): returns signal_capture rows for a sender since since_ms ordered captured_at DESC; fail-open returns [] on error.
    - test_select_recent_outbound_by_recipient (DB-gated): returns the outbound-context rows for the recipient window; fail-open returns [].
    - test_select_fail_open (DB-independent): a pool whose connection() raises yields [] (never raises).
    - test_retention_runs_once_then_sleeps: retention_loop calls mark_expired_older_than once immediately (run-once), then awaits asyncio.sleep(86400) — verified by patching sleep to cancel after the first iteration; a mark_expired error is swallowed (WARNING, loop survives).
  </behavior>
  <action>
    Create farm_agent/capture/capture_history.py with `async def select_recent_by_sender(pool, sender, since_ms) -> list[dict]` and `async def select_recent_outbound_by_recipient(pool, recipient, since_ms) -> list[dict]`, both converting since_ms to a UTC datetime, using `async with pool.connection()` + a row-factory dict conversion, fail-open returning [] (mirror outbound_repo). Create farm_agent/capture/retention.py with `async def retention_loop(pool, config) -> None`: a `while True` that FIRST runs `await mark_expired_older_than(pool, config.capture_retention_days * 86400)` (run-once for outage catch-up, Q2), logs the flagged count, swallows exceptions to a WARNING, then `await asyncio.sleep(86400)`. Import mark_expired_older_than from capture_repo.
  </action>
  <verify>
    <automated>cd src/farm-agent && uv run pytest tests/test_capture_history.py -x -q</automated>
  </verify>
  <done>capture_history.py exposes the two fail-open SELECTs; retention.py exposes retention_loop (run-once-then-daily-sleep, error-swallowing); history tests green (DB-gated tests skip cleanly when the test DB is down).</done>
</task>

<task type="auto">
  <name>Task 3: Wire capture pipeline + retention into boot.py (live inbound drain)</name>
  <read_first>
    - src/farm-agent/farm_agent/boot.py (full main() — insert wiring between run_migrations and the SIGTERM idle-wait; mirror the existing pool.close() graceful-shutdown shape)
    - src/farm-agent/farm_agent/signal_io/client.py:52-90 (SignalClient ctor: requires config, http, default_target)
    - src/farm-agent/farm_agent/signal_io/receive_loop.py:30-143 (ReceiveLoop ctor + start()/stop())
    - .planning/phases/57-signal-i-o/57-04-PLAN.md §objective (the dual-poller hazard A3 — Phase 58 IS the phase that starts the farmer-account drain; ensure only ONE receive loop runs)
    - src/farm-agent/tests/test_boot.py (boot test shape — the test cancels the task after boot; keep it passing)
  </read_first>
  <action>
    Extend boot.py main(): after run_migrations, build `http = httpx.AsyncClient()`, `signal_client = SignalClient(config=config, http=http)`, `transcribe_client = create_transcribe_client(config.whisper_url, http)`, `pipeline = create_capture_pipeline(pool, signal_client, transcribe_client, config)`. Construct `loop = ReceiveLoop(signal_client, dispatch=pipeline["handle"], config=config)` and `await loop.start()`. Start `retention_task = asyncio.create_task(retention_loop(pool, config))`. Keep the existing SIGTERM/SIGINT idle-wait. On shutdown (after stop.wait()): `await loop.stop()`, `retention_task.cancel()` (await + swallow CancelledError), `await http.aclose()`, then the existing `await pool.close()`. Log "capture pipeline live" at INFO (no config fields — T-56-06-01). Ensure exactly ONE ReceiveLoop is started (no dual poller). Keep test_boot.py green (it boots then cancels) — guard the receive loop so the boot test does not hang on a real signal-cli (the existing test injects/cancels; if needed, the loop only polls after start() and the test cancels main()).
  </action>
  <verify>
    <automated>cd src/farm-agent && uv run pytest tests/test_boot.py -x -q && uv run python -c "import ast; ast.parse(open('farm_agent/boot.py').read()); print('boot syntax ok')"</automated>
  </verify>
  <done>boot.py wires create_capture_pipeline as the receive_loop dispatch and starts the retention task; graceful shutdown stops the loop, cancels retention, closes http + pool; exactly one receive loop runs; test_boot.py still passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| inbound Signal envelope → handle() | A whitelisted (Phase 57) envelope with attacker-influenceable contentType/filename/quote fields crosses into the capture pipeline |
| handle() → disk (baseDir) | Attachment bytes written under capture_base_dir; the filename must be server-controlled (no path traversal) |
| handle() logs → log sink | Sender e164 must never appear unmasked |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-58-03-01 | Tampering | path traversal via attachment filename | mitigate | build_path derives the name from the server ULID + safe_ext(contentType) only; att.filename is NEVER used (V5/V12, capture.js:43 port) |
| T-58-03-02 | Information Disclosure | sender e164 in log lines / attachment path | mitigate | mask_number(source) on every log line; the attachment path contains only the ULID + att_id, never the e164 (V7) |
| T-58-03-03 | Denial of Service | a raising dep kills the receive loop | mitigate | handle() outer try/except returns None (D-03); per-step try/except; the receive loop's own per-envelope guard is a second layer |
| T-58-03-04 | Tampering | corpus_context injection from a live envelope | mitigate | corpus_context never passed from handle() (insert_capture hard-codes None per Plan 02) |
| T-58-03-05 | Spoofing | a second poller drains the farmer queue | mitigate | boot starts exactly ONE ReceiveLoop on the farmer account (A3); the live Node alerter must be retired/idle for this account at cutover (ops, flagged in live-fire Plan 04) |
</threat_model>

<verification>
- `cd src/farm-agent && uv run pytest tests/test_capture_pipeline.py tests/test_capture_history.py tests/test_boot.py -x` green.
- `cd src/farm-agent && uv run pytest` (full suite) green — no Phase 56/57 regression.
- `grep -n 'att.*filename\|attachment.*filename' src/farm-agent/farm_agent/capture/pipeline.py | grep -v '^#'` returns nothing (server-controlled path only).
- `grep -nc 'ReceiveLoop(' src/farm-agent/farm_agent/boot.py` returns 1 (single poller).
</verification>

<success_criteria>
- handle() never raises (D-03), enforces D-05 (disk-existence gate) and D-04 (transcription fail-open), and resolves the farmer slug + ULID id (CAP-01).
- Transcription is awaited off-loop (SC#2 / D-02 / D-03) and feeds the persisted row alongside text + image (CAP-02).
- record_reply_capture persists confirm-thread replies (Phase 50 quote-threading correctness).
- retention_loop soft-expires daily (run-once-then-sleep); capture_history exposes the Phase-59+ read seam.
- boot wires the live inbound drain Phase 57 deferred; exactly one receive loop runs.
</success_criteria>

<output>
Create `.planning/phases/58-capture-transcription/58-03-SUMMARY.md` when done.
</output>
</content>
</invoke>
