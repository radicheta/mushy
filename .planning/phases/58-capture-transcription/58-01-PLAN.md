---
phase: 58-capture-transcription
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/farm-agent/pyproject.toml
  - src/farm-agent/farm_agent/capture/__init__.py
  - src/farm-agent/tests/conftest.py
autonomous: false
requirements: [CAP-01, CAP-02]
user_setup: []

must_haves:
  truths:
    - "python-ulid is a pinned runtime dep, added only after a blocking-human legitimacy check (never auto-approved, T-58-SC)"
    - "The timestamp-seeded ULID API is probed and recorded (A1): the implementation knows whether to call ULID.from_datetime(dt) or ULID.from_timestamp(ms) before any code uses it"
    - "The capture unit suite has its fakes available: FakeCaptureRepo (toggle-raise), fake_transcribe_client (async canned result), and a whisper_http respx fixture"
  artifacts:
    - path: "src/farm-agent/pyproject.toml"
      provides: "python-ulid runtime dependency, pinned in uv.lock"
      contains: "python-ulid"
    - path: "src/farm-agent/tests/conftest.py"
      provides: "FakeCaptureRepo, fake_transcribe_client, whisper_http fixtures for the capture suite"
      contains: "FakeCaptureRepo"
    - path: "src/farm-agent/farm_agent/capture/__init__.py"
      provides: "capture package marker (Foray island, no chamber imports)"
  key_links:
    - from: "capture suite tests (Plans 02/03)"
      to: "conftest FakeCaptureRepo / fake_transcribe_client / whisper_http"
      via: "pytest fixture injection"
      pattern: "FakeCaptureRepo"
    - from: "pipeline.py ULID generation (Plan 03)"
      to: "python-ulid timestamp-seeded API"
      via: "documented probe result (A1)"
      pattern: "from_datetime|from_timestamp"
---

<objective>
Land the Phase-58 Wave-0 foundation that the leaf units (Plan 02) and the pipeline (Plan 03) build on: add the one new dependency (`python-ulid`) behind a blocking-human legitimacy gate, PROBE its timestamp-seeded API so downstream code is deterministic (Assumption A1), create the `capture/` package marker, and add the three capture-suite fakes to `conftest.py`.

Purpose: RESEARCH flags `python-ulid` as `[ASSUMED]` (slopcheck unavailable) and flags A1 (the exact API for timestamp-seeded ULID generation — `ULID.from_datetime(dt)` vs `ULID.from_timestamp(ms)`) as MEDIUM-risk unverified. Resolving both here keeps Plans 02/03 pure ports with no scavenger hunt and no guessed API. The fakes mirror the proven `FakeOutboundRepo` / `signal_http` pattern so the capture tests need no new infrastructure.
Output: pinned `python-ulid` dep + recorded API probe, `capture/__init__.py`, and the conftest fixtures.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/58-capture-transcription/58-RESEARCH.md
@.planning/phases/58-capture-transcription/58-PATTERNS.md
@.planning/phases/58-capture-transcription/58-VALIDATION.md

<interfaces>
<!-- The fakes mirror the on-disk Phase-57 idioms exactly. -->
From tests/conftest.py:117-143 (FakeOutboundRepo shape to mirror for FakeCaptureRepo):
  class FakeOutboundRepo: __init__(self, should_raise=False); self.calls: list[dict]
    async def insert_outbound(self, pool, row) -> dict: append+optionally raise; else {"ok": True}

From tests/conftest.py:97-114 (signal_http respx fixture shape to mirror for whisper_http):
  @pytest.fixture def signal_http(): with respx.mock(assert_all_called=False) as m: yield m

From RESEARCH §"Whisper Fake/Stub Strategy" Option B (fake_transcribe_client):
  async def _fake_transcribe(arg): return {"ok": True, "text": "fake transcript", "duration_ms": 100, "language": "es"}
  fake_transcribe_client = {"transcribe": _fake_transcribe}
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Package legitimacy gate — python-ulid</name>
  <read_first>
    - src/farm-agent/pyproject.toml (current deps; python-ulid is NOT present)
    - .planning/phases/58-capture-transcription/58-RESEARCH.md §"Package Legitimacy Audit" (python-ulid tagged [ASSUMED] — slopcheck unavailable at research time; source repo github.com/mdomke/python-ulid)
    - .planning/phases/57-signal-i-o/57-01-PLAN.md Task 1 (the exact blocking-human legitimacy-gate shape this mirrors)
  </read_first>
  <what-built>Nothing yet — this is a pre-install legitimacy verification required by the package gate because `python-ulid` is a net-new install tagged [ASSUMED] in RESEARCH (slopcheck was unavailable).</what-built>
  <how-to-verify>
    1. Confirm `python-ulid` on https://pypi.org/project/python-ulid/ — source repo github.com/mdomke/python-ulid (Martin Domke), established (~5 yrs), MIT, active maintenance.
    2. Confirm the GitHub repo exists and is the same project (not a typosquat of `ulid-py`).
    3. `python-ulid` is [ASSUMED]→verified legitimate. Approve to proceed with the pinned add in Task 2. (Per RESEARCH, an inline 26-char base32 ULID is an acceptable zero-dependency fallback if the package is rejected — CONTEXT does not mandate an external package.)
  </how-to-verify>
  <acceptance_criteria>
    - Human confirms `python-ulid` is legitimate (not [SLOP]/[SUS]) before any install. This checkpoint is never auto-approvable (T-58-SC); workflow.auto_advance is ignored here.
  </acceptance_criteria>
  <resume-signal>Type "approved" to install python-ulid, or "inline" to use the zero-dependency fallback.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Add python-ulid + probe its timestamp-seeded API (A1); create capture/__init__.py</name>
  <read_first>
    - src/farm-agent/pyproject.toml ([project].dependencies block — add alongside httpx/psycopg)
    - .planning/phases/58-capture-transcription/58-RESEARCH.md §"Open Questions" Q1 + Assumptions A1 (the probe to run; fallback to ULID.from_timestamp if from_datetime is absent)
    - .planning/phases/58-capture-transcription/58-PATTERNS.md §"New Dependency Flag" (probe command + fallback)
    - src/farm-agent/farm_agent/signal_io/__init__.py (package-marker shape to mirror for capture/__init__.py)
  </read_first>
  <action>
    In pyproject.toml: add `python-ulid` (latest from `pip index versions python-ulid`, pinned `>=` the resolved minor) to `[project].dependencies`. Run `uv sync` to lock. Then PROBE the timestamp-seeded API: `cd src/farm-agent && uv run python -c "from ulid import ULID; print([m for m in dir(ULID) if 'time' in m.lower() or 'date' in m.lower()])"` (note the import is `from ulid import ULID` for python-ulid, NOT `python_ulid`). Confirm whether `from_datetime(dt)` and/or `from_timestamp(ms)` exist, and verify `str(ULID.from_datetime(datetime.fromtimestamp(captured_at_ms/1000, tz=timezone.utc)))` yields a 26-char string whose decoded time component equals captured_at_ms (within ms). Record the confirmed call form (the exact one Plan 03 must use) in the plan SUMMARY under an "A1 RESOLVED" heading. Create empty `farm_agent/capture/__init__.py` (package marker; Foray island — no chamber imports).
  </action>
  <verify>
    <automated>cd src/farm-agent && uv run python -c "from ulid import ULID; from datetime import datetime, timezone; ms=1718900000000; u=str(ULID.from_datetime(datetime.fromtimestamp(ms/1000, tz=timezone.utc))); assert len(u)==26, u; print('ulid ok', u)"</automated>
  </verify>
  <done>python-ulid in [project].dependencies + uv.lock updated; the import path (`from ulid import ULID`) and the timestamp-seeded call form are confirmed by the probe and recorded as "A1 RESOLVED" in the SUMMARY; `capture/__init__.py` exists.</done>
</task>

<task type="auto">
  <name>Task 3: Add FakeCaptureRepo, fake_transcribe_client, whisper_http fixtures to conftest</name>
  <read_first>
    - src/farm-agent/tests/conftest.py (full file — TEST_ENV, FakeOutboundRepo at :117-143, signal_http respx fixture at :97-114, pool socket-skip gate)
    - .planning/phases/58-capture-transcription/58-PATTERNS.md §"Test Pattern Assignments" (FakeCaptureRepo + fake_transcribe_client + whisper_http exact shapes) + §"Shared Patterns" item 6
    - .planning/phases/58-capture-transcription/58-RESEARCH.md §"Whisper Fake/Stub Strategy" (Option A respx for transcribe_client tests; Option B injected dict for pipeline tests)
  </read_first>
  <action>
    In conftest.py, add (no new file): (1) `class FakeCaptureRepo` mirroring FakeOutboundRepo — `__init__(self, should_raise=False)`, `self.calls: list[dict]`, `async def insert_capture(self, pool, row) -> dict` that appends row and either raises RuntimeError or returns {"ok": True}; expose it via a `fake_capture_repo` fixture. (2) A module-level `async def _fake_transcribe(arg)` returning `{"ok": True, "text": "fake transcript", "duration_ms": 100, "language": "es"}` and a `fake_transcribe_client` fixture returning `{"transcribe": _fake_transcribe}` (Option B — no respx). (3) A `whisper_http` respx fixture mirroring `signal_http` (`respx.mock(assert_all_called=False)` yielding the mock transport) for the real-httpx transcribe_client tests in Plan 02. Do not change TEST_ENV — whisper_url and capture_base_dir already have working defaults in TenantConfig.
  </action>
  <verify>
    <automated>cd src/farm-agent && uv run python -c "import tests.conftest as c; assert hasattr(c, 'FakeCaptureRepo'); r=c.FakeCaptureRepo(); print('conftest ok')" && uv run pytest tests/ -x -q -k "not capture"</automated>
  </verify>
  <done>conftest exposes FakeCaptureRepo (toggle-raise), a fake_transcribe_client fixture (async canned result), and a whisper_http respx fixture; the existing Phase 56/57 suite still passes (no regression).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev machine → PyPI | A net-new package (`python-ulid`) is pulled into the runtime; a malicious/typosquat package would execute in the alerter process |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-58-SC | Tampering | uv add python-ulid (supply chain) | mitigate | Task 1 blocking-human legitimacy checkpoint before any install; never auto-approvable; inline-ULID fallback available |
| T-58-01-01 | Tampering | wrong ULID API silently mis-seeds the PK time component | mitigate | Task 2 probe (A1) verifies the decoded time component == captured_at_ms before Plan 03 uses it |
</threat_model>

<verification>
- `grep -n 'python-ulid' src/farm-agent/pyproject.toml` returns a dependency line.
- `cd src/farm-agent && uv run pytest` (full suite) green — no regression in Phase 56/57 tests.
- The SUMMARY records "A1 RESOLVED" with the exact timestamp-seeded ULID call form for Plan 03.
</verification>

<success_criteria>
- python-ulid is a pinned runtime dep, locked in uv.lock, added only after human legitimacy approval.
- The A1 ULID-API question is resolved and the exact call form recorded for Plan 03.
- conftest exposes FakeCaptureRepo + fake_transcribe_client + whisper_http for Plans 02/03.
- `capture/__init__.py` exists as the Foray-island package marker.
</success_criteria>

<output>
Create `.planning/phases/58-capture-transcription/58-01-SUMMARY.md` when done (include the "A1 RESOLVED" ULID-API note).
</output>
</content>
</invoke>
