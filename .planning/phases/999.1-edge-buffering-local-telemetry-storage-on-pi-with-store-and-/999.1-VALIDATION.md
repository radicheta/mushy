---
phase: 999.1
slug: edge-buffering-fc1-replay-on-reconnect
status: draft
nyquist_compliant: true
wave_0_complete: true
created: 2026-05-02
---

# Phase 999.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework (bridge)** | jest 29.x (existing in `src/mission-control/bridge/`) |
| **Framework (fc-core)** | pytest 7.x (existing in `src/chambers/fc-core/fc_core/test/`) |
| **Config file (bridge)** | `src/mission-control/bridge/package.json` (jest section) |
| **Config file (fc-core)** | `src/chambers/fc-core/setup.cfg` |
| **Quick run command (bridge)** | `cd src/mission-control/bridge && npm test -- --testPathPattern='(schema_migration\|buffer_replay)'` |
| **Quick run command (fc-core)** | `cd src/chambers/fc-core && python -m pytest fc_core/test/test_fc_buffer.py -x` |
| **Full suite command (bridge)** | `cd src/mission-control/bridge && npm test` |
| **Full suite command (fc-core)** | `colcon test --packages-select fc_core --pytest-args -x` |
| **Estimated runtime** | ~15s bridge / ~25s fc-core |

---

## Sampling Rate

- **After every task commit:** Run the quick command for the affected side (bridge OR fc-core)
- **After every plan wave:** Run full suite for both sides
- **Before `/gsd-verify-work`:** Full suite must be green on both sides + manual acceptance test in Plan 04
- **Max feedback latency:** 25 seconds (slower of the two)

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Decision | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|----------|------------|-----------------|-----------|-------------------|-------------|--------|
| 999.1-01-01 | 01 | 1 | D-Discretion (ON CONFLICT DO NOTHING) | — | Schema migration is idempotent; pre-flight dedupe drops collisions before adding UNIQUE constraint | unit | `cd src/mission-control/bridge && npm test -- schema_migration.test.js` | ❌ W0 | ⬜ pending |
| 999.1-01-02 | 01 | 1 | D-09 (capture-all from manifest), D-11 (camera excluded) | — | YAML manifest validates: 9 fc1/* topics, no camera-class topics | unit | `cd src/mission-control/bridge && npm test -- schema_migration.test.js` (covers YAML lint as part of same suite) | ❌ W0 | ⬜ pending |
| 999.1-02-01 | 02 | 2 | D-05, D-06, D-07 (SQLite WAL, stdlib only, indexed since-X), D-10 (24h ring), D-11 (camera-pattern raise) | — | HTTP server only binds to Tailscale interface (default 100.96.239.75); writer is WAL; pruner deletes >24h | unit | `cd src/chambers/fc-core && python -m pytest fc_core/test/test_fc_buffer.py::TestFcBufferRed -x` | ❌ W0 | ⬜ pending |
| 999.1-02-02 | 02 | 2 | D-05..D-11 (full integration of fc_buffer node) | T-fc1-tailscale-only-bind | HTTP bind grep-pinned to `http_bind` parameter, never `0.0.0.0`; camera pattern guard in `_load_topics` | unit + grep | `cd src/chambers/fc-core && python -m pytest fc_core/test/test_fc_buffer.py -x` AND `! grep -q "'0.0.0.0'" src/chambers/fc-core/fc_core/fc_buffer.py` | ❌ W0 | ⬜ pending |
| 999.1-03-01 | 03 | 2 | D-01, D-02, D-03 (poll, gap-as-probe, idempotent + resumable) | — | 15s HTTP timeout; ON CONFLICT DO NOTHING; last_ingested_ns persisted to host volume | unit | `cd src/mission-control/bridge && npm test -- buffer_replay.test.js` | ❌ W0 | ⬜ pending |
| 999.1-03-02 | 03 | 2 | D-01..D-03 + bridge live-path correctness | — | All 9 `insertTelemetry('fc.*')` call sites pass `tsNs`; live-path uses `msg.header.stamp` where available; cursor advance on live inserts prevents post-restart 24h re-fetch | unit + grep | `cd src/mission-control/bridge && npm test` AND `[ "$(grep -c 'insertTelemetry(' src/mission-control/bridge/src/index.js)" -ge 9 ]` AND `grep -q 'tsNs' src/mission-control/bridge/src/index.js` | ❌ W0 | ⬜ pending |
| 999.1-04-01 | 04 | 3 | All D-XX (deploy) | — | Repo systemd unit diffed against fc1's `/etc/systemd/system/fc-core.service` BEFORE overwrite (per `feedback_diff_repo_vs_pi_systemd`); bridge rebuilt with `--build` | manual + automated | `ssh fc1-ts 'systemctl is-active fc-core'` returns `active`; `docker logs mushy-bridge-1 \| grep -q buffer_replay` shows poller boot | ❌ W0 | ⬜ pending |
| 999.1-04-02 | 04 | 3 | Phase goal acceptance | T-no-wlan0-down | Operator NEVER touches wlan0 (`feedback_no_interface_down`); pre+post wlan0 UP check; only `sudo tailscale down/up` used | manual | See "Manual-Only Verifications" below | ❌ W0 | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

*W0 = file does not exist yet; created during execution.*

---

## Wave 0 Requirements

- [ ] `src/mission-control/bridge/test/schema_migration.test.js` — RED tests for dedupe scan + UNIQUE constraint
- [ ] `src/mission-control/bridge/test/buffer_replay.test.js` — RED tests for poll loop, 15s timeout, idempotent insert
- [ ] `src/chambers/fc-core/fc_core/test/test_fc_buffer.py` — RED tests for SQLite WAL, since-X query, 24h pruner, camera-exclusion guard, HTTP bind parameter

Existing test infrastructure (jest, pytest) covers framework needs — no new framework install required.

---

## Manual-Only Verifications

| Behavior | Decision | Why Manual | Test Instructions |
|----------|----------|------------|-------------------|
| Induced 5-min Tailscale dropout produces visible gap in OpenMCT humidity chart that fills within 60s of `tailscale up` (with original timestamps) | Phase goal | Requires real fc1 hardware + farmer-observable Mission Control UI; cannot be unit-tested | (1) Open OpenMCT humidity chart at http://elder-plops:8080. (2) `ssh fc1-ts 'ip link show wlan0 \| grep -q "state UP"'` → must succeed. (3) `ssh fc1-ts sudo tailscale down`. (4) Wait 5 min. (5) `ssh fc1-ts sudo tailscale up`. (6) `ssh fc1-ts 'ip link show wlan0 \| grep -q "state UP"'` → must STILL succeed (proves wlan0 untouched). (7) Within 60s of step 5, the chart should backfill the 5-min gap with original-timestamped points. (8) Farmer attestation. |
| fc_buffer SQLite ring stays under predicted disk budget after 24h+ uptime | D-10 (24h ring) | Requires real-world telemetry rate, not synthetic | `ssh fc1-ts 'du -h /var/lib/fc_core/buffer.sqlite'` should report < 50 MB after a full day of uptime |

---

## Validation Sign-Off

- [x] All tasks have automated verify OR are Wave 0 dependencies (Plan 04 manual is necessarily so — listed in Manual-Only)
- [x] Sampling continuity: no 3 consecutive tasks without automated verify (every wave has automated coverage)
- [x] Wave 0 covers all MISSING references (3 new test files listed)
- [x] No watch-mode flags (all commands are one-shot)
- [x] Feedback latency < 25s
- [x] `nyquist_compliant: true` set in frontmatter

**Approval:** approved 2026-05-02
