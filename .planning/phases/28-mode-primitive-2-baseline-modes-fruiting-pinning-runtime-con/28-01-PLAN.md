---
phase: 28
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/chambers/fc-msgs/package.xml
  - src/chambers/fc-msgs/CMakeLists.txt
  - src/chambers/fc-msgs/msg/Mode.msg
  - src/chambers/fc-msgs/srv/SetMode.srv
  - src/chambers/fc-core/fc_core/test/test_controller_modes.py
  - src/mission-control/bridge/test/control_param.test.js
  - src/mission-control/bridge/test/control_persist.test.js
  - .planning/phases/28-mode-primitive-2-baseline-modes-fruiting-pinning-runtime-con/28-01-SPIKE.md
autonomous: false
requirements: [MODE-01, MODE-04, MODE-05]
must_haves:
  truths:
    - "fc_msgs package builds via colcon and Mode/SetMode interfaces import successfully on a freshly-built workspace"
    - "Wave 0 test scaffolds exist and FAIL meaningfully (not collection errors), enumerating the contracts later waves implement"
    - "rclnodejs SetParameters request shape against /fc_controller/set_parameters is captured in 28-01-SPIKE.md (or pivot recorded if the API differs from research §Pattern 4)"
    - "Bridge→fc1 SSH path is verified (bridge container can ssh ubuntu@172.16.10.5 ls /var/lib/fc-core/) OR pivot to fc_buffer-hosted persist endpoint is recorded in spike"
  artifacts:
    - path: "src/chambers/fc-msgs/package.xml"
      provides: "ament_cmake msg package metadata"
      contains: "rosidl_default_generators"
    - path: "src/chambers/fc-msgs/CMakeLists.txt"
      provides: "rosidl_generate_interfaces invocation for Mode.msg + SetMode.srv"
      contains: "rosidl_generate_interfaces"
    - path: "src/chambers/fc-msgs/msg/Mode.msg"
      provides: "current_mode topic payload schema (D-13)"
      contains: "string name"
    - path: "src/chambers/fc-msgs/srv/SetMode.srv"
      provides: "set_mode service signature (D-16)"
      contains: "fc_msgs/Mode active_mode"
    - path: "src/chambers/fc-core/fc_core/test/test_controller_modes.py"
      provides: "Wave 0 test scaffolds for MODE-01..04"
      min_lines: 40
    - path: ".planning/phases/28-mode-primitive-2-baseline-modes-fruiting-pinning-runtime-con/28-01-SPIKE.md"
      provides: "rclnodejs API findings + SSH-from-bridge findings; locks Layer 2 transport"
      min_lines: 30
  key_links:
    - from: "src/chambers/fc-msgs/CMakeLists.txt"
      to: "src/chambers/fc-msgs/msg/Mode.msg"
      via: "rosidl_generate_interfaces argument list"
      pattern: "msg/Mode.msg"
    - from: "src/chambers/fc-msgs/CMakeLists.txt"
      to: "src/chambers/fc-msgs/srv/SetMode.srv"
      via: "rosidl_generate_interfaces argument list"
      pattern: "srv/SetMode.srv"
---

<objective>
Foundation wave: create the `fc_msgs` ament_cmake package skeleton (Mode.msg + SetMode.srv), drop test scaffolds for every requirement (failing-but-collected pytest + jest), and run the Wave 0 SPIKE that locks the two architectural unknowns (rclnodejs SetParameters request shape; bridge→fc1 SSH credentialing).

Purpose: Avoid Pitfall 1 (ament_python cannot host msg/srv) and Open Questions 1+2 from RESEARCH.md before any downstream wave depends on them. Spike findings are load-bearing for Wave 4 (bridge endpoints).

Output: A buildable fc_msgs package with concrete msg/srv definitions, a populated test scaffold file ready for RED→GREEN cycles in Waves 2-4, and a SPIKE.md that either confirms research §Pattern 4 verbatim or records a pivot.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/28-mode-primitive-2-baseline-modes-fruiting-pinning-runtime-con/28-CONTEXT.md
@.planning/phases/28-mode-primitive-2-baseline-modes-fruiting-pinning-runtime-con/28-RESEARCH.md
@.planning/phases/28-mode-primitive-2-baseline-modes-fruiting-pinning-runtime-con/28-VALIDATION.md
@src/chambers/fc-core/package.xml
@src/chambers/fc-core/setup.py
@src/mission-control/bridge/package.json
@scripts/pi-deploy/deploy.sh

<interfaces>
<!-- Reference msg/srv specs from RESEARCH.md §Pattern 1 — copy verbatim into Mode.msg / SetMode.srv -->

`fc_msgs/msg/Mode.msg` (D-13):
```
string name
float32 target_humidity
float32 band_low
float32 band_high
string defend_side
float32 t_target
builtin_interfaces/Time effective_since
string source
```

`fc_msgs/srv/SetMode.srv` (D-16):
```
string name
---
bool success
string reason
fc_msgs/Mode active_mode
```

`fc_msgs/CMakeLists.txt` boilerplate (RESEARCH.md §Pattern 1 — verified against Jazzy tutorial):
```cmake
cmake_minimum_required(VERSION 3.8)
project(fc_msgs)
find_package(ament_cmake REQUIRED)
find_package(builtin_interfaces REQUIRED)
find_package(rosidl_default_generators REQUIRED)
rosidl_generate_interfaces(${PROJECT_NAME}
  "msg/Mode.msg"
  "srv/SetMode.srv"
  DEPENDENCIES builtin_interfaces
)
ament_export_dependencies(rosidl_default_runtime)
ament_package()
```

`fc_msgs/package.xml` boilerplate keys:
```xml
<buildtool_depend>ament_cmake</buildtool_depend>
<buildtool_depend>rosidl_default_generators</buildtool_depend>
<depend>builtin_interfaces</depend>
<exec_depend>rosidl_default_runtime</exec_depend>
<member_of_group>rosidl_interface_packages</member_of_group>
<export><build_type>ament_cmake</build_type></export>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold fc_msgs ament_cmake package with Mode.msg + SetMode.srv</name>
  <files>src/chambers/fc-msgs/package.xml, src/chambers/fc-msgs/CMakeLists.txt, src/chambers/fc-msgs/msg/Mode.msg, src/chambers/fc-msgs/srv/SetMode.srv</files>
  <read_first>
    - .planning/phases/28-mode-primitive-2-baseline-modes-fruiting-pinning-runtime-con/28-RESEARCH.md (§Pattern 1 + §Pitfall 1 — `fc_core` is ament_python and CANNOT host rosidl interfaces; this MUST be a separate ament_cmake package)
    - .planning/phases/28-mode-primitive-2-baseline-modes-fruiting-pinning-runtime-con/28-CONTEXT.md (D-13 = Mode.msg fields verbatim; D-16 = SetMode.srv shape)
    - src/chambers/fc-core/package.xml (sibling package; `fc_msgs` lives at the same depth — `src/chambers/fc-msgs/`)
  </read_first>
  <action>
    Create the new package at `src/chambers/fc-msgs/`. Use the Write tool for every file (no heredoc).

    1. `src/chambers/fc-msgs/package.xml` — package format=3, name=fc_msgs, version=0.0.1, maintainer=Santi (santi@example.com per fc_core's package.xml), description="Custom interfaces for fruiting chamber control (Mode + SetMode)", license=MIT, with these dep blocks per `<interfaces>` above plus `<test_depend>ament_lint_auto</test_depend>` and `<test_depend>ament_lint_common</test_depend>`.

    2. `src/chambers/fc-msgs/CMakeLists.txt` — copy the boilerplate from `<interfaces>` verbatim. Set `cmake_minimum_required(VERSION 3.8)`. Do NOT include any extra `find_package(rclpy)` or python bindings — rosidl handles language bindings automatically.

    3. `src/chambers/fc-msgs/msg/Mode.msg` — copy the field list from `<interfaces>` verbatim. Order matters; `effective_since` is `builtin_interfaces/Time` (NOT `builtin_interfaces/msg/Time`). NaN-when-unset for `t_target` is encoded by the publisher writing `float('nan')` — no schema-level enforcement.

    4. `src/chambers/fc-msgs/srv/SetMode.srv` — copy verbatim from `<interfaces>`. The `---` separator MUST be exactly three dashes on its own line. The response embeds `fc_msgs/Mode active_mode` (D-16 contract: caller gets the resolved mode back so UI can confirm).

    Do NOT add `fc_msgs` as a dep of `fc_core/package.xml` yet — that lands in plan 02 alongside the `from fc_msgs.msg import Mode` import in the controller (plan 03). Decoupling avoids breaking colcon if this plan is ever reverted.
  </action>
  <verify>
    <automated>cd /mnt/slime-kingdom/opt/mushy && colcon build --packages-select fc_msgs 2>&1 | tail -20 && python3 -c "import sys; sys.path.insert(0, 'install/fc_msgs/lib/python3.12/site-packages'); from fc_msgs.msg import Mode; from fc_msgs.srv import SetMode; m = Mode(); m.name='fruiting'; m.band_low=0.945; m.band_high=0.975; m.defend_side='both'; print('OK', type(m).__name__, type(SetMode.Request()).__name__)"</automated>
  </verify>
  <done>colcon build succeeds for fc_msgs; both `from fc_msgs.msg import Mode` and `from fc_msgs.srv import SetMode` import without error; field assignment to Mode succeeds; the python3 path glob may need adjustment for the local Jazzy python minor version (`ls install/fc_msgs/lib/python*/site-packages` if 3.12 differs).</done>
</task>

<task type="auto">
  <name>Task 2: Drop Wave 0 test scaffolds (pytest for fc_core, jest for bridge)</name>
  <files>src/chambers/fc-core/fc_core/test/test_controller_modes.py, src/mission-control/bridge/test/control_param.test.js, src/mission-control/bridge/test/control_persist.test.js</files>
  <read_first>
    - .planning/phases/28-mode-primitive-2-baseline-modes-fruiting-pinning-runtime-con/28-VALIDATION.md (§Per-Task Verification Map + §Phase Requirements → Test Map enumerates the test names later waves implement)
    - .planning/phases/28-mode-primitive-2-baseline-modes-fruiting-pinning-runtime-con/28-RESEARCH.md (§Validation Architecture → Test Map for the exact test_name → behavior pairs)
    - src/chambers/fc-core/fc_core/test/test_controller.py (existing patterns — rclpy fixture style, simulation_mode setup)
    - src/mission-control/bridge/test/buffer_replay.test.js (existing jest pattern — module mocks, async)
  </read_first>
  <action>
    Drop test files with `pytest.fail("RED: implemented in plan NN-NN")` / `test.todo("RED: implemented in plan NN-NN")` style stubs. The point is to enumerate the contracts so RED→GREEN is mechanical in later waves; collection MUST succeed.

    1. `src/chambers/fc-core/fc_core/test/test_controller_modes.py` — top-level docstring referencing 28-VALIDATION.md. Add stubs (plain functions with `pytest.fail("RED — landed in plan 28-NN")`):
       - `test_resolve_active_mode_fruiting` (plan 03; MODE-01)
       - `test_back_compat_default_fruiting` (plan 02; MODE-01 D-04)
       - `test_param_callback_band_invariant` (plan 04; MODE-01)
       - `test_param_callback_defend_side_enum` (plan 04; MODE-01)
       - `test_param_callback_unknown_mode` (plan 04; MODE-01)
       - `test_param_callback_batched_band_edit_atomic` (plan 04; MODE-05 Pitfall 4 — sending `[band_low=0.94, band_high=0.96]` in one batch passes; sending `[band_low=0.99]` alone when current band_high=0.97 fails atomically)
       - `test_fruiting_preserves_humid04` (plan 03; MODE-02)
       - `test_pinning_clamps_on_high_excursion` (plan 03; MODE-02 D-09)
       - `test_pinning_defends_floor` (plan 03; MODE-02)
       - `test_set_mode_service_takes_effect_in_one_tick` (plan 04; MODE-03)
       - `test_set_mode_rejects_unknown` (plan 04; MODE-03)
       - `test_mode_swap_bumpless` (plan 04; MODE-03 D-12)
       - `test_current_mode_topic_payload` (plan 04; MODE-04)
       - `test_current_mode_late_subscribe` (plan 04; MODE-04 D-14 — TRANSIENT_LOCAL)
       - `test_current_mode_republishes_on_band_change` (plan 04; MODE-04 D-15)
       - `test_current_mode_published_at_startup` (plan 04; MODE-04 — Pitfall: TRANSIENT_LOCAL does NOT survive process restart, controller MUST publish once at startup after _resolve_active_mode)

       Each stub: `def test_X(): pytest.fail("RED — landed in plan 28-NN")`. Import `pytest` at top.

    2. `src/mission-control/bridge/test/control_param.test.js` — jest stubs using `test.todo(...)`:
       - `'POST /control/param happy path: active_mode=pinning → SetParameters called with {type:4,string_value:"pinning"}'`
       - `'POST /control/param rejects non-allowlisted param humidifier_pin → 400'`
       - `'POST /control/param rejects out-of-range pid_kp=99 → 400 (allowlist range bound)'`
       - `'POST /control/param batches band_low+band_high in one SetParameters call when both present in body.params'` (Pitfall 4)
       - `'POST /control/param coerces JS Number to DOUBLE (type:3) for band_low, STRING (type:4) for active_mode, BOOL (type:1) — not present in allowlist but coverage of toParamValue table'`

    3. `src/mission-control/bridge/test/control_persist.test.js` — jest stubs:
       - `'POST /control/persist writes overlay yaml with fc_controller.ros__parameters tree'`
       - `'POST /control/persist atomic-rename: writes .tmp then renames; previous version preserved as .bak'`
       - `'POST /control/persist rejects path traversal in param name (e.g. modes.../etc/passwd)'`
       - `'POST /control/persist transport: SSH if D-17 Layer 2 SSH path verified, else fc_buffer HTTP relay per spike'` (this stub stays todo until Task 3 spike findings land)
  </action>
  <verify>
    <automated>cd /mnt/slime-kingdom/opt/mushy && pytest src/chambers/fc-core/fc_core/test/test_controller_modes.py --collect-only -q 2>&1 | tail -25 && cd src/mission-control/bridge && npx jest test/control_param.test.js test/control_persist.test.js --listTests 2>&1 | tail -10</automated>
  </verify>
  <done>pytest collects all 16 tests in test_controller_modes.py without ImportError or syntax errors (each fails with the RED message when run, which is correct); jest discovers both new test files; all `test.todo` entries appear in jest output as "todo".</done>
</task>

<task type="auto">
  <name>Task 3: Wave 0 SPIKE — verify rclnodejs SetParameters shape and bridge→fc1 SSH path; record findings</name>
  <files>.planning/phases/28-mode-primitive-2-baseline-modes-fruiting-pinning-runtime-con/28-01-SPIKE.md</files>
  <read_first>
    - .planning/phases/28-mode-primitive-2-baseline-modes-fruiting-pinning-runtime-con/28-RESEARCH.md (§Open Questions 1+2; §Pattern 4 verified shape; §Environment Availability bridge→fc1 SSH `[ASSUMED]`)
    - .planning/phases/28-mode-primitive-2-baseline-modes-fruiting-pinning-runtime-con/28-CONTEXT.md (D-17 — two-layer architecture; persistence path discretionary at `/var/lib/fc-core/runtime_overrides.yaml`; pivot allowed if SSH absent)
    - Memory `feedback_ssh_tailscale` — fc1 wg0 IP is 172.16.10.5; `fc1-ts` is stale post-2026-05-03 cutover.
  </read_first>
  <action>
    Two-part live spike against the running stack on elder-plops + fc1. Record findings into `28-01-SPIKE.md` with three sections: §A rclnodejs API, §B SSH path, §C decisions for downstream waves.

    §A — rclnodejs SetParameters shape (Open Question 1):
    1. Inspect bridge container's pinned rclnodejs version: `docker exec mushy-bridge-1 node -e "console.log(require('rclnodejs/package.json').version)"`. Record.
    2. Live-test the request shape against fc_controller (which is running on fc1 right now):
       ```
       docker exec mushy-bridge-1 node -e "
         const r = require('rclnodejs');
         (async () => {
           await r.init();
           const n = new r.Node('spike_setparam');
           const cli = n.createClient('rcl_interfaces/srv/SetParameters', '/fc_controller/set_parameters');
           const req = { parameters: [{ name: 'pid_kp', value: { type: 3, double_value: 0.35 } }] };
           cli.sendRequest(req, (resp) => { console.log('RESP', JSON.stringify(resp)); process.exit(0); });
           setTimeout(() => { console.log('TIMEOUT'); process.exit(2); }, 5000);
         })();
       "
       ```
       Pid_kp is already declared on fc_controller and 0.35 is its current value (per fc_config.yaml line 37) — this is a no-op SetParameters that exercises the wire shape without changing controller behavior.
    3. Capture: exact request field names (snake_case vs camelCase per rclnodejs codegen), exact response shape (`results: [{successful, reason}]` vs alternative). Record verbatim in §A. If TIMEOUT or shape differs from research §Pattern 4, document the actual shape — it becomes the contract for plan 05.
    4. If type-coercion footgun observed (JS Number → INTEGER instead of DOUBLE for `pid_kp`), document remediation: each allowlist entry MUST carry an explicit `expected_type` (1=BOOL, 2=INTEGER, 3=DOUBLE, 4=STRING) and a `toParamValue(v)` helper.

    §B — bridge→fc1 SSH path (Open Question 2):
    1. From the bridge container: `docker exec mushy-bridge-1 ssh -o BatchMode=yes -o StrictHostKeyChecking=no -o ConnectTimeout=5 ubuntu@172.16.10.5 'echo ok && id && ls -la /var/lib/fc-core/'`. Record stdout + exit code.
    2. If exit=0 with `ok` present → §C records "Layer 2 transport = SSH from bridge container; overlay path = /var/lib/fc-core/runtime_overrides.yaml; write user = ubuntu (matches fc-core.service User=ubuntu)."
    3. If exit≠0 (no key in container, no reachability, container-namespace bind mount issue) → §C records the **architectural pivot**: "Layer 2 transport = bridge POSTs payload to a new fc_buffer HTTP endpoint; fc_buffer (already on fc1, already binds 172.16.10.5:8765 per fc_config.yaml) writes the overlay locally as the `ubuntu` user. Plan 06 must add `POST /control/persist` to fc_buffer (Python rclpy + http.server already in that node) and the bridge calls it instead of SSH-ing."

    §C — decisions for downstream waves:
    - Lock the rclnodejs request shape verbatim (used by plan 05 in `control_param.js → toParamValue` table).
    - Lock the Layer 2 transport (SSH from bridge OR fc_buffer HTTP relay). Plan 06 reads this section verbatim.
    - Lock overlay path: `/var/lib/fc-core/runtime_overrides.yaml` (research §Pitfall 3 recommendation; directory exists with ubuntu ownership per fc-core.service ExecStartPre).
    - Lock overlay yaml namespace: `fc_controller:\n  ros__parameters:\n    ...` (research §Open Question 4 recommendation — narrows scope vs `/**:`).
    - Note any Pitfall 3 (deploy.sh PI_HOST=fc1-ts) findings — separate from §B but relevant: `deploy.sh:5` resolves `fc1-ts` today? If yes, leave alone (plan 07 handles); if no, deploy is broken right now and plan 07's deploy.sh edit becomes blocker-priority.
  </action>
  <verify>
    <automated>test -f .planning/phases/28-mode-primitive-2-baseline-modes-fruiting-pinning-runtime-con/28-01-SPIKE.md && grep -q "## §A" .planning/phases/28-mode-primitive-2-baseline-modes-fruiting-pinning-runtime-con/28-01-SPIKE.md && grep -q "## §B" .planning/phases/28-mode-primitive-2-baseline-modes-fruiting-pinning-runtime-con/28-01-SPIKE.md && grep -q "## §C" .planning/phases/28-mode-primitive-2-baseline-modes-fruiting-pinning-runtime-con/28-01-SPIKE.md && grep -qE "(SSH|fc_buffer HTTP relay)" .planning/phases/28-mode-primitive-2-baseline-modes-fruiting-pinning-runtime-con/28-01-SPIKE.md</automated>
  </verify>
  <done>28-01-SPIKE.md exists with §A (rclnodejs request shape captured verbatim from a live call OR documented as TIMEOUT/error with proposed mitigation), §B (SSH probe result + chosen transport — SSH or fc_buffer relay), §C (locked decisions for plans 05 + 06: rclnodejs shape, Layer 2 transport, overlay path, overlay namespace).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Operator review of SPIKE.md before locking downstream architecture</name>
  <what-built>
    fc_msgs package skeleton (Mode.msg + SetMode.srv); 16 RED pytest stubs + 9 jest todo stubs that enumerate the contracts plans 02-06 will implement; 28-01-SPIKE.md with locked decisions for the rclnodejs request shape and Layer 2 transport (SSH-from-bridge or fc_buffer HTTP relay).
  </what-built>
  <files>.planning/phases/28-mode-primitive-2-baseline-modes-fruiting-pinning-runtime-con/28-01-SPIKE.md</files>
  <action>
    Operator review only — no file mutation. Read 28-01-SPIKE.md §C and verify the locked decisions match the live stack's actual capabilities. See <how-to-verify> for the three review steps.
  </action>
  <how-to-verify>
    1. Read `.planning/phases/28-mode-primitive-2-baseline-modes-fruiting-pinning-runtime-con/28-01-SPIKE.md` — confirm the §C decisions are coherent with the constraints in CONTEXT.md (D-17 two-layer; D-19 explicit Save-to-repo; D-20 farmOS UI surface).
    2. Confirm the Layer 2 transport choice: if SSH-from-bridge is locked, plan 06 stays inside the bridge code path; if fc_buffer relay is locked, plan 06 will modify `src/chambers/fc-core/fc_core/fc_buffer.py` to add a `POST /control/persist` endpoint AND the bridge to call it. Either path is valid — confirm which one matches what's actually feasible against the live stack.
    3. (Optional) Spot-check that `colcon build --packages-select fc_msgs` succeeds in your local clone before proceeding.
  </how-to-verify>
  <verify>Operator types "approved" — no automated verify (this is a checkpoint).</verify>
  <done>Operator approval received OR explicit pivot instructions captured.</done>
  <resume-signal>Type "approved" to proceed to Wave 1 (plan 28-02), or describe pivots needed in SPIKE.md decisions.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| bridge container ↔ fc1 SSH | Spike probes whether the bridge container has key-based access to fc1 as `ubuntu`. If yes, that key is a high-value secret; if no, we deliberately keep the bridge container off the SSH-key blast radius and use fc_buffer HTTP relay. |
| operator ↔ rclnodejs SetParameters | Spike calls SetParameters as a no-op (pid_kp=current_value); no controller-state mutation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-01 | Information Disclosure | spike SetParameters call | accept | Spike sends pid_kp=current_value (no-op); no new data exposed; log goes to bridge container stdout, not external sink. |
| T-28-02 | Tampering | spike SSH probe | accept | `ssh -o BatchMode=yes` only, no commands beyond `echo ok && id && ls`. Read-only check; cannot modify fc1. |
| T-28-03 | Elevation of Privilege | bridge container holding SSH key to fc1 ubuntu | mitigate | If the spike finds a key inside the container, plan 06 still defaults to fc_buffer-HTTP-relay path unless operator explicitly affirms the key's presence is intentional (matches D-19 conservative posture). Reduces blast radius of a bridge container compromise. |
</threat_model>

<verification>
- `colcon build --packages-select fc_msgs` passes from a clean state
- `pytest src/chambers/fc-core/fc_core/test/test_controller_modes.py --collect-only` reports 16 tests
- `npx jest --listTests` in bridge/ includes the two new files
- `28-01-SPIKE.md` populated with all three §A/§B/§C sections
</verification>

<success_criteria>
- fc_msgs builds and Python bindings import (Pitfall 1 dodged)
- All Wave 0 test names from 28-VALIDATION.md §Phase Requirements → Test Map are present in test_controller_modes.py as RED stubs
- Spike either confirms research §Pattern 4 verbatim or documents the actual rclnodejs shape and Layer 2 transport pivot in 28-01-SPIKE.md
- Operator approves spike findings before any downstream wave touches them
</success_criteria>

<output>
After completion, create `.planning/phases/28-mode-primitive-2-baseline-modes-fruiting-pinning-runtime-con/28-01-SUMMARY.md` recording: fc_msgs package layout, the locked rclnodejs request shape (§A), the locked Layer 2 transport (§B), and any deviations from research §Pattern 4 / §Pitfall 3.
</output>
