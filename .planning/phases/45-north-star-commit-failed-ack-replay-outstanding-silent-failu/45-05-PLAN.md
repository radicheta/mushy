---
phase: 45-north-star-commit-failed-ack-replay-outstanding-silent-failu
plan: 05
type: execute
wave: 3
depends_on: [45-04]
files_modified:
  - scripts/phase-45-backfill-outcome-acks.js
  - .planning/phases/45-north-star-commit-failed-ack-replay-outstanding-silent-failu/45-05-live-fire-prefix.md
  - .planning/phases/45-north-star-commit-failed-ack-replay-outstanding-silent-failu/45-05-live-fire-vikki-rambo.jsonl
  - .planning/phases/45-north-star-commit-failed-ack-replay-outstanding-silent-failu/45-05-live-fire-santi-lima.jsonl
autonomous: false
requirements: [ACK-01, ACK-02, ACK-03, ACK-04]

must_haves:
  truths:
    - "Draft b8a1e586… (Vikki Rambo, observation_requires_target) produces a farmer-facing failure ack delivered to Vikki's Signal DM, English, no em-dashes, named address."
    - "Draft 1fb28e70… (Santi LIMA) likewise produces a delivered failure ack to Santi's Signal DM."
    - "A second invocation of the backfill on either draft does NOT re-send the ack (idempotency via outcome_ack_sent_at; ACK-04 c-case)."
    - "Pre-fix and post-fix transcripts are persisted as named siblings under the phase directory per [[feedback_keep_paper_trail_of_intermediates]] + [[feedback_persist_paid_results_default]]."
    - "Both farmers (Vikki, Santi) confirm receipt by Signal-paste verification per [[feedback_verify_signal_send_attribution]]."
  artifacts:
    - path: "scripts/phase-45-backfill-outcome-acks.js"
      provides: "one-time backfill script that takes a list of draft ids and re-runs the dispatch path via the deployed outboundConfirm + commit-watchdog idempotency claim"
    - path: ".planning/phases/45-north-star-commit-failed-ack-replay-outstanding-silent-failu/45-05-live-fire-vikki-rambo.jsonl"
      provides: "append-only JSONL: pre-state snapshot of draft b8a1e586… + dispatch invocation + post-state + farmer-paste verification"
    - path: ".planning/phases/45-north-star-commit-failed-ack-replay-outstanding-silent-failu/45-05-live-fire-santi-lima.jsonl"
      provides: "same shape for draft 1fb28e70…"
  key_links:
    - from: "scripts/phase-45-backfill-outcome-acks.js"
      to: "src/agents/alerter/src/confirm/outbound-confirm.js (deployed)"
      via: "require the alerter module + call dispatch('send_commit_outcome_ack', ...) on the prod pool"
      pattern: "send_commit_outcome_ack"
    - from: "scripts/phase-45-backfill-outcome-acks.js"
      to: "src/agents/alerter/src/farmos/commit-db.js (deployed)"
      via: "tryMarkOutcomeAckSent claim — script honors the same idempotency, does NOT bypass"
      pattern: "tryMarkOutcomeAckSent"
---

<objective>
Live-fire UAT: replay the two outstanding silent-failure drafts through the now-shipping ack path. This is the ship-gate per [[feedback_unit_tests_dont_catch_wiring]] + [[feedback_real_data_before_ship_gate_pass]]. Also closes the 2026-05-15 NORTH-STAR violation on the production side — Vikki Rambo and Santi finally get the ack they were owed.

Sequence:
1. Confirm Plan 04 is deployed to prod (commit SHA recorded in 45-04-SUMMARY.md).
2. Snapshot pre-state of both drafts from prod signal_draft table (status, outcome_ack_sent_at, commit_failed_reason).
3. Run the backfill script for draft b8a1e586… → Vikki receives ack.
4. Verify with Vikki by Signal-paste (per [[feedback_verify_signal_send_attribution]] — reconstruct body length, not just timing).
5. Run for draft 1fb28e70… → Santi receives ack.
6. Verify with Santi.
7. Re-run the backfill once for each draft to prove idempotency (no second send).
8. Persist pre/post transcripts as named JSONL siblings.

Purpose: ACK-02, ACK-03 satisfied; ACK-01 + ACK-04 production-proven; NORTH-STAR violation closed.
Output: 2 delivered acks, 2 JSONL artifacts, 1 backfill script, farmer-paste confirmations recorded.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/45-north-star-commit-failed-ack-replay-outstanding-silent-failu/45-CONTEXT.md
@.planning/notes/2026-05-17-northstar-ack-sketch.md
@.planning/phases/45-north-star-commit-failed-ack-replay-outstanding-silent-failu/45-04-SUMMARY.md

<interfaces>
**Prod signal_draft access:**
- The alerter container connects to the prod timescale pool. The backfill script must use the SAME connection string (read from the same env vars / .env the alerter uses — typically TIMESCALE_PASSWORD + host config).
- Both drafts live in the prod signal_draft table with status=commit_failed today (verify via SELECT before running).

**Backfill mechanism (does NOT bypass idempotency):**
- The script loads the deployed outbound-confirm + commit-db modules from the alerter package (`require('../src/agents/alerter/src/confirm/outbound-confirm')` etc.).
- For each target draftId: SELECT * FROM signal_draft WHERE id = $1 → row.
- Call `commitDb.tryMarkOutcomeAckSent(pool, draftId)`.
- If ok=true → `outboundConfirm.dispatch('send_commit_outcome_ack', row, {outcome: 'failed', reason: row.commit_failed_reason || 'generic_validation_error'})`.
- If ok=false (already-claimed) → log "already_sent_skip" and move on. This is what proves ACK-04 part (c) repeat-invocation idempotency.

**Live-fire artifact rule (CONTEXT.md decision "Persist live-fire results"):**
- Each JSONL line: `{ ts, phase: 'pre'|'dispatch'|'post'|'farmer_paste'|'idempotency_recheck', draft_id, row_snapshot?, dispatch_result?, farmer_reply_text?, notes? }`.
- Append-only. Never overwrite. Per [[feedback_persist_paid_results_default]].

**Farmer-paste verification (CONTEXT.md + memory link):**
- After dispatch claims ok, ask the farmer (Vikki / Santi) to paste back the ack message they received. Record the paste verbatim in the JSONL.
- Compare paste length and key tokens (named address, log_type label, reason phrase) against the rendered string the script logged at dispatch time.
- Mismatch → STOP; root-cause before continuing.

**Identity note:**
- Vikki Rambo = farmer #2 (Signal handle "Vikki" / "vikki rambo"). Per [[farmer-language-stacks]] she is English-first.
- Santi (Don Santiago, El Presidente) = farmer #1. English per [[farmer-language-stacks]].
- Both farmers' Signal numbers resolved via FarmOS people directory per [[project_farmos_people_directory_seed]] — already done at draft-create time, stored on draftRow.sender_e164.
- Per [[feedback_no_silent_failure_after_farmer_confirm]] the hard rule binds for Vikki regardless of Santi-relaxation memory; this phase is precisely the rule being honored.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author scripts/phase-45-backfill-outcome-acks.js (script only, no run)</name>
  <files>scripts/phase-45-backfill-outcome-acks.js</files>
  <read_first>
    - src/agents/alerter/src/confirm/outbound-confirm.js (deployed dispatch contract)
    - src/agents/alerter/src/farmos/commit-db.js (tryMarkOutcomeAckSent signature)
    - src/agents/alerter/src/index.js (boot wiring — how pool + confirmOutbound + commitDb are constructed; the script must reuse the same construction)
    - Any existing scripts under scripts/ that connect to the prod alerter DB (mirror their env-loading + pool-construction)
  </read_first>
  <action>
    Author a CommonJS Node script that:
    1. Parses CLI args: `node scripts/phase-45-backfill-outcome-acks.js --draft-id <hex> [--draft-id <hex> ...] [--dry-run]`.
    2. Loads the prod alerter env vars (same .env loading the alerter container uses).
    3. Constructs a pg pool against prod timescale.
    4. Constructs (or requires constructed) outboundConfirm + commitDb the same way index.js does.
    5. For each draftId:
       a. SELECT * FROM signal_draft WHERE id=$1. If not found → log + skip.
       b. If --dry-run: print the row + the would-be rendered ack body (call renderOutcomeAck directly without dispatching) and skip the rest.
       c. Else: call tryMarkOutcomeAckSent. If ok=false → log "already_sent_skip"; emit a JSONL line with phase='idempotency_recheck' and continue.
       d. If ok=true → call outboundConfirm.dispatch('send_commit_outcome_ack', row, {outcome:'failed', reason: row.commit_failed_reason || 'generic_validation_error'}); emit JSONL phase='dispatch' with the dispatch return value.
       e. SELECT outcome_ack_sent_at again; emit JSONL phase='post' with the post-row snapshot.
    6. Exits 0 on success, non-zero on any unhandled error.
    7. JSONL output path: prints to stdout in NDJSON; the operator pipes/redirects to the named sibling file per CONTEXT.md persistence rule. (Or: accept `--out <path>` and append; script chooses based on existence — but stdout-redirect is simpler and matches the "save as named siblings" pattern.)

    Critically: the script MUST NOT bypass tryMarkOutcomeAckSent. The whole point of going through the deployed claim path is to prove the idempotency primitive end-to-end on real prod data.

    No fenced code blocks in this action — script structure described above is the contract.
  </action>
  <verify>
    <automated>node scripts/phase-45-backfill-outcome-acks.js --help 2>&1 | head -20 || node -c scripts/phase-45-backfill-outcome-acks.js</automated>
  </verify>
  <acceptance_criteria>
    - File exists.
    - `node -c scripts/phase-45-backfill-outcome-acks.js` (syntax check) exits 0.
    - grep "tryMarkOutcomeAckSent\|send_commit_outcome_ack" scripts/phase-45-backfill-outcome-acks.js shows both references.
    - grep "dry-run\|--draft-id" scripts/phase-45-backfill-outcome-acks.js shows CLI handling.
    - Script does NOT contain any `UPDATE signal_draft SET outcome_ack_sent_at` outside of indirect calls to commitDb.tryMarkOutcomeAckSent (verifiable by source inspection: no raw SQL on outcome_ack_sent_at in the script).
  </acceptance_criteria>
  <done>Script authored, syntactically valid, ready for dry-run.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Pre-flight — confirm Plan 04 deployed + dry-run both drafts</name>
  <what-built>
    Plan 04 changes shipped to the alerter container in prod. Plan 05's backfill script exists. We are about to send real Signal messages to two real farmers.
  </what-built>
  <how-to-verify>
    Run, in order, and paste the output into 45-05-live-fire-prefix.md:
    1. Confirm prod alerter contains Plan 04 changes:
       `docker exec mushy-alerter-1 grep -c send_commit_outcome_ack /app/src/farmos/commit-watchdog.js` → expect ≥ 2.
       (If container name differs, use `docker ps | grep alerter` to find it.)
    2. Confirm signal_draft has the new column on prod:
       `docker exec mushy-timescale-1 psql -U postgres -d alerter -c "\\d signal_draft" | grep outcome_ack_sent_at` → expect 1 line.
    3. Confirm the two drafts exist + are still status=commit_failed + outcome_ack_sent_at IS NULL:
       `docker exec mushy-timescale-1 psql -U postgres -d alerter -c "SELECT id, status, commit_failed_reason, outcome_ack_sent_at, sender_e164 FROM signal_draft WHERE id LIKE 'b8a1e586%' OR id LIKE '1fb28e70%';"`
    4. Dry-run the script against the two drafts:
       `node scripts/phase-45-backfill-outcome-acks.js --draft-id <full-vikki-id> --draft-id <full-santi-id> --dry-run | tee .planning/phases/45-north-star-commit-failed-ack-replay-outstanding-silent-failu/45-05-live-fire-prefix.md`
    5. Read the rendered ack bodies in the dry-run output. Verify:
       - English text, no em-dashes (grep `—` returns 0).
       - Named address present: Vikki's ack contains "Vikki," (or her sender_name), Santi's contains "Santi" or "Don Santiago" (whichever sender_name resolves).
       - Reason phrase = the farmer-vocab phrase for observation_requires_target on Vikki's (likely "Couldn't match a block").
       - "Send EDIT to fix or NO to drop." is the closing affordance.

    If anything is off — STOP. Do not proceed to live-fire. Re-run Plan 04 fixes first.
  </how-to-verify>
  <resume-signal>Type "dry-run looks good, proceed to live-fire" or describe what's wrong.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Live-fire — replay draft b8a1e586… (Vikki Rambo)</name>
  <files>.planning/phases/45-north-star-commit-failed-ack-replay-outstanding-silent-failu/45-05-live-fire-vikki-rambo.jsonl</files>
  <read_first>
    - .planning/phases/45-north-star-commit-failed-ack-replay-outstanding-silent-failu/45-05-live-fire-prefix.md (from Task 2; contains the dry-run snapshot + full draft id)
    - scripts/phase-45-backfill-outcome-acks.js
  </read_first>
  <action>
    1. Snapshot pre-state into the JSONL file (append `{ts, phase:'pre', draft_id, row_snapshot}` JSON line). Use the psql SELECT from Task 2 to capture row_snapshot.
    2. Run the script (NO --dry-run this time) for ONLY the Vikki draft:
       `node scripts/phase-45-backfill-outcome-acks.js --draft-id <full-vikki-id> >> .planning/phases/45-north-star-commit-failed-ack-replay-outstanding-silent-failu/45-05-live-fire-vikki-rambo.jsonl`
       The script's own JSONL output (phase='dispatch', phase='post') appends to the same file.
    3. Confirm Signal delivery on the alerter container side:
       `docker logs mushy-alerter-1 --since 5m 2>&1 | grep -i "send_commit_outcome_ack\|outbound-confirm"` → expect a line indicating dispatch sent for the Vikki draft.
    4. Append `{ts, phase:'alerter_log_grep', stdout: <grep output>}` as a JSONL line.

    Do NOT yet run the idempotency recheck (Task 5) or the Santi replay (Task 4). One farmer at a time, per [[feedback_smoke_before_expensive_batch]].
  </action>
  <verify>
    <automated>test -s .planning/phases/45-north-star-commit-failed-ack-replay-outstanding-silent-failu/45-05-live-fire-vikki-rambo.jsonl && grep -c '"phase":"dispatch"' .planning/phases/45-north-star-commit-failed-ack-replay-outstanding-silent-failu/45-05-live-fire-vikki-rambo.jsonl</automated>
  </verify>
  <acceptance_criteria>
    - JSONL file exists, non-empty.
    - Contains at least one line with `"phase":"pre"`, one with `"phase":"dispatch"`, one with `"phase":"post"`.
    - The post snapshot shows outcome_ack_sent_at IS NOT NULL.
    - Alerter container log shows the dispatch happened for Vikki's draft id.
  </acceptance_criteria>
  <done>Ack dispatched for Vikki; awaiting farmer-paste verification (next checkpoint).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Vikki farmer-paste verification</name>
  <what-built>
    The script dispatched a failure ack to Vikki's Signal DM ~moments ago.
  </what-built>
  <how-to-verify>
    1. Ping Vikki on Signal (DM) with a brief message in English asking her to paste back the message she just received from the bot. Wording suggestion (do not include em-dashes): "Hi Vikki, the bot just sent you a follow-up about the May 15 entry. Could you paste back exactly what you got? Thanks."
    2. Wait for her paste.
    3. Append to the JSONL file:
       `{ts, phase:'farmer_paste', draft_id:'b8a1e586…', farmer:'vikki_rambo', paste_text:'<verbatim>'}`
    4. Verify the paste against the rendered body the script logged at dispatch time (also in the JSONL). Check:
       - Named address ("Vikki," prefix)
       - English text, no em-dashes
       - log_type label present (likely "observation")
       - Reason phrase: "Couldn't match a block" (the farmer-vocab for observation_requires_target)
       - Closing: "Send EDIT to fix or NO to drop."
    5. Length sanity per [[feedback_verify_signal_send_attribution]] — body length within ±5 chars of the rendered string the script logged.

    Mismatch handling:
    - Bot sent a DIFFERENT message than expected → STOP, investigate before Santi run.
    - Vikki didn't receive anything → check signal-cli send logs; STOP.
    - Mismatch is cosmetic (e.g. trailing whitespace) → note in JSONL and continue.
  </how-to-verify>
  <resume-signal>Type "vikki paste verified, proceed to santi" or describe mismatch.</resume-signal>
</task>

<task type="auto">
  <name>Task 5: Live-fire — replay draft 1fb28e70… (Santi LIMA) + idempotency recheck on both</name>
  <files>.planning/phases/45-north-star-commit-failed-ack-replay-outstanding-silent-failu/45-05-live-fire-santi-lima.jsonl, .planning/phases/45-north-star-commit-failed-ack-replay-outstanding-silent-failu/45-05-live-fire-vikki-rambo.jsonl</files>
  <read_first>
    - .planning/phases/45-north-star-commit-failed-ack-replay-outstanding-silent-failu/45-05-live-fire-vikki-rambo.jsonl (the just-completed Vikki run, for shape reference)
    - scripts/phase-45-backfill-outcome-acks.js
  </read_first>
  <action>
    1. Pre-snapshot Santi draft 1fb28e70… (psql SELECT, append `{phase:'pre',…}` to the new santi JSONL).
    2. Run the script for ONLY Santi's draft, appending to 45-05-live-fire-santi-lima.jsonl.
    3. Verify alerter container log shows dispatch for Santi's draft id (append `{phase:'alerter_log_grep',…}`).
    4. Idempotency recheck — ACK-04 (c) repeat-invocation:
       a. Re-run the script for BOTH drafts (no flags) and append outputs:
          - Vikki: `node scripts/phase-45-backfill-outcome-acks.js --draft-id <vikki-id> >> .../45-05-live-fire-vikki-rambo.jsonl`
          - Santi: `node scripts/phase-45-backfill-outcome-acks.js --draft-id <santi-id> >> .../45-05-live-fire-santi-lima.jsonl`
       b. Both runs MUST emit `{phase:'idempotency_recheck', result:'already_sent_skip'}` lines and NO new dispatch lines.
       c. Verify the farmers do NOT receive a second message: `docker logs mushy-alerter-1 --since 1m 2>&1 | grep -c "send_commit_outcome_ack"` → 0.
       d. Append `{ts, phase:'idempotency_verified', result:'no_double_send'}` lines to both files.
  </action>
  <verify>
    <automated>grep -c '"phase":"idempotency_recheck"' .planning/phases/45-north-star-commit-failed-ack-replay-outstanding-silent-failu/45-05-live-fire-vikki-rambo.jsonl .planning/phases/45-north-star-commit-failed-ack-replay-outstanding-silent-failu/45-05-live-fire-santi-lima.jsonl</automated>
  </verify>
  <acceptance_criteria>
    - Santi JSONL file exists with phase entries: pre, dispatch, post, alerter_log_grep, idempotency_recheck, idempotency_verified.
    - Vikki JSONL file has appended idempotency_recheck + idempotency_verified entries.
    - `docker logs mushy-alerter-1 --since 5m 2>&1 | grep -c "send_commit_outcome_ack.*${SANTI_DRAFT_ID}"` shows exactly 1 (the original Santi dispatch, no second).
    - `docker logs mushy-alerter-1 --since 5m 2>&1 | grep -c "send_commit_outcome_ack.*${VIKKI_DRAFT_ID}"` shows exactly 1 (the original Vikki dispatch from Task 3, no second).
  </acceptance_criteria>
  <done>Santi dispatched; idempotency proven on both drafts (no double-send).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 6: Santi farmer-paste verification + close NORTH-STAR</name>
  <what-built>
    Santi's ack dispatched; both drafts' idempotency proven; no double-sends.
  </what-built>
  <how-to-verify>
    1. Ping Santi (Don Santiago) on Signal DM asking him to paste back the bot's recent message. Wording: "Hey Santi, the bot just sent a follow-up on the May 15 LIMA entry. Paste back what you got?"
    2. Wait for paste.
    3. Append to 45-05-live-fire-santi-lima.jsonl: `{ts, phase:'farmer_paste', draft_id:'1fb28e70…', farmer:'santi', paste_text:'<verbatim>'}`.
    4. Verify against the rendered body the script logged (same checklist as Vikki paste):
       - Named address (Santi / Don Santiago)
       - English, no em-dashes
       - Reason phrase matches the farmer-vocab for Santi's commit_failed_reason
       - "Send EDIT to fix or NO to drop." closing
    5. Final NORTH-STAR audit — open the prod signal_draft table and run:
       `docker exec mushy-timescale-1 psql -U postgres -d alerter -c "SELECT COUNT(*) FROM signal_draft WHERE status='commit_failed' AND outcome_ack_sent_at IS NULL;"` → expected 0 (no silent failures remaining in prod).
    6. If count > 0, those drafts are NEW silent failures that have accumulated since Plan 04 deployment — investigate (likely outboundConfirm wiring drift or signal-cli outage). Re-run the backfill on them as a bonus pass, but only AFTER root-causing why the live path didn't ack them.
  </how-to-verify>
  <resume-signal>Type "santi paste verified, north-star closed" or describe what remains open.</resume-signal>
</task>

</tasks>

<verification>
- 2 JSONL artifacts exist under the phase directory and are append-only (modification timestamps strictly increasing, never overwritten).
- `SELECT COUNT(*) FROM signal_draft WHERE status='commit_failed' AND outcome_ack_sent_at IS NULL` on prod = 0.
- Both farmer-paste checkpoints passed.
- No double-send observed on idempotency rechecks.
- The 2026-05-15 NORTH-STAR violation is closed (both originally-silent failures are now acked).
</verification>

<success_criteria>
- ACK-02: Vikki Rambo draft acked with delivery-confirmed message; farmer paste matches rendered body.
- ACK-03: Santi LIMA draft acked likewise.
- ACK-04: Repeat-invocation idempotency proven on prod, no double-sends.
- ACK-01: Prod silent-failure count = 0 at end of plan.
- Paper trail persisted as JSONL per [[feedback_keep_paper_trail_of_intermediates]] + [[feedback_persist_paid_results_default]].
</success_criteria>

<output>
Create `.planning/phases/45-north-star-commit-failed-ack-replay-outstanding-silent-failu/45-05-SUMMARY.md` when done. Include:
- Final prod silent-failure count (should be 0).
- Both farmer-paste verbatim text snippets.
- Links to both JSONL artifacts.
- A one-line entry suitable for ROADMAP retrospective: "Phase 45 shipped <date>: NORTH-STAR commit_failed ack live; both 2026-05-15 silent failures closed via backfill."
</output>
