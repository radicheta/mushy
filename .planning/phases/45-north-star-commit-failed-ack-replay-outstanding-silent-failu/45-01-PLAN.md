---
phase: 45-north-star-commit-failed-ack-replay-outstanding-silent-failu
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/agents/alerter/src/farmos/commit-db.js
  - src/agents/alerter/test/farmos/commit-db.test.js
autonomous: true
requirements: [ACK-04]

must_haves:
  truths:
    - "signal_draft has an outcome_ack_sent_at timestamptz column after boot"
    - "tryMarkOutcomeAckSent(draftId) returns the draft id exactly once for a given draft, null on every subsequent call"
    - "Concurrent calls to tryMarkOutcomeAckSent on the same draft converge to exactly one winner (CAS via UPDATE ... WHERE outcome_ack_sent_at IS NULL RETURNING)"
  artifacts:
    - path: "src/agents/alerter/src/farmos/commit-db.js"
      provides: "outcome_ack_sent_at migration + tryMarkOutcomeAckSent helper"
      contains: "outcome_ack_sent_at"
    - path: "src/agents/alerter/test/farmos/commit-db.test.js"
      provides: "Unit tests for tryMarkOutcomeAckSent (first call wins, second returns null, idempotency under simulated concurrency)"
  key_links:
    - from: "src/agents/alerter/src/farmos/commit-db.js"
      to: "signal_draft table"
      via: "ALTER TABLE ... IF NOT EXISTS in initCommitSchema() (boot-time)"
      pattern: "ALTER TABLE signal_draft ADD COLUMN IF NOT EXISTS outcome_ack_sent_at"
---

<objective>
Schema migration + idempotency primitive: add `signal_draft.outcome_ack_sent_at timestamptz` column and the `tryMarkOutcomeAckSent(pool, draftId)` helper that performs a conditional UPDATE returning the draft id only when the column was NULL. This is the mark-then-send claim that prevents double-sending acks (ACK-04).

Purpose: gives Plan 04 (wiring) a race-safe claim primitive before any dispatch hook is added. Ships alone; no farmer-visible behavior yet.
Output: column added at boot, helper exported from commit-db.js with passing unit tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/45-north-star-commit-failed-ack-replay-outstanding-silent-failu/45-CONTEXT.md
@.planning/notes/2026-05-17-northstar-ack-sketch.md
@src/agents/alerter/src/farmos/commit-db.js

<interfaces>
Existing migration convention in commit-db.js (Phase 40 D-02/D-07):
- initCommitSchema(pool) runs `ALTER TABLE signal_draft ADD COLUMN IF NOT EXISTS <name> <type>` statements sequentially at boot.
- Existing columns added by this module: farmos_response jsonb, committed_at timestamptz, commit_failed_reason text, commit_attempt_count int, committed_at_attempt timestamptz.
- Existing index: idx_signal_draft_status_confirmed on (status, confirmed_at) WHERE status IN ('confirmed','committing').
- initCommitSchema is invoked from src/agents/alerter/src/index.js boot path (search for `commit columns + index initialized` log line).

Existing helper signatures in commit-db.js (study before adding tryMarkOutcomeAckSent):
- markCommitted(pool, draftId, payload)
- markFailed(pool, draftId, reason)
- requeueForRetry(pool, draftId)
- releaseStaleLocks(pool, staleMin)
All return raw query results or void; errors propagate.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add outcome_ack_sent_at column to initCommitSchema</name>
  <files>src/agents/alerter/src/farmos/commit-db.js</files>
  <read_first>
    - src/agents/alerter/src/farmos/commit-db.js (entire file; locate initCommitSchema and existing ALTER TABLE pattern)
    - src/agents/alerter/src/index.js lines 84-110 (confirm init invocation order)
  </read_first>
  <behavior>
    - After initCommitSchema(pool) runs, `\d signal_draft` shows column `outcome_ack_sent_at timestamptz` (nullable, no default).
    - Re-running initCommitSchema is a no-op (IF NOT EXISTS).
    - No existing column or index is modified.
  </behavior>
  <action>
    In commit-db.js, inside initCommitSchema(pool), append one ALTER TABLE statement matching the existing pattern exactly:
    `ALTER TABLE signal_draft ADD COLUMN IF NOT EXISTS outcome_ack_sent_at timestamptz` (no default, nullable). Place it after the existing committed_at_attempt ALTER and before the CREATE INDEX block. Do NOT add an index — the column is queried only via the CAS UPDATE in Task 2, never scanned.
    Per D (Phase 45 CONTEXT): mark-then-send idempotency lives here.
  </action>
  <verify>
    <automated>cd src/agents/alerter && npm test -- test/farmos/commit-db.test.js 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - grep "outcome_ack_sent_at timestamptz" src/agents/alerter/src/farmos/commit-db.js returns ≥1 line inside initCommitSchema.
    - No new index referencing outcome_ack_sent_at in commit-db.js.
    - Existing commit-db.test.js suite still passes (no regression).
  </acceptance_criteria>
  <done>Column migration shipped; existing tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add tryMarkOutcomeAckSent helper + unit tests</name>
  <files>src/agents/alerter/src/farmos/commit-db.js, src/agents/alerter/test/farmos/commit-db.test.js</files>
  <read_first>
    - src/agents/alerter/src/farmos/commit-db.js (whole file — match export style and helper shape)
    - src/agents/alerter/test/farmos/commit-db.test.js (existing test patterns; how the suite obtains a pool / uses fake-pool.js)
    - src/agents/alerter/test/farmos/fake-pool.js (mock query interface)
  </read_first>
  <behavior>
    - First call: `tryMarkOutcomeAckSent(pool, draftId)` returns `{ ok: true, id: draftId, claimed_at: <timestamptz> }` when outcome_ack_sent_at was NULL.
    - Second call on same draft: returns `{ ok: false, reason: 'already_claimed' }` (UPDATE affected 0 rows).
    - Unknown draftId: returns `{ ok: false, reason: 'not_found' }`.
    - Bad pool / SQL error: throws (consistent with markCommitted's error style).
  </behavior>
  <action>
    Add exported helper `tryMarkOutcomeAckSent(pool, draftId)` to commit-db.js. Implementation: single-statement CAS — `UPDATE signal_draft SET outcome_ack_sent_at = now() WHERE id = $1 AND outcome_ack_sent_at IS NULL RETURNING id, outcome_ack_sent_at`. Inspect `rowCount` and `rows[0]`. Three return shapes per behavior block above; do NOT throw on "already claimed" — that is the steady-state non-error path.

    Add 3 unit tests to test/farmos/commit-db.test.js using the same fake-pool pattern already in that suite:
    1. `first call returns ok with claimed_at`
    2. `second call on already-claimed draft returns ok=false, reason=already_claimed`
    3. `unknown draftId returns ok=false, reason=not_found`

    Per CONTEXT.md decisions: "Mark-then-send semantics. On send failure, draft stays marked. Accept ≤1 dropped ack on watchdog crash mid-send rather than ever double-sending."
  </action>
  <verify>
    <automated>cd src/agents/alerter && npm test -- test/farmos/commit-db.test.js 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - grep "tryMarkOutcomeAckSent" src/agents/alerter/src/farmos/commit-db.js shows the export.
    - grep "tryMarkOutcomeAckSent" src/agents/alerter/test/farmos/commit-db.test.js shows ≥3 test cases.
    - npm test -- test/farmos/commit-db.test.js → all green, all 3 new tests pass.
    - SQL inspected by reading source: single UPDATE statement with `WHERE outcome_ack_sent_at IS NULL` and `RETURNING id`.
  </acceptance_criteria>
  <done>Helper exported, idempotency primitive proven by tests, no regressions.</done>
</task>

</tasks>

<verification>
- `grep -n "outcome_ack_sent_at" src/agents/alerter/src/farmos/commit-db.js` shows both the ALTER TABLE and the UPDATE in tryMarkOutcomeAckSent.
- `cd src/agents/alerter && npm test -- test/farmos/commit-db.test.js` exits 0.
- Boot smoke: `cd src/agents/alerter && node -e "require('./src/farmos/commit-db.js')"` exits 0 (no syntax error).
</verification>

<success_criteria>
- signal_draft.outcome_ack_sent_at column ships at boot.
- tryMarkOutcomeAckSent is the only path that writes the column.
- Unit tests cover the CAS contract (first-wins, second-loses, not-found).
- No farmer-visible behavior change yet (Plan 04 wires the dispatch).
</success_criteria>

<output>
Create `.planning/phases/45-north-star-commit-failed-ack-replay-outstanding-silent-failu/45-01-SUMMARY.md` when done.
</output>
