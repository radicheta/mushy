---
phase: 45-north-star-commit-failed-ack-replay-outstanding-silent-failu
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/agents/alerter/src/farmos/commit-outcome-preview.js
  - src/agents/alerter/test/farmos/commit-outcome-preview.test.js
  - src/agents/alerter/test/farmos/__snapshots__/commit-outcome-preview.test.js.snap
autonomous: true
requirements: [ACK-01]

must_haves:
  truths:
    - "A renderer module exists that takes (draftRow, outcome, reason?) and returns a single farmer-facing string with no em-dashes, rounded numbers, named address, and (where applicable) a farmOS link."
    - "All 5 log_types × 2 outcomes = 10 templates are exercised by snapshot tests."
    - "3 farm-level no-target success variants are exercised by snapshot tests."
    - "All 8 failure reason codes map to farmer-vocab phrases via a reasonMap export."
    - "Unknown reason codes fall back to the generic_validation_error phrasing — never bare error codes leak to farmer."
  artifacts:
    - path: "src/agents/alerter/src/farmos/commit-outcome-preview.js"
      provides: "renderOutcomeAck(draftRow, {outcome, reason, farmosLink}) + reasonMap export"
      min_lines: 100
    - path: "src/agents/alerter/test/farmos/commit-outcome-preview.test.js"
      provides: "13 snapshot tests + reasonMap fallback test + style-lock tests (no em-dashes, fmtNum applied)"
  key_links:
    - from: "src/agents/alerter/src/farmos/commit-outcome-preview.js"
      to: "src/agents/alerter/src/utils/farmer-text.js (or wherever sanitizeFarmerText/fmtNum live)"
      via: "import"
      pattern: "sanitizeFarmerText|fmtNum"
---

<objective>
Pure renderer module: 5 log_types (seeding, activity, input, observation, harvest) × 2 outcomes (success, failed) = 10 templates, plus 3 farm-level no-target success variants, plus an 8-code reason→farmer-vocab map. Output strings honor the style locks (no em-dashes, fmtNum, named address). Ships standalone with snapshot tests; no wiring yet.

Purpose: separates copy/render concerns from state-machine wiring. Plan 04 will import this and invoke from the dispatch switch.
Output: commit-outcome-preview.js with renderOutcomeAck() + reasonMap; 13 snapshot tests pinning every template.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/45-north-star-commit-failed-ack-replay-outstanding-silent-failu/45-CONTEXT.md
@.planning/notes/2026-05-17-northstar-commit-failed-reply.md
@.planning/notes/2026-05-17-northstar-ack-sketch.md

<interfaces>
Locate and import existing helpers (DO NOT reimplement):
- `sanitizeFarmerText(s)` — strips em-dashes, normalizes whitespace. Used in outbound-confirm.js / preview-builder-confirm.js. Grep: `grep -rn "sanitizeFarmerText" src/agents/alerter/src | head -5`
- `fmtNum(n)` — 1-decimal rounding, strips trailing `.0`. Grep: `grep -rn "fmtNum" src/agents/alerter/src | head -5`

Existing renderer pattern (study before writing the new module):
- `src/agents/alerter/src/confirm/preview-builder-confirm.js` — buildConfirmAck, buildDiscardAck, buildIdempotentAck, buildNudge, buildExpiredNote. Same shape: pure functions returning strings. Match this style.

draftRow shape (read from commit-db.js + Phase 38/39 docs):
- id (text/hex sha), sender_e164, sender_name (or farmer_display_name — verify), log_type ('seeding'|'activity'|'input'|'observation'|'harvest'), target (asset name/id, may be null for farm-level), farmos_response (jsonb, holds asset_ids/log_ids/link), commit_failed_reason.

The 8 failure reason codes (from CONTEXT.md decisions, English-only):
- observation_requires_target → "Couldn't match a block"
- no_target_asset_for_activity → "No asset to attach this activity to"
- asset_not_found → "Couldn't find that asset"
- duplicate_log → "Already logged"
- farmos_unreachable → "Farm server down"
- schema_invalid → "Data format issue"
- taxonomy_term_missing → "Missing a taxonomy term"
- generic_validation_error → "Data validation failed"
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Author commit-outcome-preview.js renderer + reasonMap</name>
  <files>src/agents/alerter/src/farmos/commit-outcome-preview.js</files>
  <read_first>
    - src/agents/alerter/src/confirm/preview-builder-confirm.js (mirror its module shape)
    - src/agents/alerter/src/farmos/commit-db.js (draftRow column names; especially log_type, target, farmos_response, sender_e164, commit_failed_reason)
    - .planning/phases/45-north-star-commit-failed-ack-replay-outstanding-silent-failu/45-CONTEXT.md (Ack templates section)
    - One existing test under test/farmos/ that uses snapshots, e.g. test/farmos/commit-router.test.js or commit-seeding.test.js, to identify the snapshot toolchain (jest snapshot vs node:test + assert.deepStrictEqual on stable JSON)
  </read_first>
  <behavior>
    - Exports: `renderOutcomeAck(draftRow, options)` where options = `{outcome: 'success'|'failed', reason?: string, farmosLink?: string}`.
    - Success with target: `"<Name>, saved <log_type label> for <target>. Open in farmOS: <link>"`.
    - Success no-target (farm-level, target == null): `"<Name>, saved as a general farm note since I couldn't match a specific block. Send EDIT to attach a block if you want."` — 3 minor variants for log_type ∈ {observation, activity, input} (per CONTEXT.md "3 farm-level no-target variants").
    - Failed: `"<Name>, couldn't save <log_type label>: <reason in farmer vocab>. Send EDIT to fix or NO to drop."`
    - Numbers in target / log content go through fmtNum.
    - Final output goes through sanitizeFarmerText.
    - Exports: `reasonMap` object (the 8-code → farmer-vocab map) AND a `reasonFor(code)` helper that falls back to the generic_validation_error phrasing on unknown codes.
    - Named address: prefer draftRow.sender_name; if absent, omit the leading "<Name>, " (no "friend,", no "operator,").
    - log_type labels: seeding → "seeding", activity → "activity", input → "input log", observation → "observation", harvest → "harvest". (Verify wording against existing confirm preview if a convention exists; otherwise use these.)
  </behavior>
  <action>
    Create `src/agents/alerter/src/farmos/commit-outcome-preview.js` as a pure-function module (CommonJS or ESM matching repo convention — check preview-builder-confirm.js). Exports: `renderOutcomeAck`, `reasonMap`, `reasonFor`. No DB access, no I/O, no logging. Sanitization applied last.

    Per CONTEXT.md D / decisions: English-only this phase (no i18n branch). Hard-code the 8 reason phrases inline in the reasonMap object.

    Style locks (CONTEXT.md): no em-dashes (sanitizeFarmerText enforces), fmtNum on any numeric, named address. Do NOT use `—`, `--` (in user-visible text — `--` in CLI flags inside body is forbidden too, but no flags should appear in ack copy), or em-dashes anywhere in the literal template strings.
  </action>
  <verify>
    <automated>cd src/agents/alerter && node -e "const m=require('./src/farmos/commit-outcome-preview'); console.log(typeof m.renderOutcomeAck, typeof m.reasonMap, typeof m.reasonFor)"</automated>
  </verify>
  <acceptance_criteria>
    - File exists at exact path.
    - Exports `renderOutcomeAck`, `reasonMap`, `reasonFor`.
    - `grep -v '^//' src/agents/alerter/src/farmos/commit-outcome-preview.js | grep -c '—\|–'` returns 0 (no em-dash or en-dash in source outside comments).
    - reasonMap contains all 8 keys: observation_requires_target, no_target_asset_for_activity, asset_not_found, duplicate_log, farmos_unreachable, schema_invalid, taxonomy_term_missing, generic_validation_error.
    - `grep -c "sanitizeFarmerText\|fmtNum" src/agents/alerter/src/farmos/commit-outcome-preview.js` ≥ 2.
  </acceptance_criteria>
  <done>Renderer module shipped, exports importable.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 13 snapshot tests + style-lock + reasonMap fallback tests</name>
  <files>src/agents/alerter/test/farmos/commit-outcome-preview.test.js</files>
  <read_first>
    - src/agents/alerter/src/farmos/commit-outcome-preview.js (the module under test, from Task 1)
    - test/farmos/commit-router.test.js or another test in the suite (to confirm snapshot tooling — likely jest)
    - src/agents/alerter/package.json (test script + jest config)
  </read_first>
  <behavior>
    - 10 snapshots: each of {seeding, activity, input, observation, harvest} × {success-with-target, failed-with-reason}. The failed snapshots use 5 different reason codes to exercise the map at the same time.
    - 3 snapshots: farm-level no-target success for {observation, activity, input}.
    - Style-lock test (NOT a snapshot): `expect(rendered).not.toMatch(/[—–]/)` for every rendered string across all 13 fixtures.
    - reasonMap fallback test: `reasonFor('totally_unknown_code')` returns the generic_validation_error phrasing (not the bare code).
    - Named-address test: when draftRow.sender_name is undefined, output does NOT contain "undefined" and does NOT begin with ", ".
  </behavior>
  <action>
    Write the test file using the suite's existing snapshot mechanism. Build 13 minimal draftRow fixtures inline (id, sender_name, sender_e164, log_type, target, farmos_response?, commit_failed_reason?). Snapshots pin the exact rendered string per CONTEXT.md decisions on copy.

    Add the style-lock loop test that iterates over every rendered output in the test file and asserts no em-dash / en-dash.

    Add the reasonMap fallback test and the no-name test as plain assertions (not snapshots) so they fail loudly if behavior drifts.
  </action>
  <verify>
    <automated>cd src/agents/alerter && npm test -- test/farmos/commit-outcome-preview.test.js 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - npm test -- test/farmos/commit-outcome-preview.test.js → all green.
    - Snapshot file exists at test/farmos/__snapshots__/commit-outcome-preview.test.js.snap (or in-file inline-snapshot equivalent) and contains ≥13 distinct entries.
    - Style-lock test present: grep "—\\|–\\|em-?dash" src/agents/alerter/test/farmos/commit-outcome-preview.test.js shows ≥1 hit.
    - Fallback test present: grep "reasonFor\\|unknown" test/farmos/commit-outcome-preview.test.js shows ≥1 hit on a fallback assertion.
    - All 5 log_types and 5+ reason codes appear in the snapshot file by string match.
  </acceptance_criteria>
  <done>13 templates pinned, style locks enforced, reasonMap fallback proven.</done>
</task>

</tasks>

<verification>
- `cd src/agents/alerter && npm test -- test/farmos/commit-outcome-preview.test.js` exits 0.
- Snapshot file committed.
- No em-dashes in any rendered template (style-lock loop).
</verification>

<success_criteria>
- Renderer is pure (no I/O), importable from commit-outcome-preview.js.
- All 13 templates locked under snapshot.
- 8 reason codes mapped + fallback proven.
- Style locks (no em-dash, fmtNum, named address) enforced by tests, not just by review.
</success_criteria>

<output>
Create `.planning/phases/45-north-star-commit-failed-ack-replay-outstanding-silent-failu/45-02-SUMMARY.md` when done.
</output>
