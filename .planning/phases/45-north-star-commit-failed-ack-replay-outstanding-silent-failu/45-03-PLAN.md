---
phase: 45-north-star-commit-failed-ack-replay-outstanding-silent-failu
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/agents/alerter/src/confirm/edit-handler.js
  - src/agents/alerter/test/confirm/edit-handler.test.js
autonomous: true
requirements: [ACK-01]

must_haves:
  truths:
    - "Drafts in status=commit_failed accept a farmer EDIT message and transition to status=awaiting_farmer with the EDIT correction passed to the Phase 38 extractor."
    - "EDIT on a commit_failed draft re-runs the same extraction → confirm path that an EDIT on awaiting_farmer would have followed, producing a new preview."
    - "EDIT on a draft in a state that is NOT in {awaiting_farmer, commit_failed} still rejects with the existing error path (no behavior change for those states)."
  artifacts:
    - path: "src/agents/alerter/src/confirm/edit-handler.js"
      provides: "Option X transition commit_failed → awaiting_farmer on farmer EDIT"
      contains: "commit_failed"
    - path: "src/agents/alerter/test/confirm/edit-handler.test.js"
      provides: "2 new tests: EDIT-from-commit_failed happy path + state-other-than-{awaiting_farmer,commit_failed}-still-rejects"
  key_links:
    - from: "src/agents/alerter/src/confirm/edit-handler.js"
      to: "Phase 38 extractor (re-extraction path)"
      via: "same call site EDIT-from-awaiting_farmer uses"
      pattern: "extractor|reExtract|runExtraction"
---

<objective>
State-machine: add the `commit_failed → EDIT → awaiting_farmer` transition (Option X, locked in CONTEXT.md). Without this, the failure-ack copy "Send EDIT to fix" is a lie — Plan 02's templates promise an affordance that the state machine refuses today.

Purpose: makes the failure-ack EDIT affordance truthful before Plan 04 ships the ack to farmers.
Output: edit-handler.js accepts EDIT on commit_failed drafts, mirrors the existing awaiting_farmer EDIT path.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/45-north-star-commit-failed-ack-replay-outstanding-silent-failu/45-CONTEXT.md
@.planning/notes/2026-05-17-northstar-commit-failed-reply.md
@src/agents/alerter/src/confirm/edit-handler.js

<interfaces>
edit-handler.js today (Phase 39 D-03):
- Accepts a Signal EDIT message bound to a draft.
- Today's accepted source state: `awaiting_farmer`.
- On accept: re-runs Phase 38 extractor with farmerCorrection text, updates draft, transitions back into awaiting_farmer with a new preview.
- On reject (wrong state): logs + emits a side-effect (e.g. send_edit_cap_msg or similar) — read the file to learn the exact rejection contract.

Signal_draft.status values currently in play (from commit-db.js comments):
- draft, awaiting_farmer, confirmed, committing, committed, commit_failed, discarded, expired.

Phase 38 extractor entry point — read edit-handler.js to identify how it's invoked today.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend edit-handler accepted states to include commit_failed</name>
  <files>src/agents/alerter/src/confirm/edit-handler.js</files>
  <read_first>
    - src/agents/alerter/src/confirm/edit-handler.js (entire file)
    - src/agents/alerter/src/farmos/commit-db.js (status value list + any markStatus helper)
    - src/agents/alerter/src/confirm/outbound-confirm.js (side-effect kinds emitted from edit flow)
  </read_first>
  <behavior>
    - EDIT message on a draft with status=commit_failed: handler accepts, re-runs extractor with farmerCorrection, updates draft.status to awaiting_farmer, clears outcome_ack_sent_at? — NO, leave outcome_ack_sent_at alone (the original commit's ack stands; the new attempt's ack will use the new ack-slot semantics from Plan 04). Verify the existing awaiting_farmer→awaiting_farmer EDIT path does NOT touch outcome_ack_sent_at either.
    - EDIT message on a draft with status=awaiting_farmer: unchanged behavior.
    - EDIT message on any other state (draft, confirmed, committing, committed, discarded, expired): unchanged rejection behavior.
  </behavior>
  <action>
    In edit-handler.js, locate the state guard (the if/switch that today permits only `awaiting_farmer`). Per CONTEXT.md decision Option X (locked), add `commit_failed` to the allowed source-state set. Route the rest of the handler through the existing awaiting_farmer code path — do NOT duplicate logic; do NOT clear outcome_ack_sent_at.

    No new imports needed if the extractor call lives in this file already. If a status-update helper exists (e.g. `markAwaitingFarmer(pool, draftId)`), use it; otherwise mirror the exact UPDATE the awaiting_farmer path uses.

    Per CONTEXT.md decisions: "Add transition commit_failed → EDIT → awaiting_farmer in confirm/edit-handler.js (~30 LOC + 2 tests). Re-runs Phase 38 extractor on farmerCorrection, mirroring the existing EDIT path from awaiting_farmer."
  </action>
  <verify>
    <automated>cd src/agents/alerter && npm test -- test/confirm/edit-handler.test.js 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "commit_failed" src/agents/alerter/src/confirm/edit-handler.js` ≥ 1 (state guard now mentions it).
    - Existing edit-handler.test.js suite (pre-change tests) continues to pass.
    - No edit to src/agents/alerter/src/farmos/commit-db.js (this plan does not own the schema; that is Plan 01).
  </acceptance_criteria>
  <done>State guard widened; existing tests still green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add 2 tests for Option X transition + non-target-state rejection</name>
  <files>src/agents/alerter/test/confirm/edit-handler.test.js</files>
  <read_first>
    - src/agents/alerter/test/confirm/edit-handler.test.js (existing test shape, mock setup)
    - src/agents/alerter/test/farmos/fake-pool.js (DB mock)
  </read_first>
  <behavior>
    - Test A (happy path): fixture draft with status=commit_failed, commit_failed_reason='observation_requires_target'. EDIT arrives with farmerCorrection text. Assert: extractor was invoked once with the correction; draft.status updated to awaiting_farmer; a preview side-effect was emitted (or whatever the existing awaiting_farmer→awaiting_farmer test asserts — mirror it exactly).
    - Test B (still-rejected states): for each of `confirmed`, `committed`, `discarded` — EDIT is rejected with the existing rejection code path. No transition occurs.
  </behavior>
  <action>
    Add the 2 tests to test/confirm/edit-handler.test.js, reusing the existing extractor mock + fake-pool mock pattern. Use the same describe/it block style as the existing awaiting_farmer→awaiting_farmer test. Do NOT introduce new mocking frameworks.

    Test B can be a single it() with a forEach over the 3 rejected states (matches the lean style of existing tests).
  </action>
  <verify>
    <automated>cd src/agents/alerter && npm test -- test/confirm/edit-handler.test.js 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - npm test -- test/confirm/edit-handler.test.js → all green (existing + 2 new).
    - grep -c "commit_failed" src/agents/alerter/test/confirm/edit-handler.test.js ≥ 2 (one in happy path, one in non-target-state rejection list).
    - The 3 rejected states `confirmed`, `committed`, `discarded` all referenced in Test B.
  </acceptance_criteria>
  <done>Option X transition test-locked; rejection of other states preserved.</done>
</task>

</tasks>

<verification>
- `cd src/agents/alerter && npm test -- test/confirm/edit-handler.test.js` exits 0.
- No accidental touch of commit-db.js or commit-watchdog.js from this plan.
</verification>

<success_criteria>
- commit_failed drafts accept EDIT and re-enter the extraction loop.
- All other previously-rejected states still reject.
- The "Send EDIT to fix" affordance in Plan 02's failure-ack copy is now truthful.
</success_criteria>

<output>
Create `.planning/phases/45-north-star-commit-failed-ack-replay-outstanding-silent-failu/45-03-SUMMARY.md` when done.
</output>
