---
phase: 52-session-entity-via-asset-group-activity-log-membership
plan: 04
type: execute
wave: 3
depends_on: ["52-03"]
files_modified:
  - src/agents/alerter/test/farmos/commit-seeding-session.test.js
  - src/agents/alerter/test/farmos/integration.test.js
autonomous: true
requirements: [SESSION-07]
must_haves:
  truths:
    - "Hermetic happy-path test on the May-22 fixture asserts 17 asset POSTs (1 group + 5 source + 11 children) and 12 logs (1 activity-with-flag + 11 seeding)"
    - "Test verifies the single activity-log POST has attributes.is_group_assignment=true and relationships.asset.data lists all 11 child UUIDs"
    - "Test verifies children's relationships.parent.data === [{type:'asset--fungi', id:sourceBlockId}] -- NO sessionGroupId edge"
    - "Partial-failure test: child #4 POST returns 500 -> reverse-order rollback covers the membership log (none created yet at this stage), then the 3 already-created children + their seeding logs (which become orphans), then the just-created source blocks, then the session group asset"
    - "Membership-log-failure test: all children succeed, activity log POST returns 500 -> rollback hits ALL of: created children, source blocks, session group asset"
    - "Same-day collision test: pre-seed mock-client GET response for 'inoc 2026-05-22' so it returns an existing group whose notes trailer does NOT match the current draft id -> handler advances to 'inoc 2026-05-22 #2' and creates that"
    - "Double-YES idempotent test (preserved from Phase 48): a re-run of the same draft against a populated mock still produces a clean ok:true with no duplicate creates (relies on upsertGroupAsset reuse + Phase 51 upsertFungiAsset reuse)"
  artifacts:
    - path: "src/agents/alerter/test/farmos/commit-seeding-session.test.js"
      provides: "updated count assertions, new membership-log assertions, new collision test, expanded rollback test"
    - path: "src/agents/alerter/test/farmos/integration.test.js"
      provides: "cross-handler integration check still green at new counts"
  key_links:
    - from: "commit-seeding-session.test.js Test A"
      to: "17 + 12 counts"
      via: "exact POST count assertions"
      pattern: "asset POSTs"
    - from: "commit-seeding-session.test.js collision test"
      to: "_resolveSessionName collision branch"
      via: "pre-seeded findGroupAssetByName hit with foreign draft trailer"
      pattern: "#2"
---

<objective>
Update the hermetic test suite to reflect the new entity counts and the new membership-log assertions, then add the collision + membership-log-failure tests that exercise the Plan 03 control flow.

Purpose: Lock the new shape with deterministic in-memory coverage before the live-fire ship-gate runs (Plan 05).

Output: Two modified test files. Phase 48's 7-test suite expands to ~9 scenarios.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/52-session-entity-via-asset-group-activity-log-membership/52-CONTEXT.md
@src/agents/alerter/test/farmos/commit-seeding-session.test.js
@src/agents/alerter/test/farmos/integration.test.js
@src/agents/alerter/test/farmos/mock-client.js
@src/agents/alerter/test/farmos/fixtures
@src/agents/alerter/src/farmos/commits/commit-seeding-session.js

The existing test file's Test A header reads `'Test A (happy path May 22): 16 asset POSTs, 11 seeding log POSTs, child parent[] = [source]'` (line 95) — that count + claim is now stale. Update it to `'Test A (happy path May 22): 17 asset POSTs (1 group + 5 source + 11 children), 12 logs (1 activity-with-flag + 11 seeding), child parent[] = [source] ONLY'`.

Phase 48's STATE.md notes the partial-fail test asserts 8 DELETEs in the rollback path. Under Plan 03's expanded rollback, that count grows by at most 2 (1 group asset + 1 membership log — though the latter is only created at the END so partial-fail mid-loop will have 0 activity-log DELETEs and just 1 extra group DELETE). Recompute expected counts per the failure point. The membership-log-failure test is a NEW scenario where the failure happens AFTER all children — there the expected DELETE count is 1 (group) + 11 (children) + N (source blocks that were `outcome==='created'` this run; in the happy-path fixture the source blocks are typically reused so this may be 0) = 12 minimum + 0 source DELETEs (because the source-block stubs in the fixture get `outcome==='reused'` via upsertFungiAsset against the prepopulated mock). The test should compute the expected count from the createdAssetIds list rather than hard-coding.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update commit-seeding-session.test.js — happy-path counts + membership-log assertions + collision scenario + membership-log-failure scenario</name>
  <files>src/agents/alerter/test/farmos/commit-seeding-session.test.js</files>
  <action>
    Modifications (in test-file order):
    
    1. Update Test A header text to match the new counts (per Context section above). Update the `expect(mockClient.posts['/api/asset/fungi'].length)` (or equivalent) assertion to expect 16 fungi POSTs (5 source + 11 children — unchanged) AND add a new `expect(mockClient.posts['/api/asset/group'].length).toBe(1)` for the session group. Update the seeding-log count assertion to 11 (unchanged) AND add a new `expect(mockClient.posts['/api/log/activity'].length).toBe(1)` for the membership log.
    
    2. Add new Test A assertions:
       - The single `/api/log/activity` POST body has `attributes.is_group_assignment === true`.
       - The activity POST body's `relationships.asset.data` has length 11.
       - The activity POST body's `relationships.group.data` has length 1 and the id matches the session group asset id captured from the `/api/asset/group` POST.
       - For each of the 11 child fungi POSTs, the body's `relationships.parent.data` is `[{type:'asset--fungi', id:&lt;sourceBlockId&gt;}]` — length exactly 1, NO sessionGroupId.
       - The result `log_ids[0]` is the membership log id; `log_ids.slice(1)` are the 11 seeding log ids in order.
       - The result `asset_ids[0]` is the session group id (since it was created this run).
    
    3. Update the existing partial-failure test (Test C-ish — where mock-client returns 500 on the Nth child POST): recompute the expected rollback DELETE count. Failure mid-loop means the membership log was NEVER posted, so the rollback covers: (a) any source blocks created this run, (b) any children created so far this run, (c) the session group asset. Use the createdAssetIds-from-result hookup rather than hard-coding numbers — the test should pass `expect(deletes).toContain(sessionGroupId)` and `expect(deletes.length).toBeGreaterThanOrEqual(prior_children_created + 1)`.
    
    4. Add new test: `Test E (membership log POST fails): all 11 children + 11 seeding logs succeed, mock returns 500 on /api/log/activity -> handler returns ok:false reason='membership_log_create_failed'; rollback DELETEs include the session group asset id AND all 11 child fungi ids; result.asset_ids and result.log_ids are empty arrays.`
    
    5. Add new test: `Test F (same-day collision): pre-seed mockClient's GET /api/asset/group?filter[name][value]=inoc%202026-05-22 to return a hit with notes trailer 'mushy:draft:OTHER_DRAFT' (not the current draft id) -> handler advances and POSTs 'inoc 2026-05-22 #2'. Verify the POST body's attributes.name === 'inoc 2026-05-22 #2' and the session group id in asset_ids matches the response.`
    
    6. Preserve the double-YES idempotent test (Test D-ish) — under the new shape, a re-run should: reuse session group (outcome='reused', sessionGroupId NOT in asset_ids), reuse source blocks (existing Phase 51 behavior), reuse child blocks via upsertFungiAsset noop, AND still POST a new membership log (per CONTEXT decision "creation-only for v1.10.1, duplicates acceptable"). Update its assertions to reflect: 0 group POSTs, 0 fungi POSTs, 1 activity log POST, 11 seeding log noop/patched outcomes.
    
    Mock-client extension: if mockClient.js doesn't yet route `/api/asset/group` or `/api/log/activity`, add the routing. Look at the existing routing for `/api/asset/fungi` and `/api/log/seeding` and mirror it.
  </action>
  <verify>
    <automated>cd src/agents/alerter &amp;&amp; npx jest test/farmos/commit-seeding-session.test.js</automated>
  </verify>
  <done>All ~9 scenarios pass: happy path (17+12 with membership-log assertions), partial-fail mid-loop with expanded rollback, membership-log-failure with full rollback, same-day collision -> #2, double-YES idempotent under new shape.</done>
</task>

<task type="auto">
  <name>Task 2: Update integration.test.js for new entity counts; run full alerter test suite</name>
  <files>src/agents/alerter/test/farmos/integration.test.js</files>
  <action>
    Update any cross-handler assertion in integration.test.js that hard-codes the May-22 commit's POST counts (search for `16` or `11 seeding` or similar). Replace with the new 17/12 counts. If integration.test.js asserts on result.asset_ids.length or result.log_ids.length for the seeding_session shape, increment by 1 each. If no such assertion exists, no change required.

    After edits, run the full alerter jest suite and report pass count.
  </action>
  <verify>
    <automated>cd src/agents/alerter &amp;&amp; npx jest 2>&amp;1 | tail -5</automated>
  </verify>
  <done>Full alerter jest suite is GREEN at the expanded test count. Test totals reported in summary.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

(Test-only plan; no new trust boundaries introduced.)

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-52-04-01 | Tampering | mock-client routing drift | mitigate | Mirror the existing /api/asset/fungi + /api/log/seeding routing patterns verbatim for the new endpoints |
| T-52-04-02 | Repudiation | test asserts wrong rollback order | mitigate | Test E asserts `expect(deletes.indexOf(sessionGroupId)).toBeGreaterThan(deletes.indexOf(childIds[0]))` to lock the reverse-order semantics |
</threat_model>

<verification>
- `cd src/agents/alerter && npx jest test/farmos/commit-seeding-session.test.js` is GREEN
- `cd src/agents/alerter && npx jest` (full suite) is GREEN
- New scenarios E (membership-log-failure) and F (collision -> #2) exist as named `it(...)` blocks
</verification>

<success_criteria>
- Phase 48's existing 7 scenarios remain green (with updated counts/assertions)
- 2 new scenarios pass (membership-log-failure, collision -> #2)
- Full alerter test suite is GREEN
- No assertion uses `>=` where `===` is correct — count assertions are exact
</success_criteria>

<output>
Create `.planning/phases/52-session-entity-via-asset-group-activity-log-membership/52-04-SUMMARY.md` when done.
</output>
