---
phase: 52-session-entity-via-asset-group-activity-log-membership
plan: 05
type: execute
wave: 4
depends_on: ["52-04"]
files_modified:
  - src/agents/alerter/scripts/live-fire-52.js
  - .planning/phases/52-session-entity-via-asset-group-activity-log-membership/52-LIVE-FIRE.md
autonomous: false
requirements: [SESSION-08]
user_setup:
  - service: farmOS-dev
    why: "Live-fire ship-gate writes against dev farmOS at http://10.68.155.50:18080"
    env_vars:
      - name: FARMOS_URL
        source: "http://10.68.155.50:18080 (dev — NEVER prod :8082 in this phase)"
      - name: FARMOS_USERNAME
        source: "mushy-bot account on dev farmOS"
      - name: FARMOS_PASSWORD
        source: "Operator-held credential for mushy-bot"
must_haves:
  truths:
    - "scripts/live-fire-52.js runs commitSeedingSession against the May-22 fixture on dev farmOS at :18080 and writes the result JSON to /tmp/52-live-fire-result.json"
    - "Result includes 1 session group asset id + N child asset ids + 1 membership activity log id + N seeding log ids"
    - "Membership walk: GET /api/log/activity?filter[is_group_assignment]=1&filter[asset.id]=<any_child_id> returns the session group"
    - "Lineage walk from any child still returns its strain parent (the source block) ONLY -- session group does NOT appear in the child's parent[]"
  artifacts:
    - path: "src/agents/alerter/scripts/live-fire-52.js"
      provides: "ship-gate harness for dev farmOS; clone of live-fire-48.js with prod-guard + verification probes"
    - path: ".planning/phases/52-session-entity-via-asset-group-activity-log-membership/52-LIVE-FIRE.md"
      provides: "operator runbook with env, command, expected output, membership-walk + lineage-walk verification commands, receipt section"
  key_links:
    - from: "live-fire-52.js"
      to: "commitSeedingSession on dev farmOS"
      via: "real session-cookie auth + real POST/DELETE"
      pattern: "FARMOS_URL"
    - from: "membership walk verification"
      to: "GET /api/log/activity?filter[is_group_assignment]=1&filter[asset.id]="
      via: "queryable membership encoded on activity log"
      pattern: "is_group_assignment"
---

<objective>
Stand up the live-fire ship-gate harness and the operator runbook that proves the new shape lands cleanly on dev farmOS (`:18080`) — the same instance the Phase 48 reversal evidence came from.

Purpose: Hermetic tests cover the wiring shape; only a real farmOS write can confirm that `farm_group` is actually enabled, that `asset--group` POSTs are accepted, that `log--activity` accepts `is_group_assignment:true`, and that the membership-walk query returns the right group from any child.

Output: One new script + one new runbook doc. Execution of the live-fire is OPERATOR-DRIVEN (`autonomous: false`) because credentials are operator-held and writes hit a real system.

Per CONTEXT.md: NO prod live-fire this phase. The script defends against prod misfire by refusing to run if `FARMOS_URL` ends in `:8082`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/52-session-entity-via-asset-group-activity-log-membership/52-CONTEXT.md
@.planning/notes/2026-05-24-session-as-asset-group-design.md
@src/agents/alerter/scripts/live-fire-48.js
@src/agents/alerter/src/farmos/commits/commit-seeding-session.js
@src/agents/alerter/src/farmos/client.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create live-fire-52.js harness</name>
  <files>src/agents/alerter/scripts/live-fire-52.js</files>
  <action>
    Clone `src/agents/alerter/scripts/live-fire-48.js` to `src/agents/alerter/scripts/live-fire-52.js`. Modifications:

    1. Update the file-header comment to point at 52-LIVE-FIRE.md and explain the new expected counts: 1 group + 5 source + 11 children = 17 asset POSTs (POSTs OR PATCHes/noops per Phase 51), 1 activity-with-flag + 11 seeding = 12 logs.

    2. Add a prod-guard at the top of the IIFE, immediately after reading env: if farmosUrl ends with ':8082' or includes 'prod' (case-insensitive), console.error a clear refusal message naming the URL and process.exit(3). This enforces CONTEXT.md "NO prod live-fire in this phase".

    3. Change the default `OUTPUT_PATH` to `/tmp/52-live-fire-result.json`.

    4. Change the draft id prefix to `live-fire-52-` so the audit log distinguishes runs.

    5. After commitSeedingSession returns ok:true, add a verification block that:
       - Picks the first childId from `result.asset_ids` that is NOT the sessionGroupId (sessionGroupId is at index 0 per Plan 03 contract; the harness can also infer it from the response by picking the first id whose GET returns type 'asset--group').
       - Issues `GET /api/log/activity?filter[is_group_assignment]=1&filter[asset.id]=<childId>` via the client.
       - Asserts the response contains at least one log whose `relationships.group.data[0].id === sessionGroupId`.
       - Issues `GET /api/asset/fungi/<childId>` and asserts `relationships.parent.data.length === 1` AND the single parent has `type === 'asset--fungi'` (the source block; NOT the session group).
       - Adds those two probe results to the output JSON as `membership_walk_ok: true|false` and `lineage_walk_ok: true|false`.

    6. process.exit(2) if either probe assertion fails so CI / the operator sees the ship-gate failure immediately.

    Keep all the existing behavior intact: env-var reading, draft loading from the May-22 fixture, audit logger pass-through, elapsed_ms in output, JSON written to OUTPUT_PATH and printed to stdout.
  </action>
  <verify>
    <automated>node -c /mnt/slime-kingdom/opt/mushy/src/agents/alerter/scripts/live-fire-52.js &amp;&amp; grep -q "REFUSING" /mnt/slime-kingdom/opt/mushy/src/agents/alerter/scripts/live-fire-52.js &amp;&amp; grep -q "is_group_assignment" /mnt/slime-kingdom/opt/mushy/src/agents/alerter/scripts/live-fire-52.js &amp;&amp; grep -q "live-fire-52-" /mnt/slime-kingdom/opt/mushy/src/agents/alerter/scripts/live-fire-52.js</automated>
  </verify>
  <done>Script parses with `node -c`, contains the REFUSING prod-guard, contains the is_group_assignment membership-walk probe, uses the live-fire-52- draft id prefix.</done>
</task>

<task type="auto">
  <name>Task 2: Write 52-LIVE-FIRE.md operator runbook</name>
  <files>.planning/phases/52-session-entity-via-asset-group-activity-log-membership/52-LIVE-FIRE.md</files>
  <action>
    Create the runbook with these sections (Markdown headings):

    1. **Purpose** — Ship-gate for Phase 52. Confirms farm_group module is live on dev farmOS, asset--group + activity-with-flag round-trip cleanly, and membership-walk resolves a child to its session group.

    2. **Pre-flight checklist:**
       - dev farmOS reachable: `curl -s http://10.68.155.50:18080/api/ | jq '."links"."asset--group"'` returns non-null (proves farm_group is enabled — see farmos repo commit `1857037`).
       - `mushy-bot` credentials exported as `FARMOS_USERNAME` + `FARMOS_PASSWORD`.
       - Full alerter jest suite green (Plan 04 attestation).

    3. **Run command** (literal block):
       ```
       cd src/agents/alerter
       FARMOS_URL=http://10.68.155.50:18080 \
       FARMOS_USERNAME=mushy-bot \
       FARMOS_PASSWORD=<...> \
       node scripts/live-fire-52.js
       ```

    4. **Expected counts:**
       - 1 asset--group POST (session)
       - 5 source-block touches (POST on first run, PATCH/noop on subsequent runs per Phase 51)
       - 11 child asset--fungi touches (POST first run, noop on subsequent runs)
       - 1 log--activity POST (membership, with is_group_assignment=true)
       - 11 log--seeding touches (POST first run, noop on subsequent runs)
       - Total elapsed: ≤10 seconds on a healthy network

    5. **Verification probes** (the harness runs these automatically; document the manual curl equivalents for forensics):
       - Membership walk: `curl -s "http://10.68.155.50:18080/api/log/activity?filter[is_group_assignment]=1&filter[asset.id]=<childId>" -b cookies.txt | jq '.data[0].relationships.group.data'` returns `[{type:'asset--group', id:<sessionGroupId>}]`.
       - Lineage walk: `curl -s "http://10.68.155.50:18080/api/asset/fungi/<childId>" -b cookies.txt | jq '.data.relationships.parent.data'` returns ONE entry with `type:'asset--fungi'` (the source block). NO session group id in this list.

    6. **Receipt section (operator fills after run):** placeholder for the JSON output of /tmp/52-live-fire-result.json + a 2-line PASS/FAIL verdict + timestamp.

    7. **Backfill note (deferred per CONTEXT.md):** the 11 dev children from the 2026-05-24 48-LIVE-FIRE are NOT in scope for this run — they keep their parent-only lineage and miss the new session group. A separate cleanup phase covers them.

    8. **Prod gating reminder:** prod is NOT in scope for this phase. The harness refuses to run against `:8082`. Prod cutover is a separate decision gated on `FARMOS_INTEGRATION` per the 2026-05-24 UAT findings.

    ASCII-only, no em-dashes (per `[[feedback_no_em_dashes_in_artifacts]]`).
  </action>
  <verify>
    <automated>test -f /mnt/slime-kingdom/opt/mushy/.planning/phases/52-session-entity-via-asset-group-activity-log-membership/52-LIVE-FIRE.md &amp;&amp; grep -q "is_group_assignment=1" /mnt/slime-kingdom/opt/mushy/.planning/phases/52-session-entity-via-asset-group-activity-log-membership/52-LIVE-FIRE.md &amp;&amp; grep -q ":8082" /mnt/slime-kingdom/opt/mushy/.planning/phases/52-session-entity-via-asset-group-activity-log-membership/52-LIVE-FIRE.md</automated>
  </verify>
  <done>Runbook file exists with the membership-walk curl probe documented and the :8082 prod-refusal reminder present. All eight numbered sections rendered as Markdown headings.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Operator runs the live-fire and pastes the receipt</name>
  <what-built>scripts/live-fire-52.js + 52-LIVE-FIRE.md runbook</what-built>
  <how-to-verify>
    Follow 52-LIVE-FIRE.md verbatim:
    1. Pre-flight curl confirms farm_group is enabled on dev (`asset--group` appears in /api/).
    2. Export FARMOS_USERNAME + FARMOS_PASSWORD for mushy-bot on dev.
    3. Run `FARMOS_URL=http://10.68.155.50:18080 node src/agents/alerter/scripts/live-fire-52.js`.
    4. Verify the printed JSON shows: asset_ids includes 1 session group + per-block ids; log_ids[0] is the membership activity log; `membership_walk_ok: true`; `lineage_walk_ok: true`.
    5. Spot-check in farmOS UI: navigate to the session group at /asset/<groupId>; the "Group members" view shows all 11 children.
    6. Paste the JSON receipt into the Receipt section of 52-LIVE-FIRE.md and commit.
    If the harness exits 2 (probe failure) or any expected count is off, STOP and file a finding; do NOT mark Phase 52 shipped.
  </how-to-verify>
  <resume-signal>Type "approved" with the receipt JSON pasted, or describe the failure mode for replanning</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| harness → dev farmOS | real session-cookie auth, real writes to a shared dev instance |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-52-05-01 | Tampering | accidental prod write | mitigate | Hard prod-guard in the script: exits 3 on :8082 or 'prod' in URL; runbook reinforces dev-only scope |
| T-52-05-02 | Information Disclosure | farmOS password in process env | accept | Operator-managed credential, never written to disk by the harness; matches Phase 48 live-fire posture |
| T-52-05-03 | Repudiation | second run creates duplicate session group | mitigate | `_resolveSessionName` reuses by draft-id trailer match; subsequent runs with the same draft id hit the existing group; runs with a fresh draft id allocate `#2` |
</threat_model>

<verification>
- `node -c src/agents/alerter/scripts/live-fire-52.js` parses cleanly
- 52-LIVE-FIRE.md exists with the membership-walk + lineage-walk curl probes documented
- Operator-attested run (Task 3 checkpoint) produces a JSON receipt with `ok: true`, `membership_walk_ok: true`, `lineage_walk_ok: true`
</verification>

<success_criteria>
- Harness script exists, has prod-guard, runs the membership + lineage probes after the commit
- Runbook is complete and ASCII-only (no em-dashes)
- Operator runs the live-fire on dev and the receipt is pasted into 52-LIVE-FIRE.md
- Phase 52 is shipped when the receipt shows PASS on both probes
</success_criteria>

<output>
Create `.planning/phases/52-session-entity-via-asset-group-activity-log-membership/52-05-SUMMARY.md` when done, citing the receipt section of 52-LIVE-FIRE.md as the ship-gate attestation.
</output>
