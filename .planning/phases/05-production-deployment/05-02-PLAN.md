---
phase: 05-production-deployment
plan: "02"
type: execute
wave: 2
depends_on: [05-01]
files_modified: []
autonomous: false
requirements: [DEPL-01]
user_setup: []
must_haves:
  truths:
    - "Config with target_humidity 0.80 is deployed and running on FC-1 Pi"
    - "fc-core service has been running for 24 hours without unrecoverable crash"
    - "Grower can observe humidity and humidifier state via OpenMCT dashboard"
    - "System is declared better than the timer solution"
  artifacts: []
  key_links:
    - "fc_config.yaml on Pi -> fc_controller node via ROS2 parameter load (target_humidity 0.80)"
---

<objective>
Deploy the updated config to FC-1, run the 24-hour soak test (D-04), and verify the system is production-ready for grower handoff. This plan completes DEPL-01 and closes the MVP.

Purpose: Validate that the closed-loop humidity control system runs stably in production at the farm, replacing the timer solution.
Output: Deployed production system, soak test results, MVP declaration.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-production-deployment/05-CONTEXT.md
@.planning/phases/05-production-deployment/05-RESEARCH.md
@.planning/phases/05-production-deployment/05-01-SUMMARY.md

<interfaces>
<!-- Deploy and verification commands from RESEARCH.md -->

Deploy to Pi:
```bash
./scripts/pi-deploy/deploy.sh
```

Verify config deployed:
```bash
ssh fc1 'grep target_humidity ~/mushroom_farm_ws/src/chambers/fc-core/config/fc_config.yaml'
# Expected: target_humidity: 0.80
```

Verify service active:
```bash
ssh fc1 'sudo systemctl is-active fc-core'
# Expected: active
```

Soak test final check:
```bash
ssh fc1 'sudo systemctl show fc-core --property=NRestarts,ActiveState,SubState,ExecMainStartTimestamp'
ssh fc1 'sudo journalctl -u fc-core --since "24 hours ago" --no-pager | grep -E "Humidifier|restart|error|warn" | tail -50'
```

Dashboard:
```bash
docker compose up -d openmct bridge  # on elder-plops
# Then browse to http://localhost:8080
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Deploy updated config to FC-1 and verify</name>
  <read_first>
    - src/chambers/fc-core/config/fc_config.yaml
    - scripts/pi-deploy/deploy.sh
    - .planning/phases/05-production-deployment/05-01-SUMMARY.md
  </read_first>
  <files></files>
  <action>
Run the deploy pipeline to push the updated fc_config.yaml (target_humidity: 0.80) to FC-1:

1. Run: `./scripts/pi-deploy/deploy.sh`
2. Verify service is active: `ssh fc1 'sudo systemctl is-active fc-core'` — must return `active`
3. Verify config deployed: `ssh fc1 'grep target_humidity ~/mushroom_farm_ws/src/chambers/fc-core/config/fc_config.yaml'` — must show `0.80`
4. Verify service is enabled for boot: `ssh fc1 'sudo systemctl is-enabled fc-core'` — must return `enabled`
5. Spot check logs: `ssh fc1 'sudo journalctl -u fc-core -n 10 --no-pager'` — should show recent sensor readings
6. Check restart count baseline: `ssh fc1 'sudo systemctl show fc-core --property=NRestarts'` — record the value (should be 0 after fresh restart)
7. Quick spot check that humidifier is cycling correctly: `ssh fc1 'sudo journalctl -u fc-core --since "2 min ago" --no-pager | grep -E "Humidifier"'`

If deploy.sh fails (SSH unreachable): the Pi may not be powered or network-connected. This is a blocking issue — report to user.
  </action>
  <verify>
    <automated>ssh fc1 'grep target_humidity ~/mushroom_farm_ws/src/chambers/fc-core/config/fc_config.yaml' && ssh fc1 'sudo systemctl is-active fc-core'</automated>
  </verify>
  <acceptance_criteria>
    - `ssh fc1 'grep target_humidity ...'` returns `target_humidity: 0.80`
    - `ssh fc1 'sudo systemctl is-active fc-core'` returns `active`
    - `ssh fc1 'sudo systemctl is-enabled fc-core'` returns `enabled`
    - journalctl shows recent sensor readings (within last 30 seconds)
  </acceptance_criteria>
  <done>Production config deployed to FC-1, service active and enabled, target_humidity confirmed 0.80 on Pi</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: 24-hour soak test and production declaration</name>
  <read_first>
    - .planning/phases/05-production-deployment/05-CONTEXT.md
    - docs/OPERATIONS.md
  </read_first>
  <files></files>
  <action>
This is a human-gated soak test. Claude cannot execute this — it requires 24 hours of elapsed time and physical verification at the farm.

PREREQUISITE (D-06): Pi must be physically located at the farm, connected to the humidifier power strip via the SSR-10A relay, before the soak test clock starts. If Pi is not yet at the farm, this checkpoint blocks until relocation is complete.

Before presenting the checkpoint to the user, verify the deploy from Task 1 is still active:
- Run: `ssh fc1 'sudo systemctl is-active fc-core'`
- Run: `ssh fc1 'sudo systemctl show fc-core --property=NRestarts'`

Then present the soak test instructions below.
  </action>
  <what-built>
FC-1 humidity control system deployed with production config (target_humidity: 0.80, min_dwell_time: 300s, humidity_tolerance: 0.05). The system is running on the Pi with systemd auto-restart supervision.
  </what-built>
  <how-to-verify>
**Pre-soak checklist (before starting 24h clock):**
1. Confirm Pi is physically at the farm and connected to humidifier power strip
2. Confirm SSR-10A relay is wired to GPIO27 and the power strip
3. Run: `ssh fc1 'sudo systemctl is-active fc-core'` — must return `active`
4. Run: `ssh fc1 'sudo journalctl -u fc-core --since "5 min ago" --no-pager | grep Humidifier'` — should show ON/OFF cycling
5. Open `http://localhost:8080` (OpenMCT) — verify live humidity chart updating
6. Record soak start time

**During soak (optional check-ins at ~8h and ~16h):**
- `ssh fc1 'sudo systemctl show fc-core --property=NRestarts,ActiveState'`
- Quick look at OpenMCT chart for gaps

**After 24 hours (D-04):**
1. `ssh fc1 'sudo systemctl show fc-core --property=NRestarts,ActiveState,SubState,ExecMainStartTimestamp'`
   - ActiveState must be `active`
   - Record NRestarts value (any number is OK per D-05 if service recovered)
2. `ssh fc1 'sudo journalctl -u fc-core --since "24 hours ago" --no-pager | grep -cE "error|Error|ERROR"'`
   - Record error count
3. `ssh fc1 'sudo journalctl -u fc-core --since "24 hours ago" --no-pager | grep -E "Humidifier: (ON|OFF)" | head -20'`
   - Confirm humidifier was cycling (not stuck in one state for entire 24h)
4. Check OpenMCT chart — humidity should show oscillation within the 75-85% band
5. Visual confirmation: humidifier is physically activating (mist visible, sound audible)
  </how-to-verify>
  <resume-signal>
Report soak results:
- "PASS" with NRestarts count and any notes, OR
- "FAIL" with description of what went wrong

On PASS: Type "approved — soak passed, NRestarts=[N]"
On FAIL: Describe the failure for diagnosis
  </resume-signal>
  <verify>
    <automated>ssh fc1 'sudo systemctl is-active fc-core'</automated>
  </verify>
  <acceptance_criteria>
    - User confirms Pi is physically at the farm
    - User confirms service ran for 24 hours (D-04)
    - User reports ActiveState=active after soak period
    - User reports humidifier was cycling (not stuck)
    - User declares "better than timer" per DEPL-01
  </acceptance_criteria>
  <done>24-hour soak test passed, system declared production-ready, DEPL-01 satisfied, MVP complete</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| workstation->Pi SSH | Key-based auth, private LAN |
| deploy.sh->Pi | rsync over SSH, overwrites Pi files from repo |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04 | Denial of Service | deploy.sh restarts service, causing brief control gap | accept | RestartSec=5 means less than 10s gap; dwell guard prevents rapid cycling on restart; safe-state OFF during restart is correct behavior for humidifier |
| T-05-05 | Elevation of Privilege | deploy.sh runs sudo systemctl restart | accept | fc1 SSH uses key-based auth to ubuntu user; ubuntu already has sudo for systemctl; same as all prior phases |
</threat_model>

<verification>
- deploy.sh completes successfully
- Pi config shows target_humidity: 0.80
- fc-core service active after deploy
- 24-hour soak test passes (human verification)
- Grower can observe dashboard
</verification>

<success_criteria>
- Production config deployed and verified on FC-1
- 24-hour soak test completed with service stable
- Humidity maintaining target range demonstrably better than timer
- DEPL-01 requirement satisfied
- MVP declared complete
</success_criteria>

<output>
After completion, create `.planning/phases/05-production-deployment/05-02-SUMMARY.md`
</output>
