# MUSHY-59: control-law replay vs recorded fc.pid_output

## What this validates, and what it does not

`fc.pid_output` is recorded telemetry: the raw, pre-bias output the deployed
control law actually published (`fc_controller.py` ~line 1821,
`max(0, min(1, raw_pid_output))`). Feeding recorded `fc.humidity` into the
reconstructed law (`fc_core.sim.control_loop.ControlLoop`) and comparing its
output against that recorded value involves **no chamber physics on either
side**. A mismatch here is unambiguously a control-law reconstruction error,
not a plant-model error — that is the entire point of the ticket.

`run_closed_loop()` (`fc_core/sim/replay.py`) now calls the same
`ControlLoop` class this validator drives (MUSHY-59 refactor). There is
exactly one implementation of the law in the codebase; this validates that
implementation, not a second copy of it.

## Method summary

- Window: 2026-06-29T00:00Z .. 2026-08-09T00:00Z (41 days, 3,542,400 grid
  seconds), the span reconnaissance confirmed had a stable band
  (`[0.885, 0.915]`, `defend_side='both'`) and an unchanged deployed law.
- `fc.pid_output` and `fc.humidity_target` deduped onto a 1 Hz grid (last
  value per wall-clock second — ~48% of raw samples share a second).
  `fc.humidity` (native cadence ~2.0 s median) asof-joined onto the same
  grid, backward direction, 10 s tolerance (`GAP_BREAK_S`).
- **Driving continuity** (does the replay keep stepping one `ControlLoop`
  instance) breaks only at a real RH or target hole beyond `GAP_BREAK_S`,
  or at a restart (see below). It does NOT break at the 0.9585 ramp
  stretch, because the control law reads a **static** per-mode band
  (`BandSpec(mode.band_low, mode.band_high, mode.defend_side)`,
  `fc_controller.py` ~line 1715) — never the ramping
  `_effective_setpoint` that `fc.humidity_target` actually publishes. The
  ramp is a telemetry artifact, not a control-law input.
- **Metrics** are still excluded for every second where
  `fc.humidity_target` is not within 0.0015 of a band edge — per the brief,
  even though the law itself is unaffected — plus any second with no
  recorded `fc.pid_output` (a short ingestion gap; the loop keeps running
  through it, there is just nothing to compare against).
- **Restarts** detected independently: `fc.humidity_target > 0.92`, grouped
  into events by a >300 s gap between flagged seconds. **27 restarts** in
  the window (fully explained by the documented fc-core restart re-init bug:
  `_effective_setpoint` inits to nominal `target_humidity` (0.96) and ramps
  at `delta/30` per tick — first published tick is 0.9585 — while
  `fc_controller.__init__` simultaneously builds a fresh `PID(...,
  auto_mode=False)` with zero integral and `_d_filtered = 0.0`). A new
  `ControlLoop` is instantiated at each restart's first flagged second, even
  when the telemetry has no gap there, because the real PID state resets at
  that instant regardless of the (unaffected) band.

## Warm-up policy, and the mistake that first version made

A segment starting exactly at a restart is **restart-anchored**: the real
controller was demonstrably in the identical fresh state
(`ControlLoop.__init__` also zero-inits) at that second, so the replay uses
**zero warm-up** there. Every other segment boundary is a plain telemetry
gap with unknown mid-stream integrator state, so it gets the full 6 h
(`WARMUP_S`, ~12x the 1800 s decay time constant) discarded.

The first version of this script anchored restart segments at the END of
the excluded ramp stretch (where `fc.humidity_target` reconverges near a
band edge, ~100-150 s after the restart) and reset the PID there instead.
That silently discarded 100-150 s of real integral accumulation the actual
controller had already built up while the RH sat below the band midpoint —
a manufactured discontinuity. It surfaced exactly where predicted: the
single worst cluster of errors in the whole run (up to 0.057 absolute,
consistently *under*-predicting) landed in the tens of seconds right after
2026-06-30's restart, in `below_midpoint_feather` — precisely the regime
where integral windup matters. Anchoring at the true restart instant and
replaying continuously through the (still metrics-excluded) ramp removed
it: restart-anchored and non-anchored RMSE are now nearly identical
(0.001455 vs 0.001464 — see table below), which is the sanity check this
fix should produce if the hypothesis was right.

## Restart cost accounting

| Quantity | Value | % of window |
|---|---|---:|
| Grid seconds (41 days) | 3,542,400 | 100% |
| Real telemetry holes (RH/target gap > 10 s) | 109,157 s | 3.08% |
| Ramp stretches (0.9585 anomaly + reconvergence) | 24,810 s | 0.70% |
| Seconds with no recorded `pid_output` | 2,291 s | 0.06% |
| Restart-anchored 6 h warm-up discard (15 segments) | 319,781 s | 9.03% |
| Segments too short to warm up (dropped entirely) | 95,291 s | 2.69% |
| **Used in metrics** | **2,991,070 s** | **84.4%** |

27 restarts detected; 25 produced an immediately-following drive-valid
stretch long enough to anchor a segment at zero warm-up (the other 2 landed
inside a telemetry hole, so their aftermath falls into an ordinary
full-warm-up segment instead). Because restart segments need zero warm-up
under the fixed policy, the "27 restarts x 6 h" worst case (~16% of the
window) never materializes — only 15 of the 98 total segments actually pay
the full 6 h warm-up, costing 319,781 s (9.0% of window) of that discard
budget (nominal 15 x 21,600 s = 324,000 s; actual is slightly less because
a few segments' 6 h window is truncated by the next boundary — this row
was previously omitted from the table, and the prose mis-stated the cost
as 90,000 s / 2.5%); the rest of `skipped_s` is short non-restart
fragments (telemetry blips) too brief to reach 6 h even once.

## Warm-up sensitivity

RMSE as a function of warm-up length, computed over the SAME segmentation
(restart-anchored segments always use 0 regardless of this sweep — the
sweep only varies the non-restart-anchored segments' warm-up):

| Warm-up | RMSE |
|---|---|
| 1 h | 0.0015 |
| 3 h | 0.0015 |
| 6 h | 0.0015 |
| 12 h | 0.0014 |

Flat from 1 h onward. This is itself a finding: the pre-fix version showed
real sensitivity between 1 h and 3-6 h (0.0065 -> 0.0014), which was really
the restart-anchoring artifact bleeding into the sweep (short
non-restart-anchored segments were absorbing some of what should have been
restart-anchored zero-warmup time). With restarts correctly anchored, 6 h
is conservative but not load-bearing — 1 h would have been adequate for
this window's actual integrator dynamics (tau=1800 s, so 1 h is already 2x
the time constant).

## Overall metrics

Pooled over all metric seconds, including the ~75% where the controller is
correctly idle and both series are trivially 0 (see "Zero-suppressed
statistics" below for the figure restricted to seconds where the law
actually computed something nonzero).

| Metric | Value |
|---|---|
| n | 2,991,070 |
| RMSE | 0.00146 |
| MAE | 0.00033 |
| Max absolute error | 0.1455 |
| Median error | 0.0 |
| Within 0.01 | 99.51% |
| Within 0.05 | 99.9998% |
| Within 0.10 | 99.9999% |
| Correlation | 0.999982 |

**Tolerance: 0.01 absolute (1 percentage point of the [0,1] duty output),
met on 99.5% of seconds.** Justification: `fc.pid_output` is itself
float32-quantized telemetry and the replay assumes exactly dt=1.0 s while
the real controller's timer jitters slightly around that value (ROS2 timer,
not a hard real-time guarantee) — sub-1% output noise from that jitter
alone is expected and not a reconstruction bug. 0.01 is roughly the level
at which "recorded vs predicted" stops being distinguishable from clock and
quantization noise, one to two orders of magnitude below the PID's actual
dynamic range of response to a feather-region error.

## Zero-suppressed statistics

72% of the window is `above_band_high` with the real controller correctly
idle (RH comfortably above the defended ceiling, PID pinned at 0) — and in
that regime BOTH `pid_recorded` and `pid_predicted` are exactly 0 on almost
every second. Those seconds match by construction (there is nothing for
either side to get right or wrong), so the pooled "99.51% within 0.01"
above is inflated by guaranteed agreement. Excluding every second where
BOTH series are ~0 (`abs() <= 1e-9`) isolates the seconds where the law
actually had to compute something nonzero:

| Quantity | Value |
|---|---|
| Metric seconds excluded (both series ~0) | 2,243,382 / 2,991,070 (75.0%) |
| Remaining ("earned") seconds | 747,688 |
| RMSE (zero-suppressed) | 0.00292 |
| MAE (zero-suppressed) | 0.00134 |
| Max absolute error | 0.1455 (same 2026-07-18 dual-publisher/D-kick outlier) |
| Within 0.01 (zero-suppressed) | 98.03% |
| Correlation (zero-suppressed) | 0.999968 |

RMSE roughly doubles (0.00146 -> 0.00292) and within-0.01 drops from
99.51% to 98.03% once the trivial matches are removed — a real, honestly
worse number, but still two orders of magnitude below the [0,1] output
range and still >98% within the stated tolerance. **The result holds up
on the hardest subset**: it does not depend on the 75% of seconds where
agreement was guaranteed rather than earned.

Per regime, zero-suppressed (i.e. restricted to each regime's own nonzero
seconds):

| Regime | n (nonzero) | n (full) | share nonzero | RMSE | MAE | within 0.01 |
|---|---:|---:|---:|---:|---:|---:|
| above_band_high | 26,946 | 2,155,399 | 1.2% | 0.00756 | 0.00549 | 81.9% |
| below_midpoint_feather | 370,405 | 370,405 | 100% | 0.00342 | 0.00177 | 97.3% |
| in_band | 346,855 | 461,784 | 75.1% | 0.00118 | 0.00057 | 99.99% |
| mode_c_bypass | 3,482 | 3,482 | 100% | 0.0 | 0.0 | 100% |

`below_midpoint_feather` and `mode_c_bypass` are unchanged (they are
essentially always nonzero, so zero-suppression barely touches them —
these were already the "earned" numbers). `above_band_high`'s nonzero
subset is the smallest (1.2% of that regime's seconds — the decay tail as
duty relaxes toward 0 after RH crosses back above the band) and shows the
weakest agreement of any category, 81.9% within 0.01: this is a brief
transitional edge, sensitive to exactly where dt/timing puts the decay
curve, not evidence the pooled `above_band_high` RMSE (0.00085) is
misleading about the branch as a whole — it is dominated, correctly, by
the 98.8% of that regime's seconds sitting at the guaranteed-correct 0.

## Per-regime breakdown

| Regime | n | share | RMSE | MAE | max abs | within 0.01 | corr |
|---|---:|---:|---:|---:|---:|---:|---:|
| above_band_high (linear PID branch, error > 0) | 2,155,399 | 72.1% | 0.00085 | 0.00007 | 0.0585 | 99.77% | 0.99959 |
| in_band (decay branch, error == 0) | 461,784 | 15.4% | 0.00102 | 0.00043 | 0.1455 | 99.99% | 0.99999 |
| below_midpoint_feather (quadratic/linear feather) | 370,405 | 12.4% | 0.00342 | 0.00177 | 0.0470 | 97.34% | 0.99996 |
| mode_c_bypass (open-loop, duty forced to 1.0) | 3,482 | 0.1% | 0.0 | 0.0 | 0.0 | 100% | n/a (constant) |
| freeze (`defend_side='low'` branch) | 0 | 0% | -- | -- | -- | -- | -- |

`freeze` never fires: the band is `defend_side='both'` for the entire
window, and `project_error_pct` only returns `None` under `defend_side ==
'low'`. `mode_c_bypass` is a trivial perfect match by construction (both
sides just emit the literal constant 1.0 whenever RH drops more than the
5-point bypass_threshold below the defended floor — no arithmetic to get
wrong). `below_midpoint_feather` is the hardest regime, as expected: it is
where the PID actively integrates a nonzero error and small dt/timing
differences compound, but even there 97.3% of seconds are within 0.01 and
the RMSE (0.0034) stays two orders of magnitude below the branch's dynamic
range. `above_band_high`'s pooled RMSE (0.00085) is 98.8% trivially-agreeing
seconds (both series pinned at 0) carrying the average down; its 26,946
"earned" nonzero seconds (see "Zero-suppressed statistics" above) run RMSE
0.00756 with only 81.9% within 0.01 — the weakest agreement of any
category in this dataset. Do not read the low pooled 0.00085 as strength.

## Per-day metrics (sample; full table in the pickle / rerun for all 41 days)

Most days have RMSE in the 0.0000-0.003 band. Two stand out:

- **2026-07-18**: max abs error 0.1455, the single largest error in the
  dataset, at 17:04:32-38 UTC. This is **not** a force-experiment override.
  `fc_experiments` has zero rows anywhere in the 41-day window (its last row
  is 2026-06-14). It also could not be one mechanically: the force-mode
  short-circuit (`fc_controller.py` ~1682-1700) publishes the forced duty to
  *both* `fc.pid_output` and `fc.humidity_target`, so a real force episode
  would show a non-band-edge `humidity_target` and get excluded by this
  script's own `target_ok` gate before it ever reached the metrics. At
  17:04:32-38, `fc.humidity_target` is a clean, constant 0.9150000214576721
  throughout — this second was never excluded, and the chamber was
  genuinely at 93.6% RH, well above the band ceiling, where the recorded
  `pid_output` of exactly 0.0000 is correct.
  The actual cause is a **dual-publisher episode on `fc.humidity` itself**:
  for ~7 s a second publisher interleaved onto the topic, alternating real
  (~93.5x) and intruder (~90.4x) readings at :31.26, :33.27, :35.26, and
  :37.27 (each paired with a duplicate `fc.temperature` row), while
  `fc.humidity_2` simultaneously jumped 96.4 -> 88.8 -> 90.3. The replay's
  backward asof-join latched the 90.43 intruder value on the even-second
  grid ticks, stepping the *projected* error +2.2 -> 0 in a single tick.
  The PID's derivative term, `-Kd * d_input/dt`, sees that step
  (`4.0 * 2.2 = 8.8`) and the low-pass filter (`alpha = 1/61`) brings it to
  **0.144** — matching the observed 0.1455 to three digits. The recorded
  0.0 was correct; the *prediction* was the artifact, produced by the
  replay's own asof-join handling of a dual-publisher episode, not by any
  gap in the reconstructed control law.
  This is a third error class the ticket's framing (plant error vs.
  control-law error) doesn't name: **harness input error** — the law and
  the plant are both fine, but the replay's ingestion of a genuinely
  ambiguous multi-publisher second manufactured a phantom derivative kick.
  Recommendation (not implemented here): de-duplicate multi-publisher
  seconds on `fc.humidity` (e.g. drop or flag seconds where more than one
  raw sample lands within the same grid second from what looks like a
  second source) so this outlier is removed honestly, by fixing the input,
  rather than by narrative.
- **2026-07-08/09, 2026-07-16/17**: RMSE 0.0023-0.0029, mildly elevated but
  still two orders of magnitude below the output's dynamic range — no
  restarts land on these days per the detected list, so this is ordinary
  `below_midpoint_feather` activity (the hardest regime) rather than a
  restart artifact.

No day shows systematic divergence (a day that "matches in July and
diverges in August" per the brief's concern) — the elevated-RMSE days are
scattered through the window and each is explained by regime mix
(more `below_midpoint_feather` time), not by drift in the deployed law.

## Diagnosis

**The control law reproduces recorded `fc.pid_output` within a justified
0.01-absolute tolerance on 99.5% of the validated window (98.0% on the
zero-suppressed subset that excludes the 75% of seconds where both series
are trivially 0).** The zero-suppressed result is the one that actually
says whether the law is validated where it does work, and it holds up:
RMSE roughly doubles (0.0015 -> 0.0029) but stays two orders of magnitude
below the output's dynamic range. The residual is not unexplained:
- The single largest outlier (0.1455, 2026-07-18) is explained by a
  dual-publisher episode on `fc.humidity` (not a force-experiment override
  — `fc_experiments` has zero rows in this window): the replay's
  backward asof-join latched an intruder RH sample, producing a spurious
  derivative kick. This is harness input error, not a control-law defect,
  localized to one ~6 s window.
- The next tier of outliers (up to ~0.06, scattered through
  `below_midpoint_feather`) is consistent with dt jitter around the
  assumed-exact 1.0 s and float32 telemetry quantization on
  `fc.pid_output`, both expected at the sub-percent level and not evidence
  of a reconstruction bug.
- `mode_c_bypass` and the `above_band_high` linear branch match almost to
  the float ULP; `freeze` never exercises in this window.

No gain was tuned to produce this result — `Gains()` in
`fc_core/sim/control_loop.py` are the constants read live off fc1 on
2026-08-09, unchanged since 2026-06-22.

## What this does not validate

- The reconstruction is evidenced for the P/I path and the band/feather
  projection in the `defend_side='both'` configuration ONLY.
- The D path is **not** positively validated. In a falsification sweep,
  setting `kd = 0` *improved* the fit on a 4-day slice (RMSE 0.00206 vs
  0.00236 baseline), while `kd + 10%` worsened it — the derivative term
  contributes net noise rather than signal over this window, consistent
  with dt jitter and with the 0.1455 outlier above being a D-kick.
- The `freeze` branch never fired (0 s), and the `defend_side` low/high
  branches are entirely unexercised, because the band was `both`-defended
  for the whole window.
- `mode_c_bypass`'s 3,482 s is `1.0 == 1.0` by construction and carries no
  information about whether that branch's logic is correct.

So roughly two of five branches carry no evidence from this window.

## Files

- `fc_core/sim/control_loop.py` — extracted `ControlLoop` class (MUSHY-59).
- `fc_core/sim/replay.py` — now calls `ControlLoop` instead of inline logic.
- `scripts/replay-control-law.py` — this validator.
- `999.33-07-control-replay.csv` — 60 s buckets, `idxmin`/`idxmax` (signed)
  error row kept per bucket — verified equivalent in effect to min/max
  |error| (0 of 50,081 buckets lose the bucket's max |error|) — (63,234
  rows).
- `999.33-07-control-replay-48h.csv` — full 1 Hz resolution,
  2026-07-08T00:00Z + 48 h, chosen as the 48 h window with the highest
  fraction of nonzero PID output (170,393 rows, 125,063 nonzero — an
  earlier pick, 2026-07-20, had 0 of 149,143 rows nonzero and was
  replaced for being uninformative).
