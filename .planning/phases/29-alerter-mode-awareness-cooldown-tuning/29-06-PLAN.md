---
phase: 29
plan: 06
type: execute
wave: 3
depends_on:
  - "29-03"
  - "29-04"
  - "29-05"
files_modified:
  - .planning/phases/29-alerter-mode-awareness-cooldown-tuning/29-COOLDOWN-TUNING.md
  - src/chambers/fc-core/config/fc_config.yaml
autonomous: true
requirements:
  - ALRT-10
tags:
  - cooldown-tuning
  - analysis
  - one-shot
  - phase-20-carry
must_haves:
  truths:
    - "29-COOLDOWN-TUNING.md exists with per-rule statistics extracted from `docker logs mushy-alerter` over the longest available window (target ≥14 days; documented if shorter)."
    - "Per alertType: total fires, recovery count, mean inter-fire interval (minutes), P95 fires-per-hour, recommended cooldown_min."
    - "fc_config.yaml `modes.{fruiting,pinning}.alerter.{cooldown_min,critical_cooldown_min,humidifier_stuck_min,oob_n,oob_window_min}` updated to the recommended values from the analysis."
    - "Recommendation rationale documented per rule (Pitfall 7 — restart cadence floor; Pitfall 8 — mode-swap dedup carries by alertType not (alertType,mode))."
  artifacts:
    - path: .planning/phases/29-alerter-mode-awareness-cooldown-tuning/29-COOLDOWN-TUNING.md
      provides: "One-shot tuning analysis: methodology, raw stats table, recommended defaults, rationale"
      contains: "## Per-rule Statistics"
    - path: src/chambers/fc-core/config/fc_config.yaml
      provides: "Tuned values overwriting bootstrap defaults from plan 29-03"
      contains: "modes.fruiting.alerter.cooldown_min"
  key_links:
    - from: 29-COOLDOWN-TUNING.md analysis table
      to: fc_config.yaml updates
      via: "Recommended `cooldown_min` per (mode, rule) is committed as the YAML value"
      pattern: "Recommended\\s+cooldown_min"
---

<objective>
One-shot offline tuning analysis for ALRT-10 (Phase 20 carry-from-v1.3): pull ≥14 days of `mushy-alerter` docker logs from elder-plops, compute per-rule fire/recovery/inter-fire/P95 statistics, propose tuned cooldown thresholds per mode, and commit the new defaults to `fc_config.yaml`.

Purpose: settle a year+ of farmer-gut-based cooldowns with data. Closes the v1.3 carry-forward.

Output: `29-COOLDOWN-TUNING.md` artifact with methodology + raw stats + recommendations + rationale; fc_config.yaml updated.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@./CLAUDE.md
@.planning/phases/29-alerter-mode-awareness-cooldown-tuning/29-CONTEXT.md
@.planning/phases/29-alerter-mode-awareness-cooldown-tuning/29-RESEARCH.md

<interfaces>
<!-- Tuning data source — `alert_history` Timescale table does NOT exist (verified absent via grep -r in RESEARCH); -->
<!-- the docker-logs path is the only path. RESEARCH §"Tuning Data Access" provides the recipe. -->

<!-- alerter log line shape (verify by inspecting one day before scripting) — emitted from -->
<!-- src/agents/alerter/src/index.js apply-action region (~line 100-105). -->
<!-- Look for: [send] / [recovery] / [heartbeat] / [PROBLEM] / [RECOVERY] keywords. -->

<!-- Pitfall 7 (RESEARCH): alerter restarts ZERO state.lastFiredAt — the chosen cooldown_min -->
<!-- floor must absorb operator restart cadence (~3-5 per week historically). -->

<!-- Pitfall 8: cooldowns are alertType-keyed (NOT (alertType, mode)-keyed) — D-09. -->
<!-- A pinning swap immediately after a fruiting fire reuses the SAME cooldown counter. -->
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Pull docker logs + run analysis script + draft 29-COOLDOWN-TUNING.md</name>
  <files>.planning/phases/29-alerter-mode-awareness-cooldown-tuning/29-COOLDOWN-TUNING.md</files>
  <read_first>
    - .planning/phases/29-alerter-mode-awareness-cooldown-tuning/29-RESEARCH.md §"Tuning Data Access (D-07)" — full grep recipe + pre-flight retention check
    - .planning/phases/29-alerter-mode-awareness-cooldown-tuning/29-RESEARCH.md §Pitfall 7 + §Pitfall 8
    - .planning/phases/29-alerter-mode-awareness-cooldown-tuning/29-CONTEXT.md D-07 + D-08
    - src/agents/alerter/src/index.js apply-action region (~line 100-105) — confirm log line format BEFORE scripting parser
  </read_first>
  <what-built>
    Executor will perform the following automated steps on elder-plops (where mushy-alerter runs):

    **Pre-flight:**
    ```bash
    docker logs mushy-alerter --since 14d 2>&1 | head -1
    # If timestamp is younger than 14d ago → retention is shorter; document actual window in TUNING.md.
    docker logs mushy-alerter --since 14d 2>&1 | wc -l
    # Sanity check on volume.
    ```

    **Bulk pull:**
    ```bash
    docker logs mushy-alerter --since 14d > /tmp/alerter-14d.log 2>&1
    ```

    **Verify log line grammar (one-shot inspection — do this BEFORE scripting):**
    ```bash
    head -200 /tmp/alerter-14d.log | grep -E '\[(send|recovery|heartbeat|PROBLEM|RECOVERY)\]'
    # Confirm column layout — which field is alertType, which is timestamp, etc.
    ```

    **Compute per-rule statistics** (script the parser to match actual log shape — recipe is illustrative):
    ```bash
    # Adapt awk fields to match log layout (column 1 = ISO timestamp, look for [send] line + alertType field)
    awk '/\[send\]/ {print $1, $2, $NF}' /tmp/alerter-14d.log > /tmp/sends.tsv
    awk '/\[recovery\]/ {print $1, $2, $NF}' /tmp/alerter-14d.log > /tmp/recoveries.tsv

    # For each unique alertType, compute:
    #   - total fires
    #   - total recoveries
    #   - mean inter-fire interval (in minutes)
    #   - P95 fires-per-hour
    #   - histogram of inter-fire intervals
    ```

    **Cross-reference mode swaps** (Pitfall 8) — `set_mode` service calls or `current_mode` topic transitions in the log around fire bursts. Confirm cooldown_min recommendation > typical mode-swap-to-mode-swap window.

    **Account for restart cadence** (Pitfall 7): grep for alerter container start lines (`[alerter] starting` or similar) — count starts per week; recommended cooldown_min must NOT be lower than the typical inter-restart interval, otherwise restart-spam dominates the metric.

    Then drafts `.planning/phases/29-alerter-mode-awareness-cooldown-tuning/29-COOLDOWN-TUNING.md` using this template:

    ```markdown
    # Phase 29 — Cooldown Tuning Analysis (ALRT-10 / Phase 20 carry)

    **Analysis date:** YYYY-MM-DD
    **Data window:** YYYY-MM-DD → YYYY-MM-DD (N days)
    **Source:** `docker logs mushy-alerter` on elder-plops (alert_history Timescale table absent — RESEARCH verified)

    ## Methodology

    1. Bulk-pulled docker logs (--since 14d, actual retention: <REPORT>).
    2. Parsed [send] and [recovery] lines via awk; alertType extracted from <FIELD>.
    3. Computed inter-fire intervals (in minutes), grouped by alertType.
    4. Mode swaps cross-referenced from <SOURCE — ros2 topic echo log? service call audit?>.
    5. Operator restart cadence: N restarts over <WINDOW> = X per week.

    ## Per-rule Statistics

    | alertType | total_fires | recoveries | mean_inter_fire_min | p95_per_hour | observed_cooldown_floor | proposed_cooldown_min | rationale |
    |-----------|-------------|------------|---------------------|--------------|------------------------|----------------------|-----------|
    | rh_oob    | <N> | <N> | <X> | <X> | <X> | <X> | <text> |
    | humidifier_stuck | <N> | <N> | <X> | <X> | <X> | <X> | <text> |
    | pi_offline | <N> | <N> | <X> | <X> | <X> | <X> | <text> |
    | sensor_silent | <N> | <N> | <X> | <X> | <X> | <X> | <text> |

    ## Per-mode Statistics (segmented by current_mode at fire time)

    *Phase 28 mode swaps started ~YYYY-MM-DD; segmentation is partial.*

    | mode | rh_oob fires | mean_inter_fire_min | proposed_cooldown_min |
    |------|--------------|---------------------|----------------------|
    | fruiting | <N> | <X> | <X> |
    | pinning  | <N> | <X> | <X> |

    ## Recommended fc_config.yaml Defaults

    ```yaml
    modes.fruiting.alerter.cooldown_min:          <X>
    modes.fruiting.alerter.critical_cooldown_min: <X>
    modes.fruiting.alerter.humidifier_stuck_min:  <X>
    modes.fruiting.alerter.oob_n:                 <X>
    modes.fruiting.alerter.oob_window_min:        <X>
    modes.pinning.alerter.cooldown_min:           <X>
    modes.pinning.alerter.critical_cooldown_min:  <X>
    modes.pinning.alerter.humidifier_stuck_min:   <X>
    modes.pinning.alerter.oob_n:                  <X>
    modes.pinning.alerter.oob_window_min:         <X>
    ```

    ## Caveats

    - Pitfall 7: cooldown floor accounts for ~N operator restarts per week; values below <X> would be eaten by restart-spam.
    - Pitfall 8: cooldowns are alertType-keyed; pinning's cooldown_min < (typical fruiting→pinning swap window) would cause spam.
    - alert_history Timescale table absent — analysis is log-parse only; future v2 should write to a dedicated table.
    - SCD41 RH suspect-high (memory `project_phase26_sht30_happy_path_unverified`) — rh_oob fires from SCD41 may be over-counted; analysis treats both sensors equally.
    ```
  </what-built>
  <how-to-verify>
    1. SSH to elder-plops, run the pre-flight retention probe — confirm window length.
    2. Read the generated `29-COOLDOWN-TUNING.md`:
       a. Statistics table is populated (not template placeholders).
       b. At least 4 alertTypes appear.
       c. Recommended cooldown_min for each (mode, rule) cell has a 1-sentence rationale.
       d. Caveats section names Pitfall 7 + Pitfall 8 + SCD41 caveat.
    3. Sanity-check the proposed cooldown_min values:
       - rh_oob fruiting: typically expect 30-60 min based on 2-week farmer attestation
       - humidifier_stuck: expect 60-120 min (alarms fire late by design)
       - pi_offline: expect ≤ p99 outage duration (so post-recovery alerts dedup)
    4. Reply `approved` to commit the recommendations to fc_config.yaml in Task 2, OR reply with adjustments (e.g., "raise pinning cooldown to 60min — pinning swings hard").
  </how-to-verify>
  <resume-signal>Type "approved" to proceed to Task 2 commit, or describe revisions needed.</resume-signal>
  <acceptance_criteria>
    - File `.planning/phases/29-alerter-mode-awareness-cooldown-tuning/29-COOLDOWN-TUNING.md` exists.
    - File contains a populated `## Per-rule Statistics` table (no `<X>` template placeholders remaining).
    - File contains a `## Recommended fc_config.yaml Defaults` YAML block with concrete integer values for all 10 Tier B keys.
    - File contains `## Caveats` section naming Pitfall 7, Pitfall 8, and the SCD41 caveat.
    - Operator (user) types "approved" or provides revision instructions.
    - **W8 retention escalation gate:** if the actual data window discovered in pre-flight (`docker logs --since 14d | head -1` timestamp) is < 14 days, the executor MUST NOT silently produce weak-tuned values. Instead: STOP, append a top-of-file note `## ⚠ INSUFFICIENT DATA — RECOMMEND DEFERRAL` to 29-COOLDOWN-TUNING.md with (a) actual window length, (b) recommendation to defer ALRT-10 to a future phase once retention has accumulated, (c) explicit ask to operator: "approve weak-tuning anyway" vs "defer ALRT-10 + ship Phase 29 without commit to fc_config.yaml". Operator decision becomes the resume-signal.
  </acceptance_criteria>
  <done>Tuning analysis produced + reviewed + approved (or operator-approved deferral if W8 retention gate triggered).</done>
</task>

<task type="auto">
  <name>Task 2: Commit tuned values to fc_config.yaml</name>
  <files>src/chambers/fc-core/config/fc_config.yaml</files>
  <read_first>
    - src/chambers/fc-core/config/fc_config.yaml (current state — bootstrap values from plan 29-03)
    - .planning/phases/29-alerter-mode-awareness-cooldown-tuning/29-COOLDOWN-TUNING.md (Recommended Defaults section just approved)
  </read_first>
  <action>
    For each of the 10 Tier B keys in the YAML's `fc_controller: ros__parameters:` block (added by plan 29-03), overwrite the bootstrap value with the corresponding Recommended Default from `29-COOLDOWN-TUNING.md`.

    Keys to update (DO NOT touch any other line):
    - `modes.fruiting.alerter.cooldown_min`
    - `modes.fruiting.alerter.critical_cooldown_min`
    - `modes.fruiting.alerter.humidifier_stuck_min`
    - `modes.fruiting.alerter.oob_n`
    - `modes.fruiting.alerter.oob_window_min`
    - `modes.pinning.alerter.cooldown_min`
    - `modes.pinning.alerter.critical_cooldown_min`
    - `modes.pinning.alerter.humidifier_stuck_min`
    - `modes.pinning.alerter.oob_n`
    - `modes.pinning.alerter.oob_window_min`

    Tier C globals (`pi_offline_min`, `sensor_offline_min`, `heartbeat_hour`, `max_sends_per_hour`) — review the analysis; if it recommends changes, apply them; otherwise leave at plan 29-03 defaults.

    Add an inline YAML comment at the top of the alerter sub-block citing the source:
    ```yaml
        # Phase 29 ALRT-10 (D-08) — tuned values from 29-COOLDOWN-TUNING.md (YYYY-MM-DD).
    ```

    NO production code changes; YAML-only.
  </action>
  <verify>
    <automated>cd /mnt/slime-kingdom/opt/mushy &amp;&amp; python3 -c "import yaml; d=yaml.safe_load(open('src/chambers/fc-core/config/fc_config.yaml')); p=d['fc_controller']['ros__parameters']; required=['modes.fruiting.alerter.cooldown_min','modes.fruiting.alerter.critical_cooldown_min','modes.fruiting.alerter.humidifier_stuck_min','modes.fruiting.alerter.oob_n','modes.fruiting.alerter.oob_window_min','modes.pinning.alerter.cooldown_min','modes.pinning.alerter.critical_cooldown_min','modes.pinning.alerter.humidifier_stuck_min','modes.pinning.alerter.oob_n','modes.pinning.alerter.oob_window_min']; [print(k, p[k]) for k in required]; assert all(isinstance(p[k], int) and p[k] > 0 for k in required), 'all values must be positive ints'"</automated>
  </verify>
  <acceptance_criteria>
    - YAML parses (verify command above runs without assertion failure).
    - All 10 Tier B values are positive integers.
    - At least one value differs from the plan 29-03 bootstrap defaults (otherwise the tuning analysis was non-committal — re-prompt user).
    - `git diff src/chambers/fc-core/config/fc_config.yaml` shows ONLY changes to the 10-14 Phase 29 lines.
    - `cd src/chambers/fc-core && pytest fc_core/test/test_validate_params.py -k alerter` STILL GREEN (validator accepts new values, range bounds OK).
  </acceptance_criteria>
  <done>fc_config.yaml carries the data-driven tuned defaults; validator pytest still green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docker logs → analysis script | Trusted (operator-shell, internal data). |
| 29-COOLDOWN-TUNING.md → fc_config.yaml commit | Operator-mediated (human checkpoint). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-18 | Tampering | Mistuned cooldown could cause alert spam (too short) or alert blackout (too long) | mitigate | Human checkpoint (Task 1) requires operator review of recommendations before commit. Validator range bounds (29-03 plan) cap the worst case (cooldown_min ≤ 240 min = 4 hours = no day-long blackouts). |
| T-29-19 | Information Disclosure | docker logs may contain alert message content (Signal recipient names? RH values?) | accept | Logs are kept on elder-plops only, accessible to operator only. Analysis output (29-COOLDOWN-TUNING.md) records only counts/intervals, not message bodies. |
</threat_model>

<verification>
- 29-COOLDOWN-TUNING.md exists, all template `<X>` placeholders replaced.
- fc_config.yaml diffs limited to Phase 29 lines.
- pytest TestAlerterParams still green.
- No production code changes in this plan.
</verification>

<success_criteria>
- ALRT-10 satisfied: cooldown thresholds tuned against ≥14 days of Phase 17+ data (or shorter window with documented retention).
- D-08 satisfied: tuned values land in fc_config.yaml `modes.{name}.alerter.*`.
- Analysis artifact preserved for posterity.
- Per-mode differentiation honored (fruiting tight, pinning intentionally looser).
</success_criteria>

<output>
After completion, create `.planning/phases/29-alerter-mode-awareness-cooldown-tuning/29-06-SUMMARY.md`.
</output>
