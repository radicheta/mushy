---
phase: 04-observability-integration
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - src/chambers/fc-core/config/fc_config.yaml
autonomous: false
requirements: [ACTR-01, TEST-02]

must_haves:
  truths:
    - "Humidifier SSR toggles on GPIO27 in response to control loop decisions on real FC-1 hardware"
    - "Full control loop works end-to-end: SCD41 reads humidity -> controller compares to setpoint -> SSR actuates humidifier"
    - "fc/actuators/humidifier topic is visible from workstation via ros2 topic echo with matching QoS"
  artifacts:
    - path: "src/chambers/fc-core/config/fc_config.yaml"
      provides: "Production-ready config with min_dwell_time restored"
      contains: "min_dwell_time: 300.0"
  key_links:
    - from: "fc_sensors.py SCD41"
      to: "fc_controller.py control_loop"
      via: "fc/humidity ROS topic subscription"
      pattern: "fc/humidity"
    - from: "fc_controller.py control_loop"
      to: "GPIO27 SSR"
      via: "set_humidifier() -> GPIO.output()"
      pattern: "humidifier_pin"
---

<objective>
Deploy code to FC-1 Pi and validate the complete control loop end-to-end on real hardware. Restore production config values and run a soak test.

Purpose: Satisfy ACTR-01 (humidifier GPIO control verified) and TEST-02 (full sensor-to-actuator loop on real hardware). This is the final gate before production deployment.
Output: Verified hardware operation; production-ready config deployed; soak test results logged.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-observability-integration/04-CONTEXT.md
@.planning/phases/04-observability-integration/04-RESEARCH.md
@.planning/phases/04-observability-integration/04-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restore production config, deploy to FC-1, and verify topics</name>
  <files>src/chambers/fc-core/config/fc_config.yaml</files>
  <read_first>
    - src/chambers/fc-core/config/fc_config.yaml
    - scripts/pi-deploy/deploy.sh
  </read_first>
  <action>
**Step 1 — Update `fc_config.yaml`** to restore production dwell time (per Pitfall 6 in RESEARCH.md):

Change `min_dwell_time: 5.0` to `min_dwell_time: 300.0` (the comment already says "production: 300").

Update the comment to: `min_dwell_time: 300.0  # Seconds — 5 minute minimum between humidifier toggles`

**Step 2 — Deploy to FC-1 Pi:**

```bash
./scripts/pi-deploy/deploy.sh
```

This runs rsync + colcon build + systemd restart on the Pi.

**Step 3 — Verify new actuator topic is live from workstation:**

```bash
# Check topic exists
ros2 topic list | grep actuators

# Verify QoS (should show TRANSIENT_LOCAL / RELIABLE)
ros2 topic info -v /fc/actuators/humidifier

# Read one message (must use matching QoS)
ros2 topic echo /fc/actuators/humidifier --qos-durability transient_local --qos-reliability reliable --once
```

**Step 4 — Verify existing topics still work:**

```bash
ros2 topic echo /fc/humidity --once
ros2 topic echo /fc/temperature --once
ros2 topic echo /fc/co2 --once
```

All commands run from elder-plops workstation over WireGuard VPN (or LAN if same network).

If `ros2 topic echo` for the actuator topic returns nothing, check QoS mismatch per Pitfall 2 in RESEARCH.md — use `ros2 topic info -v` to inspect offered vs requested QoS.
  </action>
  <verify>
    <automated>ssh fc1 'sudo systemctl is-active fc-core' && ros2 topic list 2>/dev/null | grep -c 'fc/actuators/humidifier'</automated>
  </verify>
  <acceptance_criteria>
    - fc_config.yaml contains `min_dwell_time: 300.0` (not 5.0)
    - `ssh fc1 'sudo systemctl is-active fc-core'` returns `active`
    - `ros2 topic list` includes `/fc/actuators/humidifier`
    - `ros2 topic info -v /fc/actuators/humidifier` shows `Durability: TRANSIENT_LOCAL` and `Reliability: RELIABLE`
    - `ros2 topic echo /fc/actuators/humidifier --qos-durability transient_local --qos-reliability reliable --once` returns a Bool message
    - `ros2 topic echo /fc/humidity --once` returns a RelativeHumidity message
    - `ros2 topic echo /fc/co2 --once` returns a Float32 message
  </acceptance_criteria>
  <done>fc-core service running on FC-1 with production config, all 4 topics publishing (humidity, temperature, CO2, actuator state), actuator topic verified TRANSIENT_LOCAL.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Soak test — verify full control loop on FC-1 hardware (TEST-02)</name>
  <files>none</files>
  <read_first>
    - src/chambers/fc-core/config/fc_config.yaml
  </read_first>
  <action>This is a human verification checkpoint. No code changes. The executor presents the verification instructions below and waits for user approval.</action>
  <what-built>Complete control loop: SCD41 sensor reads humidity -> fc_controller compares to 75% setpoint with 5% deadband -> SSR on GPIO27 toggles humidifier. Actuator state published on fc/actuators/humidifier. All telemetry visible in OpenMCT dashboard (humidity, temperature, CO2, humidifier on/off).</what-built>
  <how-to-verify>
**Prerequisite:** FC-1 Pi must be physically located at the fruiting chamber with the humidifier plugged into the SSR-controlled zapatilla on GPIO27. If Pi is still in lab, this test validates the loop with a substitute load (lamp, as done in D-06 breath test).

**Quick verification (5 min):**
1. Open terminal: `ssh fc1 'sudo journalctl -u fc-core -f'`
2. Watch for control loop debug log lines showing `Humidity: XX.X%, Humidifier: ON/OFF`
3. Confirm humidifier toggles when humidity crosses thresholds (breathe on sensor to spike CO2/humidity if needed)
4. From workstation: `ros2 topic echo /fc/actuators/humidifier --qos-durability transient_local --qos-reliability reliable` — confirm Bool changes match journalctl

**Dashboard verification:**
1. Run `docker-compose up -d` on elder-plops (starts OpenMCT + rosbridge)
2. Open `http://localhost:8080` in browser
3. Navigate to Fruiting Chamber FC-1 folder
4. Verify all 4 telemetry sources appear: Humidity, Temperature, CO2, Humidifier
5. Open each as a chart — confirm live data updating

**Soak test (1+ hour, when Pi is at farm):**
1. Let system run for 1+ hour with real humidifier connected
2. Monitor via journalctl or OpenMCT dashboard
3. Confirm humidity stays within target range (70-80%, i.e., 0.75 +/- 0.05)
4. Confirm min_dwell_time (5 min) is respected between toggles
5. Note: if Pi is still in lab, the quick verification above is sufficient for now; soak test with real humidifier will happen when Pi moves to farm (D-07)
  </how-to-verify>
  <verify>
    <automated>ssh fc1 'sudo systemctl is-active fc-core'</automated>
  </verify>
  <acceptance_criteria>
    - User confirms humidifier state changes visible in journalctl log output
    - User confirms ros2 topic echo shows Bool state matching observed behavior
    - User confirms OpenMCT dashboard shows 4 telemetry sources with live updating charts
  </acceptance_criteria>
  <done>Human verified full control loop on FC-1 hardware. TEST-02 satisfied.</done>
  <resume-signal>Type "approved" if control loop works end-to-end, or describe any issues observed. If soak test is deferred (Pi not at farm yet), type "approved-quick" to indicate quick verification passed and soak test is pending relocation.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SSH to FC-1 | Key-based auth over LAN/WireGuard; no new exposure |
| GPIO27 -> SSR -> 220V AC | Physical safety boundary; SSR rated 10A, humidifier well within |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04 | S (Spoofing) | SSH deploy to FC-1 | accept | Key-based SSH auth already established in Phase 1; no new credentials |
| T-04-05 | E (Elevation) | GPIO27 SSR control | accept | SSR defaults OFF on boot (GPIO LOW); safe-state logic drives OFF on sensor failure (Phase 3 CTRL-05) |
</threat_model>

<verification>
1. `ssh fc1 'sudo systemctl is-active fc-core'` returns `active`
2. `ros2 topic echo /fc/actuators/humidifier --qos-durability transient_local --qos-reliability reliable --once` returns Bool data
3. Human confirms control loop transitions visible in journalctl
4. Human confirms OpenMCT dashboard shows 4 telemetry sources with live data
</verification>

<success_criteria>
- fc-core running on FC-1 with production min_dwell_time (300s)
- All ROS topics publishing: fc/humidity, fc/temperature, fc/co2, fc/actuators/humidifier
- Humidifier SSR toggles in response to humidity threshold crossings (ACTR-01)
- Full sensor-to-actuator loop verified on real hardware (TEST-02)
- OpenMCT dashboard shows 4 live telemetry charts
</success_criteria>

<output>
After completion, create `.planning/phases/04-observability-integration/04-02-SUMMARY.md`
</output>
