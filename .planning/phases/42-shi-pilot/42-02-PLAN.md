---
phase: 42-shi-pilot
plan: 02
type: execute
wave: 1
depends_on: [42-01]
files_modified:
  - .planning/phases/42-shi-pilot/42-RUNBOOK.md
  - .planning/phases/42-shi-pilot/42-PILOT-LOG.md
autonomous: true
requirements: [PILOT-01, PILOT-02, PILOT-03, PILOT-04, PILOT-05, PILOT-06]
must_haves:
  truths:
    - "42-RUNBOOK.md is a strict operator playbook with one section per PILOT-NN (01..06)."
    - "Each PILOT-NN section contains: (a) Signal message template (operator -> bot), (b) expected bot reply shape, (c) farmOS verification command (calls one of the three tools from plan 42-01), (d) success criterion (boolean)."
    - "RUNBOOK references the synthetic-fixture rehearsal corpus at src/agents/alerter/test/eval/ingestion/fixtures/synthetic/ (Phase 41) for dry-run practice before the real pilot."
    - "42-PILOT-LOG.md is an append-only journal scaffold: file header + per-event section template + first entry marked `[pending operator]`."
    - "PILOT-LOG.md commit convention documented in the header: one commit per pilot event, message format `pilot(42): PILOT-NN <gist>`."
    - "No em-dashes; numerics rendered with fmtNum() semantics; address operator as Don Santiago per memory."
  artifacts:
    - path: ".planning/phases/42-shi-pilot/42-RUNBOOK.md"
      provides: "Operator playbook covering all 6 PILOT criteria with Signal templates + verification commands."
    - path: ".planning/phases/42-shi-pilot/42-PILOT-LOG.md"
      provides: "Append-only journal scaffold; operator extends it as the pilot runs."
---

<objective>
Author the operator playbook + journal scaffold. RUNBOOK tells Don Santiago which Signal messages to send at which farm event and what farmOS read-back proves the criterion landed. PILOT-LOG is the paper trail per `feedback_keep_paper_trail_of_intermediates`: every pilot event commits atomically with timestamp + Signal message id + farmOS asset/log ids.
</objective>

<tasks>
<task id="1">
<title>Write 42-RUNBOOK.md</title>
<acceptance>
- Six sections (PILOT-01..PILOT-06), each with template + expected reply + verification command + criterion.
- Pre-flight section listing required env vars (FARMOS_URL/USERNAME/PASSWORD, signal-cli reachable).
- Rehearsal section pointing at synthetic fixture corpus from Phase 41.
- No em-dashes; numerics use fmtNum() style.
</acceptance>
</task>

<task id="2">
<title>Write 42-PILOT-LOG.md scaffold</title>
<acceptance>
- Header with phase, pilot start date (TBD), commit convention.
- Per-event section template (timestamp / Signal msg id / farmOS asset id / farmOS log id / photos / commentary).
- First entry stub `[pending operator]` for PILOT-01.
- File explicitly marked append-only at the top.
</acceptance>
</task>
</tasks>

<verification>
- `grep -- '--' .planning/phases/42-shi-pilot/42-RUNBOOK.md | grep -v '^--$'` returns no em-dash hits.
- RUNBOOK references each of the three tools from plan 42-01 at least once.
- PILOT-LOG.md starts with `[pending operator]` marker.
</verification>
