---
phase: 42-shi-pilot
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tools/farmos-current-stage.js
  - tools/farmos-lineage.js
  - tools/farmos-pilot-reconstruct.js
  - tools/test/farmos-current-stage.test.js
  - tools/test/farmos-lineage.test.js
  - tools/test/farmos-pilot-reconstruct.test.js
  - tools/test/jest.config.js
autonomous: true
requirements: [PILOT-03, PILOT-05, PILOT-06]
must_haves:
  truths:
    - "Each tool is a single Node.js file at tools/<name>.js with a CLI entrypoint (`node tools/<name>.js <args>`); each is ~80-150 LOC."
    - "All three tools import createFarmosClient from src/agents/alerter/src/farmos/client.js (Phase 40 reuse) -- no new HTTP code."
    - "Tools are GET-only: they only call client.get() and client.head(); never post / patch / delete."
    - "FARMOS_URL, FARMOS_USERNAME, FARMOS_PASSWORD read from process.env; missing creds -> exit 1 with a clear message."
    - "farmos-current-stage.js implements the C1 derivation algorithm: contam > archive_spent > cold_shock > seeding > pre-inoc."
    - "farmos-current-stage.js supports --at <iso_timestamp> to scope logs to events at or before that moment."
    - "farmos-lineage.js walks parent refs through harvest and seeding logs and emits an ancestry array (bag -> harvest_batch -> block -> sterilization_batch for the pilot)."
    - "farmos-pilot-reconstruct.js emits a sorted-by-timestamp event timeline derived from farmOS logs only (no Signal refs in output)."
    - "Unit tests use Jest with a mocked farmOS client (no live HTTP); coverage for the five stage transitions (pre-inoc / colonizing / fruiting / spent / contaminated)."
    - "No em-dashes anywhere in tools/ source or tests."
    - "Output uses fmtNum() semantics for any numeric in operator-facing lines."
  artifacts:
    - path: "tools/farmos-current-stage.js"
      provides: "C1 stage derivation CLI; --at scoping; JSON output."
    - path: "tools/farmos-lineage.js"
      provides: "Ancestry-walk CLI; emits parent chain via harvest+seeding logs."
    - path: "tools/farmos-pilot-reconstruct.js"
      provides: "Timeline CLI; reconstructs lifecycle from farmOS logs alone."
    - path: "tools/test/"
      provides: "Jest unit tests with mocked farmOS responses; runs via `npx jest --config tools/test/jest.config.js`."
---

<objective>
Wave 0 query-only tools. Build three Node.js CLIs in `tools/` for pilot verification (C1 current-stage, ancestry walk, timeline reconstruct). Each tool reuses Phase 40's farmOS client. Unit-tested with mocked HTTP responses covering all five C1 stages. Operator runs these during the actual pilot to verify each PILOT-NN criterion against farmOS read-back.
</objective>

<tasks>
<task id="1">
<title>farmos-current-stage.js + tests</title>
<acceptance>
- CLI accepts `<asset_uuid>` positional + optional `--at <iso>`.
- Emits JSON: `{ asset, at, stage, evidence }`.
- Stage derivation matches RESEARCH section 1: contam > archive_spent > cold_shock > seeding > pre-inoc.
- Jest test covers all five stage transitions with mocked log payloads.
</acceptance>
</task>

<task id="2">
<title>farmos-lineage.js + tests</title>
<acceptance>
- CLI accepts `<asset_uuid>` positional.
- Emits JSON `{ chain: [{ uuid, name, type }, ...] }`, parent-first order.
- Walks via harvest (output -> source) and seeding (block -> batch) logs.
- Jest test covers the pilot lineage (bag -> harvest_batch -> block -> sterilization_batch) plus a leaf-only case (asset with no parent log).
</acceptance>
</task>

<task id="3">
<title>farmos-pilot-reconstruct.js + tests</title>
<acceptance>
- CLI accepts `<block_uuid>` positional.
- Fetches all logs referencing the block + lineage descendants (bag assets, harvest batches).
- Emits sorted-by-timestamp lines: `[<iso>] <log_type> <name?> <refs>`.
- No Signal refs in output.
- Jest test verifies sort order + line format against a fixed mocked log set.
</acceptance>
</task>
</tasks>

<verification>
- `npx jest --config tools/test/jest.config.js` green from repo root.
- `node tools/farmos-current-stage.js --help` exits 0 with usage text.
- `grep -- '--' tools/ | grep -v '^Binary'` returns no em-dash hits (only `--` double-hyphen flags allowed).
</verification>
