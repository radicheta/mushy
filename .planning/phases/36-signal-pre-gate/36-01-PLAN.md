---
phase: 36-signal-pre-gate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/36-signal-pre-gate/36-01-preflight-snapshot.md
  - .planning/phases/36-signal-pre-gate/snapshots/.gitkeep
autonomous: true
requirements: [PRE-01]
must_haves:
  truths:
    - "A dated tarball of the signal-cli-data Docker volume exists on elder-plops, readable, and contains the account state file"
    - "Current device list JSON (showing deviceId=2 linked-secondary) is captured to disk before any re-registration occurs"
    - "Current identity DB JSON is captured to disk before any re-registration occurs"
    - "Phase 35 Tier A backup is either confirmed to include the signal-cli account state file, or a gap is filed"
    - "The abort/restore-from-tarball recipe is written down and reviewable"
  artifacts:
    - path: ".planning/phases/36-signal-pre-gate/36-01-preflight-snapshot.md"
      provides: "Snapshot recipe + captured state JSON references + Phase 35 coverage verdict + restore recipe"
    - path: "/opt/mushy-backups/signal-cli-data-YYYYMMDD.tar.gz"
      provides: "Local fast-restore tarball on elder-plops (host path, captured outside repo)"
  key_links:
    - from: ".planning/phases/36-signal-pre-gate/36-01-preflight-snapshot.md"
      to: "/opt/mushy-backups/signal-cli-data-*.tar.gz"
      via: "documented path + tar -tzf verification command"
      pattern: "signal-cli-data-.*\\.tar\\.gz"
---

<objective>
Capture an exhaustively-restorable pre-flight snapshot of the current signal-cli state on elder-plops BEFORE any re-registration. This is the abort path for Plan 02. Without this, a failed SMS verification means the bot is dead with no rollback.

Purpose: D-01 + D-02 + D-03 + D-04 (CONTEXT.md). Snapshot the signal-cli-data volume to a local tarball, capture device/identity JSON for diffing later, verify Phase 35 Tier A coverage, document the restore recipe.

Output: A pre-flight artifact at `.planning/phases/36-signal-pre-gate/36-01-preflight-snapshot.md` containing captured state references + restore recipe; a tarball on elder-plops host at `/opt/mushy-backups/signal-cli-data-YYYYMMDD.tar.gz`; device/identity JSON committed to `.planning/phases/36-signal-pre-gate/snapshots/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/36-signal-pre-gate/36-CONTEXT.md
@docker-compose.override.yml

Memory references (operator already has these; for executor situational awareness):
- `project_signal_cli_primary_reregister_path.md` — re-reg recipe
- `project_signal_cli_link_gotchas.md` — copy state out of /root before cleanup
- `project_phase35_tierA_backup.md` — Phase 35 backup coverage; decrypt via `age -d -i ~/.ssh/id_ed25519`

The signal-cli service is defined in `docker-compose.override.yml` lines 43-57:
- image: `bbernhard/signal-cli-rest-api:0.200-dev`
- volume: named `signal-cli-data` mounted at `/home/.local/share/signal-cli` inside the container
- host loopback port: `127.0.0.1:8085:8080`
- bot account E.164: read from `${SIGNAL_SENDER}` in `.env` at repo root

Phase 35 Tier A nightly tarball lives on the VPS (Hetzner); local elder-plops origin path is `/opt/mushy-backups/tierA/` (per `project_phase35_tierA_backup.md`). To verify coverage we need to decrypt one nightly and check the manifest.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Snapshot signal-cli-data volume to local tarball + capture device/identity JSON</name>
  <read_first>
    - docker-compose.override.yml (lines 43-57 — signal-cli service, volume name, port mapping)
    - .env at repo root (resolve $SIGNAL_SENDER to the literal E.164 number; redact in committed files)
    - .planning/phases/36-signal-pre-gate/36-CONTEXT.md D-01, D-02, D-05
  </read_first>
  <files>
    /opt/mushy-backups/signal-cli-data-YYYYMMDD.tar.gz (host path on elder-plops, NOT in repo)
    .planning/phases/36-signal-pre-gate/snapshots/devices-YYYYMMDD.json
    .planning/phases/36-signal-pre-gate/snapshots/identities-YYYYMMDD.json
    .planning/phases/36-signal-pre-gate/snapshots/.gitkeep
  </files>
  <action>
1. Create the snapshots dir: `mkdir -p .planning/phases/36-signal-pre-gate/snapshots && touch .planning/phases/36-signal-pre-gate/snapshots/.gitkeep`
2. Create the host backup dir if missing: `sudo mkdir -p /opt/mushy-backups && sudo chown $USER /opt/mushy-backups`
3. Resolve the bot number once: `BOT=$(grep -E '^SIGNAL_SENDER=' .env | cut -d= -f2)` — keep this in env only; do NOT write the literal number into committed files (use `+<BOT>` placeholder).
4. Confirm the volume name docker recognizes: `docker volume ls --filter name=signal-cli-data --format '{{.Name}}'` — should print one of `mushy_signal-cli-data` or `signal-cli-data`. Use whatever is returned.
5. Tarball the live volume contents WITHOUT stopping signal-cli (it's an HTTP REST shim over a file-backed account; readers don't write). Use a throwaway alpine container so we don't need host paths:
   ```
   DATE=$(date -u +%Y%m%d)
   VOL=$(docker volume ls --filter name=signal-cli-data --format '{{.Name}}' | head -1)
   docker run --rm -v ${VOL}:/src:ro -v /opt/mushy-backups:/dst alpine \
     tar -czf /dst/signal-cli-data-${DATE}.tar.gz -C /src .
   ```
6. Verify the tarball has content: `tar -tzf /opt/mushy-backups/signal-cli-data-${DATE}.tar.gz | head -20` — must list ≥1 file; the account JSON file (matching pattern `data/+*` per signal-cli layout) must be present. Capture this listing into the runbook in Task 2.
7. Capture device list as JSON:
   ```
   curl -sS "http://127.0.0.1:8085/v1/devices/${BOT}" -o /tmp/devices-raw.json
   jq '.' /tmp/devices-raw.json > .planning/phases/36-signal-pre-gate/snapshots/devices-${DATE}.json
   ```
   Expected today: array with one entry where `.id == 2` (linked secondary) — confirming the receive-400 root cause. If `.id == 1` already appears, ABORT — Phase 36 is already done.
8. Capture identity DB as JSON:
   ```
   curl -sS "http://127.0.0.1:8085/v1/identities/${BOT}" -o /tmp/identities-raw.json
   jq '.' /tmp/identities-raw.json > .planning/phases/36-signal-pre-gate/snapshots/identities-${DATE}.json
   ```
9. Redact the bot E.164 from committed JSON: `sed -i "s/${BOT}/+<BOT>/g" .planning/phases/36-signal-pre-gate/snapshots/devices-${DATE}.json .planning/phases/36-signal-pre-gate/snapshots/identities-${DATE}.json` (per memory feedback on per-repo email + general PII hygiene; farmer numbers may also appear in identities — sed -i a regex `s/+598[0-9]\{7,9\}/+<REDACTED>/g` over both files).
10. Verify redaction succeeded: `grep -E '\+[0-9]{8,}' .planning/phases/36-signal-pre-gate/snapshots/*.json` MUST return empty.
  </action>
  <verify>
    <automated>
test -f /opt/mushy-backups/signal-cli-data-$(date -u +%Y%m%d).tar.gz && \
tar -tzf /opt/mushy-backups/signal-cli-data-$(date -u +%Y%m%d).tar.gz | wc -l | awk '$1 < 1 {exit 1}' && \
jq -e '.[0].id' .planning/phases/36-signal-pre-gate/snapshots/devices-$(date -u +%Y%m%d).json > /dev/null && \
jq -e 'type' .planning/phases/36-signal-pre-gate/snapshots/identities-$(date -u +%Y%m%d).json > /dev/null && \
! grep -qE '\+[0-9]{8,}' .planning/phases/36-signal-pre-gate/snapshots/*.json
    </automated>
  </verify>
  <acceptance_criteria>
    - File `/opt/mushy-backups/signal-cli-data-YYYYMMDD.tar.gz` exists and `tar -tzf` lists ≥1 entry
    - File `.planning/phases/36-signal-pre-gate/snapshots/devices-YYYYMMDD.json` is valid JSON with at least one entry having `.id` field
    - File `.planning/phases/36-signal-pre-gate/snapshots/identities-YYYYMMDD.json` is valid JSON
    - `grep -E '\+[0-9]{8,}' .planning/phases/36-signal-pre-gate/snapshots/*.json` returns no matches (no leaked E.164 numbers in committed JSON)
  </acceptance_criteria>
  <done>
    Pre-reg tarball + redacted device/identity JSON captured. Rollback material exists on disk; nothing else has been touched.
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify Phase 35 Tier A coverage + write the pre-flight + restore recipe</name>
  <read_first>
    - Output of Task 1 (volume name, tarball path, redacted JSON files)
    - .planning/phases/36-signal-pre-gate/36-CONTEXT.md D-03, D-04, D-11
    - Memory `project_phase35_tierA_backup.md` (decrypt: `age -d -i ~/.ssh/id_ed25519`; nightly path conventions)
  </read_first>
  <files>
    .planning/phases/36-signal-pre-gate/36-01-preflight-snapshot.md
  </files>
  <action>
1. Find the most recent Phase 35 Tier A nightly tarball on elder-plops. Path convention (from memory): `/opt/mushy-backups/tierA/*.tar.gz.age` or similar. Run `ls -lat /opt/mushy-backups/tierA/ 2>/dev/null | head -5` to locate it. If the dir doesn't exist locally (only on VPS), document this in step 4 below as "remote-only" and mark D-03 coverage as INDIRECT.
2. If the nightly exists locally, decrypt to /tmp and inspect the manifest WITHOUT extracting everything:
   ```
   age -d -i ~/.ssh/id_ed25519 /opt/mushy-backups/tierA/<latest>.tar.gz.age | tar -tz | grep -i signal-cli > /tmp/p35-signal-coverage.txt
   ```
3. Inspect `/tmp/p35-signal-coverage.txt`:
   - If it lists ≥1 path under signal-cli volume → D-03 verdict = COVERED.
   - If empty → D-03 verdict = GAP. File a backlog item in `.planning/STATE.md` deferred items table titled `999.51 — Phase 35 Tier A missing signal-cli account state` (mention this in the runbook + note that Plan 36-01's local tarball is the ONLY rollback path until 999.51 lands).
4. Write `.planning/phases/36-signal-pre-gate/36-01-preflight-snapshot.md` with the following sections (use literal headings):
   - `## Snapshot Captured` — list the tarball path, byte size (`du -h`), volume name, capture timestamp UTC. List the two redacted JSON files (paths relative to repo root). State the current deviceId from `devices-*.json` (must be `2`).
   - `## Phase 35 Tier A Coverage Verdict` — COVERED | GAP. If GAP, link to filed `999.51`.
   - `## Restore Recipe (abort path)` — exact commands an operator runs if Plan 02 SMS verification fails or the new primary is broken:
     ```
     # 1. Stop alerter + signal-cli
     docker compose stop alerter signal-cli
     # 2. Wipe the live volume
     docker volume rm <VOL>   # name from snapshot above
     docker volume create <VOL>
     # 3. Restore from local tarball
     docker run --rm -v <VOL>:/dst -v /opt/mushy-backups:/src alpine \
       tar -xzf /src/signal-cli-data-YYYYMMDD.tar.gz -C /dst
     # 4. Bring services back
     docker compose up -d signal-cli alerter
     # 5. Verify
     curl -sS http://127.0.0.1:8085/v1/devices/+<BOT> | jq '.[0].id'  # should print 2
     ```
   - `## Pre-Flight Checklist for Plan 02` — explicit gates the operator/Claude must clear before running re-registration: (a) tarball verified; (b) device JSON snapshot shows deviceId=2; (c) farmer #1 coordinated for 30-60 min reachability window; (d) 4G router has SIM in slot and is reachable; (e) Phase 35 verdict reviewed.
5. Commit the artifact + snapshots dir (do NOT commit the tarball — it's in /opt/mushy-backups, outside repo by design).
  </action>
  <verify>
    <automated>
test -f .planning/phases/36-signal-pre-gate/36-01-preflight-snapshot.md && \
grep -q "## Snapshot Captured" .planning/phases/36-signal-pre-gate/36-01-preflight-snapshot.md && \
grep -q "## Phase 35 Tier A Coverage Verdict" .planning/phases/36-signal-pre-gate/36-01-preflight-snapshot.md && \
grep -q "## Restore Recipe" .planning/phases/36-signal-pre-gate/36-01-preflight-snapshot.md && \
grep -q "## Pre-Flight Checklist" .planning/phases/36-signal-pre-gate/36-01-preflight-snapshot.md
    </automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/36-signal-pre-gate/36-01-preflight-snapshot.md` exists
    - Contains all four required headings: Snapshot Captured, Phase 35 Tier A Coverage Verdict, Restore Recipe, Pre-Flight Checklist for Plan 02
    - Restore Recipe is a fenced code block containing the literal command `tar -xzf` and the live volume name from Task 1
    - Phase 35 verdict is either COVERED or GAP (with backlog link)
  </acceptance_criteria>
  <done>
    Pre-flight artifact ready. Plan 02 has a documented abort path. Operator can review and approve Plan 02 entry.
  </done>
</task>

</tasks>

<verification>
End-to-end check: With the snapshot tarball and JSON in hand, an operator could (in theory) re-create the current broken-but-functional signal-cli state in under 5 minutes from a wiped volume. The pre-flight checklist makes the Plan 02 entry conditions explicit.
</verification>

<success_criteria>
- Local tarball exists on elder-plops at `/opt/mushy-backups/signal-cli-data-YYYYMMDD.tar.gz`
- Redacted device + identity JSON snapshots committed under `.planning/phases/36-signal-pre-gate/snapshots/`
- Pre-flight artifact committed at `.planning/phases/36-signal-pre-gate/36-01-preflight-snapshot.md`
- Phase 35 Tier A coverage of signal-cli state is either VERIFIED or GAP-FILED as 999.51
</success_criteria>

<output>
After completion, create `.planning/phases/36-signal-pre-gate/36-01-SUMMARY.md` per the standard summary template — include the deviceId observed in the pre-reg snapshot (proves the receive-400 root cause), the tarball byte size, and the Phase 35 coverage verdict.
</output>
