---
phase: 36-signal-pre-gate
plan: 02
type: execute
wave: 2
depends_on: ["36-01"]
files_modified:
  - .planning/phases/36-signal-pre-gate/36-RUNBOOK.md
autonomous: false
requirements: [PRE-01]
user_setup:
  - service: signal-cli
    why: "Primary re-registration requires receiving an SMS captcha on a SIM card and entering the 6-digit verification code"
    env_vars:
      - name: SIGNAL_SENDER
        source: ".env at repo root (already set; the bot E.164)"
    dashboard_config:
      - task: "Coordinate with farmer #1 (Android, on-site) for a 30-60min reachability window"
        location: "Out-of-band Signal/phone contact before starting"
      - task: "Confirm 4G router SIM is in slot and reachable; the SIM phone number = the bot E.164"
        location: "Physical 4G router (B310s-518) at on-site farm or wherever the SIM lives"
must_haves:
  truths:
    - "An operator can follow 36-RUNBOOK.md end-to-end without external clarification and complete primary re-registration"
    - "The runbook documents the pre-flight checklist, SMS captcha flow, abort path, and post-reg kickoff message verbatim"
    - "After Plan 02 execution, `curl /v1/devices/+<BOT>` returns an entry with `.id == 1`"
    - "The kickoff message (D-07) has been sent to farmer #1 and farmer #2 verbatim from the runbook"
  artifacts:
    - path: ".planning/phases/36-signal-pre-gate/36-RUNBOOK.md"
      provides: "End-to-end pre-flight + re-registration + post-reg recipe; the load-bearing artifact of Phase 36"
      min_lines: 120
      contains: "## Pre-Flight Checklist"
  key_links:
    - from: ".planning/phases/36-signal-pre-gate/36-RUNBOOK.md"
      to: "/v1/register/ and /v1/verify/ endpoints on signal-cli-rest-api"
      via: "documented curl invocations with literal endpoints"
      pattern: "curl.*v1/(register|verify)"
    - from: ".planning/phases/36-signal-pre-gate/36-RUNBOOK.md"
      to: "Plan 36-01 restore recipe"
      via: "cross-link to 36-01-preflight-snapshot.md"
      pattern: "36-01-preflight-snapshot"
---

<objective>
Author the load-bearing artifact for Phase 36: `36-RUNBOOK.md`. Then execute the runbook live to perform the primary re-registration (PRE-01).

Per CONTEXT.md D-09: primary re-registration is a one-time interactive event with an SMS captcha. The runbook IS the artifact; the script ride-along is in Plan 36-03.

Purpose: D-05, D-07, D-09, D-11 (CONTEXT.md). Produce `36-RUNBOOK.md` covering pre-flight, SMS captcha flow, post-reg kickoff message, verification queries, abort path, "what to tell the farmer" script. Execute it to flip deviceId from 2 → 1.

Output: `.planning/phases/36-signal-pre-gate/36-RUNBOOK.md` + a re-registered signal-cli instance returning deviceId=1.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/36-signal-pre-gate/36-CONTEXT.md
@.planning/phases/36-signal-pre-gate/36-01-preflight-snapshot.md
@docker-compose.override.yml

Load-bearing operator memories (verbatim recipes; planner has already vetted them — runbook should distill, not re-research):
- `/home/santi/.claude/projects/-mnt-slime-kingdom-opt-mushy/memory/project_signal_cli_primary_reregister_path.md` — the spike-validated recipe
- `/home/santi/.claude/projects/-mnt-slime-kingdom-opt-mushy/memory/project_phase25_pregate_spike_state.md` — 2026-04-27 spike PASS
- `/home/santi/.claude/projects/-mnt-slime-kingdom-opt-mushy/memory/project_signal_cli_link_gotchas.md` — copy state out of /root before cleanup
- `/home/santi/.claude/projects/-mnt-slime-kingdom-opt-mushy/memory/feedback_bridge_signal_cli_network_path.md` — bridge=127.0.0.1:8085, alerter=signal-cli:8080

Endpoint surface (from docker-compose.override.yml + bbernhard/signal-cli-rest-api):
- `POST /v1/register/+<BOT>?captcha=<token>` — kicks off SMS verification (captcha token obtainable from signalcaptchas.org/registration/generate.html)
- `POST /v1/register/+<BOT>/verify/<6-digit-code>` — completes verification
- `GET /v1/devices/+<BOT>` — list devices (post-reg: must show `.id == 1`)
- `GET /v1/identities/+<BOT>` — identity DB

Phase 25 SPEC (`.planning/milestones/v1.4-phases/25-bidirectional-signal-farmer-robot-capture-channel/25-SPEC.md`) is the predecessor — its Pre-Gate section documents the original linked-device limitation this phase resolves.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author 36-RUNBOOK.md end-to-end</name>
  <read_first>
    - .planning/phases/36-signal-pre-gate/36-CONTEXT.md (full file; all 14 decisions)
    - .planning/phases/36-signal-pre-gate/36-01-preflight-snapshot.md (cross-link target)
    - /home/santi/.claude/projects/-mnt-slime-kingdom-opt-mushy/memory/project_signal_cli_primary_reregister_path.md
    - /home/santi/.claude/projects/-mnt-slime-kingdom-opt-mushy/memory/project_phase25_pregate_spike_state.md
    - /home/santi/.claude/projects/-mnt-slime-kingdom-opt-mushy/memory/project_signal_cli_link_gotchas.md
  </read_first>
  <files>.planning/phases/36-signal-pre-gate/36-RUNBOOK.md</files>
  <action>
Write a complete runbook with the following EXACT top-level sections (preserve order):

1. `# Phase 36 Runbook — signal-cli Primary Re-Registration`
2. `## 0. Pre-Flight Checklist` — bullet list mirroring Plan 36-01's pre-flight checklist; first bullet links to `36-01-preflight-snapshot.md`. Last bullet: "farmer #1 and farmer #2 both confirmed reachable for the next 30-60 min."
3. `## 1. Capture Captcha Token` — direct operator to https://signalcaptchas.org/registration/generate.html in a browser; explain how to extract the `signalcaptcha://` link and strip the prefix to get the raw token. Note the token is single-use and ~10-min TTL.
4. `## 2. Trigger SMS Registration` — exact curl with placeholder substitution:
   ```
   curl -X POST "http://127.0.0.1:8085/v1/register/+<BOT>?captcha=<TOKEN>"
   ```
   Expected response: HTTP 201 with empty body OR 200; document the failure modes (HTTP 400 = bad captcha; HTTP 429 = rate-limited, wait 60 min). Include retry guidance: re-acquire captcha and retry up to 3 times before invoking the abort path (§7).
5. `## 3. Receive + Enter SMS Code` — the SIM phone (4G router B310s-518 or whatever holds the bot's number) receives a 6-digit SMS. Operator extracts it; then:
   ```
   curl -X POST "http://127.0.0.1:8085/v1/register/+<BOT>/verify/<CODE>"
   ```
   Expected: HTTP 201 empty body. Failure modes: 400 = wrong code; 410 = code expired (restart from §1).
6. `## 4. Verify Primary Status` — three checks, all must pass before proceeding:
   - `curl -sS http://127.0.0.1:8085/v1/devices/+<BOT> | jq '.[] | select(.id==1)'` → non-empty
   - `curl -sS http://127.0.0.1:8085/v1/receive/+<BOT>` → HTTP 200 (not 400). Empty array is fine; the 400→200 flip is the PRE-01 success signal.
   - `curl -sS http://127.0.0.1:8085/v1/identities/+<BOT> | jq '.'` → returns identity list; capture into `.planning/phases/36-signal-pre-gate/snapshots/identities-postreg-YYYYMMDD.json` for diff against pre-reg.
7. `## 5. Post-Reg Kickoff Message to Farmers` — verbatim message text in a fenced code block, ready to copy/paste through whatever channel the operator sends it. Draft per D-07 tone:
   ```
   Hey — the mushy bot just got re-registered on a new device. Your Signal app
   will show a "Safety number changed" warning the next time we DM. Tap it,
   tap "Verify safety number" (you don't have to actually verify — just dismiss
   the warning), then reply 'ok' here so I know the round-trip works.

   This re-reg also unblocks me being able to read your replies (was broken
   since Phase 25). Sorry it took a few weeks.
   ```
   Include the literal `curl` command to send this through signal-cli to each farmer (one per farmer — explicit, not a loop, so the operator can pause between them):
   ```
   curl -X POST http://127.0.0.1:8085/v2/send \
     -H "Content-Type: application/json" \
     -d '{"number":"+<BOT>","recipients":["+<FARMER1>"],"message":"<verbatim text above>"}'
   ```
8. `## 6. What to Tell the Farmer (when the safety-number warning appears)` — short script the operator can read aloud or paste to the farmer. ~3 lines. Per D-06 do NOT use auto-trust here — farmers re-accept manually.
9. `## 7. Abort Path` — cross-link to `36-01-preflight-snapshot.md § Restore Recipe`. Trigger conditions: (a) §2 returns 400/429 after 3 retries, (b) §3 code expired after 2 retries, (c) §4 verification still shows deviceId=2 after 60s. Document that after abort, the old linked-secondary device resumes; receive remains broken (the pre-Phase-36 state). Per D-05 the old linked device is NOT removed during Phase 36 — keep it as warm-rollback.
10. `## 8. Post-Reg Hand-Off to Plan 36-04` — short paragraph: now hand off to Plan 36-04 for the round-trip verification. Plan 36-03's trust-check script is a parallel deliverable that does not gate this section.
11. `## 9. Known Pitfalls` — distilled from memories:
    - Captcha token is single-use and ~10min TTL (recapture if §2 takes too long).
    - Old `README.md` in `src/agents/alerter/` contains stale password `Shiitake1!` — IGNORE per memory `project_phase25_pregate_spike_state` (caused 108006 errors in prior attempts).
    - Bridge talks to signal-cli on `127.0.0.1:8085`; alerter talks to it on compose-net `signal-cli:8080`. Do NOT unify. (memory `feedback_bridge_signal_cli_network_path`)
    - When using docker exec to inspect state, copy state OUT of `/root` before any container cleanup (memory `project_signal_cli_link_gotchas`).

Use `+<BOT>` placeholder everywhere; do NOT commit the literal E.164. Use `+<FARMER1>` / `+<FARMER2>`. The actual numbers come from `.env` at runtime — never from this committed runbook.

Minimum line count: ≥120 lines.
  </action>
  <verify>
    <automated>
test -f .planning/phases/36-signal-pre-gate/36-RUNBOOK.md && \
[ $(wc -l < .planning/phases/36-signal-pre-gate/36-RUNBOOK.md) -ge 120 ] && \
grep -q "## 0. Pre-Flight Checklist" .planning/phases/36-signal-pre-gate/36-RUNBOOK.md && \
grep -q "## 2. Trigger SMS Registration" .planning/phases/36-signal-pre-gate/36-RUNBOOK.md && \
grep -q "## 4. Verify Primary Status" .planning/phases/36-signal-pre-gate/36-RUNBOOK.md && \
grep -q "## 5. Post-Reg Kickoff Message" .planning/phases/36-signal-pre-gate/36-RUNBOOK.md && \
grep -q "## 7. Abort Path" .planning/phases/36-signal-pre-gate/36-RUNBOOK.md && \
grep -q "127.0.0.1:8085/v1/register" .planning/phases/36-signal-pre-gate/36-RUNBOOK.md && \
grep -q "select(.id==1)" .planning/phases/36-signal-pre-gate/36-RUNBOOK.md && \
! grep -qE '\+598[0-9]{7,9}' .planning/phases/36-signal-pre-gate/36-RUNBOOK.md
    </automated>
  </verify>
  <acceptance_criteria>
    - File exists, ≥120 lines
    - All 9 required top-level sections present with literal headings
    - Contains literal endpoint `127.0.0.1:8085/v1/register` and `select(.id==1)` jq verification
    - No literal E.164 numbers committed (regex `\+598[0-9]{7,9}` returns no matches; uses `+<BOT>`/`+<FARMER1>` placeholders)
    - Cross-links to `36-01-preflight-snapshot.md`
  </acceptance_criteria>
  <done>
    Runbook complete. Operator can pick it up cold and execute. Plan 36-04 has the kickoff message to reference.
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Execute the runbook live (primary re-registration)</name>
  <what-built>
    Task 1 produced `36-RUNBOOK.md`. This checkpoint executes §1–§4 of that runbook end-to-end. Claude cannot do this autonomously because §1 requires solving a CAPTCHA in a browser and §3 requires reading a 6-digit SMS from a physical SIM card.
  </what-built>
  <how-to-verify>
    1. Open `.planning/phases/36-signal-pre-gate/36-RUNBOOK.md` and confirm §0 Pre-Flight Checklist is fully green.
    2. Execute §1: acquire captcha token from https://signalcaptchas.org/registration/generate.html — paste it into chat.
    3. Claude will run §2 curl with the token; operator confirms HTTP 201.
    4. Operator reads SMS from the SIM-bearing 4G router (B310s-518) and pastes the 6-digit code.
    5. Claude runs §3 verify curl; operator confirms HTTP 201.
    6. Claude runs §4 verification queries; expected `deviceId=1` and `/v1/receive` returns 200 (not 400).
    7. Capture post-reg identity JSON to `.planning/phases/36-signal-pre-gate/snapshots/identities-postreg-YYYYMMDD.json`.
  </how-to-verify>
  <resume-signal>Operator pastes captcha token; later pastes SMS code. After §4 verification passes, type "primary live" to resume. If any step fails after retry budget, type "abort" to invoke §7 (Plan 36-01 restore recipe).</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Send the post-reg kickoff message to farmers #1 and #2</name>
  <read_first>
    - 36-RUNBOOK.md §5 (the verbatim kickoff text + curl)
    - .env at repo root (resolve $SIGNAL_SENDER + per-farmer recipient numbers — these come from existing alerter config / operator)
  </read_first>
  <files>(no repo file changes — this task makes outbound network calls only)</files>
  <action>
1. Confirm pre-conditions: post-reg deviceId=1 verified in Task 2; identity-postreg JSON captured.
2. Resolve farmer #1 E.164 (Android, on-site) and farmer #2 E.164 (zoy, Android, dev-side) — operator provides these at runtime or they're already in alerter config; do NOT commit them to repo. If they aren't readily available, ASK the operator before sending.
3. Send the kickoff message to farmer #1 using the curl from §5 with `+<FARMER1>` substituted:
   ```
   curl -X POST http://127.0.0.1:8085/v2/send \
     -H "Content-Type: application/json" \
     -d '{"number":"'"$BOT"'","recipients":["'"$FARMER1"'"],"message":"<verbatim text from §5>"}'
   ```
   Expected response: HTTP 201 with `{"timestamp": ...}`.
4. WAIT 2-3 minutes (per D-06 the operator may want to give farmer #1 time to see the safety-number warning before the second send goes out — but both farmers need it, so don't wait long).
5. Send the same message to farmer #2 with `+<FARMER2>`.
6. Record both send timestamps to `.planning/phases/36-signal-pre-gate/snapshots/kickoff-sends-YYYYMMDD.json` as `{"farmer1": {"ts": ..., "msg_ts": ...}, "farmer2": {"ts": ..., "msg_ts": ...}}` — Plan 36-04 needs these to correlate replies.
7. Do NOT auto-trust safety numbers (D-06) — farmers will re-accept manually on their phones.
  </action>
  <verify>
    <automated>
test -f .planning/phases/36-signal-pre-gate/snapshots/kickoff-sends-$(date -u +%Y%m%d).json && \
jq -e '.farmer1.msg_ts and .farmer2.msg_ts' .planning/phases/36-signal-pre-gate/snapshots/kickoff-sends-$(date -u +%Y%m%d).json > /dev/null
    </automated>
  </verify>
  <acceptance_criteria>
    - `kickoff-sends-YYYYMMDD.json` exists with both `farmer1.msg_ts` and `farmer2.msg_ts` populated (proves both sends completed with a Signal-assigned message timestamp)
    - No literal E.164 numbers in the committed file (use `farmer1` / `farmer2` keys; redact `msg_ts` is a Signal timestamp not a phone number)
  </acceptance_criteria>
  <done>
    Both farmers have received the kickoff message. PRE-01 done. PRE-02 round-trip is now in the farmers' court (Plan 36-04 collects the "ok" replies).
  </done>
</task>

</tasks>

<verification>
PRE-01 closed when `/v1/devices/+<BOT>` shows `.id == 1` and `/v1/receive` returns HTTP 200. PRE-02 verification begins after this plan ships (Plan 36-04 collects replies).
</verification>

<success_criteria>
- `36-RUNBOOK.md` ≥120 lines with all 9 sections
- Live signal-cli `/v1/devices/+<BOT>` returns `.id == 1`
- Live signal-cli `/v1/receive/+<BOT>` returns HTTP 200 (PRE-01 success signal)
- Post-reg identity JSON captured for diff against pre-reg snapshot
- Kickoff message sent to farmer #1 and farmer #2 (both timestamps recorded)
</success_criteria>

<output>
After completion, create `.planning/phases/36-signal-pre-gate/36-02-SUMMARY.md` per the standard summary template. Capture: pre-reg deviceId observed, post-reg deviceId observed, identity-fingerprint pre vs post (proves the safety-number change), both kickoff-send timestamps, any retries needed (captcha re-acquisitions, SMS resends).
</output>
