---
phase: 44-event-gate-durable-signal-outbound-tenant-aware
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - .gitignore
  - src/agents/alerter/package.json
  - src/agents/alerter/package-lock.json
  - tenants/.keep
  - src/agents/alerter/test/event-gate/.keep
  - src/agents/alerter/test/event-gate/rules.test.js
  - src/agents/alerter/test/event-gate/haiku-classifier.test.js
  - src/agents/alerter/test/event-gate/integration.test.js
  - src/agents/alerter/test/event-gate/smoke.test.js
  - src/agents/alerter/test/event-gate/haiku-live.test.js
  - src/agents/alerter/test/outbound-db.test.js
  - src/agents/alerter/test/llm-client.outbound-merge.test.js
autonomous: false
requirements: [GATE-01, GATE-02, OUTBOUND-01, OUTBOUND-02, TENANT-01]
must_haves:
  truths:
    - "tenants/*/secrets.env is gitignored before any tenant file is created (RESEARCH Pitfall 7)"
    - "yaml@^2.x is declared in src/agents/alerter/package.json and installable"
    - "All Wave 0 test stub files exist with failing/pending placeholders so downstream plans have automated <verify> targets"
  artifacts:
    - path: .gitignore
      provides: tenants/*/secrets.env exclusion
      contains: "tenants/*/secrets.env"
    - path: src/agents/alerter/package.json
      provides: yaml dep
      contains: '"yaml"'
    - path: tenants/.keep
      provides: tenants/ directory exists in repo
    - path: src/agents/alerter/test/event-gate/rules.test.js
      provides: stub for Plan-04 rules tests
    - path: src/agents/alerter/test/event-gate/haiku-classifier.test.js
      provides: stub for Plan-04 mocked-SDK test
    - path: src/agents/alerter/test/event-gate/integration.test.js
      provides: stub for capture-pipeline-with-gate
    - path: src/agents/alerter/test/event-gate/smoke.test.js
      provides: stub that reads 44-hand-classified-100.jsonl in Plan-04
    - path: src/agents/alerter/test/event-gate/haiku-live.test.js
      provides: EVAL_RUN_LIVE-gated live-API stub
    - path: src/agents/alerter/test/outbound-db.test.js
      provides: stub for Plan-02 outbound-db tests
    - path: src/agents/alerter/test/llm-client.outbound-merge.test.js
      provides: stub for Plan-05 fmtHistory merge tests
  key_links:
    - from: .gitignore
      to: tenants/mossrock/secrets.env
      via: pattern exclusion
      pattern: "tenants/\\*/secrets.env"
    - from: src/agents/alerter/package.json
      to: yaml runtime require
      via: dependencies block
      pattern: '"yaml"'
---

<objective>
Wave 0 scaffolding for Phase 44. Land the three things every downstream plan
depends on BEFORE any plan touches tenant files, calls Haiku, or wires the
outbound DAO: (a) `.gitignore` exclusion for tenant secrets, (b) the new `yaml`
npm dep, (c) test stubs for every Wave 0 gap enumerated in 44-VALIDATION.md.

Purpose: prevent accidental commit of `tenants/mossrock/secrets.env` per
RESEARCH Pitfall 7; give downstream plans real test-file targets for
`<automated>` verify blocks (Nyquist rule).

Output: gitignored tenants tree, `yaml` resolvable via `require('yaml')`, and
9 test stub files (8 new + 1 keep) that downstream plans fill in.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/44-event-gate-durable-signal-outbound-tenant-aware/44-CONTEXT.md
@.planning/phases/44-event-gate-durable-signal-outbound-tenant-aware/44-RESEARCH.md
@.planning/phases/44-event-gate-durable-signal-outbound-tenant-aware/44-VALIDATION.md
@.gitignore
@src/agents/alerter/package.json

<interfaces>
Existing `.gitignore` tail pattern (from current file) uses comment+glob lines.
Existing `src/agents/alerter/package.json` dependencies block already contains
`@anthropic-ai/sdk`, `pg`, `zod`, `ulid` — add `yaml` alongside, keep
alphabetical ordering if present.

node:test idiom for stub files (from `src/agents/alerter/test/rules.test.js`):
- `const test = require('node:test'); const assert = require('node:assert');`
- `test.skip('TODO Plan-NN', () => { });` for placeholder tests so `npm test`
  passes without false negatives.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 0.1: gitignore tenant secrets + create tenants/ skeleton</name>
  <files>.gitignore, tenants/.keep</files>
  <read_first>
    - .gitignore (full file — verify no existing `tenants/` rule)
    - .planning/phases/44-event-gate-durable-signal-outbound-tenant-aware/44-RESEARCH.md (Pitfall 7 section)
    - .planning/phases/44-event-gate-durable-signal-outbound-tenant-aware/44-CONTEXT.md (D-11 tenants layout)
  </read_first>
  <action>
    Append to repo-root `.gitignore` a new section header `# Phase 44: tenant secrets (per D-11)` followed by two glob lines `tenants/*/secrets.env` and `tenants/*/.env`. Create empty file `tenants/.keep` so the directory tracks in git (the YAML configs land in Plan-06; the keep file prevents `tenants/` from being a phantom directory between Wave 0 and Wave 2).
  </action>
  <acceptance_criteria>
    - source: `.gitignore` contains literal line `tenants/*/secrets.env`
    - source: `.gitignore` contains literal line `tenants/*/.env`
    - behavior: `git check-ignore tenants/mossrock/secrets.env` exits 0
    - behavior: `tenants/.keep` is tracked by git (`git ls-files tenants/.keep` returns the path)
  </acceptance_criteria>
  <verify>
    <automated>git check-ignore tenants/mossrock/secrets.env &amp;&amp; git check-ignore tenants/example/secrets.env</automated>
  </verify>
  <done>git check-ignore returns the two paths; `.gitignore` has the new section; `tenants/.keep` exists.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 0.2: Verify yaml@^2.x package legitimacy (RESEARCH ASSUMED gate)</name>
  <what-built>
    No code yet. Operator verifies the `yaml` npm package on npmjs.com/package/yaml is the eemeli/yaml package (not a typosquat) before Plan-00 runs `npm install`.
  </what-built>
  <how-to-verify>
    1. Open https://www.npmjs.com/package/yaml
    2. Confirm repository link is github.com/eemeli/yaml
    3. Confirm latest version is 2.x (any 2.x release acceptable per RESEARCH §Standard Stack)
    4. Confirm weekly downloads are in the millions (anti-typosquat smoke test)
    5. Reply "approved" or paste the suspicious finding.
  </how-to-verify>
  <resume-signal>Type "approved" to proceed to Task 0.3 (npm install yaml)</resume-signal>
</task>

<task type="auto">
  <name>Task 0.3: Install yaml dep in alerter package</name>
  <files>src/agents/alerter/package.json, src/agents/alerter/package-lock.json</files>
  <read_first>
    - src/agents/alerter/package.json (full file)
    - .planning/phases/44-event-gate-durable-signal-outbound-tenant-aware/44-RESEARCH.md (§Standard Stack + §Don't Hand-Roll)
  </read_first>
  <action>
    From repo root run `cd src/agents/alerter &amp;&amp; npm install yaml` (no version pin; npm picks the latest 2.x). Do not bump any other dep. Commit both `package.json` and `package-lock.json` together.
  </action>
  <acceptance_criteria>
    - source: `src/agents/alerter/package.json` dependencies object contains a `yaml` key whose value matches `^2.`
    - behavior: `cd src/agents/alerter &amp;&amp; node -e "require('yaml')"` exits 0
    - test: `cd src/agents/alerter &amp;&amp; node -e "const y=require('yaml'); console.log(typeof y.parse)"` prints `function`
  </acceptance_criteria>
  <verify>
    <automated>cd src/agents/alerter &amp;&amp; node -e "const y=require('yaml'); if(typeof y.parse!=='function') process.exit(1)"</automated>
  </verify>
  <done>yaml is requireable from the alerter package; package-lock updated.</done>
</task>

<task type="auto">
  <name>Task 0.4: Create 9 Wave 0 test stub files</name>
  <files>
    src/agents/alerter/test/event-gate/.keep,
    src/agents/alerter/test/event-gate/rules.test.js,
    src/agents/alerter/test/event-gate/haiku-classifier.test.js,
    src/agents/alerter/test/event-gate/integration.test.js,
    src/agents/alerter/test/event-gate/smoke.test.js,
    src/agents/alerter/test/event-gate/haiku-live.test.js,
    src/agents/alerter/test/outbound-db.test.js,
    src/agents/alerter/test/llm-client.outbound-merge.test.js
  </files>
  <read_first>
    - src/agents/alerter/test/rules.test.js (node:test style for stub file)
    - src/agents/alerter/test/capture-db.test.js (pool-mock pattern stub will reference in Plan-02)
    - src/agents/alerter/test/eval/ingestion/paperlog.test.js lines 1-30 (EVAL_RUN_LIVE gate pattern for haiku-live stub)
    - .planning/phases/44-event-gate-durable-signal-outbound-tenant-aware/44-VALIDATION.md (Wave 0 Requirements section)
  </read_first>
  <action>
    Create each stub file with a minimal node:test scaffold that uses `test.skip` (or `describe.skip` where applicable) so the placeholder does not break `npm test`. Each stub MUST: (a) require node:test + node:assert, (b) declare ONE skipped test whose name names the downstream plan that fills it in (e.g. `test.skip('Plan-04: rule positive image attachment', () =&gt; {})`). For `haiku-live.test.js` use the EVAL_RUN_LIVE pattern from the analog file so the stub already encodes the gate. For `smoke.test.js` reference the fixture path `.planning/phases/44-event-gate-durable-signal-outbound-tenant-aware/44-hand-classified-100.jsonl` in a top-of-file comment so Plan-01 + Plan-04 know the contract. Create `src/agents/alerter/test/event-gate/.keep` so the new test subdir tracks even before tests are real.
  </action>
  <acceptance_criteria>
    - source: all 8 .test.js files exist
    - source: each .test.js file contains at least one `test.skip(` or `describe.skip(` call referencing a Plan-NN identifier
    - source: `src/agents/alerter/test/event-gate/haiku-live.test.js` contains `process.env.EVAL_RUN_LIVE === '1'` (mirrors paperlog.test.js:14)
    - test: `cd src/agents/alerter &amp;&amp; npm test` exits 0 with the new skipped tests counted in the skip column
  </acceptance_criteria>
  <verify>
    <automated>cd src/agents/alerter &amp;&amp; npm test 2&gt;&amp;1 | tail -20 | grep -E "skip|skipped" &amp;&amp; grep -l "EVAL_RUN_LIVE" src/agents/alerter/test/event-gate/haiku-live.test.js</automated>
  </verify>
  <done>All 8 test stubs land; alerter test suite stays green with new skipped placeholders.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo → public git | committed files become world-readable; secrets at this boundary are catastrophic |
| npm registry → local node_modules | new dep `yaml` enters supply chain |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-44-00-01 | Information Disclosure | `tenants/mossrock/secrets.env` future-commit | mitigate | Task 0.1 lands `.gitignore` exclusion BEFORE any Plan-06 task creates the file (sequencing enforced by wave + depends_on graph) |
| T-44-00-SC | Tampering | npm install of `yaml` dep | mitigate | Task 0.2 blocking-human-verify checkpoint per RESEARCH Package Legitimacy Audit (ASSUMED status) |
| T-44-00-02 | Tampering | placeholder test stubs masquerading as real coverage | accept | Stubs use `test.skip` (not `test`) so the suite reports them as skipped, not passing — downstream plans MUST replace with real tests before the phase ships |
</threat_model>

<verification>
After all 4 tasks:

```bash
# Pitfall 7 closed
git check-ignore tenants/mossrock/secrets.env

# yaml resolvable
cd src/agents/alerter && node -e "require('yaml')"

# Stubs land and suite stays green
cd src/agents/alerter && npm test
```
</verification>

<success_criteria>
- .gitignore excludes `tenants/*/secrets.env` and `tenants/*/.env`
- `yaml` is in alerter package.json dependencies and requireable
- 8 Wave 0 test stubs exist; alerter `npm test` exits 0
- `tenants/.keep` and `src/agents/alerter/test/event-gate/.keep` track the new directories
</success_criteria>

<output>
Create `.planning/phases/44-event-gate-durable-signal-outbound-tenant-aware/44-00-SUMMARY.md` when done.
</output>
