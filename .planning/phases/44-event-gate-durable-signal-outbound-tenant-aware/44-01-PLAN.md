---
phase: 44-event-gate-durable-signal-outbound-tenant-aware
plan: 01
type: execute
wave: 1
depends_on: [44-00]
files_modified:
  - .planning/phases/44-event-gate-durable-signal-outbound-tenant-aware/44-01-pull-corpus.sql
  - .planning/phases/44-event-gate-durable-signal-outbound-tenant-aware/44-01-raw-corpus.jsonl
  - .planning/phases/44-event-gate-durable-signal-outbound-tenant-aware/44-hand-classified-100.jsonl
  - .planning/phases/44-event-gate-durable-signal-outbound-tenant-aware/44-01-CLASSIFICATION-RUBRIC.md
autonomous: false
requirements: [GATE-01, GATE-02]
must_haves:
  truths:
    - "100 signal_capture rows from elder-plops Timescale (captured_at >= 2026-05-10) are sampled and stratified per D-20 distribution"
    - "Each of the 100 rows is hand-classified by Don Santiago with expected_gate_action ∈ {skip, extract} and a class tag"
    - "Hand-classification distribution matches D-20: 36 hard-event / 28 confirm / 8 phantom-ack / 8 UX-meta / 12 soft-obs / 8 greetings"
    - "Smoke fixture is APPEND-ONLY JSONL per [[feedback_keep_paper_trail_of_intermediates]] (no overwrites)"
  artifacts:
    - path: .planning/phases/44-event-gate-durable-signal-outbound-tenant-aware/44-01-pull-corpus.sql
      provides: the exact SELECT used to pull from Timescale (reproducibility)
      contains: "FROM signal_capture WHERE captured_at"
    - path: .planning/phases/44-event-gate-durable-signal-outbound-tenant-aware/44-01-raw-corpus.jsonl
      provides: pre-hand-label dump (500 rows) for audit; never edited after Task 1.1
    - path: .planning/phases/44-event-gate-durable-signal-outbound-tenant-aware/44-hand-classified-100.jsonl
      provides: ship-gate fixture consumed by Plan-04 smoke harness
      contains: "tenant_id"
    - path: .planning/phases/44-event-gate-durable-signal-outbound-tenant-aware/44-01-CLASSIFICATION-RUBRIC.md
      provides: definitions of the 6 D-20 class tags so the hand-labeler has a fixed vocabulary
  key_links:
    - from: .planning/phases/44-event-gate-durable-signal-outbound-tenant-aware/44-hand-classified-100.jsonl
      to: src/agents/alerter/test/event-gate/smoke.test.js
      via: fs.readFileSync(JSONL) → assert D-22 metrics
      pattern: "44-hand-classified-100.jsonl"
---

<objective>
Ship-gate corpus. Pull 500 live captures from elder-plops Timescale
`signal_capture`, dump as JSONL (audit trail), then hand the file to Don
Santiago for the 100-row hand-classification per D-20/D-21/D-22/D-23. This MUST
ship BEFORE Plan-04 (Haiku live-fire) per `[[feedback_smoke_before_expensive_batch]]`.

Purpose: every D-22 ship-gate metric (0 chit-chat pings on 24 must-skip rows,
≥95% recall on 48 must-extract rows, 28 confirm rows bypass via Phase 39
short-circuit) is computed against THIS file. No fixture, no ship.

Output: the canonical `44-hand-classified-100.jsonl` consumed verbatim by
Plan-04's smoke harness.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/44-event-gate-durable-signal-outbound-tenant-aware/44-CONTEXT.md
@.planning/phases/44-event-gate-durable-signal-outbound-tenant-aware/44-RESEARCH.md
@.planning/notes/2026-05-17-prod-corpus-survey.md
@.planning/notes/2026-05-17-is-this-an-event-gate.md

<interfaces>
elder-plops Timescale connection — same instance used by alerter today.
Database: `mushy` (per existing `config.js`/`signal_capture` writes).
Table: `signal_capture` columns include `id, captured_at, sender, raw_text, transcript, message_type, attachment_count`.

JSONL row shape for `44-hand-classified-100.jsonl` (per D-21):
  {"tenant_id":"mossrock","capture_id":"<uuid>","sender":"<e164>","captured_at":"<iso>","raw_text":"...","transcript":"...","attachment_count":N,"class":"<one of 6 tags>","expected_gate_action":"<skip|extract>","notes":"..."}

D-20 stratification distribution (sum=100):
  36 hard-event | 28 confirm | 8 phantom-ack | 8 UX-meta | 12 soft-obs | 8 greetings
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1.1: Pull 500 captures from Timescale + dump raw JSONL + write rubric</name>
  <files>
    .planning/phases/44-event-gate-durable-signal-outbound-tenant-aware/44-01-pull-corpus.sql,
    .planning/phases/44-event-gate-durable-signal-outbound-tenant-aware/44-01-raw-corpus.jsonl,
    .planning/phases/44-event-gate-durable-signal-outbound-tenant-aware/44-01-CLASSIFICATION-RUBRIC.md
  </files>
  <read_first>
    - .planning/notes/2026-05-17-prod-corpus-survey.md (§5 sourcing recipe — the exact distribution and seed-corpus rules)
    - .planning/notes/2026-05-17-is-this-an-event-gate.md (§3 sample distribution — gives the 6 class definitions)
    - .planning/phases/44-event-gate-durable-signal-outbound-tenant-aware/44-CONTEXT.md (D-20 + D-21 + D-22)
    - src/agents/alerter/src/capture-db.js (signal_capture column list for the SELECT)
  </read_first>
  <action>
    Write the SELECT into `44-01-pull-corpus.sql` verbatim: `SELECT id, captured_at, sender, raw_text, transcript, message_type, attachment_count FROM signal_capture WHERE captured_at &gt;= '2026-05-10' ORDER BY captured_at DESC LIMIT 500;`. Run it against elder-plops Timescale (use the alerter docker network or `psql` via the bridge host per `[[project_openmct_dashboard]]`). Pipe results to `44-01-raw-corpus.jsonl` as one JSON object per line (psql `\copy ... to ... csv header` then converter, OR `psql -At -c "SELECT row_to_json(t) FROM (&lt;query&gt;) t"`). Then write `44-01-CLASSIFICATION-RUBRIC.md` defining the 6 D-20 class tags with one-paragraph definitions each: `hard-event`, `confirm`, `phantom-ack`, `UX-meta`, `soft-obs`, `greetings`. For each class document `expected_gate_action` (skip vs extract) and which rule fast-path or Haiku branch is expected to handle it.
  </action>
  <acceptance_criteria>
    - source: `44-01-pull-corpus.sql` exists and contains the literal string `FROM signal_capture WHERE captured_at`
    - behavior: `wc -l 44-01-raw-corpus.jsonl` returns between 400 and 500 (allow slack if recent backfill thin)
    - test: each line of `44-01-raw-corpus.jsonl` parses as JSON (`jq -c . &lt; 44-01-raw-corpus.jsonl &gt; /dev/null`)
    - source: rubric file defines all 6 class tags and binds each to {skip,extract}
  </acceptance_criteria>
  <verify>
    <automated>jq -c . &lt; .planning/phases/44-event-gate-durable-signal-outbound-tenant-aware/44-01-raw-corpus.jsonl &gt; /dev/null &amp;&amp; test $(wc -l &lt; .planning/phases/44-event-gate-durable-signal-outbound-tenant-aware/44-01-raw-corpus.jsonl) -ge 400 &amp;&amp; grep -q "hard-event" .planning/phases/44-event-gate-durable-signal-outbound-tenant-aware/44-01-CLASSIFICATION-RUBRIC.md</automated>
  </verify>
  <done>SQL file committed; 400-500 raw rows dumped; rubric covers 6 classes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1.2: Don Santiago hand-classifies 100 rows into the stratified fixture</name>
  <what-built>
    The raw 500-row dump from Task 1.1 exists. Don Santiago now reads the rubric and selects + tags exactly 100 rows into `44-hand-classified-100.jsonl` per the D-20 distribution (36/28/8/8/12/8). This is the ship-gate fixture. Per `[[feedback_real_data_before_ship_gate_pass]]` this cannot be automated.
  </what-built>
  <how-to-verify>
    1. Read `.planning/phases/44-event-gate-durable-signal-outbound-tenant-aware/44-01-CLASSIFICATION-RUBRIC.md` end-to-end.
    2. Open `44-01-raw-corpus.jsonl` (one row per JSON object).
    3. Append-only build `44-hand-classified-100.jsonl` one row at a time. For each chosen capture, emit a JSONL line with the shape:
       `{"tenant_id":"mossrock","capture_id":"&lt;id from raw&gt;","sender":"&lt;sender&gt;","captured_at":"&lt;ts&gt;","raw_text":"...","transcript":"...","attachment_count":N,"class":"&lt;hard-event|confirm|phantom-ack|UX-meta|soft-obs|greetings&gt;","expected_gate_action":"&lt;skip|extract&gt;","notes":"&lt;why&gt;"}`
    4. Hold the distribution to D-20 exactly: 36 hard-event, 28 confirm, 8 phantom-ack, 8 UX-meta, 12 soft-obs, 8 greetings.
    5. Include the 3 frozen-corpus seed captures named in `2026-05-17-prod-corpus-survey.md` if they are present in the raw dump.
    6. When done, reply "100 classified" with the JSONL line count or paste any disambiguation questions.
  </how-to-verify>
  <resume-signal>Type "100 classified" once `44-hand-classified-100.jsonl` has exactly 100 lines and the distribution matches D-20.</resume-signal>
</task>

<task type="auto">
  <name>Task 1.3: Validate distribution of the hand-classified fixture</name>
  <files>.planning/phases/44-event-gate-durable-signal-outbound-tenant-aware/44-hand-classified-100.jsonl</files>
  <read_first>
    - .planning/phases/44-event-gate-durable-signal-outbound-tenant-aware/44-hand-classified-100.jsonl (the file the operator just wrote)
    - .planning/phases/44-event-gate-durable-signal-outbound-tenant-aware/44-CONTEXT.md (D-20 distribution to assert against)
  </read_first>
  <action>
    Write a one-shot validation that asserts: (a) exactly 100 lines, (b) every line parses as JSON, (c) every line has `tenant_id=="mossrock"`, (d) class distribution matches D-20 exactly (36/28/8/8/12/8). On failure, print which class is off and by how many; do NOT auto-fix — kick back to Task 1.2 with the diff. Implement via a jq one-liner; do not create a script file (transient validation).
  </action>
  <acceptance_criteria>
    - behavior: line count is exactly 100
    - behavior: jq aggregate by class returns {hard-event:36, confirm:28, phantom-ack:8, UX-meta:8, soft-obs:12, greetings:8}
    - behavior: jq filter for `.tenant_id != "mossrock"` returns nothing
  </acceptance_criteria>
  <verify>
    <automated>F=.planning/phases/44-event-gate-durable-signal-outbound-tenant-aware/44-hand-classified-100.jsonl; test $(wc -l &lt; "$F") -eq 100 &amp;&amp; jq -s 'group_by(.class) | map({(.[0].class): length}) | add' "$F" | tee /dev/stderr | grep -q '"hard-event": 36' &amp;&amp; jq -c 'select(.tenant_id != "mossrock")' "$F" | (! grep -q .)</automated>
  </verify>
  <done>100 rows, exact D-20 distribution, every row tagged `tenant_id=mossrock`. Fixture is locked.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| live Timescale → committed fixture | farmer-PII raw_text/transcript flows into the repo |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-44-01-01 | Information Disclosure | farmer PII in `raw_text`/`transcript` committed to git | accept | Per `[[feedback_real_data_before_ship_gate_pass]]` — real data is necessary; repo is private; operator reviews each row during Task 1.2 |
| T-44-01-02 | Tampering | hand-classification bias (operator labels a chit-chat as event to inflate recall) | mitigate | Rubric (Task 1.1) defines the 6 class tags BEFORE labeling; Task 1.3 asserts the D-20 distribution exactly |
| T-44-01-03 | Repudiation | fixture mutated silently between Plan-01 and Plan-04 | mitigate | JSONL is append-only per `[[feedback_keep_paper_trail_of_intermediates]]`; raw dump kept as separate immutable file |
</threat_model>

<verification>
```bash
# After Task 1.3
F=.planning/phases/44-event-gate-durable-signal-outbound-tenant-aware/44-hand-classified-100.jsonl
wc -l "$F"  # → 100
jq -s 'group_by(.class) | map({(.[0].class): length}) | add' "$F"  # → D-20 distribution
```
</verification>

<success_criteria>
- `44-01-raw-corpus.jsonl` (≥400 rows) + `44-01-pull-corpus.sql` + `44-01-CLASSIFICATION-RUBRIC.md` committed
- `44-hand-classified-100.jsonl` has exactly 100 rows in the D-20 distribution
- Every row tagged `tenant_id: "mossrock"`
- Fixture is ready for Plan-04 smoke harness consumption
</success_criteria>

<output>
Create `.planning/phases/44-event-gate-durable-signal-outbound-tenant-aware/44-01-SUMMARY.md` when done.
</output>
