---
phase: 44-event-gate-durable-signal-outbound-tenant-aware
plan: 05
type: execute
wave: 3
depends_on: [44-02]
files_modified:
  - src/agents/alerter/src/capture-history.js
  - src/agents/alerter/src/llm-client.js
  - src/agents/alerter/src/capture.js
  - src/agents/alerter/test/capture-history.test.js
  - src/agents/alerter/test/llm-client.test.js
  - src/agents/alerter/test/llm-client.outbound-merge.test.js
autonomous: true
requirements: [OUTBOUND-02]
must_haves:
  truths:
    - "capture-history.js exposes selectRecentOutboundByRecipient(recipient, sinceMs) returning {sent_at, body, intent} rows from signal_outbound"
    - "fmtHistory merges inbound (signal_capture) + outbound (signal_outbound) streams sorted by timestamp ascending"
    - "Outbound lines are truncated at 400 chars; inbound lines remain at 200 (D-18)"
    - "buildUserBlock exposes lastBotOutbound as a distinct prompt field (D-19) so the LLM can reference 'the last thing you said'"
    - "fmtHistory NO LONGER reads signal_capture.llm_reply (D-17 — the band-aid is superseded; column stays populated for audit but is invisible to the LLM)"
    - "capture.js passes outboundHistory + lastBotOutbound into llmClient.compose()"
  artifacts:
    - path: src/agents/alerter/src/capture-history.js
      provides: selectRecentOutboundByRecipient
      exports: ["selectRecentOutboundByRecipient"]
    - path: src/agents/alerter/src/llm-client.js
      provides: fmtHistory(history, outboundHistory) + buildUserBlock with lastBotOutbound
      contains: "lastBotOutbound"
  key_links:
    - from: src/agents/alerter/src/llm-client.js
      to: src/agents/alerter/src/capture-history.js
      via: compose() calls both selectRecentBySender + selectRecentOutboundByRecipient
      pattern: "selectRecentOutboundByRecipient"
    - from: src/agents/alerter/src/capture.js
      to: src/agents/alerter/src/llm-client.js
      via: compose({history, outboundHistory, lastBotOutbound, ...})
      pattern: "outboundHistory"
---

<objective>
OUTBOUND-02 + finding 1b proper closure: `fmtHistory` reads the new
`signal_outbound` table (via a new sibling query in `capture-history.js`),
merges with the existing inbound stream sorted by time, and `buildUserBlock`
surfaces `lastBotOutbound` as a distinct prompt field for the LLM and for the
NEGATIVE rule fast-path in Plan-04.

This plan supersedes the v1.7.x band-aid (read `signal_capture.llm_reply`) per
D-17. The `llm_reply` column UPDATE at `capture.js:206` remains — it is now
audit-only, no longer consumed by the prompt.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/44-event-gate-durable-signal-outbound-tenant-aware/44-CONTEXT.md
@.planning/phases/44-event-gate-durable-signal-outbound-tenant-aware/44-RESEARCH.md
@.planning/phases/44-event-gate-durable-signal-outbound-tenant-aware/44-PATTERNS.md
@.planning/phases/44-event-gate-durable-signal-outbound-tenant-aware/44-02-SUMMARY.md
@.planning/notes/2026-05-17-llm-outbound-amnesia.md
@src/agents/alerter/src/capture-history.js
@src/agents/alerter/src/llm-client.js
@src/agents/alerter/src/capture.js

<interfaces>
Existing `capture-history.js` exports `createCaptureHistory({pool})` returning `{selectRecentBySender(sender, sinceMs)}` — extend with `selectRecentOutboundByRecipient(recipient, sinceMs)`.

Existing `llm-client.js`:
  - `fmtHistory(history)` at :33-40 truncates body at 200 chars per line, uses MAX_HISTORY_ROWS=20.
  - `buildUserBlock({history, sensorSnapshot, currentMessage})` at :49-62 composes the user-content prompt.
  - `compose({...})` at the SDK call site assembles messages array.

D-18 extension:
  - `fmtHistory(history, outboundHistory = [])` merges both streams.
  - Per-stream truncation: outbound = 400 chars, inbound = 200 chars.
  - Sort by timestamp ASC; slice(-MAX_HISTORY_ROWS) keeps newest 20 after sort.
  - Tag outbound with `type: 'bot:&lt;intent&gt;'`.

D-19 extension:
  - `buildUserBlock({history, outboundHistory, lastBotOutbound, sensorSnapshot, currentMessage})`.
  - Render a `## Last thing you said to the farmer` block before history.
  - lastBotOutbound shape: `{sent_at, body, intent}` (matches selectRecentOutboundByRecipient row).
  - If null, render `(none)`.

capture.js needs:
  - In the convo branch (after the gate, before compose), call `captureHistory.selectRecentOutboundByRecipient(replyTarget, capturedAtMs - 24*60*60*1000)` to fetch the 24h outbound window.
  - Pass `outboundHistory` array + `lastBotOutbound` (last element or null) into `llmClient.compose({...})`.
  - lastBot fetched at the gate (Plan-04 Task 4.3) may be reused — share the value to avoid double-query.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 5.1: Add selectRecentOutboundByRecipient to capture-history.js</name>
  <files>
    src/agents/alerter/src/capture-history.js,
    src/agents/alerter/test/capture-history.test.js
  </files>
  <read_first>
    - src/agents/alerter/src/capture-history.js (existing selectRecentBySender to mirror)
    - src/agents/alerter/test/capture-history.test.js (existing test patterns)
    - .planning/phases/44-event-gate-durable-signal-outbound-tenant-aware/44-PATTERNS.md (§capture-history.js section)
    - .planning/phases/44-event-gate-durable-signal-outbound-tenant-aware/44-CONTEXT.md (D-18)
  </read_first>
  <behavior>
    - Test 1: factory returns object containing `selectRecentOutboundByRecipient` function
    - Test 2: selectRecentOutboundByRecipient(pool-stub, '+59891840205', nowMs - 86400000) issues SELECT with `recipient_e164 = $1 AND sent_at > $2 ORDER BY sent_at ASC`
    - Test 3: returns array of `{sent_at, body, intent}` shaped rows
    - Test 4: existing selectRecentBySender tests still pass (regression guard)
  </behavior>
  <action>
    Mirror the existing `selectRecentBySender` shape verbatim per PATTERNS.md §capture-history.js. Add `selectRecentOutboundByRecipient(recipient, sinceMs)` to the returned object. SELECT columns: `sent_at, body, intent` (D-18 — these are the only fields fmtHistory consumes). Extend `test/capture-history.test.js` with the 4 behaviors above using a mocked `pool.query` jest.fn that returns rows.
  </action>
  <acceptance_criteria>
    - source: `capture-history.js` factory returns object with `selectRecentOutboundByRecipient` key
    - source: SELECT statement contains `FROM signal_outbound` + `recipient_e164 = $1` + `sent_at > $2` + `ORDER BY sent_at ASC`
    - test: `cd src/agents/alerter &amp;&amp; npm test -- capture-history` exits 0 with new + existing tests passing
  </acceptance_criteria>
  <verify>
    <automated>cd src/agents/alerter &amp;&amp; grep -q "selectRecentOutboundByRecipient" src/capture-history.js &amp;&amp; grep -q "FROM signal_outbound" src/capture-history.js &amp;&amp; npm test -- capture-history 2&gt;&amp;1 | tail -10</automated>
  </verify>
  <done>capture-history.js has the new query; tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 5.2: Extend fmtHistory to merge streams + extend buildUserBlock with lastBotOutbound</name>
  <files>
    src/agents/alerter/src/llm-client.js,
    src/agents/alerter/test/llm-client.outbound-merge.test.js,
    src/agents/alerter/test/llm-client.test.js
  </files>
  <read_first>
    - src/agents/alerter/src/llm-client.js (full file — fmtHistory at :33-40, buildUserBlock at :49-62, compose elsewhere)
    - .planning/phases/44-event-gate-durable-signal-outbound-tenant-aware/44-PATTERNS.md (§llm-client.js MODIFIED section — exact merge shape)
    - .planning/phases/44-event-gate-durable-signal-outbound-tenant-aware/44-CONTEXT.md (D-17, D-18, D-19)
    - src/agents/alerter/test/llm-client.test.js (existing tests — extend, do not replace)
  </read_first>
  <behavior>
    - Test 1 (fmtHistory empty): fmtHistory([], []) returns `'  (none)'`.
    - Test 2 (merge ordering): inbound row at t=100, outbound row at t=200, inbound row at t=300 → output lines in ASC chronological order across streams.
    - Test 3 (inbound truncation): inbound body of 500 chars truncated to 200; outbound body of 500 chars truncated to 400.
    - Test 4 (outbound type tag): outbound row with intent='convo_reply' renders as `bot:convo_reply` in the line prefix.
    - Test 5 (slice tail): 30 inbound + 10 outbound (40 total) → output has exactly MAX_HISTORY_ROWS (20) lines after sort.
    - Test 6 (lastBotOutbound in buildUserBlock): buildUserBlock with lastBotOutbound={sent_at, body:'hello', intent:'convo_reply'} contains the literal section header `## Last thing you said to the farmer` and the body 'hello'.
    - Test 7 (no lastBotOutbound): buildUserBlock with lastBotOutbound=null renders `(none)` in that section.
    - Test 8 (no llm_reply read): fmtHistory called with rows that have an `llm_reply` field does NOT include `llm_reply` in the output (D-17 — column is invisible to prompt).
  </behavior>
  <action>
    Refactor `fmtHistory` per PATTERNS.md §llm-client.js MODIFIED: accept `(history, outboundHistory = [])`, tag each row with `{ts, kind, body, type, cap}` (inbound cap=200, outbound cap=400), concat both streams, sort by `new Date(ts)`, slice(-MAX_HISTORY_ROWS), format. Outbound type prefix uses `bot:&lt;intent&gt;`. Inbound preserves `r.message_type`. Refactor `buildUserBlock` to accept `{history, outboundHistory, lastBotOutbound, sensorSnapshot, currentMessage}` and render the new `## Last thing you said to the farmer` block per PATTERNS.md §buildUserBlock. Update `compose()` to thread outboundHistory + lastBotOutbound through (signature gains those keys; capture.js Plan-05 Task 5.3 supplies them). Replace Plan-00 skip stubs in `test/llm-client.outbound-merge.test.js` with the 5 fmtHistory behaviors (Tests 1-5). Add the 3 buildUserBlock behaviors (Tests 6-8) to `test/llm-client.test.js`. Existing llm-client.test.js coverage MUST continue to pass — buildUserBlock signature change is additive (default outboundHistory=[], default lastBotOutbound=null).
  </action>
  <acceptance_criteria>
    - source: `fmtHistory` function signature is `fmtHistory(history, outboundHistory = [])`
    - source: outbound rows in output line use `bot:` type prefix
    - source: `buildUserBlock` accepts `lastBotOutbound` parameter
    - source: `llm-client.js` does NOT reference `llm_reply` anywhere (grep returns 0)
    - test: `cd src/agents/alerter &amp;&amp; npm test -- llm-client.outbound-merge` exits 0 with 5 behaviors passing
    - test: `cd src/agents/alerter &amp;&amp; npm test -- llm-client.test` exits 0 with new + existing passing
  </acceptance_criteria>
  <verify>
    <automated>cd src/agents/alerter &amp;&amp; grep -q "outboundHistory" src/llm-client.js &amp;&amp; grep -q "lastBotOutbound" src/llm-client.js &amp;&amp; test $(grep -v '^[[:space:]]*//' src/llm-client.js | grep -c "llm_reply") -eq 0 &amp;&amp; npm test -- llm-client 2&gt;&amp;1 | tail -10</automated>
  </verify>
  <done>fmtHistory merges; buildUserBlock surfaces lastBotOutbound; llm_reply no longer read; tests green.</done>
</task>

<task type="auto">
  <name>Task 5.3: Wire outboundHistory + lastBotOutbound from capture.js into llmClient.compose</name>
  <files>
    src/agents/alerter/src/capture.js,
    src/agents/alerter/test/capture.test.js
  </files>
  <read_first>
    - src/agents/alerter/src/capture.js around the gate insertion (Plan-04 Task 4.3 already added `lastBot` fetch at :147)
    - src/agents/alerter/src/capture.js around :171 (llmClient.compose call site)
    - src/agents/alerter/test/capture.test.js (extend existing patterns)
    - .planning/phases/44-event-gate-durable-signal-outbound-tenant-aware/44-PATTERNS.md (§capture.js MODIFIED — convo gate block)
  </read_first>
  <action>
    In the convo branch in `capture.js` (the `gateDecision.allow_convo || config.eventGateConvoMode === 'off'` block from Plan-04 Task 4.3): immediately before `llmClient.compose(...)`, fetch `outboundHistory = await captureHistory.selectRecentOutboundByRecipient(replyTarget, capturedAtMs - 24*60*60*1000).catch(() =&gt; [])`. Pass `outboundHistory` and `lastBotOutbound: lastBot` (the variable computed for the gate at :147 — reuse, do not requery) into the compose call. Extend `test/capture.test.js` with one assertion that compose() is invoked with both `outboundHistory` and `lastBotOutbound` keys populated when the gate allows convo.
  </action>
  <acceptance_criteria>
    - source: `capture.js` contains `selectRecentOutboundByRecipient(` call inside the convo branch
    - source: `capture.js` calls `llmClient.compose(` with `outboundHistory` and `lastBotOutbound` in the args object
    - source: `lastBot` variable from gate is reused (no second call to selectRecentOutboundByRecipient with the same args)
    - test: `cd src/agents/alerter &amp;&amp; npm test -- capture.test` exits 0
    - test: full suite green
  </acceptance_criteria>
  <verify>
    <automated>cd src/agents/alerter &amp;&amp; grep -q "selectRecentOutboundByRecipient" src/capture.js &amp;&amp; grep -q "outboundHistory" src/capture.js &amp;&amp; grep -q "lastBotOutbound" src/capture.js &amp;&amp; npm test -- capture 2&gt;&amp;1 | tail -5</automated>
  </verify>
  <done>capture.js threads both streams + lastBotOutbound into compose(); test asserts the wiring.</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-44-05-01 | Information Disclosure | Outbound body in LLM prompt includes farmer PII echoed back | accept | Equivalent to existing inbound posture; outbound bodies are bot-authored and already passed through Anthropic |
| T-44-05-02 | Tampering | Wrong-recipient outbound rows leak into another farmer's prompt | mitigate | selectRecentOutboundByRecipient filters by exact `recipient_e164` match including the `group:` prefix encoding from Plan-02 |
| T-44-05-03 | Repudiation | fmtHistory silently drops llm_reply data (D-17 transition) | accept | Per D-17 the column stays populated as an audit trail; the LLM no longer reads it. v2.0 drops the column. |
</threat_model>

<verification>
```bash
cd src/agents/alerter && npm test
# Live verify after deploy + Plan-03 wired:
psql -h elder-plops -d mushy -c "SELECT recipient_e164, intent, sent_at, LEFT(body, 60) FROM signal_outbound WHERE sent_at > now() - interval '2 hours' ORDER BY sent_at DESC LIMIT 20;"
# Smoke: send a farmer DM, observe the LLM next-turn prompt log includes the bot's previous reply
```
</verification>

<success_criteria>
- capture-history.js exposes `selectRecentOutboundByRecipient`
- fmtHistory merges inbound + outbound by timestamp with per-stream truncation (200/400)
- buildUserBlock exposes `lastBotOutbound` distinct prompt field per D-19
- llm-client.js no longer references `llm_reply` (D-17 supersedes the band-aid)
- capture.js threads both streams + lastBotOutbound through compose
- Full suite green
</success_criteria>

<output>
Create `.planning/phases/44-event-gate-durable-signal-outbound-tenant-aware/44-05-SUMMARY.md` documenting the D-17 transition (llm_reply now invisible to LLM) and confirming the lastBot variable is shared between the gate (Plan-04) and the compose wiring (this plan) for query efficiency.
</output>
