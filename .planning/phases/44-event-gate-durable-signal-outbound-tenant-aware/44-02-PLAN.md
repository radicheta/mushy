---
phase: 44-event-gate-durable-signal-outbound-tenant-aware
plan: 02
type: execute
wave: 2
depends_on: [44-00]
files_modified:
  - src/agents/alerter/src/outbound-db.js
  - src/agents/alerter/src/signal.js
  - src/agents/alerter/src/index.js
  - src/agents/alerter/test/outbound-db.test.js
  - src/agents/alerter/test/signal.test.js
autonomous: false
requirements: [OUTBOUND-01, TENANT-01]
must_haves:
  truths:
    - "signal_outbound table is created idempotently with the D-12 schema (uuid PK via gen_random_uuid + tenant_id NOT NULL + 3 indexes)"
    - "Every successful signalClient.send writes exactly one signal_outbound row via the single wrapper hook in signal.js (D-14)"
    - "Send still succeeds when the outbound insert fails (fail-open per D-03 posture; warn-only log)"
    - "Group sends encode the group id into recipient_e164 as 'group:&lt;id&gt;' (per RESEARCH open Q1 recommendation)"
  artifacts:
    - path: src/agents/alerter/src/outbound-db.js
      provides: initDb + insertOutbound + selectRecentByRecipient
      exports: ["initDb", "insertOutbound", "selectRecentByRecipient"]
    - path: src/agents/alerter/src/signal.js
      provides: send wrapper extended with intent + persistence hook
      contains: "outboundDb.insertOutbound"
    - path: src/agents/alerter/src/index.js
      provides: initDb call for signal_outbound at boot
      contains: "outboundDb.initDb"
  key_links:
    - from: src/agents/alerter/src/signal.js
      to: src/agents/alerter/src/outbound-db.js
      via: post-send try/catch insertOutbound call
      pattern: "outboundDb\\.insertOutbound"
    - from: src/agents/alerter/src/index.js
      to: src/agents/alerter/src/outbound-db.js
      via: boot-time DDL
      pattern: "outboundDb\\.initDb"
---

<objective>
OUTBOUND-01: ship the durable `signal_outbound` table + DAO + single
persistence hook inside `signal.js`. This is the foundation Plan-03 (14-site
intent rollout) and Plan-05 (fmtHistory merge) build on.

Per D-14: single hook in `signal.js` post-send — no fan-out to 14 callsites.
Per D-12: exact DDL with `tenant_id text NOT NULL` + 3 indexes from day one.
Per D-03: outbound insert failures are warn-only; send return value unchanged.

This plan does NOT yet require callers to pass `intent`. Plan-03 wires the
intents. During the window between this plan and Plan-03, rows land with
`intent='unknown'` — documented and acceptable per RESEARCH Open Q3.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/44-event-gate-durable-signal-outbound-tenant-aware/44-CONTEXT.md
@.planning/phases/44-event-gate-durable-signal-outbound-tenant-aware/44-RESEARCH.md
@.planning/phases/44-event-gate-durable-signal-outbound-tenant-aware/44-PATTERNS.md
@src/agents/alerter/src/capture-db.js
@src/agents/alerter/src/signal.js
@src/agents/alerter/src/index.js
@src/agents/alerter/test/capture-db.test.js
@src/agents/alerter/test/signal.test.js

<interfaces>
Existing `capture-db.js` exports: `initDb(pool)`, `insertCapture(pool, row)`, and uses parameterized queries via `pool.query(sql, params)`. ALTER pattern at capture-db.js:32 uses `ALTER TABLE ... ADD COLUMN IF NOT EXISTS`. The DDL idempotence shape is `CREATE TABLE IF NOT EXISTS` + `CREATE INDEX IF NOT EXISTS`.

Existing `signal.js:58` signature is:
  `async function send(body, { bypassCap = false, to } = {})`

Existing `signal.js` rate-cap pattern uses `sendHistory.push(now)` after a successful fetch — this is the insertion anchor for the persistence hook.

Existing `index.js` boot sequence calls `captureDb.initDb(pool)` early; new `outboundDb.initDb(pool)` call lives right next to it.

D-12 schema (verbatim — DO NOT modify column names, types, or index list):
  CREATE TABLE IF NOT EXISTS signal_outbound (
    id uuid PRIMARY KEY DEFAULT gen_random_uuid(),
    tenant_id text NOT NULL,
    sent_at timestamptz NOT NULL DEFAULT now(),
    recipient_e164 text NOT NULL,
    intent text NOT NULL,
    body text NOT NULL,
    attachments jsonb,
    source_module text NOT NULL,
    source_line integer,
    related_capture_id uuid,
    related_draft_id uuid
  );
  CREATE INDEX IF NOT EXISTS idx_signal_outbound_tenant_sent ON signal_outbound(tenant_id, sent_at DESC);
  CREATE INDEX IF NOT EXISTS idx_signal_outbound_recipient_sent ON signal_outbound(recipient_e164, sent_at DESC);
  CREATE INDEX IF NOT EXISTS idx_signal_outbound_intent ON signal_outbound(intent);

`gen_random_uuid()` requires the pgcrypto extension — see Task 2.1 checkpoint.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2.1: Confirm pgcrypto availability on elder-plops Timescale (RESEARCH Open Q A2)</name>
  <what-built>
    Nothing yet. Operator confirms whether pgcrypto is enabled on the elder-plops Timescale `mushy` database. Per RESEARCH Assumption A2 this is UNVERIFIED; if absent, the D-12 DDL will fail on first run.
  </what-built>
  <how-to-verify>
    1. SSH to elder-plops (or use existing alerter Docker network).
    2. Run: `psql -h localhost -U mushy -d mushy -c "SELECT extname FROM pg_extension WHERE extname='pgcrypto';"`
    3. Reply with one of:
       - "pgcrypto present" — Task 2.2 will NOT add a CREATE EXTENSION line
       - "pgcrypto absent, please add CREATE EXTENSION IF NOT EXISTS pgcrypto;" — Task 2.2 prepends it to initDb (idempotent; needs superuser only on first run)
       - "no superuser, use ulid client-side instead" — Task 2.2 deviates from D-12 (escalate to user before deviating)
  </how-to-verify>
  <resume-signal>Type one of "pgcrypto present" / "pgcrypto absent, please add CREATE EXTENSION" / paste the alternative decision</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 2.2: Implement outbound-db.js DAO (DDL + insertOutbound + selectRecentByRecipient)</name>
  <files>
    src/agents/alerter/src/outbound-db.js,
    src/agents/alerter/test/outbound-db.test.js
  </files>
  <read_first>
    - src/agents/alerter/src/capture-db.js (module shape, parameterized INSERT pattern, ALTER IF NOT EXISTS idiom)
    - src/agents/alerter/test/capture-db.test.js (pool-mock pattern verbatim — Jest jest.fn().mockResolvedValue({rows:[],rowCount:0}))
    - .planning/phases/44-event-gate-durable-signal-outbound-tenant-aware/44-PATTERNS.md (§outbound-db.js section — analog code blocks)
    - .planning/phases/44-event-gate-durable-signal-outbound-tenant-aware/44-CONTEXT.md (D-12 DDL verbatim)
    - src/agents/alerter/test/event-gate/.keep (confirm test dir exists from Plan-00)
  </read_first>
  <behavior>
    - Test 1: `initDb(pool)` issues exactly 4 queries (1 CREATE TABLE + 3 CREATE INDEX) — or 5 if pgcrypto checkpoint resolved to "absent" (1 CREATE EXTENSION first).
    - Test 2: `initDb` CREATE TABLE SQL contains `tenant_id text NOT NULL`, `recipient_e164 text NOT NULL`, `intent text NOT NULL`, `gen_random_uuid()`.
    - Test 3: `insertOutbound(pool, row)` issues one parameterized INSERT with positional placeholders $1..$10 matching the D-12 column order (tenant_id, sent_at, recipient_e164, intent, body, attachments, source_module, source_line, related_capture_id, related_draft_id).
    - Test 4: `insertOutbound` JSON.stringifies `attachments` when non-null and passes `null` when omitted.
    - Test 5: `insertOutbound` returns `{ok:true}` on success, `{ok:false, reason}` on pool.query rejection (Pattern S1 never-throw).
    - Test 6: `selectRecentByRecipient(pool, recipient, sinceMs)` issues SELECT with `WHERE recipient_e164 = $1 AND sent_at > $2 ORDER BY sent_at ASC`.
  </behavior>
  <action>
    Mirror `src/agents/alerter/src/capture-db.js` module shape: `'use strict';` + named functions + `module.exports = { initDb, insertOutbound, selectRecentByRecipient }`. Use the exact D-12 DDL verbatim (Task 2.1 may add a leading `CREATE EXTENSION IF NOT EXISTS pgcrypto;` query). DEVIATION from D-12: column types stay as D-12 (no project-style remap; `text` is already used). Mirror parameterized INSERT pattern from `capture-db.js:62-83`. `insertOutbound` returns `{ok, reason}` per Pattern S1 (never throws — the signal.js hook depends on this for fail-open). Replace the Plan-00 `test.skip` stubs in `test/outbound-db.test.js` with real tests covering all 6 behaviors above.
  </action>
  <acceptance_criteria>
    - source: `src/agents/alerter/src/outbound-db.js` exports `initDb`, `insertOutbound`, `selectRecentByRecipient`
    - source: file contains literal `CREATE TABLE IF NOT EXISTS signal_outbound`, `tenant_id text NOT NULL`, `gen_random_uuid()`, all 3 `CREATE INDEX IF NOT EXISTS idx_signal_outbound_*` lines
    - source: `insertOutbound` returns `{ok, reason}` not throw (grep for `return { ok` in catch block)
    - test: `cd src/agents/alerter &amp;&amp; npm test -- outbound-db` exits 0 with 6+ passing assertions
  </acceptance_criteria>
  <verify>
    <automated>cd src/agents/alerter &amp;&amp; npm test -- outbound-db 2&gt;&amp;1 | tail -10</automated>
  </verify>
  <done>outbound-db.js + outbound-db.test.js shipped; tests green; D-12 DDL preserved verbatim.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2.3: Wrap signal.js send with persistence hook + boot-time initDb in index.js</name>
  <files>
    src/agents/alerter/src/signal.js,
    src/agents/alerter/src/index.js,
    src/agents/alerter/test/signal.test.js
  </files>
  <read_first>
    - src/agents/alerter/src/signal.js (full file — locate `async function send` and `sendHistory.push(now)` anchor)
    - src/agents/alerter/src/index.js lines 1-100 (boot sequence — find captureDb.initDb call to mirror placement)
    - src/agents/alerter/test/signal.test.js (existing test patterns; extend, do not replace)
    - .planning/phases/44-event-gate-durable-signal-outbound-tenant-aware/44-PATTERNS.md (§signal.js section — signature extension + persistence hook code shape)
    - .planning/phases/44-event-gate-durable-signal-outbound-tenant-aware/44-RESEARCH.md (Pitfall 3 — back-compat shim for missing intent)
  </read_first>
  <behavior>
    - Test 1: send() called with all opts (intent, relatedCaptureId, relatedDraftId, sourceModule) triggers one outboundDb.insertOutbound call with matching fields after successful fetch.
    - Test 2: send() called WITHOUT intent logs a warn and writes `intent: 'unknown'` to outbound (back-compat shim per RESEARCH Pitfall 3).
    - Test 3: send() to a group target writes `recipient_e164: 'group:&lt;groupId&gt;'` (RESEARCH Open Q1 disposition).
    - Test 4: outboundDb.insertOutbound returning `{ok:false}` produces a logger.warn but does NOT throw and does NOT change send's return value (fail-open per D-03).
    - Test 5: send still passes existing rate-cap / bypassCap / target validation tests unchanged.
    - Test 6: `index.js` boot calls `outboundDb.initDb(pool)` after `captureDb.initDb(pool)`.
  </behavior>
  <action>
    Extend `signal.js` createSignalClient factory: (a) accept `outboundDb` and `tenantId` (or `config`) as constructor deps, (b) extend `send` opts to `{ bypassCap, to, intent, relatedCaptureId, relatedDraftId, sourceModule }`, (c) after the existing `sendHistory.push(now)` line insert a try/catch that calls `outboundDb.insertOutbound(pool, {...})` with: `tenant_id = config.tenantId || 'mossrock'`, `sent_at = new Date(now)`, `recipient_e164 = isStringTarget(target) ? target : ('group:' + (resolvedGroupId || target.groupId))`, `intent = intent || 'unknown'`, `body`, `source_module = sourceModule || null`, `source_line = null`, `related_capture_id = relatedCaptureId ?? null`, `related_draft_id = relatedDraftId ?? null`. If insert returns `{ok:false}`, logger.warn with the reason. NEVER let the insert path mutate the send return value. Add the `if (!intent)` warn-and-default-to-'unknown' shim per RESEARCH Pitfall 3. In `index.js`, require `outbound-db.js`, call `await outboundDb.initDb(pool)` adjacent to the existing `captureDb.initDb(pool)` call, and pass `outboundDb` + `config` (for `tenantId`) into `createSignalClient(...)`. Extend `test/signal.test.js` with the 6 behaviors above; the existing tests must still pass — pass a fake `outboundDb = {insertOutbound: jest.fn().mockResolvedValue({ok:true})}` into createSignalClient in the existing test setup.
  </action>
  <acceptance_criteria>
    - source: `signal.js` send signature contains `intent`, `relatedCaptureId`, `relatedDraftId`, `sourceModule` in the opts destructure
    - source: `signal.js` contains the literal string `outboundDb.insertOutbound`
    - source: `signal.js` contains `'group:'` prefix encoding for group targets (RESEARCH Q1)
    - source: `index.js` contains `outboundDb.initDb(pool)` call
    - test: `cd src/agents/alerter &amp;&amp; npm test -- signal` exits 0 with new behavior coverage
    - test: existing `cd src/agents/alerter &amp;&amp; npm test` full suite still green
  </acceptance_criteria>
  <verify>
    <automated>cd src/agents/alerter &amp;&amp; npm test -- signal 2&gt;&amp;1 | tail -10 &amp;&amp; cd src/agents/alerter &amp;&amp; npm test 2&gt;&amp;1 | tail -5</automated>
  </verify>
  <done>signal.js wraps send with the single persistence hook; index.js calls initDb at boot; full alerter suite green; warn-but-not-throw fail-open verified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| alerter process → Timescale | new write path; SQL injection risk if not parameterized |
| send() callers → wrapper | callers may omit intent during the Plan-02→Plan-03 window |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-44-02-01 | Tampering | SQL injection on `signal_outbound` INSERT | mitigate | All inserts parameterized via pg `$N` placeholders per `capture-db.js:62-83` pattern (Task 2.2 acceptance) |
| T-44-02-02 | Denial of Service | outbound DB write failures back-pressure the send path | mitigate | Fail-open per D-03: insertOutbound returns `{ok,reason}` (never throws); signal.js warn-but-continue (Task 2.3 behavior 4) |
| T-44-02-03 | Repudiation | rows land with `intent='unknown'` between Plan-02 ship and Plan-03 wire-up | accept | RESEARCH Open Q3 — documented; the 14-site rollout in Plan-03 closes within one wave |
| T-44-02-04 | Information Disclosure | message body persisted to a new column | accept | Same posture as existing `signal_capture.raw_text` / `llm_reply` — equivalent risk class |
</threat_model>

<verification>
```bash
cd src/agents/alerter && npm test                       # full suite green
cd src/agents/alerter && npm test -- outbound-db        # DAO unit tests
cd src/agents/alerter && npm test -- signal             # wrapper + hook tests
# Live verify (run after deploy):
psql -h elder-plops -d mushy -c "\\d signal_outbound"   # confirms DDL applied
```
</verification>

<success_criteria>
- `signal_outbound` table exists in Timescale with D-12 schema verbatim + 3 indexes
- signal.js single-hook persistence writes one row per successful send
- Send remains successful when outbound insert fails (warn-only)
- Group sends encode group id into recipient_e164 with `group:` prefix
- Full alerter test suite green
</success_criteria>

<output>
Create `.planning/phases/44-event-gate-durable-signal-outbound-tenant-aware/44-02-SUMMARY.md` with the intent-mapping table reserved for Plan-03 + the pgcrypto disposition from Task 2.1.
</output>
