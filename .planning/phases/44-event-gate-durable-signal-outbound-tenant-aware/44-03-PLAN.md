---
phase: 44-event-gate-durable-signal-outbound-tenant-aware
plan: 03
type: execute
wave: 3
depends_on: [44-02]
files_modified:
  - src/agents/alerter/src/receive-loop.js
  - src/agents/alerter/src/index.js
  - src/agents/alerter/src/capture.js
  - src/agents/alerter/src/confirm/outbound-confirm.js
  - src/agents/alerter/src/extraction/outbound.js
  - src/agents/alerter/test/integration.test.js
autonomous: true
requirements: [OUTBOUND-01]
must_haves:
  truths:
    - "All 14 signalClient.send call sites pass an explicit `intent` from the D-13 enum"
    - "Zero `intent='unknown'` rows land in signal_outbound after this plan ships"
    - "Each call site also passes `sourceModule` so the signal_outbound.source_module column is populated (RESEARCH Open Q2 disposition: option (a) — caller passes explicitly)"
  artifacts:
    - path: src/agents/alerter/src/receive-loop.js
      provides: 8 send sites updated with intent + sourceModule
    - path: src/agents/alerter/src/index.js
      provides: 3 send sites updated with intent + sourceModule
    - path: src/agents/alerter/src/capture.js
      provides: 1 send site at :197 updated with intent='convo_reply' + relatedCaptureId
    - path: src/agents/alerter/src/confirm/outbound-confirm.js
      provides: 1 send site updated with intent='confirm_prompt' + relatedDraftId
    - path: src/agents/alerter/src/extraction/outbound.js
      provides: 1 send site updated with intent='extraction_preview' + relatedCaptureId
  key_links:
    - from: src/agents/alerter/src/receive-loop.js
      to: src/agents/alerter/src/signal.js wrapper
      via: opts.intent argument
      pattern: "intent:\\s*['\"](experiment_reject|experiment_ack|experiment_cancel|experiment_complete|command_echo)"
    - from: src/agents/alerter/src/confirm/outbound-confirm.js
      to: src/agents/alerter/src/signal.js wrapper
      via: opts.intent + relatedDraftId
      pattern: "intent:\\s*['\"]confirm_prompt['\"]"
---

<objective>
Roll the `intent` argument across all 14 `signalClient.send` call sites
enumerated in `2026-05-17-signal-outbound-schema-audit.md §2`. After this plan,
the back-compat `intent='unknown'` shim from Plan-02 still exists but is never
triggered in production.

Each intent string comes from the D-13 enum:
`convo_reply | attestation_kickoff | commit_ack | ask_back | experiment_ack |
experiment_reject | experiment_cancel | experiment_complete | rh_alert |
command_echo | confirm_prompt | extraction_preview`. `commit_ack` is reserved
for Phase 45 and MUST NOT appear here.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/44-event-gate-durable-signal-outbound-tenant-aware/44-CONTEXT.md
@.planning/phases/44-event-gate-durable-signal-outbound-tenant-aware/44-RESEARCH.md
@.planning/phases/44-event-gate-durable-signal-outbound-tenant-aware/44-PATTERNS.md
@.planning/phases/44-event-gate-durable-signal-outbound-tenant-aware/44-02-SUMMARY.md
@.planning/notes/2026-05-17-signal-outbound-schema-audit.md
@src/agents/alerter/src/receive-loop.js
@src/agents/alerter/src/index.js
@src/agents/alerter/src/capture.js
@src/agents/alerter/src/confirm/outbound-confirm.js
@src/agents/alerter/src/extraction/outbound.js

<interfaces>
14 call sites (verified by `grep -n "signalClient.send" src/agents/alerter/src/` 2026-05-21 — line drift corrected from CONTEXT.md):

receive-loop.js
  :73   "experiment dispatch unavailable (bridge unreachable)"        → intent: 'experiment_reject'
  :85   experiment_ack body (multi-line)                               → intent: 'experiment_ack'
  :91   "experiment rejected: ${err}"                                  → intent: 'experiment_reject'
  :102  "experiment cancelled (ended_at=...)"                          → intent: 'experiment_cancel'
  :106  "cancel rejected: ${err}"                                      → intent: 'experiment_reject'
  :112  "experiment dispatch failed; check bridge logs"                → intent: 'experiment_reject'
  :189  exp.reply (experiment completion broadcast)                    → intent: 'experiment_complete'
  :211  parsed.reply (snooze command echo)                             → intent: 'command_echo'

index.js
  :180  action.body (rh_alert FIRING/RESOLVED via formatProblem)       → intent: 'rh_alert'
  :183  action.body (attestation_kickoff per Phase 46 D-09 + bypassCap)→ intent: 'attestation_kickoff'
  :185  action.body (alerter heartbeat / sensor messages)              → intent: 'rh_alert'

capture.js
  :197  replyText (convo compose reply)                                → intent: 'convo_reply', relatedCaptureId: id

confirm/outbound-confirm.js
  :33   body (confirm prompt — YES/NO/EDIT pre-text)                   → intent: 'confirm_prompt', relatedDraftId: draftId

extraction/outbound.js
  :55   body (extractor preview reply)                                 → intent: 'extraction_preview', relatedCaptureId: captureId

For index.js:180/183/185 the planner MUST re-grep the actual body shape to
distinguish rh_alert from attestation_kickoff — Phase 46 introduced the
fc1LastMsgTs alert and may have re-ordered. If ambiguity remains after grep,
follow this rule: bypassCap=true → attestation_kickoff; otherwise rh_alert.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 3.1: Wire intents on receive-loop.js (8 sites) + index.js (3 sites)</name>
  <files>
    src/agents/alerter/src/receive-loop.js,
    src/agents/alerter/src/index.js
  </files>
  <read_first>
    - src/agents/alerter/src/receive-loop.js (full file — 8 sites at :73, :85, :91, :102, :106, :112, :189, :211)
    - src/agents/alerter/src/index.js (around the action.body sends at :180-:185 — confirm bypassCap distinguishes attestation_kickoff)
    - .planning/phases/44-event-gate-durable-signal-outbound-tenant-aware/44-PATTERNS.md (§Send-site updates — additive edit shape)
    - .planning/phases/44-event-gate-durable-signal-outbound-tenant-aware/44-RESEARCH.md (Pitfall 2 — re-grep before editing; line drift)
  </read_first>
  <action>
    For each of the 8 receive-loop.js sites, extend the existing call from `signalClient.send(body)` to `signalClient.send(body, { intent: '&lt;mapped intent&gt;', sourceModule: 'receive-loop.js' })`. Preserve every existing `.catch(() =&gt; {})` and `{ bypassCap: true }` — additive only. For the 3 index.js sites at :180/:183/:185, distinguish rh_alert vs attestation_kickoff by the bypassCap flag in the existing call (bypassCap=true → attestation_kickoff per Phase 46). Pass `sourceModule: 'index.js'`. NEVER drop existing opts; merge them. The intent strings MUST be one of the D-13 enum values — do not invent new ones; if a site does not fit, leave it as-is and surface in the SUMMARY (no `commit_ack` — that's Phase 45).
  </action>
  <acceptance_criteria>
    - source (W5 — stricter than `grep -c`): every `signalClient.send(...)` call expression in `src/receive-loop.js` contains an `intent:` field — verified by the multiline-regex node script in `<verify>` (exactly 8 sends, all 8 with intent)
    - source (W5): every `signalClient.send(...)` call expression in `src/index.js` contains an `intent:` field — exactly 3 sends, all 3 with intent
    - source: `grep "intent: 'attestation_kickoff'" src/agents/alerter/src/index.js` matches the bypassCap=true site
    - source: no `commit_ack` string appears in either file (reserved for Phase 45)
    - test: `cd src/agents/alerter &amp;&amp; npm test` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd src/agents/alerter &amp;&amp; node -e "
const fs=require('fs');
function countSendIntents(file, expected) {
  const src = fs.readFileSync(file,'utf8').replace(/\/\/.*$/gm,'');  // strip line comments
  // Multiline-aware: find each signalClient.send( ... ) call and check it contains intent:
  const re = /signalClient\.send\s*\(([\s\S]*?)\)\s*[.;,\n]/g;
  let m, total=0, withIntent=0;
  while ((m = re.exec(src)) !== null) {
    total++;
    if (/\bintent\s*:/.test(m[1])) withIntent++;
  }
  if (total !== expected || withIntent !== expected) {
    console.error(file+': sends='+total+' withIntent='+withIntent+' expected='+expected);
    process.exit(1);
  }
}
countSendIntents('src/receive-loop.js', 8);
countSendIntents('src/index.js', 3);
" &amp;&amp; ! grep -q "commit_ack" src/receive-loop.js src/index.js &amp;&amp; npm test 2&gt;&amp;1 | tail -5</automated>
  </verify>
  <done>11 of 14 sites wired; full suite green.</done>
</task>

<task type="auto">
  <name>Task 3.2: Wire intents on capture.js, outbound-confirm.js, extraction/outbound.js (3 sites)</name>
  <files>
    src/agents/alerter/src/capture.js,
    src/agents/alerter/src/confirm/outbound-confirm.js,
    src/agents/alerter/src/extraction/outbound.js,
    src/agents/alerter/test/integration.test.js
  </files>
  <read_first>
    - src/agents/alerter/src/capture.js around the send at :197 (verify the surrounding context — the convo reply has a capture id in scope)
    - src/agents/alerter/src/confirm/outbound-confirm.js full file (the createConfirmOutbound factory has draftId in scope)
    - src/agents/alerter/src/extraction/outbound.js full file (captureId in scope)
    - src/agents/alerter/test/integration.test.js (extend the existing integration coverage with intent assertions)
    - .planning/phases/44-event-gate-durable-signal-outbound-tenant-aware/44-PATTERNS.md (§Send-site updates)
  </read_first>
  <action>
    capture.js:197 — extend the send call to `signalClient.send(replyText, { to: replyTarget, intent: 'convo_reply', relatedCaptureId: id, sourceModule: 'capture.js' })`. Preserve existing `to` opt. confirm/outbound-confirm.js:33 — `signalClient.send(body, { to: target, intent: 'confirm_prompt', relatedDraftId: draftId, sourceModule: 'outbound-confirm.js' })`. extraction/outbound.js:55 — `signalClient.send(body, { to: target, intent: 'extraction_preview', relatedCaptureId: captureId, sourceModule: 'extraction/outbound.js' })`. In test/integration.test.js add one test that mocks `signalClient.send` (or asserts on the outboundDb spy) that every code path hit during the integration exercises produces no `intent='unknown'` row.
  </action>
  <acceptance_criteria>
    - source: `grep -c "intent: 'convo_reply'" src/agents/alerter/src/capture.js` is 1
    - source: `grep -c "intent: 'confirm_prompt'" src/agents/alerter/src/confirm/outbound-confirm.js` is 1
    - source: `grep -c "intent: 'extraction_preview'" src/agents/alerter/src/extraction/outbound.js` is 1
    - source: each of the three files contains a `relatedCaptureId` or `relatedDraftId` field on the modified call
    - test: integration test asserts no `intent='unknown'` row is observed during the exercised flows
    - test: `cd src/agents/alerter &amp;&amp; npm test` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd src/agents/alerter &amp;&amp; grep -q "intent: 'convo_reply'" src/capture.js &amp;&amp; grep -q "intent: 'confirm_prompt'" src/confirm/outbound-confirm.js &amp;&amp; grep -q "intent: 'extraction_preview'" src/extraction/outbound.js &amp;&amp; npm test 2&gt;&amp;1 | tail -5</automated>
  </verify>
  <done>All 14 sites wired; integration test guards against future 'unknown' leaks; full suite green.</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-44-03-01 | Repudiation | New send site added in a future PR bypasses intent (regression) | accept | CI grep-gate against `signalClient.send(` outside `signal.js` deferred to v1.9 per CONTEXT Deferred Ideas. Integration test in Task 3.2 catches it for currently-exercised paths. |
| T-44-03-02 | Tampering | Wrong intent string mis-attributes a send (e.g. labels rh_alert as command_echo) | mitigate | Intent mapping table in 44-02-SUMMARY is the single source of truth; reviewer cross-references during code review |
</threat_model>

<verification>
```bash
cd src/agents/alerter && grep -rn "signalClient.send" src/ | wc -l   # expect 14 lines (audit count holds)
cd src/agents/alerter && grep -rn "intent:" src/ | wc -l             # expect ≥14 (one per site)
cd src/agents/alerter && npm test
# Post-deploy sanity:
psql -h elder-plops -d mushy -c "SELECT intent, COUNT(*) FROM signal_outbound WHERE sent_at > now() - interval '1 hour' GROUP BY intent;"
# → no 'unknown' rows after deploy
```
</verification>

<success_criteria>
- All 14 `signalClient.send` sites pass an explicit `intent` from the D-13 enum
- All 14 sites pass `sourceModule` so the source_module column is non-null
- Three sites also pass `relatedCaptureId`/`relatedDraftId` (capture.js, outbound-confirm.js, extraction/outbound.js)
- `commit_ack` reserved string never appears in Phase 44 code
- Full alerter test suite green
</success_criteria>

<output>
Create `.planning/phases/44-event-gate-durable-signal-outbound-tenant-aware/44-03-SUMMARY.md` with the locked intent-mapping table (one row per call site) so Phase 45 has a clean reference for `commit_ack` insertion.
</output>
