---
phase: 59-event-gate
plan: 03
type: execute
wave: 3
depends_on: [59-01, 59-02]
files_modified:
  - src/farm-agent/farm_agent/gate/event_gate.py
  - src/farm-agent/farm_agent/gate/__init__.py
  - src/farm-agent/farm_agent/boot.py
  - src/farm-agent/farm_agent/capture/pipeline.py
  - src/farm-agent/tests/test_gate_event_gate.py
autonomous: true
requirements: [GATE-01]
user_setup: []

must_haves:
  truths:
    - "create_event_gate reproduces the Node decision order exactly: rule_positive -> fast_event(allow); rule_negative -> skipped_rule_neg(deny); await classify; !ok -> forced(allow, fail-open); is_event OR confidence<0.7 -> haiku_event(allow); else haiku_chitchat(deny)"
    - "Replaying the 90-row non-holdout corpus subset through the gate -- driven by a smart per-row FakeAnthropicClient that returns is_event from each row's own hand label -- yields 0% false-positive on labeled negatives (every expected_gate_action=='skip' row -> allow_extract False) and >=95% event recall (>=95% of expected_gate_action=='extract' rows -> allow_extract True); a fixture pre-check asserts the rule fast-paths alone cover only a subset of extract rows, so the per-row mock (not a blanket return) is what carries recall -- the proof measures rule-coverage + wiring + fail-open, NOT classifier accuracy (that is Plan 04's real-Haiku run)"
    - "A classifier timeout/API error fails OPEN: the gate returns {gate:'forced', allow_extract:True} and a WARNING is logged; the gate is wired into boot.py (shared AsyncAnthropic, closed on shutdown) and into the capture pipeline (fail-open, never blocks capture)"
  artifacts:
    - path: "src/farm-agent/farm_agent/gate/event_gate.py"
      provides: "create_event_gate facade with the verbatim decision flow"
      contains: "def create_event_gate"
      min_lines: 25
    - path: "src/farm-agent/farm_agent/gate/__init__.py"
      provides: "re-exports create_event_gate"
      contains: "create_event_gate"
    - path: "src/farm-agent/farm_agent/boot.py"
      provides: "shared AsyncAnthropic singleton + gate wiring + close-on-shutdown"
      contains: "AsyncAnthropic"
    - path: "src/farm-agent/farm_agent/capture/pipeline.py"
      provides: "gate call integrated fail-open; extraction_gate written"
      contains: "gate"
    - path: "src/farm-agent/tests/test_gate_event_gate.py"
      provides: "decision-flow + fail-open + 90-row corpus-replay parity tests (smart per-row mock)"
      exports: ["test_corpus_no_false_positives", "test_corpus_event_recall", "test_fail_open_forced"]
  key_links:
    - from: "boot.py"
      to: "anthropic.AsyncAnthropic + create_event_gate(create_haiku_classifier(...))"
      via: "singleton creation mirroring httpx.AsyncClient at boot.py:70-73"
      pattern: "AsyncAnthropic"
    - from: "capture/pipeline.py handle()"
      to: "gate['classify']"
      via: "fail-open try/except, writes extraction_gate"
      pattern: "gate\\[.classify.\\]"
    - from: "test_gate_event_gate.py corpus replay"
      to: "tests/fixtures/gate/44-hand-classified-100.jsonl + HOLDOUT_ROW_IDS"
      via: "fixture load + holdout filter"
      pattern: "HOLDOUT_ROW_IDS"
---

<objective>
Land the gate facade and wire it end-to-end: `event_gate.py` (`create_event_gate`, the verbatim Node `index.js` decision flow), the `boot.py` shared `AsyncAnthropic` singleton + gate construction + close-on-shutdown, the `capture/pipeline.py` fail-open gate call that writes `extraction_gate`, and `test_gate_event_gate.py` (decision-flow unit tests + the 90-row corpus-replay parity test that proves the two ROADMAP success criteria deterministically).

Purpose: this is the assembly wave — it connects the Plan-02 leaf units into the gate and into the live capture path, and it proves SC-1 (0% false-positive on labeled negatives) and SC-2 (>=95% event recall) on the deterministic 90-row subset, plus SC-3 (fail-open + WARNING). CRITICAL non-circularity note: a blanket-mock classifier would make this proof hollow — a fixture audit shows only 28 of the 76 extract rows are covered by a rule fast-path, so the remaining 48 extract rows depend on the classifier returning `is_event=True`. The corpus test therefore drives a SMART per-row mock that returns `is_event` from each row's own hand label, so SC-1/SC-2 measure the gate WIRING + rule-coverage + fail-open faithfully (classifier ACCURACY against the real model is the deferred Plan-04 run). The boot wiring mirrors the existing `httpx.AsyncClient` singleton exactly so there is one client for the daemon lifetime. The pipeline call is fail-open: a gate error never blocks a capture from being persisted.
Output: `event_gate.py`, the boot + pipeline edits, and the corpus-replay test suite, all green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/59-event-gate/59-RESEARCH.md
@.planning/phases/59-event-gate/59-PATTERNS.md
@.planning/phases/59-event-gate/59-VALIDATION.md

<interfaces>
<!-- Node decision flow + the Python wiring analogs. -->
From src/agents/alerter/src/event-gate/index.js (port verbatim — decision order):
  pos = rulePositive(envCtx); pos.hit -> {gate:'fast_event', allow_extract:true, allow_convo:true}
  neg = ruleNegative(envCtx, lastBotOutbound, nowMs); neg.hit -> {gate:'skipped_rule_neg', allow_extract:false, allow_convo:false}
  r = await haikuClassifier.classify(envCtx)
  !r || !r.ok -> {gate:'forced', allow_extract:true, allow_convo:true}   (fail-OPEN)
  r.is_event === true || (typeof r.confidence==='number' && r.confidence<0.7) -> {gate:'haiku_event', allow_extract:true, allow_convo:true}
  else -> {gate:'haiku_chitchat', allow_extract:false, allow_convo:false}
  Python notes: not r or not r.get("ok"); r.get("is_event") is True (strict); isinstance(r.get("confidence"), (int,float)) and r.get("confidence") < 0.7

From src/farm-agent/farm_agent/boot.py:70-73 + 97-105 (the httpx singleton + shutdown to mirror):
  http = httpx.AsyncClient(); ... ; transcribe_client = create_transcribe_client(config.whisper_url, http); pipeline = create_capture_pipeline(pool, signal_client, transcribe_client, config)
  shutdown: await receive_loop.stop(); retention_task.cancel(); ...; await http.aclose(); await pool.close()

From src/farm-agent/farm_agent/capture/pipeline.py (CONFIRMED current factory signature — keyword callers + 4-positional boot caller):
  def create_capture_pipeline(pool, signal_client, transcribe_client, config, capture_repo=None, dispatch_result=None, log=None) -> {"handle": handle, "record_reply_capture": ...}
  CALLER AUDIT (done at plan time): boot.py:73 passes the first 4 args positionally and NOTHING after; ALL 9 test call sites in tests/test_capture_pipeline.py pass pool/signal_client/transcribe_client/config/capture_repo by KEYWORD. => the safe insertion point for the new param is the END of the signature (after log), so no existing positional or keyword caller shifts.
  transcribe fail-open block to mirror: try: r = await transcribe_client["transcribe"](audio_path); if not r.get("ok"): _log.warning("[capture] ... fail-open (D-04): sender=%s reason=%s", mask_number(source), r.get("reason"))
  signal_capture.extraction_gate is VARCHAR(32), migrated in Phase 56 migration 007

From src/farm-agent/farm_agent/gate/ (Plans 01/02):
  create_haiku_classifier(client, ...) -> {"classify": classify}   ;   prompts.HOLDOUT_ROW_IDS (10 ULIDs)
Fixture row schema: capture_id, raw_text, transcript, attachment_count, expected_gate_action ('extract'|'skip'), class ('hard-event'|'soft-obs'|'confirm'|'UX-meta'|'phantom-ack'|'greetings')
  -> env_ctx = {"text": row["raw_text"], "transcript": row["transcript"], "attachmentCount": row["attachment_count"]}
Fixture audit (plan time, 100 rows): 76 extract / 24 skip; rule fast-paths hit only 28/76 extract rows => 48 extract rows REQUIRE classifier is_event=True; the 2 skip rows that spuriously rule-match (UX-meta) are BOTH in HOLDOUT_ROW_IDS, so they are excluded from the 90-row subset.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Port event_gate.py facade + decision-flow & fail-open unit tests</name>
  <read_first>
    - src/agents/alerter/src/event-gate/index.js (the verbatim decision flow — copy the 1->2->3 order exactly)
    - .planning/phases/59-event-gate/59-RESEARCH.md §"Decision Order (index.js)" + the three Python translation notes (strict ===, isinstance confidence, not r.get('ok'))
    - .planning/phases/59-event-gate/59-PATTERNS.md §"farm_agent/gate/event_gate.py" (factory+closure shape from pipeline.py)
    - src/farm-agent/farm_agent/gate/rules.py + classifier.py (Plan 02 — the units this facade composes)
  </read_first>
  <behavior>
    - rule_positive hit (attachmentCount=1) -> {gate:'fast_event', allow_extract:True} WITHOUT calling the classifier
    - rule_negative hit (ack within 30min of attestation_kickoff) -> {gate:'skipped_rule_neg', allow_extract:False}
    - classifier !ok (raise_exc set) -> {gate:'forced', allow_extract:True} (fail-open)
    - classifier ok is_event=True -> {gate:'haiku_event', allow_extract:True}
    - classifier ok is_event=False confidence=0.5 -> {gate:'haiku_event', allow_extract:True} (confidence<0.7 floor)
    - classifier ok is_event=False confidence=0.95 -> {gate:'haiku_chitchat', allow_extract:False}
  </behavior>
  <action>
    Create `farm_agent/gate/event_gate.py`: `create_event_gate(haiku_classifier: dict, log=None) -> {"classify": classify}` (the gate composes the rules module functions internally — `from farm_agent.gate.rules import rule_positive, rule_negative` — matching the Node facade which receives `rules`; passing rules in is also acceptable, but keep the import-and-call form for a smaller surface). `_log = log or logger`. The `classify(env_ctx, last_bot_outbound, now_ms)` coroutine reproduces index.js verbatim: (1) `rule_positive(env_ctx)` hit -> `{"gate": "fast_event", "allow_extract": True, "allow_convo": True}`; (2) `rule_negative(env_ctx, last_bot_outbound, now_ms)` hit -> `{"gate": "skipped_rule_neg", "allow_extract": False, "allow_convo": False}`; (3) `r = await haiku_classifier["classify"](env_ctx)`; `not r or not r.get("ok")` -> `{"gate": "forced", "allow_extract": True, "allow_convo": True}` (fail-open); `r.get("is_event") is True or (isinstance(r.get("confidence"), (int, float)) and r.get("confidence") < 0.7)` -> `{"gate": "haiku_event", "allow_extract": True, "allow_convo": True}`; else `{"gate": "haiku_chitchat", "allow_extract": False, "allow_convo": False}`. Define the five gate-enum strings as module constants. Update `farm_agent/gate/__init__.py` to re-export `create_event_gate` (replace the bare package marker from Plan 01 with `from farm_agent.gate.event_gate import create_event_gate` + `__all__ = ["create_event_gate"]`). Then write the decision-flow + fail-open unit tests in `tests/test_gate_event_gate.py` (one test per behavior bullet above, including `test_fail_open_forced`), composing `create_event_gate(create_haiku_classifier(client=fake_anthropic_client))` — set `fake_anthropic_client.raise_exc` / `.tool_input` to drive each branch, and use a non-rule-matching `env_ctx` (short plain text) so steps 1+2 fall through to the classifier where needed.
  </action>
  <verify>
    <automated>cd src/farm-agent && uv run pytest -q tests/test_gate_event_gate.py -k "flow or fail_open or forced or chitchat or haiku_event" -x</automated>
  </verify>
  <done>event_gate.py reproduces the index.js decision order verbatim (incl. the 0.7 floor and strict is_event check); __init__.py re-exports create_event_gate; the decision-flow + fail-open unit tests are green.</done>
</task>

<task type="auto">
  <name>Task 2: Corpus-replay parity test (90-row non-holdout subset, smart per-row mock)</name>
  <read_first>
    - .planning/phases/59-event-gate/59-RESEARCH.md §"Validation Architecture" (SC-1/SC-2/SC-3 mapping) + Pitfall 6 (exclude the 10 holdout rows)
    - .planning/phases/59-event-gate/59-PATTERNS.md §"tests/test_gate_event_gate.py" (corpus-replay load + holdout filter) + §"@pytest.mark.live_fire skip gate" (do NOT add the live test here — that is Plan 04)
    - src/farm-agent/tests/fixtures/gate/44-hand-classified-100.jsonl (Plan 01 copy — schema: capture_id, raw_text, transcript, attachment_count, expected_gate_action, class)
    - src/farm-agent/farm_agent/gate/prompts.py (HOLDOUT_ROW_IDS — the 10 ULIDs to exclude)
    - src/farm-agent/tests/conftest.py (FakeAnthropicClient — note: its tool_input is fixed at construction; this task needs a PER-ROW input, so build a tiny per-row classifier shim rather than reusing the single-input fake)
  </read_first>
  <action>
    Add to `tests/test_gate_event_gate.py` a `load_non_holdout_rows()` helper reading the fixture JSONL (`Path(__file__).parent / "fixtures" / "gate" / "44-hand-classified-100.jsonl"`), parsing each line, filtering `r["capture_id"] not in HOLDOUT_ROW_IDS` (yields 90 rows). For each row build `env_ctx = {"text": row["raw_text"], "transcript": row["transcript"], "attachmentCount": row["attachment_count"] or 0}`.

    NON-CIRCULARITY — the proof must NOT depend on a blanket-mock that hides real misses. Implement BOTH guards:

    (a) FIXTURE PRE-CHECK (`test_corpus_rule_coverage_precheck`): replicate the rule_positive logic over the 90 rows and assert that the rule fast-paths cover STRICTLY FEWER than all extract rows (concretely: assert `28 <= rule_covered_extract < extract_total` so the count can't silently drift to "rules cover everything", which is the only way a blanket is_event=False mock could fake recall). Also assert that the number of skip rows NOT covered by any rule is the dominant majority (the labeled negatives must mostly reach the classifier), and that the >=95% recall therefore genuinely depends on the per-row classifier returning is_event=True for the ~48 rule-uncovered extract rows. Document in the test docstring: "This deterministic test proves rule-coverage + gate wiring + fail-open; CLASSIFIER ACCURACY against the real model is the deferred Plan-04 live-fire run."

    (b) SMART PER-ROW CLASSIFIER (drives SC-1/SC-2): build a per-row faithful classifier shim — a small async function `smart_classify(env_ctx)` that looks up the row by env_ctx identity (or iterate row-by-row constructing a fresh gate per row) and returns `{"ok": True, "is_event": (row["expected_gate_action"] == "extract"), "kind": row["class"], "confidence": 0.95}`. Wrap it as `{"classify": smart_classify}` and pass it directly to `create_event_gate(haiku_classifier=...)` (bypassing create_haiku_classifier for this test, since the point is to measure the GATE composition + rule prefilter, with a hand-faithful classifier standing in for Haiku). This makes SC-1/SC-2 measure the gate WIRING + rule prefilter exactly — a real recall miss (e.g. a rule that wrongly fast-paths a labeled negative, or a rule that fails to cover an event the mock also can't save) shows up as a genuine failure instead of being masked.

    Write `test_corpus_no_false_positives` (SC-1): for every row where `expected_gate_action == "skip"`, assert the gate's `allow_extract is False` — collect ALL violations and assert the list is empty (0% false-positive on labeled negatives). If any labeled-negative row spuriously fast-paths positive (e.g. a confirm-class ack that matches STRAIN_RE), surface the capture_id + class in the assertion message — this is a real Node-parity signal, not a test bug, and must be investigated before claiming SC-1, not silenced. (Plan-time audit: the only 2 spuriously-rule-positive skip rows are both in HOLDOUT_ROW_IDS, so the 90-row subset is expected to be clean; if a NEW one appears, the rule port diverged from Node.)

    Write `test_corpus_event_recall` (SC-2): for every row where `expected_gate_action == "extract"`, count how many the gate returns `allow_extract is True`; assert `recall = passed / total >= 0.95`, reporting any extract row the gate denied (capture_id + class) in the failure message.

    Use `now_ms = int(time.time() * 1000)` and `last_bot_outbound = None` for every row (the fixture rows are standalone captures; rule_negative needs a prior attestation_kickoff which the corpus does not carry).
  </action>
  <acceptance_criteria>
    - `test_corpus_rule_coverage_precheck` passes and asserts rule fast-paths cover strictly fewer than all extract rows (the per-row classifier, not a blanket return, carries recall) — so the SC-1/SC-2 proof is non-circular.
    - `test_corpus_no_false_positives` loads exactly 90 non-holdout rows and passes with zero labeled-negative rows allowed (SC-1); any spurious positive is surfaced (capture_id + class), not silenced.
    - `test_corpus_event_recall` passes at >=95% on the 90-row subset using the smart per-row classifier; any denied extract row is reported.
    - Test docstring states explicitly: deterministic test proves rule-coverage + wiring + fail-open; classifier accuracy is the deferred Plan-04 real-Haiku run.
  </acceptance_criteria>
  <verify>
    <automated>cd src/farm-agent && uv run pytest -q tests/test_gate_event_gate.py::test_corpus_rule_coverage_precheck tests/test_gate_event_gate.py::test_corpus_no_false_positives tests/test_gate_event_gate.py::test_corpus_event_recall -x</automated>
  </verify>
  <done>The corpus-replay tests load exactly 90 non-holdout rows; the pre-check proves rule fast-paths cover only a subset of extract rows (so the proof is non-circular); test_corpus_no_false_positives passes with zero labeled-negative rows allowed (SC-1); test_corpus_event_recall passes at >=95% via the smart per-row classifier (SC-2). Any spurious positive on a labeled negative is surfaced (not silenced) in the failure message.</done>
</task>

<task type="auto">
  <name>Task 3: Wire the gate into boot.py + capture pipeline (fail-open)</name>
  <read_first>
    - src/farm-agent/farm_agent/boot.py (full file — the httpx.AsyncClient singleton at :70-73, the import block at :32-41, the shutdown sequence at :97-105)
    - .planning/phases/59-event-gate/59-PATTERNS.md §"farm_agent/boot.py (modify)" (exact AsyncAnthropic wiring + shutdown close) + §"farm_agent/capture/pipeline.py (modify)" (the gate call block + factory signature change)
    - .planning/phases/59-event-gate/59-RESEARCH.md §"Open Questions" Q2 (Phase 59 wires the gate into pipeline.py; last_bot_outbound is None for now — Phase 60 fills it) + Pitfall 7 (PII in logs)
    - src/farm-agent/farm_agent/capture/pipeline.py lines 130-163 (factory signature — confirmed: pool, signal_client, transcribe_client, config, capture_repo=None, dispatch_result=None, log=None) + 236-261 (the transcribe fail-open block — the structural analog for the gate call)
    - src/farm-agent/tests/test_capture_pipeline.py (the 9 keyword-only call sites — they must pass unchanged after the signature change)
  </read_first>
  <action>
    In `boot.py`: add `import anthropic` and `from farm_agent.gate import create_event_gate` / `from farm_agent.gate.classifier import create_haiku_classifier` to the import block. After `http = httpx.AsyncClient()`, construct the shared singleton `anthropic_client = anthropic.AsyncAnthropic(api_key=config.anthropic_api_key, max_retries=2)` (one per daemon lifetime, mirroring the httpx singleton; the api_key flows only into the constructor and is never logged — T-56-06-01). Build `gate = create_event_gate(haiku_classifier=create_haiku_classifier(client=anthropic_client), log=log)` and pass `gate=gate` (BY KEYWORD) into `create_capture_pipeline(...)`. In the shutdown sequence, add `await anthropic_client.close()` immediately before `await http.aclose()`.

    In `capture/pipeline.py`: add `gate: dict | None = None` as the LAST parameter of `create_capture_pipeline`, i.e. AFTER `log` (confirmed safe by the plan-time caller audit: boot.py passes only the first 4 args positionally and nothing after; all 9 test call sites pass capture_repo by keyword — appending at the end shifts no existing caller). Update the docstring to note `gate: Optional Phase 59 event-gate dict {"classify": async(env_ctx, last_bot_outbound, now_ms) -> {...}}; default None = gate disabled (backward-compatible)`. In `handle()`, after the transcription block, add a fail-open gate call mirroring the transcribe block: if `gate is not None`, `try:` build `env_ctx = {"text": text, "transcript": transcript, "attachmentCount": len(attachment_paths)}`, `gate_result = await gate["classify"](env_ctx, None, int(time.time() * 1000))`, set `extraction_gate = gate_result.get("gate")`, and log at INFO `"[capture] gate=%s allow_extract=%s sender=%s"` with `gate_result.get("gate")`, `gate_result.get("allow_extract")`, `mask_number(source)` — never log env_ctx text/transcript (Pitfall 7); `except Exception as exc:  # noqa: BLE001` log WARNING `"[capture] gate error (fail-open): sender=%s err=%s"` with `mask_number(source)` and leave `extraction_gate` None (pipeline continues). Persist `extraction_gate` onto the capture row written to `signal_capture.extraction_gate` (VARCHAR(32), migration 007) alongside the existing columns.
  </action>
  <acceptance_criteria>
    - `gate` is the LAST parameter of create_capture_pipeline (after log); the 9 existing keyword call sites in tests/test_capture_pipeline.py and the 4-positional boot.py caller all work UNCHANGED.
    - Backward-compat: `uv run pytest -q tests/test_capture_pipeline.py` passes with NO edits to that test file (gate defaults None = disabled).
    - boot.py constructs ONE AsyncAnthropic, builds the gate, passes gate by keyword, and closes the client on shutdown; api_key never logged.
    - pipeline writes extraction_gate fail-open (a gate exception leaves extraction_gate None and the capture is still persisted); logs only masked sender + gate outcome.
  </acceptance_criteria>
  <verify>
    <automated>cd src/farm-agent && uv run python -c "src=open('farm_agent/boot.py').read(); assert 'AsyncAnthropic' in src and 'anthropic_client.close' in src; psrc=open('farm_agent/capture/pipeline.py').read(); assert 'gate' in psrc and 'extraction_gate' in psrc; print('wiring ok')" && uv run pytest -q tests/test_capture_pipeline.py -x && uv run pytest -q tests/ -x</automated>
  </verify>
  <done>boot.py creates one shared AsyncAnthropic (api_key never logged), builds the gate, passes it by keyword into the pipeline, and closes the client on shutdown; pipeline.py takes an optional gate as its LAST param (existing pipeline tests pass UNCHANGED), calls it fail-open (gate error never blocks capture), logs only masked sender + gate outcome, and writes extraction_gate to signal_capture; the full suite (incl. existing 56/57/58 tests) is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| farmer Signal message → gate → Anthropic | Untrusted farmer text flows through the gate into the LLM as a separate user turn |
| boot config (anthropic_api_key) → AsyncAnthropic | The secret crosses into the SDK constructor and must never reach logs |
| gate decision → signal_capture.extraction_gate | An internal audit value written to the DB; bounded to the 5-value enum |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-56-06-01 | Information Disclosure (API key) | boot.py AsyncAnthropic construction | mitigate | api_key flows only into the constructor; boot.py logs only lifecycle messages (existing T-56-06-01 invariant), never config fields |
| T-59-03-01 | Information Disclosure (PII) | pipeline gate-call log line | mitigate | log only gate outcome + allow_extract + mask_number(source); never env_ctx["text"]/["transcript"] (Pitfall 7) |
| T-59-03-02 | Denial of Service (gate error blocks capture) | pipeline gate call | mitigate | the gate call is wrapped in a fail-open try/except; a gate exception leaves extraction_gate None and the capture is still persisted |
| T-44-04-01 | Tampering (prompt injection) | event_gate -> classifier path | mitigate | the facade passes env_ctx unchanged to the classifier, which wraps it as a separate user turn (Plan 02); no system-prompt concatenation anywhere in the path |
| T-59-03-03 | Tampering (fail-CLOSED regression) | classifier !ok branch | mitigate | the `not r or not r.get("ok")` branch returns forced/allow_extract=True; test_fail_open_forced asserts this; a future regression to deny-on-error is caught by that test |
</threat_model>

<verification>
- `cd src/farm-agent && uv run pytest -q` full suite green.
- `grep -n 'AsyncAnthropic' src/farm-agent/farm_agent/boot.py` + `grep -n 'anthropic_client.close' boot.py` confirm singleton + shutdown.
- `grep -n 'extraction_gate' src/farm-agent/farm_agent/capture/pipeline.py` confirms the gate result is persisted.
- The corpus-replay tests prove SC-1 (0% false-positive) and SC-2 (>=95% recall) via the smart per-row mock + the non-circular rule-coverage pre-check; the fail-open unit test proves SC-3.
</verification>

<success_criteria>
- event_gate.py reproduces the Node decision flow verbatim (5-value enum, 0.7 floor, fail-open on !ok).
- The 90-row corpus replay proves SC-1 (0% false-positive on labeled negatives) and SC-2 (>=95% event recall) deterministically and non-circularly (rule-coverage pre-check + smart per-row classifier).
- The gate is wired into boot.py (shared AsyncAnthropic, closed on shutdown) and the capture pipeline (fail-open, writes extraction_gate, no PII in logs); existing pipeline tests pass unchanged.
- Full suite green; SC-3 fail-open + WARNING proven by unit test.
</success_criteria>

<output>
Create `.planning/phases/59-event-gate/59-03-SUMMARY.md` when done.
</output>
</content>
</output>
