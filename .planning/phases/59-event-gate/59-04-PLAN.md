---
phase: 59-event-gate
plan: 04
type: execute
wave: 4
depends_on: [59-03]
files_modified:
  - src/farm-agent/tests/test_gate_live_fire.py
  - src/farm-agent/pyproject.toml
autonomous: false
requirements: [GATE-01]
user_setup:
  - service: anthropic
    why: "Real-Haiku full-100-corpus accuracy validation (operator-run, costs real API calls)"
    env_vars:
      - name: ANTHROPIC_API_KEY
        source: "Anthropic Console -> API Keys (the live key; the deterministic CI suite uses a placeholder)"
      - name: GATE_LIVE_FIRE
        source: "Set to 1 to opt into the live-fire run (guards against accidental paid runs in CI)"

must_haves:
  truths:
    - "A marker/env-gated test replays the FULL 100-row corpus (incl. the 10 holdout rows) through the REAL Haiku classifier and asserts the two ROADMAP parity metrics: 0% false-positive on labeled negatives, >=95% event recall"
    - "The live-fire test is skipped by default in CI (requires ANTHROPIC_API_KEY + GATE_LIVE_FIRE=1) and is NOT required to pass for phase verification — it is operator-run like the Phase 58 live-fire"
    - "For the cache-liveness check the test calls create_haiku_classifier(client) DIRECTLY (its success shape carries usage) and asserts cache_creation_input_tokens > 0 on the first call (or cache_read_input_tokens > 0 on a warm cache), proving the system prompt cleared the 4096-token threshold; the gate facade does NOT propagate usage, so the cache check goes through the classifier, not the gate result"
  artifacts:
    - path: "src/farm-agent/tests/test_gate_live_fire.py"
      provides: "the deferred real-Haiku full-corpus accuracy harness behind @pytest.mark.live_fire + skipif"
      contains: "live_fire"
    - path: "src/farm-agent/pyproject.toml"
      provides: "the live_fire pytest marker registered (no unknown-marker warning)"
      contains: "live_fire"
  key_links:
    - from: "test_gate_live_fire.py"
      to: "real anthropic.AsyncAnthropic + the full 100-row fixture (no holdout filter)"
      via: "ANTHROPIC_API_KEY + GATE_LIVE_FIRE skip gate"
      pattern: "GATE_LIVE_FIRE"
    - from: "live-fire assertions"
      to: "the two ROADMAP parity metrics"
      via: "per-row allow_extract vs expected_gate_action"
      pattern: "recall"
---

<objective>
Add the deferred real-Haiku validation harness: a single marker/env-gated test that replays the full 100-row corpus (including the 10 holdout rows) through the REAL Anthropic Haiku classifier and asserts the two ROADMAP parity metrics — 0% false-positive on labeled negatives and >=95% event recall — plus a prompt-cache liveness check on the first call. Register the `live_fire` pytest marker.

Purpose: the deterministic Plan-03 corpus replay proves the gate WIRING + fail-open with a mocked classifier; this harness proves classifier ACCURACY against the real model. It costs real API calls and needs a live key, so it is operator-run and explicitly NOT part of default CI (mirroring the Phase 58 live-fire). This plan is `autonomous: false`: the harness is built autonomously, but the actual run is a human-gated checkpoint the operator executes when ready. The cache-liveness assertion catches the silent prompt-cache regression (Pitfall 2) that no mocked test can see.
Output: `test_gate_live_fire.py` (skipped by default) + the registered marker.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/59-event-gate/59-RESEARCH.md
@.planning/phases/59-event-gate/59-PATTERNS.md
@.planning/phases/59-event-gate/59-VALIDATION.md

<interfaces>
<!-- The skip-gate + the real client construction. -->
From .planning/phases/59-event-gate/59-PATTERNS.md §"@pytest.mark.live_fire skip gate":
  @pytest.mark.live_fire
  @pytest.mark.skipif(not os.environ.get("ANTHROPIC_API_KEY") or not os.environ.get("GATE_LIVE_FIRE"), reason="live-fire: requires ANTHROPIC_API_KEY + GATE_LIVE_FIRE=1")
  async def test_real_haiku_100_corpus(): ...

From src/farm-agent/farm_agent/gate/ (Plans 01-03):
  create_event_gate(create_haiku_classifier(client=anthropic.AsyncAnthropic(api_key=..., max_retries=2)))
  prompts.CACHEABLE_SYSTEM_BLOCKS  (the cached system block; the cache-liveness check reads create_haiku_classifier(...).classify(...)["usage"].cache_creation_input_tokens > 0 -- the classifier success shape carries usage; the gate facade does NOT)
Fixture row schema (FULL 100, no holdout filter): capture_id, raw_text, transcript, attachment_count, expected_gate_action ('extract'|'skip')
  -> env_ctx = {"text": raw_text, "transcript": transcript, "attachmentCount": attachment_count or 0}
Phase 58 live-fire precedent: .planning/phases/58-capture-transcription/58-LIVE-FIRE.md (the operator-run pattern this mirrors)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the deferred live-fire harness + register the marker</name>
  <read_first>
    - .planning/phases/59-event-gate/59-RESEARCH.md §"Deferred real-Haiku run" (the full-100 run, env-gated; the two parity metrics) + Pitfall 2 (cache threshold + cache_creation_input_tokens check)
    - .planning/phases/59-event-gate/59-PATTERNS.md §"@pytest.mark.live_fire skip gate" (exact decorator shape)
    - src/farm-agent/pyproject.toml ([tool.pytest.ini_options] markers list — register live_fire to avoid the unknown-marker warning)
    - .planning/phases/58-capture-transcription/58-LIVE-FIRE.md (the Phase 58 operator-run live-fire this mirrors)
  </read_first>
  <action>
    Create `tests/test_gate_live_fire.py`: a single `test_real_haiku_100_corpus` decorated with `@pytest.mark.live_fire` and `@pytest.mark.skipif(not os.environ.get("ANTHROPIC_API_KEY") or not os.environ.get("GATE_LIVE_FIRE"), reason="live-fire: requires ANTHROPIC_API_KEY + GATE_LIVE_FIRE=1")`. Inside: construct the REAL client `anthropic.AsyncAnthropic(api_key=os.environ["ANTHROPIC_API_KEY"], max_retries=2)`, build `gate = create_event_gate(create_haiku_classifier(client=client))`, load ALL 100 fixture rows from `tests/fixtures/gate/44-hand-classified-100.jsonl` (NO holdout filter — the holdout rows are the point of the real run), map each to `env_ctx`, and call `await gate["classify"](env_ctx, None, int(time.time()*1000))` for each. Collect per-row results. Assert SC-1: every row with `expected_gate_action == "skip"` returns `allow_extract is False` (0% false-positive); collect and report any violations with their capture_id + class. Assert SC-2: `recall = (#extract rows allowed) / (#extract rows) >= 0.95`. For the cache-liveness check, do NOT try to read `usage` off the gate result (the gate facade does not propagate it). Instead, separately build `classifier = create_haiku_classifier(client=client)` and make ONE direct `cls_result = await classifier["classify"](env_ctx)` call on the first row; its success shape is `{"ok": True, ..., "usage": <resp.usage>}`. Assert `cls_result["usage"].cache_creation_input_tokens > 0` (or `cache_read_input_tokens > 0` on a warm cache) to prove the system prompt cleared the 4096-token threshold (Pitfall 2) — if both are 0, fail with a message pointing at a truncated SYSTEM_PROMPT. (The full SC-1/SC-2 replay below still goes through the gate.) `await client.close()` in a finally. Register the marker in pyproject.toml `[tool.pytest.ini_options]` `markers = ["live_fire: real-API operator-run validation; skipped unless ANTHROPIC_API_KEY + GATE_LIVE_FIRE=1"]` (append to any existing markers list).
  </action>
  <verify>
    <automated>cd src/farm-agent && uv run pytest -q tests/test_gate_live_fire.py -v 2>&1 | grep -qi "skip" && echo "live-fire correctly skipped in CI" && uv run pytest -q --markers 2>&1 | grep -q "live_fire" && echo "marker registered"</automated>
  </verify>
  <done>test_gate_live_fire.py exists with the full-100 real-Haiku parity assertions (SC-1 0% false-positive, SC-2 >=95% recall) + the cache-liveness check; it is SKIPPED by default (no GATE_LIVE_FIRE) so it never blocks CI; the live_fire marker is registered (no unknown-marker warning).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Operator runs the real-Haiku validation (deferred)</name>
  <what-built>The marker/env-gated `test_gate_live_fire.py` harness (Task 1) that replays the full 100-row corpus through the real Haiku classifier and asserts the two ROADMAP parity metrics + prompt-cache liveness. It is skipped in CI and runs only when the operator opts in with a live key.</what-built>
  <how-to-verify>
    1. Ensure a live key is exported: `export ANTHROPIC_API_KEY=<live key>` and `export GATE_LIVE_FIRE=1`.
    2. Run: `cd src/farm-agent && uv run pytest -q tests/test_gate_live_fire.py -v -m live_fire`.
    3. Confirm the test PASSES: 0 labeled-negative rows allowed (SC-1), event recall >= 95% (SC-2), and the direct-classifier first-call usage shows `cache_creation_input_tokens > 0` (prompt-cache active).
    4. If recall < 95% or any labeled-negative slips through, record the failing capture_ids + class in the SUMMARY — this is a real classifier-accuracy signal (a port-fidelity bug if the Node gate would have classified differently), not a flaky test. Do NOT relax the threshold to make it pass.
    5. Note the run cost (the classifier usage token totals) for the milestone cost ledger.
  </how-to-verify>
  <acceptance_criteria>
    - Operator-run only; this checkpoint does NOT gate `/gsd-verify-work` for Phase 59 (the deterministic 90-row subset in Plan 03 is the CI gate). Like the Phase 58 live-fire, it is a production-readiness validation deferred to the operator. Approve when the run passes, or file the accuracy gap as a finding if it does not.
  </acceptance_criteria>
  <resume-signal>Type "passed" when the live-fire run meets both parity metrics, or "deferred" to defer the run (phase still completes on the deterministic CI gate), or describe the accuracy gap to file as a finding.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator shell (ANTHROPIC_API_KEY) → real AsyncAnthropic | The live key is exported into the test process; must not be committed or logged |
| full corpus (incl. PII-shaped farmer text) → real Anthropic API | Real farmer-message text is sent to the live API during the run |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-59-04-01 | Information Disclosure (API key) | live-fire test env | mitigate | the key is read from os.environ only, never hard-coded or logged; the default CI suite uses the conftest "test-key" placeholder and the test is skipped without GATE_LIVE_FIRE |
| T-59-04-02 | Information Disclosure (cost / accidental paid run) | CI accidentally invoking real API | mitigate | double gate (ANTHROPIC_API_KEY AND GATE_LIVE_FIRE=1) + @pytest.mark.live_fire; CI never sets GATE_LIVE_FIRE so the test is always skipped there |
| T-59-04-03 | Information Disclosure (PII to third party) | corpus text sent to Anthropic | accept | the corpus is the same hand-classified prod fixture already used for gate validation; sending it to the LLM is the gate's normal production behavior; senders are masked in logs and the run is operator-initiated |
</threat_model>

<verification>
- `cd src/farm-agent && uv run pytest -q tests/test_gate_live_fire.py` is SKIPPED (no GATE_LIVE_FIRE) — confirms it never blocks CI.
- `uv run pytest -q --markers | grep live_fire` confirms the marker is registered.
- The operator checkpoint (Task 2) is the deferred validation; phase verification does NOT depend on it.
</verification>

<success_criteria>
- The deferred real-Haiku harness exists, asserts both parity metrics on the full 100 rows + cache liveness, and is skipped by default.
- The live_fire marker is registered (no pytest unknown-marker warning).
- The operator checkpoint clearly states the run is deferred and non-CI-blocking.
</success_criteria>

<output>
Create `.planning/phases/59-event-gate/59-04-SUMMARY.md` when done (record whether the live-fire was run-now or deferred, and any accuracy findings).
</output>
