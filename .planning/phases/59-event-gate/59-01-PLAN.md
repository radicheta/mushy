---
phase: 59-event-gate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/farm-agent/pyproject.toml
  - src/farm-agent/farm_agent/gate/__init__.py
  - src/farm-agent/farm_agent/gate/prompts.py
  - src/farm-agent/tests/conftest.py
  - src/farm-agent/tests/fixtures/gate/44-hand-classified-100.jsonl
autonomous: false
requirements: [GATE-01]
user_setup: []

must_haves:
  truths:
    - "anthropic>=0.45 is a pinned runtime dep, added only after a blocking-human legitimacy check (official first-party SDK; low risk but net-new, T-59-SC)"
    - "prompts.py holds the SYSTEM_PROMPT copied byte-verbatim from Node prompts.js (>=4096 tokens so prompt-caching stays live), CACHEABLE_SYSTEM_BLOCKS, and the 10 HOLDOUT_ROW_IDS verbatim"
    - "The gate unit suite has its fake available: a MagicMock-based FakeAnthropicClient (configurable tool_input / raise_exc / no-tool-use) plus the 100-row corpus fixture committed under tests/fixtures/gate/"
  artifacts:
    - path: "src/farm-agent/pyproject.toml"
      provides: "anthropic>=0.45 runtime dependency, pinned in uv.lock"
      contains: "anthropic"
    - path: "src/farm-agent/farm_agent/gate/prompts.py"
      provides: "SYSTEM_PROMPT (verbatim), CACHEABLE_SYSTEM_BLOCKS, HOLDOUT_ROW_IDS"
      contains: "HOLDOUT_ROW_IDS"
    - path: "src/farm-agent/farm_agent/gate/__init__.py"
      provides: "gate package marker (Foray island, no chamber imports)"
    - path: "src/farm-agent/tests/conftest.py"
      provides: "FakeAnthropicClient + fake_anthropic_client fixture for the gate suite"
      contains: "FakeAnthropicClient"
    - path: "src/farm-agent/tests/fixtures/gate/44-hand-classified-100.jsonl"
      provides: "100-row hand-classified prod corpus (copied from Phase 44)"
  key_links:
    - from: "gate suite tests (Plans 02/03)"
      to: "conftest FakeAnthropicClient / fake_anthropic_client"
      via: "pytest fixture injection"
      pattern: "FakeAnthropicClient"
    - from: "classifier.py system block (Plan 02)"
      to: "prompts.CACHEABLE_SYSTEM_BLOCKS"
      via: "import"
      pattern: "CACHEABLE_SYSTEM_BLOCKS"
    - from: "corpus-replay test (Plan 03)"
      to: "prompts.HOLDOUT_ROW_IDS + tests/fixtures/gate/44-hand-classified-100.jsonl"
      via: "fixture load + holdout filter"
      pattern: "HOLDOUT_ROW_IDS"
---

<objective>
Land the Phase-59 Wave-0 foundation that the leaf units (Plan 02) and the facade (Plan 03) build on: add the one net-new runtime dependency (`anthropic>=0.45`) behind a blocking-human legitimacy gate, create the `gate/` package marker, copy the `SYSTEM_PROMPT` + `HOLDOUT_ROW_IDS` verbatim from the Node source into `prompts.py`, copy the 100-row corpus fixture into the test tree, and add the MagicMock-based `FakeAnthropicClient` to `conftest.py`.

Purpose: `anthropic` is a net-new install — it gets the package-legitimacy gate treatment (RESEARCH marks it Approved/official first-party, but it is still a new runtime dep). The verbatim prompt copy is load-bearing: dropping below the 4096-token cache threshold silently disables prompt-caching (RESEARCH Pitfall 2), so it must be copied byte-for-byte with no shortening. The fixture copy makes the test suite self-contained, and the fake mirrors the proven `FakeCaptureRepo` / `fake_transcribe_client` pattern so Plans 02/03 need no new test infrastructure.
Output: pinned `anthropic` dep, `gate/__init__.py`, `gate/prompts.py` (verbatim), the copied fixture, and the conftest fake.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/59-event-gate/59-RESEARCH.md
@.planning/phases/59-event-gate/59-PATTERNS.md
@.planning/phases/59-event-gate/59-VALIDATION.md

<interfaces>
<!-- The fake mirrors the on-disk Phase-58 idioms exactly. -->
From tests/conftest.py:153-194 (FakeCaptureRepo + fake_transcribe_client shapes to mirror for FakeAnthropicClient):
  class FakeCaptureRepo: __init__(self, should_raise=False); self.calls: list[dict]; async def insert_capture(...) -> dict
  @pytest.fixture def fake_transcribe_client(): return {"transcribe": _fake_transcribe}

From RESEARCH §"FakeAnthropicClient fixture shape" (Open Question 1) + PATTERNS §"tests/conftest.py":
  class FakeAnthropicClient: __init__(self, tool_input=None, raise_exc=None, return_no_tool_use=False); self.calls: list[dict]
    def with_options(self, **kwargs) -> self   # mirrors client.with_options(timeout=...)
    @property def messages(self) -> self
    async def create(self, **kwargs) -> MagicMock  # records kwargs; raises raise_exc, or returns content=[] when return_no_tool_use, or content=[tool_use block]
    tool_use block: block.type="tool_use", block.name="classify_capture", block.input=tool_input (attribute access, NOT dict keys -- RESEARCH Pitfall 5)

Node source of truth for prompts.py (copy verbatim, do NOT shorten):
  src/agents/alerter/src/event-gate/prompts.js  (SYSTEM_PROMPT ~24KB; CACHEABLE_SYSTEM_BLOCKS; HOLDOUT_ROW_IDS)
HOLDOUT_ROW_IDS verbatim list is in RESEARCH §"Holdout Row IDs" lines 449-462 (10 ULIDs: 7 soft-obs + 3 ux-meta).
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Package legitimacy gate — anthropic</name>
  <read_first>
    - src/farm-agent/pyproject.toml (current deps; anthropic is NOT present)
    - .planning/phases/59-event-gate/59-RESEARCH.md §"Package Legitimacy Audit" (anthropic tagged Approved — official first-party SDK; source repo github.com/anthropics/anthropic-sdk-python; slopcheck unavailable in env)
    - .planning/phases/58-capture-transcription/58-01-PLAN.md Task 1 (the exact blocking-human legitimacy-gate shape this mirrors)
  </read_first>
  <what-built>Nothing yet — this is a pre-install legitimacy verification required by the package gate because `anthropic` is a net-new runtime install (slopcheck was unavailable at research time; RESEARCH dispositions it Approved as the official first-party SDK, but the gate is never auto-approvable per T-59-SC).</what-built>
  <how-to-verify>
    1. Confirm `anthropic` on https://pypi.org/project/anthropic/ — source repo github.com/anthropics/anthropic-sdk-python (Anthropic), 3+ yrs, very high downloads, the official first-party SDK.
    2. Confirm the GitHub repo is the official Anthropic org (not a typosquat such as `anthropic-ai` or `anthropics`).
    3. `anthropic` is the first-party SDK and verified legitimate. Approve to proceed with the pinned add in Task 2.
  </how-to-verify>
  <acceptance_criteria>
    - Human confirms `anthropic` is the legitimate official SDK (not [SLOP]/[SUS]) before any install. This checkpoint is never auto-approvable (T-59-SC); workflow.auto_advance is ignored here.
  </acceptance_criteria>
  <resume-signal>Type "approved" to install anthropic.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Add anthropic dep; create gate/__init__.py; copy prompts.py verbatim from Node; copy the corpus fixture</name>
  <read_first>
    - src/farm-agent/pyproject.toml ([project].dependencies block — add alongside httpx/psycopg/python-ulid)
    - src/agents/alerter/src/event-gate/prompts.js (the SYSTEM_PROMPT + CACHEABLE_SYSTEM_BLOCKS + HOLDOUT_ROW_IDS source of truth — copy verbatim)
    - .planning/phases/59-event-gate/59-RESEARCH.md §"Pattern 4" (CACHEABLE_SYSTEM_BLOCKS list-of-blocks shape) + §"Holdout Row IDs" (the 10 ULIDs) + Pitfall 2 (4096-token cache threshold — do NOT shorten)
    - src/farm-agent/farm_agent/capture/__init__.py (package-marker shape to mirror for gate/__init__.py)
  </read_first>
  <action>
    In pyproject.toml: add `anthropic>=0.45` to `[project].dependencies`. Run `uv sync` to lock. Create `farm_agent/gate/__init__.py` re-exporting `create_event_gate` from `farm_agent.gate.event_gate` (forward-declared; the module lands in Plan 03 — keep the re-export but guard it so import does not fail before Plan 03 by importing inside `__init__` only if needed; simplest: leave `__init__.py` as a bare package marker for this plan and add the re-export in Plan 03). Create `farm_agent/gate/prompts.py` containing three module constants copied byte-verbatim from the Node `prompts.js`: `SYSTEM_PROMPT` (the ~24KB prompt string, including the D-20 taxonomy and the 15 worked examples — do NOT shorten or reflow; the 4096-token cache threshold is load-bearing per Pitfall 2), `CACHEABLE_SYSTEM_BLOCKS = [{"type": "text", "text": SYSTEM_PROMPT, "cache_control": {"type": "ephemeral"}}]`, and `HOLDOUT_ROW_IDS` (the 10 capture-id ULIDs, from Node / RESEARCH lines 449-462). NOTE: the Node `HOLDOUT_ROW_IDS` array interleaves each ULID with a trailing comment snippet (20 entries = 10 [ulid, comment] pairs); the Python port MUST be the 10 ULIDs ONLY (a flat list of the 10 26-char capture ids) so `len(HOLDOUT_ROW_IDS) == 10` and the Plan-03 holdout filter `capture_id not in HOLDOUT_ROW_IDS` works correctly. Do NOT copy the comment snippets as list entries. prompts.py has NO logic and NO imports beyond stdlib (Foray island; no chamber imports). Copy `.planning/phases/44-event-gate-durable-signal-outbound-tenant-aware/44-hand-classified-100.jsonl` to `src/farm-agent/tests/fixtures/gate/44-hand-classified-100.jsonl` byte-for-byte (create the `fixtures/gate/` dirs).
  </action>
  <verify>
    <automated>cd src/farm-agent && uv run python -c "import anthropic; from farm_agent.gate.prompts import SYSTEM_PROMPT, CACHEABLE_SYSTEM_BLOCKS, HOLDOUT_ROW_IDS; assert len(SYSTEM_PROMPT) > 20000, len(SYSTEM_PROMPT); assert CACHEABLE_SYSTEM_BLOCKS[0]['cache_control'] == {'type': 'ephemeral'}; assert len(HOLDOUT_ROW_IDS) == 10; import pathlib; assert sum(1 for l in pathlib.Path('tests/fixtures/gate/44-hand-classified-100.jsonl').read_text().splitlines() if l.strip()) == 100; print('foundation ok')"</automated>
  </verify>
  <done>anthropic>=0.45 in [project].dependencies + uv.lock updated; gate/__init__.py exists; prompts.py exports SYSTEM_PROMPT (>20,000 chars, verbatim, cache-threshold-clearing; Node source is 22,001 chars), CACHEABLE_SYSTEM_BLOCKS with ephemeral cache_control, and the 10-entry HOLDOUT_ROW_IDS; the 100-row fixture is copied to tests/fixtures/gate/.</done>
</task>

<task type="auto">
  <name>Task 3: Add FakeAnthropicClient + fake_anthropic_client fixture to conftest</name>
  <read_first>
    - src/farm-agent/tests/conftest.py (full file — TEST_ENV at :43 incl. ANTHROPIC_API_KEY="test-key", FakeCaptureRepo at :153-178, fake_transcribe_client at :181-194)
    - .planning/phases/59-event-gate/59-PATTERNS.md §"tests/conftest.py" (the exact FakeAnthropicClient shape: with_options→self, messages property→self, async create recording kwargs, MagicMock tool_use block with attribute access)
    - .planning/phases/59-event-gate/59-RESEARCH.md §"Open Questions" Q1 (use MagicMock — do NOT import private anthropic.types) + Pitfall 5 (block.type/.name/.input are attributes)
  </read_first>
  <action>
    In conftest.py, add (no new file): `from unittest.mock import MagicMock`, then `class FakeAnthropicClient` mirroring FakeCaptureRepo — `__init__(self, tool_input=None, raise_exc=None, return_no_tool_use=False)` defaulting `tool_input` to `{"is_event": True, "kind": "event", "confidence": 0.95}`, `self.calls: list[dict] = []`; a `with_options(self, **kwargs)` returning `self` (mirrors `client.with_options(timeout=...)`); a `messages` property returning `self`; and `async def create(self, **kwargs)` that appends kwargs to `self.calls`, raises `self.raise_exc` if set, returns a MagicMock with `content == []` when `return_no_tool_use`, else returns a MagicMock whose `content` is `[block]` where `block.type = "tool_use"`, `block.name = "classify_capture"`, `block.input = self.tool_input` (attribute access — never dict keys), and `usage` is a MagicMock with `input_tokens/output_tokens/cache_creation_input_tokens/cache_read_input_tokens`. Expose a `fake_anthropic_client` fixture returning a default `FakeAnthropicClient()`. Do NOT change TEST_ENV — ANTHROPIC_API_KEY="test-key" is already present at conftest.py:43.
  </action>
  <verify>
    <automated>cd src/farm-agent && uv run python -c "import tests.conftest as c; assert hasattr(c, 'FakeAnthropicClient'); f=c.FakeAnthropicClient(); assert f.with_options(timeout=2.0) is f; assert f.messages is f; import asyncio; r=asyncio.get_event_loop().run_until_complete(f.create(model='m', messages=[])); b=r.content[0]; assert b.type=='tool_use' and b.name=='classify_capture' and b.input['is_event'] is True; assert len(f.calls)==1; print('fake ok')" && uv run pytest tests/ -x -q -k "not gate"</automated>
  </verify>
  <done>conftest exposes FakeAnthropicClient (configurable tool_input / raise_exc / return_no_tool_use; records calls; with_options→self; messages→self; tool_use block via attribute access) and a fake_anthropic_client fixture; the existing Phase 56/57/58 suite still passes (no regression).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev machine → PyPI | A net-new package (`anthropic`) is pulled into the runtime; a malicious/typosquat package would execute in the alerter process |
| farmer Signal message → system prompt | Untrusted farmer text must never be concatenated into the cached system prompt (carried forward to Plan 02) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-59-SC | Tampering | uv add anthropic (supply chain) | mitigate | Task 1 blocking-human legitimacy checkpoint before any install; never auto-approvable; RESEARCH dispositions it Approved (official first-party SDK) |
| T-44-04-01 | Tampering | SYSTEM_PROMPT immutability (prompt injection blast radius) | mitigate | prompts.py copies the prompt verbatim and inert (no logic, no string interpolation); farmer text is wired as a separate user turn in Plan 02, never into this block |
| T-59-01-01 | Information Disclosure | api key / secret in prompts.py or fixture | accept | prompts.py is inert constants; the fixture is hand-classified public-shape corpus with masked senders already in the Phase 44 source — no new secret introduced |
</threat_model>

<verification>
- `grep -n 'anthropic' src/farm-agent/pyproject.toml` returns a dependency line `anthropic>=0.45`.
- `cd src/farm-agent && uv run pytest` (full suite) green — no regression in Phase 56/57/58 tests.
- `prompts.py` SYSTEM_PROMPT length clears the cache threshold (>20,000 chars; Node source is 22,001) and HOLDOUT_ROW_IDS has exactly 10 entries.
- The corpus fixture has 100 rows under `tests/fixtures/gate/`.
</verification>

<success_criteria>
- anthropic>=0.45 is a pinned runtime dep, locked in uv.lock, added only after human legitimacy approval.
- gate/__init__.py + gate/prompts.py exist; prompts.py is verbatim (no shortening) and inert.
- conftest exposes FakeAnthropicClient + fake_anthropic_client for Plans 02/03.
- The 100-row corpus fixture is committed under tests/fixtures/gate/.
</success_criteria>

<output>
Create `.planning/phases/59-event-gate/59-01-SUMMARY.md` when done (note the SYSTEM_PROMPT byte length confirming the cache threshold is cleared).
</output>
</content>
