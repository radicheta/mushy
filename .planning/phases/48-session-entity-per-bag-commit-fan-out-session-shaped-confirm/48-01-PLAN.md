---
phase: 48-session-entity-per-bag-commit-fan-out-session-shaped-confirm
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/agents/alerter/src/farmos/logs.js
  - src/agents/alerter/src/farmos/commit-db.js
  - src/agents/alerter/test/farmos/commit-db.test.js
autonomous: true
requirements: [INOC-04, INOC-06]
must_haves:
  truths:
    - "signal_draft schema accepts log_type='seeding_session' (no CHECK constraint blocks the new value; LOG_TYPES list includes it)"
    - "getCachedResponse correctly round-trips a multi-asset multi-log farmos_response (asset_ids array length >=1, log_ids array length N)"
    - "Idempotent re-commit on a draft with status='committed' returns the cached farmos_response without re-dispatching to the router (existing commit-watchdog behavior is preserved unchanged for the new log_type)"
  artifacts:
    - path: src/agents/alerter/src/farmos/logs.js
      provides: "LOG_TYPES export extended with 'seeding_session'"
    - path: src/agents/alerter/test/farmos/commit-db.test.js
      provides: "Regression tests proving the existing primitives accept the new shape"
  key_links:
    - from: src/agents/alerter/src/farmos/commits/commit-router.js
      to: src/agents/alerter/src/farmos/logs.js
      via: "LOG_TYPES guard before DISPATCH lookup"
      pattern: "LOG_TYPES.includes"
---

<objective>
Foundation plan. Phase 40 already implemented idempotency on `signal_draft` (status='committed' + `farmos_response` JSONB cache; no separate `signal_commit` table — CONTEXT.md references it but the actual implementation lives in `signal_draft`). Per `[[feedback_friction_policy_missing_vs_mismatch]]`, this plan reconciles the CONTEXT.md naming with the actual implementation silently: idempotency key is `signal_draft.id` (the draft UUID), and `farmos_response.asset_ids[]` + `farmos_response.log_ids[]` carry the session asset + N child seeding log IDs.

The only concrete change here is extending the `LOG_TYPES` allow-list in `farmos/logs.js` to include `'seeding_session'`, so the existing commit-router guard at line 27 of `commit-router.js` does not bounce the new draft type with `unsupported_log_type`. Plus regression tests proving the existing primitives (acquireCommitLock, markCommitted, getCachedResponse) accept the multi-asset multi-log shape.

Purpose: enables Plan 02's new handler dispatch + Plan 04's ack contract without breaking the legacy 5-log-type committers.
Output: extended LOG_TYPES + green test suite. No farmer-visible change yet.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/48-session-entity-per-bag-commit-fan-out-session-shaped-confirm/48-CONTEXT.md
@.planning/phases/47-multi-source-extraction-fusion-groups-shape-inoc-draft/47-CONTEXT.md
@src/agents/alerter/src/farmos/logs.js
@src/agents/alerter/src/farmos/commit-db.js
@src/agents/alerter/src/farmos/commits/commit-router.js

<interfaces>
From src/agents/alerter/src/farmos/logs.js (Phase 40):
  - LOG_TYPES: ['seeding','activity','input','observation','harvest'] (need to add 'seeding_session')
  - class UnsupportedLogTypeError (thrown by commit-router guard)
  - async function createLog(client, logType, opts) — used by commit-seeding.js child writes

From src/agents/alerter/src/farmos/commit-db.js (Phase 40 + 45):
  - acquireCommitLock(pool, draftId): UPDATE signal_draft SET status='committing' WHERE status='confirmed' returning row
  - markCommitted(pool, draftId, farmosResponse): caches {asset_ids:[], log_ids:[], file_ids:[], http_status, latency_ms}
  - markFailed(pool, draftId, reason)
  - getCachedResponse(pool, draftId): returns {status, farmos_response, commit_failed_reason}
  - tryMarkOutcomeAckSent(pool, draftId): CAS for ack-once

From src/agents/alerter/src/farmos/commits/commit-router.js:
  - DISPATCH = { seeding, activity, input, observation, harvest } — Plan 02 adds seeding_session
  - guard: if (!LOG_TYPES.includes(logType)) return unsupported_log_type
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend LOG_TYPES + regression tests for multi-asset multi-log cached response</name>
  <read_first>
    - src/agents/alerter/src/farmos/logs.js (LOG_TYPES export + UnsupportedLogTypeError; whole file)
    - src/agents/alerter/src/farmos/commit-db.js (Phase 40 primitives; whole file already in context)
    - src/agents/alerter/src/farmos/commits/commit-router.js (LOG_TYPES guard, whole file)
    - src/agents/alerter/test/farmos/commit-db.test.js IF it exists, else look at existing farmos test files for pattern
  </read_first>
  <files>
    src/agents/alerter/src/farmos/logs.js
    src/agents/alerter/test/farmos/commit-db.test.js (new or extended)
    src/agents/alerter/test/farmos/commit-router.test.js (extend if exists; create if not)
  </files>
  <behavior>
    - logs.js: LOG_TYPES array now contains 'seeding_session' as a sixth member. Existing 5 entries unchanged, order preserved (seeding_session appended at the end so any positional consumers are unaffected — grep first for positional uses; if none, append).
    - commit-router test: when given draft={log_type:'seeding_session', draft_json:{...}}, the guard at line 27 of commit-router.js MUST NOT return unsupported_log_type. Since Plan 02 hasn't shipped the handler yet, asserting "the dispatch lookup happens" is sufficient; use a fake DISPATCH map injected via require-cache mutation, or check that LOG_TYPES.includes('seeding_session') === true and that commit() with no DISPATCH entry returns ok=false with reason !== 'unsupported_log_type' (i.e. it falls through to a different failure mode).
    - commit-db test: cache a synthetic multi-asset multi-log response via markCommitted(pool, id, {asset_ids:['asset-uuid-a'], log_ids:['log-uuid-1','log-uuid-2','log-uuid-3'], file_ids:[], http_status:201, latency_ms:42}). Read back via getCachedResponse. Assert farmos_response.asset_ids.length === 1, farmos_response.log_ids.length === 3. Use pg-mem or the project's existing test pool pattern (mirror commit-db tests if any exist; if none, use a minimal jest-mocked pool whose query() echoes back the JSONB).
    - Tests run hermetic: no live pg connection. Estimated <1s.
  </behavior>
  <action>
    Implements Gray Area D schema delta per CONTEXT.md (idempotency key + multi-asset/multi-log farmos_response shape). Per D-01 reconciliation note above: no new signal_commit table; reuse signal_draft. Append 'seeding_session' to LOG_TYPES. Write the two regression tests. Do not touch commit-router.js dispatch wiring (Plan 02 owns that). Do not touch commit-watchdog.js (the watchdog reads log_type as opaque and routes via commit-router; no change needed). Do not touch commit-outcome-preview.js label map (Plan 04 owns that).
  </action>
  <verify>
    <automated>cd src/agents/alerter && npx jest test/farmos/commit-db test/farmos/commit-router --no-coverage</automated>
  </verify>
  <done>LOG_TYPES contains seeding_session; commit-router guard accepts it; commit-db round-trips a 1-asset-3-log farmos_response unchanged; existing farmos/commits tests still green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| signal_draft row (alerter-owned) -> commit-router (alerter-internal) | Same trust zone; no new boundary crossed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-48-01-01 | Tampering | LOG_TYPES change accidentally enables an unwired log_type | mitigate | Plan 02 ships the handler before any prod draft can carry the new log_type; until then commit-router falls through to a defined non-success outcome (not unsupported_log_type, which would be a misleading code) |
| T-48-01-02 | Information disclosure | Cached farmos_response JSONB leaks asset UUIDs | accept | Same trust zone as existing log_ids; no new disclosure |
| T-48-01-SC | Tampering | No new packages | accept | Zero npm deps added |
</threat_model>

<verification>
- `npx jest test/farmos/commit-db test/farmos/commit-router` all green
- `grep -c "seeding_session" src/agents/alerter/src/farmos/logs.js` >= 1
- `grep -v '^#' src/agents/alerter/src/farmos/logs.js | grep -c "seeding_session"` >= 1
</verification>

<success_criteria>
LOG_TYPES allow-list extended; commit-db primitives proven to round-trip the multi-asset/multi-log response shape; commit-router guard does not bounce seeding_session drafts. Plan 02 can now safely wire the new handler without fighting the guard.
</success_criteria>

<output>
Create `.planning/phases/48-session-entity-per-bag-commit-fan-out-session-shaped-confirm/48-01-SUMMARY.md` recording: (1) the LOG_TYPES diff applied, (2) the schema-reconciliation note (no new signal_commit table; signal_draft is the idempotency surface), (3) test results.
</output>
