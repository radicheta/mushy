---
phase: 48-session-entity-per-bag-commit-fan-out-session-shaped-confirm
plan: 05
type: execute
wave: 4
depends_on: ['48-02', '48-03', '48-04']
files_modified:
  - src/agents/alerter/test/farmos/integration/seeding-session-commit-may22.test.js
  - src/agents/alerter/test/farmos/integration/seeding-session-commit-partial-fail.test.js
  - src/agents/alerter/test/farmos/integration/seeding-session-commit-idempotent.test.js
  - src/agents/alerter/test/fixtures/seeding-session-may22-commit/
autonomous: false
requirements: [INOC-04, INOC-05, INOC-06]
must_haves:
  truths:
    - "Hermetic end-to-end pipeline replay of the May 22 seeding_session draft through commit-watchdog -> commit-router -> commitSeedingSession -> mock farmOS client writes exactly 1 'inoc 2026-05-22' fungi asset + 5 source-block assets + 11 child-block assets + 11 seeding logs"
    - "Lineage walk assertion: for each of the 11 child-block assets in the captured mock-client POST payloads, the relationships.parent.data[] array has length 2 and contains both the source-block UUID (first) and session-asset UUID (second)"
    - "Double-YES is a no-op: second invocation of commit-watchdog tickOnce on the same draft produces zero new asset/log POSTs (idempotency probe early-returns); farmer receives no second ack (Plan 04 Test C path)"
    - "commit_failed fixture: stubbed-fail on child log #4 triggers reverse-order DELETE of 11 child blocks + 5 source blocks + 1 session asset (17 DELETEs), signal_draft.status transitions to 'commit_failed', send_commit_outcome_ack dispatched with reason='partial_commit_failed'"
    - "Live-fire path (operator-gated, EVAL_RUN_LIVE=1 + FARMOS_DEV_URL): the May 22 fixture commits against a real farmOS dev instance; operator-attested in 48-LIVE-FIRE.md with timestamp + asset UUIDs created + cleanup outcome"
    - "INOC-04 attested (single-parent legacy fixture): a 1-group 5-child draft commits cleanly with same session-asset + 5 children pattern"
  artifacts:
    - path: src/agents/alerter/test/farmos/integration/seeding-session-commit-may22.test.js
      provides: "Hermetic ship-gate for the 11-log + 1-asset happy path"
    - path: src/agents/alerter/test/farmos/integration/seeding-session-commit-partial-fail.test.js
      provides: "Hermetic ship-gate for the orphan-cleanup branch"
    - path: src/agents/alerter/test/farmos/integration/seeding-session-commit-idempotent.test.js
      provides: "Hermetic ship-gate for double-YES + ack-once"
    - path: .planning/phases/48-*/48-LIVE-FIRE.md
      provides: "Operator-attested live-fire paper trail (Task 2)"
  key_links:
    - from: test/farmos/integration/seeding-session-commit-may22.test.js
      to: src/agents/alerter/src/farmos/commit-watchdog.js + commits/commit-seeding-session.js
      via: "End-to-end tickOnce() call with a seeded confirmed signal_draft row + mock farmOS client"
      pattern: "tickOnce.*seeding_session"
---

<objective>
Ship-gate plan, mirroring Phase 47-05's hermetic + live-fire pattern. Three hermetic integration tests run in CI with a fully-mocked farmOS client + in-memory pg-style pool (or jest mock); a live-fire path is gated behind `EVAL_RUN_LIVE=1` + `FARMOS_DEV_URL` env vars and is operator-attested via 48-LIVE-FIRE.md.

Per `[[feedback_unit_tests_dont_catch_wiring]]` + the Phase 47-05 precedent: unit tests with mocks miss the wiring seams between commit-watchdog -> commit-router -> commit-seeding-session -> assets/logs/audit-logger. This plan exercises the whole producer-to-consumer chain in one process.

Autonomous: false — Task 2 is a `checkpoint:human-verify` for live-fire attestation against farmOS dev.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/48-session-entity-per-bag-commit-fan-out-session-shaped-confirm/48-CONTEXT.md
@.planning/phases/48-session-entity-per-bag-commit-fan-out-session-shaped-confirm/48-01-SUMMARY.md
@.planning/phases/48-session-entity-per-bag-commit-fan-out-session-shaped-confirm/48-02-SUMMARY.md
@.planning/phases/48-session-entity-per-bag-commit-fan-out-session-shaped-confirm/48-03-SUMMARY.md
@.planning/phases/48-session-entity-per-bag-commit-fan-out-session-shaped-confirm/48-04-SUMMARY.md
@.planning/phases/47-multi-source-extraction-fusion-groups-shape-inoc-draft/47-05-PLAN.md
@.planning/phases/47-multi-source-extraction-fusion-groups-shape-inoc-draft/47-LIVE-FIRE.md
@src/agents/alerter/src/farmos/commit-watchdog.js
@src/agents/alerter/src/farmos/commits/commit-seeding-session.js
@src/agents/alerter/test/extraction/integration/seeding-session-may22.test.js

<interfaces>
May 22 fixture (mirrored from Phase 47 — reuse where possible):
  draft_json: {
    type: 'seeding_session',
    event_date: '2026-05-22',
    groups: [
      { parent:{value:'260304_SHI_5'},  species:{value:'SHI'}, qty:{value:1}, child_block_names:{value:['260522_SHI_1']} },
      { parent:{value:'260118_SHI_23'}, species:{value:'SHI'}, qty:{value:1}, child_block_names:{value:['260522_SHI_2']} },
      { parent:{value:'260118_SHI_26'}, species:{value:'SHI'}, qty:{value:1}, child_block_names:{value:['260522_SHI_3']} },
      { parent:{value:'260118_KOY_12'}, species:{value:'KOY'}, qty:{value:4}, child_block_names:{value:['260522_KOY_4','260522_KOY_5','260522_KOY_6','260522_KOY_7']} },
      { parent:{value:'260425_KOY_4'},  species:{value:'KOY'}, qty:{value:4}, child_block_names:{value:['260522_KOY_8','260522_KOY_9','260522_KOY_10','260522_KOY_11']} },
    ],
  }

commit-watchdog.tickOnce(): the entry point that pulls confirmed signal_draft rows and routes them. In tests, instantiate via createCommitWatchdog({pool, farmosClient, commitRouter, outboundConfirm, auditLogger, clock, logger}). Inject all dependencies as mocks. Seed the pool's signal_draft query result with the May 22 row at status='confirmed'.

Live-fire (Task 2):
  Requires:
    - farmOS dev instance reachable at FARMOS_DEV_URL (per existing project memory; check `.env` or .planning notes for the canonical URL)
    - Bearer token via FARMOS_API_TOKEN
    - The 'session' fungi_type taxonomy term created in farmOS dev (one-time prereq from 48-02 SUMMARY)
  Estimated cost: zero (farmOS dev is local-network; no paid API)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Three hermetic end-to-end integration tests</name>
  <read_first>
    - 48-01-SUMMARY through 48-04-SUMMARY (the foundational pieces; understand what changed)
    - .planning/phases/47-multi-source-extraction-fusion-groups-shape-inoc-draft/47-05-PLAN.md (style and harness pattern to mirror)
    - src/agents/alerter/test/extraction/integration/seeding-session-may22.test.js (Phase 47 hermetic harness — reuse mock patterns)
    - src/agents/alerter/test/farmos/ (existing test layout; figure out where integration tests for the commit side live or should live)
    - src/agents/alerter/src/farmos/commit-watchdog.js (whole file)
    - src/agents/alerter/src/farmos/commits/commit-seeding-session.js (from 48-02)
  </read_first>
  <files>
    src/agents/alerter/test/farmos/integration/seeding-session-commit-may22.test.js (new)
    src/agents/alerter/test/farmos/integration/seeding-session-commit-partial-fail.test.js (new)
    src/agents/alerter/test/farmos/integration/seeding-session-commit-idempotent.test.js (new)
    src/agents/alerter/test/fixtures/seeding-session-may22-commit/draft.json (new — the canonical May 22 draft_json)
    src/agents/alerter/test/fixtures/seeding-session-may22-commit/expected-farmos-payloads.json (new — gold POST/DELETE payloads for assertion)
  </files>
  <behavior>
    SHARED HARNESS: each test file imports a small helper buildHarness() that:
      - Creates a mock farmOSClient with .post, .get, .patch, .delete methods, each a jest.fn() with configurable per-call responses
      - Creates a mock pg pool whose .query() routes by SQL substring (SELECT FROM signal_draft WHERE status='confirmed' returns a seeded row; UPDATE signal_draft sets internal status; etc.)
      - Wires the real commit-router + real commit-seeding-session (NOT mocked — that's the whole point of an integration test) but injects the mock farmOSClient
      - Wires a mock outboundConfirm whose .dispatch is a jest.fn()
      - Wires a mock auditLogger whose .logCommit is a jest.fn()
      - Returns { watchdog, pool, farmosClient, outboundConfirm, auditLogger, clock }

    Test file 1 (seeding-session-commit-may22.test.js — INOC-04 + INOC-06 happy path):
      - Seed pool with May 22 fixture at status='confirmed'.
      - Configure farmosClient: .get('/api/asset/fungi?filter[name][value]=...') returns {ok:true, body:{data:[]}} (nothing pre-exists); .post returns {ok:true, body:{data:{id:`mock-${callCount}`}}, status:201} for each call.
      - Run watchdog.tickOnce().
      - Assert: farmosClient.post called exactly 17 times for /api/asset/fungi (1 session + 5 source blocks + 11 child blocks) AND 11 times for /api/log/seeding. Assert call ORDER: session-asset POST is FIRST; each child-block-asset POST precedes its corresponding log POST (interleaved or batched depending on handler).
      - Assert payload SHAPE on each child-block POST: relationships.parent.data has length 2; data[0] is the source-block UUID (asset created earlier in this run); data[1] is the session-asset UUID.
      - Assert pool received UPDATE signal_draft SET status='committed' WHERE id=$1 AND status='committing'; farmos_response.asset_ids.length === 17; farmos_response.log_ids.length === 11.
      - Assert outboundConfirm.dispatch called once with ('send_commit_outcome_ack', row, {outcome:'success'}).
      - Assert NO farmosClient.delete calls (success path, no cleanup).

    Test file 2 (seeding-session-commit-partial-fail.test.js — orphan cleanup):
      - Same seed as Test 1.
      - Configure farmosClient: same as Test 1 but .post('/api/log/seeding') returns {ok:false, body:{}, status:422} on the 4th log POST (first three succeed).
      - Run watchdog.tickOnce() (retryMax tick configuration: 1 to force terminal failure on first attempt; OR run tickOnce twice).
      - Assert: farmosClient.delete called 17 times (in reverse order: child blocks first, then source blocks, then session asset). Note: only 4 child blocks were created by the time of failure (3 successful logs + 1 failed log = 4 child block assets minted before failure); so DELETE count = 4 child + 5 source + 1 session = 10, NOT 17. Adjust expectation to match actual handler behavior (re-read 48-02-SUMMARY to confirm exact cleanup set).
      - Assert pool received UPDATE signal_draft SET status='commit_failed' WHERE id=$1; commit_failed_reason='partial_commit_failed'.
      - Assert outboundConfirm.dispatch called once with ('send_commit_outcome_ack', row, {outcome:'failed', reason:'partial_commit_failed'}).
      - Assert auditLogger.logCommit called with type='commit_failed' AND (separately) NOT called with type='orphan_cleanup_failed' (DELETEs succeeded in this fixture).

    Test file 3 (seeding-session-commit-idempotent.test.js — INOC-05):
      - First call: same as Test 1 happy path. Capture the resulting farmos_response.
      - Manually update the seeded pool row to status='committed' with the captured farmos_response and outcome_ack_sent_at=now() (simulating a successful prior commit).
      - Second call: pool.query for findConfirmedCandidates returns [] (status='committed' is filtered out); BUT also test the alternate idempotency path where the row IS somehow re-presented (e.g. simulate Phase 45 idempotency probe at commit-watchdog.js line 77). In that case getCachedResponse returns the cached body and auditLogger.logCommit('commit_idempotent_noop', ...) is called.
      - Assert: farmosClient.post NOT called on the second tick (zero new writes); outboundConfirm.dispatch NOT called on the second tick (no double-ack).

    Each test runs hermetic, <3s. Use jest.useFakeTimers() if the watchdog has internal setInterval; or call tickOnce() directly.
  </behavior>
  <action>
    Build the three test files + shared harness. The integration tests are the truth-teller — they catch the wiring seams that 48-01..04 unit tests miss. Per the 47-05 precedent (which caught the live-fire ask-back path the unit tests missed), DO NOT skip any test even if it duplicates a Plan-02 unit test at first glance; the integration replay is the value.
  </action>
  <verify>
    <automated>cd src/agents/alerter && npx jest test/farmos/integration/seeding-session-commit --no-coverage</automated>
  </verify>
  <done>All three integration tests green in <10s total; producer-to-consumer chain proven end-to-end; orphan cleanup exercised; idempotency proven.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Live-fire ship-gate attestation against farmOS dev (operator-gated)</name>
  <read_first>
    - 48-01-SUMMARY through 48-04-SUMMARY (especially the session-asset fungi_type/xing resolution from 48-02)
    - .planning/phases/47-multi-source-extraction-fusion-groups-shape-inoc-draft/47-LIVE-FIRE.md (paper-trail format)
    - The project memory note about the farmOS dev URL + token location (search .planning/notes/ or .env for FARMOS_DEV_URL)
  </read_first>
  <files>
    src/agents/alerter/test/farmos/integration/seeding-session-commit-may22.test.js (add EVAL_RUN_LIVE branch only)
    .planning/phases/48-session-entity-per-bag-commit-fan-out-session-shaped-confirm/48-LIVE-FIRE.md (new — paper trail)
  </files>
  <what-built>
    Hermetic integration tests are green. The remaining question is whether the wiring talks to REAL farmOS dev correctly — that the session-asset POST shape is accepted by farmOS, that the parent[] array order is preserved on read-back, and that the orphan-cleanup DELETE path actually deletes the assets.
  </what-built>
  <how-to-verify>
    PREREQUISITE (one-time): confirm or create the 'session' fungi_type taxonomy term in farmOS dev. If 48-02 SUMMARY recorded the 'allowNoFungiType' path, this prereq is moot. If it recorded the 'session-as-fungi-type' path, verify the term exists:
      curl -s "$FARMOS_DEV_URL/api/taxonomy_term/fungi_type?filter[name][value]=session" -H "Authorization: Bearer $FARMOS_API_TOKEN" | jq '.data | length'
    If it returns 0, create the term via the farmOS UI or POST /api/taxonomy_term/fungi_type with name='session'.

    1. Run live-fire:
       cd src/agents/alerter && EVAL_RUN_LIVE=1 FARMOS_DEV_URL=$FARMOS_DEV_URL FARMOS_API_TOKEN=$FARMOS_API_TOKEN npx jest test/farmos/integration/seeding-session-commit-may22.test.js --no-coverage
    2. Capture the test output (asset UUIDs created, log UUIDs created, total elapsed time).
    3. Query farmOS dev to verify the writes:
       curl -s "$FARMOS_DEV_URL/api/asset/fungi?filter[name][value]=inoc 2026-05-22" -H "Authorization: Bearer $FARMOS_API_TOKEN" | jq '.data[0].id'
       (Save the session asset UUID for next step.)
    4. Query the lineage walk: pick any child block (e.g. 260522_KOY_7):
       curl -s "$FARMOS_DEV_URL/api/asset/fungi?filter[name][value]=260522_KOY_7&include=parent" -H "Authorization: Bearer $FARMOS_API_TOKEN" | jq '.data[0].relationships.parent.data'
       Expected: an array of 2 entries; the first SHOULD resolve to the 260118_KOY_12 asset, the second to the inoc 2026-05-22 session asset.
    5. Query the session-asset's children:
       (farmOS does not natively expose children; use a parent filter on assets)
       curl -s "$FARMOS_DEV_URL/api/asset/fungi?filter[parent.id]=<session-asset-uuid>" -H "Authorization: Bearer $FARMOS_API_TOKEN" | jq '.data | length'
       Expected: 11.
    6. (Optional) Re-run the same test a second time WITHOUT clearing farmOS. Expected: name-collision branch fires, session asset created as 'inoc 2026-05-22 #2'. This is informative — document the result.
    7. Clean up: DELETE the 17 assets + 11 logs (or run the partial-fail fixture in live mode to exercise the cleanup branch — operator's call).
    8. Save the full transcript (test stdout + curl outputs from steps 3-6) to .planning/phases/48-*/48-LIVE-FIRE.md with timestamp + verdict + any deviations from hermetic expectations.

    Mirror the 47-LIVE-FIRE.md format. If anything deviates from the hermetic spec (e.g. farmOS rejects the parent[] array order; the 'session' term lookup fails; orphan cleanup leaves dangling assets), FAIL and open a Phase 49 follow-up; do NOT silently fix in 48-05 (that's a scope creep — fix in 48-02 + re-run).
  </how-to-verify>
  <resume-signal>Type "live-fire PASS" + paste the resulting LIVE-FIRE.md verdict + session-asset UUID, or "live-fire FAIL" + describe the deviation</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Live-fire alerter -> farmOS dev | Bearer-token auth; live writes to dev instance (revertible) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-48-05-01 | Tampering | Live-fire pollutes farmOS dev with test assets | mitigate | Operator cleans up after attestation (step 7); 48-LIVE-FIRE.md records all UUIDs for sweep |
| T-48-05-02 | Repudiation | Live-fire result not preserved | mitigate | Paper-trail to 48-LIVE-FIRE.md per [[feedback_keep_paper_trail_of_intermediates]] |
| T-48-05-03 | Tampering | Mock-vs-live drift on farmOS payload shape | mitigate | Task 2 IS the proof of mock-vs-live agreement |
| T-48-05-SC | Tampering | No new packages | accept | Zero npm deps added |
</threat_model>

<verification>
- Task 1: `npx jest test/farmos/integration/seeding-session-commit` all 3 tests green hermetically in <10s
- Task 2: `.planning/phases/48-*/48-LIVE-FIRE.md` exists with timestamp + verdict + session-asset UUID + lineage-walk JSON snippet
- `grep -c "260522_SHI\|260522_KOY" .planning/phases/48-*/48-LIVE-FIRE.md` >= 11
</verification>

<success_criteria>
INOC-04 (single-parent legacy still ships via session-asset shape) + INOC-05 (no silent failure post-YES) + INOC-06 (session asset + per-bag fan-out + lineage encoding) all have BOTH a hermetic CI regression guard AND an operator-attested live-fire run against farmOS dev. Mock-vs-live agreement proven. The May-22 ship-gate-style replay of the production scenario writes the expected 17 assets + 11 logs in farmOS dev and the lineage walks return the expected shape.

Phase 49 takes over for the REAL ship gate: reprocessing the original 2026-05-22 production capture (via Phase 47's extraction) through Phase 48's commit, into farmOS dev as the final operator-attested v1.9 milestone.
</success_criteria>

<output>
Create `.planning/phases/48-session-entity-per-bag-commit-fan-out-session-shaped-confirm/48-05-SUMMARY.md` recording: (1) Task 1 hermetic test results, (2) Task 2 live-fire timestamp + verdict + UUIDs + cleanup outcome, (3) any deviations between mock and live behavior, (4) explicit attestation that INOC-04/05/06 are all covered, (5) explicit handoff note to Phase 49 (real-corpus eval + May 22 prod reprocess).
</output>
