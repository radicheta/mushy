---
phase: 56-foundation
plan: 02
type: execute
wave: 2
depends_on: ["56-01"]
files_modified:
  - src/farm-agent/farm_agent/tenancy/tenant.py
  - src/farm-agent/tests/test_tenancy.py
autonomous: true
requirements: [FND-02]
must_haves:
  truths:
    - "TenantConfig.load(env) layers tenant YAML, then env, then hardcoded defaults"
    - "Secrets (ANTHROPIC_API_KEY, TIMESCALE_PASSWORD, SIGNAL_SENDER, FARMOS_PASSWORD) resolve from env only and raise RuntimeError when missing"
    - "Tenant file loader rejects path traversal outside the tenants/ base dir"
    - "Only tenancy/tenant.py reads os.environ in the whole package (boot.py excepted)"
  artifacts:
    - path: "src/farm-agent/farm_agent/tenancy/tenant.py"
      provides: "TenantConfig dataclass + load(env) layered loader + _must_env + path-traversal guard"
      contains: "class TenantConfig"
    - path: "src/farm-agent/tests/test_tenancy.py"
      provides: "unit coverage for layering, missing-secret raise, traversal guard"
      contains: "def test_"
  key_links:
    - from: "farm_agent/tenancy/tenant.py"
      to: "tenants/<id>/config.yaml + strains.yaml"
      via: "ruamel.yaml load under a resolved-path boundary check"
      pattern: "TENANTS_BASE"
---

<objective>
Port the Node `config.js` to `tenancy/TenantConfig` — the lowest node in the dependency graph and the SOLE reader of `os.environ` in business code (FND-02). Layered YAML+env+default loading; secrets env-only; path-traversal guard.

Purpose: every downstream package receives config by injection; no module reaches for env. Persistence (Plan 05) and boot (Plan 06) consume `TenantConfig`.
Output: `tenant.py` + unit tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/56-foundation/56-CONTEXT.md
@.planning/phases/56-foundation/56-RESEARCH.md
@.planning/phases/56-foundation/56-PATTERNS.md

<interfaces>
<!-- Authoritative shapes; do not explore the codebase for these. -->

TenantConfig dataclass field list: 56-RESEARCH.md "FND-02 > TenantConfig Dataclass" (frozen dataclass,
~30 fields; secrets vs non-secrets split listed).

Node analog patterns to port (mustEnv, layered pick, path-traversal boundary, farmer-map parse):
56-PATTERNS.md "tenancy/tenant.py (utility)" section quotes the exact config.js lines.

Layer order: tenant YAML -> env -> hardcoded default. Secrets via _must_env(env, key) ONLY.

Tenant config keys present in tenants/mossrock/config.yaml: SIGNAL_FARMER_MAP (object form),
SIGNAL_RECIPIENT, SIGNAL_GROUP_ID, FARMOS_URL, FARMOS_USERNAME, FARMOS_INTEGRATION,
EVENT_GATE_CONVO_MODE. Read tenants/mossrock/config.yaml + strains.yaml for the full live surface.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Port TenantConfig loader from config.js</name>
  <files>src/farm-agent/farm_agent/tenancy/tenant.py, src/farm-agent/tests/test_tenancy.py</files>
  <read_first>
    - 56-PATTERNS.md "tenancy/tenant.py (utility)" (mustEnv / pick / loadTenantFile / resolveFarmerMap quoted from config.js)
    - 56-RESEARCH.md "FND-02: TenantConfig" (dataclass field list, layer order, secrets-env-only rule)
    - src/agents/alerter/src/config.js (the full port target — layer order, defaults, farmer-map object form)
    - tenants/mossrock/config.yaml + tenants/mossrock/strains.yaml (live tenant surface)
  </read_first>
  <behavior>
    - load(env) with a tenant YAML value present returns the YAML value (highest layer).
    - load(env) with no YAML but env set returns the env value; with neither returns the hardcoded default.
    - Missing a required secret (e.g. TIMESCALE_PASSWORD absent) raises RuntimeError naming the key.
    - _load_tenant_file with a traversal filename (e.g. ../../etc/passwd) returns {} (boundary rejected).
    - SIGNAL_FARMER_MAP object form (e164 -> slug) parses to dict[str,str]; missing yields {}.
    - FARMOS_INTEGRATION coerces to bool; numeric fields (pi_offline_min, etc.) coerce to int/float.
  </behavior>
  <action>
    Create `farm_agent/tenancy/tenant.py` with a frozen `TenantConfig` dataclass carrying the full field set from RESEARCH FND-02 (secrets: signal_sender, timescale_password, anthropic_api_key, farmos_password; non-secrets: signal_recipient, signal_group_id (str|None), signal_farmer_map (dict[str,str]), strains (list[str]), event_gate_convo_mode, farmos_url, farmos_username, farmos_integration (bool), timescale_host/db/user, plus the numeric tuning fields). Implement module-level `TENANTS_BASE = Path(__file__).parent.parent.parent.parent / "tenants"`; `_must_env(env, key)` raising RuntimeError on falsy; `_load_tenant_file(tenant_id, filename)` resolving the path and returning empty dict unless the resolved path stays under TENANTS_BASE AND exists (ruamel.yaml YAML().load); `_pick(tenant_cfg, env, key, default)`; and a `load(env) -> TenantConfig` that reads TENANT_ID, loads config.yaml + strains.yaml, then assembles every field via `_pick`/`_must_env` per the layer order. Port the farmer-map object-form parse from config.js. This module is the ONLY place env is read.
    Write `tests/test_tenancy.py` covering each behavior above. Use synthetic env dicts and a tmp tenants dir (or monkeypatch TENANTS_BASE) — do NOT depend on the live tenants/mossrock files for assertions about defaults.
  </action>
  <verify>
    <automated>cd src/farm-agent && uv run pytest tests/test_tenancy.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_tenancy.py -q` passes with coverage of: layer precedence, missing-secret RuntimeError, traversal-guard returns empty dict, farmer-map parse.
    - `class TenantConfig` is a frozen dataclass; `load` is exported.
    - The FND-02 static gate holds: grep for os.environ across farm_agent excluding tenancy/tenant.py and boot.py returns empty (no business module reads env). At this point only tenant.py exists, so the gate is trivially green; it stays green as later plans land.
  </acceptance_criteria>
  <done>TenantConfig loads layered config, enforces env-only secrets, rejects traversal, and is the sole env reader.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| env -> process | secrets arrive via env (compose env_file); never persisted to YAML |
| tenant_id -> filesystem | tenant_id selects a path under tenants/; traversal risk |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-56-02-01 | Tampering | _load_tenant_file path | mitigate | Resolve path and require it stays under TENANTS_BASE; return empty dict otherwise. Unit test with a traversal filename asserts empty. |
| T-56-02-02 | Information Disclosure | secrets in YAML / logs | mitigate | Secrets resolved only via _must_env from env; never read from tenant YAML; TenantConfig is not logged at boot (Plan 06 logs "boot complete" only, no config dump). |
| T-56-02-03 | Denial of Service | malformed tenant YAML | accept | ruamel.yaml parse failure surfaces at boot (fail-fast); low risk, operator-controlled file. |
</threat_model>

<verification>
- Layered loading + secret-raise + traversal guard unit-tested.
- FND-02 env-reader gate green.
</verification>

<success_criteria>
TenantConfig is the single config primitive; secrets env-only; no business module reads env.
</success_criteria>

<output>
Create `.planning/phases/56-foundation/56-02-SUMMARY.md` when done.
</output>
