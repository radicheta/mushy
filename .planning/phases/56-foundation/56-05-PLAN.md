---
phase: 56-foundation
plan: 05
type: execute
wave: 3
depends_on: ["56-01", "56-02"]
files_modified:
  - src/farm-agent/farm_agent/persistence/pool.py
  - src/farm-agent/farm_agent/persistence/migrations.py
  - src/farm-agent/tests/test_persistence.py
autonomous: true
requirements: [FND-03]
must_haves:
  truths:
    - "build_pool returns an opened psycopg3 AsyncConnectionPool with connection-level timezone=UTC"
    - "run_migrations creates signal_capture, signal_draft, signal_outbound (+ pgcrypto, v_llm_cost_daily) with the full live column set"
    - "Running run_migrations twice against the same DB is a no-op (idempotent)"
    - "The migration runner issues NO DROP/TRUNCATE/ALTER COLUMN TYPE (except the safe text->text no-op) — additive-only vs the live Node schema"
  artifacts:
    - path: "src/farm-agent/farm_agent/persistence/pool.py"
      provides: "build_pool(config) -> AsyncConnectionPool(open=False) then await open()"
      contains: "AsyncConnectionPool"
    - path: "src/farm-agent/farm_agent/persistence/migrations.py"
      provides: "run_migrations + per-table additive CREATE/ADD COLUMN IF NOT EXISTS"
      contains: "CREATE TABLE IF NOT EXISTS"
    - path: "src/farm-agent/tests/test_persistence.py"
      provides: "pool roundtrip + idempotent-migration + additive-only-guard tests"
      contains: "def test_"
  key_links:
    - from: "farm_agent/persistence/pool.py"
      to: "farm_agent.tenancy.tenant.TenantConfig"
      via: "conninfo built from injected config (no os.environ)"
      pattern: "TenantConfig"
---

<objective>
Build `persistence/`: the shared psycopg3 async pool and the idempotent additive-only migrations covering the live tables (`signal_capture`, `signal_draft`, `signal_outbound`), ported verbatim from the Node alerter DDL (FND-03). This is the layer that touches the SHARED live TimescaleDB the Node stack still reads until Phase 65 cutover — additive-only is a hard safety gate.

Purpose: boot (Plan 06) opens this pool and runs these migrations; all future packages get the pool by injection.
Output: pool.py + migrations.py + integration tests against a throwaway Postgres.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/56-foundation/56-CONTEXT.md
@.planning/phases/56-foundation/56-RESEARCH.md
@.planning/phases/56-foundation/56-PATTERNS.md

<interfaces>
<!-- Authoritative shapes; do not explore the codebase for these. -->

build_pool shape (open=False then await open, options=-c timezone=UTC, min 1 max 5):
56-RESEARCH.md "FND-03 > AsyncConnectionPool Setup" + 56-PATTERNS.md "persistence/pool.py".

Complete table/column inventory (every column, type, nullability, default, index, the pgcrypto
extension, the v_llm_cost_daily view): 56-RESEARCH.md "FND-03 > Complete Table/Column Inventory"
(signal_capture, signal_draft, signal_outbound). The Node DDL source files to port verbatim:
src/agents/alerter/src/capture-db.js, extraction-db.js, outbound-db.js, farmos/commit-db.js.
NOTE: commit-db.js ALTERs all target signal_draft (farmos_response, committed_at,
commit_failed_reason, commit_attempt_count, committed_at_attempt, outcome_ack_sent_at,
idx_signal_draft_status_confirmed); extraction-db.js has no DDL of its own. So the migration set is
exactly three tables + pgcrypto + the view.

CRITICAL id types (PITFALLS / RESEARCH): signal_capture.id = text (ULID); signal_draft.id = text
(hex SHA-256); signal_outbound.id = uuid (gen_random_uuid, needs pgcrypto);
signal_outbound.related_capture_id / related_draft_id = text NOT uuid (hotfix). signal_msg_ts/quote_msg_ts
= bigint. The signal_outbound text->text ALTER COLUMN statements are idempotent no-ops — include them.

TenantConfig from Plan 02: build_pool receives a TenantConfig and reads timescale_host/db/user/password.
conftest TEST_ENV + pool fixture from Plan 01: tests use the session-scoped pool fixture.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: psycopg3 async pool</name>
  <files>src/farm-agent/farm_agent/persistence/pool.py, src/farm-agent/tests/test_persistence.py</files>
  <read_first>
    - 56-RESEARCH.md "FND-03 > AsyncConnectionPool Setup" + Pitfall 4 (open=False required for asyncio)
    - 56-PATTERNS.md "persistence/pool.py" (open=False then await pool.open(); options=-c timezone=UTC)
    - 56-RESEARCH.md "Environment Availability" (throwaway postgres:14 on :5434 for tests)
  </read_first>
  <behavior>
    - build_pool(config) returns an AsyncConnectionPool that is already open.
    - A SELECT 1 roundtrip through the pool returns 1.
    - The connection session timezone is UTC (SHOW timezone returns UTC).
  </behavior>
  <action>
    Create `farm_agent/persistence/pool.py` with `async def build_pool(config: TenantConfig) -> AsyncConnectionPool`: build conninfo from config.timescale_host/db/user/password plus `options=-c timezone=UTC`, construct `AsyncConnectionPool(conninfo=..., min_size=1, max_size=5, open=False)`, then `await pool.open()` and return it (open=False is mandatory for async-safe init per Pitfall 4). Import TenantConfig from tenancy (injection only — no os.environ). Add `tests/test_persistence.py::test_pool_roundtrip` using the conftest `pool` fixture: open a connection, `SELECT 1`, assert 1; `SHOW timezone` returns UTC. Tests require a reachable test Postgres (throwaway postgres:14 on :5434 or TEST_TIMESCALE_* env); if unreachable the test should skip with a clear reason (not silently pass).
  </action>
  <verify>
    <automated>cd src/farm-agent && uv run pytest tests/test_persistence.py::test_pool_roundtrip -q</automated>
  </verify>
  <acceptance_criteria>
    - test_pool_roundtrip passes against a reachable test Postgres (SELECT 1 == 1; timezone UTC), or skips with an explicit "no test DB reachable" reason.
    - pool.py constructs the pool with open=False and calls await pool.open(); no os.environ in the module.
  </acceptance_criteria>
  <done>Async pool opens UTC-pinned, roundtrips, and takes config by injection.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Idempotent additive-only migrations for all three tables</name>
  <files>src/farm-agent/farm_agent/persistence/migrations.py, src/farm-agent/tests/test_persistence.py</files>
  <read_first>
    - 56-RESEARCH.md "FND-03 > Complete Table/Column Inventory" (every column/type/index for all three tables)
    - 56-PATTERNS.md "persistence/migrations.py" (the ported DDL + additive-only constraint + pgcrypto first + partial unique index literal)
    - src/agents/alerter/src/capture-db.js, outbound-db.js, farmos/commit-db.js (verbatim DDL source)
  </read_first>
  <behavior>
    - run_migrations against a blank DB creates signal_capture, signal_draft, signal_outbound with the full column set from the inventory (verify a representative column on each table exists with the right type, e.g. signal_outbound.related_capture_id is text, signal_capture.id is text, signal_outbound.id is uuid).
    - The pgcrypto extension and v_llm_cost_daily view exist after migration.
    - Running run_migrations a SECOND time raises no error and changes nothing (idempotent).
    - The migrations.py source contains NO DROP / TRUNCATE / ALTER COLUMN ... TYPE except the documented signal_outbound text->text no-op.
  </behavior>
  <action>
    Create `farm_agent/persistence/migrations.py` with `run_migrations(pool)` opening one connection and calling `_run_capture_migrations`, `_run_draft_migrations`, `_run_outbound_migrations`, `_run_commit_migrations` in sequence. Port each table's DDL verbatim from the inventory: CREATE TABLE IF NOT EXISTS with the base columns, then ADD COLUMN IF NOT EXISTS for every Phase 37/40/44/45/49/50/53/999.53 column, then CREATE INDEX IF NOT EXISTS for each index. `_run_outbound_migrations` MUST start with `CREATE EXTENSION IF NOT EXISTS pgcrypto` and include the two idempotent `ALTER TABLE signal_outbound ALTER COLUMN related_capture_id/related_draft_id TYPE text` no-op statements (commented as compat for hosts that ran the original uuid schema). Use the exact partial-index predicate string literals from the Node source (e.g. status IN ('pending','awaiting_farmer')). Include the v_llm_cost_daily CREATE OR REPLACE VIEW. The commit/audit columns are the signal_draft ALTERs from commit-db.js (farmos_response, committed_at, commit_failed_reason, commit_attempt_count DEFAULT 0, committed_at_attempt, outcome_ack_sent_at + idx_signal_draft_status_confirmed) — fold them into `_run_commit_migrations`.
    Add `tests/test_persistence.py::test_migrations_idempotent` (run twice, no error, table list unchanged) and `::test_migrations_create_expected_tables` (assert the three tables + pgcrypto + view exist with representative column types). Add `::test_migrations_additive_only` as a SOURCE assertion: read migrations.py and assert it contains no `DROP `, no `TRUNCATE`, and no `ALTER COLUMN` ... `TYPE` other than the two whitelisted text->text lines. NOTE: `test_migrations_additive_only` is DB-independent (pure source grep) and MUST NEVER be skipped — apply `pytest.mark.skipif(no test DB)` ONLY to `test_migrations_idempotent` and `test_migrations_create_expected_tables`. The additive-only guard protects the shared LIVE Node DB and must run in CI even with no test Postgres reachable.
  </action>
  <verify>
    <automated>cd src/farm-agent && uv run pytest tests/test_persistence.py -q</automated>
  </verify>
  <acceptance_criteria>
    - All test_persistence.py tests pass; `test_migrations_additive_only` (source grep, DB-independent) ALWAYS runs and passes. Only `test_migrations_idempotent` and `test_migrations_create_expected_tables` may skip (with explicit reason) when no test DB is reachable: tables created with correct types, second run is a no-op, source has no destructive DDL.
    - `grep -nE "DROP |TRUNCATE|ALTER COLUMN .* TYPE" src/farm-agent/farm_agent/persistence/migrations.py` shows only the two whitelisted signal_outbound text->text lines (and nothing else).
    - `grep -c "CREATE TABLE IF NOT EXISTS" src/farm-agent/farm_agent/persistence/migrations.py` returns 3.
    - `grep -c "CREATE EXTENSION IF NOT EXISTS pgcrypto" src/farm-agent/farm_agent/persistence/migrations.py` returns 1.
  </acceptance_criteria>
  <done>All three live tables reproduced additively and idempotently; the additive-only safety gate is asserted in source; the shared Node schema is never broken.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Python migrations -> shared live TimescaleDB | the Node alerter + bridge read the same DB until Phase 65 cutover |
| config -> DB connection | credentials injected via TenantConfig; UTC enforced at connection |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-56-05-01 | Tampering / Denial of Service | destructive DDL against shared DB | mitigate (HIGH) | Additive-only: migrations issue only CREATE/ADD COLUMN/CREATE INDEX IF NOT EXISTS + CREATE OR REPLACE VIEW. test_migrations_additive_only asserts the source has NO DROP/TRUNCATE/ALTER COLUMN TYPE (except whitelisted text->text no-op). A destructive statement would take down the live Node alerter — this gate blocks it. |
| T-56-05-02 | Tampering | naive (non-UTC) datetime inserts | mitigate | options=-c timezone=UTC at connection level (defense-in-depth; Phase 58+ also uses datetime.now(timezone.utc)). |
| T-56-05-03 | Information Disclosure | DB password in conninfo / logs | mitigate | Password comes from TenantConfig (env-only); conninfo is not logged; boot logs only "boot complete". |
| T-56-05-04 | Denial of Service | brief lock from text->text ALTER on active table | accept | No-op cast on a low-volume table; documented inline; negligible for this workload (RESEARCH A6). |
</threat_model>

<verification>
- Pool opens UTC-pinned and roundtrips.
- Three live tables + pgcrypto + view created additively; second run no-op.
- Source-level additive-only guard asserted.
</verification>

<success_criteria>
A blank test DB gains all expected tables with correct column types; re-running migrations is a no-op; the runner issues zero destructive DDL against the shared schema.
</success_criteria>

<output>
Create `.planning/phases/56-foundation/56-05-SUMMARY.md` when done.
</output>
