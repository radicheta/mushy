---
phase: 56-foundation
plan: 03
type: execute
wave: 2
depends_on: ["56-01"]
files_modified:
  - src/farm-agent/tests/test_foray_seam.py
  - src/farm-agent/.lint-imports
autonomous: true
requirements: [FND-05]
must_haves:
  truths:
    - "A pytest fails the build the moment any non-chamber package imports from chamber/"
    - "The seam test catches both the `from farm_agent.chamber import X` and `import farm_agent.chamber` forms"
    - "With no chamber/ package present, the seam test passes (vacuously) and stays armed"
  artifacts:
    - path: "src/farm-agent/tests/test_foray_seam.py"
      provides: "grep-based Foray seam gate runnable in pytest"
      contains: "chamber"
    - path: "src/farm-agent/.lint-imports"
      provides: "import-linter forbidden-contract config (optional secondary gate)"
      contains: "forbidden"
  key_links:
    - from: "tests/test_foray_seam.py"
      to: "farm_agent/ source tree"
      via: "subprocess grep -rE for chamber imports, excluding chamber/ dir"
      pattern: "from farm_agent.chamber"
---

<objective>
Implement the FND-05 Foray seam: a static CI gate that FAILS the build if any non-`chamber` package imports from `chamber/`. Primary mechanism is a zero-dependency grep wired into pytest; the import-linter config is committed as a secondary gate for when `chamber/` lands in Phase 63.

Purpose: the Foray island boundary is the milestone's load-bearing architectural invariant (`chamber/` is the sole mushy-private package). It must be armed in Phase 56 so it trips the instant a violation appears.
Output: `test_foray_seam.py` + `.lint-imports`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/56-foundation/56-CONTEXT.md
@.planning/phases/56-foundation/56-RESEARCH.md
@.planning/phases/56-foundation/56-PATTERNS.md

<interfaces>
<!-- Authoritative shapes; do not explore the codebase for these. -->

Grep approach + the test_foray_seam.py shape: 56-RESEARCH.md "FND-05: Foray CI Seam" (Option B grep,
plus the import-linter Option A .lint-imports contract). The grep must use the -rE regex form catching
BOTH `^import farm_agent.chamber` and `from farm_agent.chamber`, excluding the chamber/ dir.

ROADMAP success criterion 4 phrases the token as `from alerter.chamber`; the actual package is
`farm_agent` (D-03), so the real import to forbid is `farm_agent.chamber`. The test asserts on the
real package name; note this divergence in a comment so a future reader is not confused.

FORAY_PACKAGES list (Phase 56 dirs that exist): farm_agent/tenancy, farm_agent/persistence,
farm_agent/extraction. signal_io/confirm/farmos_client/capture/llm are not created this phase.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire the Foray seam grep gate into pytest + commit import-linter config</name>
  <files>src/farm-agent/tests/test_foray_seam.py, src/farm-agent/.lint-imports</files>
  <read_first>
    - 56-RESEARCH.md "FND-05: Foray CI Seam" (grep Option B exact regex; import-linter Option A contract; the test_foray_seam.py reference)
  </read_first>
  <behavior>
    - With no chamber/ package present, the seam test PASSES (no violating import exists).
    - A synthetic violating import (`from farm_agent.chamber import x`) placed in a tmp foray file would make the grep match and the test FAIL — assert this by running the grep against a tmp fixture string in the test, OR by a second test that greps a constructed temp tree.
    - The grep matches both the `from farm_agent.chamber ...` and `import farm_agent.chamber` forms.
  </behavior>
  <action>
    Create `tests/test_foray_seam.py` with `test_no_chamber_imports_in_foray`: run `grep -rE` for the pattern matching `^import farm_agent\.chamber` OR `from farm_agent\.chamber`, with `--include=*.py`, over the existing foray package dirs (farm_agent/tenancy, farm_agent/persistence, farm_agent/extraction), excluding the chamber/ dir. Assert the gate is satisfied when grep finds nothing (returncode != 0 or empty stdout). Add a second test `test_seam_trips_on_violation` that constructs a tmp dir with a file containing `from farm_agent.chamber import x`, runs the same grep against it, and asserts the grep MATCHES (proving the gate is armed, not vacuous). Add a comment recording the ROADMAP-vs-actual token divergence (`alerter.chamber` in the success criterion vs the real `farm_agent.chamber`).
    Create `src/farm-agent/.lint-imports` with the import-linter `forbidden` contract from RESEARCH (root_packages = farm_agent; forbidden_modules = farm_agent.chamber; source_modules = the foray packages). This is committed now as the Phase-63 secondary gate; the grep test is the primary Phase-56 gate. Do not add import-linter to the pytest run yet (chamber/ does not exist, so the contract is inert).
  </action>
  <verify>
    <automated>cd src/farm-agent && uv run pytest tests/test_foray_seam.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_foray_seam.py -q` passes: the no-violation test is green AND the armed-gate test proves the grep matches a synthetic `from farm_agent.chamber` import.
    - `grep -c "chamber" src/farm-agent/tests/test_foray_seam.py` returns at least 1.
    - `grep -c "forbidden" src/farm-agent/.lint-imports` returns at least 1.
    - The seam test does not import any production module (pure subprocess grep), so it cannot itself introduce a violation.
  </acceptance_criteria>
  <done>Foray seam gate armed in pytest; trips on a synthetic chamber import; import-linter config staged for Phase 63.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| package -> package | Foray island boundary; chamber/ is the only mushy-private package |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-56-03-01 | Tampering | accidental cross-package import of chamber/ | mitigate | grep gate in pytest fails the build on any `farm_agent.chamber` import from a foray package; armed-gate test proves it is not vacuous. |
| T-56-03-02 | Repudiation | grep misses the bare `import` form | mitigate | Regex covers both `^import farm_agent.chamber` and `from farm_agent.chamber` (RESEARCH Option B fix). |
</threat_model>

<verification>
- Seam test green with no chamber/; armed-gate test proves it trips on a synthetic violation.
- .lint-imports forbidden contract committed for Phase 63.
</verification>

<success_criteria>
The Foray boundary is statically enforced and armed from Phase 56 onward.
</success_criteria>

<output>
Create `.planning/phases/56-foundation/56-03-SUMMARY.md` when done.
</output>
