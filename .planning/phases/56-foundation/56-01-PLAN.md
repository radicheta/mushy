---
phase: 56-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/farm-agent/pyproject.toml
  - src/farm-agent/.python-version
  - src/farm-agent/uv.lock
  - src/farm-agent/Dockerfile
  - src/farm-agent/farm_agent/__init__.py
  - src/farm-agent/farm_agent/tenancy/__init__.py
  - src/farm-agent/farm_agent/persistence/__init__.py
  - src/farm-agent/farm_agent/extraction/__init__.py
  - src/farm-agent/farm_agent/extraction/schemas/__init__.py
  - src/farm-agent/tests/__init__.py
  - src/farm-agent/tests/conftest.py
  - src/farm-agent/tests/fixtures/submission_json_schema.json
autonomous: true
requirements: [FND-01]
must_haves:
  truths:
    - "uv sync resolves and writes a committed uv.lock with pydantic, psycopg[binary], psycopg-pool, ruamel.yaml + dev tools"
    - "The farm_agent package and its Foray sub-packages import cleanly under Python 3.12"
    - "The Node SUBMISSION_JSON_SCHEMA is committed as a deterministic JSON fixture for the FND-04 parity gate"
    - "conftest.py exposes a TEST_ENV dict and an async pool fixture stub other plans build on"
  artifacts:
    - path: "src/farm-agent/pyproject.toml"
      provides: "uv project + pytest-asyncio auto mode + ruff config"
      contains: "asyncio_mode"
    - path: "src/farm-agent/Dockerfile"
      provides: "python:3.12-slim uv build"
      contains: "python:3.12-slim"
    - path: "src/farm-agent/tests/fixtures/submission_json_schema.json"
      provides: "committed Node zod JSON Schema fixture"
      contains: "definitions"
    - path: "src/farm-agent/tests/conftest.py"
      provides: "shared TEST_ENV + async pool fixture scaffold"
      contains: "TEST_ENV"
  key_links:
    - from: "src/farm-agent/Dockerfile"
      to: "pyproject.toml + uv.lock"
      via: "COPY then uv sync --no-dev"
      pattern: "uv sync"
---

<objective>
Scaffold the `src/farm-agent/` uv project: the package layout (Foray sub-packages as empty `__init__.py`), the pyproject/uv lock, the python:3.12-slim Dockerfile, the pytest conftest scaffold, and the committed Node JSON-Schema fixture that the FND-04 parity gate (Plan 04) diffs against.

Purpose: every other Phase 56 plan imports from `farm_agent.*`, runs under this pytest config, or (Plan 04) reads the committed fixture. This is the Wave-0 foundation.
Output: a buildable, importable, testable empty skeleton + the fixture file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/56-foundation/56-CONTEXT.md
@.planning/phases/56-foundation/56-RESEARCH.md
@.planning/phases/56-foundation/56-PATTERNS.md

<interfaces>
<!-- Authoritative shapes the executor must reproduce. Do not explore the codebase for these. -->

pyproject.toml + Dockerfile shapes: 56-RESEARCH.md "FND-01" section and 56-PATTERNS.md
"pyproject.toml (config)" / "Dockerfile (config)" sections (verbatim shapes given there).

The conftest async-pool fixture + TEST_ENV dict: 56-PATTERNS.md "tests/conftest.py (test)" section.

Fixture generation: 56-RESEARCH.md "Generating the Fixture" — a one-time Node command run from
src/agents/alerter/ that dumps SUBMISSION_JSON_SCHEMA to JSON. Key facts the fixture MUST satisfy:
top-level {"$ref": "#/definitions/Submission", "definitions": {...}, "$schema": ".../draft-07#"};
uses "definitions" (not "$defs"); "anyOf" for the 6-member draft union; "additionalProperties": false
present on every object; ~6155 chars.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the uv project + package skeleton + Dockerfile</name>
  <files>src/farm-agent/pyproject.toml, src/farm-agent/.python-version, src/farm-agent/uv.lock, src/farm-agent/Dockerfile, src/farm-agent/farm_agent/__init__.py, src/farm-agent/farm_agent/tenancy/__init__.py, src/farm-agent/farm_agent/persistence/__init__.py, src/farm-agent/farm_agent/extraction/__init__.py, src/farm-agent/farm_agent/extraction/schemas/__init__.py, src/farm-agent/tests/__init__.py</files>
  <read_first>
    - 56-RESEARCH.md "FND-01: asyncio Skeleton + Docker" (pyproject + Dockerfile shapes)
    - 56-PATTERNS.md "pyproject.toml (config)" and "Dockerfile (config)" (authoritative shapes + departures from farmos-agent)
    - src/farmos-agent/Dockerfile (the ROS shape we deliberately depart from)
  </read_first>
  <action>
    Create `src/farm-agent/pyproject.toml` per the RESEARCH shape: name `farm-agent`, `requires-python = ">=3.12"`, runtime deps pydantic>=2.13, psycopg[binary]>=3.3, psycopg-pool>=3.3, ruamel.yaml>=0.19; dev extras pytest>=9.1, pytest-asyncio>=1.4, ruff>=0.15, import-linter>=2.11. Include `[tool.pytest.ini_options]` with `asyncio_mode = "auto"` and `testpaths = ["tests"]`; `[tool.ruff]` line-length 100, target py312; hatchling build backend.
    Pin Python with `src/farm-agent/.python-version` containing `3.12` (RESEARCH Open Question 1: the repo-root `mushroom_farm` pyenv version is not installed and resolves to 3.10 — pin 3.12 locally so the project does not inherit the stale root version).
    Run `uv sync` from `src/farm-agent/` to generate and COMMIT `uv.lock` (deterministic Docker installs).
    Create empty `__init__.py` for each Foray sub-package dir per the D-03 layout: `farm_agent/`, `farm_agent/tenancy/`, `farm_agent/persistence/`, `farm_agent/extraction/`, `farm_agent/extraction/schemas/`, and `tests/`. Do NOT create `chamber/` (Phase 63) and do NOT create `signal_io/confirm/farmos_client/capture/llm/` (no code lands there in Phase 56 — empty dirs add nothing and dilute the Foray grep target).
    Create `src/farm-agent/Dockerfile` per the RESEARCH shape: `FROM python:3.12-slim-bookworm`, `RUN pip install uv`, WORKDIR /app, COPY pyproject.toml + uv.lock first (layer cache), `RUN uv sync --no-dev`, COPY farm_agent/, `CMD ["uv", "run", "python", "-m", "farm_agent"]`. Do not add libpq-dev (psycopg[binary] bundles it; RESEARCH A7).
  </action>
  <verify>
    <automated>cd src/farm-agent && uv sync && uv run python -c "import farm_agent, farm_agent.tenancy, farm_agent.persistence, farm_agent.extraction.schemas; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `uv sync` exits 0 and `src/farm-agent/uv.lock` exists and is committed.
    - `uv run python -c "import farm_agent.tenancy, farm_agent.persistence, farm_agent.extraction.schemas"` prints OK (all sub-packages importable).
    - `grep -v '^#' src/farm-agent/pyproject.toml | grep -c 'asyncio_mode'` returns 1.
    - `grep -c 'python:3.12-slim' src/farm-agent/Dockerfile` returns at least 1; no `libpq-dev` string anywhere in the Dockerfile.
    - No `chamber/` directory created under `src/farm-agent/farm_agent/`.
  </acceptance_criteria>
  <done>uv project resolves, package + Foray sub-packages import under 3.12, Dockerfile builds on slim base with bundled libpq.</done>
</task>

<task type="auto">
  <name>Task 2: Generate + commit the Node JSON-Schema fixture and the conftest scaffold</name>
  <files>src/farm-agent/tests/fixtures/submission_json_schema.json, src/farm-agent/tests/conftest.py</files>
  <read_first>
    - 56-RESEARCH.md "FND-04 > Generating the Fixture" (the one-time Node command) and "The Fixture: Committed Node Output" (structural facts the fixture must satisfy)
    - 56-PATTERNS.md "tests/conftest.py (test)" (TEST_ENV dict + async pool fixture shape)
    - src/agents/alerter/src/extraction/schemas/index.js (confirm the export name SUBMISSION_JSON_SCHEMA before running the dump)
  </read_first>
  <action>
    Generate the fixture by running the Node dump from `src/agents/alerter/` (per RESEARCH): require the schemas index module, JSON.stringify `SUBMISSION_JSON_SCHEMA` with 2-space indent, write to `src/farm-agent/tests/fixtures/submission_json_schema.json`. First confirm the actual export path/name in `index.js` (RESEARCH names it `SUBMISSION_JSON_SCHEMA`); adjust the require target if the index re-exports from a sub-module. Commit the JSON verbatim — it is the ground-truth ship gate, not regenerated in CI. The fixture MUST contain the key `definitions` (draft-7), `anyOf` (6-member draft union), and `additionalProperties` (false on every object), and be roughly 6155 chars.
    Create `src/farm-agent/tests/conftest.py` with: a module-level `TEST_ENV` dict carrying every required TenantConfig env key with safe test defaults (TENANT_ID=test; TIMESCALE_HOST/DB/USER/PASSWORD from `os.environ.get("TEST_TIMESCALE_*", default)`; placeholder secrets SIGNAL_SENDER/ANTHROPIC_API_KEY/FARMOS_PASSWORD — non-real test placeholders, never logged); and a `pytest_asyncio.fixture(scope="session")` named `pool` that loads config, builds the pool, runs migrations, yields, closes. The `pool` fixture imports `farm_agent.tenancy.tenant.load`, `farm_agent.persistence.pool.build_pool`, `farm_agent.persistence.migrations.run_migrations` — those modules do not exist yet (Plans 02/03 create them), so import them INSIDE the fixture body (local import) so test collection does not crash before those plans land. Add a comment that the `pool` fixture activates once Plans 02+03 ship.
  </action>
  <verify>
    <automated>cd src/farm-agent && grep -q definitions tests/fixtures/submission_json_schema.json && grep -q anyOf tests/fixtures/submission_json_schema.json && grep -q additionalProperties tests/fixtures/submission_json_schema.json && uv run pytest tests/ -q --co</automated>
  </verify>
  <acceptance_criteria>
    - `tests/fixtures/submission_json_schema.json` exists and contains the strings `definitions`, `anyOf`, and `additionalProperties` (draft-7 Node shape).
    - `uv run pytest tests/ -q --co` (collect-only) exits 0 — conftest imports do not crash collection even though tenancy/persistence modules do not exist yet.
    - `grep -c "TEST_ENV" tests/conftest.py` returns at least 1.
    - No real secret value appears in the fixture or conftest (placeholders only).
  </acceptance_criteria>
  <done>Committed Node fixture present with draft-7 shape; conftest collects cleanly with a guarded async pool fixture awaiting Plans 02+03.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build-time package resolution | uv pulls wheels from PyPI; supply-chain surface |
| committed fixture file | the JSON-Schema fixture is the FND-04 ground truth; must carry no secrets/PII |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-56-01-SC | Tampering | uv/PyPI installs (pydantic, psycopg, ruamel.yaml, pytest, ruff, import-linter) | mitigate | All packages are industry-standard with a committed `uv.lock` pinning exact versions+hashes; RESEARCH Package Legitimacy Audit reviewed them as `[ASSUMED]` well-established. No `[SUS]`/`[SLOP]` packages. Re-run is reproducible from the lockfile. |
| T-56-01-ID | Information Disclosure | tests/fixtures/submission_json_schema.json | mitigate | Fixture is farmOS schema SHAPE only (field names/types/enums) — no secrets, no PII. Acceptance criterion asserts placeholder-only; the dump is structural JSON Schema, not data. |
| T-56-01-ID2 | Information Disclosure | tests/conftest.py TEST_ENV | accept | Contains placeholder secret values (`test-key`, `+10000000000`) only; never real credentials; never logged. Low risk. |
</threat_model>

<verification>
- `uv sync` resolves; `uv.lock` committed.
- `farm_agent` + Foray sub-packages import under 3.12.
- Dockerfile uses python:3.12-slim, no libpq-dev.
- Fixture committed with draft-7 `definitions`/`anyOf`/`additionalProperties`.
- `uv run pytest tests/ -q --co` collects cleanly.
</verification>

<success_criteria>
The skeleton builds and imports; the JSON-Schema fixture is committed; pytest collects without error. Downstream plans (02 tenancy, 03 foray seam, 04 schemas, 05 persistence, 06 boot) can all build on this.
</success_criteria>

<output>
Create `.planning/phases/56-foundation/56-01-SUMMARY.md` when done.
</output>
