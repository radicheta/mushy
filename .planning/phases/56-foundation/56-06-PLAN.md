---
phase: 56-foundation
plan: 06
type: execute
wave: 4
depends_on: ["56-01", "56-02", "56-05"]
files_modified:
  - src/farm-agent/farm_agent/boot.py
  - src/farm-agent/farm_agent/__main__.py
  - src/farm-agent/tests/test_boot.py
  - docker-compose.override.yml
autonomous: true
requirements: [FND-01]
must_haves:
  truths:
    - "python -m farm_agent boots: loads TenantConfig, opens the pool, runs migrations, logs 'boot complete', idles until SIGTERM"
    - "boot.py is the ONLY module importing across Foray package boundaries"
    - "Boot completes in under 5 seconds against a test DB"
    - "The alerter-py compose service builds from src/farm-agent and coexists with the live Node alerter (no shared files, additive schema)"
    - "No secrets are emitted in boot logs"
  artifacts:
    - path: "src/farm-agent/farm_agent/boot.py"
      provides: "async main(): config -> pool -> migrations -> 'boot complete' -> SIGTERM idle"
      contains: "boot complete"
    - path: "src/farm-agent/farm_agent/__main__.py"
      provides: "asyncio.run(main()) entry for python -m farm_agent"
      contains: "asyncio.run"
    - path: "docker-compose.override.yml"
      provides: "alerter-py service (build context src/farm-agent, env_file string form)"
      contains: "alerter-py"
  key_links:
    - from: "farm_agent/boot.py"
      to: "tenancy.load + persistence.build_pool + persistence.run_migrations"
      via: "the single cross-package wiring point"
      pattern: "run_migrations"
---

<objective>
Wire the daemon: `boot.py` (the SOLE cross-package importer) loads TenantConfig, opens the persistence pool, runs migrations, logs "boot complete", and idles on a SIGTERM event; `__main__.py` is the `python -m farm_agent` entry; and the `alerter-py` compose service builds from `src/farm-agent/` to coexist with the live Node alerter (FND-01).

Purpose: this is the integration point that makes the skeleton a runnable, testable daemon and proves the 5-second boot gate.
Output: boot.py + __main__.py + boot test + the compose service block.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/56-foundation/56-CONTEXT.md
@.planning/phases/56-foundation/56-RESEARCH.md
@.planning/phases/56-foundation/56-PATTERNS.md

<interfaces>
<!-- Authoritative shapes; do not explore the codebase for these. -->

boot.py + __main__.py shapes (SIGTERM idle loop, t0 timing, "boot complete in %.2fs"):
56-RESEARCH.md "FND-01 > Boot Pattern" + 56-PATTERNS.md "boot.py" and "__main__.py".
boot.py is the ONLY module allowed to import across Foray package boundaries (D-03 / FND-05).

Consumes Plan 02 tenancy.load(env) and Plan 05 persistence.build_pool(config) + run_migrations(pool).

alerter-py compose service shape: 56-PATTERNS.md "docker-compose.override.yml (modified)" + RESEARCH
"FND-01 > Compose Integration". CRITICAL: env_file uses the bare STRING form
(- tenants/mossrock/secrets.env), NEVER the object form — the object form silently drops on compose
v2.40 (prod outage 2026-05-23, memory feedback_compose_env_file_object_form_silently_drops). Mirror
the env vars from the existing alerter: block. Build context ./src/farm-agent. networks: signal-net + default.

Boot-in-5s test: call main() against a test DB with a SIGTERM/cancel after boot, capture elapsed,
assert < 5.0 (RESEARCH "Boot-in-5s testability"). The 5s target is conservative for opening a pool +
~15 idempotent SQL statements.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: boot.py + __main__.py + boot-in-5s test</name>
  <files>src/farm-agent/farm_agent/boot.py, src/farm-agent/farm_agent/__main__.py, src/farm-agent/tests/test_boot.py</files>
  <read_first>
    - 56-RESEARCH.md "FND-01 > Boot Pattern" and "Boot-in-5s testability"
    - 56-PATTERNS.md "boot.py (utility)" and "__main__.py (utility)"
  </read_first>
  <behavior>
    - main() loads config via tenancy.load, builds the pool, runs migrations, logs a line containing "boot complete", then waits on an asyncio.Event bound to SIGTERM/SIGINT, then closes the pool.
    - Against a test DB, boot (config -> pool -> migrations -> "boot complete") completes in < 5.0 seconds.
    - No secret values (password, api key, signal sender) appear in any boot log line.
  </behavior>
  <action>
    Create `farm_agent/boot.py` per the RESEARCH/PATTERNS shape: `async def main()` records t0 (time.monotonic), configures logging (asctime/name/levelname/message), calls `load(os.environ)` then `await build_pool(config)` then `await run_migrations(pool)`, logs `"boot complete in %.2fs"` with elapsed, registers SIGTERM+SIGINT handlers that set an asyncio.Event, `await stop.wait()`, then `await pool.close()`. boot.py is the ONLY module that imports from tenancy AND persistence (cross-package). Create `farm_agent/__main__.py` = `asyncio.run(main())`. Do NOT log the config object or any secret.
    Write `tests/test_boot.py::test_boot_completes_in_5s`: run main() against the test DB (use TEST_ENV/monkeypatched os.environ + the test Postgres), arrange for the SIGTERM-wait to be released immediately after "boot complete" (e.g. cancel the task or fire the stop event via the loop right after the log), capture elapsed time, assert < 5.0. Add `::test_boot_logs_no_secrets`: capture log records during boot and assert no record message contains the test password / api key / signal sender placeholder values. Skip DB-dependent assertions with an explicit reason if no test DB is reachable.
  </action>
  <verify>
    <automated>cd src/farm-agent && uv run pytest tests/test_boot.py -q</automated>
  </verify>
  <acceptance_criteria>
    - test_boot_completes_in_5s passes (boot < 5.0s) against a reachable test DB, or skips with an explicit reason.
    - test_boot_logs_no_secrets passes: no boot log line contains a secret placeholder value.
    - boot.py emits a log line containing the literal "boot complete"; `grep -c "boot complete" src/farm-agent/farm_agent/boot.py` returns at least 1.
    - boot.py is the only module importing from BOTH tenancy and persistence; `__main__.py` calls asyncio.run(main()).
  </acceptance_criteria>
  <done>Daemon boots end-to-end, logs "boot complete" under 5s, leaks no secrets, idles on SIGTERM.</done>
</task>

<task type="auto">
  <name>Task 2: Add the alerter-py compose service (coexists with the live Node alerter)</name>
  <files>docker-compose.override.yml</files>
  <read_first>
    - 56-PATTERNS.md "docker-compose.override.yml (modified)" (the alerter: block to mirror; env_file STRING form hotfix)
    - 56-RESEARCH.md "FND-01 > Compose Integration"
    - docker-compose.override.yml (the existing alerter: service block, ~lines 62-169)
    - CLAUDE.md (compose is repo-root docker-compose.yml + override; rebuild bridge/services with --build)
  </read_first>
  <action>
    Add an `alerter-py` service to docker-compose.override.yml that builds from `./src/farm-agent` (Dockerfile from Plan 01), restart unless-stopped, joins networks signal-net + default, and mirrors the env vars from the existing `alerter:` block (TENANT_ID=mossrock, TIMESCALE_HOST/DB/USER/PASSWORD, and the rest of the alerter env surface). Use the env_file BARE STRING form pointing at the mossrock secrets.env file (NOT the object form — the object form silently drops on compose v2.40; document the reason inline exactly as the alerter block does). This service must NOT replace or share any file with the live `alerter:` service — it coexists until the Phase 65 cutover; the shared TimescaleDB is touched only via the additive migrations from Plan 05. Do not change the existing alerter block. Validate the compose file parses with `docker compose config`.
  </action>
  <verify>
    <automated>docker compose -f docker-compose.yml -f docker-compose.override.yml config >/dev/null && echo COMPOSE_OK</automated>
  </verify>
  <acceptance_criteria>
    - `docker compose ... config` parses without error and prints COMPOSE_OK.
    - `grep -c "alerter-py" docker-compose.override.yml` returns at least 1.
    - The alerter-py env_file uses the bare string list form (not the `path:/required:` object form).
    - The pre-existing `alerter:` block is unchanged (no diff to its lines).
  </acceptance_criteria>
  <done>alerter-py service builds from src/farm-agent and coexists with the live Node alerter; compose validates.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| boot.py -> all Foray packages | the single permitted cross-package importer (Foray seam) |
| compose env -> process | secrets injected via env_file string form; never logged |
| alerter-py -> shared TimescaleDB | coexists with live Node alerter; additive schema only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-56-06-01 | Information Disclosure | secrets in boot logs | mitigate | boot.py logs only "boot complete in Ns"; never logs the config object or env; test_boot_logs_no_secrets asserts no secret placeholder appears in log records. |
| T-56-06-02 | Tampering | env_file object form silently drops secrets | mitigate | Use the bare string form (memory feedback_compose_env_file_object_form_silently_drops); inline comment + acceptance criterion enforce it. |
| T-56-06-03 | Denial of Service | alerter-py disrupts the live Node alerter | mitigate | New service shares no files with alerter:; only additive migrations touch the shared DB (Plan 05 gate); coexistence is the design until Phase 65 cutover. |
| T-56-06-04 | Elevation of Privilege | boot importing chamber across the seam | mitigate | boot.py is the documented sole cross-package importer; chamber/ does not exist yet; the Plan 03 seam gate stays armed. |
</threat_model>

<verification>
- Daemon boots < 5s, logs "boot complete", leaks no secrets.
- boot.py is the sole cross-package importer; __main__ runs it.
- alerter-py compose service validates and coexists with the Node alerter.
</verification>

<success_criteria>
`python -m farm_agent` (and the alerter-py container) boots the daemon: config -> pool -> migrations -> "boot complete" within 5s, then idles on SIGTERM, without disturbing the live Node alerter.
</success_criteria>

<output>
Create `.planning/phases/56-foundation/56-06-SUMMARY.md` when done.
</output>
