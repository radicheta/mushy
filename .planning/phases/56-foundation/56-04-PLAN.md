---
phase: 56-foundation
plan: 04
type: tdd
wave: 2
depends_on: ["56-01"]
files_modified:
  - src/farm-agent/farm_agent/extraction/schemas/provenance.py
  - src/farm-agent/farm_agent/extraction/schemas/seeding.py
  - src/farm-agent/farm_agent/extraction/schemas/activity.py
  - src/farm-agent/farm_agent/extraction/schemas/input.py
  - src/farm-agent/farm_agent/extraction/schemas/observation.py
  - src/farm-agent/farm_agent/extraction/schemas/harvest.py
  - src/farm-agent/farm_agent/extraction/schemas/seeding_session.py
  - src/farm-agent/farm_agent/extraction/schemas/submission.py
  - src/farm-agent/tests/test_schema_parity.py
autonomous: true
requirements: [FND-04]
must_haves:
  truths:
    - "A normalized structural diff of Submission.model_json_schema() versus the committed Node fixture shows zero substantive discrepancies"
    - "Every nested pydantic model emits additionalProperties:false (extra='forbid' everywhere)"
    - "ObservationLog rejects state=None AND notes=None; the discriminated union uses ObservationLogBase (no validator)"
    - "event_timestamp fields reject bare dates (require the T separator) so they emit type:string with no date-time format, matching zod"
  artifacts:
    - path: "src/farm-agent/farm_agent/extraction/schemas/submission.py"
      provides: "top-level Submission model + DraftUnion discriminated union + SUBMISSION_JSON_SCHEMA export"
      contains: "model_json_schema"
    - path: "src/farm-agent/farm_agent/extraction/schemas/provenance.py"
      provides: "Provenanced generic emitting inline {value,confidence,sources} additionalProperties:false"
      contains: "Generic"
    - path: "src/farm-agent/tests/test_schema_parity.py"
      provides: "normalized structural diff + forbid-extra spot check + observation validator test"
      contains: "normalize_schema"
  key_links:
    - from: "tests/test_schema_parity.py"
      to: "tests/fixtures/submission_json_schema.json"
      via: "normalize_schema then structural equality"
      pattern: "submission_json_schema.json"
---

<objective>
Port the zod extraction schemas to pydantic v2 and land the FND-04 JSON-Schema parity gate — the single most important Phase 56 deliverable and the structural defense against the zod->pydantic drift PITFALLS.md ranks as the #1 silent-regression risk. The gate must pass BEFORE any LLM call exists in the Python stack.

Purpose: the LLM receives this JSON Schema as its tool input_schema in Phase 60; drift changes LLM behavior silently. The parity test catches it at the foundation.
Output: 8 schema modules + a structural-diff parity test (RED->GREEN).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/56-foundation/56-CONTEXT.md
@.planning/phases/56-foundation/56-RESEARCH.md
@.planning/phases/56-foundation/56-PATTERNS.md

<interfaces>
<!-- Authoritative shapes; do not explore the codebase for these. The PATTERNS file quotes every -->
<!-- zod source schema verbatim and gives the exact pydantic translation per model. -->

Per-model zod source + pydantic translation: 56-PATTERNS.md sections seeding.py / activity.py /
input.py / observation.py / harvest.py / provenance.py / seeding_session.py / submission.py.

The committed fixture (Plan 01): tests/fixtures/submission_json_schema.json — RAW Node output:
top-level {"$ref":"#/definitions/Submission","definitions":{...},"$schema":".../draft-07#"},
anyOf 6-member union, additionalProperties:false everywhere.

normalize_schema() + the structural diff + the discriminated-union/exclusiveMinimum risks:
56-RESEARCH.md "FND-04 > The Pytest Structural Diff" and "Does FND-04 Need a Spike?" and Pitfalls 1-2.

Critical translation rules (56-PATTERNS.md "Shared Patterns" + per-model):
- .strict() -> ConfigDict(extra='forbid') on EVERY model incl. nested (Provenanced, SeedingSessionGroup, ConflictEntry, DraftSubmission, Submission).
- z.number().int().positive() -> int Field(gt=0) (NOT ge=1 — must emit exclusiveMinimum:0).
- z.string().datetime() -> str + field_validator requiring 'T' (NOT datetime type).
- SeedingSession.event_date / SeedingSessionGroup species regex -> str Field(pattern=...) (bare date).
- z.discriminatedUnion('type',[...]) -> Annotated[Union[...], Field(discriminator='type')]; union member ORDER = seeding, activity, input, observation(Base), harvest, seeding_session.
- ChildBlockNameOrSentinel union: put Literal['NEEDS_SEQ'] FIRST to match anyOf member order.
- Provenanced(T) emits INLINE object (not $ref) -> Generic[T] BaseModel.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Spike — minimal Submission with one log type, prove the diff is only cosmetic</name>
  <files>src/farm-agent/farm_agent/extraction/schemas/provenance.py, src/farm-agent/farm_agent/extraction/schemas/seeding.py, src/farm-agent/farm_agent/extraction/schemas/submission.py, src/farm-agent/tests/test_schema_parity.py</files>
  <read_first>
    - 56-RESEARCH.md "Does FND-04 Need a Spike?" + Pitfall 1 ($defs vs definitions) + Pitfall 2 (exclusiveMinimum form)
    - 56-PATTERNS.md "submission.py", "seeding.py", "provenance.py" (exact translations)
    - 56-RESEARCH.md "The Pytest Structural Diff" (normalize_schema)
  </read_first>
  <behavior>
    - normalize_schema() rewrites pydantic's $defs -> definitions and #/$defs/ -> #/definitions/.
    - A Submission containing only SeedingLog produces, after normalization, an anyOf member structurally equal to the fixture's seeding fragment (same required[], additionalProperties:false, qty exclusiveMinimum:0, block_name pattern, event_timestamp type:string with NO date-time format).
    - The ONLY remaining diffs are cosmetic: pydantic `title` keys and array ordering — the normalizer strips/sorts these.
  </behavior>
  <action>
    Build the spike: `provenance.py` (Provenanced Generic[T] + SourceEnum), `seeding.py` (SeedingLog per PATTERNS), and a temporary `submission.py` whose DraftUnion contains ONLY SeedingLog. Write `tests/test_schema_parity.py` with `normalize_schema(schema)` (handle $defs->definitions, #/$defs/->#/definitions/, strip `title` keys, sort `required` arrays) and a recursive structural comparator. First write the comparison test against the seeding fragment of the fixture and run it RED, then adjust the normalizer until the seeding fragment diff is cosmetic-only. Verify Pitfall 2 directly: confirm pydantic emits qty as `exclusiveMinimum: 0` (numeric draft-7 form) given `int Field(gt=0)`; if it emits the boolean/2020 form, extend the normalizer to reconcile both forms and record the finding in the SUMMARY. If the seeding fragment diff is SUBSTANTIVE (additionalProperties missing, wrong types) rather than cosmetic, STOP and flag in the SUMMARY that the full translation needs a follow-on plan before Phase 60.
  </action>
  <verify>
    <automated>cd src/farm-agent && uv run pytest tests/test_schema_parity.py -q -k seeding</automated>
  </verify>
  <acceptance_criteria>
    - The seeding-fragment parity assertion passes after normalization (cosmetic-only diff).
    - SUMMARY records the verdict on Pitfall 2 (exclusiveMinimum form) and whether the diff was cosmetic or substantive.
    - provenance.py Provenanced emits an inline object with additionalProperties:false (not a $ref) for its seeding usage.
  </acceptance_criteria>
  <done>Spike proves the zod->pydantic discriminated-union + Provenanced shapes diff only cosmetically; normalizer finalized.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Port all 6 log types + seeding_session, complete the parity gate</name>
  <files>src/farm-agent/farm_agent/extraction/schemas/activity.py, src/farm-agent/farm_agent/extraction/schemas/input.py, src/farm-agent/farm_agent/extraction/schemas/observation.py, src/farm-agent/farm_agent/extraction/schemas/harvest.py, src/farm-agent/farm_agent/extraction/schemas/seeding_session.py, src/farm-agent/farm_agent/extraction/schemas/submission.py, src/farm-agent/tests/test_schema_parity.py</files>
  <read_first>
    - 56-PATTERNS.md sections activity.py / input.py / observation.py / harvest.py / seeding_session.py / submission.py (exact zod->pydantic translations)
    - 56-RESEARCH.md "FND-04 > Pydantic Model Structure" (ObservationLogBase vs ObservationLog, event_timestamp T-validator, full Submission)
  </read_first>
  <behavior>
    - The full Submission.model_json_schema(), normalized, equals the committed fixture structurally (zero substantive diff) — test_submission_schema_matches_fixture.
    - Every BaseModel subclass emits additionalProperties:false — test_all_models_forbid_extra spot-checks each nested model.
    - ObservationLog raises when both state and notes are None; passes when either is set — test_observation_requires_state_or_notes.
    - The discriminated union in submission.py uses ObservationLogBase (no cross-field validator), matching zod's discriminatedUnion pure-object requirement.
    - A bare-date event_timestamp (no 'T') is rejected by the field_validator on seeding/activity/input/observation/harvest.
  </behavior>
  <action>
    Port the remaining models per PATTERNS: `activity.py` (ActivityLog, Literal enum for ACTIVITY_NAMES), `input.py` (InputLog), `observation.py` (ObservationLogBase + ObservationLog with model_validator(mode='after')), `harvest.py` (HarvestLog, qty_g float Field(gt=0)), `seeding_session.py` (SeedingSessionGroup, ConflictEntry, SeedingSession with event_date bare-date pattern and ChildBlockNameOrSentinel union with Literal['NEEDS_SEQ'] first). Finalize `submission.py`: DraftUnion = Annotated[Union[SeedingLog, ActivityLog, InputLog, ObservationLogBase, HarvestLog, SeedingSession], Field(discriminator='type')]; DraftSubmission + Submission (both extra='forbid'); export SUBMISSION_JSON_SCHEMA = Submission.model_json_schema(). Add the shared event_timestamp T-separator field_validator to each log type. Expand `tests/test_schema_parity.py` to the three named tests above against the FULL fixture. Run RED (full diff will surface remaining drift), then fix model definitions (NOT the fixture — the fixture is ground truth) until GREEN. The only permitted normalizer adjustments are cosmetic ($defs/title/ordering/exclusiveMinimum-form per the spike); any substantive fixture edit is forbidden.
  </action>
  <verify>
    <automated>cd src/farm-agent && uv run pytest tests/test_schema_parity.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_schema_parity.py -q` passes all three tests (full structural match, forbid-extra spot check, observation validator).
    - `grep -c "extra='forbid'" src/farm-agent/farm_agent/extraction/schemas/*.py` shows the directive on every model file (count >= number of nested models).
    - The committed fixture file is byte-unchanged from Plan 01 (the gate adapts pydantic, never the fixture). Verify with git: the fixture has no diff in this plan's commits.
    - submission.py exports SUBMISSION_JSON_SCHEMA.
  </acceptance_criteria>
  <done>All 6 log types + seeding_session ported; full Submission JSON Schema structurally matches the Node fixture; parity gate GREEN before any LLM code exists.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pydantic schema -> LLM tool input_schema (future) | schema shape silently steers LLM behavior in Phase 60 |
| committed fixture | ground-truth Node schema; must not carry secrets/PII (shape only) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-56-04-01 | Tampering | silent zod->pydantic schema drift | mitigate | Structural-diff parity test fails the build on any substantive divergence; runs before any LLM call (FND-04 ship gate). |
| T-56-04-02 | Tampering | fixture edited to force a green | mitigate | Acceptance criterion forbids fixture edits (git no-diff check); only pydantic models or the cosmetic normalizer may change. |
| T-56-04-03 | Information Disclosure | fixture contents | accept | Fixture is JSON Schema SHAPE only (field names/types/enums) — no secrets, no PII. |
</threat_model>

<verification>
- Spike proves cosmetic-only diff for one type; full gate proves zero substantive diff across all six.
- extra='forbid' on every model; observation cross-field validator enforced.
- Fixture unchanged (ground truth).
</verification>

<success_criteria>
model_json_schema() structurally matches the Node SUBMISSION_JSON_SCHEMA fixture with zero substantive discrepancies; the parity gate is green before Phase 57+ adds any LLM code.
</success_criteria>

<output>
Create `.planning/phases/56-foundation/56-04-SUMMARY.md` when done.
</output>
