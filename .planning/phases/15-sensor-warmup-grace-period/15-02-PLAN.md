---
phase: 15-sensor-warmup-grace-period
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
autonomous: true
requirements:
  - SENS-01
must_haves:
  truths:
    - "REQUIREMENTS.md lists SENS-01 as an active v1.2.1 requirement (not 'Future' and not 'Out of Scope')"
    - "Traceability table maps SENS-01 → Phase 15"
    - "No duplicate SENS-01 entries remain in the file"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "SENS-01 promoted to v1.2.1 active section with Phase 15 traceability"
      contains: "SENS-01"
  key_links:
    - from: "REQUIREMENTS.md v1.2.1 section"
      to: "Phase 15"
      via: "Traceability table row"
      pattern: "SENS-01.*Phase 15"
---

<objective>
Close the REQUIREMENTS.md inconsistency called out in 15-RESEARCH.md §Open Questions #3: SENS-01 is currently listed both under "Future Requirements" (§Sensor Stability) AND under "Out of Scope" ("Sensor warm-up grace — Dropped from v1.2 scope — workaround holds"). Since Phase 15 is promoting it to v1.2.1 hotfix, promote SENS-01 to an active v1.2.1 requirement, remove the out-of-scope row, and add Phase 15 to the traceability table.

Purpose: keep REQUIREMENTS.md as a single source of truth for roadmap traceability before the Phase 15 commit graph grows.

Output: clean REQUIREMENTS.md diff; single, unambiguous SENS-01 entry; traceability coverage updated.

Runs in parallel with Plan 01 (no file overlap).
</objective>

<execution_context>
@/home/santi/.claude/get-shit-done/workflows/execute-plan.md
@/home/santi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/15-sensor-warmup-grace-period/15-CONTEXT.md
@.planning/phases/15-sensor-warmup-grace-period/15-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Promote SENS-01 to v1.2.1 active requirement; dedupe; update traceability</name>
  <read_first>
    - .planning/REQUIREMENTS.md (full file — note current layout: v1.2 Requirements → Future Requirements → Out of Scope → Traceability)
    - .planning/phases/15-sensor-warmup-grace-period/15-RESEARCH.md (§Open Questions item 3)
  </read_first>
  <files>.planning/REQUIREMENTS.md</files>
  <action>
    Apply these edits to `.planning/REQUIREMENTS.md`:

    1. **Add a new top-level section for v1.2.1** after the existing "## v1.2 Requirements" block (which ends at the "### Infrastructure" list around line 27) and BEFORE "## Future Requirements". Insert:

       ```markdown
       ## v1.2.1 Requirements

       Requirements for milestone v1.2.1: Hotfix — camera stall + sensor warmup (filed 2026-04-17).

       ### Sensor Stability

       - [ ] **SENS-01**: Sensor warm-up grace period at fc-core boot to avoid false actuation from SCD41 settling — controller early-returns from control_loop for the first 20s post-boot (ANDed with _humidity_buffer full); emits `/fc1/sensor_health` DiagnosticStatus (WARN→OK) for Phase 16 consumption. Promoted from backlog 999.8 at farmer's explicit ask 2026-04-17.

       ```

    2. **Remove** the SENS-01 entry from "## Future Requirements" / "### Sensor Stability" (the line currently reading):
       ```
       - **SENS-01**: Sensor warm-up grace period at fc-core boot to avoid false actuation from SCD41 settling (backlog 999.8)
       ```
       If "### Sensor Stability" becomes empty as a result, remove the "### Sensor Stability" sub-heading too (to avoid an empty section).

    3. **Remove** the corresponding row from the "## Out of Scope" table:
       ```
       | Sensor warm-up grace | Dropped from v1.2 scope — workaround ("ignore first minute") holds |
       ```

    4. **Append a new row** to the Traceability table (after the last `INFRA-03 | Phase 11 | Pending` row):
       ```
       | SENS-01 | Phase 15 | In Progress |
       ```

    5. **Update the Coverage summary** at the bottom. Currently it reads:
       ```
       **Coverage:**
       - v1.2 requirements: 9 total
       - Mapped to phases: 9
       - Unmapped: 0 ✓
       ```
       Add a v1.2.1 line:
       ```
       **Coverage:**
       - v1.2 requirements: 9 total
       - v1.2.1 requirements: 1 total (SENS-01)
       - Mapped to phases: 10
       - Unmapped: 0 ✓
       ```

    6. **Update the footer timestamp line:**
       Change `*Last updated: 2026-04-12 — traceability filled in after roadmap creation*`
       to `*Last updated: 2026-04-17 — SENS-01 promoted to v1.2.1 active (Phase 15)*`

    Do NOT reorder or touch v1.2 requirement entries, FarmOS sections, CAM/INFRA blocks, or TEL-01/TEL-02 entries. Surgical edit only.
  </action>
  <verify>
    <automated>grep -c "SENS-01" .planning/REQUIREMENTS.md | grep -qx "3" &amp;&amp; grep -q "v1.2.1 Requirements" .planning/REQUIREMENTS.md &amp;&amp; grep -q "SENS-01 | Phase 15" .planning/REQUIREMENTS.md &amp;&amp; ! grep -q "Sensor warm-up grace | Dropped" .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "SENS-01" .planning/REQUIREMENTS.md` returns exactly 3 (v1.2.1 section entry, traceability row, coverage line)
    - `grep -q "## v1.2.1 Requirements" .planning/REQUIREMENTS.md` succeeds
    - `grep "SENS-01 | Phase 15" .planning/REQUIREMENTS.md` shows the traceability row
    - `grep "Sensor warm-up grace | Dropped" .planning/REQUIREMENTS.md` returns nothing (out-of-scope row removed)
    - `grep -A1 "### Sensor Stability" .planning/REQUIREMENTS.md` under "## Future Requirements" does NOT list SENS-01 (either section is gone or contains different content)
    - No changes to FarmOS Integration, Camera & 4G, Infrastructure, or TEL-* entries
  </acceptance_criteria>
  <done>REQUIREMENTS.md has one canonical SENS-01 entry (v1.2.1 active), traceability complete, no dupes.</done>
</task>

<task type="auto">
  <name>Task 2: Commit REQUIREMENTS.md update</name>
  <read_first>
    - ./CLAUDE.md (no Co-Authored-By)
  </read_first>
  <files>(git commit only)</files>
  <action>
    Stage and commit the REQUIREMENTS.md update with no Co-Authored-By trailer:
    ```bash
    git add .planning/REQUIREMENTS.md
    git commit -m "docs(requirements): promote SENS-01 to v1.2.1 active (Phase 15)

    Removes duplicate listing in 'Future Requirements' and 'Out of Scope'
    tables; adds canonical v1.2.1 section and Phase 15 traceability row.
    Research (15-RESEARCH.md §Open Questions #3) flagged the inconsistency."
    ```
  </action>
  <verify>
    <automated>cd /mnt/slime-kingdom/opt/mushy &amp;&amp; git log -1 --pretty=%B | grep -q "SENS-01" &amp;&amp; ! git log -1 --pretty=%B | grep -qi "Co-Authored-By" &amp;&amp; git diff --name-only HEAD~1 HEAD | grep -qx ".planning/REQUIREMENTS.md"</automated>
  </verify>
  <acceptance_criteria>
    - `git log -1 --pretty=%B` subject references SENS-01 and v1.2.1
    - `git log -1 --pretty=%B | grep -i "Co-Authored-By"` returns nothing
    - `git diff --name-only HEAD~1 HEAD` is exactly `.planning/REQUIREMENTS.md` (only file in commit)
    - `git status` clean
  </acceptance_criteria>
  <done>Commit landed; traceability source-of-truth updated.</done>
</task>

</tasks>

<verification>
- REQUIREMENTS.md has exactly 3 `SENS-01` hits
- Single canonical v1.2.1 requirements section with SENS-01 active
- Traceability row maps SENS-01 → Phase 15
- No Co-Authored-By in commit
</verification>

<success_criteria>
- File-state checks from Task 1 acceptance criteria all pass
- Commit landed with correct subject, no Co-Authored-By
</success_criteria>

<output>
After completion, create `.planning/phases/15-sensor-warmup-grace-period/15-02-SUMMARY.md` noting the diff + commit SHA.
</output>
