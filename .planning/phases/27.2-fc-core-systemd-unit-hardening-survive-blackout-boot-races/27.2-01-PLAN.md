---
phase: 27.2-fc-core-systemd-unit-hardening-survive-blackout-boot-races
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/pi-deploy/fc-core.service
  - .planning/ROADMAP.md
  - .planning/phases/27.2-fc-core-systemd-unit-hardening-survive-blackout-boot-races/evidence/cold-reboot-journal.log
  - .planning/phases/27.2-fc-core-systemd-unit-hardening-survive-blackout-boot-races/evidence/wg0-down-at-boot-journal.log
autonomous: false
requirements:
  - SYS-01
  - SYS-02
  - SYS-03
  - SYS-04
must_haves:
  truths:
    - "After cold reboot of fc1, fc-core reaches active(running) without any manual `systemctl reset-failed` or `systemctl start`"
    - "After a boot where wg-quick@wg0 is disabled, fc-core fails into restart backoff (not start-limit-hit), and once wg-quick@wg0 is re-enabled fc-core recovers automatically within one StartLimit window (≤50s)"
    - "Live unit on fc1 (/etc/systemd/system/fc-core.service) matches scripts/pi-deploy/fc-core.service byte-for-byte after deploy"
    - "ROADMAP.md no longer claims SYS-01 'currently waits on `ip link show wg0`' — text reflects actual IPv4-wait behavior"
    - "fc-core.service has explicit After=wg-quick@wg0.service and Wants=wg-quick@wg0.service in [Unit]"
    - "fc-core.service still has Restart=always, RestartSec=10, StartLimitIntervalSec=300, StartLimitBurst=5 (no regression)"
    - "fc-core.service contains zero references to tailscaled or tailscale0"
  artifacts:
    - path: "scripts/pi-deploy/fc-core.service"
      provides: "Hardened systemd unit with explicit wg-quick@wg0 dependency"
      contains: "After=wg-quick@wg0.service"
    - path: ".planning/phases/27.2-fc-core-systemd-unit-hardening-survive-blackout-boot-races/evidence/cold-reboot-journal.log"
      provides: "journalctl evidence that cold reboot ends green without manual intervention"
    - path: ".planning/phases/27.2-fc-core-systemd-unit-hardening-survive-blackout-boot-races/evidence/wg0-down-at-boot-journal.log"
      provides: "journalctl evidence that wg0-down-at-boot scenario fails-safe and self-recovers when wg0 returns"
    - path: ".planning/ROADMAP.md"
      provides: "Corrected Phase 27.2 requirement text and checkboxes"
  key_links:
    - from: "scripts/pi-deploy/fc-core.service"
      to: "wg-quick@wg0.service"
      via: "After= and Wants= in [Unit]"
      pattern: "(After|Wants)=.*wg-quick@wg0"
    - from: "fc-core.service ExecStartPre"
      to: "wg0 IPv4 address"
      via: "60-attempt poll on `ip -4 addr show wg0 | grep -q inet`"
      pattern: "ip -4 addr show wg0"
---

<objective>
Close the 2026-05-02 blackout incident class on fc1: harden fc-core.service so it
reaches `active (running)` after any cold boot — including the case where wg0 is not
yet ready — with zero manual intervention.

Purpose: SYS-03 cleanup (explicit wg-quick@wg0 dependency, defense-in-depth alongside
the existing IPv4 polling loop) and SYS-04 validation (cold reboot + wg0-down-at-boot
scenarios), plus correcting the stale ROADMAP.md text claiming SYS-01 still waits on
`ip link show wg0`.

Output: Updated unit on fc1, two journalctl evidence logs in the phase directory,
ROADMAP.md text corrected, all four SYS-* requirements closed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/27.2-fc-core-systemd-unit-hardening-survive-blackout-boot-races/27.2-CONTEXT.md
@scripts/pi-deploy/fc-core.service

<deployment_notes>
- fc1 is reachable over wg0 at 172.16.10.5 (NOT Tailscale).
- Deploy method: edit → commit → push to `fc1/prod` branch → run `scripts/pi-deploy/deploy.sh` on fc1.
  Boot-time `fc-update.service` also pulls `fc1/prod` on every boot, so a reboot will
  pick up unit changes even if `deploy.sh` is not run manually.
- ALWAYS diff the live unit on fc1 against the repo before editing
  (memory: feedback_diff_repo_vs_pi_systemd). Per CONTEXT D-02, repo and live currently
  match byte-for-byte (verified 2026-05-06), but re-verify at execute time.
- DO NOT change `Restart=always` to `Restart=on-failure` — `ros2 launch` exits 0 on
  child crashes (memory: feedback_systemd_restart_ros2_launch). `on-failure` would NOT
  trigger restarts on the failure mode this phase is hardening against.
- DO NOT reintroduce any tailscaled / tailscale0 references — 27.1 removed them
  (memory + CONTEXT D-10).
</deployment_notes>

<current_unit>
Current `scripts/pi-deploy/fc-core.service` `[Unit]` block:

```ini
[Unit]
Description=FC-1 Mushroom Farm ROS2 Control
After=network-online.target fc-update.service
Wants=network-online.target fc-update.service
StartLimitIntervalSec=300
StartLimitBurst=5
```

Target `[Unit]` block after Task 1:

```ini
[Unit]
Description=FC-1 Mushroom Farm ROS2 Control
After=network-online.target fc-update.service wg-quick@wg0.service
Wants=network-online.target fc-update.service wg-quick@wg0.service
StartLimitIntervalSec=300
StartLimitBurst=5
```

The ExecStartPre IPv4-wait loop, ExecStart, Restart=always, RestartSec=10, and all
Environment= lines stay byte-for-byte identical. Only the two `After=` and `Wants=`
lines are modified.
</current_unit>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Edit fc-core.service unit, fix ROADMAP.md text, deploy to fc1</name>
  <files>scripts/pi-deploy/fc-core.service, .planning/ROADMAP.md</files>
  <read_first>
    - scripts/pi-deploy/fc-core.service (current state — confirm [Unit] block matches the `<current_unit>` snippet above)
    - .planning/ROADMAP.md lines 193-208 (Phase 27.2 entry — text on SYS-01 is stale)
    - .planning/REQUIREMENTS.md lines 17-22 (v1.5.0.1 SYS section)
    - .planning/phases/27.2-fc-core-systemd-unit-hardening-survive-blackout-boot-races/27.2-CONTEXT.md (full file — D-01 through D-11)
  </read_first>
  <action>
1. Diff the live unit on fc1 against the repo before editing:
   ```
   ssh ubuntu@172.16.10.5 'cat /etc/systemd/system/fc-core.service' | diff - scripts/pi-deploy/fc-core.service
   ```
   Expected: zero diff (per CONTEXT D-02, verified 2026-05-06). If non-zero, STOP and
   reconcile drift before proceeding (per memory feedback_diff_repo_vs_pi_systemd).

2. Edit `scripts/pi-deploy/fc-core.service` `[Unit]` block. Change exactly two lines:
   - `After=network-online.target fc-update.service`
     → `After=network-online.target fc-update.service wg-quick@wg0.service`
   - `Wants=network-online.target fc-update.service`
     → `Wants=network-online.target fc-update.service wg-quick@wg0.service`

   Do NOT touch any other line. Specifically preserve verbatim:
   - `StartLimitIntervalSec=300`
   - `StartLimitBurst=5`
   - `Restart=always`
   - `RestartSec=10`
   - The full `ExecStartPre=/bin/bash -c '... ip -4 addr show wg0 ...'` block
   - The full `ExecStart=` line
   - All `Environment=` lines

3. Edit `.planning/ROADMAP.md` Phase 27.2 entry (around lines 193-208). Per CONTEXT D-01
   and D-11, fix the stale text and update checkboxes:
   - The bullet on the progress table at line ~90 (`- [ ] **Phase 27.2: ...** ... Still open: SYS-01 ... + SYS-04 validation reboot`)
     → change "Still open: SYS-01 ... + SYS-04 validation reboot" to "Still open: SYS-04 validation reboot."
     and update the parenthetical describing what is in the unit to reflect the IPv4 wait + explicit wg-quick@wg0 dependency.
   - In the Phase 27.2 Requirements list (lines ~197-201):
     - `- [x] **SYS-02**` — leave as-is.
     - `- [x] **SYS-03 (partial)**` — change to `- [x] **SYS-03**` and rewrite the prose:
       "Unit has explicit `After=wg-quick@wg0.service` and `Wants=wg-quick@wg0.service`
       (kernel-WG brings up wg0 at boot). IPv4 polling loop kept as belt-and-braces."
     - `- [ ] **SYS-01**` — change to `- [x] **SYS-01**` and rewrite to:
       "`ExecStartPre` waits for IPv4 on `wg0` via 60-attempt × 1s loop on
       `ip -4 addr show wg0 | grep -q inet` (already shipped via 27.1 transport switch
       commits; previous ROADMAP text describing `ip link show wg0` was stale)."
     - `- [ ] **SYS-04**` — leave unchecked here; it flips to `[x]` only after Tasks 2 and 3 produce evidence.

4. Also update `.planning/REQUIREMENTS.md` SYS section traceability table (lines ~95-104)
   so SYS-01 and SYS-03 rows show "Complete" with a brief note. Leave SYS-04 as
   "Pending — reboot validation" until Tasks 2 and 3 complete.

5. Commit the unit + doc changes together:
   ```
   git add scripts/pi-deploy/fc-core.service .planning/ROADMAP.md .planning/REQUIREMENTS.md
   git commit -m "fc-core(27.2): explicit wg-quick@wg0 dep + roadmap text fix"
   ```
   (NO Co-Authored-By trailer — memory feedback_no_coauthor.)

6. Deploy to fc1:
   ```
   git push origin main
   git push origin main:fc1/prod
   ssh ubuntu@172.16.10.5 'cd ~/mushroom_farm_ws && bash scripts/pi-deploy/deploy.sh'
   ```
   (If `deploy.sh` lives at a different path, locate it first via
   `ssh ubuntu@172.16.10.5 'ls scripts/pi-deploy/'`.)

7. After deploy, verify the unit on fc1 picked up the change:
   ```
   ssh ubuntu@172.16.10.5 'sudo systemctl daemon-reload && sudo systemctl cat fc-core.service'
   ```
   Confirm both new `wg-quick@wg0.service` references are present, then restart fc-core
   so it picks up the new dependency graph:
   ```
   ssh ubuntu@172.16.10.5 'sudo systemctl restart fc-core.service && sleep 15 && systemctl status fc-core.service --no-pager'
   ```
   Confirm `Active: active (running)` before proceeding to Task 2.
  </action>
  <verify>
    <automated>grep -E "^After=.*wg-quick@wg0\.service" scripts/pi-deploy/fc-core.service && grep -E "^Wants=.*wg-quick@wg0\.service" scripts/pi-deploy/fc-core.service && grep -E "^Restart=always$" scripts/pi-deploy/fc-core.service && grep -E "^RestartSec=10$" scripts/pi-deploy/fc-core.service && grep -E "^StartLimitIntervalSec=300$" scripts/pi-deploy/fc-core.service && grep -E "^StartLimitBurst=5$" scripts/pi-deploy/fc-core.service && grep -E "ip -4 addr show wg0" scripts/pi-deploy/fc-core.service && ! grep -i "tailscale" scripts/pi-deploy/fc-core.service && ! grep "currently waits on .ip link show wg0." .planning/ROADMAP.md && ssh ubuntu@172.16.10.5 'systemctl is-active fc-core.service' | grep -q '^active$'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "wg-quick@wg0.service" scripts/pi-deploy/fc-core.service` returns 2 (one After=, one Wants=).
    - `grep -c "Restart=always" scripts/pi-deploy/fc-core.service` returns 1.
    - `grep -c "RestartSec=10" scripts/pi-deploy/fc-core.service` returns 1.
    - `grep -c "StartLimitIntervalSec=300" scripts/pi-deploy/fc-core.service` returns 1.
    - `grep -c "StartLimitBurst=5" scripts/pi-deploy/fc-core.service` returns 1.
    - `grep -c "ip -4 addr show wg0" scripts/pi-deploy/fc-core.service` returns ≥1.
    - `grep -ic "tailscale" scripts/pi-deploy/fc-core.service` returns 0.
    - `grep -c "currently waits on .ip link show wg0." .planning/ROADMAP.md` returns 0.
    - ROADMAP.md Phase 27.2 SYS-01 line begins with `- [x] **SYS-01**`.
    - ROADMAP.md Phase 27.2 SYS-03 line begins with `- [x] **SYS-03**` (no longer `(partial)`).
    - `ssh ubuntu@172.16.10.5 'systemctl is-active fc-core.service'` prints `active`.
    - `ssh ubuntu@172.16.10.5 'systemctl cat fc-core.service' | grep -c "wg-quick@wg0.service"` returns 2.
  </acceptance_criteria>
  <done>Unit edited, deployed to fc1, doc text corrected, fc-core active on fc1, all acceptance greps pass.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Cold-reboot validation (SYS-04 scenario 1)</name>
  <files>.planning/phases/27.2-fc-core-systemd-unit-hardening-survive-blackout-boot-races/evidence/cold-reboot-journal.log</files>
  <read_first>
    - scripts/pi-deploy/fc-core.service (post-Task-1 state)
    - .planning/phases/27.2-fc-core-systemd-unit-hardening-survive-blackout-boot-races/27.2-CONTEXT.md (D-05 scenario 1, D-06 pass criteria, D-08 timing)
  </read_first>
  <what-built>Hardened fc-core.service with explicit wg-quick@wg0 dependency, deployed and active on fc1 (per Task 1).</what-built>
  <how-to-verify>
1. Note the current time: `date -u +%Y-%m-%dT%H:%M:%SZ`. Record this as T0.

2. Trigger a cold reboot of fc1:
   ```
   ssh ubuntu@172.16.10.5 'sudo reboot'
   ```
   The SSH session will drop. Wait ~90 seconds for fc1 to come back.

3. Re-establish SSH and capture the journal for fc-core for this boot:
   ```
   ssh ubuntu@172.16.10.5 'journalctl -u fc-core.service -b -o short-iso' \
     > .planning/phases/27.2-fc-core-systemd-unit-hardening-survive-blackout-boot-races/evidence/cold-reboot-journal.log
   ```

4. Confirm fc-core reached active(running) without any manual `systemctl reset-failed`
   or `systemctl start`:
   ```
   ssh ubuntu@172.16.10.5 'systemctl is-active fc-core.service'
   ```
   Expect: `active`.

5. Check the journal log for evidence:
   ```
   grep -E "(Started|Active: active)" .planning/phases/27.2-.../evidence/cold-reboot-journal.log
   grep -c "reset-failed" .planning/phases/27.2-.../evidence/cold-reboot-journal.log  # should be 0
   grep "fc-core: wg0 has IPv4" .planning/phases/27.2-.../evidence/cold-reboot-journal.log
   ```

6. Compute time from boot to fc-core active:
   ```
   ssh ubuntu@172.16.10.5 'systemctl show fc-core.service -p ActiveEnterTimestamp; uptime -s'
   ```
   Note delta in seconds. Append a one-line summary to the evidence log:
   ```
   "Boot at <uptime -s>; fc-core active at <ActiveEnterTimestamp>; delta = <N>s. Zero manual intervention."
   ```

7. Confirm telemetry is flowing — on elder-plops, check that `fc1/temperature` topic
   has fresh data within 2 minutes of fc1 boot:
   ```
   docker exec mushy-bridge-1 curl -s http://localhost:8765/health 2>/dev/null \
     || curl -s http://localhost:8765/api/telemetry/latest?topic=fc1/temperature
   ```
   (Adjust to whatever bridge endpoint surfaces last-message-time.) Record fresh
   timestamp post-boot.

8. Commit the evidence file:
   ```
   git add .planning/phases/27.2-fc-core-systemd-unit-hardening-survive-blackout-boot-races/evidence/cold-reboot-journal.log
   git commit -m "evidence(27.2): cold-reboot SYS-04 scenario 1 PASS"
   git push origin main
   ```

PASS = fc-core reaches active without `reset-failed`, evidence log committed, telemetry
fresh within 2 min of boot. FAIL = anything that required typing a recovery command.
  </how-to-verify>
  <acceptance_criteria>
    - File `.planning/phases/27.2-fc-core-systemd-unit-hardening-survive-blackout-boot-races/evidence/cold-reboot-journal.log` exists, is non-empty (`wc -l` > 5).
    - `grep -c "reset-failed" .planning/phases/27.2-.../evidence/cold-reboot-journal.log` returns 0.
    - `grep -c "fc-core: wg0 has IPv4" .planning/phases/27.2-.../evidence/cold-reboot-journal.log` returns ≥1.
    - `grep -c "start-limit-hit" .planning/phases/27.2-.../evidence/cold-reboot-journal.log` returns 0.
    - `ssh ubuntu@172.16.10.5 'systemctl is-active fc-core.service'` returns `active`.
    - Evidence log contains the one-line "Boot at … fc-core active at … delta = Ns. Zero manual intervention." summary.
    - File is committed to git (`git log --oneline -- .planning/phases/27.2-.../evidence/cold-reboot-journal.log` returns ≥1 commit).
  </acceptance_criteria>
  <resume-signal>Type "approved" if cold reboot ended green with zero manual intervention, or describe any required recovery command and we re-plan.</resume-signal>
  <done>Cold reboot ended in active(running) without manual intervention; journal evidence committed; telemetry fresh within 2 min of boot.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: wg0-down-at-boot validation (SYS-04 scenario 2 — the actual incident class)</name>
  <files>.planning/phases/27.2-fc-core-systemd-unit-hardening-survive-blackout-boot-races/evidence/wg0-down-at-boot-journal.log, .planning/ROADMAP.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/phases/27.2-fc-core-systemd-unit-hardening-survive-blackout-boot-races/27.2-CONTEXT.md (D-05 scenario 2, D-06 pass criteria, specifics § "wg0-down-at-boot scenario is the test that closes the original 2026-05-02 incident class")
    - .planning/phases/27.2-fc-core-systemd-unit-hardening-survive-blackout-boot-races/evidence/cold-reboot-journal.log (Task 2 output, for comparison)
  </read_first>
  <what-built>Cold-reboot happy path proven (Task 2). This task proves the unhappy path that the 2026-05-02 incident actually was: wg0 not yet ready when fc-core starts.</what-built>
  <how-to-verify>
1. On fc1, disable wg-quick@wg0 so it does NOT come up at next boot:
   ```
   ssh ubuntu@172.16.10.5 'sudo systemctl disable wg-quick@wg0.service && sudo systemctl stop wg-quick@wg0.service'
   ```
   NOTE: This drops the wg0 transport — your SSH over 172.16.10.5 may freeze after stop.
   If so, re-establish via the alternate path the operator uses on-LAN. Easiest sequence
   is to disable then immediately reboot in a single command so wg0 is only down across
   the boot, not across an interactive window:

   ```
   ssh ubuntu@172.16.10.5 'sudo systemctl disable wg-quick@wg0.service && sudo reboot'
   ```

2. Wait ~90 seconds. fc1 will be unreachable over wg0 (because wg0 is intentionally
   down). On the home-LAN side, reach fc1 via its LAN IP/mDNS — operator knows this
   path; if no LAN access, this validation requires physical-or-LAN access to fc1.

3. Once fc1 is back up (without wg0), capture the journal:
   ```
   <local-fc1-shell>$ sudo journalctl -u fc-core.service -b -o short-iso \
     > /tmp/wg0-down-at-boot-journal.log
   ```
   Expected pattern in the log:
   - `fc-core: waiting for wg0 IPv4...`
   - 60 attempts (or until timeout)
   - `fc-core: wg0 did not acquire IPv4 after 60s -- aborting start`
   - service exits 1, systemd schedules restart, retries 5 times within 50s,
     then enters StartLimit backoff (NOT a stuck rcl_create_node loop).

4. Confirm fc-core is in failed state with restart backoff (NOT permanently dead):
   ```
   <local-fc1-shell>$ systemctl status fc-core.service --no-pager
   ```
   Expected: `Active: failed` or `activating (auto-restart)` — NOT `active (running)`,
   and the journal should NOT contain `start-limit-hit` mid-window (within first 60s
   of failure cycle).

5. Now bring wg-quick@wg0 back up and confirm fc-core auto-recovers WITHOUT manual
   `systemctl reset-failed && systemctl start fc-core`:
   ```
   <local-fc1-shell>$ sudo systemctl enable wg-quick@wg0.service
   <local-fc1-shell>$ sudo systemctl start wg-quick@wg0.service
   <local-fc1-shell>$ sleep 60
   <local-fc1-shell>$ systemctl is-active fc-core.service
   ```
   PASS criterion (D-06): fc-core flips to `active` on its own within one StartLimit
   window (≤50s of next ExecStartPre attempt) after wg0 returns. If `reset-failed` is
   needed, this is a FAIL — restart-backoff cycle is too short and StartLimit kicked
   in before recovery; we'd need to widen StartLimitIntervalSec or use Restart=always
   with `RestartSec` adjustment.

6. Capture the recovery in the journal too:
   ```
   <local-fc1-shell>$ sudo journalctl -u fc-core.service -b -o short-iso \
     >> /tmp/wg0-down-at-boot-journal.log
   ```
   Append a summary line at the end of the file:
   ```
   "wg0 disabled at boot; fc-core failed into restart backoff (no start-limit-hit within first <N>s).
   wg0 re-enabled at <T1>; fc-core active at <T2>; recovery delta = <T2-T1>s. Zero manual intervention."
   ```

7. Copy evidence to the planning repo (from elder-plops or wherever the repo lives):
   ```
   scp ubuntu@<fc1-lan-or-wg0>:/tmp/wg0-down-at-boot-journal.log \
     .planning/phases/27.2-fc-core-systemd-unit-hardening-survive-blackout-boot-races/evidence/wg0-down-at-boot-journal.log
   ```

8. Re-confirm baseline: `ssh ubuntu@172.16.10.5 'systemctl is-active fc-core.service'`
   returns `active` and `systemctl is-enabled wg-quick@wg0.service` returns `enabled`.

9. Update ROADMAP.md and REQUIREMENTS.md to flip SYS-04 to `[x]`:
   - ROADMAP.md Phase 27.2 SYS-04 line → `- [x] **SYS-04** — Validation reboot SHIPPED <date>; cold-reboot + wg0-down-at-boot scenarios both end green without manual intervention. Evidence: .planning/phases/27.2-.../evidence/{cold-reboot,wg0-down-at-boot}-journal.log.`
   - ROADMAP.md progress-table bullet at line ~90 → flip `- [ ]` to `- [x]` and replace "Still open" prose with "SHIPPED <date>."
   - REQUIREMENTS.md SYS-04 traceability row → `Complete (<date>)`.

10. Commit:
    ```
    git add .planning/phases/27.2-.../evidence/wg0-down-at-boot-journal.log \
            .planning/ROADMAP.md .planning/REQUIREMENTS.md
    git commit -m "evidence(27.2): wg0-down-at-boot SYS-04 scenario 2 PASS; phase shipped"
    git push origin main
    ```

PASS = fc-core fails into restart backoff (not start-limit-hit within first 60s), then
auto-recovers when wg0 returns, with zero `reset-failed`. FAIL = anything else.
  </how-to-verify>
  <acceptance_criteria>
    - File `.planning/phases/27.2-.../evidence/wg0-down-at-boot-journal.log` exists, non-empty.
    - `grep -c "fc-core: wg0 did not acquire IPv4 after 60s" .planning/phases/27.2-.../evidence/wg0-down-at-boot-journal.log` returns ≥1.
    - `grep -c "fc-core: wg0 has IPv4" .planning/phases/27.2-.../evidence/wg0-down-at-boot-journal.log` returns ≥1 (recovery line, after wg0 re-enabled).
    - `grep -c "Zero manual intervention" .planning/phases/27.2-.../evidence/wg0-down-at-boot-journal.log` returns ≥1 (summary line).
    - `ssh ubuntu@172.16.10.5 'systemctl is-active fc-core.service'` returns `active` at task end.
    - `ssh ubuntu@172.16.10.5 'systemctl is-enabled wg-quick@wg0.service'` returns `enabled` at task end.
    - ROADMAP.md SYS-04 line begins with `- [x] **SYS-04**`.
    - ROADMAP.md Phase 27.2 progress-table bullet begins with `- [x]`.
    - REQUIREMENTS.md SYS-04 traceability row contains "Complete".
    - Both evidence files committed (`git log --oneline -- .planning/phases/27.2-.../evidence/` returns ≥2 commits).
  </acceptance_criteria>
  <resume-signal>Type "approved" if both validation scenarios passed with zero manual intervention. If recovery required `reset-failed` or fc-core stuck in start-limit-hit, describe what happened and we re-plan timing knobs.</resume-signal>
  <done>wg0-down-at-boot scenario produces clean restart-backoff (no start-limit-hit), auto-recovers when wg0 returns, evidence committed, all SYS-* requirements flipped to complete.</done>
</task>

</tasks>

<verification>
- All four SYS-* requirements (SYS-01, SYS-02, SYS-03, SYS-04) marked `[x]` in
  REQUIREMENTS.md and ROADMAP.md.
- `scripts/pi-deploy/fc-core.service` and `/etc/systemd/system/fc-core.service` on fc1
  match (verified via `ssh ubuntu@172.16.10.5 'diff <(cat /etc/systemd/system/fc-core.service) -' < scripts/pi-deploy/fc-core.service`).
- Two evidence journal logs committed under `.planning/phases/27.2-.../evidence/`.
- No regressions on Restart=always / RestartSec=10 / StartLimitIntervalSec=300 /
  StartLimitBurst=5 / IPv4 ExecStartPre loop.
- Zero `tailscale*` references in the unit.
- ROADMAP.md no longer claims `ip link show wg0` is the current behavior.
</verification>

<success_criteria>
- 2026-05-02 blackout incident class is closed: cold reboot AND wg0-down-at-boot both
  end with fc-core active, zero manual `reset-failed`, telemetry fresh within 2 min.
- Phase 27.2 frontmatter goal achieved: "fc-core comes up green on its own after any
  cold boot or transport bounce."
- Evidence committed so future regressions can be diffed against this baseline.
</success_criteria>

<output>
After completion, create `.planning/phases/27.2-fc-core-systemd-unit-hardening-survive-blackout-boot-races/27.2-01-SUMMARY.md` with:
- Final unit diff (the two After=/Wants= lines)
- Cold reboot timing: boot → fc-core active delta in seconds
- wg0-down recovery timing: wg0 enable → fc-core active delta in seconds
- Pointers to both evidence files
- Confirmation that all 4 SYS-* requirements are now `[x]`
- Note on whether v1.5.0.1 milestone is now complete (BUF-04 still pending natural-event attestation per REQUIREMENTS.md, so milestone close depends on that, not this phase)
</output>