---
phase: 62-farmos-write-path
plan: 03
type: tdd
wave: 2
depends_on: ["62-01"]
files_modified:
  - src/farm-agent/farm_agent/farmos/merge.py
  - src/farm-agent/tests/test_farmos_merge.py
  - src/farm-agent/tests/fixtures/farmos/merge_golden.json
autonomous: true
requirements: [FWR-02]
must_haves:
  truths:
    - "merge.py produces a byte-identical merged JSON:API payload to Node merge.js for the same input"
    - "Identity scalars name + type raise IdentityMutationError on mutation"
    - "array-ref fields (parent/qr_codes/farm_id_tag) set-union by id; scalar rels (fungi_type/fungi_xing) null=take, equal=noop, differ=conflict-keep-existing; status same scalar rule; notes split-dedup-join on the separator"
  artifacts:
    - path: "src/farm-agent/farm_agent/farmos/merge.py"
      provides: "merge_asset_fields + IdentityMutationError + STABLE_NOTES_SEPARATOR"
      contains: "def merge_asset_fields"
    - path: "src/farm-agent/tests/fixtures/farmos/merge_golden.json"
      provides: "Node merge.js golden outputs for cross-language byte-identity"
  key_links:
    - from: "tests/test_farmos_merge.py"
      to: "merge_golden.json (Node merge.js outputs)"
      via: "assert merge_asset_fields(existing, incoming).merged == golden.expected"
      pattern: "merge_golden"
---

<objective>
Faithful byte-identical Python port of the Node pure merge module (`farmos/merge.js`)
and the cross-language identity fixture that proves SC2's stable-identity claim.

D-05 reconciliation: stable identity is NAME-BASED, not a hex digest. SC2's "same
stable-identity hex digest" wording is inaccurate; the real assertion (this plan)
is: `merge_asset_fields` produces a byte-identical merged JSON:API payload to Node
`merge_asset_fields` for the same input. The fixture captures Node `merge.js` outputs
as golden JSON; the Python test asserts deep equality.

Purpose: the merge layer is the heart of upsert-by-stable-identity (FWR-02); a faithful
port is what makes a second commit produce 0 duplicate / 0 spurious-PATCH assets.
Output: farm_agent/farmos/merge.py + cross-language golden fixture + tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/62-farmos-write-path/62-CONTEXT.md
@.planning/phases/62-farmos-write-path/62-PATTERNS.md
</context>

<feature>
  <name>merge_asset_fields (byte-identical port of merge.js)</name>
  <files>src/farm-agent/farm_agent/farmos/merge.py, src/farm-agent/tests/test_farmos_merge.py, src/farm-agent/tests/fixtures/farmos/merge_golden.json</files>
  <read_first>
    - src/agents/alerter/src/farmos/merge.js (lines 1-133: the complete source of truth — constants 8-12, IdentityMutationError 14-22, deepClone 24-26, unionArrayRef 28-39, mergeNotes 41-52, mergeAssetFields 54-131)
    - src/agents/alerter/test/farmos/merge.test.js (the Node test cases — reuse the SAME existing/incoming inputs to generate the golden fixture and assert parity)
    - 62-PATTERNS.md (the merge.py section: constants, exception class, notes rule, deepcopy mapping)
  </read_first>
  <behavior>
    - identity mutation: incoming.attributes.name != existing.attributes.name raises IdentityMutationError("name"); incoming.type != existing.type raises IdentityMutationError("type")
    - array-ref union: existing parent [{id:A}] + incoming parent [{id:A},{id:B}] -> merged parent has A then B (first-seen order, dedup by id)
    - scalar rel null=take: existing fungi_type data null, incoming {id:X} -> merged takes X
    - scalar rel differ=conflict: existing {id:X}, incoming {id:Y} -> merged KEEPS X, conflicts has {field:'fungi_type', existing:X, incoming:Y, kind:'scalar_conflict'}
    - scalar attr status: same null=take / equal=noop / differ=conflict rule
    - notes: split on "\n---\n", trim, drop empties, dedup preserving order, rejoin; format 'plain_text'
    - cross-language: for every (existing, incoming) pair in merge_golden.json, merge_asset_fields(existing, incoming).merged deep-equals golden.merged AND conflicts deep-equals golden.conflicts
  </behavior>
  <implementation>
    RED: write test_farmos_merge.py first. Build merge_golden.json by running the Node
    merge.js over the merge.test.js input pairs (a tiny one-off node script that
    requires ../src/farmos/merge and JSON.stringifies {input, merged, conflicts} for
    each case; commit the resulting JSON as the fixture). The Python test loads the
    fixture and asserts parity; initially fails (no merge.py).

    GREEN: port merge.js to merge_asset_fields(existing: dict, incoming: dict) ->
    dict with keys "merged" and "conflicts". Constants STABLE_NOTES_SEPARATOR="\n---\n",
    ARRAY_REF_FIELDS=["parent","qr_codes","farm_id_tag"], SCALAR_REL_FIELDS=
    ["fungi_type","fungi_xing"], SCALAR_ATTR_FIELDS=["status"]. IdentityMutationError
    with .field/.existing/.incoming. Use copy.deepcopy for the deepClone equivalent.
    Replicate unionArrayRef (Map-by-id insertion order: existing first, then incoming,
    skip null id, no overwrite), merge_notes (split/strip/filter/dedup/join), and
    mergeAssetFields field-by-field exactly including the conflict-keep-existing
    semantics. Match the JSON:API relationship shapes: scalar rels compared via
    rel["data"]["id"]; array rels via rel["data"] list. Ensure Python dict key
    ordering does not affect equality (compare via ==, which is order-insensitive for
    dicts).

    REFACTOR: extract _union_array_ref and _merge_notes private helpers mirroring
    merge.js function boundaries. Re-run; all golden cases green.
  </implementation>
  <verify>
    <automated>cd src/farm-agent && uv run pytest tests/test_farmos_merge.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "def merge_asset_fields" src/farm-agent/farm_agent/farmos/merge.py` returns 1
    - `grep -c "class IdentityMutationError" src/farm-agent/farm_agent/farmos/merge.py` returns 1
    - merge_golden.json contains >= 6 (existing, incoming, merged, conflicts) cases derived from merge.test.js
    - Every golden case asserts byte-identical merged + conflicts (deep equality) between Python and the captured Node output
    - Identity-mutation case raises IdentityMutationError (test asserts both name and type)
    - conflict-keep-existing: differing scalar rel keeps existing id and records the conflict (test asserts merged is unchanged)
  </acceptance_criteria>
  <done>merge.py is a byte-identical port; cross-language golden fixture proves parity; tests green.</done>
</feature>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| extractor draft fields → merged farmOS payload | incoming asset data merged into an existing record; a faulty merge could silently overwrite existing identity/strain |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-62-07 | Tampering | silent strain/identity overwrite on merge | mitigate | conflict-keep-existing for scalar rels (fungi_type) + IdentityMutationError on name/type; never silent overwrite (ports merge.js T-51-03) |
| T-62-08 | Information Disclosure | notes dedup losing audit markers | mitigate | notes split-dedup-join preserves mushy:draft markers (byte-identical port + golden fixture) |
| T-62-SC | Tampering | pip installs | accept | no new packages (stdlib copy + json only) |
</threat_model>

<verification>
- merge.py deep-equals Node merge.js on every golden case (byte-identical merged + conflicts).
- Identity mutation raises; conflicts keep existing.
</verification>

<success_criteria>
SC2 (reconciled per D-05): Python merge_asset_fields produces a byte-identical merged
JSON:API payload to Node merge.js for the same input.
</success_criteria>

<output>
Create `.planning/phases/62-farmos-write-path/62-03-SUMMARY.md` when done.
</output>
