---
phase: 62-farmos-write-path
plan: 11
type: execute
wave: 7
depends_on: ["62-04", "62-10"]
files_modified:
  - src/farm-agent/farm_agent/farmos/commit_watchdog.py
  - src/farm-agent/farm_agent/boot.py
  - src/farm-agent/tests/test_farmos_commit_watchdog.py
  - src/farm-agent/tests/test_boot.py
autonomous: true
requirements: [FWR-04, FWR-01, FWR-03]
must_haves:
  truths:
    - "The commit watchdog ticks immediately on boot then every commit_watchdog_interval_ms; it releases stale locks, finds origin='python' confirmed candidates, acquires a per-row lock, runs the fidelity gate, commits via the router, and marks committed/failed/requeued"
    - "A draft failing the CSV fidelity gate is held as fidelity_cross_check_unverified and is NOT committed to farmOS (gate runs BEFORE the farmOS call)"
    - "boot.py constructs a shared farmOS httpx client and starts exactly one commit_watchdog_loop task, cancelled cleanly on shutdown"
    - "the loop never-throws (Exception -> WARNING + continue) and re-raises CancelledError"
  artifacts:
    - path: "src/farm-agent/farm_agent/farmos/commit_watchdog.py"
      provides: "commit_watchdog_loop + tick_once (release/find/lock/gate/commit/mark)"
      contains: "fidelity"
    - path: "src/farm-agent/farm_agent/boot.py"
      provides: "shared farmOS client + commit_watchdog task wiring"
      contains: "commit_watchdog_loop"
  key_links:
    - from: "boot.py"
      to: "commit_watchdog_loop(pool, farmos_client, config)"
      via: "asyncio.create_task"
      pattern: "create_task\\(\\s*commit_watchdog_loop"
    - from: "commit_watchdog tick"
      to: "fidelity_gate.check_fidelity"
      via: "pre-commit gate hold"
      pattern: "check_fidelity"
---

<objective>
Port the commit watchdog (`commit-watchdog.js`) to an asyncio loop mirroring the
existing confirm watchdog, wire the CSV fidelity gate into the pre-commit path
(Plan 04), and wire the shared farmOS client + watchdog task into boot.py. This is the
runtime that drains Python-owned confirmed drafts to farmOS — it depends on Plan 10
(router + origin-guarded DAO) and Plan 04 (fidelity gate), and transitively on Plan 01
(the redeploy checkpoint must be approved before this runs against the shared DB).

Purpose: FWR-04 (origin-guarded drain loop) + FWR-01 (commit execution) + FWR-03
(fidelity gate active in the live commit path). Output: commit_watchdog.py + boot
wiring + tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/62-farmos-write-path/62-CONTEXT.md
@.planning/phases/62-farmos-write-path/62-PATTERNS.md

<interfaces>
From src/farm-agent/farm_agent/farmos/commit_db.py (Plan 10): find_confirmed_candidates, acquire_commit_lock, mark_committed, mark_failed, requeue_for_retry, release_stale_locks
From src/farm-agent/farm_agent/farmos/commits/commit_router.py (Plan 10): commit(client, draft, ctx)
From src/farm-agent/farm_agent/farmos/fidelity_gate.py (Plan 04): check_fidelity(draft, csv_rows), load_fidelity_csv(path)
From src/farm-agent/farm_agent/farmos/client.py (Plan 02): create_farmos_client(farmos_url, username, password, http)
From src/farm-agent/farm_agent/confirm/watchdog.py (existing): the immediate-then-sleep never-throws loop shape to mirror
From src/farm-agent/farm_agent/boot.py (existing): the asyncio.create_task + shutdown-cancel wiring pattern (lines 102-135); config has farmos_url/username/password, commit_watchdog_interval_ms, commit_watchdog_batch_cap, commit_retry_max, fidelity_csv_path
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Port commit_watchdog.py (tick + loop) with fidelity-gate pre-commit hold</name>
  <files>src/farm-agent/farm_agent/farmos/commit_watchdog.py, src/farm-agent/tests/test_farmos_commit_watchdog.py</files>
  <read_first>
    - src/agents/alerter/src/farmos/commit-watchdog.js (lines 1-202: _isTransient 8-14, _processRow 81-156 (idempotency probe, pre-lock backoff, acquire, commit, markCommitted/requeue/markFailed), tickOnce 158-177 (releaseStaleLocks -> findConfirmedCandidates -> per-row), start/stop 179-196)
    - src/farm-agent/farm_agent/confirm/watchdog.py (the immediate-then-sleep never-throws loop + asyncio.Lock + CancelledError re-raise to mirror exactly)
    - src/farm-agent/farm_agent/farmos/fidelity_gate.py (check_fidelity return shape: pass / strain_mismatch hold_status)
    - 62-PATTERNS.md (commit_watchdog.py section: loop shape, tick, origin-guard SELECT)
  </read_first>
  <behavior>
    - immediate tick on boot then sleep(commit_watchdog_interval_ms/1000) loop; CancelledError re-raises; Exception -> WARNING + continue
    - tick: release_stale_locks -> find_confirmed_candidates(origin='python') -> per row: acquire_commit_lock; on win run the fidelity gate then commit_router.commit; mark_committed on ok; requeue_for_retry on transient + attempts<max; mark_failed otherwise
    - fidelity hold: a row whose check_fidelity returns reason "strain_mismatch" is held as fidelity_cross_check_unverified and is NOT committed (no commit_router.commit call); the ask-back is dispatched (best-effort)
    - transient classifier: http_status None or >=500 or reason matching timeout/abort/econnreset -> transient
    - never-throws: a per-row exception logs WARNING and continues the batch
  </behavior>
  <action>
    Port commit_watchdog.py with `tick_once(pool, farmos_client, config, *, lock=None,
    db=None, router=None, csv_rows=None)` and `commit_watchdog_loop(pool, farmos_client,
    config)` mirroring confirm/watchdog.py exactly (immediate tick, asyncio.Lock,
    interval = commit_watchdog_interval_ms/1000, CancelledError re-raise). The tick:
    release_stale_locks, find_confirmed_candidates(batch_cap), per row acquire_commit_lock
    (skip on rowcount!=1), then BEFORE the router call run check_fidelity(locked_row,
    csv_rows); if it returns reason "strain_mismatch", transition the draft to
    fidelity_cross_check_unverified (a commit_db helper or an UPDATE setting status +
    keeping origin='python') and skip the commit, dispatching the ask_back_msg
    best-effort; otherwise commit_router.commit and mark_committed / requeue_for_retry
    (transient + attempt<commit_retry_max) / mark_failed. Port _is_transient. Load
    csv_rows once via load_fidelity_csv(config.fidelity_csv_path) when set (empty list
    when unset — fidelity is a no-op pass-through). Mirror the per-row try/except
    never-throws.
  </action>
  <verify>
    <automated>cd src/farm-agent && uv run pytest tests/test_farmos_commit_watchdog.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "check_fidelity" src/farm-agent/farm_agent/farmos/commit_watchdog.py` returns 1 or more
    - a fidelity strain_mismatch row is held as fidelity_cross_check_unverified and commit_router.commit is NEVER called for it (test asserts via spy)
    - a clean row is committed and mark_committed called once; a transient failure requeues; a terminal failure marks failed (tests assert each branch)
    - the loop ticks immediately, re-raises CancelledError, and a tick Exception logs WARNING + continues (test asserts loop survives a thrown tick)
    - tick processes only origin='python' candidates (relies on commit_db SELECT; test asserts find_confirmed_candidates is the source)
  </acceptance_criteria>
  <done>commit_watchdog drains origin='python' drafts with a pre-commit fidelity hold; never-throws; commit-watchdog parity green.</done>
</task>

<task type="auto">
  <name>Task 2: Wire shared farmOS client + commit_watchdog task into boot.py</name>
  <files>src/farm-agent/farm_agent/boot.py, src/farm-agent/tests/test_boot.py</files>
  <read_first>
    - src/farm-agent/farm_agent/boot.py (lines 73-135: the httpx client + signal client construction, the confirm_watchdog create_task at 104-105, and the shutdown cancel block 121-135 — mirror this for the commit watchdog)
    - src/farm-agent/tests/test_boot.py (the boot-completes test that cancels the task after boot)
    - src/farm-agent/farm_agent/farmos/client.py (create_farmos_client signature)
  </read_first>
  <action>
    In boot.main(), after the existing client construction, build the shared farmOS
    client via create_farmos_client(config.farmos_url, config.farmos_username,
    config.farmos_password, http) reusing the existing httpx.AsyncClient `http`. Add
    `commit_watchdog_task = asyncio.create_task(commit_watchdog_loop(pool, farmos_client,
    config))` alongside the confirm_watchdog task. In the shutdown block, cancel and
    await commit_watchdog_task with the CancelledError swallow pattern (mirroring the
    confirm/retention tasks). Do NOT log any config field or secret (T-56-06-01). Only
    construct the farmOS client when config.farmos_integration is true; when false,
    skip the watchdog task (mirror the Node farmosIntegration gate). Update test_boot to
    assert the commit watchdog task is created (and not created when farmos_integration
    is false).
  </action>
  <verify>
    <automated>cd src/farm-agent && uv run pytest tests/test_boot.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "commit_watchdog_loop" src/farm-agent/farm_agent/boot.py` returns 1 or more
    - exactly one commit_watchdog task created when farmos_integration is true; none when false (test asserts)
    - the task is cancelled + awaited with CancelledError swallowed on shutdown (test asserts clean shutdown, no warning)
    - no config field/secret is logged at boot (grep boot.py shows no f-string interpolating farmos_password/url into a log call)
  </acceptance_criteria>
  <done>boot wires a shared farmOS client + one commit_watchdog task, gated on farmos_integration, clean shutdown; test_boot green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| watchdog → shared signal_draft (drain) | the live Python drain of origin='python' confirmed drafts |
| watchdog → farmOS via client | confirmed farmer data written to the farm record of truth |
| CSV → pre-commit gate | non-authoritative cross-check gating the write |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-62-30 | Tampering | committing a strain-mismatched draft | mitigate | fidelity gate runs BEFORE the router call; mismatch held as fidelity_cross_check_unverified (no commit) |
| T-62-31 | Denial of Service | a tick exception killing the drain loop | mitigate | never-throws loop (WARNING + continue), CancelledError re-raise |
| T-62-32 | Elevation of Privilege | second drain poller / wrong DB | mitigate | exactly one task wired in boot, gated on farmos_integration; reads origin='python' candidates only |
| T-62-33 | Information Disclosure | secret/config in boot logs | mitigate | T-56-06-01: never log config fields/secrets (only lifecycle lines) |
| T-62-SC | Tampering | pip installs | accept | no new package |
</threat_model>

<verification>
- Watchdog drains origin='python' candidates, holds fidelity mismatches, marks committed/failed/requeued, never-throws.
- boot wires one watchdog task gated on farmos_integration; clean shutdown; no secret logging.
</verification>

<success_criteria>
FWR-04 + FWR-01 + FWR-03: the live commit drain is origin-guarded, executes commits via
the router, and holds CSV-fidelity mismatches before any farmOS write.
</success_criteria>

<output>
Create `.planning/phases/62-farmos-write-path/62-11-SUMMARY.md` when done.
</output>
