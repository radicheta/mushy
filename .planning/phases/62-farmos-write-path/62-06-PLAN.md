---
phase: 62-farmos-write-path
plan: 06
type: execute
wave: 3
depends_on: ["62-02", "62-03"]
files_modified:
  - src/farm-agent/farm_agent/farmos/logs.py
  - src/farm-agent/tests/test_farmos_logs.py
autonomous: true
requirements: [FWR-02]
must_haves:
  truths:
    - "create_log builds a log--{type} JSON:API payload with the mushy:draft:{draftId} notes marker and rejects non-native types with UnsupportedLogTypeError"
    - "upsert_log for seeding looks up by the stable key (type=seeding, asset.id == assetIds[0]); miss -> POST-create, hit -> merge file+notes then PATCH (or noop)"
    - "non-seeding native types fall through to POST-only (null stable key)"
    - "a second upsert_log for the same seeding asset produces outcome='noop' or a single merged PATCH, never a duplicate seeding log"
  artifacts:
    - path: "src/farm-agent/farm_agent/farmos/logs.py"
      provides: "LOG_TYPES, NATIVE_LOG_TYPES, UnsupportedLogTypeError, LOG_STABLE_KEYS, create_log, upsert_log"
      contains: "def upsert_log"
  key_links:
    - from: "logs.upsert_log"
      to: "farmOS /api/log/seeding?filter[asset.id][value]"
      via: "stable-key lookup then PATCH-merge-or-POST"
      pattern: "filter\\[asset.id\\]\\[value\\]"
---

<objective>
Faithful port of the Node log layer (`farmos/logs.js`): native log types, the
`mushy:draft:{draftId}` notes marker, and the Phase-51 upsert-by-stable-key for
`seeding` (B5 invariant: exactly one seeding log per child asset). The seeding stable
key makes a re-run idempotent — a second commit merges file+notes into the existing
log instead of creating a duplicate (FWR-02, SC2).

Purpose: the log half of upsert-by-stable-identity. Output: farm_agent/farmos/logs.py
+ tests (faithful to logs.test.js cases).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/62-farmos-write-path/62-CONTEXT.md
@.planning/phases/62-farmos-write-path/62-PATTERNS.md

<interfaces>
From src/farm-agent/farm_agent/farmos/client.py (Plan 02): client["get"/"post"/"patch"]
From src/farm-agent/farm_agent/farmos/merge.py (Plan 03): STABLE_NOTES_SEPARATOR = "\n---\n"
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Port logs.py (create_log + LOG_STABLE_KEYS + helpers)</name>
  <files>src/farm-agent/farm_agent/farmos/logs.py, src/farm-agent/tests/test_farmos_logs.py</files>
  <read_first>
    - src/agents/alerter/src/farmos/logs.js (lines 1-94: NATIVE_LOG_TYPES, LOG_TYPES, UnsupportedLogTypeError, LogIdentityCollision, LOG_STABLE_KEYS 45-53, _buildNoteValue 55-57, _buildLogBody 59-81, createLog 83-94)
    - src/agents/alerter/test/farmos/logs.test.js (the create_log + stable-key cases to mirror)
    - src/farm-agent/farm_agent/farmos/merge.py (STABLE_NOTES_SEPARATOR import)
  </read_first>
  <behavior>
    - create_log seeding: builds {data:{type:"log--seeding", attributes:{name, timestamp:floor, status:"done", notes:{value:"<notes>\nmushy:draft:<id>", format:"plain_text"}}, relationships:{asset:{data:[{type:"asset--fungi", id}]}}}}; POST /api/log/seeding; returns {ok, logId}
    - create_log non-native: create_log(client, "seeding_session", ...) raises UnsupportedLogTypeError
    - LOG_STABLE_KEYS["seeding"]({asset_ids:[A]}) -> {"path": "/api/log/seeding?filter[asset.id][value]=<enc A>"}; activity/input/observation/harvest -> None
    - file relationship: fileIds present -> relationships.file.data list of file--file
  </behavior>
  <action>
    Port the module-level constants NATIVE_LOG_TYPES=["seeding","activity","input",
    "observation","harvest"], LOG_TYPES=[*NATIVE,"seeding_session"], exception classes
    UnsupportedLogTypeError(log_type) and LogIdentityCollision(log_type, asset_id,
    matched_ids). LOG_STABLE_KEYS as a dict mapping "seeding" to a callable returning
    the filter[asset.id][value] path (urllib.parse.quote) or None when asset_ids
    empty, and the other four to None. `_build_note_value(notes, draft_id)` appends
    "mushy:draft:" + draft_id. `_build_log_body(log_type, opts)` builds the JSON:API
    payload with math.floor(timestamp), status "done", notes marker, asset+file
    relationships. async `create_log(client, log_type, opts)` rejects non-native with
    UnsupportedLogTypeError then POSTs /api/log/<type>, returning {"ok", "logId",
    "http_status"} or {"ok": False, "reason": "http_<status|network>"}.
  </action>
  <verify>
    <automated>cd src/farm-agent && uv run pytest tests/test_farmos_logs.py -x -q -k "create or stable or marker"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "mushy:draft:" src/farm-agent/farm_agent/farmos/logs.py` returns 1 or more
    - create_log raises UnsupportedLogTypeError for "seeding_session" (test asserts)
    - LOG_STABLE_KEYS["seeding"] returns the filter[asset.id][value] path; other native types map to None
    - _build_log_body floors timestamp and sets status "done" + notes marker (test asserts payload shape)
  </acceptance_criteria>
  <done>create_log + stable-key constants + note marker ported; create/marker tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Port upsert_log (lookup-by-stable-key -> merge-PATCH-or-POST)</name>
  <files>src/farm-agent/farm_agent/farmos/logs.py, src/farm-agent/tests/test_farmos_logs.py</files>
  <read_first>
    - src/agents/alerter/src/farmos/logs.js (lines 96-330: _setUnionRefs, _mergeNotes, _arraysEqualById, _sortMatches, _emitAudit, upsertLog 149-319 — the full upsert flow incl 412 soft-compare retry)
    - src/agents/alerter/test/farmos/logs.test.js (the upsert miss/hit/noop/collision/identity-mismatch cases)
  </read_first>
  <behavior>
    - null stable key (activity): upsert_log delegates to create_log, returns outcome="created"
    - seeding miss: stable-key lookup returns empty -> create_log, outcome="created"
    - seeding hit noop: existing log already has the file+notes -> no PATCH, outcome="noop"
    - seeding hit patch: incoming adds a file id -> set-union file + dedup notes -> PATCH, outcome="patched"
    - identity mismatch: existing asset.data != incoming asset.data -> {ok:False, reason:"log_identity_mismatch"}
    - collision: >1 match -> sort oldest-first, pick canonical, warnings includes "LogIdentityCollision:<n>"
  </behavior>
  <action>
    Port `upsert_log(client, type, opts)`: reject non-native with UnsupportedLogTypeError;
    keyFn = LOG_STABLE_KEYS[type]; if None -> create_log pass-through (outcome="created").
    Else build the key; None -> {ok:False, reason:"missing_stable_key"}. GET the stable
    path; non-ok -> http error. Empty matches -> create_log (outcome="created"). Hits:
    _sort_matches (by attributes.created then id), pick canonical[0], on >1 append
    "LogIdentityCollision:<n>" to warnings (+ optional audit). GET the full canonical
    log, compute incoming via _build_log_body, assert asset.data equality
    (_arrays_equal_by_id) else log_identity_mismatch, set-union file refs
    (_set_union_refs), merge notes (_merge_notes split-dedup-join), compute
    scalar conflicts on timestamp/status/name, if neither file nor notes changed ->
    outcome="noop", else PATCH with If-Match (soft revision_id) and a single 412
    soft-compare retry (re-GET, re-merge, re-PATCH). Return {ok, logId, outcome,
    conflicts, etag_source, http_status, warnings}. Reason strings verbatim to Node.
  </action>
  <verify>
    <automated>cd src/farm-agent && uv run pytest tests/test_farmos_logs.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - Re-run idempotency: two upsert_log calls for the same (seeding, asset_id) yield at most one created log; the second is outcome in {"noop","patched"} (test asserts no second POST)
    - identity mismatch returns reason "log_identity_mismatch" when asset.data differs
    - file set-union dedups by id; notes merge dedups on the separator (tests assert)
    - >1 stable-key match yields a warnings entry "LogIdentityCollision:<n>" and picks the oldest
  </acceptance_criteria>
  <done>upsert_log lookup-merge-PATCH-or-POST ported with 412 retry, collision + identity guards; logs.test.js parity green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| draft log fields → farmOS log create/patch | a duplicate or mis-merged seeding log corrupts the farm event record |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-62-15 | Tampering | duplicate seeding logs on re-run | mitigate | stable-key lookup-then-upsert; B5 one-log-per-asset invariant |
| T-62-16 | Tampering | wrong log merged onto another asset's log | mitigate | _arrays_equal_by_id asset-identity guard -> log_identity_mismatch (no PATCH) |
| T-62-17 | Repudiation | lost audit trail on log collision | mitigate | LogIdentityCollision warning + oldest-canonical pick (no silent overwrite) |
| T-62-SC | Tampering | pip installs | accept | stdlib only, no new package |
</threat_model>

<verification>
- create_log + stable-key + marker; upsert miss/hit/noop/patch/collision/identity-mismatch parity with logs.test.js.
- Re-run produces 0 duplicate seeding logs.
</verification>

<success_criteria>
FWR-02 (log half): upsert-by-stable-key for seeding is byte-faithful to Node; re-runs
do not duplicate logs.
</success_criteria>

<output>
Create `.planning/phases/62-farmos-write-path/62-06-SUMMARY.md` when done.
</output>
