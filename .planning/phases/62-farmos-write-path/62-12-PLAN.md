---
phase: 62-farmos-write-path
plan: 12
type: execute
wave: 8
depends_on: ["62-11"]
files_modified:
  - src/farm-agent/tests/test_farmos_live_fire.py
  - src/farm-agent/tests/fixtures/farmos/live_fire_seeding_draft.json
autonomous: false
requirements: [FWR-01, FWR-02, FWR-03]
must_haves:
  truths:
    - "Running the Python commit path twice against dev farmOS :18080 creates 0 duplicate asset--fungi on the second run (upsert-by-name / stable identity)"
    - "An attached image uploads via POST /api/asset/{type}/{uuid}/image and appears on the asset's image field in dev farmOS"
    - "A strain-mismatch draft is held as fidelity_cross_check_unverified and NOT committed against dev"
  artifacts:
    - path: "src/farm-agent/tests/test_farmos_live_fire.py"
      provides: "dev-farmOS live-fire: double-run dedup + image-on-field + fidelity-hold assertions"
      contains: "FWR_LIVE_FIRE"
  key_links:
    - from: "test_farmos_live_fire"
      to: "dev farmOS :18080 /api/asset/fungi + /api/asset/{type}/{uuid}/image"
      via: "commit_router.commit run twice + JSON:API count/image-field query"
      pattern: "filter\\[name\\]\\[value\\]"
---

<objective>
In-phase live-fire against dev farmOS :18080 (D-04 — IN scope, NOT deferred). Prior
phases 58-60 deferred their real-model runs; Phase 62's upsert/image/fidelity SCs NEED
a real dev-farmOS commit because the write path is exactly the kind of wiring seam unit
tests miss ([[feedback_unit_tests_dont_catch_wiring]]). Dev creds are resolved
([[project_dev_farmos_18080_rejects_prod_bot_creds]]).

The live-fire runs the Python commit path twice against dev and asserts:
  (a) 0 duplicate asset--fungi created on the second run (upsert-by-name, SC2);
  (b) the uploaded image appears on the asset's image field (SC3);
  (c) a strain-mismatch draft is held as fidelity_cross_check_unverified (SC4).

Gated by FWR_LIVE_FIRE=1 + dev creds in env (mirrors test_gate_live_fire.py opt-in);
never runs in CI.

Purpose: prove FWR-01/FWR-02/FWR-03 against real dev farmOS. Output: a live-fire test +
seeding draft fixture, plus operator confirmation of the asset/image in the dev UI.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/62-farmos-write-path/62-CONTEXT.md
@.planning/phases/62-farmos-write-path/62-PATTERNS.md

<interfaces>
From src/farm-agent/farm_agent/farmos/client.py (Plan 02): create_farmos_client
From src/farm-agent/farm_agent/farmos/commits/commit_router.py (Plan 10): commit(client, draft, ctx)
From src/farm-agent/farm_agent/farmos/assets.py (Plan 07): find_asset_by_name, _clear_cache
From src/farm-agent/farm_agent/farmos/fidelity_gate.py (Plan 04): check_fidelity, load_fidelity_csv
Dev farmOS: FARMOS_URL ":18080", FARMOS_USERNAME/FARMOS_PASSWORD (dev mushy-bot creds), both API-named "Mossrock"; image route POST /api/asset/{type}/{uuid}/image
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 0: Confirm dev :18080 creds + FWR_LIVE_FIRE env before live-fire</name>
  <action>Operator action: confirm dev farmOS :18080 reachability + valid dev mushy-bot creds, then export FARMOS_URL (dev :18080), FARMOS_USERNAME, FARMOS_PASSWORD, and FWR_LIVE_FIRE=1 into the run environment so Task 1 can execute against dev (never prod :8082).</action>
  <what-built>The full Python write path (client, merge, upsert, router, watchdog) is
  ready to commit against real dev farmOS. The live-fire needs dev creds + reachability;
  these are operator-provided (sandbox blocks reading secrets.env from bash).</what-built>
  <how-to-verify>
    1. Confirm dev farmOS :18080 is reachable from the run host and the dev mushy-bot
       creds are valid (login 200). Dev creds were RESOLVED (password reset to match
       secrets.env, [[project_dev_farmos_18080_rejects_prod_bot_creds]]).
    2. Export into the run environment: FARMOS_URL pointing at dev :18080,
       FARMOS_USERNAME + FARMOS_PASSWORD (dev mushy-bot), and FWR_LIVE_FIRE=1.
    3. Confirm the run targets DEV, never prod :8082 (the live-fire creates real assets;
       dev is rebuild-acceptable, prod is not).
  </how-to-verify>
  <resume-signal>Type "creds-set" once dev :18080 is reachable with valid creds and FWR_LIVE_FIRE=1 is exported. Then the live-fire test runs.</resume-signal>
</task>

<task type="auto">
  <name>Task 1: Live-fire double-run dedup + image-on-field + fidelity-hold against dev :18080</name>
  <files>src/farm-agent/tests/test_farmos_live_fire.py, src/farm-agent/tests/fixtures/farmos/live_fire_seeding_draft.json</files>
  <read_first>
    - src/farm-agent/tests/test_gate_live_fire.py (lines 1-40: the operator-run opt-in env-gated live-fire skip pattern to mirror — skip unless FWR_LIVE_FIRE=1 + creds present)
    - src/farm-agent/farm_agent/farmos/commits/commit_router.py (commit entrypoint)
    - src/farm-agent/farm_agent/farmos/assets.py (find_asset_by_name to count assets by name; _clear_cache to force a real second-run lookup)
    - 62-CONTEXT.md D-04 (the exact assertions) + .planning/STATE.md (Phase 55B re-smoke mechanics: dev :18080 idempotent-reuse, /api not /jsonapi)
  </read_first>
  <action>
    Create test_farmos_live_fire.py, skipped unless FWR_LIVE_FIRE=1 AND FARMOS_URL +
    FARMOS_USERNAME + FARMOS_PASSWORD are set (mark live_fire). Build a real
    create_farmos_client against dev. Load a seeding draft fixture
    (live_fire_seeding_draft.json — a unique block_name keyed off a timestamp so the
    test is re-runnable, an attached image path to a small committed test JPEG, and a
    strain present in the curated-14 set). Run A: commit_router.commit -> assert ok,
    record the created asset id; query GET /api/asset/fungi?filter[name][value]=<name>
    and assert exactly 1 asset. Run B: _clear_cache() then commit_router.commit again ->
    assert ok and outcome reflects reuse; re-query the name filter and assert STILL
    exactly 1 asset (0 duplicates created — SC2). Image (SC3): GET the asset (or its
    group for a session draft) and assert the `image` relationship/field is non-empty
    and references the uploaded file id. Fidelity (SC4): build a strain-mismatch draft
    (block name says one strain, fidelity CSV fixture says another), run check_fidelity
    + the commit path, and assert it is held as fidelity_cross_check_unverified and NO
    asset is created for it. Use /api (not /jsonapi — wrong for this farmOS). Persist
    the created uuids to a paper-trail artifact under the run dir per
    [[feedback_keep_paper_trail_of_intermediates]].
  </action>
  <verify>
    <automated>cd src/farm-agent && FWR_LIVE_FIRE=1 uv run pytest tests/test_farmos_live_fire.py -x -q -m live_fire</automated>
  </verify>
  <acceptance_criteria>
    - Second run creates 0 duplicate asset--fungi: the name-filter query returns exactly 1 asset after both runs (test asserts count == 1)
    - The image appears on the asset's `image` field in dev farmOS (test asserts the field is non-empty and matches the uploaded file id)
    - A strain-mismatch draft is held as fidelity_cross_check_unverified and creates 0 assets (test asserts)
    - The test is SKIPPED (not failed) when FWR_LIVE_FIRE/creds are absent (never runs in CI)
    - A uuid paper-trail artifact is written for the run
  </acceptance_criteria>
  <done>Live-fire proves 0-duplicate upsert + image-on-field + fidelity-hold against dev :18080.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Operator-confirm the single asset + image in dev farmOS UI</name>
  <action>Operator action: visually confirm in the dev farmOS :18080 UI that exactly one asset exists for the run's unique block name, its image renders on the image field, and the strain-mismatch draft produced no asset.</action>
  <what-built>The live-fire created a fungi asset (and/or session group) on dev farmOS
  :18080 and attached an image to its image field, then proved a second run added 0
  duplicates and a strain-mismatch draft was held.</what-built>
  <how-to-verify>
    1. Open dev farmOS :18080 (API-named "Mossrock") and find the asset created by the
       live-fire (by the unique timestamped block name from the run paper-trail).
    2. Confirm exactly ONE asset exists with that name (no duplicate from run B).
    3. Confirm the attached image renders on the asset's image field.
    4. Confirm the strain-mismatch draft did NOT produce an asset (held as
       fidelity_cross_check_unverified).
  </how-to-verify>
  <resume-signal>Type "verified" once the single asset + its image are confirmed in the dev farmOS UI and the mismatch draft produced no asset, or describe any discrepancy.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| live-fire run → dev farmOS :18080 | real writes to a real (rebuild-acceptable) farmOS; must never target prod :8082 |
| dev creds in env | secrets present in the run environment only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-62-34 | Elevation of Privilege | live-fire accidentally hitting prod :8082 | mitigate | checkpoint confirms FARMOS_URL is dev :18080; test asserts the configured url is the dev port before any write |
| T-62-35 | Information Disclosure | dev creds leaking to logs/paper-trail | mitigate | paper-trail stores uuids only, never creds; client never logs password/cookie |
| T-62-36 | Tampering | dirty dev state masking a real duplicate | mitigate | unique timestamped block name per run + name-filter count assertion (not a global asset count) |
| T-62-SC | Tampering | pip installs | accept | no new package |
</threat_model>

<verification>
- SC2: second run -> 0 duplicate asset--fungi (name-filter count == 1).
- SC3: image on the asset image field in dev farmOS.
- SC4: strain-mismatch held as fidelity_cross_check_unverified, 0 assets.
- Operator confirms the single asset + image in the dev UI.
</verification>

<success_criteria>
SC2 + SC3 + SC4 proven live against dev farmOS :18080: byte-identical upsert (0 dupes),
field-scoped image upload lands on the image field, and the fidelity gate holds
mismatches.
</success_criteria>

<output>
Create `.planning/phases/62-farmos-write-path/62-12-SUMMARY.md` when done.
</output>
