---
phase: 62-farmos-write-path
plan: "03"
subsystem: farmos-merge
tags: [merge, port, tdd, cross-language, golden-fixture]
dependency_graph:
  requires: ["62-01"]
  provides: ["merge_asset_fields", "IdentityMutationError", "STABLE_NOTES_SEPARATOR", "merge_golden.json"]
  affects: ["farm_agent.farmos.assets (upsert merge step)"]
tech_stack:
  added: []
  patterns: ["faithful-port", "TDD RED/GREEN", "cross-language golden fixture"]
key_files:
  created:
    - src/farm-agent/farm_agent/farmos/merge.py
    - src/farm-agent/tests/test_farmos_merge.py
    - src/farm-agent/tests/fixtures/farmos/merge_golden.json
  modified: []
decisions:
  - "Golden fixture generated by running Node merge.js on merge.test.js inputs via one-off script; 8 cases captured"
  - "Python dict ordering does not affect equality since tests compare via == (order-insensitive for dicts)"
  - "Used copy.deepcopy as equivalent of JSON.parse(JSON.stringify(...)) -- correct for JSON-serializable asset dicts"
metrics:
  duration: "~10 minutes"
  completed: "2026-06-28"
  tasks: 3
  files_created: 3
  tests: 19
requirements: [FWR-02]
---

# Phase 62 Plan 03: merge_asset_fields byte-identical port -- Summary

Faithful Python port of `src/agents/alerter/src/farmos/merge.js` with a cross-language golden fixture proving byte-identical semantics between Node and Python for all merge cases.

## What Was Built

`merge_asset_fields(existing, incoming) -> {"merged": dict, "conflicts": list}` with exact field-merge rules ported from Node:

- `STABLE_NOTES_SEPARATOR = "\n---\n"` constant
- `IdentityMutationError(field, existing, incoming)` exception with `.field`, `.existing`, `.incoming` attrs
- `_union_array_ref`: set-union by id, existing-first insertion order (mirrors `unionArrayRef`)
- `_merge_notes`: split/strip/filter/dedup/join on separator (mirrors `mergeNotes`)
- Full `merge_asset_fields` field-by-field merge: array-ref union, scalar-rel null=take/equal=noop/differ=conflict, scalar-attr same rule, notes, identity checks

## Tests

19 tests, all green:
- 7 cross-language golden parity cases (parametrized from `merge_golden.json`)
- 1 golden identity-mutation case (asserts thrown shape matches Node)
- 11 behavioral unit tests mirroring `merge.test.js`

## Cross-Language Golden Fixture

`tests/fixtures/farmos/merge_golden.json`: 8 cases generated by running Node `merge.js` on `merge.test.js` inputs. Cases cover:

1. `array_ref_union_order` -- set-union preserves existing-first order
2. `array_ref_union_dedup` -- set-union dedup drops duplicate ids
3. `scalar_rel_equal_noop` -- equal value is a noop
4. `scalar_rel_differ_conflict` -- differing value surfaces conflict, retains existing
5. `notes_dedup` -- split-dedup-join on separator
6. `stub_marker_survives` -- STUB marker preserved through merge
7. `scalar_rel_null_take` -- null existing takes incoming value
8. `identity_mutation_name` -- captured as `thrown` shape (field/existing/incoming/message)

## Deviations from Plan

None - plan executed exactly as written.

## TDD Gate Compliance

1. RED commit `97c6045`: `test(62-03)` -- failing tests (ModuleNotFoundError for merge.py)
2. GREEN commit `48059cf`: `feat(62-03)` -- implementation; 19 tests pass
3. No REFACTOR pass needed (helpers already extracted as `_union_array_ref` / `_merge_notes`)

## Known Stubs

None.

## Threat Flags

No new security surface. `merge_asset_fields` is a pure function operating on already-fetched dicts; no network calls, no DB writes, no file access.

## Self-Check: PASSED

- `/mnt/slime-kingdom/opt/mushy/src/farm-agent/farm_agent/farmos/merge.py` exists
- `/mnt/slime-kingdom/opt/mushy/src/farm-agent/tests/test_farmos_merge.py` exists
- `/mnt/slime-kingdom/opt/mushy/src/farm-agent/tests/fixtures/farmos/merge_golden.json` exists
- Commits `97c6045` and `48059cf` verified in git log
- `grep -c "def merge_asset_fields" merge.py` = 1
- `grep -c "class IdentityMutationError" merge.py` = 1
- Golden fixture has 8 cases (>= 6 required)
- 19 tests pass
