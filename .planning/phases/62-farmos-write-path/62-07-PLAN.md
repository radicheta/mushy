---
phase: 62-farmos-write-path
plan: 07
type: execute
wave: 4
depends_on: ["62-03", "62-05"]
files_modified:
  - src/farm-agent/farm_agent/farmos/assets.py
  - src/farm-agent/tests/test_farmos_assets.py
autonomous: true
requirements: [FWR-02]
must_haves:
  truths:
    - "find_asset_by_name resolves name->id via filter[name][value], LRU-cached, transport failure is not a miss"
    - "upsert_fungi_asset: miss -> POST create (outcome='created'); hit -> GET + merge_asset_fields -> PATCH if non-noop (outcome='patched') else outcome='noop'; scalar conflict keeps existing (outcome='noop', conflicts populated)"
    - "every created asset carries the mushy:draft:{draftId} notes trailer and fungi_type(strain)+fungi_xing relationships"
    - "a second upsert_fungi_asset for the same name creates 0 new assets (name-based stable identity)"
  artifacts:
    - path: "src/farm-agent/farm_agent/farmos/assets.py"
      provides: "find_asset_by_name, create_fungi_asset, upsert_fungi_asset, delete_fungi_asset"
      contains: "def upsert_fungi_asset"
  key_links:
    - from: "assets.upsert_fungi_asset"
      to: "merge.merge_asset_fields"
      via: "GET existing -> merge -> PATCH-or-noop"
      pattern: "merge_asset_fields"
---

<objective>
Faithful port of the Node fungi-asset upsert primitive (`farmos/assets.js`): name-based
stable identity (D-05), the create/patch/noop merge cycle through merge.py, the
LRU name cache, the `mushy:draft:{draftId}` trailer, and soft revision_id concurrency.
This is the asset half of upsert-by-stable-identity and the core of SC2 (a second
commit creates 0 duplicate assets).

Purpose: FWR-02. Output: farm_agent/farmos/assets.py + tests (faithful to assets.test.js).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/62-farmos-write-path/62-CONTEXT.md
@.planning/phases/62-farmos-write-path/62-PATTERNS.md

<interfaces>
From src/farm-agent/farm_agent/farmos/merge.py (Plan 03): merge_asset_fields(existing, incoming) -> {"merged","conflicts"}; IdentityMutationError
From src/farm-agent/farm_agent/farmos/qr.py (Plan 05): bind_qr_on_create(payload, qr_codes)
From src/farm-agent/farm_agent/farmos/fungi_type_cache.py (Plan 05): ensure_fungi_type_uuid(client, name, *, create=False)
From src/farm-agent/farm_agent/farmos/fungi_xing_cache.py (Plan 05): get_fungi_xing_uuid(client, name)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Port find_asset_by_name + create_fungi_asset + LRU name cache + _build_asset_body</name>
  <files>src/farm-agent/farm_agent/farmos/assets.py, src/farm-agent/tests/test_farmos_assets.py</files>
  <read_first>
    - src/agents/alerter/src/farmos/assets.js (lines 22-141: NAME_CACHE LRU 22-46, findAssetByName 48-61, _buildAssetBody 67-120, createFungiAsset 122-135, resolveOrCreateAsset 137-141, isStubAsset 143-148, STUB_BACKFILL_MARKER 20)
    - src/agents/alerter/test/farmos/assets.test.js (find/create cache cases)
    - src/farm-agent/farm_agent/farmos/qr.py + fungi_type_cache.py + fungi_xing_cache.py (Plan 05 signatures)
  </read_first>
  <behavior>
    - find_asset_by_name hit: GET /api/asset/fungi?filter[name][value]=<enc>; data[0] -> {"found": True, "asset_id": id}; caches name->id
    - find_asset_by_name cached: second call returns {"found": True, "asset_id": id, "cached": True} without a GET
    - find_asset_by_name transport failure: ok=False -> {"found": False, "error": "http_<status|network>"} (not a miss)
    - create_fungi_asset: resolves fungi_type (strain) + fungi_xing terms, builds payload with name/status:active/notes-with-mushy-draft-trailer/relationships, binds qr id_tag, POSTs /api/asset/fungi -> {"ok": True, "assetId", outcome via wrapper}
    - missing fungi_type_name (allow_no_fungi_type False) -> {"ok": False, "reason": "missing_fungi_type_name"}
  </behavior>
  <action>
    Port assets.py. Module-level cap-32 OrderedDict NAME_CACHE with _cache_get
    (move-to-end), _cache_set (evict oldest beyond 32), _clear_cache. STUB_BACKFILL_MARKER
    string verbatim + pure is_stub_asset predicate. async `find_asset_by_name(client,
    name)` per behavior. async `_build_asset_body(client, opts)` -> resolves
    ensure_fungi_type_uuid(create=opts.create_missing_fungi_type) + get_fungi_xing_uuid,
    builds attributes {name, status:"active", notes:{value: (notes+"\n" if notes)+
    "mushy:draft:"+draft_id, format:"plain_text"}}, relationships fungi_xing (+ fungi_type
    if resolved, + parent if parent_ids), payload type "asset--fungi", and qr.bind_qr_on_create
    when qr_codes; returns {"ok", "payload", "attributes", "relationships"} or
    {"ok": False, "reason"}. async `create_fungi_asset(client, opts)` POSTs and caches
    name->assetId. Keep opts keys snake_case (name, parent_ids, fungi_type_name,
    fungi_xing_name, draft_id, qr_codes, notes, allow_no_fungi_type, create_missing_fungi_type)
    and document the JS camelCase->snake mapping in a header comment.
  </action>
  <verify>
    <automated>cd src/farm-agent && uv run pytest tests/test_farmos_assets.py -x -q -k "find or create or cache or build"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "filter\[name\]\[value\]" src/farm-agent/farm_agent/farmos/assets.py` returns 1
    - `grep -c "mushy:draft:" src/farm-agent/farm_agent/farmos/assets.py` returns 1 or more
    - find_asset_by_name caches: a second lookup for the same name makes 0 additional GETs (test asserts fake client GET count)
    - create_fungi_asset with no fungi_type_name (and allow_no_fungi_type False) returns reason "missing_fungi_type_name"
    - created payload type is "asset--fungi" with fungi_xing + fungi_type relationships and id_tag bound for qr_codes
  </acceptance_criteria>
  <done>find/create + LRU cache + body builder + stub predicate ported; subset tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Port upsert_fungi_asset (merge cycle + soft-revision concurrency)</name>
  <files>src/farm-agent/farm_agent/farmos/assets.py, src/farm-agent/tests/test_farmos_assets.py</files>
  <read_first>
    - src/agents/alerter/src/farmos/assets.js (lines 150-331: _isMergeNoop 153-174, upsertFungiAsset 189-312 incl singleton-rel normalization, soft revision compare + 1 retry, deleteFungiAsset 317-331)
    - src/farm-agent/farm_agent/farmos/merge.py (merge_asset_fields + IdentityMutationError)
  </read_first>
  <behavior>
    - miss: name not found -> create_fungi_asset -> {ok:True, assetId, outcome:"created", conflicts:[]}
    - hit noop: GET existing, merge yields no structural change -> no PATCH, outcome="noop"
    - hit patch: merge adds a parent/qr ref -> PATCH merged body -> outcome="patched"
    - hit conflict: differing scalar rel -> outcome="noop", conflicts non-empty, merged keeps existing (no PATCH)
    - identity mutation: incoming name != existing -> {ok:False, reason:"identity_mutation"}
    - second upsert same name+same fields -> outcome in {"noop"}; creates 0 assets
  </behavior>
  <action>
    Port `upsert_fungi_asset(client, opts)`. Miss path delegates to create_fungi_asset
    and wraps outcome="created". Hit path: _build_asset_body, normalize incoming
    fungi_type/fungi_xing from array `{data:[{...}]}` to singleton `{data:{...}}` (the
    shape merge.py SCALAR_REL_FIELDS expects). `_get_existing()` GETs /api/asset/fungi/<id>.
    `_attempt()`: GET existing; read drupal_internal__revision_id (etag_source
    "soft_compare" if present else "absent"); merge_asset_fields catching
    IdentityMutationError -> identity result; conflicts>0 -> noop with conflicts;
    _is_merge_noop -> noop; else soft re-GET compare revision (mismatch -> race);
    PATCH /api/asset/fungi/<id> with optional If-Match header. Retry budget 1 on race;
    still racing -> outcome="noop", reason="concurrency_loss". `_is_merge_noop` compares
    normalized attributes (notes value-only, drop drupal_internal__revision_id) +
    relationships via stable JSON dump. delete_fungi_asset best-effort + name-cache
    invalidation. Return {ok, assetId, outcome, conflicts, etag_source, http_status}.
  </action>
  <verify>
    <automated>cd src/farm-agent && uv run pytest tests/test_farmos_assets.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "def upsert_fungi_asset" src/farm-agent/farm_agent/farmos/assets.py` returns 1
    - Re-run: two upsert_fungi_asset calls for the same name with identical fields create exactly ONE asset; the second is outcome="noop" with 0 PATCH (test asserts client POST count==1 and no PATCH on the second)
    - differing scalar rel yields outcome="noop" with non-empty conflicts and NO PATCH (merged keeps existing)
    - identity mutation (changed name) returns reason "identity_mutation"
    - soft revision race retries exactly once then returns concurrency_loss
  </acceptance_criteria>
  <done>upsert_fungi_asset merge cycle ported; re-run creates 0 duplicates; assets.test.js parity green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| draft asset fields → farmOS asset create/patch | a duplicate or silently-overwritten fungi asset corrupts lineage/strain |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-62-18 | Tampering | duplicate asset on re-run | mitigate | name-based find-then-upsert; outcome="created" only on true miss |
| T-62-19 | Tampering | silent strain/identity overwrite | mitigate | merge conflict-keep-existing + IdentityMutationError -> identity_mutation (no PATCH) |
| T-62-20 | Tampering | lost update under concurrent commit | mitigate | soft revision_id compare + single retry -> concurrency_loss (no blind PATCH) |
| T-62-SC | Tampering | pip installs | accept | stdlib json/collections only, no new package |
</threat_model>

<verification>
- find/create/cache + upsert merge cycle parity with assets.test.js.
- Second upsert for the same name creates 0 assets (SC2 core).
</verification>

<success_criteria>
FWR-02 (asset half): name-based upsert is byte-faithful to Node; re-run yields 0
duplicate assets.
</success_criteria>

<output>
Create `.planning/phases/62-farmos-write-path/62-07-SUMMARY.md` when done.
</output>
