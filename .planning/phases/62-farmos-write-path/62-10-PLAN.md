---
phase: 62-farmos-write-path
plan: 10
type: execute
wave: 6
depends_on: ["62-01", "62-08", "62-09"]
files_modified:
  - src/farm-agent/farm_agent/farmos/commits/commit_router.py
  - src/farm-agent/farm_agent/farmos/commit_db.py
  - src/farm-agent/tests/test_farmos_commit_router.py
  - src/farm-agent/tests/test_farmos_commit_db.py
autonomous: true
requirements: [FWR-01, FWR-04]
must_haves:
  truths:
    - "commit_router.commit dispatches one signal_draft.log_type to its handler after normalize(), returning the uniform envelope; unsupported types return reason='unsupported_log_type'"
    - "commit_db.find_confirmed_candidates SELECTs status='confirmed' AND origin='python' (the Python side of the origin guard)"
    - "every Python commit_db write that sets status confirmed/committing carries origin='python' so the live Node watchdog never drains it"
    - "acquire_commit_lock is an atomic CAS (WHERE status='confirmed') so a dup/restart race collapses to rowcount=0"
  artifacts:
    - path: "src/farm-agent/farm_agent/farmos/commit_db.py"
      provides: "find_confirmed_candidates (origin-guarded), acquire_commit_lock, mark_committed, mark_failed, requeue_for_retry, release_stale_locks"
      contains: "origin='python'"
    - path: "src/farm-agent/farm_agent/farmos/commits/commit_router.py"
      provides: "commit(client, draft, ctx) dispatch table over all six handlers"
      contains: "DISPATCH"
  key_links:
    - from: "commit_db.find_confirmed_candidates"
      to: "signal_draft"
      via: "SELECT WHERE status='confirmed' AND origin='python'"
      pattern: "origin='python'"
---

<objective>
Port the commit router (`commits/commit-router.js`) and the Python commit-lifecycle DAO
(`commit-db.js`) — the latter being the Python SIDE of the origin guard. The router
dispatches a draft's log_type to one of the six handlers after normalize(). The DAO's
find_confirmed_candidates SELECTs `status='confirmed' AND origin='python'`, and every
status write the Python pipeline performs stamps `origin='python'` so the live Node
watchdog (which now excludes origin='python', Plan 01) never drains Python drafts.

This plan does WRITE to the shared signal_draft (committing/committed transitions),
so it depends on Plan 01 (the redeploy checkpoint must be approved first — D-02).

Purpose: FWR-01 (router) + FWR-04 (Python origin-guarded DAO). Output: commit_router.py
+ commit_db.py + tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/62-farmos-write-path/62-CONTEXT.md
@.planning/phases/62-farmos-write-path/62-PATTERNS.md

<interfaces>
From src/farm-agent/farm_agent/farmos/commits/normalize.py (Plan 08): normalize(draft)
From src/farm-agent/farm_agent/farmos/commits/commit_*.py (Plans 08/09): commit_seeding, commit_activity, commit_input, commit_observation, commit_harvest, commit_seeding_session
From src/farm-agent/farm_agent/farmos/logs.py (Plan 06): LOG_TYPES, UnsupportedLogTypeError
From src/farm-agent/farm_agent/confirm/confirm_repo.py (existing): the never-throws psycopg3 DAO + CAS pattern (async with pool.connection(); conn.transaction(); result.rowcount)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Port commit_router.py (dispatch + uniform envelope)</name>
  <files>src/farm-agent/farm_agent/farmos/commits/commit_router.py, src/farm-agent/tests/test_farmos_commit_router.py</files>
  <read_first>
    - src/agents/alerter/src/farmos/commits/commit-router.js (full — 70 lines: DISPATCH table, commit() guard + normalize + envelope + UnsupportedLogTypeError catch)
    - src/agents/alerter/test/farmos/commit-router.test.js (dispatch + unsupported + envelope cases)
    - 62-PATTERNS.md (commit_router.py section: DISPATCH dict, envelope keys, latency_ms)
  </read_first>
  <behavior>
    - dispatch: commit(client, {log_type:"seeding", ...}) calls commit_seeding(client, normalize(draft), ctx) and returns {ok, asset_ids, log_ids, file_ids, attachments_failed, latency_ms, reason}
    - unsupported: log_type missing or not in LOG_TYPES -> {ok:False, reason:"unsupported_log_type", asset_ids:[], log_ids:[], file_ids:[], latency_ms}
    - handler exception -> {ok:False, reason:<str>, empty id lists, latency_ms}
    - normalize is applied before dispatch; the persisted draft_json is not mutated
  </behavior>
  <action>
    Port commit_router.py with a DISPATCH dict mapping the six log types to their
    handler coroutines, async `commit(client, draft, ctx=None)`: guard log_type against
    LOG_TYPES (unsupported_log_type envelope), call the handler with normalize(draft),
    measure latency via time.monotonic, normalize the result into the uniform envelope
    (asset_ids/log_ids/file_ids/attachments_failed defaulting to []), and catch
    UnsupportedLogTypeError + generic Exception into the failure envelope. No DB access
    here — pure dispatch over the injected farmOS client.
  </action>
  <verify>
    <automated>cd src/farm-agent && uv run pytest tests/test_farmos_commit_router.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "DISPATCH" src/farm-agent/farm_agent/farmos/commits/commit_router.py` returns 1 or more and the dict has all six log types
    - unknown log_type returns reason "unsupported_log_type" with empty id lists (test asserts)
    - the handler receives normalize(draft) (test asserts the normalized shape reached a fake handler) and the input draft_json is not mutated
    - a handler raising returns ok=False with the exception message as reason and a latency_ms field
  </acceptance_criteria>
  <done>commit_router dispatches all six handlers via normalize with the uniform envelope; commit-router.test.js parity green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Port commit_db.py (origin-guarded Python commit-lifecycle DAO)</name>
  <files>src/farm-agent/farm_agent/farmos/commit_db.py, src/farm-agent/tests/test_farmos_commit_db.py</files>
  <read_first>
    - src/agents/alerter/src/farmos/commit-db.js (lines 47-146: findConfirmedCandidates 47-60, acquireCommitLock 62-78, markCommitted 80-94, markFailed 96-110, requeueForRetry 112-128, releaseStaleLocks 130-146)
    - src/farm-agent/farm_agent/confirm/confirm_repo.py (the never-throws psycopg3 DAO pattern: async with pool.connection(); conn.transaction(); result.rowcount; %s params; returns {ok, rowcount})
    - 62-PATTERNS.md (commit_watchdog origin-guard SELECT note + atomic CAS pattern)
  </read_first>
  <behavior>
    - find_confirmed_candidates: SELECT * FROM signal_draft WHERE status='confirmed' AND origin='python' ORDER BY confirmed_at ASC NULLS LAST LIMIT %s; returns [] on error
    - acquire_commit_lock: UPDATE ... SET status='committing', committed_at_attempt=now(), commit_attempt_count=commit_attempt_count+1, origin='python' WHERE id=%s AND status='confirmed' RETURNING *; rowcount=1 win, 0 race-lost
    - mark_committed: UPDATE status='committed', farmos_response=%s::jsonb, committed_at=now(), origin='python' WHERE id=%s AND status='committing'
    - mark_failed: UPDATE status='commit_failed', commit_failed_reason=%s, committed_at=now() WHERE id=%s AND status='committing'
    - requeue_for_retry: UPDATE status='confirmed', origin='python' WHERE id=%s AND status='committing' (committed_at_attempt preserved)
    - release_stale_locks: UPDATE status='confirmed', committed_at_attempt=NULL WHERE status='committing' AND committed_at_attempt < now() - interval; returns released_ids
    - all helpers never raise; write helpers return {ok, rowcount}
  </behavior>
  <action>
    Port commit_db.py as a never-throws psycopg3 DAO mirroring confirm_repo.py shape
    ($1->%s, AsyncConnectionPool). CRITICAL origin guard (D-01): find_confirmed_candidates
    SELECT adds `AND origin='python'`; acquire_commit_lock, mark_committed, and
    requeue_for_retry all SET `origin='python'` on their UPDATE so any Python-touched
    row stays Python-owned and the live Node watchdog (origin != 'python') ignores it.
    Use the interval-as-string pattern `(%s || ' minutes')::interval` with str(stale_min)
    per confirm_repo.py. Each write helper returns {"ok": True, "rowcount": n} or
    {"ok": False, "reason": str(e)}; find_* returns [] on error. Serialize
    farmos_response via json.dumps (None -> NULL). No CHECK constraint reliance —
    statuses validated in application code.
  </action>
  <verify>
    <automated>cd src/farm-agent && uv run pytest tests/test_farmos_commit_db.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "origin='python'" src/farm-agent/farm_agent/farmos/commit_db.py` returns 4 or more (find SELECT + acquire + mark_committed + requeue)
    - find_confirmed_candidates SQL contains both `status='confirmed'` and `origin='python'` (test asserts captured SQL)
    - acquire_commit_lock is a CAS on `status='confirmed'`: a second concurrent acquire returns rowcount=0 (test asserts via fake pool / ephemeral DB)
    - mark_committed/mark_failed are CAS on `status='committing'` (rowcount=0 when not committing)
    - all helpers never raise on a DB error (return ok=False / [])
  </acceptance_criteria>
  <done>commit_db.py is an origin-guarded never-throws commit-lifecycle DAO; commit-db parity tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Python commit_db → shared TimescaleDB signal_draft | the Python side of the origin-guard coexistence boundary; an unstamped row leaks to prod |
| router → farmOS via injected client | dispatch surface for all log types |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-62-27 | Elevation of Privilege | Python row drained by live Node watchdog | mitigate | every Python status write stamps origin='python'; find SELECT also filters origin='python' |
| T-62-28 | Tampering | double-commit on dup/restart race | mitigate | acquire_commit_lock CAS WHERE status='confirmed' (rowcount=0 on race) |
| T-62-29 | Denial of Service | stuck 'committing' locks blocking drain | mitigate | release_stale_locks reclaims locks older than the configured stale window |
| T-62-SC | Tampering | pip installs | accept | stdlib json/psycopg3 (already vendored), no new package |
</threat_model>

<verification>
- router dispatch + unsupported + envelope parity.
- commit_db find SELECT filters origin='python'; all write helpers stamp origin='python'; CAS races collapse to rowcount=0.
</verification>

<success_criteria>
FWR-01 (router) + FWR-04 (Python origin-guarded DAO): the dispatch + lifecycle DAO are
faithful and structurally Python-owned.
</success_criteria>

<output>
Create `.planning/phases/62-farmos-write-path/62-10-SUMMARY.md` when done.
</output>
