---
phase: 62-farmos-write-path
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/farm-agent/farm_agent/persistence/migrations.py
  - src/farm-agent/tests/test_persistence.py
  - src/agents/alerter/src/farmos/commit-db.js
  - src/agents/alerter/test/farmos/commit-db.test.js
autonomous: false
requirements: [FWR-04]
must_haves:
  truths:
    - "A Python process that flips a signal_draft to status='confirmed' with origin='python' is NEVER drained by the live Node commit-watchdog"
    - "Legacy and Node-written rows default to origin='node' and continue to be drained by the Node watchdog unchanged"
    - "The signal_draft table carries the Phase 40 commit-lifecycle columns the Python commit_db will write"
  artifacts:
    - path: "src/farm-agent/farm_agent/persistence/migrations.py"
      provides: "origin column + Phase 40 commit columns (additive, idempotent)"
      contains: "ADD COLUMN IF NOT EXISTS origin text NOT NULL DEFAULT 'node'"
    - path: "src/agents/alerter/src/farmos/commit-db.js"
      provides: "Node commit-watchdog SELECT origin-guarded"
      contains: "AND origin != 'python'"
  key_links:
    - from: "Python commit_db (Plan 10)"
      to: "signal_draft.origin"
      via: "every confirmed/committing write sets origin='python'"
      pattern: "origin='python'"
    - from: "Node findConfirmedCandidates"
      to: "signal_draft"
      via: "SELECT ... WHERE status='confirmed' AND origin != 'python'"
      pattern: "origin != 'python'"
---

<objective>
Commit the origin guard FIRST (D-02 hard sequencing, FWR-04, SC1). This is the
structural prevention of the shared-Timescale prod-leak: until the patched Node
commit-watchdog is deployed to prod, NO Python process may write status='confirmed'
to the shared signal_draft table. Every later Python commit plan depends on this plan.

Three ordered steps, in this exact order:
  1. Python migration adds `origin text NOT NULL DEFAULT 'node'` plus the Phase 40
     commit-lifecycle columns (additive, idempotent) to signal_draft.
  2. Node `commit-db.js` findConfirmedCandidates gains `AND origin != 'python'`.
  3. Operator REDEPLOYS the Node alerter to prod (checkpoint). Runner = mushy side
     edits + deploys the Node alerter ([[feedback_cross_repo_runner_must_be_named]]).

Purpose: structurally guarantee Python validation/commit drafts cannot leak to prod
farmOS via the live 30s Node drain watchdog ([[project_backfill_confirmed_drafts_leak_to_prod_via_live_watchdog]]).
Output: guarded schema column, guarded Node SELECT, redeployed Node alerter.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/62-farmos-write-path/62-CONTEXT.md
@.planning/phases/62-farmos-write-path/62-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add origin column + Phase 40 commit-lifecycle columns to signal_draft migration</name>
  <files>src/farm-agent/farm_agent/persistence/migrations.py, src/farm-agent/tests/test_persistence.py</files>
  <read_first>
    - src/farm-agent/farm_agent/persistence/migrations.py (lines 150-217: `_run_draft_migrations`, the ADD COLUMN IF NOT EXISTS block this extends)
    - src/agents/alerter/src/farmos/commit-db.js (lines 19-45: `initDb` — the Node Phase 40 columns being mirrored: farmos_response, committed_at, commit_failed_reason, commit_attempt_count, committed_at_attempt, outcome_ack_sent_at, plus the idx_signal_draft_status_confirmed index)
    - src/farm-agent/tests/test_persistence.py (existing migration assertion pattern)
  </read_first>
  <action>
    Extend `_run_draft_migrations(conn)` in migrations.py, AFTER the existing Phase 39
    column block (after line 216), with additive idempotent `ALTER TABLE signal_draft
    ADD COLUMN IF NOT EXISTS` statements mirroring Node commit-db.js initDb:
    `origin text NOT NULL DEFAULT 'node'` (Phase 62 D-01 — the guard), then
    `farmos_response jsonb`, `committed_at timestamptz`, `commit_failed_reason text`,
    `commit_attempt_count int NOT NULL DEFAULT 0`, `committed_at_attempt timestamptz`,
    `outcome_ack_sent_at timestamptz`. Add the partial index
    `CREATE INDEX IF NOT EXISTS idx_signal_draft_status_confirmed ON signal_draft (status, confirmed_at) WHERE status IN ('confirmed','committing')`.
    Update the allowed-status comment block to document the new application-validated
    status value `fidelity_cross_check_unverified` (Phase 62 D-06) alongside the
    existing pending|awaiting_farmer|confirmed|discarded|expired|needs_review|
    committing|committed|commit_failed values — NO pg CHECK constraint (mirrors Phase
    38/39 precedent, validated in application code only). Add a test asserting the
    `origin` column exists with default 'node' and that all six commit columns exist
    after run_migrations on an ephemeral DB.
  </action>
  <verify>
    <automated>cd src/farm-agent && uv run pytest tests/test_persistence.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "ADD COLUMN IF NOT EXISTS origin text NOT NULL DEFAULT 'node'" src/farm-agent/farm_agent/persistence/migrations.py` returns 1
    - All six Phase 40 columns present: `grep -v '^#' src/farm-agent/farm_agent/persistence/migrations.py | grep -c "farmos_response\|committed_at\b\|commit_failed_reason\|commit_attempt_count\|committed_at_attempt\|outcome_ack_sent_at"` returns 6 or more
    - Migration is idempotent: running run_migrations twice on the same DB raises no error (test asserts)
    - Test asserts a freshly-inserted signal_draft row has origin='node' when origin is not specified
    - `idx_signal_draft_status_confirmed` partial index created
  </acceptance_criteria>
  <done>signal_draft has origin (default 'node') + the six commit-lifecycle columns; migration idempotent; test_persistence passes.</done>
</task>

<task type="auto">
  <name>Task 2: Patch Node commit-watchdog SELECT with origin guard</name>
  <files>src/agents/alerter/src/farmos/commit-db.js, src/agents/alerter/test/farmos/commit-db.test.js</files>
  <read_first>
    - src/agents/alerter/src/farmos/commit-db.js (lines 47-60: `findConfirmedCandidates` — the exact function to patch)
    - src/agents/alerter/test/farmos/commit-db.test.js (existing findConfirmedCandidates test + fake-pool query-capture pattern)
    - src/agents/alerter/test/farmos/fake-pool.js (the fake pg pool used to assert SQL text)
  </read_first>
  <action>
    In `findConfirmedCandidates(pool, batchCap)` change the SQL WHERE clause from
    `WHERE status='confirmed'` to `WHERE status='confirmed' AND origin != 'python'`
    (D-01). Preserve `ORDER BY confirmed_at ASC NULLS LAST LIMIT $1` and the
    never-throws try/catch returning `r.rows || []`. Do NOT touch any other function.
    Add/extend a commit-db.test.js case asserting the captured SQL string contains
    `origin != 'python'` and that a fake row with origin='python' is excluded while
    origin='node' / NULL-defaulted rows are returned. Keep the existing tests green.
    No em-dashes in the file ([[feedback_no_em_dashes_in_artifacts]]).
  </action>
  <verify>
    <automated>cd src/agents/alerter && npx jest test/farmos/commit-db.test.js 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "AND origin != 'python'" src/agents/alerter/src/farmos/commit-db.js` returns 1
    - findConfirmedCandidates still ORDER BY confirmed_at ASC NULLS LAST and never-throws (try/catch returns [])
    - New test asserts an origin='python' row is NOT returned by findConfirmedCandidates
    - All pre-existing commit-db.test.js cases still pass
  </acceptance_criteria>
  <done>Node findConfirmedCandidates excludes origin='python' rows; commit-db.test.js green.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Redeploy the patched Node alerter to prod (origin-guard goes live)</name>
  <action>Operator action (no Claude automation): rebuild + redeploy the live Node alerter image carrying the Task-2 commit-db.js change to prod, then confirm the RUNNING container has the `origin != 'python'` clause. This is the structural gate; no Python commit-write plan may run until this is confirmed live.</action>
  <what-built>Tasks 1+2 added the origin column (Python migration) and the
  `AND origin != 'python'` clause to the live Node commit-watchdog SELECT. The
  structural prod-leak guarantee (SC1) only holds once the PATCHED Node alerter is
  running in prod. This is the cross-repo runner step: mushy side deploys the Node
  alerter.</what-built>
  <how-to-verify>
    1. Build/deploy the patched Node alerter to prod (the live alerter container that
       polls the shared Timescale :5432 and writes to prod farmOS :8082). Use the
       project deploy path for the alerter (rebuild image with the commit-db.js change,
       recreate the container).
    2. Confirm the RUNNING container has the patched code (not a stale branch —
       [[project_mc_deployed_off_stale_branch_drift]]): exec into the container and
       `grep "origin != 'python'" /app/src/farmos/commit-db.js` (or the in-image path)
       returns the clause.
    3. Confirm the Node alerter boots cleanly and its commit-watchdog logs its startup
       line after redeploy.
  </how-to-verify>
  <resume-signal>Type "deployed" once the patched Node alerter is confirmed running in prod with the origin-guard clause present in the RUNNING container. Until then, NO Python plan that writes status='confirmed'/'committing' to the shared signal_draft may execute.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Python commit path → shared TimescaleDB signal_draft | Python and the live Node watchdog share one prod table; an unguarded confirmed row is drained to prod farmOS within ~30s |
| Operator deploy → prod Node alerter | the redeploy is the moment the guarantee becomes live |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-62-01 | Tampering | shared signal_draft drain | mitigate | origin column + Node `AND origin != 'python'` clause; Python writes set origin='python' (Plan 10) |
| T-62-02 | Elevation of Privilege | Python drafts leaking to prod farmOS | mitigate | hard-sequenced redeploy checkpoint; no Python write-path plan runs before "deployed" |
| T-62-03 | Repudiation | stale-branch redeploy (drift) | mitigate | checkpoint requires grep of RUNNING container, not branch ([[project_mc_deployed_off_stale_branch_drift]]) |
| T-62-SC | Tampering | npm/pip installs | accept | this plan adds no new package installs (schema + SQL only) |
</threat_model>

<verification>
- Python migration idempotent; origin defaults 'node'; six commit columns present.
- Node SELECT excludes origin='python'; commit-db.test.js green.
- Patched Node alerter confirmed running in prod (operator checkpoint).
</verification>

<success_criteria>
SC1 satisfied: a Python process writing status='confirmed' with origin='python'
cannot be drained by the live Node commit-watchdog (structural, not a runbook step).
</success_criteria>

<output>
Create `.planning/phases/62-farmos-write-path/62-01-SUMMARY.md` when done.
</output>
