---
phase: 62-farmos-write-path
plan: 05
type: execute
wave: 3
depends_on: ["62-02"]
files_modified:
  - src/farm-agent/farm_agent/farmos/qr.py
  - src/farm-agent/farm_agent/farmos/fungi_type_cache.py
  - src/farm-agent/farm_agent/farmos/fungi_xing_cache.py
  - src/farm-agent/farm_agent/farmos/files.py
  - src/farm-agent/tests/test_farmos_qr.py
  - src/farm-agent/tests/test_farmos_caches.py
  - src/farm-agent/tests/test_farmos_files.py
autonomous: true
requirements: [FWR-01, FWR-02]
must_haves:
  truths:
    - "resolve_qr resolves a QR via filter[id_tag.id][value] then falls back to filter[name][value] on an empty id_tag hit; transport failures are not a miss"
    - "fungi_type / fungi_xing caches resolve a term name to its taxonomy UUID, LRU-capped, with ensure-create gated by an explicit create flag"
    - "upload_field_attachment POSTs octet-stream to {collection}/{uuid}/{field} (field='image'), creating+linking in one call, skip-on-missing"
  artifacts:
    - path: "src/farm-agent/farm_agent/farmos/files.py"
      provides: "upload_field_attachment(s) + _extract_file_id"
      contains: "def upload_field_attachment"
    - path: "src/farm-agent/farm_agent/farmos/qr.py"
      provides: "resolve_qr + bind_qr_on_create"
      contains: "id_tag"
  key_links:
    - from: "files.upload_field_attachment"
      to: "farmOS POST {collection}/{uuid}/image"
      via: "client post_binary octet-stream"
      pattern: "/image"
---

<objective>
Port the client-dependent lookup + IO primitives the commit handlers need:
`qr.py` (resolveQr id_tag-then-name fallback + bindQrOnCreate), `fungi_type_cache.py`
and `fungi_xing_cache.py` (taxonomy term name->UUID LRU caches, with ensure-create
gated), and `files.py` (the field-scoped octet-stream image upload — the ONLY route
that works on this farmOS: POST /api/asset/{type}/{uuid}/image, never /api/file/file
which 415s, never the `file` field which 422s on jpg
[[project_farmos_image_upload_needs_field_scoped_route]]).

Purpose: shared primitives for assets.py (Plan 07) and the commit handlers (Plans
08/09). The image upload satisfies SC3's route (FWR-01); the QR + caches feed upsert
(FWR-02). Output: four farmos modules + tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/62-farmos-write-path/62-CONTEXT.md
@.planning/phases/62-farmos-write-path/62-PATTERNS.md

<interfaces>
From src/farm-agent/farm_agent/farmos/client.py (Plan 02 — the injected client dict):
- client["get"](path) / client["post"](path, body) / client["post_binary"](path, bytes, filename=..., timeout_ms=...) -> {"ok", "status", "body", ...}
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Port qr.py + fungi_type_cache.py + fungi_xing_cache.py</name>
  <files>src/farm-agent/farm_agent/farmos/qr.py, src/farm-agent/farm_agent/farmos/fungi_type_cache.py, src/farm-agent/farm_agent/farmos/fungi_xing_cache.py, src/farm-agent/tests/test_farmos_qr.py, src/farm-agent/tests/test_farmos_caches.py</files>
  <read_first>
    - src/agents/alerter/src/farmos/qr.py (lines 35-64: resolveQr id_tag-then-name fallback + bindQrOnCreate; ID_TAG_TYPE='other')
    - src/agents/alerter/src/farmos/fungi-type-cache.js (lines 13-77: LRU CACHE_MAX=16, getFungiTypeUuid, ensureFungiTypeUuid create-gated)
    - src/agents/alerter/src/farmos/fungi-xing-cache.js (lines 12-52: LRU CACHE_MAX=4, getFungiXingUuid)
    - src/farm-agent/farm_agent/confirm/strain_ask_back.py (an existing pure-module style reference for Python idioms in this package)
  </read_first>
  <behavior>
    - resolve_qr id_tag hit: GET /api/asset/fungi?filter[id_tag.id][value]=<enc> with data[0] -> {"found": True, "asset_id": <id>, "path": "id_tag"}
    - resolve_qr name fallback: id_tag ok but empty -> GET filter[name][value]=<enc>; data[0] -> {"found": True, "asset_id": id, "path": "name"}
    - resolve_qr transport failure: id_tag call ok=False -> {"found": False, "error": "http_<status|network>", "path": "id_tag"} (NOT a miss; no name fallback)
    - get_fungi_type_uuid hit caches name->uuid; second call returns cached=True without a GET
    - ensure_fungi_type_uuid create=False on not_found -> passes through not_found; create=True on fungi_type_not_found -> POSTs taxonomy_term/fungi_type and returns created uuid
  </behavior>
  <action>
    Port qr.py with `ID_TAG_TYPE = "other"`, async `resolve_qr(client, qr_code)`
    (urllib.parse.quote encoding, id_tag-first then name-fallback, transport-failure
    is-not-a-miss, try/except returning {"found": False, "error": str(e)}), and
    `bind_qr_on_create(payload, qr_codes)` writing
    `payload["data"]["attributes"]["id_tag"] = [{"id": c, "type": ID_TAG_TYPE, "location": ""} ...]`.
    Port fungi_type_cache.py with a cap-16 OrderedDict LRU (_get/_set move-to-end +
    evict-oldest), `get_fungi_type_uuid(client, type_name)` hitting
    `/api/taxonomy_term/fungi_type?filter[name][value]=<enc>` (404 ->
    fungi_type_taxonomy_missing; empty data -> fungi_type_not_found), and
    `ensure_fungi_type_uuid(client, type_name, *, create=False)` that on
    fungi_type_not_found + create POSTs the term and caches it. Port fungi_xing_cache.py
    identically with cap-4 and `/api/taxonomy_term/fungi_xing`. All return
    {"ok": bool, "uuid": str, "reason": str} envelopes verbatim to the Node reasons.
  </action>
  <verify>
    <automated>cd src/farm-agent && uv run pytest tests/test_farmos_qr.py tests/test_farmos_caches.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "filter\[id_tag.id\]\[value\]" src/farm-agent/farm_agent/farmos/qr.py` returns 1
    - resolve_qr falls back to name only when id_tag returns ok+empty; a transport failure returns found=False with error and path='id_tag' (test asserts no name call made)
    - get_fungi_type_uuid second call for the same name does NO second GET (cached) — test asserts the fake client GET count is 1
    - ensure_fungi_type_uuid(create=True) POSTs the term on fungi_type_not_found; create=False passes the not_found through
    - Reason strings match Node verbatim: fungi_type_taxonomy_missing, fungi_type_not_found, fungi_xing_not_found
  </acceptance_criteria>
  <done>qr + both fungi caches ported with LRU + create-gating; tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Port files.py field-scoped image upload</name>
  <files>src/farm-agent/farm_agent/farmos/files.py, src/farm-agent/tests/test_farmos_files.py</files>
  <read_first>
    - src/agents/alerter/src/farmos/files.js (lines 56-115: _extractFileId 61-68, uploadFieldAttachment 70-94, uploadFieldAttachments 96-107 — the field-scoped route; IGNORE the legacy uploadAttachment /api/file/file path which 415s)
    - src/farm-agent/farm_agent/farmos/client.py (Plan 02 post_binary signature: post_binary(path, bytes, filename=, timeout_ms=))
    - 62-PATTERNS.md (files.py section: skip-on-missing, _extract_file_id single-vs-array)
  </read_first>
  <behavior>
    - happy path: upload_field_attachment(client, "/api/asset/fungi", uuid, "image", abs_path) reads bytes, POSTs octet-stream to /api/asset/fungi/<uuid>/image, returns {"ok": True, "file_id": <id>}
    - missing file: a non-existent abs_path returns {"ok": False, "reason": "attachment_missing", "skipped": True, "path": abs_path} and does NOT call the client
    - extract id (array body): body.data is a list -> file_id is data[-1].id; body.data is an object -> file_id is data.id
    - batch: upload_field_attachments returns {"file_ids": [...], "skipped": [...], "failed": [...]}
  </behavior>
  <action>
    Port files.py exposing async `upload_field_attachment(client, collection_path,
    uuid, field, abs_path, filename=None, opts=None)` -> builds url
    f"{collection_path}/{uuid}/{field}", reads bytes via Path(abs_path).read_bytes()
    inside try/except (missing -> attachment_missing skipped True; read error ->
    read_failed), POSTs via client["post_binary"](url, bytes, filename=fn,
    timeout_ms=30000), on ok returns {"ok": True, "file_id": _extract_file_id(body)}
    else {"ok": False, "reason": "http_<status|network>", "http_status": status}.
    `_extract_file_id(body)` handles list (last element id) vs object (id) vs None.
    `upload_field_attachments(...)` loops collecting file_ids/skipped/failed. The
    `field` argument is always "image" for fungi/group assets (the file field 422s).
    No re-encoding; bytes uploaded as-is.
  </action>
  <verify>
    <automated>cd src/farm-agent && uv run pytest tests/test_farmos_files.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "post_binary" src/farm-agent/farm_agent/farmos/files.py` returns 1 or more
    - upload url is f"{collection_path}/{uuid}/{field}" (test asserts the captured path ends with /<uuid>/image)
    - missing file returns attachment_missing + skipped True and makes NO client call (test asserts)
    - _extract_file_id returns data[-1].id for a list body and data.id for an object body (both asserted)
  </acceptance_criteria>
  <done>files.py uploads via the field-scoped octet-stream route, skip-on-missing, id-extract both shapes; tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem attachment bytes → farmOS octet-stream upload | local file bytes streamed to farmOS field route |
| QR/term lookups → farmOS query params | url-encoded farmer-supplied QR + strain strings in filter params |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-62-12 | Tampering | wrong upload route silently dropping photos | mitigate | field-scoped /image route only; reason=http_<status> surfaced on non-2xx (never silent) |
| T-62-13 | Denial of Service | huge/missing attachment hanging commit | mitigate | 30s binary timeout; skip-on-missing returns skipped, commit continues |
| T-62-14 | Injection | unencoded QR/term in filter query | mitigate | urllib.parse.quote on every filter value (ports encodeURIComponent) |
| T-62-SC | Tampering | pip installs | accept | stdlib pathlib/urllib only, no new package |
</threat_model>

<verification>
- resolve_qr id_tag-then-name fallback; transport failure not a miss.
- caches LRU + create-gating; reasons match Node.
- files upload via /image octet-stream, skip-on-missing, id-extract both shapes.
</verification>

<success_criteria>
The lookup + IO primitives (qr, fungi caches, field-scoped image upload) are ready for
the asset upsert layer and commit handlers (FWR-01 image route, FWR-02 lookups).
</success_criteria>

<output>
Create `.planning/phases/62-farmos-write-path/62-05-SUMMARY.md` when done.
</output>
