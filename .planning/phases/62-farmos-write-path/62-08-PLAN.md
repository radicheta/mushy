---
phase: 62-farmos-write-path
plan: 08
type: execute
wave: 5
depends_on: ["62-05", "62-06", "62-07"]
files_modified:
  - src/farm-agent/farm_agent/farmos/commits/__init__.py
  - src/farm-agent/farm_agent/farmos/commits/normalize.py
  - src/farm-agent/farm_agent/farmos/commits/commit_activity.py
  - src/farm-agent/farm_agent/farmos/commits/commit_input.py
  - src/farm-agent/farm_agent/farmos/commits/commit_observation.py
  - src/farm-agent/farm_agent/farmos/commits/commit_harvest.py
  - src/farm-agent/tests/test_farmos_normalize.py
  - src/farm-agent/tests/test_farmos_commit_simple.py
autonomous: true
requirements: [FWR-01]
must_haves:
  truths:
    - "normalize(draft) is a PURE function returning a NEW draft with draft_json reshaped to commit-shape per log_type; it does NOT mutate input and is idempotent"
    - "commit_activity / commit_input / commit_observation / commit_harvest each build their farmOS logs (and assets where applicable) and return the uniform {ok, asset_ids, log_ids, file_ids, reason} envelope"
  artifacts:
    - path: "src/farm-agent/farm_agent/farmos/commits/normalize.py"
      provides: "normalize(draft) extractor->commit shape transformer"
      contains: "def normalize"
  key_links:
    - from: "commit_* handlers"
      to: "logs.upsert_log / logs.create_log"
      via: "per-type log creation"
      pattern: "upsert_log\\|create_log"
---

<objective>
Port the router-side normalizer (`commits/normalize.js`) and the four simpler per-type
commit handlers (`commit-activity`, `commit-input`, `commit-observation`,
`commit-harvest`). normalize translates extractor-shape draft_json to commit-shape
(pure, idempotent, no mutation); the handlers create the farmOS logs/assets for their
log type. The seeding family is Plan 09; the router that dispatches across all six is
Plan 10.

Purpose: FWR-01 — reproduce all (non-seeding) log types and the normalize layer.
Output: commits/normalize.py + four handlers + tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/62-farmos-write-path/62-CONTEXT.md
@.planning/phases/62-farmos-write-path/62-PATTERNS.md

<interfaces>
From src/farm-agent/farm_agent/farmos/logs.py (Plan 06): create_log, upsert_log, LOG_TYPES, NATIVE_LOG_TYPES
From src/farm-agent/farm_agent/farmos/assets.py (Plan 07): upsert_fungi_asset, find_asset_by_name
From src/farm-agent/farm_agent/farmos/qr.py (Plan 05): resolve_qr
From src/farm-agent/farm_agent/farmos/files.py (Plan 05): upload_field_attachment(s)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Port normalize.py (pure extractor->commit shape transform)</name>
  <files>src/farm-agent/farm_agent/farmos/commits/__init__.py, src/farm-agent/farm_agent/farmos/commits/normalize.py, src/farm-agent/tests/test_farmos_normalize.py</files>
  <read_first>
    - src/agents/alerter/src/farmos/commits/normalize.js (lines 24-130: the full pure transform — common transforms 40-51, per-type switch 56-125)
    - src/agents/alerter/test/farmos/normalize.test.js (idempotency + per-type cases to mirror)
    - 62-PATTERNS.md (normalize.py section: event_timestamp->timestamp floor, asset_ref-><UNKNOWN> filter, per-type rules)
  </read_first>
  <behavior>
    - no mutation: normalize(draft) returns a new draft; draft["draft_json"] is not the same object and pushing to a returned array does not affect the input
    - idempotent: normalize(normalize(d)) == normalize(d)
    - common: event_timestamp ISO string -> timestamp unix-seconds floored (skip if timestamp already a number); asset_ref string -> qr_codes list with <UNKNOWN> -> []
    - activity: name -> activity_subtype (if absent)
    - harvest: source_block_refs -> source_qr_codes; harvest_batch_id -> harvest_batch_name; qty_g -> bags [{weight_grams}] when bags absent
    - input: recipe_lot PREPENDED to notes then deleted
    - observation: state appended to notes as "state: <v>" then deleted
  </behavior>
  <action>
    Port normalize(draft) -> dict per normalize.js. Shallow-copy draft_json, clone the
    five array fields (qr_codes, source_block_refs, source_qr_codes, bags,
    input_ingredients). Common transforms: if timestamp not int/float and
    event_timestamp is str -> floor(datetime.fromisoformat(...).timestamp()); if
    qr_codes not a list and asset_ref is str -> [] for "<UNKNOWN>" else [asset_ref].
    Per-log_type branch (activity/harvest/seeding/input/observation) verbatim to the
    JS guards (note: the seeding species->species_code transform stays here too even
    though the seeding handler is Plan 09). Return {**draft, "draft_json": out}. Pure,
    no I/O. Make commits/__init__.py a package marker.
  </action>
  <verify>
    <automated>cd src/farm-agent && uv run pytest tests/test_farmos_normalize.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "def normalize" src/farm-agent/farm_agent/farmos/commits/normalize.py` returns 1
    - normalize does not mutate input (test asserts input draft_json unchanged and returned object is distinct)
    - normalize(normalize(d)) deep-equals normalize(d) for an activity, harvest, input, observation, and seeding fixture (idempotency)
    - asset_ref "<UNKNOWN>" -> qr_codes [] (test asserts)
    - input recipe_lot is prepended to notes and removed from the field
  </acceptance_criteria>
  <done>normalize.py is a pure idempotent port; normalize.test.js parity green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Port commit_activity, commit_input, commit_observation, commit_harvest</name>
  <files>src/farm-agent/farm_agent/farmos/commits/commit_activity.py, src/farm-agent/farm_agent/farmos/commits/commit_input.py, src/farm-agent/farm_agent/farmos/commits/commit_observation.py, src/farm-agent/farm_agent/farmos/commits/commit_harvest.py, src/farm-agent/tests/test_farmos_commit_simple.py</files>
  <read_first>
    - src/agents/alerter/src/farmos/commits/commit-activity.js (full — 37 lines)
    - src/agents/alerter/src/farmos/commits/commit-input.js (full — 34 lines, ingredients-into-notes serializer)
    - src/agents/alerter/src/farmos/commits/commit-observation.js (full — 70 lines; image attach path — use the field-scoped files.upload_field_attachment, NOT the legacy /api/file/file path)
    - src/agents/alerter/src/farmos/commits/commit-harvest.js (full — 119 lines; source-block resolution + bags)
    - src/agents/alerter/test/farmos/commit-activity.test.js, commit-input.test.js, commit-observation.test.js, commit-harvest.test.js
  </read_first>
  <behavior>
    - each handler signature is async commit_<type>(client, draft, ctx=None) -> {ok, asset_ids, log_ids, file_ids, reason}
    - commit_activity: resolves qr_codes to asset ids, creates a log--activity (name from activity_subtype), returns log_ids
    - commit_input: serializes input_ingredients into notes, creates a log--input
    - commit_observation: resolves asset, creates a log--observation; if an attachment path is present, attaches via files.upload_field_attachment to the asset image field (best-effort; failure does not flip ok)
    - commit_harvest: resolves source blocks, creates harvest log + bags per the JS shape
    - unresolved required field returns {ok:False, reason:<specific>} (e.g. missing_asset / missing_block)
  </behavior>
  <action>
    Port each handler faithfully to its JS twin, signature async commit_<type>(client,
    draft, ctx=None). Use resolve_qr/find_asset_by_name for asset resolution,
    create_log/upsert_log for log creation, and files.upload_field_attachment for any
    image attach (observation) targeting the asset's image field via the field-scoped
    route (replace any legacy /api/file/file usage — that route 415s on this farmOS).
    Each returns the uniform envelope {ok, asset_ids, log_ids, file_ids,
    attachments_failed, http_status, reason}. Preserve the exact reason strings and
    field-resolution order from the JS. Never-throws at the handler level (commit_router
    in Plan 10 also wraps).
  </action>
  <verify>
    <automated>cd src/farm-agent && uv run pytest tests/test_farmos_commit_simple.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - All four handlers expose async commit_<type>(client, draft, ctx=None) returning the uniform envelope (test asserts keys asset_ids/log_ids/file_ids present)
    - commit_observation attaches images via files.upload_field_attachment to the `image` field; an attachment failure does NOT flip ok to False (test asserts ok stays True with attachments_failed populated)
    - `grep -rc "/api/file/file" src/farm-agent/farm_agent/farmos/commits/` returns 0 (legacy 415 route never used)
    - commit_harvest produces a harvest log + bags matching the JS fixture shape
    - missing required field returns the JS-verbatim reason string (per-type test asserts)
  </acceptance_criteria>
  <done>Four simpler handlers ported with field-scoped image attach; commit-*.test.js parity green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| extractor draft_json → commit-shape transform | normalize reshapes farmer-derived data before it hits farmOS |
| handler → farmOS log/asset writes | a malformed log corrupts the farm record |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-62-21 | Tampering | normalize mutating the persisted draft_json | mitigate | pure function, deep-copied arrays, idempotency-guarded (no mutation) |
| T-62-22 | Tampering | observation photo silently dropped via wrong route | mitigate | field-scoped /image upload only; attachments_failed surfaced, never silent |
| T-62-23 | Denial of Service | one handler exception killing a commit batch | mitigate | handler + router (Plan 10) never-throws; reason returned |
| T-62-SC | Tampering | pip installs | accept | stdlib only, no new package |
</threat_model>

<verification>
- normalize pure + idempotent; four handlers parity with their commit-*.test.js.
- No legacy /api/file/file usage; image attach via field-scoped route.
</verification>

<success_criteria>
FWR-01 (non-seeding handlers + normalize): activity/input/observation/harvest commit
their log types faithfully; normalize is a pure idempotent shape transform.
</success_criteria>

<output>
Create `.planning/phases/62-farmos-write-path/62-08-SUMMARY.md` when done.
</output>
