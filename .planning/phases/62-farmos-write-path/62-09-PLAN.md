---
phase: 62-farmos-write-path
plan: 09
type: execute
wave: 5
depends_on: ["62-05", "62-06", "62-07"]
files_modified:
  - src/farm-agent/farm_agent/farmos/group_assets.py
  - src/farm-agent/farm_agent/farmos/activity_logs.py
  - src/farm-agent/farm_agent/farmos/commits/commit_seeding.py
  - src/farm-agent/farm_agent/farmos/commits/commit_seeding_session.py
  - src/farm-agent/tests/test_farmos_group_assets.py
  - src/farm-agent/tests/test_farmos_commit_seeding.py
  - src/farm-agent/tests/test_farmos_commit_seeding_session.py
autonomous: true
requirements: [FWR-01]
must_haves:
  truths:
    - "commit_seeding resolves QR (path B reuse vs path A create), upserts the block fungi asset, and upserts exactly one seeding log per block"
    - "commit_seeding_session preflights the session group asset, creates N children with parent=[sourceBlock] only, attaches page images to the group image field, writes the membership activity log, and rolls back all-or-nothing on partial failure"
    - "a confirmed seeding session with an attached image uploads via the field-scoped /api/asset/group/{uuid}/image route and the image lands on the group image field"
  artifacts:
    - path: "src/farm-agent/farm_agent/farmos/commits/commit_seeding_session.py"
      provides: "seeding_session handler with image attach + all-or-nothing rollback"
      contains: "def commit_seeding_session"
    - path: "src/farm-agent/farm_agent/farmos/group_assets.py"
      provides: "find_group_asset_by_name, upsert_group_asset, delete_group_asset"
  key_links:
    - from: "commit_seeding_session"
      to: "files.upload_field_attachments(/api/asset/group, uuid, image)"
      via: "best-effort page-image attach"
      pattern: "/api/asset/group"
---

<objective>
Port the seeding commit family: `group_assets.py` + `activity_logs.py` (the session
group asset + membership/group-assignment log helpers), `commit_seeding.py` (single
inoc), and `commit_seeding_session.py` (multi-parent batch session with page-image
attach + all-or-nothing rollback). This is the path that exercises SC3's field-scoped
image upload onto a group asset and SC2's upsert idempotency on real seeding data.

Purpose: FWR-01 — the seeding/seeding_session log types + image upload. Output: two
farmos helper modules + two commit handlers + tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/62-farmos-write-path/62-CONTEXT.md
@.planning/phases/62-farmos-write-path/62-PATTERNS.md

<interfaces>
From src/farm-agent/farm_agent/farmos/assets.py (Plan 07): upsert_fungi_asset, delete_fungi_asset
From src/farm-agent/farm_agent/farmos/logs.py (Plan 06): upsert_log
From src/farm-agent/farm_agent/farmos/qr.py (Plan 05): resolve_qr
From src/farm-agent/farm_agent/farmos/files.py (Plan 05): upload_field_attachments
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Port group_assets.py + activity_logs.py + commit_seeding.py</name>
  <files>src/farm-agent/farm_agent/farmos/group_assets.py, src/farm-agent/farm_agent/farmos/activity_logs.py, src/farm-agent/farm_agent/farmos/commits/commit_seeding.py, src/farm-agent/tests/test_farmos_group_assets.py, src/farm-agent/tests/test_farmos_commit_seeding.py</files>
  <read_first>
    - src/agents/alerter/src/farmos/groupAssets.js (full — 107 lines: findGroupAssetByName, upsertGroupAsset, deleteGroupAsset; mirrors assets.js shape on asset--group)
    - src/agents/alerter/src/farmos/activityLogs.js (full — 75 lines: createGroupAssignmentLog, deleteActivityLog)
    - src/agents/alerter/src/farmos/commits/commit-seeding.js (full — 93 lines: path A/B QR resolution, block upsert, single seeding log upsert)
    - src/agents/alerter/test/farmos/groupAssets.test.js, commit-seeding.test.js
  </read_first>
  <behavior>
    - commit_seeding path B (QR resolves): reuses existing block id, creates 0 assets, upserts one seeding log
    - commit_seeding path A (QR new): upsert_fungi_asset for the block (fungi_xing="block", fungi_type=strain), tracks created only when outcome="created", then upsert seeding log
    - ambiguous: >1 resolved QR -> {ok:False, reason:"ambiguous_qr_seeding"}
    - missing strain / block_name -> {ok:False, reason:"missing_strain"|"missing_block_name"}
    - group_assets.upsert_group_asset: name-based upsert on asset--group mirroring fungi upsert (mushy:draft trailer in notes)
  </behavior>
  <action>
    Port group_assets.py (find_group_asset_by_name via filter[name][value] on
    /api/asset/group; upsert_group_asset name-based create/patch with mushy:draft
    notes trailer; delete_group_asset). Port activity_logs.py (create_group_assignment_log
    binding child ids to the session group; delete_activity_log). Port commit_seeding.py
    verbatim to commit-seeding.js: QR loop splitting path_b_ids vs path_a_qrs,
    ambiguous_qr_seeding guard, strain/block_name guards, upsert_fungi_asset(block) with
    created-only tracking, batch_name preserved in notes ("sterilization_batch: ..."),
    upsert_log("seeding", name="Inoc <block_name|block_id>", asset_ids=[block_id]).
    Return {ok, asset_ids (created only), log_ids, file_ids:[], http_status}.
  </action>
  <verify>
    <automated>cd src/farm-agent && uv run pytest tests/test_farmos_group_assets.py tests/test_farmos_commit_seeding.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - commit_seeding path B reuses the resolved block (asset_ids empty/created-only) and creates 0 new assets (test asserts)
    - ambiguous (>1 resolved QR) returns reason "ambiguous_qr_seeding"
    - missing strain returns "missing_strain"; missing block_name returns "missing_block_name"
    - upsert_group_asset on a re-run with the same name creates 0 new group assets (name-based, test asserts)
    - seeding log name is "Inoc <block_name or block_id>" and asset_ids carries only created blocks
  </acceptance_criteria>
  <done>group_assets + activity_logs + commit_seeding ported; commit-seeding.test.js parity green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Port commit_seeding_session.py (image attach + all-or-nothing rollback)</name>
  <files>src/farm-agent/farm_agent/farmos/commits/commit_seeding_session.py, src/farm-agent/tests/test_farmos_commit_seeding_session.py</files>
  <read_first>
    - src/agents/alerter/src/farmos/commits/commit-seeding-session.js (full — 315 lines: epochSecondsForDate, _resolveSessionName collision #1..#9, _cleanup reverse-order rollback, commitSeedingSession preflight->image attach->children loop->membership log->success shape)
    - src/agents/alerter/test/farmos/commit-seeding-session.test.js (preflight/children/rollback/image-attach cases)
    - src/farm-agent/farm_agent/farmos/files.py (upload_field_attachments signature)
  </read_first>
  <behavior>
    - preflight: resolve session name "inoc <date>" with #2..#9 collision suffix (idempotent re-commit matches mushy:draft trailer); upsert the asset--group session entity
    - image attach: when ctx.session_page_paths non-empty, upload_field_attachments("/api/asset/group", group_id, "image", paths); a failure populates attachments_failed but does NOT change ok
    - children: per group, upsert source block (unless NO_PARENT), then qty child blocks each parent=[sourceBlock] ONLY, then one seeding log per child
    - rollback: any child/log/membership failure triggers _cleanup reverse-order (membership log -> created assets reverse -> just-created session group); returns ok:False reason:"partial_commit_failed"
    - success: returns asset_ids (session group if just-created + created assets), log_ids (membership + child logs), file_ids (uploaded), attachments_failed
  </behavior>
  <action>
    Port commit_seeding_session.py faithfully. epoch_seconds_for_date(date_str) parses
    "<date>T00:00:00Z". _resolve_session_name probes baseName then "#2".."#9", matching
    an existing group whose notes contain "mushy:draft:<draftId>" (idempotent re-commit)
    or the first miss; exhaustion -> session_name_collision_exhausted. Preflight
    upsert_group_asset. OPTIONAL image attach via files.upload_field_attachments to the
    group `image` field, best-effort (failure never flips ok; log a WARNING with the
    failed count). Children loop: validate group shape; upsert source block when parent
    != "NO_PARENT" (track created); per qty upsert child block parent=[source] then
    upsert_log("seeding", name="Inoc <childName>", asset_ids=[childId]). Post-loop
    create_group_assignment_log (membership). _cleanup(reverse order) on any failure ->
    partial_commit_failed with orphan-cleanup accounting in farmos_response. Wrap the
    whole body in try/except returning commit_seeding_session_error. Return the success
    envelope shape verbatim.
  </action>
  <verify>
    <automated>cd src/farm-agent && uv run pytest tests/test_farmos_commit_seeding_session.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "/api/asset/group" src/farm-agent/farm_agent/farmos/commits/commit_seeding_session.py` returns 1 or more
    - image attach targets the `image` field via upload_field_attachments; an attach failure leaves ok=True with attachments_failed populated (test asserts)
    - children carry parent=[sourceBlock] ONLY (no session-group edge on children) — test asserts the child upsert parent_ids
    - a child-block failure triggers reverse-order cleanup and returns reason "partial_commit_failed" (test asserts deletes called in reverse + just-created group deleted)
    - idempotent re-commit: re-running against a populated farmOS reuses the session group (matched by mushy:draft trailer) and creates 0 duplicate group assets
  </acceptance_criteria>
  <done>commit_seeding_session ported with field-scoped group image attach + all-or-nothing rollback; commit-seeding-session.test.js parity green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| session draft + page images → farmOS group/asset/log + octet-stream upload | a partial failure can orphan assets; a wrong route drops page photos |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-62-24 | Tampering | orphaned assets after partial session commit | mitigate | all-or-nothing reverse-order _cleanup; orphan-cleanup accounting in farmos_response |
| T-62-25 | Tampering | duplicate session group / children on re-run | mitigate | name + mushy:draft trailer idempotent reuse; created-only tracking |
| T-62-26 | Tampering | page photo silently dropped | mitigate | field-scoped /api/asset/group/{uuid}/image route; attachments_failed surfaced, attach failure non-fatal by design |
| T-62-SC | Tampering | pip installs | accept | stdlib only, no new package |
</threat_model>

<verification>
- commit_seeding path A/B + guards; commit_seeding_session preflight/children/rollback/image-attach parity.
- Re-run creates 0 duplicate group/children; image lands on group image field.
</verification>

<success_criteria>
FWR-01 (seeding family): seeding + seeding_session commit faithfully, with the
field-scoped image upload and all-or-nothing rollback preserved.
</success_criteria>

<output>
Create `.planning/phases/62-farmos-write-path/62-09-SUMMARY.md` when done.
</output>
