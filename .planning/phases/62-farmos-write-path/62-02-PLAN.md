---
phase: 62-farmos-write-path
plan: 02
type: execute
wave: 2
depends_on: ["62-01"]
files_modified:
  - src/farm-agent/farm_agent/farmos/__init__.py
  - src/farm-agent/farm_agent/farmos/client.py
  - src/farm-agent/tests/test_farmos_client.py
autonomous: true
requirements: [FWR-01]
must_haves:
  truths:
    - "The client authenticates against farmOS via POST /user/login?_format=json and stores the session Cookie + csrf_token"
    - "Every request returns a never-throws envelope {ok, status, body, latency_ms} (network failure never raises)"
    - "A 401/403 triggers exactly one re-auth retry; 5xx and network errors retry with backoff [1s,4s,16s]"
    - "post_binary sends application/octet-stream with Content-Disposition: file; filename=..."
  artifacts:
    - path: "src/farm-agent/farm_agent/farmos/client.py"
      provides: "httpx async farmOS client factory (get/post/patch/post_binary/head/delete)"
      min_lines: 120
    - path: "src/farm-agent/tests/test_farmos_client.py"
      provides: "auth, retry, reauth, never-throws, octet-stream header tests"
  key_links:
    - from: "farmos/client.py"
      to: "farmOS /user/login?_format=json"
      via: "httpx.AsyncClient POST, extract Set-Cookie + csrf_token"
      pattern: "user/login"
---

<objective>
Faithful Python port of the Node farmOS HTTP client (`farmos/client.js`) using
httpx.AsyncClient. Session-cookie + X-CSRF-Token auth, 10s per-call timeout, 3x
exponential backoff on transient errors, single 401/403 re-auth retry, JSON:API
content type, octet-stream binary upload. Never-throws envelope. Every later farmOS
module (assets, logs, files, commits, watchdog) consumes this via dependency injection.

Purpose: the single auth + transport seam for the whole write path (FWR-01).
Output: farm_agent/farmos/client.py + tests; the farmos package `__init__.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/62-farmos-write-path/62-CONTEXT.md
@.planning/phases/62-farmos-write-path/62-PATTERNS.md

<interfaces>
From src/farm-agent/farm_agent/capture/transcribe_client.py (the never-throws httpx factory analog):
- create_transcribe_client(api_url, http: httpx.AsyncClient, timeout_ms, log) -> dict
- inner async fn returns {"ok": bool, "reason": str} envelopes; catches httpx.TimeoutException + Exception
From src/farm-agent/farm_agent/tenancy/tenant.py (TenantConfig, frozen dataclass):
- config.farmos_url: str   (dev ":18080" / prod ":8082" selected by env FARMOS_URL)
- config.farmos_username: str
- config.farmos_password: str   (secret, env-only)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Port the farmOS client factory (auth + request envelope)</name>
  <files>src/farm-agent/farm_agent/farmos/__init__.py, src/farm-agent/farm_agent/farmos/client.py</files>
  <read_first>
    - src/agents/alerter/src/farmos/client.js (lines 13-196: the full source-of-truth — _authenticate 36-64, _isTransientError 66-72, _doFetch 74-108, _request 110-170, the get/post/patch/postBinary/head/delete wrappers 172-194)
    - src/farm-agent/farm_agent/capture/transcribe_client.py (lines 28-92: never-throws httpx factory + TimeoutException handling to mirror)
    - 62-PATTERNS.md (the client.py section: return envelope shape, octet-stream mapping, JSON:API headers)
  </read_first>
  <behavior>
    - login: first non-skip-auth request POSTs /user/login?_format=json with {name, pass}; on 200 stores cookie = first Set-Cookie segment before ';' and csrf = body.csrf_token in a closure dict _session
    - GET success: returns {"ok": True, "status": 200, "body": <parsed dict>, "latency_ms": int}
    - 401 then success: a 401 response triggers exactly one re-auth then retries the original request; second 401 returns ok=False without infinite loop
    - 5xx retry: a 500 then 200 retries once with backoff and returns ok=True
    - network error never raises: httpx.TimeoutException / ConnectError returns {"ok": False, "status": None, "body": None, "error": <str>} and never propagates
    - octet-stream: post_binary sets Content-Type application/octet-stream + Content-Disposition file; filename="X" and 30s timeout
  </behavior>
  <action>
    Create farm_agent/farmos/client.py exposing `create_farmos_client(farmos_url,
    username, password, http: httpx.AsyncClient, *, backoff_ms=(1000,4000,16000),
    timeout_ms=10000, retry_max=3, log=None) -> dict` returning a dict of async
    callables `{"get","post","patch","post_binary","head","delete","_session"}`.
    Mirror client.js exactly: `_authenticate()` POSTs `/user/login?_format=json` with
    JSON body `{"name": username, "pass": password}`, Accept application/json, reads
    `Set-Cookie` (take segment before ';') and body `csrf_token`, stores both in a
    closure `_session = {"cookie": None, "csrf": None, "authed_at": None}`; raise on
    missing cookie/csrf (auth_response_malformed). `_do_fetch` builds headers Accept
    application/vnd.api+json, Cookie, X-CSRF-Token; for non-GET JSON body sets
    Content-Type application/vnd.api+json and serializes; for binary sets octet-stream
    + Content-Disposition file; filename. `_request` lazily authenticates when
    _session["cookie"] is None (unless skip_auth), retries transient (httpx
    TimeoutException, ConnectError, network) with backoff_ms[min(attempt,2)] up to
    retry_max-1, one-shot reauth on 401/403 (did_reauth flag), transient 5xx retry,
    then parses by content-type (vnd.api+json/json -> .json(), else .text). Return
    envelope `{"ok": 200<=status<300, "status": int|None, "body": dict|str|None,
    "latency_ms": int}` and `"error"` on network failure. post_binary wraps _request
    POST with binary=True + timeout_ms=30000. Use time.monotonic for latency. Make
    farm_agent/farmos/__init__.py an empty package marker (or re-export
    create_farmos_client). No fenced code in actions; follow client.js identifiers
    verbatim.
  </action>
  <verify>
    <automated>cd src/farm-agent && uv run pytest tests/test_farmos_client.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "user/login?_format=json" src/farm-agent/farm_agent/farmos/client.py` returns 1
    - `grep -c "application/octet-stream" src/farm-agent/farm_agent/farmos/client.py` returns 1
    - `grep -c "X-CSRF-Token" src/farm-agent/farm_agent/farmos/client.py` returns 1
    - Test: a mocked 401-then-200 sequence reauths exactly once (assert login called twice total)
    - Test: httpx.TimeoutException returns ok=False with error key, no exception escapes
    - Test: backoff order is (1000,4000,16000) ms (assert via injected sleep spy / no real sleeps)
  </acceptance_criteria>
  <done>create_farmos_client authenticates, retries, reauths, never-throws, sends octet-stream; tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Python farmOS client → farmOS (dev/prod) | session-cookie + CSRF auth over HTTP; credentials cross here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-62-04 | Information Disclosure | password/cookie in logs | mitigate | client NEVER logs password, cookie, or csrf; only status + latency (mirror Node + boot.py T-56-06-01) |
| T-62-05 | Spoofing | stale/expired session reuse | mitigate | one-shot reauth on 401/403; cookie+csrf re-fetched on auth |
| T-62-06 | Denial of Service | hung farmOS request | mitigate | 10s per-call timeout (30s for binary); bounded retry_max=3 |
| T-62-SC | Tampering | pip installs | accept | uses already-vendored httpx (no new package; see milestone STACK lock) |
</threat_model>

<verification>
- Auth POSTs /user/login?_format=json; cookie + csrf stored.
- 401 reauths once; 5xx + network retries with [1s,4s,16s] backoff; never raises.
- Binary path sends octet-stream + Content-Disposition.
</verification>

<success_criteria>
A reusable never-throws httpx farmOS client exists for every Phase 62 farmOS module
(FWR-01 transport seam).
</success_criteria>

<output>
Create `.planning/phases/62-farmos-write-path/62-02-SUMMARY.md` when done.
</output>
