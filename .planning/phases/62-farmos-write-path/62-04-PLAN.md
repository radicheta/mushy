---
phase: 62-farmos-write-path
plan: 04
type: tdd
wave: 2
depends_on: ["62-01"]
files_modified:
  - src/farm-agent/farm_agent/farmos/fidelity_gate.py
  - src/farm-agent/farm_agent/tenancy/tenant.py
  - src/farm-agent/tests/test_farmos_fidelity_gate.py
  - src/farm-agent/tests/test_strain_poy_koy_regression.py
  - src/farm-agent/tests/fixtures/farmos/fidelity_csv_sample.csv
autonomous: true
requirements: [FWR-03]
must_haves:
  truths:
    - "A draft whose block-name strain disagrees with the CSV cross-check is HELD as fidelity_cross_check_unverified and NOT committed"
    - "On disagreement the gate emits a farmer ask-back message (no silent hold) and surfaces the hold status"
    - "A block absent from the CSV is a no-op pass (CSV is non-authoritative, never hard-rejects)"
    - "POY is never silently resolved to KOY (named regression fixture against the curated-14 resolver)"
  artifacts:
    - path: "src/farm-agent/farm_agent/farmos/fidelity_gate.py"
      provides: "check_fidelity + render_fidelity_ask_back + load_fidelity_csv"
      contains: "fidelity_cross_check_unverified"
    - path: "src/farm-agent/tests/test_strain_poy_koy_regression.py"
      provides: "named POY->KOY silent-misattribution regression guard"
  key_links:
    - from: "fidelity_gate.check_fidelity"
      to: "draft hold status"
      via: "returns hold_status='fidelity_cross_check_unverified' + ask_back_msg on strain_mismatch"
      pattern: "fidelity_cross_check_unverified"
---

<objective>
Port the v1.11 commit-time CSV fidelity gate to Python (D-06/D-07) and add the named
POY->KOY regression fixture (D-08, FWR-03). On a block-name vs CSV strain disagreement
the gate HOLDS the draft as `fidelity_cross_check_unverified` AND sends a farmer
ask-back (not a silent hold — [[feedback_no_silent_failure_after_farmer_confirm]]),
preventing POY-committed-as-KOY ([[project_backfill_extraction_fidelity_38pct_silent_misattribution]]).
CSV is NON-authoritative (a second interpretation); on disagreement it FLAGS/holds for
human review, never silent hard-reject ([[project_backfill_csv_is_not_ground_truth]]).

The curated-14 resolver already exists (confirm/strain_ask_back.py, exact-match,
rejects unknown); this plan adds a named POY->KOY regression test asserting POY is never
silently remapped to KOY.

Purpose: FWR-03 — preserve the v1.11 fidelity hard gate + regression-guard the strain
misattribution class. Output: fidelity_gate.py, a tenant config CSV-path field, fixture
CSV, and two named tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/62-farmos-write-path/62-CONTEXT.md
@.planning/phases/62-farmos-write-path/62-PATTERNS.md

<interfaces>
From src/farm-agent/farm_agent/confirm/strain_ask_back.py (existing curated-14 resolver):
- CURATED_14: list[str]  (SHI SH2 KOY MAI MALI KOS DT CAS CAZ WIN ALM MOR BP LIMA)
- resolve_strain(code, curated_set) -> {known, code, nearest?}  (EXACT-match only; nearest is DISPLAY-only, never auto-remap — T-61-09)
- render_strain_ask_back(seen_code, nearest) -> str
From src/farm-agent/farm_agent/tenancy/tenant.py (frozen dataclass TenantConfig + _pick layered loader):
- fields added via the dataclass + the load() body using _pick(tenant_cfg, env, KEY, default)
</interfaces>
</context>

<feature>
  <name>CSV fidelity gate + POY->KOY regression guard</name>
  <files>src/farm-agent/farm_agent/farmos/fidelity_gate.py, src/farm-agent/farm_agent/tenancy/tenant.py, src/farm-agent/tests/test_farmos_fidelity_gate.py, src/farm-agent/tests/test_strain_poy_koy_regression.py, src/farm-agent/tests/fixtures/farmos/fidelity_csv_sample.csv</files>
  <read_first>
    - src/farm-agent/farm_agent/confirm/strain_ask_back.py (lines 88-145: resolve_strain exact-match + render_strain_ask_back — the existing curated-14 resolver to regression-guard)
    - src/farm-agent/farm_agent/tenancy/tenant.py (the TenantConfig dataclass field block + the load() body _pick pattern for adding fidelity_csv_path)
    - 62-PATTERNS.md (fidelity_gate.py section: check_fidelity return shape, new status string, ask-back template, CSV load)
    - .planning/STATE.md (Phase 55B section: the buildCsvBudget / fidelity_cross_check_unverified Node behavior being preserved)
  </read_first>
  <behavior>
    - pass (no CSV row for block): check_fidelity(draft, csv_rows) for a block not in CSV -> {"pass": False, "reason": "block_not_in_csv"} treated as no-op (caller commits)
    - pass (agreement): draft block strain == CSV strain -> {"pass": True}
    - hold (disagreement): draft strain POY vs CSV strain KOY -> {"pass": False, "reason": "strain_mismatch", "draft_strain": "POY", "csv_strain": "KOY", "hold_status": "fidelity_cross_check_unverified", "ask_back_msg": <non-empty str>}
    - ask-back text: render_fidelity_ask_back("260101_POY_1", "POY", "KOY") names the block, both strains, and asks which is correct; ASCII-only, no em-dash
    - POY->KOY regression: resolve_strain("POY", CURATED_14)["known"] is False AND ["code"] == "POY" (never silently returns KOY); nearest may be KOY but is display-only
  </behavior>
  <implementation>
    RED: write test_farmos_fidelity_gate.py + test_strain_poy_koy_regression.py first.
    The fidelity test loads tests/fixtures/farmos/fidelity_csv_sample.csv (columns:
    block_name, strain_code — include at least one POY-row whose block name a draft
    will mislabel as KOY, and one agreeing row). The POY regression test asserts
    resolve_strain("POY", CURATED_14) is not known and code stays "POY".

    GREEN: create farm_agent/farmos/fidelity_gate.py:
      - load_fidelity_csv(path) -> list[dict] (csv.DictReader; returns [] on missing
        file — non-fatal, CSV is non-authoritative D-07)
      - check_fidelity(draft, csv_rows) -> dict per the behavior block. Resolve the
        draft's block_name -> expected strain by parsing the B5 name ({YYMMDD}_{SPECIES3}_{SEQ})
        species token AND/OR draft_json species_code; match the CSV row by block_name.
        Absent -> block_not_in_csv (pass-through). Present + equal -> pass True.
        Present + differ -> hold with hold_status="fidelity_cross_check_unverified"
        and ask_back_msg from render_fidelity_ask_back.
      - render_fidelity_ask_back(block_name, draft_strain, csv_strain) -> str: two ASCII
        lines naming the block + both strains + asking which is correct or reply YES to
        keep draft value (mirror strain_ask_back tone, no em-dash).
    Add `fidelity_csv_path: str` to the TenantConfig dataclass and load it in load()
    via `_pick(tenant_cfg, env, "FIDELITY_CSV_PATH", "")` (empty default; prod sets the
    real path per D-07). Wire nothing into the commit path here (Plan 11 wires the gate
    into the watchdog); this plan delivers the pure gate + config + fixtures + tests.

    REFACTOR: ensure check_fidelity is pure (no I/O); csv load is separate.
  </implementation>
  <verify>
    <automated>cd src/farm-agent && uv run pytest tests/test_farmos_fidelity_gate.py tests/test_strain_poy_koy_regression.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "fidelity_cross_check_unverified" src/farm-agent/farm_agent/farmos/fidelity_gate.py` returns 1 or more
    - check_fidelity returns hold_status + ask_back_msg on strain_mismatch (test asserts both keys present and ask_back_msg non-empty)
    - check_fidelity returns block_not_in_csv (pass-through) when the block is absent from the CSV (never hard-reject)
    - `grep -c "fidelity_csv_path" src/farm-agent/farm_agent/tenancy/tenant.py` returns 2 or more (dataclass field + load assignment)
    - test_strain_poy_koy_regression asserts resolve_strain("POY", CURATED_14) known is False and code is "POY" (never KOY)
    - render_fidelity_ask_back output contains no em-dash character
  </acceptance_criteria>
  <done>Pure CSV fidelity gate holds + asks-back on disagreement; config field added; POY->KOY regression guarded; tests green.</done>
</feature>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CSV file → fidelity gate | a second, non-authoritative interpretation of the notebooks; ~38% historical disagreement |
| gate decision → farmOS commit | a wrong pass commits POY as KOY to the farm record of truth (integrity) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-62-09 | Tampering | silent strain misattribution (POY->KOY) | mitigate | hold as fidelity_cross_check_unverified + ask-back on disagreement; resolver exact-match, nearest display-only |
| T-62-10 | Spoofing | over-trusting a non-authoritative CSV (hard-reject true data) | mitigate | block_not_in_csv is a pass-through; disagreement FLAGS/holds, never silent hard-reject (D-07) |
| T-62-11 | Denial of Service | missing/malformed CSV blocking all commits | mitigate | load_fidelity_csv returns [] on missing file; absent rows pass through |
| T-62-SC | Tampering | pip installs | accept | stdlib csv only, no new package |
</threat_model>

<verification>
- Disagreement holds as fidelity_cross_check_unverified + ask-back; agreement passes; absent-block passes.
- POY never silently resolves to KOY (named fixture).
</verification>

<success_criteria>
SC4 + SC5 (gate logic): the v1.11 CSV fidelity gate holds disagreements with a farmer
ask-back; the POY->KOY silent-misattribution class is regression-guarded.
</success_criteria>

<output>
Create `.planning/phases/62-farmos-write-path/62-04-SUMMARY.md` when done.
</output>
