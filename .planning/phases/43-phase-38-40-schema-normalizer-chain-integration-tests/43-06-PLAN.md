---
phase: 43
plan: 06
type: execute
wave: 4
depends_on:
  - 43-01
  - 43-05
files_modified:
  - .planning/phases/43-phase-38-40-schema-normalizer-chain-integration-tests/43-SCHEMA-04-ATTESTATION.md
autonomous: true
requirements:
  - SCHEMA-04

must_haves:
  truths:
    - "`npm test` (no env vars, no flags) runs both test/farmos/normalize.test.js and test/farmos/integration/extractor-to-commit.test.js"
    - "The integration suite is not behind any FARMOS_INTEGRATION gate or skip flag"
    - "Attestation note records the runtime output proving default-run"
  artifacts:
    - path: ".planning/phases/43-phase-38-40-schema-normalizer-chain-integration-tests/43-SCHEMA-04-ATTESTATION.md"
      provides: "literal `npm test` output demonstrating both new files run by default"
      contains: "extractor-to-commit"
  key_links:
    - from: "alerter/jest.config.js testMatch '**/test/**/*.test.js'"
      to: "test/farmos/integration/extractor-to-commit.test.js + test/farmos/normalize.test.js"
      via: "default Jest test discovery"
      pattern: "extractor-to-commit"
---

<objective>
Cap SCHEMA-04 by attesting that the new test files (`test/farmos/normalize.test.js` from Plan 43-01 and `test/farmos/integration/extractor-to-commit.test.js` from Plan 43-05) run under the default `npm test` invocation — no `FARMOS_INTEGRATION=1` gate, no flags, no skip pragmas.

Purpose: The pre-existing `test/farmos/integration.test.js` is gated by `FARMOS_INTEGRATION=1` (verified during planning — see context). The new suite must NOT follow that pattern. SCHEMA-04 codifies the default-run discipline; this plan provides the literal evidence.

Output: `43-SCHEMA-04-ATTESTATION.md` capturing the `npm test` output that lists both new files in the test summary.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/43-phase-38-40-schema-normalizer-chain-integration-tests/43-CONTEXT.md
@src/agents/alerter/jest.config.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify default-run and capture attestation</name>
  <files>.planning/phases/43-phase-38-40-schema-normalizer-chain-integration-tests/43-SCHEMA-04-ATTESTATION.md</files>
  <read_first>
    - src/agents/alerter/jest.config.js (current testMatch + testPathIgnorePatterns — confirm test/farmos/integration/ is NOT in the ignore list)
    - src/agents/alerter/test/farmos/integration.test.js (the OLD gated suite — verify it's still gated; our new test/farmos/integration/extractor-to-commit.test.js must NOT inherit that gating)
    - src/agents/alerter/test/farmos/integration/extractor-to-commit.test.js (the new suite from Plan 43-05 — verify no env-gate or describe.skip)
    - src/agents/alerter/test/farmos/normalize.test.js (from Plan 43-01 — verify no gate)
  </read_first>
  <action>
    1. Verify jest config does NOT exclude `test/farmos/integration/`:
       `grep -A2 testPathIgnorePatterns src/agents/alerter/jest.config.js` — current value is `['/node_modules/', '/fixtures/', '/helpers/', '/test/eval/']`. Confirm none of these patterns match `test/farmos/integration/`. If a future maintainer added an exclude, fix the config (single-line edit) to remove it.

    2. Verify the new tests have no skip-gate:
       `grep -E "(FARMOS_INTEGRATION|describe\\.skip|it\\.skip|process\\.env\\.\\w+ ===)" src/agents/alerter/test/farmos/integration/extractor-to-commit.test.js src/agents/alerter/test/farmos/normalize.test.js`
       Expected output: empty (or only matches inside comments — manually verify each hit). If a real gate is present, REMOVE it (Plan 43-05 should not have shipped one, but defense in depth).

    3. Run the default suite and capture output:
       `cd src/agents/alerter && npm test 2>&1 | tee /tmp/43-npm-test-output.txt | tail -40`

       From the output, extract:
       - The "Test Suites:" summary line (e.g., "Test Suites: 22 passed, 22 total")
       - The "Tests:" summary line (e.g., "Tests: 245 passed, 245 total")
       - Lines containing `extractor-to-commit` and `normalize.test` (jest prints PASS/FAIL per file — these MUST appear)

    4. Write `43-SCHEMA-04-ATTESTATION.md` with:
       - Header: phase, date, command run, working directory
       - Pasted excerpt of the test-suites/tests summary lines
       - Pasted excerpt of the PASS lines for both new files (full path as jest prints them)
       - Explicit attestation: "SCHEMA-04 satisfied: both files run under default `npm test`, no env gate."
       - Cross-reference to D-13 (CONTEXT.md) and SCHEMA-04 requirement text.

    5. Sanity check: confirm the OLD gated suite (`test/farmos/integration.test.js`) was SKIPPED in the default run (look for "skipped" or absent from PASS list). This is expected and documents the difference between the old gated path and the new default-run path. Note this contrast in the attestation.
  </action>
  <verify>
    <automated>cd src/agents/alerter && npm test 2>&1 | grep -E "(test/farmos/normalize|test/farmos/integration/extractor-to-commit)" | head -5 && test -f .planning/phases/43-phase-38-40-schema-normalizer-chain-integration-tests/43-SCHEMA-04-ATTESTATION.md</automated>
  </verify>
  <done>
    43-SCHEMA-04-ATTESTATION.md exists with literal `npm test` output proving both files run by default. No FARMOS_INTEGRATION gate present in either new file. jest config does not exclude test/farmos/integration/.
  </done>
</task>

</tasks>

<verification>
- `cd src/agents/alerter && npm test 2>&1 | grep -c "test/farmos/normalize.test.js"` >= 1
- `cd src/agents/alerter && npm test 2>&1 | grep -c "test/farmos/integration/extractor-to-commit.test.js"` >= 1
- `grep -E "FARMOS_INTEGRATION|describe\\.skip" src/agents/alerter/test/farmos/normalize.test.js src/agents/alerter/test/farmos/integration/extractor-to-commit.test.js | grep -v '^\\s*//'` returns nothing (no real gates)
- 43-SCHEMA-04-ATTESTATION.md contains the pasted PASS lines from the runtime output
</verification>

<success_criteria>
- SCHEMA-04 satisfied with literal runtime evidence
- The default-run discipline is documented; a future maintainer adding a gate has a clear reference (CONTEXT D-13 + this attestation) for why it would be wrong
</success_criteria>

<output>
After completion, create `.planning/phases/43-phase-38-40-schema-normalizer-chain-integration-tests/43-06-SUMMARY.md`
</output>
