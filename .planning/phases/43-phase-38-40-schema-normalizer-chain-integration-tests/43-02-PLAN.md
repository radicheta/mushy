---
phase: 43
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/agents/alerter/src/farmos/qr.js
  - src/agents/alerter/test/farmos/qr.test.js
autonomous: true
requirements:
  - SCHEMA-01

must_haves:
  truths:
    - "resolveQr falls back to filter[name][value]=<qrCode> when id_tag lookup returns empty"
    - "Result shape includes `path: 'id_tag' | 'name'` indicating which lookup matched"
    - "Both-miss case returns found=false with no crash"
  artifacts:
    - path: "src/agents/alerter/src/farmos/qr.js"
      provides: "resolveQr with id_tag-first, name-on-miss fallback"
    - path: "src/agents/alerter/test/farmos/qr.test.js"
      provides: "tests covering id_tag hit, id_tag miss + name hit, both-miss, http error"
  key_links:
    - from: "qr.js resolveQr"
      to: "client.get(/api/asset/fungi?filter[name][value]=...)"
      via: "fallback when id_tag returns empty data array"
      pattern: "filter\\[name\\]\\[value\\]="
---

<objective>
Extend `src/agents/alerter/src/farmos/qr.js` `resolveQr(client, qrCode)` per D-06 with an id_tag-first, name-on-miss fallback. Return shape gains `path: 'id_tag' | 'name'` to indicate which lookup matched.

Purpose: Harvest chain (Test 5 in Plan 43-05) feeds extractor `source_block_refs` (block names like `260512_DT_11`) through normalize() into `source_qr_codes`, then to `resolveQr`. Without name-fallback, the lookup fails because block names are not id_tag values. D-08 documents the name-collision risk inline (no programmatic dedup in v1.7).

Output: `qr.js` with extended resolveQr ready to be consumed by chain integration tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/43-phase-38-40-schema-normalizer-chain-integration-tests/43-CONTEXT.md
@src/agents/alerter/src/farmos/qr.js
@src/agents/alerter/test/farmos/qr.test.js
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add name-fallback tests to qr.test.js (RED)</name>
  <files>src/agents/alerter/test/farmos/qr.test.js</files>
  <read_first>
    - src/agents/alerter/test/farmos/qr.test.js (existing tests — append, don't rewrite)
    - src/agents/alerter/src/farmos/qr.js (current resolveQr behavior, lines 22-35)
    - .planning/phases/43-phase-38-40-schema-normalizer-chain-integration-tests/43-CONTEXT.md D-06, D-07, D-08
  </read_first>
  <behavior>
    Three new tests to APPEND to the existing describe block:
    - Test A: id_tag hit returns path='id_tag' — already covered; verify path assertion is still green (no change needed to existing test).
    - Test B (NEW): id_tag returns empty, name lookup returns hit. Mock client.get returns `{ ok: true, status: 200, body: { data: [] } }` on first call (id_tag) and `{ ok: true, status: 200, body: { data: [{ id: 'asset-named-7' }] } }` on second call (name). Assert: result.found===true, result.assetId==='asset-named-7', result.path==='name'. Assert client.get was called twice: first url matches /filter\[id_tag\.id\]\[value\]=/, second url matches /filter\[name\]\[value\]=/.
    - Test C (NEW): both id_tag and name return empty. Assert result.found===false, result.path is either 'name' or 'id_tag' (planner's choice — pick one and assert that). Verify two calls were made.
    - Test D (NEW): id_tag returns http_500. Assert resolveQr surfaces http_500 immediately WITHOUT falling back to name lookup (an HTTP error is not a "miss" — it's a transport failure). Assert client.get called exactly once.
  </behavior>
  <action>
    Append 3 new `it(...)` blocks to the existing describe in qr.test.js. Use the existing `mockClient({ getImpl })` helper. For multi-call mocks, build a getImpl that increments a counter and returns different responses per call (jest.fn(...).mockResolvedValueOnce(...).mockResolvedValueOnce(...) is the idiomatic pattern — switch to that style or hand-roll the counter). Run `npm test -- test/farmos/qr.test.js` and confirm new tests fail (RED).
  </action>
  <verify>
    <automated>cd src/agents/alerter && npm test -- test/farmos/qr.test.js 2>&1 | grep -E "(FAIL|✕|Tests:)" | head -5</automated>
  </verify>
  <done>
    qr.test.js has 3 new failing tests for the name-fallback behavior. Existing tests still pass (no regression in test setup).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement name-fallback in qr.js resolveQr (GREEN)</name>
  <files>src/agents/alerter/src/farmos/qr.js</files>
  <read_first>
    - src/agents/alerter/test/farmos/qr.test.js (the spec, just-written)
    - src/agents/alerter/src/farmos/qr.js (current resolveQr, lines 22-35)
    - .planning/phases/43-phase-38-40-schema-normalizer-chain-integration-tests/43-CONTEXT.md D-06, D-08
  </read_first>
  <action>
    Modify `resolveQr(client, qrCode)` to:
    1. Keep the existing id_tag call as-is.
    2. If the id_tag call returns `ok: true` but `data: []` (empty array — i.e., no asset matches the id_tag), make a second GET request: `/api/asset/fungi?filter[name][value]=<enc>` where `enc = encodeURIComponent(qrCode)`.
    3. Parse the second response: if `data` array is non-empty, return `{ found: true, assetId: arr[0].id, path: 'name' }`.
    4. If second response is also empty, return `{ found: false, path: 'name' }`.
    5. If second response is http-error, return `{ found: false, error: 'http_' + status, path: 'name' }`.
    6. HTTP error on FIRST call (id_tag) returns immediately with path='id_tag' (no fallback — per Test D). Preserve existing behavior at line 26.
    7. catch (e) at outer try returns `{ found: false, error: e.message }` — preserve existing shape (no path field on uncaught throw is acceptable; existing test at line 33 does not assert path on this branch).

    Add an inline comment block above resolveQr documenting D-06 (id_tag-first, name-on-miss) and D-08 (first-result-wins on name collision — farmer-side discipline risk, not programmatically deduped).

    Run `npm test -- test/farmos/qr.test.js` and confirm all tests pass (GREEN).
    Also run `npm test` and confirm no other tests regressed (bindQrOnCreate untouched; existing commit-*.test.js uses mock clients that return id_tag hits — no fallback triggered).
  </action>
  <verify>
    <automated>cd src/agents/alerter && npm test -- test/farmos/qr.test.js 2>&1 | tail -5</automated>
  </verify>
  <done>
    qr.js resolveQr falls back to name lookup on id_tag miss. All qr.test.js tests pass. Full `npm test` shows no regressions in commit-*.test.js suites.
  </done>
</task>

</tasks>

<verification>
- `cd src/agents/alerter && npm test -- test/farmos/qr.test.js` exits 0 with new tests passing
- `cd src/agents/alerter && npm test` exits 0 (no regressions)
- `grep -c "filter\[name\]\[value\]" src/agents/alerter/src/farmos/qr.js` returns >=1
- Comment block above resolveQr references D-06 and D-08
</verification>

<success_criteria>
- resolveQr returns path='id_tag' on id_tag hit
- resolveQr returns path='name' on id_tag miss + name hit
- resolveQr returns found=false on both-miss
- HTTP errors on id_tag call do NOT trigger name fallback (transport vs miss distinction)
</success_criteria>

<output>
After completion, create `.planning/phases/43-phase-38-40-schema-normalizer-chain-integration-tests/43-02-SUMMARY.md`
</output>
