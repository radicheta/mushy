---
phase: 43
plan: 03
type: execute
wave: 2
depends_on:
  - 43-01
files_modified:
  - src/agents/alerter/src/farmos/commits/commit-router.js
autonomous: true
requirements:
  - SCHEMA-01
  - SCHEMA-03

must_haves:
  truths:
    - "commit-router.js calls normalize() on the draft before dispatching to the per-log_type handler"
    - "All existing commit-*.test.js suites still pass (their commit-shape fixtures exercise SCHEMA-03 idempotency via the new path)"
  artifacts:
    - path: "src/agents/alerter/src/farmos/commits/commit-router.js"
      provides: "router that normalizes before dispatch"
      contains: "require('./normalize').normalize"
  key_links:
    - from: "commit-router.js"
      to: "normalize.js"
      via: "require('./normalize').normalize(draft) before fn(client, ..., ctx) call"
      pattern: "require\\(.\\./normalize.\\)\\.normalize"
---

<objective>
Wire `normalize()` into `commit-router.js` at line 36–37 per D-02. Single-line edit: replace `const r = await fn(client, draft, ctx);` with the normalized-draft call.

Purpose: With normalize.js exporting an idempotent transform (Plan 43-01), wiring it into the dispatch site means every existing commit-shape fixture exercises the SCHEMA-03 pass-through path automatically. New extractor-shape inputs (chain tests in Plan 43-05) reach the commit handlers in the shape they expect.

Output: commit-router.js with the normalizer in the dispatch path.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/43-phase-38-40-schema-normalizer-chain-integration-tests/43-CONTEXT.md
@src/agents/alerter/src/farmos/commits/commit-router.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire normalize() into commit-router.js</name>
  <files>src/agents/alerter/src/farmos/commits/commit-router.js</files>
  <read_first>
    - src/agents/alerter/src/farmos/commits/commit-router.js (current file, lines 14-46)
    - src/agents/alerter/src/farmos/commits/normalize.js (the module being wired in, written in Plan 43-01)
    - .planning/phases/43-phase-38-40-schema-normalizer-chain-integration-tests/43-CONTEXT.md D-02
  </read_first>
  <action>
    Edit `src/agents/alerter/src/farmos/commits/commit-router.js`:
    1. At the top of the dispatch try-block (around line 36), wrap the draft via the normalizer. Concretely: add a `require('./normalize').normalize(draft)` (or pre-require at top of file and call `normalize(draft)` inline) and pass the result to `fn(client, ..., ctx)`. Inline-require is fine per D-02 ("a one-line edit at the dispatch site"); top-of-file require is also acceptable if the planner finds it cleaner. Preserve all existing error handling (UnsupportedLogTypeError catch, generic catch, timing, result envelope).
    2. Add a one-line comment referencing Phase 43 / D-02 above the normalize call so future readers understand why the normalize layer exists.
    3. Do NOT mutate `draft` in place. normalize() already returns a new object — pass that new object to `fn`, but leave the original `draft` parameter untouched (other code may inspect it via `ctx`, audit-logger, etc.).
    4. Run the full alerter test suite: `cd src/agents/alerter && npm test`. ALL existing tests must pass — the commit-*.test.js fixtures are commit-shape, so normalize() is a no-op pass-through for them (SCHEMA-03 in action).
    5. If ANY existing test fails after this edit: the failure indicates a normalize.js bug (idempotency hole) — file the failure and return to Plan 43-01 to fix, do NOT patch over by reshaping the test or guarding the call.
  </action>
  <verify>
    <automated>cd src/agents/alerter && npm test 2>&1 | tail -10</automated>
  </verify>
  <done>
    commit-router.js calls normalize() before dispatch. `cd src/agents/alerter && npm test` exits 0 with all existing suites still passing.
  </done>
</task>

</tasks>

<verification>
- `grep -c "normalize" src/agents/alerter/src/farmos/commits/commit-router.js` returns >=2 (require + call site)
- `cd src/agents/alerter && npm test` exits 0
- All pre-existing commit-*.test.js suites pass without modification (proof of SCHEMA-03 idempotency over real curated fixtures)
</verification>

<success_criteria>
- normalize() is called inside commit-router.commit before dispatch
- Zero regressions in existing test suite
- SCHEMA-03 implicitly validated by ~50+ existing commit-shape test fixtures
</success_criteria>

<output>
After completion, create `.planning/phases/43-phase-38-40-schema-normalizer-chain-integration-tests/43-03-SUMMARY.md`
</output>
