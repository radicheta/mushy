---
phase: 43
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/agents/alerter/src/farmos/commits/normalize.js
  - src/agents/alerter/test/farmos/normalize.test.js
autonomous: true
requirements:
  - SCHEMA-01
  - SCHEMA-03

must_haves:
  truths:
    - "normalize(draft) produces a new draft with draft_json reshaped to commit-shape per log_type"
    - "normalize(draft) is idempotent: commit-shape input passes through byte-identical"
    - "normalize(draft) does NOT mutate the input draft or its draft_json"
  artifacts:
    - path: "src/agents/alerter/src/farmos/commits/normalize.js"
      provides: "pure normalize(draft) function, exported via module.exports.normalize"
      min_lines: 80
    - path: "src/agents/alerter/test/farmos/normalize.test.js"
      provides: "unit tests for all 5 log_types + idempotency + non-mutation"
      min_lines: 120
  key_links:
    - from: "normalize.js"
      to: "draft.draft_json"
      via: "returns { ...draft, draft_json: out } with new object"
      pattern: "return.*\\.\\.\\.draft.*draft_json"
---

<objective>
Create the router-side normalizer `normalize.js` that translates extractor-shape draft_json -> commit-shape per log_type, plus its unit test suite including the SCHEMA-03 idempotency assertion.

Purpose: Close the extractor<->commit shape mismatch (2026-05-15 lion's-mane regression) with the smallest blast radius. Preserves both schemas at their natural boundaries; normalized shape lives only inside the commit dispatch frame.

Output: `normalize.js` ready to be wired into `commit-router.js` in Plan 43-03.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/43-phase-38-40-schema-normalizer-chain-integration-tests/43-CONTEXT.md
@.planning/notes/2026-05-16-schema-audit.md
@src/agents/alerter/src/farmos/commits/commit-input.js
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write normalize.test.js unit tests (RED)</name>
  <files>src/agents/alerter/test/farmos/normalize.test.js</files>
  <read_first>
    - src/agents/alerter/src/farmos/commits/commit-input.js (lines 10-25 — notes-serializer pattern that recipe_lot prepends BEFORE)
    - src/agents/alerter/src/farmos/commits/commit-activity.js (lines 14-29 — fields commit reads)
    - src/agents/alerter/src/farmos/commits/commit-harvest.js (lines 27-45, 74 — fields commit reads)
    - src/agents/alerter/src/farmos/commits/commit-observation.js (lines 12-37)
    - src/agents/alerter/src/farmos/commits/commit-seeding.js (lines 22-65)
    - src/agents/alerter/src/extraction/schemas/activity.js, observation.js, input.js, harvest.js, seeding.js (extractor-shape contracts — FROZEN)
    - .planning/notes/2026-05-16-schema-audit.md §3 Option A sketch (lines 384-454) AS AMENDED BY D-09 (recipe_lot PREPEND, not append) and D-12 (single-bag synth)
    - .planning/phases/43-phase-38-40-schema-normalizer-chain-integration-tests/43-CONTEXT.md D-01..D-12
  </read_first>
  <behavior>
    Test coverage required (~10 tests):
    - Common transforms (apply to all log_types):
      - `event_timestamp` ISO -> `timestamp` unix seconds (Math.floor(Date.parse(iso)/1000))
      - `asset_ref` string -> `qr_codes` single-element array
      - `asset_ref === '<UNKNOWN>'` -> `qr_codes` is empty array []
      - Missing both `event_timestamp` and `timestamp` -> `timestamp` left undefined (downstream falls back to Date.now()/1000)
    - Per-log_type (per D-04, follows audit §3 sketch):
      - activity: `name` -> `activity_subtype`
      - harvest: `source_block_refs` -> `source_qr_codes` (verbatim rename, no regex per D-05); `harvest_batch_id` -> `harvest_batch_name`; `qty_g` -> `bags: [{weight_grams: qty_g}]` single synthesized bag (per D-12); when qty_g absent, bags remains absent
      - seeding: `species` -> `species_code` (only if species_code absent); `batch_name` and `parent_batch_name` left distinct (per D-11)
      - input: `recipe_lot` PREPENDED to notes as `recipe_lot: <value>\n` BEFORE any existing notes content (per D-09). NOTE: this is BEFORE commit-input's own ingredients serializer runs; final notes order in commit-input output will be `recipe_lot:...\n\nIngredients:\n...`
      - observation: `state` appended to notes as `state: <value>` (audit §3 sketch)
    - Idempotency (SCHEMA-03): for each log_type, build a hand-shaped commit-shape draft (qr_codes, timestamp number, activity_subtype/source_qr_codes/etc.), pass through normalize(), assert deep-equal to input draft_json — byte-identical pass-through
    - Non-mutation: assert normalize(draft) returns a NEW object (not === draft) and that input draft.draft_json is unchanged after call
  </behavior>
  <action>
    Create `test/farmos/normalize.test.js` following the existing `test/farmos/commit-*.test.js` style (Jest, plain CommonJS require). Import target: `const { normalize } = require('../../src/farmos/commits/normalize');` — file does not exist yet, tests MUST fail until Task 2 lands. Each test has a single describe/it. Use minimal hand-built draft objects with `{ log_type, draft_json: {...} }` shape (mirror what commit-watchdog passes). Do NOT mock anything — normalize is pure. For idempotency tests, use draft fixtures from `test/farmos/fixtures/curated/` as reference (read-only — those files already have commit-shape). Run `npm test -- test/farmos/normalize.test.js` and confirm it fails with "Cannot find module '../../src/farmos/commits/normalize'" — that is the RED state.
  </action>
  <verify>
    <automated>cd src/agents/alerter && npm test -- test/farmos/normalize.test.js 2>&1 | grep -E "(Cannot find module|FAIL)" | head -3</automated>
  </verify>
  <done>
    test/farmos/normalize.test.js exists with ~10 test cases covering common transforms, all 5 log_types, idempotency per log_type, and non-mutation. Test run fails because normalize.js does not yet exist (RED).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement normalize.js (GREEN)</name>
  <files>src/agents/alerter/src/farmos/commits/normalize.js</files>
  <read_first>
    - src/agents/alerter/test/farmos/normalize.test.js (just-written, the spec)
    - .planning/notes/2026-05-16-schema-audit.md §3 Option A code sketch (lines 384-454) — starting point
    - .planning/phases/43-phase-38-40-schema-normalizer-chain-integration-tests/43-CONTEXT.md D-01..D-12 (amendments to the audit sketch)
    - src/agents/alerter/src/farmos/commits/commit-input.js (verify recipe_lot PREPEND lands before line 25 serializer runs)
  </read_first>
  <action>
    Implement `normalize(draft)` per D-01 (pure function, returns new draft with `draft_json` reshaped). Follow audit §3 sketch as starting point, modified by:
    - D-03: common transforms (event_timestamp -> timestamp, asset_ref -> qr_codes, filter `<UNKNOWN>` sentinel)
    - D-04: per-log_type transforms via switch on `draft.log_type`
    - D-05: harvest source_block_refs -> source_qr_codes verbatim (no regex filter)
    - D-09: input recipe_lot PREPENDS to notes (NOT append). Use `out.notes = 'recipe_lot: ' + out.recipe_lot + (out.notes ? '\n' + out.notes : '')` so the existing commit-input.js:25 ingredient serializer chains AFTER this. Final notes will be `recipe_lot: RB-2026-05\nIngredients:\n- oat 1kg\n- gypsum 50g` (or similar — commit-input may add its own separator).
    - D-11: seeding batch_name vs parent_batch_name left distinct (no fold)
    - D-12: harvest qty_g -> bags single-bag synth only when qty_g present
    - Idempotency (D-01, D-18): the pattern `if (!Array.isArray(out.qr_codes) && typeof out.asset_ref === 'string')` etc. naturally guards — if commit-shape already present, the guarded transform is skipped. Verify every per-log_type transform uses this same guard so idempotency falls out of the structure.
    - Return shape: `{ ...draft, draft_json: out }` where `out = { ...dj }` — NEW objects, no mutation of input.
    Export: `module.exports = { normalize };` Match existing CommonJS style in sibling commit-*.js files. Add a top-of-file comment block matching the style of commit-input.js / qr.js (purpose, Phase 43 reference). No fenced code blocks here — read the audit sketch for the body.
    Run tests: `cd src/agents/alerter && npm test -- test/farmos/normalize.test.js` — all tests MUST pass.
  </action>
  <verify>
    <automated>cd src/agents/alerter && npm test -- test/farmos/normalize.test.js 2>&1 | tail -5</automated>
  </verify>
  <done>
    normalize.js exists at src/agents/alerter/src/farmos/commits/normalize.js, exports `normalize` function. All tests in normalize.test.js pass (GREEN). Existing commit-*.test.js suites still pass (zero regressions — normalize.js not yet wired in).
  </done>
</task>

</tasks>

<verification>
- `cd src/agents/alerter && npm test -- test/farmos/normalize.test.js` exits 0 with ~10 passing tests including idempotency assertions for all 5 log_types
- `cd src/agents/alerter && npm test` exits 0 (zero regressions in pre-existing suites — normalize.js not yet wired into commit-router)
- `grep -c "module.exports" src/agents/alerter/src/farmos/commits/normalize.js` returns >=1
- normalize.js does not mutate its input (verified by non-mutation test)
</verification>

<success_criteria>
- SCHEMA-01 (per-log_type transforms) unit-level GREEN
- SCHEMA-03 (idempotency) unit-level GREEN
- normalize.js ready for wire-in by Plan 43-03
- No regressions in existing alerter test suite
</success_criteria>

<output>
After completion, create `.planning/phases/43-phase-38-40-schema-normalizer-chain-integration-tests/43-01-SUMMARY.md`
</output>
