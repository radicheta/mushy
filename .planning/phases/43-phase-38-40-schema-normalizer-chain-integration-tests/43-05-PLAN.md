---
phase: 43
plan: 05
type: execute
wave: 3
depends_on:
  - 43-01
  - 43-02
  - 43-03
  - 43-04
files_modified:
  - src/agents/alerter/test/farmos/integration/extractor-to-commit.test.js
autonomous: true
requirements:
  - SCHEMA-01
  - SCHEMA-02
  - SCHEMA-04

must_haves:
  truths:
    - "5 chain tests live at src/agents/alerter/test/farmos/integration/extractor-to-commit.test.js"
    - "Each test runs the full pipeline: extractor.extract() -> normalize() -> commit handler against mock-client"
    - "Each test asserts shape at three boundaries: post-extract (extractor-shape), post-normalize (commit-shape), commit result (success or classified failure)"
    - "Test 2 uses the real 2026-05-15 lion's-mane transcript from 43-FIXTURES.md (NOT a paraphrase)"
    - "All 5 tests run under `npm test` by default — no FARMOS_INTEGRATION=1 gate"
  artifacts:
    - path: "src/agents/alerter/test/farmos/integration/extractor-to-commit.test.js"
      provides: "5-test chain suite covering seeding, activity (lion's mane regression guard), observation, input, harvest"
      min_lines: 250
  key_links:
    - from: "extractor-to-commit.test.js"
      to: "src/agents/alerter/src/extraction/pipeline.js + src/farmos/commits/normalize.js + src/farmos/commits/commit-router.js"
      via: "extract -> normalize -> commit pipeline; LLM mocked via test/extraction/helpers, farmOS mocked via test/farmos/mock-client.js"
      pattern: "extractor\\.extract|normalize|commit"
---

<objective>
Create 5 chain integration tests at `src/agents/alerter/test/farmos/integration/extractor-to-commit.test.js` per audit Option C, D-13..D-17. Each test runs a full extractor -> normalize -> commit pipeline against mocked LLM (via existing extractor test helpers) and mocked farmOS (via existing `mock-client.js`). Suite runs by default under `npm test`.

Purpose: Catch the next extractor<->commit schema drift before it ships. Test 2 is the named 2026-05-15 regression guard — uses the real lion's-mane transcript from `43-FIXTURES.md`. With normalize.js (Plan 43-01) wired in (Plan 43-03), Test 2 must pass; without it, Test 2 fails — proving the regression guard works.

Output: 5-test suite GREEN against the wired-in normalize() path.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/43-phase-38-40-schema-normalizer-chain-integration-tests/43-CONTEXT.md
@.planning/phases/43-phase-38-40-schema-normalizer-chain-integration-tests/43-FIXTURES.md
@.planning/notes/2026-05-16-schema-audit.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Author 5 chain integration tests with three-boundary assertions</name>
  <files>src/agents/alerter/test/farmos/integration/extractor-to-commit.test.js</files>
  <read_first>
    - .planning/phases/43-phase-38-40-schema-normalizer-chain-integration-tests/43-FIXTURES.md (Test 2 transcript + recipe)
    - .planning/notes/2026-05-16-schema-audit.md §3 Option C tests 1-5 sketches (lines 520-562)
    - src/agents/alerter/test/farmos/mock-client.js (mock farmOS client — seeded fungi assets; reuse without modification per D-15)
    - src/agents/alerter/test/extraction/helpers/fake-anthropic-server.js (LLM mock helper — wire as the Anthropic responder per D-14)
    - src/agents/alerter/src/extraction/pipeline.js (entry point for extract — find the export name and parameter shape)
    - src/agents/alerter/src/farmos/commits/commit-router.js (post-43-03; uses normalize() internally — tests can either call router or call normalize() then individual commit-* fn; recommend router for SCHEMA-01 coverage of the full path)
    - src/agents/alerter/src/farmos/commits/normalize.js (exported for direct shape-boundary assertions)
    - src/agents/alerter/test/farmos/commit-activity.test.js (style template for assertion idioms against mock-client)
    - src/agents/alerter/src/extraction/schemas/{activity,observation,input,harvest,seeding}.js (extractor-shape markers — use to assert post-extract boundary)
  </read_first>
  <behavior>
    Structure (per D-13, D-17): one describe block, 5 it() tests. Each test follows the three-boundary pattern:

    (a) post-extract boundary: assert extractor-shape markers present on draft_json (e.g., `asset_ref` string, `event_timestamp` ISO, `name` enum, `source_block_refs` array, `qty_g` number, `recipe_lot` string — per log_type)
    (b) post-normalize boundary: assert commit-shape markers present (e.g., `qr_codes` array, `timestamp` number, `activity_subtype` string, `source_qr_codes` array, `bags` array — per log_type). Call normalize() directly to capture the intermediate shape; do NOT only observe it through the commit handler.
    (c) post-commit boundary: assert `commit_success: true` (or a classified failure with the exact `reason` enum value documented in 43-FIXTURES.md for Test 2's commit-failure framing); for success paths, assert the specific side-effect (right asset id in relationship, right id_tag binding, right notes content, right timestamp).

    Failure messages must name which boundary failed (per D-17). Use Jest's optional message argument on expect, OR wrap assertions in a small helper like `assertExtractorShape(draft, expected)` / `assertCommitShape(draft, expected)` that throws with the boundary name in the message.

    Tests (audit §3 Option C as the starting structure):

    1. seeding chain: Whisper transcript like "Inoc-ed block 260516_DT_1 from batch BATCH-2026-05-16-001 with 1kg shiitake grain". Mock LLM to return the expected Zod-valid seeding extractor output (asset_ref="260516_DT_1", species="shiitake", block_name="260516_DT_1", parent_batch_name="BATCH-2026-05-16-001", qty=1000, event_timestamp ISO). After normalize: species_code present, timestamp unix, qr_codes from asset_ref. Commit: assert commit_success and block created/found, strain resolved to "SHI" (via farmOS-side fungi_type cache — verify the mock has SHI seeded; check mock-client.js fixtures first).

    2. activity chain (THE 2026-05-15 REGRESSION GUARD, per D-16): use the verbatim transcript from 43-FIXTURES.md. Mock LLM to return the live 2026-05-15 extractor shape (name="relocate", asset_ref=`<UNKNOWN>` OR `260415_LIMA_1` depending on which framing 43-FIXTURES.md recommends — pick the framing where the test ends in a meaningful assertion). Pick ONE: (i) commit-failure path: asset_ref=`<UNKNOWN>` -> qr_codes=[] -> commit returns `ok: false, reason: 'no_target_asset_for_activity'` — assert this CLEAN classified failure (proves the chain doesn't die on schema mismatch, which is what the regression was); OR (ii) happy path: asset_ref=`260415_LIMA_1`, seed a LIMA block in mock-client setup, assert `commit_success: true` and activity_subtype="relocate" on the created log. Recommend (ii) — it's a stronger regression guard because it asserts the WHOLE chain works, not just that it fails cleanly.

    3. observation chain: transcript like "pin emergence on 260513_SHI_2 with photo". Mock LLM to return observation extractor shape (asset_ref="260513_SHI_2", state="pinning", event_timestamp ISO). After normalize: state appended to notes ("state: pinning"). Commit: assert log created, asset relationship to 260513_SHI_2, notes contain "state: pinning". (file_id assertion optional — depends on whether the chain exercises file upload, which is probably out of scope for v1 — focus on log creation.)

    4. input chain: transcript like "Mixed substrate for 260514_KOY_3 with oat 1kg, gypsum 50g, recipe RB-2026-05". Mock LLM to return input extractor shape (asset_ref="260514_KOY_3", recipe_lot="RB-2026-05", event_timestamp ISO). After normalize: notes prepended with "recipe_lot: RB-2026-05" (per D-09 — verify the PREPEND happens, not append). Commit: assert log created, notes content contains both "recipe_lot: RB-2026-05" AND any ingredient-serializer output if input_ingredients present (or just the recipe_lot line if not). Document inline that the multi-ingredient extractor schema extension is deferred (per D-09 + v1.8 candidate).

    5. harvest chain: transcript like "Picked 3 bags from 260512_DT_11: 250g, 230g, 260g, batch HBATCH-2026-05-15-DT-001". Mock LLM to return harvest extractor shape (source_block_refs=["260512_DT_11"], qty_g=740 [sum, since extractor schema is single number per D-12], harvest_batch_id="HBATCH-2026-05-15-DT-001", event_timestamp ISO). After normalize: source_qr_codes=["260512_DT_11"], bags=[{weight_grams: 740}] single synthesized bag, harvest_batch_name="HBATCH-2026-05-15-DT-001". Commit: assert ONE bag asset created (single-bag synth per D-12), strain regex-resolves to "DT" from harvest_batch_name, source asset relationship via name-fallback resolve (since "260512_DT_11" is not an id_tag — verifies Plan 43-02 name-fallback). Document the multi-bag gap inline (v1.8 candidate per D-12).

    Setup: per-test beforeEach builds a fresh mock-client and (for tests with seeded blocks) calls mock-client setup helpers to seed the required fungi assets. For LLM mock: use the fake-anthropic-server helper to register canned responses per test (one response per test — no shared state).

    Skip-gate: NONE. The describe block runs unconditionally — no `process.env.FARMOS_INTEGRATION === '1'` check (per D-13, SCHEMA-04). Add an inline comment at the top stating "Phase 43 SCHEMA-04: this suite runs under `npm test` by default. Do NOT add a gate."
  </behavior>
  <action>
    Create `src/agents/alerter/test/farmos/integration/extractor-to-commit.test.js`. Use CommonJS require (match alerter style). Top-of-file comment block names Phase 43, D-13..D-17, SCHEMA-04, and the regression-guard role of Test 2. Use the audit sketch (notes/2026-05-16-schema-audit.md §3 Option C lines 520-562) as the starting structure — don't reinvent the assertion shape.

    Wiring: import `extractor.extract` (or whatever the pipeline.js entry is — read the file first), `normalize`, `commit` (from commit-router.js — preferred so Plan 43-03's wire-in is exercised end-to-end), and the mock helpers. For Test 2, copy the verbatim transcript text from 43-FIXTURES.md.

    Run: `cd src/agents/alerter && npm test -- test/farmos/integration/extractor-to-commit.test.js`. All 5 tests must pass. If a test fails because of a bug in normalize.js, fix normalize.js (back-edit Plan 43-01) rather than weakening the assertion. If a test fails because the mock-client doesn't have the required seeded fungi asset, extend mock-client.js (small additive change is fine — but only seed what the test needs; do not refactor mock-client structure).

    Also run full `npm test` and confirm zero regressions in pre-existing suites.

    Optional regression-guard demo (manual, NOT a committed assertion): `git stash` only `src/agents/alerter/src/farmos/commits/normalize.js`, run Test 2, observe failure, `git stash pop`, run Test 2, observe pass. Note the manual demo in 43-05-SUMMARY.md; do NOT add a hook that does this automatically.
  </action>
  <verify>
    <automated>cd src/agents/alerter && npm test -- test/farmos/integration/extractor-to-commit.test.js 2>&1 | tail -10</automated>
  </verify>
  <done>
    test/farmos/integration/extractor-to-commit.test.js exists with 5 passing tests. Each test asserts post-extract, post-normalize, post-commit shape boundaries. Test 2 uses the verbatim 2026-05-15 lion's-mane transcript from 43-FIXTURES.md. Full `npm test` exits 0.
  </done>
</task>

</tasks>

<verification>
- `test -f src/agents/alerter/test/farmos/integration/extractor-to-commit.test.js`
- `cd src/agents/alerter && npm test -- test/farmos/integration/extractor-to-commit.test.js` exits 0 with 5 passing tests
- `grep -c "lion's mane block into the fruiting chamber" src/agents/alerter/test/farmos/integration/extractor-to-commit.test.js` >= 1 (verbatim transcript present)
- `grep -c "FARMOS_INTEGRATION" src/agents/alerter/test/farmos/integration/extractor-to-commit.test.js | head -n 1 | grep -v '^# '` returns 0 (no env gate)
- Full `npm test` exits 0
</verification>

<success_criteria>
- SCHEMA-01: every log_type round-trips extractor->normalize->commit GREEN
- SCHEMA-02: Test 2 uses real 2026-05-15 transcript and commits end-to-end
- SCHEMA-04 partially satisfied (default-run verified ad-hoc by `npm test`); Plan 43-06 caps the SCHEMA-04 gate
- Manual regression-guard demo documented in SUMMARY (stash normalize.js -> Test 2 fails -> unstash -> passes)
</success_criteria>

<output>
After completion, create `.planning/phases/43-phase-38-40-schema-normalizer-chain-integration-tests/43-05-SUMMARY.md`
</output>
