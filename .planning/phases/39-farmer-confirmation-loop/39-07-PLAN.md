---
phase: 39-farmer-confirmation-loop
plan: 07
type: execute
wave: 3
depends_on: ["01", "02", "03", "04", "05", "06"]
files_modified:
  - src/agents/alerter/test/confirm/integration.test.js
  - src/agents/alerter/test/confirm/fixtures/prod-draft-awaiting.json
  - src/agents/alerter/test/confirm/fixtures/curated/*.json
  - .planning/phases/39-farmer-confirmation-loop/39-RUNBOOK.md
  - .planning/phases/39-farmer-confirmation-loop/39-EVAL-REPORT.md
  - src/agents/alerter/test/confirm/fixtures/README.md
autonomous: true
requirements: [CONF-01, CONF-02, CONF-03, CONF-04, CONF-05]
must_haves:
  truths:
    - "8 synthetic integration scenarios + 1 real-prod fixture all pass against the wired alerter (in-memory pg pool + mocked signal-cli + mocked LLM extractor): YES happy path, NO discard, duplicate YES no-op, EDIT once with preview re-render, EDIT 3-then-cap, nudge at 0.8*timeout, expire at timeout, superseded-by-newer-draft"
    - "Real-prod-fixture (D-09a ship gate) replays a known awaiting_farmer draft from /mnt/mossrock/shared/mushdatadump-prod/ as a JSON snapshot through the YES, NO, EDIT (mocked re-extract), and timeout paths -- curated-only is INSUFFICIENT"
    - "39-RUNBOOK.md captures the live-farmer Signal UAT script for operator-deferred verification (same pattern as Phase 25/37)"
    - "39-EVAL-REPORT.md records pass/fail per scenario with concrete artifact paths so the verification gate can confirm the ship-gate evidence exists on disk"
  artifacts:
    - path: "src/agents/alerter/test/confirm/integration.test.js"
      provides: "End-to-end integration test wiring receive-loop -> parser -> state-machine -> confirm-db -> outbound, with mocked signal-cli and mocked LLM"
    - path: "src/agents/alerter/test/confirm/fixtures/prod-draft-awaiting.json"
      provides: "One real-prod inoc draft frozen at status=awaiting_farmer (operator-blessed JSON snapshot from mushdatadump-prod) -- the ship-gate witness"
    - path: ".planning/phases/39-farmer-confirmation-loop/39-RUNBOOK.md"
      provides: "Operator-driven live-Signal UAT script: 8 scenarios + observability checklist + rollback steps"
    - path: ".planning/phases/39-farmer-confirmation-loop/39-EVAL-REPORT.md"
      provides: "Final eval report with per-scenario status + fixture provenance + any deferred items"
  key_links:
    - from: "integration.test.js"
      to: "src/agents/alerter/src/confirm/* + receive-loop.js"
      via: "Module imports + in-memory pool + mocked signal + mocked extractor"
      pattern: "createReceiveLoop|createWatchdog|createConfirmOutbound|createEditHandler"
    - from: "fixtures/prod-draft-awaiting.json"
      to: "/mnt/mossrock/shared/mushdatadump-prod/"
      via: "Operator-curated snapshot (manifest captured in README.md)"
      pattern: "prod-draft-awaiting"
---

<objective>
Land the ship-gate evidence. Eight synthetic integration scenarios prove the wired alerter executes Phase 39's five success criteria across every relevant transition; one real-prod-fixture scenario satisfies the project-memory `feedback_real_data_before_ship_gate_pass.md` rule (Phase 38 was re-opened on 2026-05-12 because curated-only PASS was retracted; Phase 39 must not repeat that). A runbook captures the operator-driven live UAT for after-the-fact farmer verification, and an eval report records the test status with concrete on-disk evidence.

This plan does NOT modify Phase 39 source code. It exclusively writes test code, fixtures, and documentation that gate the ship attestation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<tasks>
<task id="1">
<title>Capture the real-prod fixture (D-09a ship-gate)</title>
<read_first>
- /mnt/mossrock/shared/mushdatadump-prod/ (directory listing -- pick a 2026-05-12 inoc session that produced a clean awaiting_farmer draft)
- .planning/phases/39-farmer-confirmation-loop/39-CONTEXT.md D-09a
- Memory rules: feedback_real_data_before_ship_gate_pass, feedback_persist_paid_results_default
- src/agents/alerter/src/extraction/extraction-db.js (the row shape we need to snapshot)
</read_first>
<action>
1. List `/mnt/mossrock/shared/mushdatadump-prod/` and pick one inoc session whose Phase 38 extraction successfully produced a clean draft (status=awaiting_farmer, farmer_facing_preview populated). If multiple are available, pick the one with the simplest single-event seeding (not paper-log batch).

2. Run a one-shot query against the live Timescale (read-only, no writes) -- if access is unavailable in the sandbox, fall back to constructing the fixture from the on-disk artifact under `/mnt/mossrock/shared/mushdatadump-prod/<session>/` using the Phase 38 EVAL-REPORT lineage (the eval harness saved per-fixture extraction outputs that include the same draft fields).

3. Write the captured row to `src/agents/alerter/test/confirm/fixtures/prod-draft-awaiting.json` as a JSON object with the signal_draft columns (id, sender_e164, farmos_person, source_capture_ids, status='awaiting_farmer', log_type, draft_json, per_field_confidence, askback_turns, farmer_facing_preview, reply_target_kind, group_id). Strip the real sender_e164 down to a fixture-safe form: replace with `'+15550001234'` and document the swap in the fixture's `_provenance` block.

4. Add a sibling `_provenance` field inside the JSON capturing: source session path, capture date, the field-substitutions performed, and a one-line operator note (e.g. "Don Santiago, this is the 260512_DT_1 inoc draft").

5. Create `src/agents/alerter/test/confirm/fixtures/README.md` documenting:
   - What `prod-draft-awaiting.json` is and why it's load-bearing for the ship gate.
   - Field-substitution policy (real phone numbers MUST be replaced with `+15550001234`-style synthetic).
   - How to refresh the fixture (the operator pulls a new awaiting_farmer row, runs the same redaction, commits a new fixture file with a date suffix).
   - Pointer to memory `feedback_real_data_before_ship_gate_pass.md` and Phase 38's retraction history.

6. Also under `src/agents/alerter/test/confirm/fixtures/curated/`, place 2-3 small JSON fixtures for the synthetic scenarios that need richer draft data than is convenient to inline (e.g. `seeding-clean.json`, `seeding-low-conf.json`). Each is a few lines of crafted JSON, no provenance block needed.

DO NOT commit any real phone numbers or any other PII to the fixtures.
</action>
<acceptance_criteria>
- `test -f src/agents/alerter/test/confirm/fixtures/prod-draft-awaiting.json` -- file exists
- `node -e "const f=require('./src/agents/alerter/test/confirm/fixtures/prod-draft-awaiting.json'); console.log(f.status, !!f.farmer_facing_preview, !!f._provenance)"` prints `awaiting_farmer true true`
- No real-looking E.164 numbers in fixtures: `grep -E "\\+[0-9]{10,12}" src/agents/alerter/test/confirm/fixtures/*.json | grep -v "+15550001234" | wc -l` returns 0 (excluding the synthetic ones)
- fixtures/README.md exists and references both memory rules (`grep -c "feedback_real_data_before_ship_gate_pass" src/agents/alerter/test/confirm/fixtures/README.md` >= 1)
- The provenance block names the source session path under `/mnt/mossrock/shared/mushdatadump-prod/`
</acceptance_criteria>
</task>

<task id="2">
<title>Author the 9 integration scenarios in integration.test.js</title>
<read_first>
- src/agents/alerter/src/index.js (the wiring sequence we're reproducing in the test)
- src/agents/alerter/src/receive-loop.js + src/agents/alerter/src/confirm/* (the live modules under test)
- src/agents/alerter/test/confirm/fake-pool.js (Plan 01-task-5 -- the in-memory pool)
- src/agents/alerter/test/extraction/pipeline.test.js (Phase 38 integration test for ergonomic patterns -- mocked signal, mocked extractor, real state-machine)
- src/agents/alerter/test/confirm/fixtures/prod-draft-awaiting.json (task 1)
- .planning/phases/39-farmer-confirmation-loop/39-RESEARCH.md section 10 + 11 (test layout + Nyquist dimensions)
</read_first>
<action>
Create `src/agents/alerter/test/confirm/integration.test.js`. Single top-level `describe('Phase 39 integration', ...)` block. For each scenario, build a fresh in-memory pool (seeded with a clean signal_draft row + signal_draft_event empty), a mocked signalClient (`{ send: spy }`), a mocked extractor (only used in scenarios 4/5/9), a mock clock, and a real-module composition for everything else (confirmDb, parser, state-machine, preview, outbound, edit-handler, watchdog, receive-loop).

Scenarios:

1. **YES happy path (CONF-01, CONF-02)** -- seed `awaiting_farmer` row from `fixtures/curated/seeding-clean.json`; deliver envelope with `text: 'YES'`; assert: pool row status='confirmed', confirmed_at non-null, one event row (event='yes'), signalClient.send called once with target=sender_e164 and body containing 'Locked in'.

2. **NO discard (CONF-03)** -- seed clean row; envelope `'NO'`; assert: status='discarded', discarded_at non-null, event='no', send called once with body containing 'Discarded'. Original `signal_capture` rows for the source captures are untouched (CONF-03's audit trail requirement; assert via pool spy that no DELETE / UPDATE ran on capture-store tables).

3. **Duplicate YES no-op (CONF-02)** -- seed row in status='confirmed' (already terminal); deliver `'YES'`; assert: confirmDraft returns rowCount=0, NO new event row inserted, signalClient.send called once with body containing 'Already locked in' (the soft re-affirm, D-02a).

4. **EDIT once -> re-extract -> preview re-render (CONF-04)** -- seed `awaiting_farmer` row at edit_turn_count=0; mock extractor to return a revised draft; deliver `'EDIT change qty to 12'`; assert: pool row edit_turn_count=1, draft_json updated, farmer_facing_preview updated, event='edit' with payload.ok=true, signalClient.send called once with body matching the new preview (containing the reply-instruction suffix).

5. **EDIT 3 times then cap (CONF-04)** -- seed row; deliver 3 EDIT replies; mock extractor to return progressively-revised drafts; on the 4th EDIT, assert: pool row status='needs_review', terminal_reason='edit_cap_exceeded', event='edit_cap_exceeded', signalClient.send body contains the cap message naming "3 tries".

6. **Nudge fires at 0.8*timeout (CONF-05)** -- seed `awaiting_farmer` row with updated_at = now - 25min (>0.8*30 = 24); call watchdog.tickOnce() once; assert: nudge_sent_at set, event='nudge_sent', signalClient.send called once with body matching the nudge template AND containing remaining-minutes as a whole integer (e.g. '5 min').

7. **Expire fires at full timeout (CONF-05)** -- seed `awaiting_farmer` row with nudge_sent_at = now - 10min, updated_at = now - 31min; tickOnce(); assert: status='expired', expired_at non-null, terminal_reason='timeout_expired', event='expired', signalClient.send called once with body containing 'Draft expired' AND 'Nothing was written'.

8. **Superseded-by-newer-draft** -- seed row A (awaiting_farmer, sender='+15550001234'); insert a hypothetical row B (also awaiting_farmer, same sender, newer updated_at -- bypassing the partial unique index in the test by inserting directly via the fake pool); deliver `'YES'` from sender; assert: parser/receive-loop confirms B (latest), and the test setup should also exercise the `confirmDb.expireDraft(A.id, 'superseded_by_newer_draft')` path -- which is normally called by the EDIT handler/state-machine when superseding is detected. Even if the live code path doesn't auto-supersede on a YES (because the partial-unique-index makes this impossible in prod), the test exists to lock the SUPERSEDED event behavior for any future code that calls it directly.

9. **Real-prod fixture ship-gate (D-09a)** -- load `fixtures/prod-draft-awaiting.json` directly into the pool's signal_draft table. Run THREE sub-scenarios:
   - 9a YES -> confirmed (assert: matches scenario 1's invariants, just with real-data preview body)
   - 9b NO -> discarded (matches scenario 2)
   - 9c EDIT (mocked extractor return) -> preview re-rendered + edit_turn_count=1 (matches scenario 4)
   This is the ship-gate witness: if scenarios 1-8 PASS but 9 fails, ship is BLOCKED. Document this in 39-EVAL-REPORT.md.

Each scenario is one `it(...)` block, no shared mutable state between scenarios (rebuild pool + spies in `beforeEach`). Total: 9 it-blocks + 3 sub-asserts inside 9 = 11 effective scenarios.

Run with the alerter's test command; assert exit 0.
</action>
<acceptance_criteria>
- Test file exists with >= 9 it-blocks (scenario 9 may be one block with 3 sub-asserts, OR 3 sub-blocks -- either is acceptable; if 3 sub-blocks, total >= 11)
- Scenario 9 loads `fixtures/prod-draft-awaiting.json` -- assert via `grep -c "prod-draft-awaiting" src/agents/alerter/test/confirm/integration.test.js` >= 1
- Scenarios 1-7 each issue at least one assertion on `signalClient.send.mock.calls[0]` (or equivalent spy access) verifying body content
- Scenarios 4, 5, 9c assert mock extractor calls (e.g. `mockExtractor.extract.mock.calls.length === 1` for scenario 4)
- Scenarios 6, 7 invoke `watchdog.tickOnce()` directly (no real setInterval) to keep the test deterministic
- Test exits 0
- The full alerter test suite still exits 0
</acceptance_criteria>
</task>

<task id="3">
<title>39-RUNBOOK.md -- operator-driven live UAT script</title>
<read_first>
- .planning/phases/25-bidirectional-signal-capture/*-RUNBOOK.md (if one exists) OR the closest analog from a Phase that shipped to a live farmer (Phase 27, 28, 30, 31, 33 -- look for the runbook pattern)
- .planning/phases/39-farmer-confirmation-loop/39-CONTEXT.md (success criteria + style locks)
- 39-RESEARCH.md section 11 (validation dimensions)
</read_first>
<action>
Create `.planning/phases/39-farmer-confirmation-loop/39-RUNBOOK.md`. Sections:

**Header:** Phase 39 LIVE UAT runbook. Date / operator / target sender (Don Santiago's own phone for the dry-run; farmers when promoted).

**Preflight (the project memory `feedback_fc1_remote_action_preflight_protocol` mindset, adapted for alerter changes):**
- alerter container running on elder-plops with `docker compose ps mushy-alerter` -> healthy
- The 4 new env vars set or defaulted (`docker compose exec mushy-alerter env | grep -E 'DRAFT_PENDING|DRAFT_NUDGE|DRAFT_WATCHDOG|MAX_EDIT'`)
- signal_draft has the new columns: `psql -c '\\d signal_draft' | grep -E 'edit_turn_count|nudge_sent_at|confirmed_at|discarded_at|expired_at|terminal_reason'`
- signal_draft_event table exists: `psql -c '\\d signal_draft_event'`

**Scenarios** (one numbered subsection per criterion):

1. **YES happy path** (CONF-01, CONF-02): Send a clean seeding message via Signal. Wait for preview. Reply YES. Expect: "Locked in. Writing now. (draft <truncId>)". Validate: `SELECT status, confirmed_at, terminal_reason FROM signal_draft WHERE id='<id>'` returns `confirmed | <ts> | farmer_yes`. Phase 40 farmOS write should happen if Phase 40 is also live; otherwise the row sits at status=confirmed.

2. **Duplicate YES no-op** (CONF-02): Reply YES again to the same preview. Expect: "Already locked in. Check the previous message." Validate: zero new rows in signal_draft_event for this draft (only the original 'yes' event).

3. **NO discard** (CONF-03): Send a fresh message. Reply NO. Expect: "Discarded. Nothing written." Validate: `SELECT status FROM signal_draft WHERE id='<id>'` -> `discarded`. Original Signal message text still visible in `signal_capture` table.

4. **EDIT loop** (CONF-04): Send a message with intentional ambiguity (e.g. wrong qty). Reply `EDIT qty was 12 not 7`. Expect: new preview with qty=12. Reply `EDIT actually species is OYS`. Expect: new preview with species changed. Reply `EDIT timestamp was 10am not 2pm`. Expect: new preview. Reply with a 4th EDIT. Expect: cap message naming "3 tries". Validate: `SELECT edit_turn_count, status FROM signal_draft WHERE id='<id>'` -> `3 | needs_review`.

5. **Timeout + nudge** (CONF-05): Send a fresh message. Wait 24 minutes (or set `DRAFT_PENDING_TIMEOUT_MIN=5` temporarily in the alerter env for a 4-min nudge dry-run). Do not reply. Expect: nudge message after 0.8*timeout. Wait another 0.2*timeout. Expect: "Draft expired. Nothing was written." Validate: `SELECT status, nudge_sent_at, expired_at FROM signal_draft WHERE id='<id>'` -> `expired | <ts> | <ts>`. Validate: NO farmOS write occurred (CONF-05 critical: "Stale drafts never auto-commit").

**Observability checklist:**
- `docker compose logs mushy-alerter --tail 200 | grep -E '\\[receive\\]|\\[watchdog\\]'` should show one log line per state transition.
- `SELECT event, count(*) FROM signal_draft_event GROUP BY event` should reflect the UAT activity.

**Rollback:**
- `docker compose down mushy-alerter; git revert <merge commit>; docker compose up -d mushy-alerter` returns the alerter to pre-Phase-39 behavior. Schema columns remain (idempotent, harmless on rollback); audit table remains.

**Sign-off:** Operator name + date when all 5 criteria pass live.
</action>
<acceptance_criteria>
- File exists
- All 5 CONF requirements appear in the scenarios (`grep -c "CONF-0" .planning/phases/39-farmer-confirmation-loop/39-RUNBOOK.md` >= 5)
- Preflight has at least 4 bullets covering container state + env vars + schema columns + audit table
- Rollback section names the specific revert command + docker compose sequence
- No em-dashes in the runbook (`grep -c $'—' .planning/phases/39-farmer-confirmation-loop/39-RUNBOOK.md` == 0)
</acceptance_criteria>
</task>

<task id="4">
<title>39-EVAL-REPORT.md -- pass/fail evidence with on-disk artifact paths</title>
<read_first>
- .planning/phases/38-extraction-pipeline/38-EVAL-REPORT-plan09-run2-PASS.md (the Phase 38 closeout-eval format -- match this shape)
- 39-RESEARCH.md section 11 (Nyquist dimensions to report against)
- Memory rule feedback_keep_paper_trail_of_intermediates: each run produces a named artifact, never overwritten
</read_first>
<action>
Create `.planning/phases/39-farmer-confirmation-loop/39-EVAL-REPORT.md`. Sections:

**Header:** date, plan reference (39-07), commit sha (placeholder TBD-on-commit), verdict block: PASS / FAIL / PARTIAL.

**Scenario summary table:**

| # | Scenario | Requirements | Status | Evidence path |
|---|----------|--------------|--------|---------------|
| 1 | YES happy path | CONF-01, CONF-02 | PASS | src/agents/alerter/test/confirm/integration.test.js#it('YES happy path') |
| 2 | NO discard | CONF-03 | PASS | ... |
| ... | ... | ... | ... | ... |
| 9 | **Real-prod fixture (ship gate)** | CONF-01..05 | PASS | src/agents/alerter/test/confirm/fixtures/prod-draft-awaiting.json + integration.test.js#it('prod-draft-awaiting') |

Status values: PASS / FAIL / DEFERRED. PASS requires at-the-time-of-commit `node <test-runner> exit 0` and the existence of the listed evidence file on disk.

**Nyquist coverage:** one table row per validation dimension from 39-RESEARCH.md section 11, with "satisfied by scenario N" or "deferred".

**Deferred / known-limitations:**
- Live-farmer UAT (39-RUNBOOK.md) -- operator-deferred per Phase 25/37 pattern.
- Group-thread confirm replies (D-06a) -- locked to DM-only for v1.7; revisit in 999.20 sub-part (b).
- Live LLM smoke for EDIT loop -- not run as part of this plan (integration test mocks the extractor). Memory rule `feedback_persist_paid_results_default`: if the operator runs a live smoke, results MUST land in a per-call unique JSONL path under `.planning/phases/39-farmer-confirmation-loop/eval-smoke/<date>-<n>.jsonl` (not the plan's main eval report).

**Closing checklist** (for the verifier):
- [ ] All 9 scenarios PASS at the linked evidence path
- [ ] Fixture `prod-draft-awaiting.json` has a valid `_provenance` block
- [ ] No em-dashes in any Phase 39 artifact (`find .planning/phases/39-farmer-confirmation-loop -name '*.md' -exec grep -l $'—' {} +` returns empty)
- [ ] No em-dashes in any Phase 39 source (`grep -rl $'—' src/agents/alerter/src/confirm/` returns empty)
- [ ] docker-compose.override.yml has all 4 new env vars (per Plan 02)
- [ ] schema_invalid is exercised by at least one integration test
- [ ] real-prod fixture is loaded by at least one integration test (`grep -c prod-draft-awaiting src/agents/alerter/test/confirm/integration.test.js` >= 1)
</action>
<acceptance_criteria>
- File exists
- Scenario summary table has >= 9 rows
- Each row's "Evidence path" is a concrete file path that exists on disk (verified by parsing the table and stat-checking each path)
- Closing checklist has at least 7 line items
- All checkboxes start UNCHECKED -- the verifier marks them
- Verdict block names PASS / FAIL / PARTIAL (placeholder TBD until the executor fills it after the test suite runs)
</acceptance_criteria>
</task>
</tasks>

<verification>
1. `test -f src/agents/alerter/test/confirm/fixtures/prod-draft-awaiting.json && node -e "const f=require('./src/agents/alerter/test/confirm/fixtures/prod-draft-awaiting.json'); if(f.status!=='awaiting_farmer'||!f._provenance) process.exit(1)"` exits 0
2. The integration test file exits 0 against the wired alerter
3. The full alerter test suite exits 0 (no regressions)
4. `grep -c prod-draft-awaiting src/agents/alerter/test/confirm/integration.test.js` >= 1 (real-prod fixture exercised)
5. `test -f .planning/phases/39-farmer-confirmation-loop/39-RUNBOOK.md && test -f .planning/phases/39-farmer-confirmation-loop/39-EVAL-REPORT.md`
6. `grep -l $'—' .planning/phases/39-farmer-confirmation-loop/*.md src/agents/alerter/src/confirm/*.js 2>/dev/null | wc -l` returns 0 (no em-dashes anywhere in Phase 39 artifacts or source)
7. The 9-scenario eval table in 39-EVAL-REPORT.md references CONF-01..CONF-05 across the rows
</verification>
