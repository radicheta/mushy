---
phase: 39-farmer-confirmation-loop
plan: 02
type: execute
wave: 0
depends_on: []
files_modified:
  - src/agents/alerter/src/config.js
  - docker-compose.override.yml
  - src/agents/alerter/test/config.test.js
autonomous: true
requirements: [CONF-04, CONF-05]
must_haves:
  truths:
    - "Four new env-driven knobs read from config.js: DRAFT_PENDING_TIMEOUT_MIN (30), DRAFT_NUDGE_FRACTION (0.8), DRAFT_WATCHDOG_INTERVAL_MS (60000), MAX_EDIT_TURNS (3)"
    - "Each new env var is listed in docker-compose.override.yml mushy-alerter environment block as `- X=${X:-default}` (per memory feedback_compose_env_passthrough_not_envfile)"
    - "config.load() returns frozen object with the four new fields with the documented defaults when env is unset"
    - "DRAFT_NUDGE_FRACTION is clamped to (0, 1) -- any value outside that range falls back to 0.8 with a logger warn (matches existing clampThreshold pattern)"
  artifacts:
    - path: "src/agents/alerter/src/config.js"
      provides: "config.draftPendingTimeoutMin, config.draftNudgeFraction, config.draftWatchdogIntervalMs, config.maxEditTurns"
    - path: "docker-compose.override.yml"
      provides: "compose-level env passthrough for the four knobs"
    - path: "src/agents/alerter/test/config.test.js"
      provides: "unit test asserting defaults + override behavior for all four"
  key_links:
    - from: "docker-compose.override.yml"
      to: "src/agents/alerter/src/config.js"
      via: "environment: passthrough"
      pattern: "DRAFT_PENDING_TIMEOUT_MIN"
---

<objective>
Wire the four runtime knobs Phase 39 needs (`DRAFT_PENDING_TIMEOUT_MIN`, `DRAFT_NUDGE_FRACTION`, `DRAFT_WATCHDOG_INTERVAL_MS`, `MAX_EDIT_TURNS`) into both code (`config.js`) and the running container (`docker-compose.override.yml`). Memory rule: shipping the env reader in code without the matching compose passthrough is the same trap that swallowed `ALERT_SHT30_ENABLED=false` on 2026-05-12 -- the alerter never sees the value. Both halves ship in the same plan.

This is Wave 0 because the watchdog (Plan 06) and the EDIT cap (Plan 05) both depend on these knobs at construction time.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<tasks>
<task id="1">
<title>Add four env-driven knobs to config.js</title>
<read_first>
- src/agents/alerter/src/config.js (existing parseIntEnv / parseFloatEnv / clampThreshold patterns + how Phase 38 added maxAskbackTurns and draftIdleGapMin)
- .planning/phases/39-farmer-confirmation-loop/39-RESEARCH.md section 9 (env-var table)
- .planning/phases/39-farmer-confirmation-loop/39-CONTEXT.md D-03a, D-04, D-04a, D-04c
</read_first>
<action>
In `src/agents/alerter/src/config.js`, inside `load(env)`'s frozen-object return, add four entries near the existing Phase 38 entries (`draftIdleGapMin`, `maxAskbackTurns`):

- `draftPendingTimeoutMin: parseIntEnv(env, 'DRAFT_PENDING_TIMEOUT_MIN', 30)`
- `draftNudgeFraction: clampFraction(parseFloatEnv(env, 'DRAFT_NUDGE_FRACTION', 0.8))` -- where `clampFraction(f)` is a new tiny helper at the top of the file: returns `f` if `0 < f < 1`, otherwise returns 0.8 and `console.warn('[config] DRAFT_NUDGE_FRACTION out of range, using 0.8')`. (Match the existing `clampThreshold` style.)
- `draftWatchdogIntervalMs: parseIntEnv(env, 'DRAFT_WATCHDOG_INTERVAL_MS', 60000)`
- `maxEditTurns: parseIntEnv(env, 'MAX_EDIT_TURNS', 3)`

Add a header-block comment near the new lines: `// Phase 39 (D-03a, D-04..D-04c): confirm-loop timeouts + edit cap. See docker-compose.override.yml for matching env passthrough.`
</action>
<acceptance_criteria>
- `node -e "const c=require('./src/agents/alerter/src/config.js'); const r=c.load({SIGNAL_SENDER:'+1',SIGNAL_RECIPIENT:'+1'}); console.log(r.draftPendingTimeoutMin, r.draftNudgeFraction, r.draftWatchdogIntervalMs, r.maxEditTurns)"` prints `30 0.8 60000 3`
- Override test: `node -e "const c=require('./src/agents/alerter/src/config.js'); const r=c.load({SIGNAL_SENDER:'+1',SIGNAL_RECIPIENT:'+1',DRAFT_PENDING_TIMEOUT_MIN:'45',MAX_EDIT_TURNS:'5'}); console.log(r.draftPendingTimeoutMin, r.maxEditTurns)"` prints `45 5`
- clampFraction warns + falls back: setting `DRAFT_NUDGE_FRACTION=1.5` returns 0.8
- New comment block referencing D-03a + D-04c present
</acceptance_criteria>
</task>

<task id="2">
<title>Add matching env passthrough to docker-compose.override.yml</title>
<read_first>
- docker-compose.override.yml (mushy-alerter `environment:` block; locate where existing knobs like MAX_ASKBACK_TURNS or DRAFT_IDLE_GAP_MIN are listed)
- .planning/phases/39-farmer-confirmation-loop/39-CONTEXT.md (canonical_refs: feedback_compose_env_passthrough_not_envfile.md)
</read_first>
<action>
Inside the mushy-alerter (or whatever the alerter service is named in `docker-compose.override.yml`) `environment:` list, add four lines using the project's existing shape `- KEY=${KEY:-default}`:

```yaml
      - DRAFT_PENDING_TIMEOUT_MIN=${DRAFT_PENDING_TIMEOUT_MIN:-30}
      - DRAFT_NUDGE_FRACTION=${DRAFT_NUDGE_FRACTION:-0.8}
      - DRAFT_WATCHDOG_INTERVAL_MS=${DRAFT_WATCHDOG_INTERVAL_MS:-60000}
      - MAX_EDIT_TURNS=${MAX_EDIT_TURNS:-3}
```

Place them next to the existing Phase 38 entries (`DRAFT_IDLE_GAP_MIN`, `MAX_ASKBACK_TURNS`) so reviewers can see the loop knobs grouped. Defaults MUST match config.js defaults exactly.
</action>
<acceptance_criteria>
- `grep -c "DRAFT_PENDING_TIMEOUT_MIN" docker-compose.override.yml` >= 1
- `grep -c "DRAFT_NUDGE_FRACTION" docker-compose.override.yml` >= 1
- `grep -c "DRAFT_WATCHDOG_INTERVAL_MS" docker-compose.override.yml` >= 1
- `grep -c "MAX_EDIT_TURNS" docker-compose.override.yml` >= 1
- All four lines use the `${KEY:-default}` form (so leaving them out of `.env` is safe)
- `docker compose config` exits 0 (YAML is valid -- run after edits)
</acceptance_criteria>
</task>

<task id="3">
<title>Unit test the new config knobs</title>
<read_first>
- src/agents/alerter/test/config.test.js if exists, else look at any existing test against config.js (e.g. test/maxSendsPerHour or similar)
- src/agents/alerter/src/config.js (post-task-1 state)
</read_first>
<action>
Add (or extend) `src/agents/alerter/test/config.test.js` with assertions:

1. `it('Phase 39 defaults', ...)` -- `config.load({SIGNAL_SENDER:'+x',SIGNAL_RECIPIENT:'+y'})` returns `{draftPendingTimeoutMin:30, draftNudgeFraction:0.8, draftWatchdogIntervalMs:60000, maxEditTurns:3}`
2. `it('Phase 39 env overrides', ...)` -- with all four env vars set, returned object reflects the set values (parse as int/float as appropriate)
3. `it('DRAFT_NUDGE_FRACTION clamps out-of-range to 0.8', ...)` -- inputs `'-0.1'`, `'0'`, `'1'`, `'1.5'`, `'abc'` (abc rejected by parseFloatEnv -- throws -- so this case is omitted; the float-validity check is upstream) all yield 0.8. Use stubs to silence console.warn.
4. `it('returned object is frozen', ...)` -- attempting to mutate `r.maxEditTurns = 99` throws in strict mode.

Run with `pnpm --filter mushy-alerter test -- test/config.test.js` and confirm exit 0.
</action>
<acceptance_criteria>
- Test file contains all four new it-blocks (`grep -c "Phase 39" src/agents/alerter/test/config.test.js` >= 1 + 3 more relevant cases)
- Test exit code 0
- The clamp test explicitly covers at least two out-of-range values
</acceptance_criteria>
</task>
</tasks>

<verification>
1. `node -e "const c=require('./src/agents/alerter/src/config.js'); const r=c.load({SIGNAL_SENDER:'+x',SIGNAL_RECIPIENT:'+y'}); console.log(JSON.stringify({t:r.draftPendingTimeoutMin,f:r.draftNudgeFraction,i:r.draftWatchdogIntervalMs,m:r.maxEditTurns}))"` prints `{"t":30,"f":0.8,"i":60000,"m":3}`
2. `grep -E "DRAFT_PENDING_TIMEOUT_MIN|DRAFT_NUDGE_FRACTION|DRAFT_WATCHDOG_INTERVAL_MS|MAX_EDIT_TURNS" docker-compose.override.yml | wc -l` returns 4 (one per knob)
3. `docker compose config -q` exits 0 (validates compose YAML)
4. Alerter config test exits 0
</verification>
