---
phase: 54.2-strain-detection-trigger
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/agents/alerter/src/index.js
  - src/agents/alerter/src/extraction/outbound.js
  - src/agents/alerter/test/farmos/startup-wiring.test.js
  - src/agents/alerter/test/extraction/outbound.test.js
autonomous: true
requirements: [STRAIN-08]

must_haves:
  truths:
    - "createExtractionPipeline receives a farmosClient (or null when farmOS creds absent)"
    - "The extraction dispatcher can route send_strain_ask_back without throwing unknown_side_effect"
    - "A source-level test fails if index.js stops forwarding farmosClient into the pipeline"
    - "A source-level test fails if extraction/outbound.js loses the send_strain_ask_back case"
  artifacts:
    - path: "src/agents/alerter/src/index.js"
      provides: "Lifted farmosClient construction injected into createExtractionPipeline"
      contains: "farmosClient,"
    - path: "src/agents/alerter/src/extraction/outbound.js"
      provides: "send_strain_ask_back dispatch case routing through sendAskBack"
      contains: "send_strain_ask_back"
    - path: "src/agents/alerter/test/farmos/startup-wiring.test.js"
      provides: "Source-level guard: index.js forwards farmosClient into createExtractionPipeline"
      contains: "createExtractionPipeline"
  key_links:
    - from: "src/agents/alerter/src/index.js"
      to: "createExtractionPipeline"
      via: "farmosClient injection in the construction object"
      pattern: "createExtractionPipeline\\(\\{[\\s\\S]*?farmosClient"
    - from: "src/agents/alerter/src/extraction/pipeline.js (Wave 2 consumer)"
      to: "src/agents/alerter/src/extraction/outbound.js"
      via: "dispatch('send_strain_ask_back', draftRow)"
      pattern: "case 'send_strain_ask_back'"
---

<objective>
Wire the two foundational seams the Wave 2 strain-detection gate depends on:
1. Inject a farmOS client into the extraction pipeline (it has none today).
2. Add the `send_strain_ask_back` case to the EXTRACTION dispatcher (the case
   currently lives only in the confirm dispatcher, so a pipeline dispatch would
   throw `unknown side_effect` — the `eb8332b` bug class).

Each seam ships with a source-level wiring guard (STRAIN-08) because injected-fake
unit tests cannot catch a dropped DI wire or a missing dispatch case — this is the
exact failure that bit Phase 54.1 and the 2026-05-30 dispatch bug.

Purpose: The Wave 2 gate calls `getFungiTypeUuid(farmosClient, code)` and dispatches
`send_strain_ask_back` from the pipeline. Both are inert until these seams exist.
Output: A pipeline that receives farmosClient; an extraction dispatcher that routes
the strain ask-back; two source-level guards.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/54.2-strain-detection-trigger/54.2-CONTEXT.md
@.planning/phases/54.2-strain-detection-trigger/54.2-RESEARCH.md
@.planning/phases/54.2-strain-detection-trigger/54.2-PATTERNS.md

<interfaces>
<!-- Confirmed by reading source. Executor should use these directly. -->

index.js pipeline construction (index.js:161-171), no farmosClient today:
  createExtractionPipeline({ pool, extractor, extractionDb, stateMachine,
    previewBuilder, config, logger, clock, outboundDispatcher })

index.js existing farmosClient construction, currently scoped INSIDE the
commit-watchdog `if (config.farmosUsername && config.farmosPassword)` block
(index.js:330-338) — must be LIFTED above :161 and reused:
  const farmosClient = farmos.createFarmosClient({ farmosUrl, username,
    password, logger, backoffMs, retryMax });

index.js graceful-degrade precedent (index.js:366-367):
  logger.warn('[commit-watchdog] disabled: farmOS credentials missing');

extraction/outbound.js dispatch switch (outbound.js ~:120-140), 2-arg
`dispatch(sideEffect, draftRow)`; sendAskBack(draftRow) (outbound.js:77-90)
sanitizes + sends draftRow.farmer_facing_preview. The send_starting_seq_askback
case routes through sendAskBack unchanged.

startup-wiring.test.js precedent (:126-140): fs.readFileSync(index.js) +
regex match on createReceiveLoop({...}) forwarding extractionDb.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Lift and inject farmosClient into the extraction pipeline</name>
  <files>src/agents/alerter/src/index.js</files>
  <read_first>
    - src/agents/alerter/src/index.js (the pipeline construction at :161-171 and the existing farmosClient construction at :330-338, plus the graceful-degrade WARN at :366-367)
    - .planning/phases/54.2-strain-detection-trigger/54.2-PATTERNS.md (the `src/index.js — lift + inject farmosClient` section: exact construction + injection excerpts)
    - .planning/phases/54.2-strain-detection-trigger/54.2-RESEARCH.md (`## Wiring task: the pipeline has no farmOS client` — the load-bearing restructure)
  </read_first>
  <action>
    In src/index.js, lift the `farmos.createFarmosClient({...})` construction (currently
    at index.js:330-338, scoped inside the commit-watchdog `if (config.farmosUsername &&
    config.farmosPassword)` block) to ABOVE the `createExtractionPipeline` call at :161,
    into a `let farmosClient = null;` assigned only when the same credentials check passes.
    Add `farmosClient,` to the `createExtractionPipeline({...})` construction object.
    Reuse the SAME instance in the commit-watchdog block (do not construct twice — remove
    the now-redundant inner construction and reference the lifted `farmosClient`). When
    credentials are absent, `farmosClient` stays null and is forwarded as null (the Wave 2
    gate guards `if (farmosClient)` and skips, mirroring the existing commit-watchdog WARN
    at index.js:367). Do not change any farmosClient construction options.
  </action>
  <verify>
    <automated>cd src/agents/alerter && npx jest test/farmos/startup-wiring.test.js</automated>
  </verify>
  <acceptance_criteria>
    - src/index.js constructs farmosClient at most once (grep for `createFarmosClient` returns a single call site)
    - The createExtractionPipeline({...}) object literal in index.js contains a `farmosClient` key
    - When farmOS credentials are absent, farmosClient is null and forwarded (no throw)
    - `npx jest test/farmos/startup-wiring.test.js` exits 0
  </acceptance_criteria>
  <done>farmosClient is constructed once, lifted above the pipeline, and injected into createExtractionPipeline; commit-watchdog reuses the same instance.</done>
</task>

<task type="auto">
  <name>Task 2: Add send_strain_ask_back to the extraction dispatcher (Pitfall 5 seam)</name>
  <files>src/agents/alerter/src/extraction/outbound.js</files>
  <read_first>
    - src/agents/alerter/src/extraction/outbound.js (the dispatch switch and the `send_starting_seq_askback` case; sendAskBack at :77-90 reads draftRow.farmer_facing_preview)
    - .planning/phases/54.2-strain-detection-trigger/54.2-PATTERNS.md (the `src/extraction/outbound.js — send_strain_ask_back dispatch case` section — recommends Option (b): pipeline pre-renders the body into farmer_facing_preview, this case routes through sendAskBack unchanged, smaller diff than widening to 3-arg)
    - src/agents/alerter/src/confirm/outbound-confirm.js:283 (the OTHER dispatcher's strain case — payload-semantics reference only; NOT the path the pipeline uses)
  </read_first>
  <action>
    In src/extraction/outbound.js's dispatch switch, add a `case 'send_strain_ask_back':`
    that returns `await sendAskBack(draftRow || {})` — mirroring the `send_starting_seq_askback`
    case exactly (Option (b) from PATTERNS.md). Do NOT widen the dispatcher to a 3-arg
    `(sideEffect, draftRow, extras)` form; the Wave 2 pipeline pre-renders the batched
    ask-back body into `draftRow.farmer_facing_preview`, which `sendAskBack` already
    sanitizes and sends. This keeps the strain-render glue in the pipeline (where the
    batched multi-unknown body is composed) and the dispatcher diff minimal.
  </action>
  <verify>
    <automated>cd src/agents/alerter && npx jest test/extraction/outbound.test.js</automated>
  </verify>
  <acceptance_criteria>
    - src/extraction/outbound.js contains a `case 'send_strain_ask_back'` that routes through sendAskBack
    - Dispatching send_strain_ask_back with a draftRow carrying farmer_facing_preview does NOT return reason 'unknown_side_effect'
    - The dispatcher signature is unchanged (still 2-arg `dispatch(sideEffect, draftRow)`)
    - `npx jest test/extraction/outbound.test.js` exits 0
  </acceptance_criteria>
  <done>The extraction dispatcher routes send_strain_ask_back through sendAskBack; a pipeline dispatch of that side-effect no longer throws unknown_side_effect.</done>
</task>

<task type="auto">
  <name>Task 3: Source-level wiring guards for both seams (STRAIN-08)</name>
  <files>src/agents/alerter/test/farmos/startup-wiring.test.js, src/agents/alerter/test/extraction/outbound.test.js</files>
  <read_first>
    - src/agents/alerter/test/farmos/startup-wiring.test.js (the :126-140 precedent — fs.readFileSync(index.js) + regex on createReceiveLoop forwarding extractionDb)
    - src/agents/alerter/test/extraction/outbound.test.js (existing dispatcher tests, for the dispatch-case guard style)
    - .planning/phases/54.2-strain-detection-trigger/54.2-PATTERNS.md (the `test/farmos/startup-wiring.test.js` section — the exact STRAIN-08 regex shape for forwards-farmosClient)
  </read_first>
  <action>
    In test/farmos/startup-wiring.test.js, add a guard mirroring the :136 precedent:
    fs-read src/index.js, match `createExtractionPipeline\(\{([\s\S]*?)\}\)` and assert the
    captured object body matches `/(^|[\s,{])farmosClient\s*[,}]/`. Add a second guard
    (here or in test/extraction/outbound.test.js) that fs-reads src/extraction/outbound.js
    text and asserts it contains a `send_strain_ask_back` case (regex `/case\s+['"]send_strain_ask_back['"]/`).
    Both are source-text guards (fs-read + regex), NOT injected-fake unit tests — they catch
    the DI-drop / missing-case class that injected fakes pass green on.
  </action>
  <verify>
    <automated>cd src/agents/alerter && npx jest test/farmos/startup-wiring.test.js test/extraction/outbound.test.js</automated>
  </verify>
  <acceptance_criteria>
    - test/farmos/startup-wiring.test.js asserts index.js forwards farmosClient into createExtractionPipeline (fs-read + regex)
    - A source-level guard asserts extraction/outbound.js contains a send_strain_ask_back case
    - Reverting Task 1's injection makes the first guard fail; reverting Task 2's case makes the second guard fail
    - `npx jest test/farmos/startup-wiring.test.js test/extraction/outbound.test.js` exits 0
  </acceptance_criteria>
  <done>Two source-level guards exist and fail if either seam is removed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| alerter → farmOS API | farmOS taxonomy responses are consumed to decide draft existence/hold. Not attacker-controlled in this farm context, but availability matters. |
| farmer Signal message → alerter | Farmer-trusted input; no new surface added by this plan (pure DI/dispatch wiring). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-54.2-01 | Denial of Service | farmosClient construction at boot | accept | If farmOS creds absent, farmosClient is null and the Wave 2 gate skips with a WARN (graceful degrade, mirrors index.js:367). Boot never crashes. |
| T-54.2-02 | Tampering | extraction dispatcher routing | mitigate | send_strain_ask_back routes through the existing audited sendAskBack path; no new send surface or auth bypass. Source-level guard (Task 3) prevents silent loss of the case. |
| T-54.2-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan; no new dependencies. RESEARCH.md confirms "No npm install — Package Legitimacy Audit not applicable." |
</threat_model>

<verification>
- `cd src/agents/alerter && npx jest test/farmos/startup-wiring.test.js test/extraction/outbound.test.js` green.
- Full suite remains green: `cd src/agents/alerter && npx jest` (baseline 1232 pass / 9 skipped / 0 fail).
- Manual grep: `grep -c createFarmosClient src/agents/alerter/src/index.js` returns 1 (single construction).
</verification>

<success_criteria>
- createExtractionPipeline receives farmosClient (null-safe when creds absent).
- The extraction dispatcher routes send_strain_ask_back through sendAskBack.
- Two source-level guards fail if either seam regresses.
- Full alerter suite green.
</success_criteria>

<output>
Create `.planning/phases/54.2-strain-detection-trigger/54.2-01-SUMMARY.md` when done.
</output>
