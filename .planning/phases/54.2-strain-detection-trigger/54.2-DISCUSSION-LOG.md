# Phase 54.2 Discussion Log

**Session:** 2026-06-01 (update of existing 2026-05-30 CONTEXT.md)

## Areas presented

Q1 insert point / Q2 persistence (STRAIN-07) / Q3 multi-group remap / Q4 gate ordering.

## What happened

Santi did not pick from the four options. He challenged the framing directly:
**"this all seems way too complicated. looks like we're having too many sources
of truth?"**

That challenge landed on Q2. Three lists were competing to define "what strains
exist": `strains.yaml`/`config.strains`, farmOS `fungi_type` taxonomy, and a
proposed new timescale `strain_codes` table. The old decision #1 ("match curated
set, not live farmOS terms") is what forced the third source into existence.

### Decision: collapse to ONE source of truth
- **farmOS taxonomy is authoritative for strain existence** (reverses old
  decision #1). Resolver existence-checks via the existing
  `getFungiTypeUuid(client, code)`. No new DB table; STRAIN-07 dissolves.
- `strains.yaml` demoted to extraction-vocab-prior only (one list, one job).
- Cost accepted: 4 bogus dev terms must be cleaned so they don't suppress the
  ask in dev (prod is clean).

### Follow-up instruction from Santi
> "on yes, just push the thing and implement UPSERT for strains"

- On farmer YES: upsert the term (existing `ensureFungiTypeUuid{create:true}` --
  already built/wired) + commit the draft. No new persistence layer. The phase
  only needs to TRIGGER the already-built upsert.

## Deferred to research / planning (not user gray areas)
- R1: gate ordering vs `starting_seq` + verify SEQ-reply re-entry (avoid
  committing a draft missing either SEQ or strain-confirm).
- R2: multi-group CORRECTION remap (YES-path multi-group already works).

## Deferred ideas
None raised.
