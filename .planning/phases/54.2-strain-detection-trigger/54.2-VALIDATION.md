---
phase: 54.2
slug: strain-detection-trigger
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-06-01
---

# Phase 54.2 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.
> Authoritative test design lives in 54.2-RESEARCH.md `## Validation Architecture`.
> The planner fills the Per-Task Verification Map once plans exist.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | jest (alerter test suite) |
| **Config file** | `src/agents/alerter/package.json` / jest config |
| **Quick run command** | `cd src/agents/alerter && npx jest src/extraction src/farmos src/confirm` |
| **Full suite command** | `cd src/agents/alerter && npx jest` |
| **Estimated runtime** | ~30-60 seconds |

---

## Sampling Rate

- **After every task commit:** Run the quick run command for the touched module(s)
- **After every plan wave:** Run the full suite command
- **Before `/gsd:verify-work`:** Full suite must be green
- **Max feedback latency:** 60 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| TBD | TBD | TBD | STRAIN-05/06/08 | — | unknown code held, never silent-committed | unit + wiring | `npx jest` | ❌ W0 | ⬜ pending |

*Planner replaces TBD rows with concrete task IDs. Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] Strain-detection unit tests — `collectUnknownStrains` walks top-level `species_code` AND per-group `groups[].species.value`, returns DISTINCT unknowns (STRAIN-05)
- [ ] farmOS existence-gate unit test — clean `fungi_type_not_found` ⇒ ask; infra/HTTP error ⇒ pass-through, NO ask (Decision #2 / STRAIN-06)
- [ ] **Startup-wiring / source-level reachability test** — proves the detection gate is reachable from the live capture path AND that the pipeline's dispatcher can route `send_strain_ask_back` (guards the unwired-dispatcher class; precedent `startup-wiring.test.js:126`) (STRAIN-08, [[feedback_unit_tests_dont_catch_wiring]])
- [ ] Gate-ordering integration test — a `seeding_session` needing BOTH `starting_seq` and strain-confirm commits with both resolved, never one missing (R1)

*The reachability/wiring test is the load-bearing Nyquist requirement — unit-green in isolation is explicitly insufficient here (it's what bit 54.1 and the `eb8332b` dispatch bug).*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Live unknown-strain capture is held + farmer receives ask-back, then YES upserts term + commits | STRAIN-06/08 | End-to-end depends on live farmOS + Signal dispatch | DEV PREREQUISITE: admin-DELETE the 4 bogus dev `fungi_type` terms (LIM/SHIITAKE/OYS/CAR) first, else dev reads them as known and suppresses the ask. Then drive a capture with an unknown code; confirm hold + ask-back + YES→commit. |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references (esp. the reachability/wiring test)
- [ ] No watch-mode flags
- [ ] Feedback latency < 60s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
