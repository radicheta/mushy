---
phase: 49-real-session-eval-corpus-may-22-ship-gate-reprocess
plan: 04
type: execute
wave: 3
depends_on: ['49-01', '49-02', '49-03']
files_modified:
  - src/agents/alerter/test/eval/ingestion/fixtures/sessions/<third-session>/ground-truth.json
  - src/agents/alerter/test/eval/ingestion/fixtures/sessions/<third-session>/MANIFEST.md
  - src/agents/alerter/test/eval/ingestion/fixtures/sessions/<third-session>/audio.*
  - src/agents/alerter/test/eval/ingestion/fixtures/sessions/<third-session>/paper-log.jpg
  - src/agents/alerter/test/eval/ingestion/fixtures/sessions/<third-session>/mock-extraction.json
  - .planning/phases/49-real-session-eval-corpus-may-22-ship-gate-reprocess/49-SHIP-GATE.md
autonomous: false
requirements: [INOC-07]
must_haves:
  truths:
    - "Eval corpus contains 3+ named-regression sessions: 2026-05-22, 2026-05-12, and one additional real inoc session"
    - "Third session ground-truth.json validates against the seeding_session Zod schema (Plan 02's sessions.test.js picks it up automatically via sessions-loader)"
    - "49-SHIP-GATE.md exists as an operator-driven runbook mirroring 48-LIVE-FIRE.md shape, with ASCII-only commands and no em-dashes"
    - "Runbook covers: (1) sanity hermetic run, (2) discard the two failed May-22 production drafts via scripts/discard-drafts.js --apply, (3) reprocess May-22 audio+photo through new pipeline pointed at farmOS dev, (4) verify 11 logs + session asset land cleanly + lineage walk reconstructs the session, (5) attest INOC-07"
    - "Reprocess output JSONL writes to a UNIQUE path under /mnt/mossrock/shared/mushdatadump-prod/2026-05-22_inoc_santi_reprocess_v1.9/ (never overwrites the original failed run, per [[feedback_persist_paid_results_default]])"
    - "Runbook attestation 'Result' section is empty + ready for operator to fill (mirrors 48-LIVE-FIRE.md)"
  artifacts:
    - path: src/agents/alerter/test/eval/ingestion/fixtures/sessions/<third-session>/
      provides: "Third named-regression fixture, closing INOC-07's >=3 sessions bar"
    - path: .planning/phases/49-real-session-eval-corpus-may-22-ship-gate-reprocess/49-SHIP-GATE.md
      provides: "Operator-driven runbook for v1.9 ship gate"
  key_links:
    - from: 49-SHIP-GATE.md Step 2 (discard)
      to: src/agents/alerter/scripts/discard-drafts.js (from Plan 03)
      via: "node scripts/discard-drafts.js --uuid e3a564d063d4... --uuid 6edaaba7deb0... --reason '...' --apply"
      pattern: "discard-drafts.js"
    - from: 49-SHIP-GATE.md Step 3 (reprocess)
      to: src/agents/alerter/test/eval/ingestion/sessions.test.js (live-fire branch from Plan 02)
      via: "EVAL_RUN_LIVE=1 ANTHROPIC_API_KEY=... npx jest test/eval/ingestion/sessions.test.js -t '2026-05-22'"
      pattern: "EVAL_RUN_LIVE=1"
    - from: 49-SHIP-GATE.md Step 4 (verify)
      to: farmOS dev API
      via: "curl GET /api/asset/fungi + /api/log/seeding with bearer token"
      pattern: "/api/asset/fungi"
---

<objective>
Two complementary outputs in one plan: (1) add the third named-regression session to the corpus (per CONTEXT Gray Area F -- planner-selected at execution time) so INOC-07's ">=3 sessions" success criterion is met, and (2) ship `49-SHIP-GATE.md`, the operator-driven runbook that discards the two failed May-22 prod drafts and reprocesses the May-22 capture end-to-end through the new pipeline to farmOS dev as the v1.9 ship gate.

Why both in one plan: the third corpus session is a small artifact (mirrors Plan 01/02 shapes) and the ship-gate runbook is itself a document-only artifact (no code). Together they form the v1.9 ship-gate closure. Per CONTEXT Gray Area D, the ship-gate is operator-driven -- the runbook is the deliverable; the actual May-22 reprocess execution is the operator's responsibility post-merge, with results appended to the runbook's "Result" section.

Autonomous: false -- Task 2 is a `checkpoint:human-verify` for the runbook's structural review (operator sanity-checks the commands before execution); Task 3 is operator-deferred execution that lands as a future "Result" amendment.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/49-real-session-eval-corpus-may-22-ship-gate-reprocess/49-CONTEXT.md
@.planning/phases/49-real-session-eval-corpus-may-22-ship-gate-reprocess/49-01-SUMMARY.md
@.planning/phases/49-real-session-eval-corpus-may-22-ship-gate-reprocess/49-02-SUMMARY.md
@.planning/phases/49-real-session-eval-corpus-may-22-ship-gate-reprocess/49-03-SUMMARY.md
@.planning/phases/48-session-entity-per-bag-commit-fan-out-session-shaped-confirm/48-LIVE-FIRE.md
@.planning/phases/47-multi-source-extraction-fusion-groups-shape-inoc-draft/47-LIVE-FIRE.md
@src/agents/alerter/scripts/discard-drafts.js
@src/agents/alerter/test/eval/ingestion/sessions.test.js

<interfaces>
Third-session selection (CONTEXT Gray Area F):
  Primary candidates (operator-pickable at execution time):
    a) `/mnt/mossrock/shared/mushdatadump-prod/2026-05-13_backlog_unprocessed/` -- contents inspected during planning: contains harvest + observation captures, NO inoc session. RULE OUT this dir.
    b) The broader `/mnt/mossrock/shared/mushdatadump/` NFS corpus (project_phase38_production_logs_available) -- pick a smaller real inoc session whose hand labels are recoverable from a Phase 38 SUMMARY or the existing replay-output.txt in that capture dir.
    c) Any other inoc session that has surfaced in `/mnt/mossrock/shared/mushdatadump-prod/` between this plan being written and being executed.
  Selection criterion: must be a real inoc session (NOT a harvest / observation / squash); paired audio + paper-log photo preferred; hand labels recoverable; smaller cardinality acceptable (single-parent or 2-3 group counts are fine -- Gray Area F notes a smaller session is intentional to exercise a different shape than May-22).
  The executor SHOULD select option (b) by default: scan `/mnt/mossrock/shared/mushdatadump/` for an inoc-tagged session, then build the ground-truth from the existing labels.
  If no suitable real session can be located, the executor stops and surfaces a `checkpoint:decision` asking the operator to either (i) point at a specific real session or (ii) accept a synthetic fixture in the unnamed-corpus tier (no regression_guard) as a degraded fallback.

Discard target UUIDs (from CONTEXT Specifics):
  e3a564d063d4...  (truncated in CONTEXT.md; runbook must include FULL UUIDs -- operator fetches them from prod timescale before invoking the script)
  6edaaba7deb0...
  The runbook documents how the operator retrieves the full UUIDs:
    psql "$PG_PROD" -c "SELECT id, created_at, status FROM signal_draft WHERE id LIKE 'e3a564d063d4%' OR id LIKE '6edaaba7deb0%';"

Reprocess output path (CONTEXT carried policy):
  /mnt/mossrock/shared/mushdatadump-prod/2026-05-22_inoc_santi_reprocess_v1.9/
    - run-<ISO8601>.jsonl
    - extraction-draft.json
    - farmos-commit-result.json
    - operator-notes.md
  The runbook tells the operator to mkdir -p this dir before reprocess, write each artifact to a uniquely timestamped path, and NEVER touch the parent `2026-05-22_inoc_santi/` original-capture dir.

Mirror 48-LIVE-FIRE.md structure exactly:
  # Phase 49 Ship-Gate -- ...
  **Status:** OPERATOR-DEFERRED ...
  **Hermetic ship-gate:** PASS (sessions.test.js named-regression: N tests green) -- npx jest test/eval/ingestion/sessions.test.js
  **Operator runbook last revised:** <date>
  ## Why operator-deferred -- one paragraph citing Gray Area D + the v1.9 dev-farmOS-only policy
  ## Prerequisites -- numbered list (farmOS dev reachable + token + fungi_xing 'block' term exists + fungi_type SHI/KOY exist + Phase 47/48 ship-gate green + Plans 49-01/02/03 merged)
  ## Operator steps -- numbered Step 1..N
    Step 1: Hermetic sanity (sessions.test.js)
    Step 2: Retrieve full UUIDs of the two failed drafts from prod timescale
    Step 3: Dry-run discard (no --apply)
    Step 4: Apply discard (with --apply)
    Step 5: Mkdir reprocess output dir under mushdatadump-prod
    Step 6: Live-fire extraction via EVAL_RUN_LIVE=1 sessions.test.js -t '2026-05-22' OR a Node script that wires the real extractor + saves the draft JSON to the reprocess dir
    Step 7: Live-fire farmOS dev commit -- reuse 48-LIVE-FIRE Step 2 Node script verbatim, but draft is loaded from Step 6's saved JSON instead of the committed fixture
    Step 8: Verify session asset + lineage walk + child count (mirror 48-LIVE-FIRE Steps 3-5 exactly)
    Step 9: Append result to "Result" section -- timestamp, operator, asset UUIDs, log UUIDs, lineage walk JSON, deviations
    Step 10: Cleanup -- mirror 48-LIVE-FIRE Step 7 (delete the 11 children + session asset from farmOS dev; leave source blocks)
  ## Deviation policy -- mirror 48-LIVE-FIRE.md; deviations FAIL the gate + open follow-up; do NOT silently fix in Phase 49
  ## Result -- empty stub matching 48-LIVE-FIRE.md
  ## Files -- references to all relevant code
  ## Cross-references -- 48-LIVE-FIRE.md, 47-LIVE-FIRE.md, CONTEXT.md

ASCII discipline (CONTEXT):
  - no em-dashes anywhere in the runbook
  - use `--` for inline separators
  - use `?` or `n/a` instead of em-dash placeholders
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add third named-regression session to corpus</name>
  <read_first>
    - .planning/phases/49-real-session-eval-corpus-may-22-ship-gate-reprocess/49-CONTEXT.md (Gray Area F selection criteria)
    - .planning/phases/49-real-session-eval-corpus-may-22-ship-gate-reprocess/49-02-SUMMARY.md (the equality-assertion shape Plan 02 wired)
    - .planning/phases/38-extraction-pipeline/ (scan SUMMARYs for any real inoc session hand labels beyond Plan-09's May-12)
    - src/agents/alerter/test/eval/ingestion/fixtures/sessions/2026-05-22_inoc_santi/ground-truth.json (canonical shape to mirror)
  </read_first>
  <files>
    src/agents/alerter/test/eval/ingestion/fixtures/sessions/<third-session>/ground-truth.json (new)
    src/agents/alerter/test/eval/ingestion/fixtures/sessions/<third-session>/MANIFEST.md (new)
    src/agents/alerter/test/eval/ingestion/fixtures/sessions/<third-session>/audio.{m4a|aac} (copy from source corpus, if present)
    src/agents/alerter/test/eval/ingestion/fixtures/sessions/<third-session>/paper-log.jpg (copy from source corpus, if present)
    src/agents/alerter/test/eval/ingestion/fixtures/sessions/<third-session>/mock-extraction.json (new)
  </files>
  <behavior>
    - Third session is a real inoc session located by scanning `/mnt/mossrock/shared/mushdatadump/` (broader NFS corpus per project memory) OR any post-2026-05-12 prod inoc session that has accreted into `mushdatadump-prod/`.
    - Subdir name: `<YYYY-MM-DD>_inoc_<who>` matching the established naming convention.
    - ground-truth.json: full seeding_session draft with the canonical per-field provenance shape; meta.regression_guard=true; meta.notes documents the selection rationale + cardinality (single-parent vs multi-parent, group count, child count).
    - MANIFEST.md: ```json``` block + prose, ASCII only.
    - mock-extraction.json: tool_use envelope around the ground-truth draft (same shape Plan 02 wired).
    - sessions.test.js (Plan 02) picks up the new fixture automatically via the loader -- a re-run of `npx jest test/eval/ingestion/sessions.test.js` must produce 3 named-regression test cases all green.
  </behavior>
  <action>
    1. Search for a real inoc session under `/mnt/mossrock/shared/mushdatadump/` and `/mnt/mossrock/shared/mushdatadump-prod/` (exclude harvests, observations, squashes). Use `ls`, `grep -r 'inoc'`, and inspect MANIFEST.md files. Prefer a session with paired audio + paper-log and an existing replay-output.txt or hand-label artifact.
    2. If exactly one suitable session is found, proceed. If multiple, pick the smallest-cardinality one (different shape from May-22's 5 groups -- prefer 1-2 group session per Gray Area F).
    3. If NO suitable session is found, STOP and emit a checkpoint:decision asking the operator to either point at a specific session path or accept a degraded fallback (synthetic fixture with regression_guard:false in the unnamed-corpus tier; Plan 02's test would not cover it as a hard gate).
    4. On selection: create the fixture dir; copy audio + photo into it under the standard names; build ground-truth.json by hand-labeling from the audio transcript + photo + any existing replay-output.txt; build MANIFEST.md; build mock-extraction.json by wrapping ground-truth in the tool_use envelope (mirror Plan 02's per-fixture mock-extraction.json).
    5. Re-run `npx jest test/eval/ingestion/sessions.test.js` -- expect 3 named-regression cases green. If the new fixture fails the equality assertion, iterate on either the ground-truth shape or the mock-extraction.json (they should be mirror-images of each other modulo the meta block).
  </action>
  <verify>
    <automated>cd src/agents/alerter && npx jest test/eval/ingestion/sessions.test.js --no-coverage && ls test/eval/ingestion/fixtures/sessions/ | wc -l</automated>
  </verify>
  <done>
    Third fixture committed; sessions.test.js green with 3 named-regression cases; ls returns at least 3 subdirs.
  </done>
</task>

<task type="auto">
  <name>Task 2: Author 49-SHIP-GATE.md runbook</name>
  <read_first>
    - .planning/phases/48-session-entity-per-bag-commit-fan-out-session-shaped-confirm/48-LIVE-FIRE.md (the structural template to mirror)
    - .planning/phases/47-multi-source-extraction-fusion-groups-shape-inoc-draft/47-LIVE-FIRE.md (the cost-paper-trail precedent)
    - .planning/phases/49-real-session-eval-corpus-may-22-ship-gate-reprocess/49-03-SUMMARY.md (discard-drafts.js CLI exact usage)
    - src/agents/alerter/scripts/discard-drafts.js (the actual CLI surface)
    - src/agents/alerter/test/eval/ingestion/sessions.test.js (the EVAL_RUN_LIVE branch to invoke)
  </read_first>
  <files>
    .planning/phases/49-real-session-eval-corpus-may-22-ship-gate-reprocess/49-SHIP-GATE.md (new)
  </files>
  <behavior>
    - Mirror 48-LIVE-FIRE.md structure exactly (see <interfaces> for the section-by-section breakdown).
    - All commands are ASCII; no em-dashes; use `--` and `?` and `n/a`.
    - Every command is operator-runnable verbatim: full curl/psql/node/jest invocations, complete env var references, no `$YOUR_VAR_HERE` placeholders that aren't documented in Prerequisites.
    - Step 2 (retrieve UUIDs) provides the exact psql command + the truncated prefixes from CONTEXT.md (e3a564d063d4 + 6edaaba7deb0).
    - Steps 3 + 4 invoke `node scripts/discard-drafts.js` with the right arg shape (dry-run + then --apply).
    - Step 5 creates the reprocess output dir under the unique-path policy.
    - Steps 6 + 7 either chain `EVAL_RUN_LIVE=1` jest -t '2026-05-22' OR a small Node script that runs the real extractor + then commitSeedingSession + saves all intermediate artifacts to the reprocess dir.
    - Step 8 mirrors 48-LIVE-FIRE Steps 3-5 verbatim (session asset query + lineage walk on 260522_KOY_7 + child count).
    - Step 9 documents the "Result" section template (Date, Operator, Elapsed_ms, Session UUID, Child UUIDs, Log UUIDs, Lineage walk JSON, Children count, Cleanup outcome, Deviations, Verdict).
    - Step 10 mirrors 48-LIVE-FIRE Step 7 cleanup (delete the 11 children + session asset; leave source blocks).
    - Deviation policy: any farmOS / extraction divergence FAILS the gate + opens a follow-up phase; do NOT silently patch in Phase 49.
    - "Result" stub is empty + ready for operator to fill.
  </behavior>
  <action>
    1. Open 48-LIVE-FIRE.md as the structural template. Copy section headers + adapt content.
    2. Write Prerequisites covering: farmOS dev reachable + FARMOS_API_TOKEN + 'block' fungi_xing term + SHI/KOY fungi_type terms + PG_PROD_CONN_STRING for the discard step + Plans 49-01/02/03 merged + Phase 47 + 48 hermetic ship-gates green.
    3. Write each step per the contract above. Use the exact full UUIDs once the operator-fetched form is established (or leave the truncated prefix + the psql command to resolve them).
    4. Write the Deviation policy section mirroring 48-LIVE-FIRE.md's.
    5. Write the empty Result stub matching 48-LIVE-FIRE.md's shape.
    6. Cross-reference to 47-LIVE-FIRE.md, 48-LIVE-FIRE.md, and 49-CONTEXT.md.
    7. ASCII-grep the file: confirm no em-dashes (Unicode U+2014) or en-dashes (U+2013) anywhere.
  </action>
  <verify>
    <automated>test -f .planning/phases/49-real-session-eval-corpus-may-22-ship-gate-reprocess/49-SHIP-GATE.md && python3 -c "import sys; d=open('.planning/phases/49-real-session-eval-corpus-may-22-ship-gate-reprocess/49-SHIP-GATE.md').read(); sys.exit(1 if any(c in d for c in ['–','—','―']) else 0)" && grep -c "discard-drafts.js" .planning/phases/49-real-session-eval-corpus-may-22-ship-gate-reprocess/49-SHIP-GATE.md</automated>
  </verify>
  <done>
    49-SHIP-GATE.md committed; mirrors 48-LIVE-FIRE.md structure; ASCII-only; discard-drafts.js invocation present; 10 operator steps with full commands; Result stub empty + ready for operator.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Operator runbook sanity review</name>
  <what-built>
    Third fixture committed and sessions.test.js green with 3 named regressions. 49-SHIP-GATE.md drafted with full operator commands for discard + reprocess + farmOS-dev verification + cleanup.
  </what-built>
  <how-to-verify>
    1. Read 49-SHIP-GATE.md end-to-end. Confirm:
       - Prerequisites cover everything you'd need before kicking off (farmOS dev URL, token, prod PG conn string, hermetic green).
       - Step 2 psql command would actually resolve the two truncated UUIDs against prod timescale (do NOT run yet; just read).
       - Step 4 discard invocation uses --apply only after Step 3 dry-run sanity.
       - Step 5 reprocess output path matches `2026-05-22_inoc_santi_reprocess_v1.9/`; original `2026-05-22_inoc_santi/` is never touched.
       - Steps 6-8 chain extraction + farmOS dev commit + verification with concrete commands.
       - Step 10 cleanup deletes only the 11 child blocks + session asset (NOT source blocks 260118_*, 260304_*, 260425_*).
       - No em-dashes anywhere (the automated grep already confirmed; eyeball spot-check).
    2. If any command would not run verbatim (missing env var, wrong path, typo in UUID prefix), flag it and the executor revises before merge.
    3. The actual ship-gate execution itself is operator-deferred; this checkpoint only reviews the RUNBOOK, not the execution.
  </how-to-verify>
  <resume-signal>Type "runbook approved" or describe the revisions needed</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator-driven reprocess against farmOS dev | Real writes; bounded by dev-only policy (no prod farmOS) |
| Live Anthropic API call (paid) | Cost-bounded by single-session live-fire (~$0.05 per Phase 44 baseline) |
| Discard CLI invocation against prod timescale | Bounded by Plan 03's dry-run-by-default + idempotency + the two explicit UUIDs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-49-04-01 | Tampering | Wrong-UUID discard could mark a healthy draft discarded | mitigate | Plan 03's dry-run-by-default + Step 3 in runbook is explicit dry-run before --apply |
| T-49-04-02 | Information disclosure | farmOS dev token in operator env | accept | Operator-only context; not committed to repo |
| T-49-04-03 | Tampering | Reprocess overwrites original failed-run JSONL | mitigate | Unique output path under _reprocess_v1.9/ per [[feedback_persist_paid_results_default]] |
| T-49-04-04 | DoS | Runaway live-fire spend | mitigate | Single-session scope; EVAL_RUN_LIVE=1 gate; max_tokens cap from Phase 47 carries forward |
| T-49-04-05 | Repudiation | Ship-gate execution not paper-trailed | mitigate | Step 9 mandates Result section append with timestamp + operator + UUIDs + verdict |
| T-49-04-SC | Tampering | No new npm packages | accept | Plan adds zero deps |
</threat_model>

<verification>
- `npx jest test/eval/ingestion/sessions.test.js` green (3 named regressions)
- `ls src/agents/alerter/test/eval/ingestion/fixtures/sessions/` returns at least 3 subdirs
- `test -f .planning/phases/49-real-session-eval-corpus-may-22-ship-gate-reprocess/49-SHIP-GATE.md`
- Python ASCII check on 49-SHIP-GATE.md returns no Unicode dashes (U+2013/2014/2015)
- `grep -c "discard-drafts.js" 49-SHIP-GATE.md` returns at least 2 (dry-run + apply invocations)
- `grep -c "2026-05-22_inoc_santi_reprocess_v1.9" 49-SHIP-GATE.md` returns at least 1
- Task 3 checkpoint resolved with "runbook approved"
</verification>

<success_criteria>
INOC-07 satisfied: >=3 named-regression sessions in corpus + ship-gate runbook ready for operator execution. v1.9 milestone closure-ready: the actual operator execution lands as a Result-section amendment to 49-SHIP-GATE.md post-merge, NOT as a code change to Phase 49. Any deviation observed during the live ship-gate execution opens a follow-up phase rather than silent patching here.
</success_criteria>

<output>
Create `.planning/phases/49-real-session-eval-corpus-may-22-ship-gate-reprocess/49-04-SUMMARY.md` recording: (1) the third session selected (name, source path, cardinality), (2) sessions.test.js green with 3 named-regression cases, (3) 49-SHIP-GATE.md committed with 10 operator steps + ASCII discipline confirmed, (4) explicit note that ship-gate execution is operator-deferred and will land as a Result-section amendment, (5) INOC-07 attestation status (ready to attest after operator runs ship-gate + appends Result).
</output>
