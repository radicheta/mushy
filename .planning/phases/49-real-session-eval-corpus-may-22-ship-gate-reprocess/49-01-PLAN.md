---
phase: 49-real-session-eval-corpus-may-22-ship-gate-reprocess
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/agents/alerter/src/extraction/extraction-db.js
  - src/agents/alerter/test/extraction/extraction-db.test.js
  - src/agents/alerter/test/eval/ingestion/fixtures/sessions/2026-05-22_inoc_santi/ground-truth.json
  - src/agents/alerter/test/eval/ingestion/fixtures/sessions/2026-05-22_inoc_santi/MANIFEST.md
  - src/agents/alerter/test/eval/ingestion/fixtures/sessions/2026-05-22_inoc_santi/audio.m4a
  - src/agents/alerter/test/eval/ingestion/fixtures/sessions/2026-05-22_inoc_santi/paper-log.jpg
  - src/agents/alerter/test/eval/ingestion/sessions-loader.js
  - src/agents/alerter/test/eval/ingestion/sessions-loader.test.js
autonomous: true
requirements: [INOC-07]
must_haves:
  truths:
    - "signal_draft table has nullable columns discarded_reason TEXT + discarded_at TIMESTAMPTZ (added idempotently in initDb)"
    - "test/eval/ingestion/fixtures/sessions/2026-05-22_inoc_santi/ exists with audio.m4a, paper-log.jpg, ground-truth.json, MANIFEST.md"
    - "ground-truth.json is a literal seeding_session draft (5 groups, 11 children 260522_SHI_1..3 + 260522_KOY_4..11, event_date 2026-05-22) plus meta:{ capture_date, source_path, regression_guard: true, notes }"
    - "sessions-loader.js exports loadSessionsCorpus(dir) returning { name, manifest, groundTruth, audioPath?, photoPath? } per session subdir; named sessions surface manifest.regression_guard==true"
    - "sessions-loader.test.js asserts loader returns exactly 1 entry for the May-22 fixture with regression_guard:true and the 11 expected child block names"
  artifacts:
    - path: src/agents/alerter/test/eval/ingestion/fixtures/sessions/2026-05-22_inoc_santi/ground-truth.json
      provides: "Canonical hand-labeled expected draft for May 22 (5 groups, 11 children, full seeding_session shape)"
    - path: src/agents/alerter/test/eval/ingestion/fixtures/sessions/2026-05-22_inoc_santi/MANIFEST.md
      provides: "Capture metadata + regression_guard flag"
    - path: src/agents/alerter/test/eval/ingestion/sessions-loader.js
      provides: "Sibling of fixtures-loader.js, iterates sessions/<dir>/ and yields normalized fixtures"
  key_links:
    - from: src/agents/alerter/test/eval/ingestion/sessions-loader.js
      to: src/agents/alerter/test/eval/ingestion/fixtures/sessions/
      via: "fs.readdirSync of named subdirs; each must have ground-truth.json + MANIFEST.md"
      pattern: "loadSessionsCorpus"
---

<objective>
Lay the corpus foundation for Phase 49. Two atomic pieces: (1) extend `signal_draft` schema with the two columns the discard script (Plan 03) needs (`discarded_reason text`, `discarded_at timestamptz`) via idempotent `ALTER TABLE IF NOT EXISTS` in `initDb`; (2) carve the new `test/eval/ingestion/fixtures/sessions/` corpus subdir, drop in the first named regression fixture (2026-05-22), and ship a `sessions-loader.js` sibling of `fixtures-loader.js` so downstream plans can iterate the corpus.

Purpose: every later plan (02 named-regression test, 03 discard script, 04 ship-gate runbook + 3rd session) depends on these primitives. Schema + loader + first fixture is the irreducible bedrock per `[[feedback_unit_tests_dont_catch_wiring]]` -- ship the producer columns AND the consumer scaffolding in one plan so no later plan finds itself blocked.

Output: schema migration committed, loader + first fixture committed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/49-real-session-eval-corpus-may-22-ship-gate-reprocess/49-CONTEXT.md
@.planning/phases/47-multi-source-extraction-fusion-groups-shape-inoc-draft/47-CONTEXT.md
@src/agents/alerter/src/extraction/extraction-db.js
@src/agents/alerter/test/eval/ingestion/fixtures-loader.js
@src/agents/alerter/test/eval/ingestion/paperlog.test.js

<interfaces>
Existing initDb shape (extraction-db.js lines 26-59):
  CREATE TABLE IF NOT EXISTS signal_draft ( ... )
  CREATE INDEX IF NOT EXISTS idx_signal_draft_sender_status
  CREATE UNIQUE INDEX IF NOT EXISTS idx_signal_draft_in_flight_per_sender
  ALTER TABLE signal_draft ADD COLUMN IF NOT EXISTS needs_review_reason text
The pattern to extend: append two more `ALTER TABLE ... ADD COLUMN IF NOT EXISTS` calls.

fixtures-loader.js exports loadSyntheticCorpus(dir) returning Fixture[] of shape
  { name, kind, envelope, attachments[], expected, mockResponse?, mock_transcript? }
sessions-loader.js mirrors that shape but with a sessions-specific output:
  { name, manifest, groundTruth, audioPath?, photoPath? }
No envelope/expected/mockResponse -- the named-sessions test (Plan 02) builds its own pipeline input.

May-22 source files (operator-attested copies from prod corpus):
  audio:     /mnt/mossrock/shared/mushdatadump-prod/2026-05-12_inoc_santi/HOZad9ymvNJTXmRREgcW.m4a
             (NOTE: filename predates folder rename; the May-22 capture was misfiled under the 05-12 dir per CONTEXT Specifics; verify with the .pre-vad-fix.txt transcript content before copying. If actual May-22 audio lives elsewhere under mushdatadump-prod, use that path instead.)
  photo:     /mnt/mossrock/shared/mushdatadump-prod/2026-05-12_inoc_santi/XAbzzUidkLR3irhVmjea.jpg
  Both copy into the new sessions/2026-05-22_inoc_santi/ fixture dir.

Canonical ground-truth shape for May 22 (from CONTEXT Specifics):
  type: 'seeding_session'
  event_date: '2026-05-22'
  groups: 5
    - SHI x1 parent 260304_SHI_5  child 260522_SHI_1
    - SHI x1 parent 260118_SHI_23 child 260522_SHI_2
    - SHI x1 parent 260118_SHI_26 child 260522_SHI_3
    - KOY x4 parent 260118_KOY_12 children 260522_KOY_4..7
    - KOY x4 parent 260425_KOY_4  children 260522_KOY_8..11
  Per-field shape mirrors 47-04 seeding-session Zod schema (parent.value + .sources[] + .confidence). For ground-truth.json, populate sources[] with the realistic combination per CONTEXT.md (audio for SHI parents from voice notes, paper_log_photo for KOY parents from the photo, audio+paper_log_photo where both contributed). Confidence may be omitted in the truth file -- equality assertions in Plan 02 will key off {value, sources} only.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add discarded_reason + discarded_at columns to signal_draft</name>
  <read_first>
    - src/agents/alerter/src/extraction/extraction-db.js (entire initDb function, lines 26-60)
    - src/agents/alerter/test/extraction/extraction-db.test.js (existing migration tests, to mirror style)
  </read_first>
  <files>
    src/agents/alerter/src/extraction/extraction-db.js (modify initDb)
    src/agents/alerter/test/extraction/extraction-db.test.js (extend)
  </files>
  <behavior>
    - After running initDb against a fresh pg-mem (or jest-pg) pool, `information_schema.columns` for signal_draft includes `discarded_reason text NULL` and `discarded_at timestamptz NULL`.
    - Running initDb twice in a row does not error (IF NOT EXISTS semantics).
    - Existing inserts that omit the new columns still succeed (insertDraft path unchanged).
  </behavior>
  <action>
    In initDb, after the existing `ALTER TABLE signal_draft ADD COLUMN IF NOT EXISTS needs_review_reason text` line, append two more idempotent ALTERs: one for `discarded_reason text` and one for `discarded_at timestamptz`. Match the existing style exactly. Do not modify insertDraft -- columns are NULL by default and Plan 03's script is the only writer. Extend extraction-db.test.js: add a test that initDb is idempotent and the two new columns appear in the column list. Use whatever pool harness the existing tests use (likely a jest-pg helper or pg-mem -- match the file).
  </action>
  <verify>
    <automated>cd src/agents/alerter && npx jest test/extraction/extraction-db.test.js --no-coverage</automated>
  </verify>
  <done>Migration columns added idempotently; tests green; no insertDraft regression.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create sessions/ corpus subdir, May 22 fixture, sessions-loader.js</name>
  <read_first>
    - src/agents/alerter/test/eval/ingestion/fixtures-loader.js (entire file, especially loadSyntheticCorpus + the directory iteration pattern)
    - src/agents/alerter/test/eval/ingestion/paperlog.test.js (to mirror loader test style)
    - .planning/phases/47-multi-source-extraction-fusion-groups-shape-inoc-draft/47-04-SUMMARY.md (for the canonical seeding_session shape)
  </read_first>
  <files>
    src/agents/alerter/test/eval/ingestion/sessions-loader.js (new)
    src/agents/alerter/test/eval/ingestion/sessions-loader.test.js (new)
    src/agents/alerter/test/eval/ingestion/fixtures/sessions/2026-05-22_inoc_santi/ground-truth.json (new)
    src/agents/alerter/test/eval/ingestion/fixtures/sessions/2026-05-22_inoc_santi/MANIFEST.md (new)
    src/agents/alerter/test/eval/ingestion/fixtures/sessions/2026-05-22_inoc_santi/audio.m4a (copy from prod corpus)
    src/agents/alerter/test/eval/ingestion/fixtures/sessions/2026-05-22_inoc_santi/paper-log.jpg (copy from prod corpus)
  </files>
  <behavior>
    - sessions-loader.js exports `loadSessionsCorpus(dir, { logger } = {})` returning an array of `{ name, dir, manifest, groundTruth, audioPath, photoPath }` -- one entry per direct child subdir that contains `ground-truth.json` and `MANIFEST.md`. Missing groundTruth: warn + skip. Missing MANIFEST.md: warn + skip. audioPath/photoPath are set to absolute paths when files matching `audio.*` (m4a/aac/wav/mp3) or `paper-log*.jpg|jpeg|png` exist in the subdir; null otherwise.
    - manifest is the parsed front-matter-style or JSON-front-block of MANIFEST.md. For simplicity, use plain JSON in a fenced ```json``` block inside MANIFEST.md OR an adjacent `manifest.json`. Per Gray Area A, MANIFEST.md is human-readable -- so adopt: MANIFEST.md starts with a fenced ```json``` block containing `{ regression_guard: bool, capture_date, source_path, notes }`, followed by prose. The loader extracts the first ```json``` fenced block and JSON.parses it.
    - sessions-loader.test.js: builds a tmp dir with the real fixture (or points at the committed fixture path), asserts: returns length>=1, entry.name === '2026-05-22_inoc_santi', entry.manifest.regression_guard === true, entry.groundTruth.event_date === '2026-05-22', entry.groundTruth.groups.length === 5, total children across groups === 11, audioPath !== null, photoPath !== null.
    - ground-truth.json is a literal seeding_session draft per the canonical shape (see <interfaces>). Include the full per-field provenance shape from 47-04 (parent.{value,sources}, species.{value,sources}, qty.{value,sources}, child_block_names.{value:[],sources}). Top-level `meta: { capture_date: '2026-05-22', source_path: '/mnt/mossrock/shared/mushdatadump-prod/2026-05-12_inoc_santi/' (corrected path noted in MANIFEST), regression_guard: true, notes: '...' }`.
    - MANIFEST.md begins with a ```json``` block containing the manifest object, then a prose section explaining capture origin + any operator notes (no em-dashes; use `--` or commas).
  </behavior>
  <action>
    1. Create the directory `src/agents/alerter/test/eval/ingestion/fixtures/sessions/2026-05-22_inoc_santi/`.
    2. Copy the May 22 audio + photo from prod corpus. Per CONTEXT Specifics the files are `HOZad9ymvNJTXmRREgcW.m4a` + `XAbzzUidkLR3irhVmjea.jpg` under `/mnt/mossrock/shared/mushdatadump-prod/2026-05-12_inoc_santi/` (the May-22 capture was misfiled under the May-12 dir during Phase 47 live-fire; the transcript content confirms the May-22 session). Rename to `audio.m4a` and `paper-log.jpg`. If the source files are large (>10MB), confirm git-LFS or commit them anyway -- the alerter repo accepts these per the synthetic fixture precedent.
    3. Author `ground-truth.json` from the canonical shape in <interfaces> -- 5 groups, 11 children, full per-field provenance shape. Include `meta` with regression_guard:true. ASCII only, no em-dashes.
    4. Author `MANIFEST.md` -- ```json``` block first (`{regression_guard,capture_date,source_path,notes}`), then a prose paragraph documenting that this is the Phase 49 named regression guard, capture-misfiled-under-May-12 caveat included.
    5. Author `sessions-loader.js` per the contract above. Use the same code style as fixtures-loader.js (CJS, fs.readdirSync, logger.warn on missing files, throw only on programmer error).
    6. Author `sessions-loader.test.js` per the assertions above. Use the same jest config that picks up `test/eval/ingestion/*.test.js`.
    7. Run the loader test; iterate until green.
  </action>
  <verify>
    <automated>cd src/agents/alerter && npx jest test/eval/ingestion/sessions-loader.test.js --no-coverage</automated>
  </verify>
  <done>
    sessions/2026-05-22_inoc_santi/ directory committed with all four artifacts (audio, photo, ground-truth, MANIFEST); sessions-loader.js + test green; loader returns the expected May-22 entry with regression_guard:true and 11 children.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| prod-corpus audio/photo into test fixture | Real prod data may carry voice content of human subjects; gitignore check before commit |
| signal_draft schema migration | Runs at alerter boot; idempotent ALTER limits blast radius |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-49-01-01 | Information disclosure | Real-voice audio fixture committed to repo | accept | Audio is Santi's own voice; project owner is also farmer1; mossrock OSS-Foray α-lock policy applies (no external PII) |
| T-49-01-02 | Tampering | Migration runs on every alerter boot | mitigate | IF NOT EXISTS gates both ALTERs; idempotency unit-tested |
| T-49-01-03 | DoS | Large audio file in repo balloons clone size | accept | Single file ~few MB; if it crosses 10MB add to .gitattributes for LFS |
| T-49-01-SC | Tampering | No new npm packages | accept | Plan adds zero deps |
</threat_model>

<verification>
- `npx jest test/extraction/extraction-db.test.js` green (includes new column assertion)
- `npx jest test/eval/ingestion/sessions-loader.test.js` green
- `ls src/agents/alerter/test/eval/ingestion/fixtures/sessions/2026-05-22_inoc_santi/` shows `audio.m4a`, `paper-log.jpg`, `ground-truth.json`, `MANIFEST.md`
- `jq '.groups | length' src/agents/alerter/test/eval/ingestion/fixtures/sessions/2026-05-22_inoc_santi/ground-truth.json` returns 5
- `jq '[.groups[].child_block_names.value[]] | length' .../ground-truth.json` returns 11
</verification>

<success_criteria>
Foundation primitives for Phase 49 are committed: schema columns exist for the discard script to write to; sessions-loader.js + first named fixture exist for Plan 02 to iterate; the regression_guard:true flag is wired and asserted. Plans 02/03/04 are unblocked.
</success_criteria>

<output>
Create `.planning/phases/49-real-session-eval-corpus-may-22-ship-gate-reprocess/49-01-SUMMARY.md` recording: (1) the two new column names + idempotency test result, (2) the May-22 fixture layout + ground-truth shape summary (group/child counts), (3) loader contract documentation, (4) the source path correction note (May-22 capture filed under May-12 dir).
</output>
