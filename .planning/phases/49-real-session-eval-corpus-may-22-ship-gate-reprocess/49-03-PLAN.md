---
phase: 49-real-session-eval-corpus-may-22-ship-gate-reprocess
plan: 03
type: execute
wave: 2
depends_on: ['49-01']
files_modified:
  - src/agents/alerter/scripts/discard-drafts.js
  - src/agents/alerter/scripts/discard-drafts.test.js
autonomous: true
requirements: [INOC-07]
must_haves:
  truths:
    - "discard-drafts.js is a standalone idempotent CLI accepting --uuid <uuid> (repeatable) + --reason '<text>' + --apply"
    - "Default mode is dry-run: lists rows that WOULD be updated, exits 0 without writing"
    - "With --apply, opens a transactional UPDATE writing status='discarded', discarded_reason=<text>, discarded_at=now() where current status != 'discarded' and id IN (uuids)"
    - "Re-running on already-discarded UUIDs is a no-op (WHERE status != 'discarded' filters them out); script exits 0 + logs '0 rows updated, N already discarded'"
    - "Missing --uuid or missing --reason exits 2 with usage; --apply without --uuid same"
    - "Unit test covers: dry-run prints + does not write, --apply writes, idempotent no-op on re-run, malformed args exit 2"
  artifacts:
    - path: src/agents/alerter/scripts/discard-drafts.js
      provides: "Reusable signal_draft discard CLI"
    - path: src/agents/alerter/scripts/discard-drafts.test.js
      provides: "Idempotency + dry-run + arg-parsing regression"
  key_links:
    - from: src/agents/alerter/scripts/discard-drafts.js
      to: src/agents/alerter/src/extraction/extraction-db.js (signal_draft schema with discarded_reason + discarded_at from Plan 01)
      via: "pg pool UPDATE signal_draft SET status='discarded', discarded_reason=$1, discarded_at=now()"
      pattern: "UPDATE signal_draft.*discarded_reason"
---

<objective>
Ship the discard CLI. Standalone script in `src/agents/alerter/scripts/discard-drafts.js` that takes UUID(s) + a reason and marks the corresponding `signal_draft` rows as `discarded` -- writing the columns added in Plan 01. Dry-run by default; only writes under `--apply`. Idempotent (re-running on already-discarded rows is a no-op + logged). Reusable beyond Phase 49 (per CONTEXT Gray Area E).

This plan does NOT discard the two real May-22 drafts -- that happens in Plan 04's operator-driven ship-gate runbook (Gray Area D: operator-driven, not auto-CI). This plan only ships the tool + tests.

Purpose: closes the discard-mechanism half of INOC-07's "drafts marked discarded" requirement. Plan 04's runbook invokes this script with the two real UUIDs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/49-real-session-eval-corpus-may-22-ship-gate-reprocess/49-CONTEXT.md
@.planning/phases/49-real-session-eval-corpus-may-22-ship-gate-reprocess/49-01-SUMMARY.md
@src/agents/alerter/scripts/replay-prod-corpus.js
@src/agents/alerter/scripts/phase-45-backfill-outcome-acks.js
@src/agents/alerter/src/extraction/extraction-db.js

<interfaces>
Existing scripts/ patterns (read replay-prod-corpus.js and phase-45-backfill-outcome-acks.js):
  - Node CJS top-level async IIFE
  - Reads connection string from process.env.PG_CONN_STRING (or whichever env var the existing scripts use -- match exact name)
  - Uses 'pg' Client (not Pool) for one-shot scripts
  - Logs structured one-line-per-event to stdout

CLI contract:
  Usage: node scripts/discard-drafts.js --uuid <uuid> [--uuid <uuid>...] --reason "<text>" [--apply]

  --uuid <uuid>     Draft UUID. Repeatable.
  --reason "<text>" Reason string written to discarded_reason. Required.
  --apply           Without this flag, dry-run: SELECT + log + exit 0 without writing.
  --help            Print usage + exit 0.

  Exit codes:
    0 -- success (including dry-run + no-op on already-discarded)
    2 -- arg parse / usage error
    1 -- pg error

Use built-in process.argv parsing (no new commander/yargs dep -- match the existing scripts which appear to do manual argv parsing per the scripts/ inventory).

SQL contracts:
  Dry-run query:
    SELECT id, status, log_type, sender_e164 FROM signal_draft WHERE id = ANY($1::text[])
    -- log each row, flag those where status='discarded' as 'already-discarded skip'

  Apply (one transaction):
    BEGIN;
    UPDATE signal_draft
       SET status='discarded',
           discarded_reason=$1,
           discarded_at=now(),
           updated_at=now()
     WHERE id = ANY($2::text[])
       AND status != 'discarded'
     RETURNING id, status, discarded_reason, discarded_at;
    COMMIT;

  Log each RETURNING row + count summary ('N rows updated, M already discarded, K unknown').

Test harness pattern:
  scripts in this repo use Jest with a pg test client. Read how phase-45-backfill-outcome-acks.test.js (if present) is structured; otherwise mirror extraction-db.test.js's pool setup. Spin up a temp signal_draft row, run the script as a child_process spawn OR refactor the script to export a runnable async function (discardDrafts({pool, uuids, reason, apply})) and unit-test that directly. Prefer the latter -- separates CLI parsing from DB logic.

Recommended file shape:
  module.exports = { parseArgs, discardDrafts };
  if (require.main === module) { (async () => { ... CLI entry ... })(); }
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Author discard-drafts.js + test</name>
  <read_first>
    - src/agents/alerter/scripts/replay-prod-corpus.js (entire -- argv parsing + pg client + logging conventions)
    - src/agents/alerter/scripts/phase-45-backfill-outcome-acks.js (entire -- another concrete pattern)
    - src/agents/alerter/src/extraction/extraction-db.js (initDb function -- confirm Plan 01's discarded_reason + discarded_at columns exist; this script depends on them)
    - src/agents/alerter/test/extraction/extraction-db.test.js (for the pool harness pattern -- pg-mem? real pg? mirror it)
    - .planning/phases/49-real-session-eval-corpus-may-22-ship-gate-reprocess/49-CONTEXT.md (Gray Area E -- the canonical contract for this script)
  </read_first>
  <files>
    src/agents/alerter/scripts/discard-drafts.js (new)
    src/agents/alerter/scripts/discard-drafts.test.js (new)
  </files>
  <behavior>
    - parseArgs(argv) -> { uuids, reason, apply, help } | throws Error('<usage message>')
      - --uuid <u> repeatable; collected into array
      - --reason "<text>" required (single value); empty string is invalid
      - --apply boolean flag
      - --help boolean flag
      - Unknown args throw usage error
    - discardDrafts({ pool, uuids, reason, apply, logger }) -> { dryRun, candidates:[{id,status,...}], updated:[], alreadyDiscarded:[], unknown:[] }
      - Always runs SELECT first to classify rows: present-and-not-discarded -> candidates; present-and-discarded -> alreadyDiscarded; not-in-DB -> unknown.
      - If apply, runs UPDATE in a transaction and assigns updated[] from RETURNING.
      - logger.info one structured line per row + a one-line summary at the end.
    - CLI entry: parses argv, instantiates a pg Client from process.env, calls discardDrafts, exits with the right code.
    - Tests cover:
      1. parseArgs happy path -- multiple --uuid + --reason + --apply -> correct shape
      2. parseArgs missing --reason -> throws
      3. parseArgs missing --uuid -> throws
      4. parseArgs --help -> { help:true }
      5. discardDrafts dry-run: inserts a test signal_draft row, calls without apply, asserts no DB mutation, candidates.length===1
      6. discardDrafts apply: same row, with apply, asserts row.status==='discarded', discarded_reason matches, discarded_at !== null
      7. discardDrafts idempotent: re-runs apply on the same uuid, asserts updated.length===0, alreadyDiscarded.length===1, row unchanged on second run
      8. discardDrafts unknown uuid: passes a non-existent uuid, asserts unknown.length===1, no error
  </behavior>
  <action>
    1. Create discard-drafts.js with the parseArgs + discardDrafts exports + CLI entry per the contract. ASCII only, no em-dashes anywhere.
    2. Create discard-drafts.test.js covering all 8 cases above. Use the same pg test-pool helper that extraction-db.test.js uses (jest-pg, pg-mem, or whatever pattern is in place). For tests 5-8 the test must initDb first (which now includes the new columns from Plan 01) then insert a synthetic signal_draft row, then exercise discardDrafts.
    3. Iterate until green. Confirm by running the test suite + a manual CLI smoke (dry-run only) against a tmp test DB.
    4. Document the CLI usage at the top of discard-drafts.js as a header comment block.
  </action>
  <verify>
    <automated>cd src/agents/alerter && npx jest scripts/discard-drafts.test.js --no-coverage && node scripts/discard-drafts.js --help</automated>
  </verify>
  <done>
    discard-drafts.js + test green; --help prints usage and exits 0; dry-run + apply + idempotency + unknown-uuid all covered. Script is reusable beyond Phase 49.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator-supplied UUIDs on CLI | Untrusted only inasmuch as a typo could hit the wrong row; --apply is the explicit gate |
| pg connection string from env | Same trust posture as other alerter scripts; reuse the existing env var |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-49-03-01 | Tampering | Accidental mass-discard via typo | mitigate | Dry-run is the default; --apply required; SELECT-first classification surfaces unknown UUIDs before any UPDATE |
| T-49-03-02 | Repudiation | No audit log of who discarded what | mitigate | discarded_reason captures the reason text; updated_at + discarded_at are timestamps; structured stdout log is the operator trail |
| T-49-03-03 | Elevation of privilege | Script writes to signal_draft | accept | Same DB role as the alerter itself; no escalation |
| T-49-03-SC | Tampering | No new npm packages | accept | Reuses existing 'pg' dep |
</threat_model>

<verification>
- `npx jest scripts/discard-drafts.test.js` green (all 8 cases)
- `node scripts/discard-drafts.js --help` exits 0 + prints usage
- `node scripts/discard-drafts.js --uuid notreal --reason "x"` (dry-run) exits 0 + logs 'unknown'
- `grep -c "em-dash\|--" src/agents/alerter/scripts/discard-drafts.js` -- only the literal `--apply` etc., no em-dashes
</verification>

<success_criteria>
Discard CLI shipped + tested. Plan 04's ship-gate runbook can invoke it with the two real May-22 draft UUIDs. The dry-run-by-default + idempotency posture makes the script safe to keep around as a permanent maintenance tool.
</success_criteria>

<output>
Create `.planning/phases/49-real-session-eval-corpus-may-22-ship-gate-reprocess/49-03-SUMMARY.md` recording: (1) CLI usage block, (2) the discardDrafts exported-function contract, (3) the 8 test cases passing, (4) note that Plan 04 invokes it on the two real May-22 UUIDs.
</output>
