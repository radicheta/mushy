---
phase: 50-signal-native-quote-threading
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/agents/alerter/src/outbound-db.js
  - src/agents/alerter/src/capture-db.js
  - src/agents/alerter/test/outbound-db.test.js
  - src/agents/alerter/test/capture-db.test.js
autonomous: true
requirements: [QUOT-01, QUOT-02]
must_haves:
  truths:
    - "signal_outbound has nullable column signal_msg_ts bigint (added idempotently in outbound-db.initDb)"
    - "signal_outbound has partial index idx_signal_outbound_msg_ts ON (signal_msg_ts) WHERE signal_msg_ts IS NOT NULL"
    - "signal_capture has nullable column signal_msg_ts bigint (added idempotently in capture-db.initDb)"
    - "signal_capture has nullable columns quote_msg_ts bigint + quote_author_e164 text"
    - "Re-running initDb twice does not error (IF NOT EXISTS / IF NOT EXISTS index semantics)"
    - "Existing insertOutbound + insertCapture paths still succeed without supplying any of the new columns (they default NULL)"
  artifacts:
    - path: src/agents/alerter/src/outbound-db.js
      provides: "Idempotent ALTER + index for signal_outbound.signal_msg_ts"
    - path: src/agents/alerter/src/capture-db.js
      provides: "Idempotent ALTERs for signal_capture.signal_msg_ts, quote_msg_ts, quote_author_e164"
  key_links:
    - from: src/agents/alerter/src/outbound-db.js
      to: signal_outbound table
      via: "ALTER TABLE signal_outbound ADD COLUMN IF NOT EXISTS signal_msg_ts bigint + CREATE INDEX IF NOT EXISTS idx_signal_outbound_msg_ts"
      pattern: "signal_msg_ts"
    - from: src/agents/alerter/src/capture-db.js
      to: signal_capture table
      via: "ALTER TABLE signal_capture ADD COLUMN IF NOT EXISTS {signal_msg_ts bigint, quote_msg_ts bigint, quote_author_e164 text}"
      pattern: "quote_msg_ts"
---

<objective>
Lay the schema bedrock for Phase 50. Three idempotent column additions across two tables plus one partial index, all executed inside the existing `initDb` functions so the boot-time migration carries them forward without operator intervention. No data migration; new rows from Plans 02 and 04 onward populate the columns; older rows remain NULL and the inbound resolver gracefully falls through.

Purpose: every downstream plan in this phase depends on these columns existing. Plan 02 writes `signal_outbound.signal_msg_ts`. Plan 03 reads `signal_capture.signal_msg_ts` for the outbound quote payload. Plan 04 writes `signal_capture.{signal_msg_ts, quote_msg_ts, quote_author_e164}` and reads `signal_outbound.signal_msg_ts` for inbound resolution. Per `[[feedback_unit_tests_dont_catch_wiring]]`, ship the producer schema in one atomic plan so no later plan finds itself blocked on a missing column.

Output: schema migrations committed in `outbound-db.js` + `capture-db.js`; unit tests assert idempotency and column presence.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/50-signal-native-quote-threading/50-CONTEXT.md
@src/agents/alerter/src/outbound-db.js
@src/agents/alerter/src/capture-db.js

<interfaces>
Existing outbound-db.js initDb pattern (lines 1-50 area):
  CREATE TABLE IF NOT EXISTS signal_outbound ( ... )
  ALTER TABLE signal_outbound ALTER COLUMN related_capture_id TYPE text
  ALTER TABLE signal_outbound ALTER COLUMN related_draft_id TYPE text
  CREATE INDEX IF NOT EXISTS idx_signal_outbound_tenant_sent ON signal_outbound(tenant_id, sent_at DESC)
  CREATE INDEX IF NOT EXISTS idx_signal_outbound_recipient_sent ON signal_outbound(recipient_e164, sent_at DESC)
  CREATE INDEX IF NOT EXISTS idx_signal_outbound_intent ON signal_outbound(intent)
Extend: one `ALTER TABLE ADD COLUMN IF NOT EXISTS signal_msg_ts bigint` plus the partial index.

Existing capture-db.js initDb pattern (lines 32-44 area):
  ALTER TABLE signal_capture ADD COLUMN IF NOT EXISTS group_id text
  ALTER TABLE signal_capture ADD COLUMN IF NOT EXISTS farmos_person text
  ALTER TABLE signal_capture ADD COLUMN IF NOT EXISTS reply_target_kind text
  ALTER TABLE signal_capture ADD COLUMN IF NOT EXISTS input_tokens int
  ... (the established additive pattern)
Extend: three `ALTER TABLE ADD COLUMN IF NOT EXISTS` calls in the same block.

Index shape (CONTEXT D-02):
  CREATE INDEX IF NOT EXISTS idx_signal_outbound_msg_ts
  ON signal_outbound (signal_msg_ts)
  WHERE signal_msg_ts IS NOT NULL;

Test harness:
  Existing outbound-db.test.js and capture-db.test.js (or sibling boot tests) drive a pg-mem or jest-pg pool through initDb and assert schema. Mirror whichever harness those files already use; do NOT introduce a new test framework.

NOT in scope for this plan:
  - Modifying insertOutbound / insertCapture signatures (Plans 02 + 04 do that).
  - Backfilling historical rows (CONTEXT explicit: only new rows from Plan 02 onward).
  - Writing to the new columns (no producer yet; tests insert with the columns NULL and assert that path still succeeds).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add signal_outbound.signal_msg_ts column + partial index</name>
  <read_first>
    - src/agents/alerter/src/outbound-db.js (entire initDb function)
    - src/agents/alerter/test/outbound-db.test.js (existing migration test style; if absent, scan for whatever harness invokes initDb against a pool and asserts column existence)
  </read_first>
  <files>
    src/agents/alerter/src/outbound-db.js (modify initDb)
    src/agents/alerter/test/outbound-db.test.js (extend; create if no sibling boot test exists)
  </files>
  <behavior>
    - After initDb runs, information_schema.columns for signal_outbound includes signal_msg_ts bigint NULL.
    - After initDb runs, pg_indexes shows idx_signal_outbound_msg_ts present.
    - Running initDb twice does not error.
    - Existing insertOutbound calls that do NOT supply signal_msg_ts still succeed (column defaults NULL).
  </behavior>
  <action>
    In outbound-db.js initDb, after the existing CREATE INDEX block, append two idempotent statements (order: column first, index second):
      ALTER TABLE signal_outbound ADD COLUMN IF NOT EXISTS signal_msg_ts bigint
      CREATE INDEX IF NOT EXISTS idx_signal_outbound_msg_ts ON signal_outbound (signal_msg_ts) WHERE signal_msg_ts IS NOT NULL
    Match the existing call style (await pool.query in sequence; no transaction wrapper; no comments beyond a one-line marker referencing Phase 50 D-02). Do NOT modify insertOutbound's parameter list or INSERT statement -- Plan 02 owns that. Extend the test file with a case that: (a) calls initDb twice, (b) asserts the column appears in pg_attribute (or information_schema.columns), (c) asserts the partial index appears in pg_indexes with WHERE clause present, (d) inserts a row via insertOutbound omitting signal_msg_ts and confirms the row lands with signal_msg_ts NULL. If the existing test file uses a fixture pool helper, reuse it; do not introduce a new harness.
  </action>
  <verify>
    <automated>cd src/agents/alerter && npx jest test/outbound-db.test.js --no-coverage</automated>
  </verify>
  <done>signal_outbound.signal_msg_ts column + partial index added idempotently; tests green; insertOutbound back-compat preserved.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add signal_capture.{signal_msg_ts, quote_msg_ts, quote_author_e164} columns</name>
  <read_first>
    - src/agents/alerter/src/capture-db.js (entire initDb function; pay attention to the additive ALTER block lines 32-44 area)
    - src/agents/alerter/test/capture-db.test.js (existing migration test style; if absent, scan for the harness used by neighbouring tests)
  </read_first>
  <files>
    src/agents/alerter/src/capture-db.js (modify initDb)
    src/agents/alerter/test/capture-db.test.js (extend; create if no sibling boot test exists)
  </files>
  <behavior>
    - After initDb runs, information_schema.columns for signal_capture includes signal_msg_ts bigint NULL, quote_msg_ts bigint NULL, quote_author_e164 text NULL.
    - Running initDb twice does not error.
    - Existing insertCapture calls that do NOT supply the new columns still succeed (columns default NULL).
  </behavior>
  <action>
    In capture-db.js initDb, append three idempotent ALTER statements after the existing ALTER block (the one that ends with extraction_gate VARCHAR(32)). Use the established style:
      ALTER TABLE signal_capture ADD COLUMN IF NOT EXISTS signal_msg_ts bigint
      ALTER TABLE signal_capture ADD COLUMN IF NOT EXISTS quote_msg_ts bigint
      ALTER TABLE signal_capture ADD COLUMN IF NOT EXISTS quote_author_e164 text
    No new index needed (CONTEXT does not require it; quote_msg_ts is looked up FROM signal_outbound, not into signal_capture). Do NOT modify insertCapture's parameter list or INSERT statement -- Plan 04 owns that. Extend the capture-db test (or sibling) with a case that: (a) calls initDb twice, (b) asserts all three columns appear in pg_attribute, (c) inserts a capture via insertCapture omitting the new fields and confirms the row lands with the three columns NULL. Reuse the existing pool harness.
  </action>
  <verify>
    <automated>cd src/agents/alerter && npx jest test/capture-db.test.js --no-coverage</automated>
  </verify>
  <done>Three signal_capture columns added idempotently; tests green; insertCapture back-compat preserved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Boot-time schema migration on alerter container start | Idempotent ALTERs limit blast radius; runs against prod timescale on next deploy |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-50-01-01 | Tampering | initDb runs every alerter boot | mitigate | IF NOT EXISTS on column + index; idempotency unit-tested in both tasks |
| T-50-01-02 | DoS | New partial index slows insertOutbound | accept | Partial WHERE signal_msg_ts IS NOT NULL keeps the index small; signal_outbound write volume is modest (no hypertable) |
| T-50-01-03 | Information disclosure | quote_author_e164 stores a phone number | accept | Same posture as existing signal_capture.sender (already an e164); no new exposure surface |
| T-50-01-SC | Tampering | No new npm packages | accept | Plan adds zero deps |
</threat_model>

<verification>
- `npx jest test/outbound-db.test.js` green (idempotency + column + index assertions)
- `npx jest test/capture-db.test.js` green (idempotency + 3 column assertions)
- `grep -c "signal_msg_ts" src/agents/alerter/src/outbound-db.js` returns at least 2 (ALTER + index)
- `grep -c "signal_msg_ts\|quote_msg_ts\|quote_author_e164" src/agents/alerter/src/capture-db.js | grep -v '^#'` shows the three ALTER statements present
- No changes to insertOutbound / insertCapture signatures
</verification>

<success_criteria>
Schema primitives for Phase 50 land. Plan 02 can write signal_outbound.signal_msg_ts. Plan 04 can write the three signal_capture columns and resolve quotes via idx_signal_outbound_msg_ts. Boot-time migration is idempotent across redeploys.
</success_criteria>

<output>
Create `.planning/phases/50-signal-native-quote-threading/50-01-SUMMARY.md` recording: (1) the four new column names + table mappings, (2) the partial index definition, (3) idempotency test results, (4) the explicit non-change to insertOutbound / insertCapture (Plans 02 + 04 own those edits).
</output>
