---
phase: 50-signal-native-quote-threading
plan: 05
type: execute
wave: 4
depends_on: ['50-01', '50-02', '50-03', '50-04']
files_modified:
  - .planning/phases/50-signal-native-quote-threading/50-LIVE-FIRE.md
autonomous: false
requirements: [QUOT-01, QUOT-02, QUOT-03, QUOT-04, QUOT-05, QUOT-06]
must_haves:
  truths:
    - "50-LIVE-FIRE.md exists in the phase dir mirroring 45/47/48 live-fire shape (Prerequisites + numbered Operator steps + Deviation policy + empty Result stub + Cross-references)"
    - "Runbook walks the operator through: (1) deploy the four code plans to prod alerter, (2) trigger a fresh commit_failed by sending the bot a no-asset-ref capture, (3) observe the resulting commit_outcome_ack arriving in Signal as a QUOTE-REPLY visually attached to the original capture (screenshot evidence), (4) farmer quote-replies 'EDIT block 260415_LIMA_1' to the ack, (5) verify the system routes EDIT to the EXACT quoted draft (not most-recent-active) via timescale + alerter logs, (6) verify the polite-terminal-ack path by quote-replying an already-committed draft, (7) verify the numbered ask-back path by manually engineering >1 active drafts and sending a no-quote EDIT, (8) attest QUOT-01..06 in the Result section"
    - "Runbook uses ASCII only -- no em-dashes anywhere"
    - "Every command is operator-runnable verbatim: full psql queries, full signal-cli inspections, full alerter log grep patterns, no placeholder env vars not documented in Prerequisites"
    - "Deviation policy is explicit: any deviation FAILS the gate and opens a follow-up phase; do NOT silently patch in Phase 50"
    - "Result section is empty and ready for the operator to fill (timestamp, operator, screenshot path, draft UUIDs touched, lineage logs, QUOT-01..06 per-requirement verdicts, cleanup outcome, deviations, overall verdict)"
  artifacts:
    - path: .planning/phases/50-signal-native-quote-threading/50-LIVE-FIRE.md
      provides: "Operator-driven live-fire runbook + QUOT-01..06 attestation slots"
  key_links:
    - from: 50-LIVE-FIRE.md Step 3 (visual observation)
      to: farmer's Signal client (DM thread with bot)
      via: "manual screenshot showing ack rendered as quote-reply attached to the original capture bubble"
      pattern: "screenshot"
    - from: 50-LIVE-FIRE.md Step 5 (routing verification)
      to: signal_draft + signal_capture tables on prod timescale
      via: "psql SELECT correlating capture.quote_msg_ts -> outbound.signal_msg_ts -> draft.id; alerter log line tagged with the resolved draft id"
      pattern: "findDraftByQuotedMsgTs"
---

<objective>
Live-fire ship-gate for Phase 50. Three hermetic unit-test suites (Plans 02, 03, 04) prove the mechanism in isolation; this plan proves it end-to-end against prod signal-cli + prod timescale + a real farmer-on-phone in the loop. Per `[[feedback_unit_tests_dont_catch_wiring]]` and the Phase 47/48/49 precedent, the live-fire IS the ship-gate -- unit-test passage alone does not close this phase.

Purpose: the visual confirmation on the farmer's Signal client is the single irreducible signal that the mechanism works as designed. The /v2/send spike proved signal-cli accepts the quote payload; live-fire proves the farmer's client renders it as the expected quote bubble + that an inbound quote round-trips through receive-loop -> capture-db -> findDraftByQuotedMsgTs -> the correct draft. Plus the two corner cases (terminal-draft polite ack, >1-active numbered ask-back) get manual exercise.

Autonomous: false -- Task 1 is `checkpoint:human-verify` for the runbook's structural review (operator sanity-checks commands before execution); Task 2 is operator-deferred live execution that lands as a Result-section amendment to 50-LIVE-FIRE.md post-merge.

Output: 50-LIVE-FIRE.md committed; operator runs the gate; QUOT-01..06 attestation amends the Result section.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/50-signal-native-quote-threading/50-CONTEXT.md
@.planning/phases/50-signal-native-quote-threading/50-01-SUMMARY.md
@.planning/phases/50-signal-native-quote-threading/50-02-SUMMARY.md
@.planning/phases/50-signal-native-quote-threading/50-03-SUMMARY.md
@.planning/phases/50-signal-native-quote-threading/50-04-SUMMARY.md
@.planning/phases/47-multi-source-extraction-fusion-groups-shape-inoc-draft/47-LIVE-FIRE.md
@.planning/phases/48-session-entity-per-bag-commit-fan-out-session-shaped-confirm/48-LIVE-FIRE.md
@.planning/phases/49-real-session-eval-corpus-may-22-ship-gate-reprocess/49-SHIP-GATE.md

<interfaces>
Structural template (mirror 47-LIVE-FIRE.md + 48-LIVE-FIRE.md + 49-SHIP-GATE.md):
  # Phase 50 Live-Fire -- Signal-native quote threading
  **Status:** OPERATOR-DEFERRED until prod deploy + manual exercise
  **Hermetic ship-gate:** PASS (npx jest test/ green across Plans 01-04)
  **Operator runbook last revised:** <date the runbook was authored>
  ## Why operator-deferred
    One paragraph citing [[feedback_unit_tests_dont_catch_wiring]] + the visual-on-phone requirement.
  ## Prerequisites (numbered)
    1. Plans 50-01..04 merged + deployed to prod alerter (docker-compose up -d --build alerter from repo root)
    2. signal-cli REST 0.14.2+ on the bot host (spike-verified version)
    3. Bot phone (+59891840205) reachable via Signal
    4. Farmer phone (Santi's, per project memory) authed in the bot's signal-cli store
    5. PG_PROD_CONN_STRING env var set to prod timescale
    6. Operator has SSH to elder-plops (alerter logs) + can read .planning/farmer-language-stacks for the test farmer's UI language (Santi: en/es/fr; test in English)
    7. Phase 47/48/49 ship-gates already green (so we know commit_outcome_ack path is reachable end-to-end)
    8. Plan 50-04 numbered-ask-back side_effect wired through outbound-confirm (Plan-04 executor decision; runbook documents the exact intent label)
  ## Operator steps (10 numbered)
    Step 1: Sanity -- hermetic suite still green
      cd src/agents/alerter && npx jest test/ --no-coverage
      Expected: full suite green.
    Step 2: Deploy + smoke
      Pull latest, docker compose up -d --build alerter, tail logs for "initDb" + "schema migration: signal_msg_ts" + "schema migration: quote_msg_ts".
      docker logs alerter 2>&1 | tail -100 | grep -E "(initDb|signal_msg_ts|quote_msg_ts)"
      Expected: idempotent ALTER messages OR clean boot (no errors).
    Step 3: Trigger a fresh commit_failed
      Send a capture to the bot that will fail commit. Options:
        (a) Send a Signal message with "harvest of nothing" (no quantity, no parent ref) -- the LLM extracts an observation with missing target -> commit_failed.
        (b) Reuse the no-asset-ref pattern from Phase 45 live-fire (45-05-SUMMARY).
      Wait for the commit_outcome_ack to arrive on Santi's phone.
      Expected: ack arrives within ~30s; ack is RENDERED AS A SIGNAL QUOTE-REPLY visually attached to the original capture bubble.
      Operator action: take a screenshot. Save it as 50-LIVE-FIRE_ack-quote.png alongside the runbook.
    Step 4: Verify outbound persistence
      psql "$PG_PROD_CONN_STRING" -c "SELECT id, signal_msg_ts, intent, related_draft_id, sent_at FROM signal_outbound WHERE intent='commit_outcome_ack' ORDER BY sent_at DESC LIMIT 5;"
      Expected: top row has signal_msg_ts NOT NULL (this is what the farmer will quote back to in Step 5).
      Attest QUOT-01.
    Step 5: Farmer quote-reply EDIT
      On Santi's phone, long-press the ack bubble in Signal, tap Reply, type "EDIT block 260415_LIMA_1" (or whichever real block exists at test time), send.
      Wait ~10s for processing.
      Expected: alerter logs show findDraftByQuotedMsgTs hit + the EDIT routes to the specific draft from Step 3, not to the most-recent-active (verify by comparing draft.id in the log vs the draft.id from Step 4's related_draft_id).
      docker logs alerter 2>&1 | tail -100 | grep -E "(findDraftByQuotedMsgTs|quote_msg_ts|routing)"
      psql "$PG_PROD_CONN_STRING" -c "SELECT id, quote_msg_ts, quote_author_e164, sender, raw_text FROM signal_capture ORDER BY captured_at DESC LIMIT 3;"
      Attest QUOT-02 (capture wrote quote_msg_ts), QUOT-03 (routing matched the quoted draft), QUOT-04 (the original ack carried the quote payload).
    Step 6: Verify the polite-terminal-ack path
      Wait for the EDIT from Step 5 to land + commit successfully (or operator commits via YES). The draft is now in 'committed' status.
      On Santi's phone, quote-reply that same ack again with "NO" (or any text).
      Expected: bot responds with the polite "that one is already closed" ack; the draft stays committed; no second commit attempt fires.
      docker logs alerter 2>&1 | tail -50 | grep -E "(quoted_terminal|already.closed)"
      psql verification: draft.status remains 'committed'; no new signal_draft row mutation.
      Attest QUOT-03 polite-terminal branch.
    Step 7: Verify the numbered ask-back fallback (manual >1-active engineering)
      Engineer two simultaneously-active drafts:
        (a) Send capture A to the bot, wait for confirm prompt, do NOT YES/NO yet.
        (b) Send capture B to the bot, wait for confirm prompt, do NOT YES/NO yet.
        Both drafts are now status=awaiting_farmer.
      Send a no-quote EDIT to the bot: "EDIT something" (plain text, not a Signal quote-reply).
      Expected: bot responds with numbered ask-back: "Which one are you replying about?\n1. {date} {log_type} (...)\n2. {date} {log_type} (...)\nReply with the number, or quote the original message." No draft is mutated this turn.
      Attest QUOT-06.
      Cleanup: send YES or NO to both drafts to clear them.
    Step 8: Verify fail-open posture
      Engineer a draft whose source_capture is intentionally missing signal_msg_ts (e.g. manually UPDATE signal_capture SET signal_msg_ts = NULL WHERE id = '<some capture id>' for a fresh draft).
      Trigger a commit_outcome_ack for that draft.
      Expected: ack still fires; alerter logs warn "no quote target for draft=..."; ack body is unquoted but contains the Plan-06 disambiguator template; farmer still receives a useful ack.
      Attest QUOT-05.
    Step 9: Append result
      Fill the Result section of 50-LIVE-FIRE.md:
        - Date, Operator
        - Screenshot file references (Step 3, Step 7)
        - draft UUIDs touched (Step 3-7)
        - psql output excerpts (signal_outbound row, signal_capture row, draft.status)
        - Per-requirement verdict: QUOT-01 PASS/FAIL, QUOT-02 PASS/FAIL, ..., QUOT-06 PASS/FAIL
        - Deviations observed
        - Overall verdict
    Step 10: Cleanup
      No farmOS asset cleanup needed (Phase 50 does not write to farmOS). Drafts touched in steps 3-7 either committed legitimately or got discarded; signal_capture rows are persistent (no cleanup). The ack screenshots stay attached to the runbook as evidence.
  ## Deviation policy
    Any failure of QUOT-01..06 FAILS the gate. The runbook is amended with the failure mode; a follow-up phase is opened (Phase 50.x) to address it. Do NOT silently patch in Phase 50.
    Common failure modes to be alert for (drawn from spike + memory):
      - signal-cli responds 4xx on quote-bearing /v2/send -> indicates version drift; spike pinned 0.14.2.
      - Signal client (Android vs iOS) renders the quote as "Original message" rather than full bubble -> CONTEXT acknowledges this; not a failure if the message_id round-trips correctly, BUT operator should screenshot both clients if available.
      - Numbered ask-back fires when only 1 active draft -> regression of QUOT-06; FAIL.
      - findDraftByQuotedMsgTs returns most-recent-active when a quote target IS present -> regression of QUOT-03; FAIL.
  ## Result (empty stub for operator)
    ### Run <N>
    Date:
    Operator:
    Elapsed:
    Screenshots:
    Drafts touched:
    Captures touched:
    Per-requirement verdict:
      QUOT-01:
      QUOT-02:
      QUOT-03:
      QUOT-04:
      QUOT-05:
      QUOT-06:
    Deviations:
    Verdict:
  ## Files
    src/agents/alerter/src/{signal.js, outbound-db.js, capture-db.js, receive-loop.js}
    src/agents/alerter/src/confirm/{confirm-db.js, edit-handler.js, outbound-confirm.js}
    src/agents/alerter/test/...
  ## Cross-references
    .planning/phases/45-.../45-05-SUMMARY.md (the live-fire that surfaced this whole problem)
    .planning/phases/47-.../47-LIVE-FIRE.md (live-fire shape reference)
    .planning/phases/48-.../48-LIVE-FIRE.md (live-fire shape reference)
    .planning/phases/49-.../49-SHIP-GATE.md (operator-deferred runbook reference)
    50-CONTEXT.md
    Memory: [[feedback_unit_tests_dont_catch_wiring]], [[feedback_no_silent_failure_after_farmer_confirm]], [[feedback_verify_signal_send_attribution]]

ASCII discipline (CONTEXT carried policy):
  no em-dashes (U+2014), no en-dashes (U+2013), no horizontal-bar (U+2015)
  use '--' for inline separators
  use 'n/a' or '?' for placeholders
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author 50-LIVE-FIRE.md</name>
  <read_first>
    - .planning/phases/48-session-entity-per-bag-commit-fan-out-session-shaped-confirm/48-LIVE-FIRE.md (closest structural template)
    - .planning/phases/47-multi-source-extraction-fusion-groups-shape-inoc-draft/47-LIVE-FIRE.md (paper-trail precedent)
    - .planning/phases/49-real-session-eval-corpus-may-22-ship-gate-reprocess/49-SHIP-GATE.md (the operator-deferred shape)
    - .planning/phases/45-north-star-commit-failed-ack-replay-outstanding-silent-failu/45-05-SUMMARY.md (the failure mode this phase closes)
  </read_first>
  <files>
    .planning/phases/50-signal-native-quote-threading/50-LIVE-FIRE.md (new)
  </files>
  <behavior>
    - Mirror 48-LIVE-FIRE.md / 49-SHIP-GATE.md structure section-for-section per the <interfaces> outline above.
    - Every command is verbatim-runnable: full psql query strings, full docker logs invocations, full signal-cli commands, full file paths.
    - All env vars referenced in commands appear in Prerequisites.
    - Step 3 explicitly tells the operator to take a phone screenshot.
    - Step 5 cites the specific log message tags ("findDraftByQuotedMsgTs", "quote_msg_ts") that Plan 04 emits.
    - Step 6 cites the polite-terminal log message tag (Plan 04 emits 'quoted_terminal' or similar; runbook references the actual string the executor used).
    - Step 7 includes the exact draft-engineering sequence for >1-active state + the expected ask-back body text shape.
    - Step 8 includes the UPDATE statement for engineering the NULL-signal_msg_ts case + cleans up after.
    - Deviation policy section enumerates the 4 common failure modes from <interfaces>.
    - Result section is an empty stub matching 48-LIVE-FIRE.md's shape.
    - Cross-references include the four memory tags and the four sibling runbooks.
    - ASCII only; no em-dashes; no en-dashes.
  </behavior>
  <action>
    1. Open 48-LIVE-FIRE.md as the structural template. Copy section headers + adapt content per the <interfaces> outline.
    2. Author Prerequisites covering all 8 items.
    3. Author Steps 1-10 per the <interfaces> contract. Include the exact psql query strings, docker log grep patterns, and signal-cli inspection commands. Where a log message tag is referenced, use the tag string the Plan-04 executor actually wrote (consult 50-04-SUMMARY -- if the SUMMARY lists slightly different tag strings than this plan anticipated, MATCH THE SUMMARY).
    4. Author the Deviation policy section per <interfaces>.
    5. Author the empty Result stub (mirroring 48-LIVE-FIRE.md's shape).
    6. Author Files + Cross-references sections.
    7. ASCII-grep the file: confirm no U+2013 / U+2014 / U+2015 anywhere. Replace any em-dashes that snuck in with `--`.
  </action>
  <verify>
    <automated>test -f .planning/phases/50-signal-native-quote-threading/50-LIVE-FIRE.md && python3 -c "import sys; d=open('.planning/phases/50-signal-native-quote-threading/50-LIVE-FIRE.md').read(); sys.exit(1 if any(c in d for c in ['–','—','―']) else 0)" && grep -c "QUOT-0" .planning/phases/50-signal-native-quote-threading/50-LIVE-FIRE.md</automated>
  </verify>
  <done>50-LIVE-FIRE.md committed; ASCII-only; 10 operator steps with full commands; QUOT-01..06 attestation slots present; Result stub empty + ready for operator.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Operator runbook sanity review</name>
  <what-built>
    50-LIVE-FIRE.md drafted with 10 operator steps covering deploy + commit_failed trigger + visual quote-ack observation + inbound quote round-trip + polite-terminal branch + numbered ask-back branch + fail-open posture. QUOT-01..06 attestation slots present. Result section empty + ready for operator to fill post-execution.
  </what-built>
  <how-to-verify>
    1. Read 50-LIVE-FIRE.md end-to-end. Confirm:
       - Prerequisites cover everything needed before kicking off (alerter deployed, PG conn string, signal-cli version, farmer phone reachable, Plans 47/48/49 ship-gates green).
       - Step 2 deploy command matches the alerter compose pattern from project memory ([[project_openmct_dashboard]]: docker-compose up -d --build).
       - Step 3 trigger pattern (no-asset-ref capture) is realistic + matches Phase 45's failure-mode generator.
       - Step 4 psql query correctly inspects signal_outbound.signal_msg_ts.
       - Step 5 docker logs grep tags match what Plan 04 actually emits (cross-check against 50-04-SUMMARY).
       - Step 6 polite-terminal path works as described (cross-check 50-04 routing).
       - Step 7 >1-active engineering sequence is operator-runnable (the two-capture-no-YES dance).
       - Step 8 NULL-signal_msg_ts engineering UPDATE is safe + reversible.
       - Step 9 Result stub captures everything the operator will need to attest.
       - Step 10 cleanup is honest about "no cleanup needed beyond what farmer already did" (no farmOS cleanup unlike 48-LIVE-FIRE).
       - Deviation policy is explicit on the 4 common failure modes.
       - No em-dashes anywhere (the automated grep already confirmed; eyeball spot-check).
    2. If any command would not run verbatim (missing env var documentation, wrong log-tag string, typo in psql), flag it and the executor revises before merge.
    3. The actual live-fire execution itself is operator-deferred; this checkpoint only reviews the RUNBOOK, not the execution.
  </how-to-verify>
  <resume-signal>Type "runbook approved" or describe the revisions needed</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator-driven live-fire against prod signal-cli + prod timescale | Manual + audited; no farmOS writes in Phase 50 so blast radius is bounded to signal-cli outbound + signal_capture rows |
| Real-farmer phone in the loop (Santi) | Per [[user_santi_radicheta_farmer1_trinity]], farmer1 is the operator -- no third-party PII exposure |
| Numbered ask-back manual engineering exposes >1 active drafts | Drafts are Santi's own; same-user data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-50-05-01 | Tampering | Live-fire creates messy signal_draft / signal_outbound rows on prod | accept | Drafts get committed or discarded as part of the runbook; no orphan state |
| T-50-05-02 | Information disclosure | Screenshots of Signal client could include other unrelated chat content | mitigate | Runbook tells operator to screenshot ONLY the bot-thread bubble + crop before saving to repo |
| T-50-05-03 | Repudiation | Live-fire results not paper-trailed | mitigate | Step 9 mandates Result section append with per-requirement verdict + screenshots + psql output |
| T-50-05-04 | DoS | Engineered >1-active state confuses farmer if not cleaned up | mitigate | Step 7 explicitly cleans up by YES/NO-ing both engineered drafts |
| T-50-05-SC | Tampering | No new npm packages | accept | Plan adds zero deps |
</threat_model>

<verification>
- `test -f .planning/phases/50-signal-native-quote-threading/50-LIVE-FIRE.md`
- Python ASCII check on 50-LIVE-FIRE.md returns no Unicode dashes (U+2013/2014/2015)
- `grep -c "QUOT-0" 50-LIVE-FIRE.md` returns >= 6 (all six requirements referenced)
- `grep -c "findDraftByQuotedMsgTs" 50-LIVE-FIRE.md` returns >= 1
- `grep -c "signal_msg_ts" 50-LIVE-FIRE.md` returns >= 2
- `grep -c "numbered ask-back\|ask_back" 50-LIVE-FIRE.md` returns >= 1
- Task 2 checkpoint resolved with "runbook approved"
- Post-merge, operator executes the runbook + appends a Result section that records QUOT-01..06 PASS verdicts (this is the actual ship-gate; lands as a future commit, not in Phase 50 directly)
</verification>

<success_criteria>
50-LIVE-FIRE.md committed + runbook-approved by operator review. The live-fire execution itself is operator-deferred and lands as a Result-section amendment post-merge. QUOT-01..06 attestation is the closing artifact for Phase 50; any deviation observed during the live ship-gate opens a Phase 50.x follow-up rather than silent patching.
</success_criteria>

<output>
Create `.planning/phases/50-signal-native-quote-threading/50-05-SUMMARY.md` recording: (1) 50-LIVE-FIRE.md committed + section-by-section structure, (2) the explicit checkpoint:human-verify gate resolved with runbook-approved verdict, (3) the deferred-execution note (operator runs the gate post-merge; Result lands as amendment), (4) the four common failure modes the runbook calls out, (5) QUOT-01..06 attestation status (READY pending operator execution).
</output>
