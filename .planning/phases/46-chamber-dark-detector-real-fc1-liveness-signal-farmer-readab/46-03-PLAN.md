---
phase: 46-chamber-dark-detector
plan: 03
type: execute
wave: 2
depends_on: [46-01, 46-02]
files_modified: []
autonomous: false
requirements: [CD-01, CD-02, CD-03, CD-04]
must_haves:
  truths:
    - "Bridge and alerter containers are rebuilt together on elder-plops (atomic deploy per CONTEXT.md)"
    - "GET /health on the running bridge returns a fc1 block with last_msg_ts (number) and last_msg_age_sec (number)"
    - "Simulating fc1 silence (or restarting fc-core) drives the alerter to FIRING for perType.pi within piOfflineMin"
    - "While pi is FIRING, no per-sensor (scd41/sht30/rh/humidifier) Signal send is logged"
    - "When fc1 publishes again, alerter pi clears and per-sensor evaluation resumes"
  artifacts:
    - path: ".planning/phases/46-chamber-dark-detector-real-fc1-liveness-signal-farmer-readab/46-03-SMOKE.md"
      provides: "Smoke verification log: timestamps + curl outputs + docker logs evidence"
      contains: "fc1.last_msg_age_sec"
  key_links:
    - from: "docker-compose up -d --build bridge alerter"
      to: "Both containers up with new schema"
      via: "atomic rebuild on elder-plops"
      pattern: "Recreating mushy-bridge.*Recreating mushy-alerter"
    - from: "curl http://localhost:3000/health"
      to: "fc1.last_msg_age_sec exposed"
      via: "additive /health field from plan 46-01"
      pattern: "last_msg_age_sec"
---

<objective>
Atomic coordinated rebuild of bridge + alerter on elder-plops (dev=prod), followed by a smoke verification that confirms the 2026-05-20 outage scenario would now fire a single chamber-dark Signal alert instead of N per-sensor alerts. Closes all four CD-01..CD-04 by attestation.

Purpose: Plans 46-01 and 46-02 ship green tests. This plan is the deploy + observe step. Per CONTEXT.md `code_context.Integration Points`, bridge alone or alerter alone is incomplete (alerter alone could crash on null without the new bridge field); both must rebuild in a single `docker-compose up -d --build bridge alerter` command.

Output: `46-03-SMOKE.md` captures curl, docker logs, and the timeline of the controlled chamber-dark simulation. Farmer-facing Signal message format is attested.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/46-chamber-dark-detector-real-fc1-liveness-signal-farmer-readab/46-CONTEXT.md
@.planning/debug/alerter-co2-only-not-pi.md

<interfaces>
Deploy target: elder-plops (dev = prod, per `[[project_elder_plops_dual_role]]`).
Compose entry point: repo-root `docker-compose.yml` + `docker-compose.override.yml` (per CLAUDE.md).
Rebuild command: `docker-compose up -d --build bridge alerter`.
Bridge `/health` reachable at `http://localhost:3000/health` from elder-plops (per existing bridge config).
Alerter logs: `docker logs --tail 200 mushy-alerter-1` (per debug session pattern).
Live fc1 connectivity is via the wg0 tunnel; current outage simulation is achieved by restarting fc-core on fc1 (per `[[feedback_fc1_remote_action_preflight_protocol]]`) OR by pausing the bridge subscriptions briefly (less invasive).

ALERT_PI_OFFLINE_MIN is 10 (per debug session "Config during outage"). To keep this smoke under ~12 min, the executor MAY temporarily override `ALERT_PI_OFFLINE_MIN=1` in `docker-compose.override.yml` for the duration of the smoke, then restore to 10 and rebuild. Document the override in the smoke log.

Pre-flight per `[[feedback_fc1_remote_action_preflight_protocol]]`: confirm fc1 is reachable and chamber is in a benign state (not mid-flush) BEFORE inducing silence. If chamber is in fruiting and farmer is offline, defer this checkpoint and DO NOT induce real fc-core restart -- instead only assert /health exposes the field and tests are green; full live attestation deferred to operator window.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Atomic rebuild + /health schema verification</name>
  <files>.planning/phases/46-chamber-dark-detector-real-fc1-liveness-signal-farmer-readab/46-03-SMOKE.md</files>
  <read_first>
    - .planning/phases/46-chamber-dark-detector-real-fc1-liveness-signal-farmer-readab/46-CONTEXT.md (constraints block, code_context.Integration Points)
    - CLAUDE.md (Build System + Running Services -- the docker-compose pattern at the repo root)
    - .planning/phases/46-chamber-dark-detector-real-fc1-liveness-signal-farmer-readab/46-01-PLAN.md (the /health.fc1 schema being verified)
    - .planning/phases/46-chamber-dark-detector-real-fc1-liveness-signal-farmer-readab/46-02-PLAN.md (the alerter consumer schema)
    - docker-compose.yml and docker-compose.override.yml (at repo root)
  </read_first>
  <action>
    On elder-plops repo root, run `docker-compose up -d --build bridge alerter`. Capture `docker-compose ps` showing both containers `Up (healthy)`. Then curl `http://localhost:3000/health` and pipe through `jq` to confirm the response has a `fc1` block with `last_msg_ts` (number-or-null) and `last_msg_age_sec` (number-or-null). Wait 30s and curl again -- `last_msg_age_sec` should increase if no fc1 messages arrived in the interval, or be near 0 if fc1 is publishing live. Record all output verbatim in `46-03-SMOKE.md` with timestamps. Do NOT proceed to task 2 if curl shows the field missing -- that means plan 46-01 did not deploy correctly.
  </action>
  <verify>
    <automated>curl -s http://localhost:3000/health | jq -e '.fc1 | has("last_msg_ts") and has("last_msg_age_sec")'</automated>
  </verify>
  <acceptance_criteria>
    - `46-03-SMOKE.md` exists with sections "Rebuild" and "Health Schema Verification"
    - `docker-compose ps` output in SMOKE.md shows mushy-bridge-1 AND mushy-alerter-1 with status `Up` (or `Up (healthy)`)
    - `curl http://localhost:3000/health | jq .fc1.last_msg_age_sec` returns a number or null (not undefined / not absent)
    - `curl http://localhost:3000/health | jq .fc1.last_msg_ts` returns a number or null
    - The rebuild command in SMOKE.md is exactly `docker-compose up -d --build bridge alerter` (single atomic command, per CONTEXT.md)
    - `docker logs --tail 50 mushy-alerter-1 2>&1 | grep -i 'crash\\|TypeError\\|undefined is not'` is empty (alerter consumes new field without crashing)
  </acceptance_criteria>
  <done>Bridge and alerter both running new images; /health schema present; no startup crashes; evidence logged in SMOKE.md.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Farmer/operator attestation of chamber-dark behaviour</name>
  <what-built>
    Bridge tracks `fc1LastMsgTs` aggregated across 9 fc1 topics; alerter consumes it as a third OR-trigger for `isPiOffline`; pi-alert message is now chamber-level ("FC-1 offline ?? no telemetry XXm. chamber uncontrolled. last RH XX% @ HH:MM."); per-sensor watchdogs are suppressed while pi is FIRING. All implemented per CONTEXT.md D-01..D-08.
  </what-built>
  <how-to-verify>
    Pre-flight (per `[[feedback_fc1_remote_action_preflight_protocol]]`):
    1. Confirm with farmer that the chamber is in a state where ~10-15 min of uncontrolled humidity is acceptable (typically not mid-fruiting; or operator window).
    2. Temporarily set `ALERT_PI_OFFLINE_MIN=1` in `docker-compose.override.yml` for the alerter service; rebuild alerter only: `docker-compose up -d --build alerter`. Confirm via `docker exec mushy-alerter-1 env | grep ALERT_PI_OFFLINE_MIN`.

    Induce chamber-dark:
    3. On fc1, `sudo systemctl stop fc-core.service` (or pull the ethernet between elder-plops and fc1 for ~2 min if SSH is unreliable -- never `ip link down` over SSH per `[[feedback_no_interface_down]]`).
    4. Wait ~90 seconds.

    Expected observations:
    5. `curl http://localhost:3000/health | jq .fc1.last_msg_age_sec` returns a number > 60.
    6. `docker logs --since 3m mushy-alerter-1 | grep -E 'pi|FC-1'` shows ONE pi FIRING transition and ONE Signal send containing the literal substring `FC-1 offline` and `chamber uncontrolled`.
    7. `docker logs --since 3m mushy-alerter-1 | grep -E 'scd41|sht30|rh.oob|humidifier.stuck' | grep -c 'sent'` is 0 (per-sensor alerts suppressed per D-07).
    8. Farmer receives ONE Signal message on +5...3012 with that chamber-level text (no em-dashes, rounded numbers).

    Recover:
    9. On fc1, `sudo systemctl start fc-core.service` (or reconnect cable).
    10. Within ~10 sec, `curl http://localhost:3000/health | jq .fc1.last_msg_age_sec` returns < 5.
    11. `docker logs --since 1m mushy-alerter-1` shows pi clearing.

    Restore production cooldown:
    12. Set `ALERT_PI_OFFLINE_MIN` back to 10 in `docker-compose.override.yml`. Rebuild alerter: `docker-compose up -d --build alerter`. Confirm env restored.

    Record everything in `46-03-SMOKE.md` under section "Induced Outage Attestation" with timestamps, curl outputs, docker log snippets, and the farmer's received Signal message verbatim (copy from farmer screenshot or message).

    DEFERRAL PATH: If chamber is in active fruiting and farmer is unavailable for ~15 min window, do NOT induce real outage. Instead, log under "Deferred Attestation" that schema verification (task 1) is complete + tests are green, and attestation of induced outage is deferred to an operator window. Update SMOKE.md accordingly.
  </how-to-verify>
  <resume-signal>
    Type "approved" to mark the phase shipped (or "deferred: <reason>" to defer induced-outage attestation but accept tests + schema verification as sufficient closure of CD-01..CD-04 pending operator window).
  </resume-signal>
</task>

</tasks>

<verification>
SMOKE.md contains all required sections. If induced-outage path was taken, the captured docker logs show exactly one `pi` FIRING transition and zero per-sensor Signal sends during the silence window. Farmer-received message matches D-05 / D-06 shape.
</verification>

<success_criteria>
- Both containers rebuilt atomically (single `docker-compose up -d --build bridge alerter`)
- /health.fc1 schema verified live
- Either: (a) induced-outage attestation shows ONE chamber-level Signal message + zero per-sensor alerts during silence, OR (b) explicit deferral logged with operator window scheduled
- ALERT_PI_OFFLINE_MIN restored to 10 if temporarily overridden
- Phase 46 closes CD-01..CD-04
</success_criteria>

<output>
Create `.planning/phases/46-chamber-dark-detector-real-fc1-liveness-signal-farmer-readab/46-03-SUMMARY.md` when done, linking to 46-03-SMOKE.md and noting whether the induced-outage path or deferred path was taken.
</output>
