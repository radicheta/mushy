---
phase: 46-chamber-dark-detector
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/agents/alerter/src/bridge-client.js
  - src/agents/alerter/src/rules.js
  - src/agents/alerter/src/state.js
  - src/agents/alerter/src/message.js
  - src/agents/alerter/test/rules.test.js
  - src/agents/alerter/test/state.test.js
  - src/agents/alerter/test/message.test.js
  - src/agents/alerter/test/bridge-client.test.js
autonomous: true
requirements: [CD-02, CD-03, CD-04]
must_haves:
  truths:
    - "Alerter receives fc1LastMsgTs through pollHealth -> onLiveness payload (D-02 consumer side)"
    - "D-03: isPiOffline returns true when fc1LastMsgTs is stale even though wsConnected===true && rosConnected===true; existing wsConnected and rosConnected triggers RETAINED"
    - "D-04: chamber-dark reuses piOfflineMin and criticalCooldownMin; no new env var introduced"
    - "perType.pi transitions to FIRING when a pi_liveness event carries a stale fc1LastMsgTs"
    - "D-05: formatProblem({alertType:'pi'}) renders a chamber-level message containing 'FC-1 offline' and 'chamber uncontrolled', using lastKnown RH/T/timestamp when present"
    - "D-06: When lastKnown is null, message falls back to 'no recent samples' without crashing"
    - "D-07: When perType.pi is FIRING, per-sensor watchdogs (sht30_fresh, scd41_fresh, RH-OOB, humidifier-stuck) are suppressed at evaluation time"
    - "D-08: Per-sensor alerts do NOT suppress chamber-dark (one-directional)"
  artifacts:
    - path: "src/agents/alerter/src/rules.js"
      provides: "isPiOffline third OR-trigger on stale fc1LastMsgTs"
      contains: "fc1LastMsgTs"
    - path: "src/agents/alerter/src/bridge-client.js"
      provides: "pollHealth + ws_close forward fc1LastMsgTs in onLiveness payload"
      contains: "fc1LastMsgTs"
    - path: "src/agents/alerter/src/state.js"
      provides: "pi_liveness handler stores fc1LastMsgTs in state and passes to isPiOffline; per-sensor suppression when perType.pi.state === FIRING"
      contains: "fc1LastMsgTs"
    - path: "src/agents/alerter/src/message.js"
      provides: "chamber-level formatProblem({alertType:'pi'}) using lastKnown payload"
      contains: "FC-1 offline"
    - path: "src/agents/alerter/test/rules.test.js"
      provides: "isPiOffline stale-fc1LastMsgTs test"
      contains: "fc1LastMsgTs"
    - path: "src/agents/alerter/test/state.test.js"
      provides: "pi_liveness drives perType.pi to FIRING; per-sensor suppression test"
      contains: "fc1LastMsgTs"
  key_links:
    - from: "src/agents/alerter/src/bridge-client.js pollHealth"
      to: "h.fc1.last_msg_ts"
      via: "onLiveness({ fc1LastMsgTs: h.fc1 ? h.fc1.last_msg_ts : null, ... })"
      pattern: "h\\.fc1"
    - from: "src/agents/alerter/src/state.js pi_liveness handler"
      to: "isPiOffline call"
      via: "fc1LastMsgTs passed through to isPiOffline args"
      pattern: "fc1LastMsgTs"
    - from: "perType.pi.state === FIRING"
      to: "per-sensor evaluators (sht30, scd41, rh, humidifier-stuck)"
      via: "suppression branch in evaluation loop"
      pattern: "perType\\.pi\\.state"
---

<objective>
Wire the bridge's new `fc1LastMsgTs` signal (from plan 46-01) into the alerter as the actual chamber-dark trigger, rewrite the `pi` alert message to be chamber-level, and suppress per-sensor noise while chamber-dark is firing. Implements CD-02, CD-03, and the alerter-side portion of CD-04.

Purpose: Today's `isPiOffline` keys off alerter<->bridge WS and a one-shot `rosReady` boot flag (see `.planning/debug/alerter-co2-only-not-pi.md`); neither reflects fc1 publisher liveness. This plan adds the third OR-trigger, retains the existing two (D-03), reuses existing `piOfflineMin` and `criticalCooldownMin` knobs (D-04), and converts the farmer-facing message from per-sensor "co2 sensor offline" to chamber-level "FC-1 offline ?? no telemetry XXm. chamber uncontrolled. last RH XX% @ HH:MM." (D-05, D-06).

Output: All tests-first commits land before plan 46-03 rebuilds. The 2026-05-20 outage scenario, replayed as a unit test, produces ONE pi-FIRING transition (D-07) instead of N per-sensor alerts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/46-chamber-dark-detector-real-fc1-liveness-signal-farmer-readab/46-CONTEXT.md
@.planning/debug/alerter-co2-only-not-pi.md

<interfaces>
Current `isPiOffline` signature (src/agents/alerter/src/rules.js:33):
```
isPiOffline({ wsConnected, rosConnected, nowMs, wsLastConnectedMs,
              rosDisconnectedSinceMs, config }) -> boolean
```
Extend with one additional optional arg: `fc1LastMsgTs` (number | null | undefined).
Behavior per D-03: third OR-trigger fires when `fc1LastMsgTs != null && (nowMs - fc1LastMsgTs) > config.piOfflineMin * 60000`. `undefined` (old caller) must NOT trigger (defence in depth for graceful degradation per CONTEXT.md `code_context.Integration Points`).

Current pollHealth onLiveness payload (src/agents/alerter/src/bridge-client.js:26-34):
```
onLiveness({ wsConnected, rosConnected, humidifierLastMsgTs, nowMs })
```
Extend with `fc1LastMsgTs: h.fc1 ? h.fc1.last_msg_ts : null`. The ws_close path at line 57-64 must mirror this using the cached `lastHealth.fc1.last_msg_ts`.

Current state.js pi handling (state.js:500-525):
- Already builds `lastKnown` payload (lines 513-520) -- ready to consume
- Already calls `isPiOffline({ wsConnected, rosConnected, nowMs, wsLastConnectedMs, rosDisconnectedSinceMs, config })`
- Add `fc1LastMsgTs: next.fc1LastMsgTs` to that arg object
- A second isPiOffline call exists in the `tick` case (line ~536) -- update that one identically

State machine: `perType.pi.state` enums include FIRING. Per-sensor evaluators in the same eval loop run for keys: sht30, scd41, rh, humidifierStuck (search `driveAlertType(next.perType.<key>` patterns). Per D-07 wrap their invocations in `if (next.perType.pi.state !== 'FIRING') { ... }`. Per D-08 do NOT add the inverse guard around the pi evaluator.

formatProblem signature (src/agents/alerter/src/message.js): called as `formatProblem({ alertType, ... })`. The pi branch currently emits a WS/ROS-flavoured message. Rewrite to:
- When `lastKnown != null`: `FC-1 offline ?? no telemetry <ageMin>m. chamber uncontrolled. last RH <fmtNum(lastKnown.rh)>% @ <HH:MM from lastKnown.tsMs>.`
- When `lastKnown == null`: `FC-1 offline ?? no telemetry <ageMin>m. chamber uncontrolled. no recent samples.`
- No em-dashes anywhere ([[feedback_no_em_dashes_in_artifacts]])
- Use existing `fmtNum` helper if present in message.js; otherwise 1-decimal with trailing .0 stripped per [[feedback_round_farmer_numbers]]
- `HH:MM` is the UTC `tsMs` rendered as zero-padded `HH:MM`
- `ageMin` = `Math.round((nowMs - lastKnown.tsMs)/60000)` if lastKnown, else from `lastSeenMs`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing tests for the new isPiOffline trigger, pi_liveness state transition, message rewrite, and per-sensor suppression</name>
  <files>
    src/agents/alerter/test/rules.test.js,
    src/agents/alerter/test/state.test.js,
    src/agents/alerter/test/message.test.js,
    src/agents/alerter/test/bridge-client.test.js
  </files>
  <read_first>
    - src/agents/alerter/test/rules.test.js (existing isPiOffline test patterns)
    - src/agents/alerter/test/state.test.js (existing pi_liveness drive patterns; find `perType.pi` tests)
    - src/agents/alerter/test/message.test.js (existing formatProblem patterns)
    - src/agents/alerter/test/bridge-client.test.js (existing pollHealth onLiveness fixture pattern)
    - src/agents/alerter/src/rules.js lines 26-45 (isPiOffline)
    - src/agents/alerter/src/state.js lines 490-560 (pi_liveness + tick handlers, lastKnown assembly)
    - src/agents/alerter/src/message.js (current formatProblem pi branch)
    - src/agents/alerter/src/bridge-client.js lines 20-65 (pollHealth + ws_close onLiveness)
    - .planning/phases/46-chamber-dark-detector-real-fc1-liveness-signal-farmer-readab/46-CONTEXT.md (D-03..D-08)
  </read_first>
  <behavior>
    rules.test.js -- ADD tests:
    - "isPiOffline returns true when fc1LastMsgTs is stale beyond piOfflineMin even though wsConnected===true && rosConnected===true"
    - "isPiOffline returns false when fc1LastMsgTs is fresh and both WS and ROS are connected"
    - "isPiOffline returns false when fc1LastMsgTs is undefined (old bridge / graceful degradation) and WS+ROS connected"
    - "isPiOffline still returns true for stale wsLastConnectedMs alone (existing trigger retained, D-03)"
    - "isPiOffline still returns true for rosDisconnectedSinceMs > threshold alone (existing trigger retained, D-03)"

    state.test.js -- ADD tests:
    - "pi_liveness event carrying stale fc1LastMsgTs drives perType.pi to FIRING and emits a send action"
    - "while perType.pi.state === FIRING, a sensor_health event with !scd41_fresh does NOT emit a scd41 send action (D-07 suppression)"
    - "while perType.pi.state === FIRING, RH-OOB does NOT emit an rh send action"
    - "while perType.pi.state === FIRING, humidifier-stuck does NOT emit a humidifier send action"
    - "per-sensor scd41 FIRING does NOT prevent pi from also evaluating (D-08 one-directional)"
    - "when fc1LastMsgTs becomes fresh again, perType.pi clears and per-sensor evaluation resumes on the next tick"

    message.test.js -- ADD tests:
    - "formatProblem({alertType:'pi', lastKnown:{rh:94.0, temp:24.1, humidifier:'OFF', tsMs:<fixed>}, nowMs, lastSeenMs}) contains 'FC-1 offline' and 'chamber uncontrolled' and 'last RH 94%'"
    - "formatProblem({alertType:'pi', lastKnown:null, nowMs, lastSeenMs}) contains 'no recent samples' and does not throw"
    - "formatProblem({alertType:'pi', ...}) output contains no em-dash character (U+2014)"
    - "formatProblem({alertType:'pi', ...}) rounds RH per fmtNum semantics: 94.05 -> '94.1', 94.0 -> '94'"

    bridge-client.test.js -- ADD tests:
    - "pollHealth forwards fc1LastMsgTs from /health.fc1.last_msg_ts in the onLiveness payload"
    - "pollHealth forwards fc1LastMsgTs: null when /health has no fc1 block (old bridge)"
    - "ws_close path forwards cached lastHealth.fc1.last_msg_ts in onLiveness"
  </behavior>
  <action>
    Add tests to the four existing test files (do not create new files). Match the existing describe/it/jest conventions in each file. For state.test.js use the same `applyEvent` / `driveAlertType` harness existing pi_liveness tests use. For message.test.js include a fixed `tsMs` (e.g. 2026-05-20T13:04:00Z) so HH:MM rendering is deterministic. All new tests MUST fail before task 2 (no implementation yet).
  </action>
  <verify>
    <automated>cd src/agents/alerter && npm test -- rules.test.js state.test.js message.test.js bridge-client.test.js 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "fc1LastMsgTs" src/agents/alerter/test/rules.test.js` is at least 3
    - `grep -c "fc1LastMsgTs" src/agents/alerter/test/state.test.js` is at least 2
    - `grep -c "FC-1 offline" src/agents/alerter/test/message.test.js` is at least 1
    - `grep -c "chamber uncontrolled" src/agents/alerter/test/message.test.js` is at least 1
    - `grep -c "no recent samples" src/agents/alerter/test/message.test.js` is at least 1
    - `grep -c "fc1LastMsgTs" src/agents/alerter/test/bridge-client.test.js` is at least 2
    - `cd src/agents/alerter && npm test -- rules.test.js state.test.js message.test.js bridge-client.test.js` exits non-zero (new tests fail)
    - No existing test was DELETED (line count of existing test files only grows)
  </acceptance_criteria>
  <done>Failing tests committed; they encode CD-02, CD-03, and D-03..D-08 verbatim as behaviour.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement bridge-client fc1LastMsgTs forwarding, isPiOffline third trigger, state per-sensor suppression, and chamber-level pi message</name>
  <files>
    src/agents/alerter/src/bridge-client.js,
    src/agents/alerter/src/rules.js,
    src/agents/alerter/src/state.js,
    src/agents/alerter/src/message.js
  </files>
  <read_first>
    - src/agents/alerter/src/bridge-client.js (full file)
    - src/agents/alerter/src/rules.js lines 20-50 (isPiOffline)
    - src/agents/alerter/src/state.js lines 480-620 (pi_liveness + tick + per-sensor evaluators)
    - src/agents/alerter/src/message.js (full file -- locate the alertType:'pi' branch and any fmtNum helper)
    - src/agents/alerter/test/rules.test.js, state.test.js, message.test.js, bridge-client.test.js (from task 1 -- contracts to satisfy)
    - .planning/phases/46-chamber-dark-detector-real-fc1-liveness-signal-farmer-readab/46-CONTEXT.md (decisions block in full)
  </read_first>
  <action>
    bridge-client.js: In `pollHealth`, extend the `onLiveness` payload with `fc1LastMsgTs: h.fc1 ? h.fc1.last_msg_ts : null`. In the `ws.on('close')` handler at line 57-64, mirror: `fc1LastMsgTs: lastHealth && lastHealth.fc1 ? lastHealth.fc1.last_msg_ts : null`. Catch branch (line 34) also `fc1LastMsgTs: null`.

    rules.js: Extend `isPiOffline` destructured args with `fc1LastMsgTs` (no default = undefined). Add third OR-block AFTER the existing two, BEFORE `return false`:
    `if (fc1LastMsgTs != null) { if (nowMs - fc1LastMsgTs > thresholdMs) return true; }`
    Use `!= null` (not `!==`) so both `undefined` and `null` skip the new branch -- old callers and old bridges keep working unchanged (D-03 + graceful degradation per CONTEXT.md).

    state.js: In the `pi_liveness` event handler (currently around line 490-525), persist `next.fc1LastMsgTs = evt.fc1LastMsgTs` near the existing `next.wsConnected = ...` assignments. Add `fc1LastMsgTs: next.fc1LastMsgTs` to BOTH isPiOffline arg objects (the one in the pi_liveness case AND the one in the tick case around line 536). Initial state default: `fc1LastMsgTs: null` in the state factory (search for `wsConnected: false` to find the initial-state location).

    state.js per-sensor suppression (D-07): locate each `driveAlertType(next.perType.<sht30|scd41|rh|humidifierStuck>, ...)` call site in the eval loop and wrap with:
    `if (next.perType.pi.state !== 'FIRING') { <existing block> }`
    Do NOT wrap the `driveAlertType(next.perType.pi, ...)` call (D-08).

    message.js formatProblem pi branch:
    - Compute `ageMin = lastKnown != null ? Math.round((nowMs - lastKnown.tsMs)/60000) : Math.round((nowMs - lastSeenMs)/60000)`
    - If `lastKnown != null`: build `FC-1 offline ?? no telemetry ${ageMin}m. chamber uncontrolled. last RH ${fmtNum(lastKnown.rh)}% @ ${hhmm(lastKnown.tsMs)}.`
    - Else: build `FC-1 offline ?? no telemetry ${ageMin}m. chamber uncontrolled. no recent samples.`
    - `fmtNum`: reuse if exported in message.js; otherwise add a local helper that does `Number(n).toFixed(1).replace(/\.0$/, '')`
    - `hhmm`: local helper that does `new Date(tsMs).toISOString().slice(11,16)`
    - Verify no em-dash (U+2014) appears in output literal

    Run all alerter tests. Confirm green. Do NOT touch unrelated files. Do NOT modify the `piOfflineMin` or `criticalCooldownMin` config plumbing (D-04 says no new env var).
  </action>
  <verify>
    <automated>cd src/agents/alerter && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "fc1LastMsgTs" src/agents/alerter/src/rules.js` is at least 2
    - `grep -c "fc1LastMsgTs" src/agents/alerter/src/bridge-client.js` is at least 3 (pollHealth, ws_close, catch)
    - `grep -c "fc1LastMsgTs" src/agents/alerter/src/state.js` is at least 3 (assignment, pi_liveness isPiOffline call, tick isPiOffline call, initial state)
    - `grep -c "FC-1 offline" src/agents/alerter/src/message.js` is at least 1
    - `grep -c "chamber uncontrolled" src/agents/alerter/src/message.js` is at least 1
    - `grep -c "no recent samples" src/agents/alerter/src/message.js` is at least 1
    - `grep -P "[\\x{2014}]" src/agents/alerter/src/message.js` returns no matches (no em-dash)
    - `grep -c "perType.pi.state" src/agents/alerter/src/state.js` is at least 4 (one suppression guard per suppressed evaluator: sht30, scd41, rh, humidifierStuck; existing references retained)
    - `cd src/agents/alerter && npm test` exits 0
    - No new env vars added (no `process.env.ALERT_CHAMBER_DARK*` anywhere): `grep -r "ALERT_CHAMBER_DARK" src/agents/alerter/` returns no matches
    - `isPiOffline` retains the wsConnected and rosConnected branches (grep for `wsLastConnectedMs` and `rosDisconnectedSinceMs` in rules.js still hits)
  </acceptance_criteria>
  <done>All alerter tests green; CD-02 + CD-03 implemented per D-03..D-08; ready for atomic rebuild in plan 46-03.</done>
</task>

</tasks>

<verification>
Run `cd src/agents/alerter && npm test`. Expect 100% pass. Spot-check that the suppression branches do NOT wrap the pi evaluator itself (D-08).
</verification>

<success_criteria>
- All alerter tests green (no regression in unrelated suites)
- isPiOffline has 3 OR-triggers; existing 2 retained
- Per-sensor suppression is one-directional (D-08)
- pi-alert message is chamber-level and em-dash-free
- No new env vars
</success_criteria>

<output>
Create `.planning/phases/46-chamber-dark-detector-real-fc1-liveness-signal-farmer-readab/46-02-SUMMARY.md` when done.
</output>
