---
phase: 46-chamber-dark-detector
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/mission-control/bridge/src/index.js
  - src/mission-control/bridge/test/fc1_liveness.test.js
autonomous: true
requirements: [CD-01, CD-04]
must_haves:
  truths:
    - "D-01: Bridge updates fc1LastMsgTs as max(ts) on every subscribed fc1 topic (9 topics)"
    - "D-02: /health exposes both fc1.last_msg_ts and fc1.last_msg_age_sec"
    - "Test asserts /health.fc1.last_msg_age_sec advances as topics fire"
  artifacts:
    - path: "src/mission-control/bridge/src/index.js"
      provides: "fc1LastMsgTs aggregator + /health.fc1 block"
      contains: "fc1LastMsgTs"
    - path: "src/mission-control/bridge/test/fc1_liveness.test.js"
      provides: "Unit test for /health fc1 freshness"
      contains: "last_msg_age_sec"
  key_links:
    - from: "src/mission-control/bridge/src/index.js subscribers"
      to: "fc1LastMsgTs"
      via: "markFc1Active(tsMs) helper called from every fc1 topic subscriber"
      pattern: "markFc1Active"
    - from: "GET /health response"
      to: "fc1LastMsgTs"
      via: "fc1: { last_msg_ts, last_msg_age_sec } block"
      pattern: "last_msg_age_sec"
---

<objective>
Add a real fc1-liveness signal on the bridge side: aggregate `fc1LastMsgTs = max(ts)` across all 9 subscribed fc1 topics, and expose it on `/health` as `fc1.last_msg_ts` and `fc1.last_msg_age_sec`. Implements CD-01 + bridge-side portion of CD-04.

Purpose: Today's `rosReady` boot flag is a one-shot that never reflects publisher silence (see `.planning/debug/alerter-co2-only-not-pi.md`). This plan creates the actual heartbeat the alerter will consume in plan 46-02.

Output: Bridge subscribers each call a `markFc1Active(tsMs)` helper; `/health` gains the additive `fc1` block; unit test green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/46-chamber-dark-detector-real-fc1-liveness-signal-farmer-readab/46-CONTEXT.md
@.planning/debug/alerter-co2-only-not-pi.md

<interfaces>
Bridge `/health` response shape (current, from index.js:314-332):
```
{ status, db, ros: { connected }, camera: {...},
  humidifier: { last_msg_ts },
  snapshots: {...}, snapshots_last_24h, oldest_snapshot_at }
```

Bridge fc1 subscriptions (from index.js, all using transientLocalQos):
- /fc1/humidity         (line ~765)
- /fc1/temperature      (line ~781)
- /fc1/temperature_2    (line ~800)
- /fc1/humidity_2       (line ~814)
- /fc1/co2              (line ~829)
- /fc1/actuators/humidifier        (line ~856)
- /fc1/actuators/humidifier_duty   (line ~875)
- /fc1/sensor_health    (line ~927)
- /fc1/control/pid_output (line ~911)

Note: /fc1/control/humidity_target, /fc1/control/current_mode_json,
/fc1/control/alerter_mode_overrides, /fc1/control/alerter_globals,
/fc1/control/experiment_event, /fc1/camera/compressed are NOT in CD-01's
list (CONTEXT.md D-01) and MUST NOT mark fc1 active. Only the 9 listed
data/state topics count.

Module-level state in bridge (index.js:38-39):
```
let rosReady = false;
let humidifierLastMsgTs = null;
```
Add: `let fc1LastMsgTs = null;` next to humidifierLastMsgTs.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing bridge /health fc1 freshness test</name>
  <files>src/mission-control/bridge/test/fc1_liveness.test.js</files>
  <read_first>
    - src/mission-control/bridge/src/index.js (lines 1-50 for module state, 290-333 for /health)
    - src/mission-control/bridge/test/snapshot.test.js (for express-app test patterns used in this repo)
    - src/mission-control/bridge/test/burn_bar.test.js (lighter-weight express test pattern)
    - .planning/phases/46-chamber-dark-detector-real-fc1-liveness-signal-farmer-readab/46-CONTEXT.md (D-01, D-02)
  </read_first>
  <behavior>
    - Test name: "fc1LastMsgTs is null before any topic fires; /health.fc1.last_msg_ts and .last_msg_age_sec are null"
    - Test name: "markFc1Active(ts) updates fc1LastMsgTs to max(prev, ts); older ts does not regress the value"
    - Test name: "/health.fc1.last_msg_age_sec is Math.round((Date.now() - fc1LastMsgTs)/1000) when fc1LastMsgTs is set"
    - Test name: "every fc1 data topic handler bumps fc1LastMsgTs" -- enumerate the 9 topics from CD-01 by topic-name string and assert each marks active
  </behavior>
  <action>
    Create the test file. Either export `markFc1Active` + a getter for `fc1LastMsgTs` from a tiny new module the bridge requires (Claude's discretion per CONTEXT.md), OR test through the express `/health` route using supertest-style harness consistent with existing bridge tests. If a helper module is introduced, name it `src/mission-control/bridge/src/fc1_liveness.js` exporting `{ markFc1Active, getFc1LastMsgTs, getFc1LastMsgAgeSec }`. Tests MUST fail initially (no implementation in plan task 2 yet).
  </action>
  <verify>
    <automated>cd src/mission-control/bridge && npm test -- fc1_liveness.test.js 2>&1 | grep -E "FAIL|failing|0 passed" | head -5</automated>
  </verify>
  <acceptance_criteria>
    - File `src/mission-control/bridge/test/fc1_liveness.test.js` exists
    - File contains the four named test cases above (grep for the literal test names)
    - `cd src/mission-control/bridge && npm test -- fc1_liveness.test.js` exits non-zero (tests fail because implementation not yet wired)
    - Test file enumerates all 9 fc1 topic names from CD-01 (humidity, temperature, humidity_2, temperature_2, co2, humidifier, humidifier_duty, sensor_health, pid_output)
  </acceptance_criteria>
  <done>Failing tests committed; clearly assert the schema CD-01 promises.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement fc1LastMsgTs aggregator + /health.fc1 block</name>
  <files>src/mission-control/bridge/src/index.js</files>
  <read_first>
    - src/mission-control/bridge/src/index.js (full module-level state block lines 38-50; every fc1.* subscriber from line 760 to 1064)
    - src/mission-control/bridge/test/fc1_liveness.test.js (from task 1 -- contract to satisfy)
    - .planning/phases/46-chamber-dark-detector-real-fc1-liveness-signal-farmer-readab/46-CONTEXT.md (D-01, D-02, code_context section "Established Patterns")
  </read_first>
  <action>
    Add module-level `let fc1LastMsgTs = null;` adjacent to existing `humidifierLastMsgTs`. Add a `markFc1Active(tsMs)` helper (inline function or small module per CONTEXT.md discretion) that updates `fc1LastMsgTs = Math.max(fc1LastMsgTs ?? 0, tsMs)`. Call `markFc1Active(Date.now())` from each of the 9 fc1 subscriber callbacks per the interfaces block above. Use `Date.now()` (wall-clock arrival at bridge), not `msg.header.stamp` -- aligns with how `humidifierLastMsgTs` is currently tracked. In the `app.get('/health', ...)` handler add a sibling block:
    `fc1: { last_msg_ts: fc1LastMsgTs, last_msg_age_sec: fc1LastMsgTs === null ? null : Math.round((Date.now() - fc1LastMsgTs) / 1000) }`
    placed immediately after the existing `humidifier:` block. Do NOT touch `rosReady` (CONTEXT.md says retain it). Do NOT subscribe to any new topics. Do NOT modify the per-topic broadcast payloads -- only insert the `markFc1Active` call.
  </action>
  <verify>
    <automated>cd src/mission-control/bridge && npm test -- fc1_liveness.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `src/mission-control/bridge/src/index.js` contains the literal token `fc1LastMsgTs`
    - `src/mission-control/bridge/src/index.js` contains the literal token `last_msg_age_sec` in the /health handler block
    - `grep -c 'markFc1Active' src/mission-control/bridge/src/index.js` returns at least 10 (1 definition + 9 subscriber calls)
    - `grep -v '^\s*//' src/mission-control/bridge/src/index.js | grep -c "markFc1Active("` is 9 (call sites; comments excluded)
    - `cd src/mission-control/bridge && npm test -- fc1_liveness.test.js` exits 0
    - `cd src/mission-control/bridge && npm test` exits 0 (no regression in existing bridge tests)
    - /health.fc1.last_msg_ts is null OR a number; never undefined
  </acceptance_criteria>
  <done>Tests green; /health exposes the additive fc1 block; no regressions; rosReady untouched.</done>
</task>

</tasks>

<verification>
Run the bridge test suite. Spot-check via grep that every fc1 data topic subscriber has the `markFc1Active` call -- the 4 control/* topics excluded by CD-01 and the camera topic MUST NOT have it.
</verification>

<success_criteria>
- All bridge tests green
- /health.fc1.{last_msg_ts, last_msg_age_sec} present and behavior matches CD-01
- Bridge changes are purely additive -- no existing field renamed or removed
</success_criteria>

<output>
Create `.planning/phases/46-chamber-dark-detector-real-fc1-liveness-signal-farmer-readab/46-01-SUMMARY.md` when done.
</output>
