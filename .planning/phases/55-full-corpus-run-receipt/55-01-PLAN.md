---
phase: 55-full-corpus-run-receipt
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/agents/alerter/scripts/backfill-notebook.js
  - src/agents/alerter/scripts/backfill-notebook.test.js
  - src/agents/alerter/scripts/build-backfill-receipt.js
  - src/agents/alerter/scripts/build-backfill-receipt.test.js
autonomous: true
requirements: [BACK-09, BACK-10]
must_haves:
  truths:
    - "Operator can select all 73 corpus pages in one run via a flag instead of a numeric cap"
    - "A full-corpus run emits a git-tracked receipt under .planning/notes/ (not the gitignored run dir)"
    - "A sibling JSONL lists one line per created asset UUID and one per created log UUID"
    - "Per-shape stats report n/ok/held/failed and YES rate keyed by log_type, tagged as bulk-backfill auto-YES (never human-YES signal)"
  artifacts:
    - path: "src/agents/alerter/scripts/backfill-notebook.js"
      provides: "--all-pages flag sets limit to Infinity so selectPages returns the full corpus"
      contains: "all-pages"
    - path: "src/agents/alerter/scripts/build-backfill-receipt.js"
      provides: "buildUuidJsonl + computePerShapeStats + notesReceiptPath/notesJsonlPath copy-out in buildReceipt"
      exports: ["buildUuidJsonl", "computePerShapeStats"]
    - path: "src/agents/alerter/scripts/build-backfill-receipt.test.js"
      provides: "Unit coverage for the three new behaviors"
      contains: "computePerShapeStats"
  key_links:
    - from: "scripts/backfill-notebook.js parseArgs"
      to: "selectPages"
      via: "opts.limit = Infinity when opts.allPages is set"
      pattern: "allPages"
    - from: "scripts/backfill-notebook.js main() finally{} buildReceipt call"
      to: "build-backfill-receipt.js buildReceipt notesReceiptPath/notesJsonlPath"
      via: "pass .planning/notes/ destination params on full-corpus runs"
      pattern: "notesReceiptPath"
---

<objective>
Extend the existing Phase 54 backfill harness so a single operator command can run the FULL 2025 notebook corpus (73 pages) and produce the two permanent BACK-09 artifacts plus the BACK-10 per-shape stats. All changes are small, additive, and hermetically Jest-testable. This plan does NOT execute any live paid-LLM run -- it ships the tooling the operator RUNBOOK (Plan 02) drives.

Purpose: Go from the Cycle ceiling (<=20 pages, gitignored run-dir receipt) to a full-corpus run whose receipt + UUID JSONL live in git-tracked `.planning/notes/`, with per-shape confirm-accuracy stats explicitly tagged so v1.13 narrowing never mistakes bulk-backfill auto-YES for human-YES.

Output:
- `--all-pages` flag in `backfill-notebook.js` (selects all 73 PAGE_REGEX pages).
- `buildUuidJsonl(runSummary)` + `computePerShapeStats(runSummary)` in `build-backfill-receipt.js`.
- `buildReceipt()` extended with optional `notesReceiptPath` / `notesJsonlPath` copy-out params + a BACK-10 stats section.
- New unit tests in the existing test files; full alerter suite stays green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/55-full-corpus-run-receipt/55-CONTEXT.md
@.planning/phases/55-full-corpus-run-receipt/55-RESEARCH.md

<interfaces>
<!-- Verified from codebase 2026-05-25. Executor should use these directly. -->

backfill-notebook.js relevant surface:
- parseArgs(argv): returns opts including { allPages?, limit (default 5), dryRun, resumeFrom, runId, bulkBackfill, farmer, cycle, corpusDir }.
  Boolean flags are matched by `if (arg === '--xxx') { opts.x = true; continue; }` BEFORE the `=`-split block.
- selectPages(allPages, { limit, resumeFrom }): `const cap = Math.max(0, Number(limit) || 0); return allPages.slice(start, start + cap);`
  VERIFIED: Number(Infinity)||0 === Infinity and slice(0, Infinity) returns the full array.
- main() reads `selectPages(allPages, { limit: opts.limit, resumeFrom: opts.resumeFrom })` at the top of dispatch.
- main() finally{} calls buildReceipt({ runDir, runSummary, csvPath, runId, cycleNumber, farmosUrl, elapsedSec }) -- destructured params, so new optional params are backward-compatible.
- PAGE_REGEX matches IMG_3775..IMG_3861 (73 files actually exist, up to IMG_3847). USAGE is a template string; module.exports lists all helpers.

build-backfill-receipt.js relevant surface:
- buildReceipt({ runDir, runSummary, csvPath, runId, cycleNumber, farmosUrl, elapsedSec, generatedAt }):
  builds `lines[]`, joins to `body`, applies `body = body.replace(/[----]/g, '--')` (em-dash scrub), writes `<runDir>/receipt.md`, returns receiptPath.
- computeAggregate(runSummary, csvRowsAll): returns { pages, drafts, assets_created, assets_reused, duplicate_asset_count, logs_created, per_strain, unknown_strain_codes, upsert_stability }.
- module.exports currently lists: ACTIVE_STRAIN_CODES, parseCsv, loadCsvForPage, strainSetFromCsv, strainSetFromCommits, computeCsvDiff, pageDateForImage, renderPageSection, computeAggregate, aggregateCost, buildReceipt.

commit entry shape (each element of page.commits[], produced by processDraftsForCapture):
  { draftId, log_type, ok, asset_ids: [], log_ids: [], reason?, strain_codes?: [], block_name?: null }
  where ok is one of: true | false | 'held' | 'skipped' | 'dry-run'.
  'held' carries reason 'strain_unknown_pending_confirm' and empty asset_ids/log_ids.
  page entry shape: { captureId, pagePath, ok, draftIds: [], commits: [] }.

KNOWN_SHAPES (the five locked log_type values): seeding, observation, activity, harvest, input.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add --all-pages flag to backfill-notebook.js</name>
  <read_first>
    - src/agents/alerter/scripts/backfill-notebook.js (parseArgs lines ~77-105, USAGE lines ~57-75, main() limit usage line ~628, selectPages lines ~156-165)
    - src/agents/alerter/scripts/backfill-notebook.test.js (describe('parseArgs') ~line 39 and describe('selectPages') ~line 149 -- the source-of-truth analog for flag + selection tests)
  </read_first>
  <behavior>
    - parseArgs(['--all-pages']) returns opts.allPages === true.
    - parseArgs([]) returns opts.allPages === false (default present in the opts initializer).
    - When opts.allPages is true, opts.limit is set to Infinity before selectPages is called (assert via main() dry-run integration OR a direct unit on the resolution step).
    - selectPages(all, { limit: Infinity }) returns the entire array (regression-guard the Infinity path explicitly).
    - --all-pages does NOT change prod-guard, farmer-gate, or missing-env behavior.
  </behavior>
  <action>
    In `parseArgs`: add `allPages: false` to the opts initializer object, and add a boolean-flag branch `if (arg === '--all-pages') { opts.allPages = true; continue; }` alongside the existing `--bulk-backfill` / `--dry-run` branches (before the `=`-split block).
    In `main`: after `const opts = parseArgs(argv)` resolves and AFTER the help short-circuit, set `if (opts.allPages) opts.limit = Infinity;` so the existing `selectPages(allPages, { limit: opts.limit, ... })` call picks up the full corpus. Do NOT alter selectPages itself -- it already handles Infinity (verified).
    Update the USAGE template string: add a `--all-pages` line documenting "Process all corpus pages (overrides --limit)." Keep ASCII-only, no em-dashes.
    Do NOT add the skip-list flag or resume merge logic -- those are RUNBOOK-documented operator steps (Plan 02), out of scope here.
  </action>
  <verify>
    <automated>cd src/agents/alerter && npx jest --testPathPattern=backfill-notebook -t "all-pages" 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `npx jest --testPathPattern=backfill-notebook` passes including new --all-pages assertions.
    - `node scripts/backfill-notebook.js --help` output contains the literal string `--all-pages`.
    - `grep -n "all-pages\|allPages" scripts/backfill-notebook.js` shows the flag in parseArgs, main, and USAGE (>=3 hits).
    - No change to exit-code behavior for prod-guard (3), farmer-gate (4), missing-env (5), run-id collision (6).
  </acceptance_criteria>
  <done>--all-pages parses to opts.allPages=true, resolves opts.limit to Infinity in main(), selectPages returns all pages under it, and the help banner documents it; existing exit-code gates untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add buildUuidJsonl + computePerShapeStats to build-backfill-receipt.js</name>
  <read_first>
    - src/agents/alerter/scripts/build-backfill-receipt.js (computeAggregate lines ~210-298 for the commit-iteration analog; module.exports lines ~416-428)
    - src/agents/alerter/scripts/backfill-notebook.js (processDraftsForCapture lines ~300-432 -- the source-of-truth for the commit entry shape: ok values true/false/'held'/'skipped', log_type, asset_ids, log_ids, block_name, draftId)
    - src/agents/alerter/scripts/build-backfill-receipt.test.js (describe('computeAggregate') ~line 141 -- the analog for runSummary-fixture unit tests)
  </read_first>
  <behavior>
    - buildUuidJsonl(runSummary) emits one JSON line per asset UUID (type:'asset') and one per log UUID (type:'log'); each line carries uuid, log_type, page (basename), draft_id; asset lines also carry block_name. Output ends with a trailing newline when non-empty, and is the empty string when there are no UUIDs.
    - Each emitted line is valid JSON (parseable by JSON.parse).
    - computePerShapeStats(runSummary) returns { tag: 'bulk_backfill_auto_yes', by_shape, total } where by_shape is keyed by log_type seeded with the five KNOWN_SHAPES (seeding/observation/activity/harvest/input), each bucket { n, ok, held, failed }.
    - ok counts c.ok === true; held counts c.ok === 'held'; everything else (false/'skipped') counts failed. n is the per-shape total of those three. 'dry-run' entries are not expected in a real run but, if present, count as failed (not ok).
    - YES rate is derivable as ok/(ok+held+failed) per shape; assert the raw counts (rate computed at render time).
    - An unexpected log_type (not in KNOWN_SHAPES) creates its own bucket rather than being dropped.
  </behavior>
  <action>
    Add `const KNOWN_SHAPES = ['seeding', 'observation', 'activity', 'harvest', 'input'];` near ACTIVE_STRAIN_CODES.
    Implement `buildUuidJsonl(runSummary)`: iterate `runSummary || []`, compute `pageBase = path.basename(page.pagePath || page.page || '')`, then for each `c` in `page.commits || []` push a JSON.stringify line per uuid in `c.asset_ids || []` (type 'asset', with block_name) and per uuid in `c.log_ids || []` (type 'log'); include `log_type: c.log_type || null`, `page: pageBase`, `draft_id: c.draftId || null`. Return `lines.join('\n') + (lines.length ? '\n' : '')`.
    Implement `computePerShapeStats(runSummary)`: seed by_shape with the five KNOWN_SHAPES buckets {n,ok,held,failed}; iterate commits; bucket by `c.log_type || 'unknown'` (create bucket if missing); increment n + the matching ok/held/failed counter (c.ok === true -> ok; c.ok === 'held' -> held; else failed); maintain a parallel `total` bucket. Return `{ tag: 'bulk_backfill_auto_yes', by_shape, total }`. The literal tag string `bulk_backfill_auto_yes` per BACK-10 is non-negotiable (v1.13 greps for it).
    Add both functions to module.exports.
    Add new test blocks `describe('buildUuidJsonl', ...)` and `describe('computePerShapeStats', ...)` to build-backfill-receipt.test.js using small hand-built runSummary fixtures (cover: asset+log lines, empty input -> '', a 'held' draft, a 'failed' draft, an unexpected log_type bucket, the literal tag assertion).
  </action>
  <verify>
    <automated>cd src/agents/alerter && npx jest --testPathPattern=build-backfill-receipt -t "buildUuidJsonl|computePerShapeStats|per-shape" 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - `npx jest --testPathPattern=build-backfill-receipt` passes including the two new describe blocks.
    - `grep -c "bulk_backfill_auto_yes" scripts/build-backfill-receipt.js` >= 1.
    - buildUuidJsonl('' / [] / undefined) returns '' (no trailing newline on empty).
    - computePerShapeStats result.tag === 'bulk_backfill_auto_yes' and result.by_shape has all five KNOWN_SHAPES keys.
    - A 'held' commit increments by_shape[shape].held (not ok), a false commit increments failed.
  </acceptance_criteria>
  <done>Both pure functions exist, are exported, correctly bucket the verified commit-entry shape, and carry the literal BACK-10 tag; unit tests cover asset/log/held/failed/empty/unknown-shape cases.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire notes-copy-out + BACK-10 section into buildReceipt and the harness finally{}</name>
  <read_first>
    - src/agents/alerter/scripts/build-backfill-receipt.js (buildReceipt lines ~325-414 -- the em-dash scrub at line ~409 and the single `<runDir>/receipt.md` write at ~411-412 are the source-of-truth analog)
    - src/agents/alerter/scripts/backfill-notebook.js (main() finally{} buildReceipt call lines ~748-764)
    - src/agents/alerter/scripts/build-backfill-receipt.test.js (describe('buildReceipt') ~line 233 -- analog for tmp-dir write assertions)
  </read_first>
  <behavior>
    - buildReceipt accepts optional notesReceiptPath and notesJsonlPath params (default undefined). Existing callers (Cycle 1/2) that omit them are unchanged: only `<runDir>/receipt.md` is written.
    - When notesReceiptPath is provided, the SAME em-dash-scrubbed body string written to <runDir>/receipt.md is also written to notesReceiptPath (mkdir -p its parent first). No second render pass (avoids divergence + re-introducing em-dashes per Pitfall 5).
    - When notesJsonlPath is provided, buildUuidJsonl(runSummary) output is written there (mkdir -p parent first).
    - The receipt body includes a new section headed `## BACK-10 Per-shape stats (bulk-backfill auto-YES -- not human-YES signal for v1.13)` rendering a table of shape | n | ok | held | failed | yes_rate_pct, plus the literal tag line. yes_rate_pct rounds to 1 decimal (n=0 -> n/a, never division-by-zero).
    - buildReceipt remains crash-safe: it is still callable from finally{} and never throws on a partial runSummary.
    - In backfill-notebook.js main() finally{}, when opts.allPages is set, pass notesReceiptPath + notesJsonlPath into the existing buildReceipt call so the full-corpus run writes the two .planning/notes/ artifacts; Cycle runs (no --all-pages) keep writing only the gitignored run-dir receipt.
  </behavior>
  <action>
    In `buildReceipt`: destructure `notesReceiptPath` and `notesJsonlPath` from the param object. Call `computePerShapeStats(runSummary)` and append the BACK-10 section to `lines` BEFORE the join (place it after the Phase 51 upsert-stability section, before `## Farmer review`). Render the per-shape table; compute yes_rate_pct = bucket.n ? round1(100 * bucket.ok / bucket.n) : 'n/a'. Echo the literal `tag: bulk_backfill_auto_yes` on its own line in that section. After the existing `body.replace(/[----]/g, '--')` scrub, keep the `<runDir>/receipt.md` write, THEN: if notesReceiptPath, `fs.mkdirSync(path.dirname(notesReceiptPath), { recursive: true })` and write the same `body`; if notesJsonlPath, mkdir parent + write `buildUuidJsonl(runSummary)`. Return the existing receiptPath (do not change the return contract).
    In `backfill-notebook.js` main() finally{}: inside the existing `if (opts.bulkBackfill)` buildReceipt call, add `notesReceiptPath` / `notesJsonlPath` props derived from a notes basename when `opts.allPages` is true. Use a date-stamped basename pattern `2026-XX-XX-2025-notebook-backfill-receipt` resolved against the repo `.planning/notes/` dir (the literal date is filled by the operator/run date; derive from `now` or `new Date()` as YYYY-MM-DD). Guard the whole copy-out inside the same try/catch already wrapping buildReceipt so a notes-write failure only warn-logs.
    Keep all receipt text ASCII-only; rely on the existing em-dash scrub (do not re-render after scrubbing).
  </action>
  <verify>
    <automated>cd src/agents/alerter && npx jest --testPathPattern=build-backfill-receipt -t "notesReceiptPath|notesJsonlPath|BACK-10" 2>&1 | tail -25 && npx jest --testPathPattern="backfill-notebook" 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `npx jest --testPathPattern="build-backfill-receipt|backfill-notebook"` all green.
    - buildReceipt called WITHOUT notesReceiptPath writes only `<runDir>/receipt.md` (no extra files) -- regression-guard the Cycle path.
    - buildReceipt called WITH notesReceiptPath + notesJsonlPath writes both, parent dirs auto-created, and the notes receipt body byte-equals the run-dir receipt body (same scrubbed string).
    - The receipt body contains the literal `bulk-backfill auto-YES -- not human-YES signal for v1.13` section header and a yes_rate_pct column.
    - Receipt body contains no `--` or `--` characters (`! grep -q $'--\|--' <receipt>`).
  </acceptance_criteria>
  <done>buildReceipt optionally copies the same scrubbed body to .planning/notes/ + writes a UUID JSONL sibling, embeds the tagged BACK-10 per-shape section, stays crash-safe; the harness passes the notes paths only on --all-pages runs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator CLI -> harness | Operator-supplied flags (--all-pages, --farmer, FARMOS_URL); already gated by prod-guard + farmer-gate + missing-env checks |
| harness -> .planning/notes/ (git-tracked) | Receipt/JSONL written into a committed directory; must be ASCII-only and contain no secrets |
| harness commit-flip -> shared timescale -> live prod watchdog | The prod-leak hazard (GA1); mitigated OPERATIONALLY in Plan 02 runbook, NOT in this plan's code |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-55-01 | Repudiation | BACK-10 per-shape stats consumed by v1.13 | mitigate | Literal `tag: 'bulk_backfill_auto_yes'` field + section header `not human-YES signal for v1.13`; computePerShapeStats hard-codes the tag so it cannot be omitted |
| T-55-02 | Information Disclosure | `.planning/notes/` receipt (git-tracked) | mitigate | Reuse existing em-dash scrub; write the SAME scrubbed body (no second render); receipt carries only block_names + UUIDs + counts, no credentials/tokens |
| T-55-03 | Denial of Service (budget) | paid-LLM spend on a 73-page run | accept (this plan) | This plan ships no live-run code; the smoke-5-before-full-run gate is a RUNBOOK step in Plan 02; runIdExistsGuard (exit 6) already prevents accidental re-spend |
| T-55-04 | Tampering | full-corpus run leaks confirmed drafts to PROD via shared-DB watchdog | transfer (to Plan 02 runbook) | GA1: OPERATIONAL isolation only (throwaway postgres OR stop alerter), enforced as a HARD runbook pre-flight; no commit-watchdog code change per locked decision |
| T-55-SC | Tampering | npm/pip/cargo installs | n/a | No new package installs in this plan (additive edits to two existing files); no install tasks, so the legitimacy gate does not apply |
</threat_model>

<verification>
- Full alerter suite green after all three tasks: `cd src/agents/alerter && npx jest --passWithNoTests 2>&1 | tail -15` (baseline 1,342 pass / 9 skipped / 0 fail; expect net-positive count, 0 fail).
- `node scripts/backfill-notebook.js --all-pages --dry-run --corpus-dir=/mnt/slime-kingdom/shared/mushdatadump/jpeg` lists 73 selected pages and spends nothing (dry-run short-circuits before any DB/pipeline/LLM call).
- No live paid-LLM run is performed by any task in this plan.
</verification>

<success_criteria>
- `--all-pages` selects the full corpus (73 pages) in dry-run; `--limit` default behavior unchanged when the flag is absent.
- `buildUuidJsonl` + `computePerShapeStats` exported, unit-tested, and correct against the verified commit-entry shape.
- `buildReceipt` writes the BACK-09 `.planning/notes/` receipt + UUID JSONL only when notes paths are passed (full-corpus runs), and embeds the tagged BACK-10 per-shape section.
- Cycle-1/2 receipt behavior (gitignored run-dir only) is regression-guarded.
- Full Jest suite green, 0 fail.
</success_criteria>

<output>
Create `.planning/phases/55-full-corpus-run-receipt/55-01-SUMMARY.md` when done.
</output>
