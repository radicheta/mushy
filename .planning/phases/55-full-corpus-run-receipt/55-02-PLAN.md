---
phase: 55-full-corpus-run-receipt
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/55-full-corpus-run-receipt/55-FULL-CORPUS-RUNBOOK.md
  - .planning/phases/55-full-corpus-run-receipt/55-PROMOTION-DECISION.md
autonomous: true
requirements: [BACK-11]
must_haves:
  truths:
    - "The operator has a step-by-step runbook to execute the full-corpus paid-LLM backfill safely"
    - "The runbook HARD pre-flight enforces prod-leak isolation operationally (throwaway postgres OR stop alerter), replacing the falsified dev-DB-not-prod assumption"
    - "The runbook gates the full run on a smoke-5 step and on Cycle-2 farmer sign-off"
    - "A prod-promotion decision doc defaults to dev-only and documents the per-session-class opt-in path"
  artifacts:
    - path: ".planning/phases/55-full-corpus-run-receipt/55-FULL-CORPUS-RUNBOOK.md"
      provides: "Operator runbook: isolation pre-flight + dry-run + smoke-5 + full --all-pages run + receipt verification + crash-recovery"
      min_lines: 80
    - path: ".planning/phases/55-full-corpus-run-receipt/55-PROMOTION-DECISION.md"
      provides: "BACK-11 prod-promotion decision: default dev-only, per-session-class opt-in process, upsert path if promoting"
      min_lines: 40
  key_links:
    - from: "55-FULL-CORPUS-RUNBOOK.md pre-flight"
      to: "GA1 operational isolation"
      via: "throwaway postgres on 5433 OR docker stop mushy-alerter-1, verified explicitly not by trusting env"
      pattern: "mushy-alerter|5433"
    - from: "55-FULL-CORPUS-RUNBOOK.md full-run step"
      to: "scripts/backfill-notebook.js --all-pages"
      via: "operator command after smoke-5 passes"
      pattern: "all-pages"
---

<objective>
Author the two operator-facing documents that turn the Plan 01 tooling into a safe, executable full-corpus backfill: the full-corpus RUNBOOK (whose HARD pre-flight bakes in the GA1 prod-leak isolation) and the BACK-11 prod-promotion decision doc. These are DOC deliverables only. No task here executes the live paid-LLM corpus run; that is an operator step inside the runbook, gated on Cycle-2 farmer sign-off.

Purpose: The live full-corpus run is operator-triggered (GA2). It needs a runbook that (a) replaces the falsified Phase-54 "DATABASE_URL points at dev, not prod" assumption with explicit operational isolation, (b) enforces smoke-before-expensive-batch, and (c) documents crash recovery for a 73-page run. BACK-11 records that prod write is dev-only by default and opt-in per session-class.

Output:
- 55-FULL-CORPUS-RUNBOOK.md with numbered operator steps and the isolation pre-flight as a hard gate.
- 55-PROMOTION-DECISION.md, the BACK-11 decision doc (default dev-only; per-session-class prod opt-in).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/55-full-corpus-run-receipt/55-CONTEXT.md
@.planning/phases/55-full-corpus-run-receipt/55-RESEARCH.md
@.planning/phases/54-backfill-harness-dev-farmos-smoke-20-pages/54-CYCLE-2-RUNBOOK.md

<interfaces>
<!-- Reference material the doc authors must reflect accurately. -->

Prod-leak mechanism (VERIFIED from code, 55-RESEARCH.md Q5):
- commit-watchdog.js polls SELECT FROM signal_draft WHERE status='confirmed' against the SHARED timescale (localhost:5432).
- The live mushy-alerter-1 is wired to PROD farmOS (:8082). When the backfill flips drafts to confirmed in that shared DB, the watchdog commits them to PROD.
- The falsified Phase-54 step was "echo DATABASE_URL should be the alerter dev DB". There is NO separate dev TimescaleDB; it is shared.
- VERIFIED 2026-05-26: the harness leaves drafts at status='confirmed' after its own dev commit (it never advances them to 'committed' -- backfill-notebook.js only sets 'confirmed'/'needs_review'). So Option B (stop watchdog) only DEFERS the leak: on restart the watchdog drains the still-'confirmed' backfill drafts to PROD. Option B is safe ONLY with a pre-restart cleanup; Option A (throwaway pg) has no such race and is the default. See [[project_v113_watchdog_origin_guard_candidate]] for the durable origin-guard fix.

Harness flags available after Plan 01 (do NOT re-author code here):
- --all-pages (new; selects all 73 PAGE_REGEX pages), --dry-run, --limit=<n>, --bulk-backfill, --farmer=santi, --resume-from=<basename>, --run-id=<id>, --corpus-dir=<path>.
- Exit codes: 0 ok | 1 fatal | 3 prod-guard | 4 farmer-gate | 5 missing-env | 6 run-id collision.
- Full-corpus runs cause buildReceipt to write .planning/notes/2026-XX-XX-2025-notebook-backfill-receipt.md plus .jsonl.

Corpus facts (VERIFIED): 73 PAGE_REGEX pages (IMG_3775..IMG_3847); operator-known-bad skip list IMG_3790, IMG_3810, IMG_3820; cost estimate about 7 to 10 USD for full run, about 0.50 USD for smoke-5.

GA2 gate: full run is gated on Phase 54 Cycle-1 AND Cycle-2 dev smokes completing with FARMER SIGN-OFF. Phase 55 does not re-own the 54-05/06 cycle runbooks.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author 55-FULL-CORPUS-RUNBOOK.md with the GA1 isolation pre-flight</name>
  <read_first>
    - .planning/phases/54-backfill-harness-dev-farmos-smoke-20-pages/54-CYCLE-2-RUNBOOK.md (structural analog: prerequisite gate, page-selection, numbered steps, receipt-review verdict)
    - .planning/phases/55-full-corpus-run-receipt/55-RESEARCH.md (Q4 smoke story, Q5 isolation pre-flight assertions Option A/B, Pitfalls 1/3, Open Questions 1/2/3)
    - .planning/phases/55-full-corpus-run-receipt/55-CONTEXT.md (GA1 + GA2 locked decisions)
  </read_first>
  <action>
    Author 55-FULL-CORPUS-RUNBOOK.md modeled on the Cycle-2 runbook structure. Required sections, in order:
    (1) Header plus the literal line "Operator runs this. Do not delegate to an autonomous agent." plus a GA2 gate note.
    (2) PREREQUISITE GATE: the Phase 54 Cycle-2 receipt must exist with verdict SIGN-OFF (grep gate, abort if absent). Reference the Cycle-2 sign-off explicitly.
    (3) HARD PRE-FLIGHT, PROD-LEAK ISOLATION (the load-bearing section). State plainly that the Phase-54 "DATABASE_URL is the dev DB" assumption is FALSIFIED: there is one shared timescale on 5432 and the prod-pointing mushy-alerter-1 watchdog polls status='confirmed' every 30s, so confirmed backfill drafts leak to PROD. Provide BOTH isolation options as explicit copy-pasteable verified-not-trusted checks, but mark Option A as the DEFAULT/RECOMMENDED and Option B as a fallback that carries a hard extra obligation. Option A (DEFAULT): throwaway postgres on 5433 (run an lsof port-free check first per Open Question 3; export a fresh DATABASE_URL; assert it does NOT contain 5432). The prod watchdog never connects to this DB, so there is no leak and no cleanup race; drop the container after the run. Option B (FALLBACK, NOT default): docker stop mushy-alerter-1 for the run window. CRITICAL CAVEAT the runbook MUST state in full: stopping the watchdog only DEFERS the leak, it does NOT prevent it. The harness commits content to dev :18080 directly but leaves each draft at status='confirmed' (verified 2026-05-26: backfill-notebook.js never advances drafts to 'committed'). So on restart the watchdog will drain every still-'confirmed' backfill draft straight to PROD. Therefore Option B REQUIRES a mandatory pre-restart cleanup step: BEFORE docker start mushy-alerter-1, delete or re-status the backfill drafts in the shared DB (e.g. UPDATE signal_draft SET status='discarded' WHERE needs_review_reason='bulk_backfill_santi', or DELETE them) and verify zero rows remain at status='confirmed' for the backfill origin. Also include the explicit operator-OK note that prod RH alerting pauses for the whole window (cite the safety-net concern). The pre-flight MUST also assert: dev farmOS :18080 reachable, FARMOS_URL contains no :8082 or prod substring, ANTHROPIC_API_KEY set, alerter Jest suite green.
    (4) SMOKE-BEFORE-FULL gating: a dry-run step (all-pages dry-run confirms 73 pages, 0 USD), then a paid smoke step (5 pages, about 0.50 USD), then the full step (all-pages only after the smoke receipt is clean). Honor smoke-before-expensive-batch.
    (5) FULL RUN command: all-pages bulk-backfill farmer=santi against dev :18080 with a fresh run-id; note the roughly 7 to 10 USD spend and that the receipt plus UUID JSONL land in .planning/notes/.
    (6) CRASH RECOVERY: document resume-from=<basename> plus a FRESH run-id (cite Pitfall 3: runIdExistsGuard exit 6); note that two partial run dirs are accepted and the final receipt may be concatenated manually per Open Question 2.
    (7) SKIP LIST: document IMG_3790 / IMG_3810 / IMG_3820 as operator-known-bad (per Open Question 1 the accepted simpler path: they likely produce per-page failure reasons rather than crashing; the operator may resume past them).
    (8) RECEIPT VERIFICATION plus AFTERCARE: verify the .planning/notes/ receipt plus JSONL exist; pass criteria duplicate_asset_count == 0 AND upsert_stability.unstable == empty AND the BACK-10 section is present and tagged bulk_backfill_auto_yes; if Option B was used, the aftercare ORDER is mandatory: FIRST run the pre-restart cleanup from (3) and verify zero backfill drafts remain at status='confirmed' in the shared DB, THEN docker start mushy-alerter-1, THEN confirm alerter health restored. Restarting before the cleanup leaks the corpus to prod.
    Keep the document ASCII-only with no em-dashes; round any farmer-facing numbers. The document is for the human operator and MUST NOT instruct an autonomous agent to run the backfill.
  </action>
  <verify>
    <automated>cd /mnt/slime-kingdom/opt/mushy && f=.planning/phases/55-full-corpus-run-receipt/55-FULL-CORPUS-RUNBOOK.md; test -f "$f" && grep -qi mushy-alerter "$f" && grep -q 5433 "$f" && grep -qi all-pages "$f" && grep -qi smoke "$f" && grep -qi sign-off "$f" && grep -qi bulk_backfill_auto_yes "$f" && grep -Pq '[\x{2013}\x{2014}]' "$f" && echo HAS_EMDASH_FAIL || echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - 55-FULL-CORPUS-RUNBOOK.md exists and is at least 80 lines.
    - Contains the GA1 isolation pre-flight referencing both mushy-alerter-1 (Option B) and a throwaway postgres on port 5433 (Option A), with Option A marked DEFAULT.
    - Option B explicitly documents that stopping the watchdog only DEFERS the leak and REQUIRES a mandatory pre-restart cleanup (re-status/delete backfill drafts so none remain at status='confirmed') BEFORE docker start.
    - Contains a dry-run step, a paid smoke-5 step, and a full --all-pages step in that order.
    - References the Cycle-2 SIGN-OFF prerequisite gate.
    - Documents the resume-from + fresh run-id crash-recovery path and the IMG_3790/3810/3820 skip list.
    - Contains no em-dash or en-dash characters (the verify automated check prints PASS, not HAS_EMDASH_FAIL).
    - Does NOT instruct an autonomous agent to execute the backfill (operator-only).
  </acceptance_criteria>
  <done>The operator runbook exists with the falsified-assumption replacement, both GA1 isolation options as verified-not-trusted checks, the smoke-before-full gating, the full-corpus command, crash recovery, skip list, and receipt verification; ASCII-only.</done>
</task>

<task type="auto">
  <name>Task 2: Author 55-PROMOTION-DECISION.md (BACK-11)</name>
  <read_first>
    - .planning/phases/55-full-corpus-run-receipt/55-CONTEXT.md (BACK-11 "default prod-promotion = dev-only; prod write opt-in per session-class"; Deferred Ideas)
    - .planning/phases/55-full-corpus-run-receipt/55-RESEARCH.md (BACK-11 row in Phase Requirements table; Architectural Responsibility Map "Promotion decision = static markdown doc, not code")
    - .planning/notes/2026-05-24-prod-write-receipt.md (the existing May-22 prod stub write; the precedent for a deliberate, scoped prod write)
  </read_first>
  <action>
    Author 55-PROMOTION-DECISION.md, a static markdown decision doc (NOT generated code). Required content:
    (1) DECISION header stating the default: the 2025-notebook backfill is DEV-ONLY by default. No autonomous prod write. State this is the BACK-11 record.
    (2) RATIONALE: dev :18080 is the validation target; prod :8082 carries live captures plus the May-22 stubs; per GA1 the live prod watchdog makes any prod-facing confirmed-draft flow hazardous. Reference the prod-leak hazard memory.
    (3) PER-SESSION-CLASS OPT-IN PROCESS: define what a session-class is for this corpus (e.g. by log_type / shape, or by a curated subset of pages/dates the operator explicitly trusts after dev review). Spell out the gate: a session-class may be promoted to prod ONLY when (a) its dev receipt shows duplicate_asset_count == 0 and upsert_stability stable, (b) the operator explicitly opts that class in, and (c) the prod write goes through the Phase 51 upsert path (content-addressable by block_name) so it enriches the May-22 stubs in place rather than minting duplicates.
    (4) PROD WRITE PATH IF PROMOTING: note that promotion uses the same harness against a prod-pointed FARMOS_URL with the prod-guard intentionally satisfied by operator opt-in, AND that the GA1 isolation still applies (the shared-DB watchdog concern). Cross-reference 55-FULL-CORPUS-RUNBOOK.md for the isolation mechanics. Make explicit that this doc does NOT authorize a prod run; it documents the criteria for a future operator decision.
    (5) OUT OF SCOPE / DEFERRED: the commit-watchdog origin-guard (deferred), the 4 bogus dev-farmOS term cleanup (admin DELETE deferred), v1.13 narrowing (separate milestone, consumes BACK-10).
    Keep ASCII-only, no em-dashes. This is a decision artifact, default-reject for prod.
  </action>
  <verify>
    <automated>cd /mnt/slime-kingdom/opt/mushy && f=.planning/phases/55-full-corpus-run-receipt/55-PROMOTION-DECISION.md; test -f "$f" && grep -qi dev-only "$f" && grep -qi session-class "$f" && grep -qi upsert "$f" && grep -Pq '[\x{2013}\x{2014}]' "$f" && echo HAS_EMDASH_FAIL || echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - 55-PROMOTION-DECISION.md exists and is at least 40 lines.
    - States dev-only as the default decision and that prod write is never autonomous.
    - Defines a per-session-class opt-in process with explicit promotion criteria (clean dev receipt + operator opt-in + Phase 51 upsert path).
    - Cross-references the full-corpus runbook for GA1 isolation mechanics.
    - Lists the deferred items (origin-guard, bogus-term cleanup, v1.13 narrowing) as out of scope.
    - Contains no em-dash or en-dash characters (verify prints PASS).
  </acceptance_criteria>
  <done>The BACK-11 decision doc exists, defaults to dev-only, defines the per-session-class prod opt-in gate with criteria, ties promotion to the Phase 51 upsert path and GA1 isolation, and records the deferred items; ASCII-only.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator -> live prod farmOS (:8082) | A promotion decision could authorize prod writes; this doc default-rejects and gates them |
| backfill confirmed-drafts -> shared timescale -> prod watchdog | The GA1 prod-leak hazard; mitigated OPERATIONALLY by the runbook pre-flight, never by code |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-55-04 | Tampering | full-corpus run leaks confirmed drafts to PROD via shared-DB watchdog | mitigate | RUNBOOK HARD pre-flight: GA1 operational isolation (throwaway postgres on 5433 OR stop mushy-alerter-1), verified-not-trusted; replaces the falsified DATABASE_URL assumption. No commit-watchdog code change per locked GA1 |
| T-55-05 | Elevation of Privilege | accidental prod promotion of unreviewed data | mitigate | 55-PROMOTION-DECISION.md defaults dev-only; prod write requires explicit per-session-class operator opt-in plus a clean dev receipt plus the Phase 51 upsert path |
| T-55-06 | Denial of Service (budget) | paid-LLM over-spend on the 73-page run | mitigate | RUNBOOK enforces dry-run then smoke-5 (about 0.50 USD) before the full run; runIdExistsGuard (exit 6) prevents accidental re-spend |
| T-55-07 | Information Disclosure | runbook/decision docs are git-tracked | accept | Docs carry only operational steps, port numbers, and corpus filenames; no credentials. ASCII-only enforced to avoid LLM-tell artifacts |
| T-55-SC | Tampering | npm/pip/cargo installs | n/a | This plan ships only markdown docs; no package installs, so the legitimacy gate does not apply |
</threat_model>

<verification>
- Both docs exist with the verified content gates (see per-task automated checks).
- Neither doc instructs an autonomous agent to run the live backfill; both are operator-facing.
- The runbook pre-flight enforces GA1 isolation explicitly (port/container checks), not by trusting an env var.
- No code is modified by this plan; no Jest run required (doc-only plan).
</verification>

<success_criteria>
- 55-FULL-CORPUS-RUNBOOK.md and 55-PROMOTION-DECISION.md exist, ASCII-only, no em-dashes.
- The runbook bakes in the GA1 isolation pre-flight (both options), the smoke-before-full gating, the Cycle-2 sign-off prerequisite, crash recovery, and the skip list.
- BACK-11 decision doc defaults to dev-only with a documented per-session-class prod opt-in path.
- No task executes the live paid-LLM corpus run.
</success_criteria>

<output>
Create `.planning/phases/55-full-corpus-run-receipt/55-02-SUMMARY.md` when done.
</output>
