---
phase: 47-multi-source-extraction-fusion-groups-shape-inoc-draft
plan: 02
type: execute
wave: 2
depends_on: ['47-01']
files_modified:
  - src/agents/alerter/src/extraction/prompts/system.js
  - src/agents/alerter/test/extraction/extractor.test.js
autonomous: true
requirements: [INOC-01, INOC-02, INOC-03]
must_haves:
  truths:
    - "System prompt teaches the model to emit seeding_session when the capture describes >1 child block (groups.length >= 1 AND total children > 1)"
    - "System prompt teaches photo-wins-silently conflict policy with a worked example that populates draft.conflicts[]"
    - "System prompt teaches needs_input='starting_seq' + 'NEEDS_SEQ' sentinel when paper-log photo absent"
    - "Few-shot includes a multi-parent multi-species May-22-shape example that emits 1 seeding_session draft with 5 groups + 11 children"
    - "Existing 7 legacy few-shots still resolve their tu_fewshot_N tool_use IDs with matching tool_result blocks (no Anthropic 400)"
  artifacts:
    - path: src/agents/alerter/src/extraction/prompts/system.js
      provides: "Updated SYSTEM_PROMPT + FEW_SHOT with seeding_session policy + example"
      contains: "seeding_session"
  key_links:
    - from: src/agents/alerter/src/extraction/prompts/system.js
      to: src/agents/alerter/src/extraction/schemas/seeding-session.js
      via: "few-shot tool_use input shape must validate against SeedingSession schema"
      pattern: "type: 'seeding_session'"
---

<objective>
Revise the extraction system prompt (prompts/system.js) to teach the model the new `seeding_session` shape, the conflict-resolution policy (photo wins silently, log to conflicts[]), and the missing-SEQ ask-back policy (emit `needs_input='starting_seq'` + `'NEEDS_SEQ'` sentinel children). Add one new few-shot example that mirrors the May-22 multi-parent multi-species shape. Existing legacy single-event and batch-mode few-shots stay intact.

Purpose: the prompt is the contract between the extractor and the model; without this revision the model has no reason to emit the new shape.

Output: revised system.js + extended extractor unit test that asserts the few-shot list is well-formed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/47-multi-source-extraction-fusion-groups-shape-inoc-draft/47-CONTEXT.md
@src/agents/alerter/src/extraction/prompts/system.js
@src/agents/alerter/src/extraction/schemas/seeding-session.js
@src/agents/alerter/src/extraction/extractor.js

<interfaces>
extractor.js:53-91 buildInitialUserContent injects a tool_result with tool_use_id='tu_fewshot_3' as the FIRST block of the live user turn. Any new few-shot tool_use ID added BEFORE 'tu_fewshot_3' must have its own tool_result IN THE NEXT user-message block of the few-shot list (per Plan 38-07 Rule 1 bug fix). New few-shot ID convention: 'tu_fewshot_4' (then bump the live-turn tool_result to tu_fewshot_4 if appended after).

CACHEABLE_SYSTEM_BLOCKS and cacheableFewShot() are the two exports consumed by extractor.js:118-119.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add seeding_session policy text to SYSTEM_PROMPT</name>
  <read_first>
    - src/agents/alerter/src/extraction/prompts/system.js (whole file — 238 lines)
    - .planning/phases/47-multi-source-extraction-fusion-groups-shape-inoc-draft/47-CONTEXT.md (Gray Areas 1/3/4 + style locks)
    - src/agents/alerter/src/extraction/schemas/seeding-session.js (canonical field names + sentinels)
  </read_first>
  <files>
    src/agents/alerter/src/extraction/prompts/system.js
    src/agents/alerter/test/extraction/extractor.test.js (assertion add only)
  </files>
  <behavior>
    - SYSTEM_PROMPT gains a "Session vs single-event seeding" section explaining: when the capture describes >1 child block (audio enumerates multiple bags, OR photo shows a multi-row paper log with same date), emit type='seeding_session' with groups[] instead of N separate seeding drafts. Cardinality rule: total children > 1 → seeding_session; total children == 1 AND no session context → seeding.
    - SYSTEM_PROMPT gains a "Provenance" section: every provenanced field carries {value, confidence, sources[]}. sources is a non-empty subset of ['audio','paper_log_photo','bag_label_photo','text','model_inference']. When multiple sources agree, list all of them. When only one contributed, list just that one.
    - SYSTEM_PROMPT gains a "Conflict resolution" section: when audio and paper_log_photo disagree on a value, PHOTO WINS. Set the field's .value to the photo's value, include both sources in .sources, set .confidence to the photo's confidence. ALSO emit an entry in draft.conflicts[] with shape {path, candidates[2], resolution:'photo_wins_implicit'}. Never mention conflicts in any human-readable output text.
    - SYSTEM_PROMPT gains a "Missing SEQ" section: when child block_names have no source (no paper-log photo AND audio doesn't enumerate SEQ numbers), emit child_block_names.value as an array of 'NEEDS_SEQ' literals matching qty, and set draft.needs_input='starting_seq'. The pipeline will ask the farmer back. DO NOT guess a starting SEQ.
    - Style rules from CONTEXT.md still enforced in any new prose (no em-dashes, "block number" not "SEQ" in any farmer-facing string — though this prompt is model-facing, keep the convention).
  </behavior>
  <action>
    Edit the SYSTEM_PROMPT array in prompts/system.js. Insert four new sections after the existing "Field rules" block and before "Year handling (CRITICAL)": (1) "Session vs single-event seeding", (2) "Provenance (groups-shape only)", (3) "Conflict resolution (groups-shape only)", (4) "Missing SEQ ask-back". Use the field-name vocabulary from the seeding-session schema verbatim (parent, species, qty, child_block_names, event_date, groups, conflicts, needs_input). Reference 'NEEDS_SEQ' literal verbatim. Add a sentinel literal example for parent: 'NO_PARENT' when extractor cannot identify a parent batch from any source. Keep the rest of SYSTEM_PROMPT byte-identical.
  </action>
  <verify>
    <automated>cd src/agents/alerter && node -e "const {CACHEABLE_SYSTEM_BLOCKS} = require('./src/extraction/prompts/system'); const t = CACHEABLE_SYSTEM_BLOCKS[0].text; ['seeding_session','NEEDS_SEQ','photo_wins_implicit','needs_input','starting_seq','conflicts'].forEach(k=>{ if(!t.includes(k)) throw new Error('missing '+k); }); console.log('ok')"</automated>
  </verify>
  <done>SYSTEM_PROMPT contains all 6 required tokens above; existing extractor.test.js prompt-shape assertions still pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add May-22-shape multi-parent few-shot example</name>
  <read_first>
    - src/agents/alerter/src/extraction/prompts/system.js (FEW_SHOT array — line ~82 onwards)
    - .planning/phases/47-multi-source-extraction-fusion-groups-shape-inoc-draft/47-CONTEXT.md (INOC-01 canonical shape: 5 groups, 11 children, 260522_SHI_1..3 + 260522_KOY_4..11; KOY parents 118-12 and 425-4 from audio)
    - src/agents/alerter/src/extraction/extractor.js lines 53-91 (buildInitialUserContent tool_result handshake — tu_fewshot_N → tool_result mapping)
    - src/agents/alerter/test/extraction/extractor.test.js (existing few-shot well-formedness pattern)
  </read_first>
  <files>
    src/agents/alerter/src/extraction/prompts/system.js
    src/agents/alerter/test/extraction/extractor.test.js
  </files>
  <behavior>
    - FEW_SHOT array gains a new (user, assistant) pair representing the May-22-shape session. User turn carries a synthesized audio transcript snippet + a paper_log_photo placeholder text block (no real image bytes — few-shot is text-only). Assistant turn emits ONE submit_extraction tool_use whose drafts[0].draft is type='seeding_session', event_date='2026-05-22', groups.length===5, total child_block_names across groups===11, child names exactly 260522_SHI_1, 260522_SHI_2, 260522_SHI_3 in group 1 + 260522_KOY_4..7 in group 2 + 260522_KOY_8..11 in group 3 (the 5-group framing from CONTEXT.md is 3 SHI singles as 3 separate single-child groups + 2 KOY multi-child groups — pick whichever structure best illustrates the policy; document the choice in the SUMMARY).
    - Each provenanced field has sources reflecting where the data came from in the few-shot user turn (parent: ['audio'], species: ['audio','paper_log_photo'], qty: ['audio','paper_log_photo'], child_block_names: ['paper_log_photo']).
    - continuity='start_new' with a one-sentence reason.
    - Per-field-confidence dict has entries keyed on the new shape's field paths.
    - New tool_use ID 'tu_fewshot_4'; a tool_result for tu_fewshot_4 is added as the FIRST content block of the NEXT user turn in the few-shot list (preserving the Plan 38-07 Rule 1 invariant). The existing tu_fewshot_3 handshake done by extractor.buildInitialUserContent in the live turn is NOT broken — append the new pair BEFORE the existing tu_fewshot_3-emitting pair so tu_fewshot_3 stays the last few-shot ID handled by the live turn's tool_result.
    - extractor.test.js gains a test: walk the FEW_SHOT list and assert every tool_use ID has either (a) a tool_result with matching tool_use_id in the immediately following user-turn content, or (b) is the last tool_use in the list (which extractor.js's live-turn handshake handles).
  </behavior>
  <action>
    Edit FEW_SHOT in prompts/system.js. Position the new pair as the second-to-last pair so the existing final pair (currently ending with tu_fewshot_3) remains the boundary that extractor.js's live-turn tool_result closes. Add the tu_fewshot_4 tool_result block as the first element of the user turn that immediately follows the new assistant turn. In extractor.test.js, add a test that iterates FEW_SHOT looking for tool_use blocks and asserts each one is closed by a matching tool_result in the next user turn (except the final one, which is closed by extractor.buildInitialUserContent at runtime).
  </action>
  <verify>
    <automated>cd src/agents/alerter && npx jest test/extraction/extractor.test.js --no-coverage</automated>
  </verify>
  <done>extractor.test.js green including new tool_use/tool_result handshake assertion; FEW_SHOT validates the new few-shot's drafts[0].draft against SeedingSession (assert via Submission.safeParse in the new test).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Prompt → LLM | Prompt drift can silently change extractor behavior; cache_control:'ephemeral' makes regressions cheap to detect via token usage |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-47-02-01 | Tampering | FEW_SHOT well-formedness | mitigate | Task 2 test walks tool_use→tool_result pairing; reproduces Plan 38-07 Rule 1 guard |
| T-47-02-02 | Repudiation | Silent prompt drift across deploys | accept | git history is the audit trail; live-fire in 47-05 catches behavior drift |
</threat_model>

<verification>
- `npx jest test/extraction/extractor.test.js` green
- `grep -c "tu_fewshot_4" src/agents/alerter/src/extraction/prompts/system.js` >= 2 (the tool_use definition + its matching tool_result)
- Manual: visually inspect the new few-shot in a `git diff` to confirm the May-22 shape (5 groups, 11 children, correct child block_names)
</verification>

<success_criteria>
The system prompt now teaches the four new policies (session-vs-single, provenance, conflict, missing-SEQ) and the FEW_SHOT list contains one well-formed multi-parent multi-species example with a tu_fewshot_4 tool_use that is closed by a matching tool_result before the live-turn handshake takes over.
</success_criteria>

<output>
Create `.planning/phases/47-multi-source-extraction-fusion-groups-shape-inoc-draft/47-02-SUMMARY.md` noting: (1) which 5-group structuring was chosen for the few-shot (3 SHI singletons + 2 KOY multi-child OR 1 SHI multi-child + 4 KOY-shaped groups), (2) confirmation that tu_fewshot_3 remains the live-handshake boundary, (3) any token-cost-related observations from a dry extractor.test.js run.
</output>
