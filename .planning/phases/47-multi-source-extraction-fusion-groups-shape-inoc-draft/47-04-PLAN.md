---
phase: 47-multi-source-extraction-fusion-groups-shape-inoc-draft
plan: 04
type: execute
wave: 2
depends_on: ['47-01']
files_modified:
  - src/agents/alerter/src/extraction/preview-builder.js
  - src/agents/alerter/test/extraction/sanitize.test.js
autonomous: true
requirements: [INOC-01]
must_haves:
  truths:
    - "preview-builder.buildPreview accepts a seeding_session draft and returns a non-empty farmer-facing string"
    - "The placeholder preview shows total child count + group count and an explicit 'Group-by-parent preview coming in Phase 48' note (operator-visible) without leaking any 'conflicts' content"
    - "Existing 5-legacy-type preview rendering remains byte-identical (regression guard)"
  artifacts:
    - path: src/agents/alerter/src/extraction/preview-builder.js
      provides: "seeding_session branch in buildPreview"
  key_links:
    - from: src/agents/alerter/src/extraction/preview-builder.js
      to: src/agents/alerter/src/extraction/schemas/seeding-session.js
      via: "buildPreview reads draft.groups[*].child_block_names.value to count total children"
      pattern: "seeding_session"
---

<objective>
Add a minimal placeholder preview for `seeding_session` drafts in `preview-builder.js`. Phase 48 ships the production group-by-parent table; Phase 47's job is to ensure no draft type hits buildPreview without a handler and that the placeholder is honest about being a placeholder while still being scannable by the farmer for sanity check.

Purpose: pipeline.js calls previewBuilder.buildPreview on any ask-back side-effect path. Without a seeding_session branch, a synthetic conflict fixture or any ambiguity would crash the preview render. This plan closes that seam minimally.

Output: extended buildPreview + tests; no farmer-facing claims about conflicts (per Gray Area 4 lock).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/47-multi-source-extraction-fusion-groups-shape-inoc-draft/47-CONTEXT.md
@src/agents/alerter/src/extraction/preview-builder.js
@src/agents/alerter/src/extraction/schemas/seeding-session.js

<interfaces>
buildPreview({draft, perFieldConfidence, threshold, requiredFields}) currently switches on draft.type via TOP_Q_TEMPLATES + classifyField + body rendering. For seeding_session, the body shape (provenanced groups[]) does not fit the current flat-field renderer; we branch early.

sanitizeFarmerText, fmtNum, renderScalar are already in this module.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: seeding_session placeholder branch in buildPreview</name>
  <read_first>
    - src/agents/alerter/src/extraction/preview-builder.js (whole file — 205 lines)
    - .planning/phases/47-multi-source-extraction-fusion-groups-shape-inoc-draft/47-CONTEXT.md (preview minimal-shape spec; Gray Area 4 silent-conflict lock)
    - src/agents/alerter/test/extraction/sanitize.test.js (existing test patterns for preview output)
    - src/agents/alerter/src/extraction/schemas/seeding-session.js (provenanced field shape)
  </read_first>
  <files>
    src/agents/alerter/src/extraction/preview-builder.js
    src/agents/alerter/test/extraction/sanitize.test.js
  </files>
  <behavior>
    - buildPreview, when draft.type === 'seeding_session', returns a short multi-line string:
      Line 1: "{N} blocks across {G} groups for {event_date}" where N = sum of group.qty.value (use group.child_block_names.value.length when available, otherwise group.qty.value) and G = groups.length and event_date is the draft.event_date.
      Line 2: blank
      Line 3: when draft.needs_input === 'starting_seq': "Awaiting block-number to start at." (literal; the actual ask-back prompt is rendered by pipeline's buildStartingSeqAskBackText, not here)
                otherwise: "Group-by-parent preview coming in Phase 48."
      Line 4+: optional — one line per group: "{species.value} x {qty.value} from {parent.value}" using renderScalar; if parent.value === 'NO_PARENT', render "no parent recorded".
    - NEVER include the word "conflict" or any rendering of draft.conflicts[] anywhere in the output (per Gray Area 4 lock). The conflicts[] array is forensics-only.
    - Output passes through sanitizeFarmerText (no em-dashes); numbers through fmtNum.
    - Tests cover: (a) May-22-shape draft (5 groups, 11 children, no needs_input) → string contains "11 blocks across 5 groups", "2026-05-22", per-group lines for each group; (b) needs_input='starting_seq' draft → string contains "Awaiting block-number to start at."; (c) draft with a populated conflicts[] entry → output string MUST NOT contain "118-23" "118-25" "conflict" or any conflict candidate text (use indexOf assertions); (d) parent.value === 'NO_PARENT' → renders "no parent recorded"; (e) existing seeding/activity/observation tests still pass (regression).
  </behavior>
  <action>
    Add a `if (draft && draft.type === 'seeding_session') { ... return sanitizeFarmerText(out); }` early-return branch in buildPreview before the existing required-fields walk. Implement the body per the behavior block. Add the 5 new tests to sanitize.test.js (it already houses preview-builder tests by file convention — verify by reading it; if not, place them in a new test/extraction/preview-builder-session.test.js).
  </action>
  <verify>
    <automated>cd src/agents/alerter && npx jest test/extraction/sanitize.test.js --no-coverage</automated>
  </verify>
  <done>All 5 new tests green; legacy preview tests untouched and green; no occurrence of "conflict" in any seeding_session-branch output.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| draft.conflicts[] (audit-only) → preview output | Renderer must NEVER expose conflict content to farmer per Gray Area 4 lock |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-47-04-01 | Information disclosure | conflicts[] leaked into preview string | mitigate | Test (c) asserts conflict candidate strings absent from output; code branch never touches draft.conflicts |
| T-47-04-02 | Tampering | Preview rendering crashes on missing provenance fields | mitigate | Use safe optional chaining throughout; render '[?]' for any missing provenanced .value |
</threat_model>

<verification>
- `npx jest test/extraction/sanitize.test.js` green
- `grep -i "conflict" src/agents/alerter/src/extraction/preview-builder.js` returns ZERO matches inside the seeding_session branch (the branch may not even reference the word)
</verification>

<success_criteria>
preview-builder.buildPreview no longer crashes or returns empty on a seeding_session draft; the placeholder text is honest about Phase 48 ownership but still gives the farmer a scannable summary (N blocks, G groups, date); conflicts[] are never rendered.
</success_criteria>

<output>
Create `.planning/phases/47-multi-source-extraction-fusion-groups-shape-inoc-draft/47-04-SUMMARY.md` noting: (1) the final placeholder text used, (2) whether tests landed in sanitize.test.js or a new file, (3) any deviation from the 4-line format.
</output>
