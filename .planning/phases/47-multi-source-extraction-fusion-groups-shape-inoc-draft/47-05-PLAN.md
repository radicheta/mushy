---
phase: 47-multi-source-extraction-fusion-groups-shape-inoc-draft
plan: 05
type: execute
wave: 3
depends_on: ['47-02', '47-03', '47-04']
files_modified:
  - src/agents/alerter/test/extraction/integration/seeding-session-may22.test.js
  - src/agents/alerter/test/fixtures/seeding-session-may22/
  - src/agents/alerter/test/extraction/integration/seeding-session-conflict.test.js
  - src/agents/alerter/test/extraction/integration/seeding-session-photo-absent.test.js
autonomous: false
requirements: [INOC-01, INOC-02, INOC-03, INOC-05]
must_haves:
  truths:
    - "Replaying the May 22 captures end-to-end emits exactly 1 seeding_session draft with 5 groups and 11 children matching 260522_SHI_1..3 + 260522_KOY_4..11"
    - "Each provenanced field's sources[] reflects the actual contributing sources from the fixture"
    - "Synthetic conflict fixture produces draft.groups[N].parent.value == photo's value AND draft.conflicts[0] captures both candidates AND the rendered farmer preview contains neither candidate string nor 'conflict'"
    - "Photo-absent fixture produces draft.needs_input='starting_seq' AND every child_block_names.value entry equals 'NEEDS_SEQ' AND a numeric farmer reply via handleStartingSeqReply fills child_block_names with valid BLOCK_NAME_RE strings"
    - "Hermetic unit-test path (mocked Anthropic client) runs in CI; live-fire integration path is gated behind an EVAL_RUN_LIVE=1 env flag mirroring the Phase 44 paid-eval convention"
  artifacts:
    - path: src/agents/alerter/test/fixtures/seeding-session-may22/
      provides: "Local fixtures (audio transcript text + image bytes OR text descriptor) for the May 22 session"
    - path: src/agents/alerter/test/extraction/integration/seeding-session-may22.test.js
      provides: "Ship-gate INOC-01 + INOC-02 test"
    - path: src/agents/alerter/test/extraction/integration/seeding-session-conflict.test.js
      provides: "INOC-03 conflict-logging test"
    - path: src/agents/alerter/test/extraction/integration/seeding-session-photo-absent.test.js
      provides: "INOC-05 ask-back path test"
  key_links:
    - from: test/extraction/integration/seeding-session-may22.test.js
      to: src/extraction/pipeline.js enqueue + handleStartingSeqReply
      via: "end-to-end pipeline call with mocked Anthropic client (or live with EVAL_RUN_LIVE=1)"
      pattern: "createExtractionPipeline"
---

<objective>
Ship-gate plan. Capture three integration tests using real May 22 prod capture data (audio + photo + text follow-up) as gold fixtures + two synthetic edge-case fixtures (conflict + photo-absent). Tests run hermetically in CI with a mocked Anthropic client whose response is hand-crafted to match expected extractor output; the same tests can be promoted to live-API runs via EVAL_RUN_LIVE=1 (~$0.05/run estimated, per Phase 44 cache-empirical baseline) when the operator wants a real ship-gate.

Purpose: the four INOC requirements only matter if a real-data replay proves them. Per `[[feedback_unit_tests_dont_catch_wiring]]`, unit tests with mocks miss seams; this plan provides BOTH the hermetic mock-mode for CI AND a live-API switch for operator-gated ship-gate.

Autonomous: false — Task 2 includes a `checkpoint:human-verify` for live-fire attestation, OR (when the operator chooses) a human-action to fetch the prod capture rows.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/47-multi-source-extraction-fusion-groups-shape-inoc-draft/47-CONTEXT.md
@.planning/phases/47-multi-source-extraction-fusion-groups-shape-inoc-draft/47-01-SUMMARY.md
@.planning/phases/47-multi-source-extraction-fusion-groups-shape-inoc-draft/47-02-SUMMARY.md
@.planning/phases/47-multi-source-extraction-fusion-groups-shape-inoc-draft/47-03-SUMMARY.md
@src/agents/alerter/src/extraction/pipeline.js
@src/agents/alerter/src/extraction/schemas/seeding-session.js
@src/agents/alerter/test/extraction/pipeline.test.js

<interfaces>
Capture IDs in prod signal_capture:
  audio 01KS8KHYTRJDZQEM5C4P989B8B (761-char transcript)
  photo 01KS8KHYTSYYGV500ZQVEY12VX (paper-log image)
  text  01KS8PT5YH9G76Y3BC54TZV19B (131-char follow-up)
Expected emit: 1 seeding_session draft, event_date='2026-05-22', 5 groups, 11 children, child_block_names = 260522_SHI_1..3 + 260522_KOY_4..11.

Pipeline contract: createExtractionPipeline({pool, extractor, extractionDb, stateMachine, previewBuilder, config, outboundDispatcher}). For integration tests use a real in-memory pool (pg-mem) OR a stubbed pool object — match whichever pattern the existing test/extraction/integration.test.js uses.

Anthropic mock pattern: extractor accepts {client: injectedClient}. Inject a jest.fn() that returns the expected tool_use response shape: { content: [{type:'tool_use', name:'submit_extraction', input: {drafts:[{draft:{...}, per_field_confidence:{...}}], continuity:'start_new', continuity_reason:'...'}}], usage: {input_tokens:0,output_tokens:0,...} }.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Hermetic ship-gate tests (mocked Anthropic) — INOC-01/02/03/05</name>
  <read_first>
    - All three SUMMARY files from 47-01, 47-02, 47-03 (output contracts agreed there)
    - src/agents/alerter/test/extraction/integration.test.js (existing integration harness style)
    - .planning/phases/47-multi-source-extraction-fusion-groups-shape-inoc-draft/47-CONTEXT.md (canonical expected output for each fixture)
    - src/agents/alerter/src/extraction/pipeline.js (post-47-03 shape — enqueue + handleStartingSeqReply)
  </read_first>
  <files>
    src/agents/alerter/test/extraction/integration/seeding-session-may22.test.js (new)
    src/agents/alerter/test/extraction/integration/seeding-session-conflict.test.js (new)
    src/agents/alerter/test/extraction/integration/seeding-session-photo-absent.test.js (new)
    src/agents/alerter/test/fixtures/seeding-session-may22/transcript.txt (new — paste from prod capture; or hand-crafted equivalent if prod read deferred to Task 2)
    src/agents/alerter/test/fixtures/seeding-session-may22/text-followup.txt (new)
    src/agents/alerter/test/fixtures/seeding-session-may22/paper-log-photo.jpg (new — copied from prod or fixture-substitute)
    src/agents/alerter/test/fixtures/seeding-session-may22/expected-extractor-response.json (new — golden mock response)
  </files>
  <behavior>
    - may22.test.js: instantiates createExtractionPipeline with a stub pool (jest mock), real extractionDb (or jest mock that records insertDraft/updateDraftStatus calls), stateMachine, the real preview-builder, and a mock outboundDispatcher. Anthropic client is injected as a jest.fn returning expected-extractor-response.json. Pipeline.enqueue is called with the May 22 capture (text+transcript+image_paths pointing at the fixture). Assertions: extractionDb.insertDraft was called once with draft_json.type==='seeding_session', .event_date==='2026-05-22', .groups.length===5, sum-of-children===11, child_block_names.value flat array deep-equals ['260522_SHI_1','260522_SHI_2','260522_SHI_3','260522_KOY_4','260522_KOY_5','260522_KOY_6','260522_KOY_7','260522_KOY_8','260522_KOY_9','260522_KOY_10','260522_KOY_11']. Each provenanced field's sources[] is non-empty and is a subset of SOURCE_ENUM. Side-effect dispatched is the standard ask-back-or-confirm path (not send_starting_seq_askback).
    - conflict.test.js: same harness but expected-extractor-response payload has groups[1].parent.value='260118_SHI_25' (photo's value), groups[1].parent.sources=['audio','paper_log_photo'], draft.conflicts=[{path:'groups[1].parent.value', candidates:[{value:'260118_SHI_23',source:'audio',confidence:0.7},{value:'260118_SHI_25',source:'paper_log_photo',confidence:0.95}], resolution:'photo_wins_implicit'}]. Assertions: draft persisted with conflicts[] intact. preview-builder.buildPreview output called on this draft contains NEITHER '118-23' NOR '118-25' NOR 'conflict' anywhere.
    - photo-absent.test.js: expected-extractor-response payload has draft.needs_input='starting_seq', every child_block_names.value entry === 'NEEDS_SEQ'. Assertions: pipeline dispatches 'send_starting_seq_askback' exactly once; draft status is AWAITING_FARMER; farmer_facing_preview contains "block number". Then call handleStartingSeqReply({draftId, replyText:'4', captureCtx:{senderName:'Santi'}}); assert child_block_names across all groups now contain only strings matching BLOCK_NAME_RE, sequential starting at 4, needs_input cleared, side-effect 'send_seeding_session_filled_preview' dispatched.
    - All three tests are jest unit tests, no network IO; estimated runtime <2s each.
  </behavior>
  <action>
    Build the three test files plus the four fixture files. For the May 22 fixtures, EITHER (a) hand-craft a representative transcript text + photo descriptor that matches the canonical CONTEXT.md shape, OR (b) defer the prod-capture read to Task 2 and leave the fixture files as placeholders with TODO markers — pick (a) for Task 1 so the hermetic test runs in CI immediately. Build expected-extractor-response.json by hand to match the locked CONTEXT.md output spec — this is the gold answer the mocked LLM "produces". The test asserts pipeline behavior given this exact LLM output; downstream Task 2 swaps the mock for a real Anthropic call to prove the live model also produces (close to) this output. Configure jest.config or test-runner glob to pick up test/extraction/integration/*.test.js.
  </action>
  <verify>
    <automated>cd src/agents/alerter && npx jest test/extraction/integration/seeding-session --no-coverage</automated>
  </verify>
  <done>All three integration tests green; mock-mode CI run completes in <10s total; INOC-01/02/03/05 all have a hermetic regression guard.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Live-fire ship-gate attestation (operator-gated)</name>
  <read_first>
    - .planning/phases/47-multi-source-extraction-fusion-groups-shape-inoc-draft/47-05-SUMMARY.md (if Task 1 already wrote it) OR Task 1's test artifacts
    - .planning/notes/2026-05-23-live-fire-paper-trail.md if present, or the Phase 44 live-fire convention (per `[[feedback_persist_paid_results_default]]`)
  </read_first>
  <files>
    src/agents/alerter/test/extraction/integration/seeding-session-may22.test.js (toggle EVAL_RUN_LIVE branch only)
    .planning/phases/47-multi-source-extraction-fusion-groups-shape-inoc-draft/47-LIVE-FIRE.md (new — paper trail)
    src/agents/alerter/test/fixtures/seeding-session-may22/* (replace hand-crafted fixtures with prod-capture-extracted ones)
  </files>
  <what-built>
    Hermetic ship-gate tests are green in CI. The remaining question is whether the LIVE Anthropic client, given the REAL May 22 audio transcript + photo bytes from prod signal_capture, also emits the expected seeding_session shape.
  </what-built>
  <how-to-verify>
    1. From elder-plops, run: `PG="postgresql://...mossrock prod..." node -e "const {Client}=require('pg'); const c=new Client({connectionString:process.env.PG}); c.connect().then(async()=>{const r=await c.query(\"SELECT id, text, transcripts, attachment_paths FROM signal_capture WHERE id IN ('01KS8KHYTRJDZQEM5C4P989B8B','01KS8KHYTSYYGV500ZQVEY12VX','01KS8PT5YH9G76Y3BC54TZV19B')\"); console.log(JSON.stringify(r.rows,null,2)); await c.end();})"` and copy the three rows into src/agents/alerter/test/fixtures/seeding-session-may22/ (transcript.txt = audio row's transcripts joined; text-followup.txt = text row's text; paper-log-photo.jpg = first attachment_path of photo row).
    2. Run live-fire: `cd src/agents/alerter && EVAL_RUN_LIVE=1 ANTHROPIC_API_KEY=$ANTHROPIC_API_KEY npx jest test/extraction/integration/seeding-session-may22.test.js --no-coverage`.
    3. Save full transcript (stdout + stderr + final draft JSON) to `.planning/phases/47-multi-source-extraction-fusion-groups-shape-inoc-draft/47-LIVE-FIRE.md` with timestamp + cost (sum input_tokens * input_price + output_tokens * output_price for claude-sonnet-4-6).
    4. Visually verify the live draft matches the expected (5 groups, 11 children, names 260522_SHI_1..3 + 260522_KOY_4..11). Acceptable deviation: KOY parent strings may differ in exact format (e.g., '260118_KOY_12' vs '260118_KOY_25') because the audio's "118-12" decoding depends on the model's parent-batch inference — flag any deviation in the SUMMARY.
    5. If the live draft has the right shape but different parent strings, that's a Phase 47 PASS (per CONTEXT.md INOC-01 wording — the named child_block_names list is the regression guard, not the parent strings). If the live draft has fewer/more groups or wrong child count, FAIL — open a follow-up to tune the prompt and re-run.
  </how-to-verify>
  <resume-signal>Type "live-fire PASS" + paste the resulting draft summary, or "live-fire FAIL" + describe the deviation</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| prod signal_capture rows → test fixtures | Real prod data may carry PII; fixture path stays gitignored if it does (verify before commit) |
| Live Anthropic API call | Paid; cost-bounded by max_tokens=16384 + prompt caching |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-47-05-01 | Information disclosure | Prod capture content in test fixtures | mitigate | If audio/photo carry PII, add fixture path to .gitignore; commit only sanitized derivatives |
| T-47-05-02 | Tampering | Mock response drifts from live behavior | mitigate | Task 2 live-fire attestation is the proof that mock and live agree |
| T-47-05-03 | DoS | Runaway live-fire spend | mitigate | EVAL_RUN_LIVE=1 gate; max_tokens=16384 cap; single-fixture single-call ≈ $0.05 per Phase 44 baseline |
| T-47-05-SC | Tampering | No new packages | accept | Plan adds zero npm deps |
</threat_model>

<verification>
- Task 1: `npx jest test/extraction/integration/seeding-session` all 3 tests green hermetically
- Task 2: `47-LIVE-FIRE.md` exists with timestamp, cost, and PASS/FAIL verdict; live draft contains the 11 named child_block_names
- `grep -c "260522_SHI\|260522_KOY" .planning/phases/47-*/47-LIVE-FIRE.md` returns ≥11
</verification>

<success_criteria>
INOC-01..03 + INOC-05 each have a hermetic CI regression guard AND an operator-attested live-fire run on the named real May 22 prod capture. Mock-mode keeps CI fast and free; live-mode is the ship-gate proof that the prompt + schema + pipeline actually compose against the real Anthropic API on the real fixture. INOC-04 (single-parent legacy session, carry-forward) is NOT covered here — Phase 48 owns it.
</success_criteria>

<output>
Create `.planning/phases/47-multi-source-extraction-fusion-groups-shape-inoc-draft/47-05-SUMMARY.md` recording: (1) Task 1 hermetic test results, (2) Task 2 live-fire timestamp + cost + verdict, (3) any deviations between mock and live output, (4) confirmation that INOC-01/02/03/05 all have both a hermetic and a live attestation. Note INOC-04 explicitly as Phase 48 scope.
</output>
