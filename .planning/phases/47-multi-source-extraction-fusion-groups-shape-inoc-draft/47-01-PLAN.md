---
phase: 47-multi-source-extraction-fusion-groups-shape-inoc-draft
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/agents/alerter/src/extraction/schemas/seeding-session.js
  - src/agents/alerter/src/extraction/schemas/provenance.js
  - src/agents/alerter/src/extraction/schemas/index.js
  - src/agents/alerter/src/extraction/state-machine.js
  - src/agents/alerter/test/extraction/schemas.test.js
  - src/agents/alerter/test/extraction/seeding-session.test.js
autonomous: true
requirements: [INOC-01, INOC-02, INOC-03]
must_haves:
  truths:
    - "Zod SeedingSession schema parses a 5-group/11-child May-22-shape input and rejects off-schema fields"
    - "Each provenanced field validates as {value, confidence, sources[]} with sources from a fixed enum"
    - "Draft discriminated union still accepts all 5 legacy types (seeding/activity/input/observation/harvest) AND seeding_session"
    - "DRAFT_JSON_SCHEMA / SUBMISSION_JSON_SCHEMA round-trip through inlineTopLevelRef without breaking Anthropic tools[0].input_schema shape"
  artifacts:
    - path: src/agents/alerter/src/extraction/schemas/seeding-session.js
      provides: "SeedingSession + SeedingSessionGroup + ConflictEntry Zod schemas"
    - path: src/agents/alerter/src/extraction/schemas/provenance.js
      provides: "Provenanced<T> factory + SOURCE_ENUM"
    - path: src/agents/alerter/src/extraction/schemas/index.js
      provides: "Discriminated-union extension + LOG_TYPES updated"
  key_links:
    - from: src/agents/alerter/src/extraction/schemas/index.js
      to: src/agents/alerter/src/extraction/schemas/seeding-session.js
      via: "require + Draft = z.discriminatedUnion('type', [..., SeedingSession])"
      pattern: "SeedingSession.*require.*seeding-session"
---

<objective>
Foundation plan for Phase 47. Introduce three new Zod schemas — `SeedingSession`, `SeedingSessionGroup`, `ConflictEntry` — plus a reusable `Provenanced<T>` factory, and extend the existing `Draft` discriminated union with the new `seeding_session` top-level type. Legacy `SeedingLog` stays. Adds `seeding_session` to `LOG_TYPES` and `REQUIRED_FIELDS`.

Purpose: every other Phase 47 plan (prompt, pipeline, preview, ship-gate) depends on this contract existing as code. Interface-first ordering — write contracts before consumers.

Output: two new schema files, an extended `schemas/index.js` and `state-machine.js`, and unit tests proving the shape parses and rejects.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/47-multi-source-extraction-fusion-groups-shape-inoc-draft/47-CONTEXT.md
@src/agents/alerter/src/extraction/schemas/index.js
@src/agents/alerter/src/extraction/schemas/seeding.js
@src/agents/alerter/src/extraction/schemas/observation.js
@src/agents/alerter/src/extraction/state-machine.js

<interfaces>
Existing union (schemas/index.js):
  Draft = z.discriminatedUnion('type', [SeedingLog, ActivityLog, InputLog, ObservationLogBase, HarvestLog])
  LOG_TYPES = ['seeding','activity','input','observation','harvest']
  DRAFT_JSON_SCHEMA = zodToJsonSchema(Draft, 'Draft')

Existing block-name regex (schemas/seeding.js): BLOCK_NAME_RE = /^[0-9]{6}_[A-Z]{2,4}_[0-9]+$/

State-machine REQUIRED_FIELDS map (state-machine.js:23) — extend with seeding_session entry.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Provenance factory + ConflictEntry + SeedingSession schemas</name>
  <read_first>
    - .planning/phases/47-multi-source-extraction-fusion-groups-shape-inoc-draft/47-CONTEXT.md (Gray Area 1, 2, 4 lock sections — schema shape is non-negotiable)
    - src/agents/alerter/src/extraction/schemas/seeding.js (style: z.object().strict(), BLOCK_NAME_RE reuse)
    - src/agents/alerter/src/extraction/schemas/observation.js (style: ObservationLogBase vs refine'd export pattern)
  </read_first>
  <files>
    src/agents/alerter/src/extraction/schemas/provenance.js (new)
    src/agents/alerter/src/extraction/schemas/seeding-session.js (new)
    src/agents/alerter/test/extraction/seeding-session.test.js (new)
  </files>
  <behavior>
    - provenance.js exports SOURCE_ENUM = z.enum(['audio','paper_log_photo','bag_label_photo','text','model_inference']) and a Provenanced(valueSchema) factory returning z.object({ value: valueSchema, confidence: z.number().min(0).max(1), sources: z.array(SOURCE_ENUM).min(1) }).strict()
    - seeding-session.js exports ConflictEntry = z.object({ path: z.string().min(1), candidates: z.array(z.object({ value: z.unknown(), source: SOURCE_ENUM, confidence: z.number().min(0).max(1) }).strict()).min(2), resolution: z.enum(['photo_wins_implicit','ask_back_required','accepted_consensus']) }).strict()
    - seeding-session.js exports SeedingSessionGroup with these provenanced fields: parent = Provenanced(z.string().min(1)) (CAN be the sentinel literal 'NO_PARENT' when extractor cannot infer), species = Provenanced(z.string().regex(/^[A-Z]{2,4}$/)), qty = Provenanced(z.number().int().positive()), child_block_names = Provenanced(z.array(z.string()).min(1)). child_block_names value strings MUST satisfy either BLOCK_NAME_RE (re-exported from seeding.js) OR the literal 'NEEDS_SEQ' sentinel — enforce via z.union or a regex disjunction; tests must cover both branches.
    - seeding-session.js exports SeedingSession = z.object({ type: z.literal('seeding_session'), event_date: z.string().regex(/^\d{4}-\d{2}-\d{2}$/), groups: z.array(SeedingSessionGroup).min(1), needs_input: z.enum(['starting_seq']).optional(), conflicts: z.array(ConflictEntry).optional(), notes: z.string().optional() }).strict()
    - Tests cover: (a) May-22-shape happy path (5 groups, 11 children) parses; (b) off-schema field at top level → rejected; (c) provenanced field missing 'sources' → rejected; (d) child_block_names with mixed 'NEEDS_SEQ' + valid block_names → parses; (e) ConflictEntry with only 1 candidate → rejected; (f) species lowercase → rejected.
  </behavior>
  <action>
    Create the three files per the behavior block. Re-export BLOCK_NAME_RE from seeding.js (do not duplicate the regex). Use `.strict()` on every z.object per the EXT-01 anchor. Match the existing file header comment style (purpose, plan reference, EXT anchor). Place tests in test/extraction/seeding-session.test.js using the same jest+require style as schemas.test.js.
  </action>
  <verify>
    <automated>cd src/agents/alerter && npx jest test/extraction/seeding-session.test.js --no-coverage</automated>
  </verify>
  <done>All 6 behavior tests pass; both new schema files exist with .strict() z.object exports; no off-schema fields tolerated.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire SeedingSession into Draft union + extend LOG_TYPES + REQUIRED_FIELDS</name>
  <read_first>
    - src/agents/alerter/src/extraction/schemas/index.js (whole file — discriminatedUnion + DRAFT_JSON_SCHEMA + Submission wrapper)
    - src/agents/alerter/src/extraction/state-machine.js lines 20-50 (REQUIRED_FIELDS map + DRAFT_STATUS)
    - src/agents/alerter/src/extraction/extractor.js lines 30-55 (inlineTopLevelRef — verify new shape still inlines)
    - src/agents/alerter/test/extraction/schemas.test.js (existing union test pattern)
    - .planning/phases/47-multi-source-extraction-fusion-groups-shape-inoc-draft/47-CONTEXT.md (Gray Area 1 — coexistence with SeedingLog)
  </read_first>
  <files>
    src/agents/alerter/src/extraction/schemas/index.js
    src/agents/alerter/src/extraction/state-machine.js
    src/agents/alerter/test/extraction/schemas.test.js
  </files>
  <behavior>
    - Draft discriminatedUnion includes SeedingSession as a 6th member; LOG_TYPES freezes to ['seeding','activity','input','observation','harvest','seeding_session']
    - DRAFT_JSON_SCHEMA still validates as an Anthropic-compatible single JSON object after the union extension (no top-level $ref blow-up after inlineTopLevelRef)
    - state-machine.REQUIRED_FIELDS gains a 'seeding_session' entry: ['event_date','groups']
    - schemas.test.js: a new test parses a minimal valid seeding_session through the Draft union AND through Submission.drafts[0].draft; rejects a seeding_session with empty groups[]
    - Existing 5 legacy types still parse (regression guard — keep current tests green)
  </behavior>
  <action>
    Add `const { SeedingSession } = require('./seeding-session');` to schemas/index.js. Append SeedingSession to the discriminatedUnion array. Append 'seeding_session' to LOG_TYPES. Export SeedingSession from module.exports. In state-machine.js, add `seeding_session: ['event_date','groups']` to REQUIRED_FIELDS. In schemas.test.js, add tests asserting Draft.safeParse and Submission.safeParse accept a minimal seeding_session and reject the empty-groups case; verify the existing 5-type assertions still pass.
  </action>
  <verify>
    <automated>cd src/agents/alerter && npx jest test/extraction/schemas.test.js test/extraction/state-machine.test.js test/extraction/seeding-session.test.js --no-coverage</automated>
  </verify>
  <done>All schema + state-machine + new-session tests green; LOG_TYPES has 6 entries; REQUIRED_FIELDS['seeding_session'] = ['event_date','groups']; no regression on legacy types.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM tool_use → validator | Anthropic submits arbitrary JSON; Zod is the gate |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-47-01-01 | Tampering | SeedingSession.groups[] | mitigate | `.strict()` on every nested object; SOURCE_ENUM closed set; BLOCK_NAME_RE re-used (no string-typed escape hatch) |
| T-47-01-02 | Information disclosure | conflicts[] persisted in draft_json | accept | Internal forensics field; never rendered to farmer (Gray Area 4 lock); audit-only |
| T-47-01-03 | DoS | Unbounded groups[] | mitigate | min(1) enforced; no cap (real sessions ≤ 8); add cap if real abuse appears |
</threat_model>

<verification>
- `npx jest test/extraction/seeding-session.test.js test/extraction/schemas.test.js test/extraction/state-machine.test.js` all green
- `grep -n "seeding_session" src/agents/alerter/src/extraction/schemas/index.js src/agents/alerter/src/extraction/state-machine.js` returns ≥3 hits
- `node -e "console.log(JSON.stringify(require('./src/agents/alerter/src/extraction/schemas').DRAFT_JSON_SCHEMA).length)"` succeeds (smoke for JSON Schema generation)
</verification>

<success_criteria>
SeedingSession, SeedingSessionGroup, ConflictEntry, Provenanced<T> all exist as `.strict()` Zod schemas and are wired into the Draft discriminated union without breaking any of the 5 legacy log-type parsers. Phase 48's commit fan-out and Phase 47-02..05 can import these schemas directly.
</success_criteria>

<output>
Create `.planning/phases/47-multi-source-extraction-fusion-groups-shape-inoc-draft/47-01-SUMMARY.md` summarizing artifacts created, tests added, and any deviations (especially around the `'NEEDS_SEQ'` sentinel handling in child_block_names since downstream plans depend on it).
</output>
