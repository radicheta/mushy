# Phase 38 -- 2026-05-12 mid-day pause, handoff for next session

**Status:** uncommitted work tree. Don Santiago paused the session because Claude was sloppy.

## TL;DR of the session

1. Verified last night's Plan 07 "PASS" verdict was structurally honest but content-vacuous: B5 0% precision was a SCORER bug (no GT supplied -> precision = 0/N regardless of extraction quality), not an extractor bug.
2. Found and fixed three real bugs in the extractor stack while testing on IMG_3775 + IMG_3800:
   - **Eval harness threw away expensive drafts** -- partial.json existed only every 10 fixtures and was unreliable; final results were never persisted. Patched to stream per-fixture JSONL + write final results.json + capture Anthropic usage tokens for real cost telemetry.
   - **Year hallucination** -- mushdatadump = 2025 paper notebook with no year written on pages; extractor was inventing years (2002 last night, 2026 this morning) at ~0.6 confidence. Fixed via `corpusContext: {default_year: 2025}` plumbed through extractor + prompt.
   - **B5 regex too strict** -- `{A-Z}{3}` hardcoded in `BLOCK_NAME_RE`; production has 2-letter codes (DT). Relaxed to `{A-Z}{2,4}`.
3. Structural change: extractor moved from single-`draft` to **multi-`drafts[]`** shape to handle real paper-log pages (21-24 individuation events per page across multiple species, with parent-batch lineage). Schema, prompt, few-shot, validator, and extractor unit tests all migrated. 125/125 unit tests green.
4. Live API verification on IMG_3800: **21/21 drafts match Don Santiago's ground truth perfectly**. Date 2025-08-06, species DT/CAS/SHI, parent-batch shorthand `0627-2` -> `250627_DT_2` all correct.

## Mistakes this session (so next session doesn't repeat them)

- **Burned an IMG_3775 24-draft live API result** by re-running the script with a "persistence" that overwrote the same JSON file. Added per-image timestamped files + append-only JSONL aggregate AFTER the data was lost. Cost: ~$0.09 of redo if Don Santiago wants 3775 GT.
- **Took the Plan 07 "PASS" verdict at face value last night** and lost confidence in the eval -- but the B5 0% was a flag I should have spotted before claiming "structurally honest" without checking the scorer code.
- **Didn't ask about corpus context** before running on mushdatadump last night. Don Santiago calls his paper logs "the 2025 notebook"; this should have been a question before the first paid eval.

## Files changed (all uncommitted)

```
src/agents/alerter/src/extraction/schemas/seeding.js       # parent_batch_name + regex relax
src/agents/alerter/src/extraction/schemas/index.js          # Submission -> {drafts[], continuity, ...}
src/agents/alerter/src/extraction/validator.js              # walks drafts[] for observation refine
src/agents/alerter/src/extraction/prompts/system.js         # multi-event + corpus_context + new few-shot
src/agents/alerter/src/extraction/extractor.js              # corpusContext arg, usage capture, drafts[] return + legacy fallback
src/agents/alerter/test/extraction/extractor.test.js        # validInput() uses drafts[] shape
src/agents/alerter/test/eval/extraction/mushdatadump.test.js # EVAL_MAX_FIXTURES, per-fixture JSONL, results.json, usage aggregation
src/agents/alerter/test/eval/extraction/scoring.js          # b5PrecisionRecall adds regexValidRate
src/agents/alerter/test/eval/extraction/report.js           # surfaces usage + cost + regex-valid rate
src/agents/alerter/scripts/eval-two.js                       # NEW -- live 1-or-2-image extractor smoke; per-image timestamped output
```

## Open work (NOT done this session)

- **Pipeline rewiring.** `pipeline.js`, `state-machine.js`, `outbound.js` still consume the old single-`draft` shape via the back-compat fallback. New `drafts[]` shape means **only draft #1 reaches farmOS** if you trigger the live alerter today. The pipeline must iterate `result.drafts` and route each through state-machine + outbound separately. ETA: medium chunk; touches the persistence/queue logic in pipeline.js.
- **IMG_3775 ground truth.** Don Santiago has not supplied per-event GT for 3775. The 24-draft extraction from earlier this session was destroyed by the overwrite bug; re-running costs ~$0.09. Worth doing only if Don Santiago wants a second GT-anchored fixture.
- **AI-SPEC / CONTEXT updates.** D-04 still says "{SPECIES3}"; CONTEXT does not mention multi-event-per-page or parent_batch_name. Worth folding into 38-CONTEXT.md or a SPEC delta before Plan 08 commits.

## Live API spend this session

~$0.30 across 5 calls (initial smoke, IMG_3800 truncation failures x2, IMG_3800 success, IMG_3775 success-then-lost).

## Confirmed ground truth (Don Santiago, 2026-05-12)

**IMG_3800.jpg** -> 21 individuation events, 2025-08-06, page-level SEQ 1-21, species mix DT/CAS/SHI, parent-batch via MMDD-SEQ shorthand decoded with corpus year + column species (e.g. `0627-2` in DT column -> `250627_DT_2`).

Saved in: `.planning/phases/38-extraction-pipeline/plan08-runs/IMG_3800_rescued-2026-05-12.json`
