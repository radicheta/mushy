---
phase: 38-extraction-pipeline
plan: 06
type: execute
wave: 2
depends_on: [04, 05]
files_modified:
  - src/agents/alerter/src/extraction/outbound.js
  - src/agents/alerter/src/extraction/pipeline.js
  - src/agents/alerter/src/index.js
  - src/agents/alerter/test/extraction/outbound.test.js
autonomous: true
requirements: [EXT-04]
must_haves:
  truths:
    - "When state transitions to AWAITING_FARMER, outbound.dispatch('send_ask_back', draftRow) sends the farmer_facing_preview via signalClient honoring the originating capture's reply_target_kind (DM vs group)"
    - "When state transitions to NEEDS_REVIEW, outbound.dispatch('send_needs_review_ping', draftRow) sends a Signal DM to Don Santiago using the OPERATOR_E164 env (existing convention), addressing him as Don Santiago (NOT 'operator' as referent), with no em-dashes"
    - "All outbound text is sanitized via preview-builder.sanitizeFarmerText before transmission"
    - "Outbound failures are logged but never throw -- the draft row stays in its new state and a retry path is left for follow-up phases"
    - "Group sends use group_id from the originating capture (Phase 37 internal_id form, not the prefixed group.<...> form -- per STATE decision [37-04])"
  artifacts:
    - path: "src/agents/alerter/src/extraction/outbound.js"
      provides: "createOutboundDispatcher({signalClient, config, logger}) -> {dispatch}"
      exports: ["createOutboundDispatcher"]
    - path: "src/agents/alerter/test/extraction/outbound.test.js"
      provides: "Mocked signalClient.send + DM vs group routing + needs-review ping + sanitize verification"
  key_links:
    - from: "outbound.js"
      to: "signalClient.send"
      via: "signalClient.send({target: dmOrGroup, text: sanitized})"
      pattern: "signalClient\\.send"
    - from: "pipeline.js"
      to: "outbound.dispatch"
      via: "stub replaced by real dispatcher at index.js boot"
      pattern: "outboundDispatcher\\.dispatch"
---

<objective>
Replace Plan 05's outbound stub with real Signal sends. Two outbound paths: (1) ask-back to the originating farmer honoring DM-vs-group, (2) needs-review ping to Don Santiago when an ask-back loop terminates without resolution.

Purpose: This is the last piece of EXT-04. Without it, drafts get stuck in AWAITING_FARMER but no farmer ever hears from us. With it, the loop closes.

Output: outbound.js dispatcher + unit tests + boot wiring in src/index.js.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/38-extraction-pipeline/38-CONTEXT.md
@.planning/phases/38-extraction-pipeline/38-PATTERNS.md
@.planning/phases/38-extraction-pipeline/38-04-PLAN.md
@.planning/phases/38-extraction-pipeline/38-05-PLAN.md

@src/agents/alerter/src/signal.js
@src/agents/alerter/src/config.js
@src/agents/alerter/src/extraction/preview-builder.js
@src/agents/alerter/src/index.js

<interfaces>
<!-- signalClient.send shape (mirror existing alerter usage in src/index.js + src/snooze.js): -->
<!--   await signalClient.send({ target: <string-or-group-object>, text: <sanitized string> }) -->
<!--   returns { ok:true } | { ok:false, reason } -->

<!-- Reply target shape (Phase 37 D-14/D-15): -->
<!--   if reply_target_kind === 'dm':   target = sender E.164 -->
<!--   if reply_target_kind === 'group':target = { groupId: <bare internal_id> }   // signal.js wraps the prefix; see STATE decision [37-04] -->

<!-- Operator-ping recipient (STATE memory: project_farmer_phone_map): -->
<!--   Don Santiago (Santi) -- +59892893012, slug 'santi' -->
<!--   Reuse env: SIGNAL_DEFAULT_RECIPIENT is currently Santi in elder-plops .env. -->
<!--   For Plan 06, add a config-derived `operatorRecipient` keyed to SIGNAL_DEFAULT_RECIPIENT (same person). -->
<!--   In the needs-review message, address as "Don Santiago" or "Mr Human" or "El Presidente" -- NEVER "operator" as a referent. -->
<!--   ("operator" the abstract role name is fine in code/comments; this rule applies to farmer-/operator-facing message text.) -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: outbound.js dispatcher + tests (RED then GREEN)</name>
  <read_first>
    - src/agents/alerter/src/signal.js (full -- send signature + group handling)
    - src/agents/alerter/src/extraction/preview-builder.js (sanitizeFarmerText export)
    - .planning/STATE.md Decisions section: [37-04] SIGNAL_GROUP_ID is bare internal_id form; signal.js wraps the prefix
    - memory: feedback_dont_say_operator_say_don_santiago (planner_context: "operator" as abstract role name is fine; never as referent for Santi-the-human)
    - memory: project_farmer_phone_map (Don Santiago = +59892893012)
  </read_first>
  <behavior>
    createOutboundDispatcher({signalClient, config, logger, previewBuilder, operatorRecipient}):
    - Returns {dispatch}.
    - dispatch(sideEffect, draftRow): async, never throws.
      - 'send_ask_back':
        - text = previewBuilder.sanitizeFarmerText(draftRow.farmer_facing_preview).
        - target = draftRow.reply_target_kind === 'group' ? { groupId: draftRow.group_id } : draftRow.sender_e164
        - await signalClient.send({target, text}); log result (warn on ok:false, info on ok:true with first 40 chars of text).
      - 'send_needs_review_ping':
        - text = sanitize(`Hey Don Santiago, draft ${draftRow.id.slice(0,10)} for ${draftRow.sender_e164} hit the 3-turn ask-back cap. Marked for manual review. Reason: ${draftRow.needs_review_reason || 'askback_cap'}.`)
        - target = operatorRecipient (E.164 from config / env)
        - send + log.
      - 'mark_expired' / 'handoff_to_phase_39' / 'noop': no-op (logger.debug).
      - Unknown sideEffect: logger.warn.
    - Guarantees:
      - `target` is never undefined (validate before send; on failure log + return {ok:false, reason:'no_target'}).
      - Output text always passes sanitize.
      - No em-dash present (test asserts on text after dispatch).
  </behavior>
  <action>
    1. Write `test/extraction/outbound.test.js` RED. Use:
       - `const signalClient = { send: jest.fn().mockResolvedValue({ok:true}) }`
       - `const previewBuilder = require('../../src/extraction/preview-builder')`
       - silentLogger
       Test cases:
       - `(send_ask_back DM) draftRow with reply_target_kind:'dm' -> signalClient.send called with target = sender_e164`
       - `(send_ask_back group) draftRow with reply_target_kind:'group', group_id:'someId' -> signalClient.send called with target = { groupId: 'someId' }`
       - `(send_ask_back text sanitized) farmer_facing_preview containing em-dash -> outbound text strips it`
       - `(send_needs_review_ping) target = operatorRecipient, text contains 'Don Santiago' and does NOT contain 'operator' as a referent`
       - `(send_needs_review_ping) text contains the truncated draft id and the sender E.164`
       - `(send_needs_review_ping) text contains no em-dash`
       - `(handoff_to_phase_39) -> no signalClient.send call`
       - `(mark_expired) -> no signalClient.send call`
       - `(noop unknown side effect) -> logger.warn called, no send`
       - `(signalClient.send rejects) -> dispatch returns {ok:false, reason}, never throws`
       Run -- RED. Commit RED: `test(38-06): outbound dispatcher for ask-back + needs-review`.
    2. Implement `src/extraction/outbound.js`. Run tests -- GREEN. Commit: `feat(38-06): outbound dispatcher for ask-back + needs-review`.

    Linguistic discipline (verified):
    - No `—` anywhere in source or output.
    - Operator-facing text uses "Don Santiago" (or "Mr Human", "El Presidente" -- pick "Don Santiago" as canonical).
    - Numbers in any operator-facing text run through fmtNum (no float soup, even in a ping message).
  </action>
  <verify>
    <automated>cd src/agents/alerter && npm test -- test/extraction/outbound.test.js</automated>
  </verify>
  <acceptance_criteria>
    - >=10 outbound tests green.
    - `cd src/agents/alerter && grep -E "—" src/extraction/outbound.js` no matches.
    - `cd src/agents/alerter && grep "Don Santiago" src/extraction/outbound.js` >= 1.
    - `cd src/agents/alerter && grep -E "^[[:space:]]*['\"\\\`].*operator.*['\"\\\`]" src/extraction/outbound.js` returns no farmer-facing strings using "operator" as a referent (audit any matches -- code variable names like `operatorRecipient` are fine; STRING LITERALS sent to farmers/operator are NOT).
    - Full alerter suite still green: `cd src/agents/alerter && npm test`.
  </acceptance_criteria>
  <done>outbound dispatcher landed with tested DM/group routing + sanitization + needs-review ping addressing Don Santiago.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire real outbound dispatcher in src/index.js (replace Plan 05 stub)</name>
  <read_first>
    - src/agents/alerter/src/index.js (current pipeline construction + signalClient creation)
    - .planning/phases/38-extraction-pipeline/38-05-PLAN.md (stub outboundDispatcher injection point)
    - src/agents/alerter/src/extraction/outbound.js (Task 1 output)
  </read_first>
  <behavior>
    - On boot, after signalClient is created and config is loaded, instantiate `outboundDispatcher = createOutboundDispatcher({signalClient, config, logger, previewBuilder, operatorRecipient: config.signalDefaultRecipient})`.
    - Pass into createExtractionPipeline in place of the stub.
    - Boot log: `[boot] extraction outbound dispatcher ready -> ${config.signalDefaultRecipient}` (sanitize this log line for the operator surface too -- though it's an internal log, keep house style).
  </behavior>
  <action>
    1. Edit `src/agents/alerter/src/index.js`. Replace the stub `outboundDispatcher` from Plan 05 Task 2 with the real one. Pull `createOutboundDispatcher` from `./extraction/outbound`. The previewBuilder reference: `const previewBuilder = require('./extraction/preview-builder')`.
    2. Verify config.signalDefaultRecipient exists (it should -- it's the existing alerter env). If the key name is different in current config.js, use that name (e.g. `config.signalRecipient` or similar -- grep config.js to confirm).
    3. Run `cd src/agents/alerter && npm test` -- full suite green.
    4. Smoke: `cd src/agents/alerter && timeout 5 node -e "process.env.ANTHROPIC_API_KEY='sk-t'; process.env.SIGNAL_DEFAULT_RECIPIENT='+1'; require('./src/index')"` succeeds.
    5. Commit: `feat(38-06): wire real outbound dispatcher into pipeline`.
  </action>
  <verify>
    <automated>cd src/agents/alerter && npm test</automated>
  </verify>
  <acceptance_criteria>
    - Full alerter suite green.
    - `cd src/agents/alerter && grep "createOutboundDispatcher" src/index.js` >= 1.
    - `cd src/agents/alerter && grep -c "outboundDispatcher" src/index.js` >= 2 (created once, passed once).
    - No em-dashes introduced.
  </acceptance_criteria>
  <done>End-to-end: capture -> extraction pipeline -> outbound -> Signal. Ready for Plan 07 ship-gate eval.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| alerter -> Signal (outbound to farmer) | Sanitized strings cross; farmer trust boundary. |
| alerter -> Signal (outbound to Don Santiago) | Sanitized strings cross; operator trust boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-06-01 | Tampering | LLM-controlled preview text reaching farmer with em-dash or float-soup | mitigate | sanitizeFarmerText pass applied at dispatch time (defense in depth -- preview-builder also sanitizes). Test asserts em-dash absent. |
| T-38-06-02 | Spoofing | wrong-group leak (sending DM-meant content to a group thread) | mitigate | reply_target_kind comes from signal_capture (Phase 37 D-14); test covers both DM and group paths; no fallback default that could mis-route. |
| T-38-06-03 | Information disclosure | leaking sender E.164 to the wrong farmer | mitigate | needs-review ping goes to operatorRecipient ONLY (env-pinned); ask-back goes to the originating sender via captureRow data; no cross-routing path exists. |
| T-38-06-04 | DoS | Signal API outage blocks pipeline | mitigate | signalClient.send rejection caught; dispatch returns {ok:false}; pipeline state already persisted (draft row stays in AWAITING_FARMER); next farmer message re-runs the loop. |
</threat_model>

<verification>
- `cd src/agents/alerter && npm test -- test/extraction/outbound.test.js` green.
- `cd src/agents/alerter && grep -E "—" src/extraction/outbound.js src/index.js` no matches.
- Full alerter suite green.
- Smoke boot succeeds.
</verification>

<success_criteria>
- outbound.js + 10+ tests landed.
- index.js wires real dispatcher.
- DM vs group routing verified; sanitization verified; needs-review ping addresses Don Santiago.
</success_criteria>

<output>
After completion, create `.planning/phases/38-extraction-pipeline/38-06-SUMMARY.md`.
</output>
