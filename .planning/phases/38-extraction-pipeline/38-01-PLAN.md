---
phase: 38-extraction-pipeline
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - src/agents/alerter/package.json
  - src/agents/alerter/src/extraction/schemas/seeding.js
  - src/agents/alerter/src/extraction/schemas/activity.js
  - src/agents/alerter/src/extraction/schemas/input.js
  - src/agents/alerter/src/extraction/schemas/observation.js
  - src/agents/alerter/src/extraction/schemas/harvest.js
  - src/agents/alerter/src/extraction/schemas/index.js
  - src/agents/alerter/test/extraction/schemas.test.js
autonomous: true
requirements: [EXT-01, EXT-02, EXT-05]
must_haves:
  truths:
    - "Each B7 log type (seeding, activity, input, observation, harvest) has a Zod schema that round-trips to a JSON Schema accepted as Anthropic tool input_schema"
    - "block_name field rejects strings not matching ^[0-9]{6}_[A-Z]{3}_[0-9]+$ (B5)"
    - "Discriminated-union Draft schema is exhaustive over the 5 log types"
    - "harvest log accepts a multi-parent source-block reference list (C4)"
  artifacts:
    - path: "src/agents/alerter/src/extraction/schemas/index.js"
      provides: "Discriminated-union Draft schema + Anthropic input_schema export"
    - path: "src/agents/alerter/src/extraction/schemas/seeding.js"
      provides: "Zod SeedingLog schema with B5 block_name regex"
    - path: "src/agents/alerter/src/extraction/schemas/harvest.js"
      provides: "Zod HarvestLog schema with multi-parent ref list"
    - path: "src/agents/alerter/test/extraction/schemas.test.js"
      provides: "Round-trip + B5 + exhaustiveness + harvest-lineage tests"
  key_links:
    - from: "schemas/index.js"
      to: "zod-to-json-schema"
      via: "discriminated union -> JSON Schema export"
      pattern: "zodToJsonSchema\\("
---

<objective>
Land the Zod schema foundation for Phase 38. Every downstream task validates LLM tool-use output against these schemas, and Plan 03's extractor builds Anthropic `input_schema` from `schemas/index.js`. This plan is Wave 0 -- no dependencies, no LLM calls, pure types + unit tests.

Purpose: A schema-conformant extraction pipeline starts with the schema. Get this right and Plans 02-07 have a single source of truth for what a valid draft looks like.

Output: 5 per-log-type schema files + a barrel exporting the discriminated union and an Anthropic-ready JSON Schema, gated by an exhaustive unit-test suite.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-extraction-pipeline/38-CONTEXT.md
@.planning/phases/38-extraction-pipeline/38-RESEARCH.md
@.planning/phases/38-extraction-pipeline/38-PATTERNS.md

@src/agents/alerter/package.json
@src/agents/alerter/src/config.js

<interfaces>
<!-- The locked schema field maps live in 38-RESEARCH.md §8 (required-field map per log type) -->
<!-- and 38-CONTEXT.md §canonical_refs -> farmos repo `2026-05-09-fungi-schema-strawman.md`. -->
<!-- Per-log-type required fields (RESEARCH.md §8): -->
<!--   seeding:     {species, block_name, qty, event_timestamp} -->
<!--   activity:    {name in [sterilize|sterilize_failed|water|relocate|cold_shock|archive_spent|contam], asset_ref, event_timestamp} -->
<!--   input:       {recipe_lot, asset_ref, event_timestamp} -->
<!--   observation: {asset_ref, state | notes, event_timestamp} -->
<!--   harvest:     {harvest_batch_id, source_block_refs[] (>=1), qty_g, event_timestamp} -->
<!-- B5 block_name regex (CONTEXT D-04 + RESEARCH §1.3): ^[0-9]{6}_[A-Z]{3}_[0-9]+$ -->
<!-- Per-field confidence map shape: z.record(z.string(), z.number().min(0).max(1)) -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Zod deps + scaffold schema directory</name>
  <read_first>
    - src/agents/alerter/package.json (current dependencies block, scripts block)
    - .planning/phases/38-extraction-pipeline/38-PATTERNS.md (§MOD package.json -- exact version pins)
    - .planning/phases/38-extraction-pipeline/38-RESEARCH.md (§"Pattern 1" for tool-use + Zod usage rationale)
  </read_first>
  <behavior>
    - Test 1: `require('zod')` succeeds from the alerter package
    - Test 2: `require('zod-to-json-schema')` succeeds and exports `zodToJsonSchema`
    - Test 3: The directory `src/agents/alerter/src/extraction/schemas/` exists and is committed (even if empty placeholder files are added in this task)
  </behavior>
  <action>
    1. Edit `src/agents/alerter/package.json`. Add to `dependencies`, alphabetically positioned, with these exact pins:
       - `"zod": "^3.23.0"`
       - `"zod-to-json-schema": "^3.23.0"`
       Do NOT add `eval:extraction` script yet -- that lands in Plan 07.
    2. Run `cd src/agents/alerter && npm install` to update `package-lock.json`. Commit lockfile alongside `package.json`.
    3. Create directory `src/agents/alerter/src/extraction/schemas/` (mkdir -p). Add an empty `index.js` placeholder with just `'use strict';` -- Tasks 2 and 3 fill it in.

    Per memory `feedback_no_em_dashes_in_artifacts.md`, do not use em-dashes in any string literals you author. Tests assert this in Plan 04, but begin the discipline now.
  </action>
  <verify>
    <automated>cd src/agents/alerter && node -e "require('zod'); require('zod-to-json-schema'); console.log('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `cd src/agents/alerter && node -e "require('zod'); require('zod-to-json-schema'); console.log('ok')"` prints `ok` and exits 0.
    - `src/agents/alerter/package.json` shows zod ^3.23.0 and zod-to-json-schema ^3.23.0 in `dependencies` (verify with `cat src/agents/alerter/package.json | grep -E 'zod'`).
    - `ls src/agents/alerter/src/extraction/schemas/index.js` succeeds.
    - `cd src/agents/alerter && npm test` still passes (no regressions in existing alerter tests).
  </acceptance_criteria>
  <done>Zod toolchain installed; schema directory committed and importable.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Author per-log-type Zod schemas + tests (RED then GREEN)</name>
  <read_first>
    - .planning/phases/38-extraction-pipeline/38-RESEARCH.md (§8 required-field map per log type; §1.3 B5 regex)
    - .planning/phases/38-extraction-pipeline/38-CONTEXT.md (§decisions D-03 per-field confidence; §canonical_refs for fungi schema strawman)
    - .planning/phases/38-extraction-pipeline/38-PATTERNS.md (§schemas/index.js + per-type files -- Zod skeleton)
    - src/agents/alerter/test/llm-client.test.js (jest style, silentLogger pattern -- mirror for schemas.test.js)
    - /mnt/slime-kingdom/shared/farmos/.planning/notes/2026-05-09-fungi-schema-strawman.md (authoritative for activity.name enum + observation states + harvest qty unit) -- if file not accessible, fall back to per-CONTEXT D-04 lists already captured in this PLAN.
  </read_first>
  <behavior>
    - schemas/seeding.js: parses `{type:'seeding', species, block_name:'260511_SHI_4', qty:10, event_timestamp:'2026-05-11T12:00:00Z', confidence:{species:0.9, ...}}` -> ok; rejects `block_name:'260511_SHI'` (missing seq); rejects `qty:-1`; rejects unknown extra top-level fields via `.strict()`.
    - schemas/activity.js: accepts `name` in the locked enum {sterilize, sterilize_failed, water, relocate, cold_shock, archive_spent, contam}; rejects others.
    - schemas/input.js: requires `recipe_lot` + `asset_ref` + `event_timestamp`.
    - schemas/observation.js: requires `asset_ref` + `event_timestamp` + at least one of `state` or `notes`.
    - schemas/harvest.js: requires `harvest_batch_id`, `source_block_refs` as a string array with `.min(1)`, `qty_g` positive number, `event_timestamp`. Test `source_block_refs:['260511_SHI_3','260511_SHI_4','260511_SHI_5']` parses to a 3-element array (multi-parent C4).
    - All five schemas include `confidence: z.record(z.string(), z.number().min(0).max(1))` for per-field LLM self-rated confidence (D-03).
    - All five use `.strict()` so off-schema fields fail safeParse -- this is the EXT-01 anchor.
  </behavior>
  <action>
    1. Create `src/agents/alerter/test/extraction/schemas.test.js` FIRST (RED). Cover:
       - `(seeding) accepts valid` -- ok=true case
       - `(seeding) B5 block_name regex rejects '260511_SHI'` -- ok=false
       - `(seeding) B5 block_name regex accepts '260511_SHI_4'`
       - `(seeding) negative qty rejected`
       - `(seeding) .strict() rejects unknown top-level field`
       - `(activity) name enum exhaustive` -- table-driven loop over all 7 valid names, plus a rejection case for `'frobnicate'`
       - `(input) missing recipe_lot rejected`
       - `(observation) requires state OR notes -- pure asset_ref rejected`
       - `(harvest) multi-parent source_block_refs parses 3 refs intact`
       - `(harvest) empty source_block_refs rejected by .min(1)`
       - `(per-field confidence) record(string, number 0..1) rejects 1.5`
       Use jest's `silentLogger` pattern from `test/llm-client.test.js`. No DB, no Anthropic SDK.
    2. Run `cd src/agents/alerter && npm test -- test/extraction/schemas.test.js` -- expect RED (all tests fail because schema files do not yet exist). Commit RED: `test(38-01): add failing zod schema tests for B7 log types`.
    3. Create the five per-type schema files: `src/agents/alerter/src/extraction/schemas/{seeding,activity,input,observation,harvest}.js`. Each begins with `'use strict'; const { z } = require('zod');` and exports `{ <Name>Log }` (capitalized, e.g. `SeedingLog`, `HarvestLog`).
    4. Each schema uses `z.object({...}).strict()`. Use `z.literal('<type>')` on the `type` field for the discriminated union in Task 3. Use `.regex(/^[0-9]{6}_[A-Z]{3}_[0-9]+$/, 'B5 block_name')` on `block_name` in seeding. Use `z.string().datetime()` on `event_timestamp`. Use `z.number().int().positive()` on `qty`.
    5. For activity, declare the name enum exactly as: `z.enum(['sterilize','sterilize_failed','water','relocate','cold_shock','archive_spent','contam'])`.
    6. Use `z.object({...}).strict()` on every schema (EXT-01 -- no off-schema fields).
    7. Run tests again -- expect GREEN. Commit GREEN: `feat(38-01): implement zod schemas for B7 log types`.

    Per memory `feedback_no_em_dashes_in_artifacts.md`: no em-dashes in any error message strings inside schemas (e.g. don't pass `'must be in form X -- Y'` to `.regex()`; use `'B5 block_name'` or prose).
  </action>
  <verify>
    <automated>cd src/agents/alerter && npm test -- test/extraction/schemas.test.js</automated>
  </verify>
  <acceptance_criteria>
    - All schema tests green: `cd src/agents/alerter && npm test -- test/extraction/schemas.test.js` exits 0 with the expected test count from `<behavior>` (>=11 tests).
    - `cd src/agents/alerter && grep -n "z.enum" src/extraction/schemas/activity.js` shows all 7 activity name values.
    - `cd src/agents/alerter && grep -c ".strict()" src/extraction/schemas/*.js` is at least 5 (one per file).
    - `cd src/agents/alerter && grep -E "—" src/extraction/schemas/*.js` returns no matches (no em-dashes).
    - Two commits land (RED test commit, then GREEN implementation commit).
  </acceptance_criteria>
  <done>Five per-log-type Zod schemas authored with `.strict()` envelopes; B5 regex enforced on seeding; multi-parent harvest list verified; all >=11 unit tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Discriminated-union Draft schema + Anthropic input_schema export</name>
  <read_first>
    - src/agents/alerter/src/extraction/schemas/seeding.js (and the other 4 from Task 2)
    - .planning/phases/38-extraction-pipeline/38-RESEARCH.md (§"Pattern 1: Anthropic Tool-Use Forced Call with Zod-emitted Schema" -- input_schema construction)
    - .planning/phases/38-extraction-pipeline/38-PATTERNS.md (§schemas/index.js -- discriminated union + zod-to-json-schema usage)
    - node_modules/zod-to-json-schema/README.md (if available; otherwise the npm description -- API is `zodToJsonSchema(schema, name?)`)
  </read_first>
  <behavior>
    - `index.js` exports a `Draft` z.discriminatedUnion('type', [SeedingLog, ActivityLog, InputLog, ObservationLog, HarvestLog]).
    - `index.js` exports `DRAFT_JSON_SCHEMA = zodToJsonSchema(Draft, 'Draft')` -- this is the value passed as `tools[0].input_schema` to Anthropic.
    - `index.js` exports `LOG_TYPES = Object.freeze(['seeding','activity','input','observation','harvest'])` for callers needing an iterable list.
    - Test: `Draft.safeParse({type:'seeding', ...valid fields})` -> ok=true.
    - Test: `Draft.safeParse({type:'frobnicate', ...})` -> ok=false (discriminator rejects unknown type).
    - Test: exhaustiveness -- `LOG_TYPES.length === 5` AND every name in `LOG_TYPES` matches a Draft option.
    - Test: `DRAFT_JSON_SCHEMA` is a plain object with a `oneOf` or `anyOf` (discriminated-union output from zod-to-json-schema) and contains every log type's required fields. Assert top-level shape only (don't deep-equal the entire JSON Schema -- the library version may shift detail).
    - Test: round-trip -- `DRAFT_JSON_SCHEMA` JSON.stringify + JSON.parse yields equal object (no functions, no Symbols leaked).
  </behavior>
  <action>
    1. Extend `src/agents/alerter/test/extraction/schemas.test.js` with a new `describe('index.js -- Draft union')` block covering the 5 behavior bullets above. Run -- expect RED on the index.js tests only.
    2. Write `src/agents/alerter/src/extraction/schemas/index.js`:
       ```js
       'use strict';
       const { z } = require('zod');
       const { zodToJsonSchema } = require('zod-to-json-schema');
       const { SeedingLog } = require('./seeding');
       const { ActivityLog } = require('./activity');
       const { InputLog } = require('./input');
       const { ObservationLog } = require('./observation');
       const { HarvestLog } = require('./harvest');

       const Draft = z.discriminatedUnion('type', [SeedingLog, ActivityLog, InputLog, ObservationLog, HarvestLog]);
       const DRAFT_JSON_SCHEMA = zodToJsonSchema(Draft, 'Draft');
       const LOG_TYPES = Object.freeze(['seeding','activity','input','observation','harvest']);

       module.exports = { Draft, DRAFT_JSON_SCHEMA, LOG_TYPES,
         SeedingLog, ActivityLog, InputLog, ObservationLog, HarvestLog };
       ```
    3. Run tests -- expect GREEN. If `zod-to-json-schema` output uses `definitions`/`$ref` (it usually does when a `name` arg is supplied), adjust the test's shape assertion to look up the named def rather than the root. Document the exact shape in a `// EXT-01 anchor:` comment above `DRAFT_JSON_SCHEMA`.
    4. Commit GREEN: `feat(38-01): discriminated-union Draft schema + Anthropic input_schema export`.
  </action>
  <verify>
    <automated>cd src/agents/alerter && npm test -- test/extraction/schemas.test.js</automated>
  </verify>
  <acceptance_criteria>
    - All schema + index tests green together.
    - `cd src/agents/alerter && node -e "const s = require('./src/extraction/schemas'); console.log(JSON.stringify(s.DRAFT_JSON_SCHEMA).slice(0,80))"` prints a JSON-Schema-shaped string (contains `definitions` or `$schema` or `oneOf`/`anyOf`).
    - `cd src/agents/alerter && node -e "const s = require('./src/extraction/schemas'); console.log(s.LOG_TYPES.length === 5)"` prints `true`.
    - `cd src/agents/alerter && npm test` (full suite) still green -- no regression.
  </acceptance_criteria>
  <done>Draft discriminated union + Anthropic-ready `DRAFT_JSON_SCHEMA` + frozen `LOG_TYPES` exported and unit-tested. Plan 03 can `require('./schemas')` and pass `DRAFT_JSON_SCHEMA` straight to Anthropic.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM output -> alerter process | Untrusted JSON from Anthropic crosses into our memory; Zod is the gate. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-01-01 | Tampering | schemas/*.js | mitigate | `.strict()` on every schema rejects off-schema fields injected via prompt or model drift. |
| T-38-01-02 | Tampering | schemas/seeding.js block_name | mitigate | B5 regex `^[0-9]{6}_[A-Z]{3}_[0-9]+$` blocks malformed asset references that could confuse downstream FarmOS write code (Phase 40). |
| T-38-01-03 | Information disclosure | schemas/index.js | accept | JSON Schema export contains only field names + types, no secrets; suitable for shipping in Anthropic API request bodies. |
</threat_model>

<verification>
- All schemas have `.strict()` (grep verification).
- B5 regex test cases cover both accept and reject.
- Discriminated union rejects unknown `type` discriminator.
- `DRAFT_JSON_SCHEMA` is serializable JSON.
- Full alerter suite still green.
</verification>

<success_criteria>
- 5 per-log-type Zod schemas + 1 union/JSON-Schema barrel committed.
- >=11 unit tests passing.
- No em-dashes in any new file.
- `package.json` + `package-lock.json` updated with zod + zod-to-json-schema.
</success_criteria>

<output>
After completion, create `.planning/phases/38-extraction-pipeline/38-01-SUMMARY.md` per the template.
</output>
