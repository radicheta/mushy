---
phase: 38-extraction-pipeline
plan: 05
type: execute
wave: 2
depends_on: [01, 02, 03, 04]
files_modified:
  - src/agents/alerter/src/capture.js
  - src/agents/alerter/src/extraction/index.js
  - src/agents/alerter/src/extraction/pipeline.js
  - src/agents/alerter/src/index.js
  - src/agents/alerter/test/extraction/integration.test.js
autonomous: true
requirements: [EXT-03, EXT-04]
must_haves:
  truths:
    - "After capture.js inserts a signal_capture row with farmos_person != '(unassigned)', it enqueues an extraction job (fire-and-forget, .catch logger.warn -- never awaited)"
    - "The extraction job: (1) looks up in-flight draft for sender via extraction-db, (2) calls extractor.extract with the new capture(s) + in-flight draft, (3) applies forceStartNewIfIdle, (4) persists draft via insertDraft or update, (5) runs state-machine.transition, (6) dispatches side_effects (send_ask_back queued for Plan 06)"
    - "Replay-safe id: rerunning the extraction job on the same capture set yields the same signal_draft id (D-02a -- computeDraftId is deterministic; ON CONFLICT DO NOTHING tolerated)"
    - "Continuity decision honored: LLM says 'append' -> source_capture_ids array extended; 'replace' -> draft_json replaced; 'start_new' -> previous in-flight closed as EXPIRED, new draft inserted"
    - "Capture pipeline's 30s budget is not blocked by extraction (fire-and-forget seam)"
  artifacts:
    - path: "src/agents/alerter/src/extraction/pipeline.js"
      provides: "Async worker: enqueue(captureCtx) orchestrates extraction-db + extractor + state-machine"
      exports: ["createExtractionPipeline"]
    - path: "src/agents/alerter/src/extraction/index.js"
      provides: "Barrel: re-exports createExtractionPipeline and the per-module submodules"
    - path: "src/agents/alerter/src/capture.js"
      provides: "Step 3 enqueues extraction job after farmos_person resolution"
  key_links:
    - from: "src/agents/alerter/src/capture.js"
      to: "extractionPipeline.enqueue(captureCtx)"
      via: "fire-and-forget .catch(logger.warn) at end of step 3"
      pattern: "extractionPipeline\\.enqueue.*\\.catch"
    - from: "pipeline.js"
      to: "extractor.extract + extraction-db CRUD + state-machine.transition"
      via: "sequential await chain inside try/catch"
      pattern: "extractor\\.extract"
---

<objective>
Wire Plans 02 + 03 + 04 together into a single async pipeline, and hook the capture path so that every new farmer message (with a resolved farmos_person) enqueues an extraction job. Per PATTERNS.md advisory: hook `capture.js`, NOT `receive-loop.js`, because capture.js already has `farmosPerson` resolved + the signal_capture row inserted.

Purpose: The control-plane modules from Plans 02-04 are inert without a controller. This plan is the controller. It composes them, persists state, and queues outbound effects.

Output: A `createExtractionPipeline` factory + capture.js hook + index.js wiring + one integration test that exercises the full pipeline against a stub pool + mocked Anthropic SDK.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/38-extraction-pipeline/38-CONTEXT.md
@.planning/phases/38-extraction-pipeline/38-PATTERNS.md
@.planning/phases/38-extraction-pipeline/38-RESEARCH.md
@.planning/phases/38-extraction-pipeline/38-01-PLAN.md
@.planning/phases/38-extraction-pipeline/38-02-PLAN.md
@.planning/phases/38-extraction-pipeline/38-03-PLAN.md
@.planning/phases/38-extraction-pipeline/38-04-PLAN.md

@src/agents/alerter/src/capture.js
@src/agents/alerter/src/receive-loop.js
@src/agents/alerter/src/index.js

<interfaces>
<!-- pipeline.enqueue(captureCtx) signature -- accept everything capture.js already has on hand: -->
<!--   { -->
<!--     captureId, -->
<!--     sender,             // E.164 -->
<!--     farmosPerson,       // resolved slug -->
<!--     text,               // farmer text -->
<!--     transcripts,        // [] of strings -->
<!--     attachmentPaths,    // [] of absolute filesystem paths -->
<!--     replyTargetKind,    // 'dm' | 'group' -->
<!--     groupId,            // null or signal group internal_id -->
<!--     capturedAtMs, -->
<!--   } -->

<!-- pipeline orchestration sequence (PATTERNS §state-machine.js "transitions reference" mirrors capture.js): -->
<!--   1. const inFlight = await extractionDb.getInFlightForSender(pool, sender) -->
<!--   2. const forceStartNew = stateMachine.forceStartNewIfIdle(inFlight, nowMs, config.draftIdleGapMin) -->
<!--   3. const captures = [{captureId, text, transcripts, attachmentPaths}] -->
<!--   4. const draftId = computeDraftId([...inFlight?.source_capture_ids || [], captureId].sort()) -->
<!--      (NB: when forceStartNew, inFlight ignored; draftId = computeDraftId([captureId])) -->
<!--   5. const result = await extractor.extract({captures, inFlightDraft: forceStartNew ? null : inFlight}) -->
<!--   6. apply continuity_decision: -->
<!--        - 'append'  -> source_capture_ids = [...inFlight.source_capture_ids, captureId]; update draft -->
<!--        - 'replace' -> source_capture_ids stays, draft_json replaced; update draft -->
<!--        - 'start_new' (or forceStartNew) -> mark inFlight EXPIRED, insert new -->
<!--   7. const transition = stateMachine.transition(prevState, {type:'extraction_result', draft:result.draft, perFieldConfidence:result.per_field_confidence, now_ms}) -->
<!--   8. updateDraftStatus(pool, draftId, transition.nextStatus, {...extras incl preview text from preview-builder if AWAITING_FARMER}) -->
<!--   9. For each side_effect in transition.side_effects: -->
<!--      - 'send_ask_back': enqueue an outbound-reply job (Plan 06's outbound module; in this plan it's a stub function that logs the intent) -->
<!--      - 'send_needs_review_ping': stub log -->
<!--      - 'mark_expired' / 'noop': nothing -->
<!--      - 'handoff_to_phase_39': nothing -- Plan 39 polls for ready_for_confirm rows -->

<!-- Plan 06 replaces the stubs with real signalClient.send calls. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: pipeline.js + extraction/index.js barrel (RED then GREEN)</name>
  <read_first>
    - src/agents/alerter/src/capture.js (full -- step 1-7 ordering)
    - src/agents/alerter/src/llm-client.js (factory + never-throw pattern)
    - .planning/phases/38-extraction-pipeline/38-PATTERNS.md (§extraction/index.js barrel + §MOD src/index.js -- createExtractionPipeline shape)
    - All four prior plans' must_haves + interfaces blocks.
  </read_first>
  <behavior>
    createExtractionPipeline({pool, extractor, extractionDb, stateMachine, previewBuilder, config, logger, clock, outboundDispatcher}):
    - Returns {enqueue}.
    - enqueue(captureCtx): async. Performs the full orchestration sequence above. Never throws -- top-level try/catch logs `[extraction] error: ${e.message}` and returns {ok:false, reason}.
    - clock injection: `clock.now() -> ms` so tests can pin time (mirrors snooze.js + capture.js test style).
    - outboundDispatcher: an injected handle with method `.dispatch(sideEffect, draftRow)`. In Plan 05 this is a stub that logs `[extraction] would dispatch: ${sideEffect}`. Plan 06 replaces with real signal-client sends.
    - All side-effects from state-machine.transition are dispatched in order.
    - For AWAITING_FARMER transitions, preview-builder.buildPreview is called and persisted in farmer_facing_preview before dispatch.
  </behavior>
  <action>
    1. Write `test/extraction/integration.test.js` RED. Use:
       - `jest.mock('@anthropic-ai/sdk', ...)` for extractor's SDK
       - A stub pool: `{ query: jest.fn() }` returning canned rows for getInFlightForSender and rowCounts for inserts/updates
       - A stub clock: `{ now: () => Date.parse('2026-05-12T12:00:00Z') }`
       - A stub outboundDispatcher: `{ dispatch: jest.fn() }`
       Test cases:
       - `(R1) new sender, no in-flight: extraction returns ok=true + askBack=true -> insertDraft called with status=pending, then updateDraftStatus to AWAITING_FARMER, dispatch('send_ask_back', row)`
       - `(R2) new sender, no in-flight: extraction returns askBack=false -> updateDraftStatus to READY_CONFIRM, dispatch('handoff_to_phase_39')`
       - `(R3) in-flight exists, idle 35min: forceStartNewIfIdle returns 'start_new' -> existing draft expired, new draft inserted with start_new continuity`
       - `(R4) in-flight exists, 5min ago, LLM continuity_decision='append' -> existing draft updated with extended source_capture_ids`
       - `(R5) extractor returns ok:false -> pipeline returns {ok:false}, no draft persisted with bogus status, logged at warn`
       - `(R6) 3rd ask-back turn cap -> updateDraftStatus to NEEDS_REVIEW, dispatch('send_needs_review_ping')`
       - `(R7) pipeline.enqueue NEVER throws even when pool.query throws` (verify try { await enqueue() } catch never reached)
       - `(R8) preview text written to farmer_facing_preview before dispatch fires` (assert update-call ordering via mock.calls)
       Run -- RED. Commit RED: `test(38-05): extraction pipeline orchestration`.
    2. Implement `src/extraction/pipeline.js` and `src/extraction/index.js` (barrel that re-exports createExtractionPipeline + per-module exports for convenient testing).
    3. Run tests -- GREEN. Commit: `feat(38-05): extraction pipeline orchestrator`.

    No em-dashes in any log message or string literal.
  </action>
  <verify>
    <automated>cd src/agents/alerter && npm test -- test/extraction/integration.test.js</automated>
  </verify>
  <acceptance_criteria>
    - >=8 integration tests green.
    - `cd src/agents/alerter && grep -E "—" src/extraction/pipeline.js src/extraction/index.js` no matches.
    - `cd src/agents/alerter && grep -c "try" src/extraction/pipeline.js` >= 1 (top-level try/catch).
    - `cd src/agents/alerter && grep "ok: false" src/extraction/pipeline.js` >= 1 (never-throw envelope).
    - Full `npm test` still green.
  </acceptance_criteria>
  <done>pipeline.js + barrel landed. capture.js hook in Task 2 will trigger it.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: capture.js hook + src/index.js wire-up</name>
  <read_first>
    - src/agents/alerter/src/capture.js (full -- end of step 3 where farmosPerson is resolved, before LLM compose, lines 89-184)
    - src/agents/alerter/src/index.js (current createCapturePipeline construction; PATTERNS §MOD src/index.js)
    - .planning/phases/38-extraction-pipeline/38-PATTERNS.md (§MOD src/capture.js + §MOD src/receive-loop.js -- "Insertion strategy (RECOMMENDED -- async chain inside capture pipeline, not receive-loop)")
  </read_first>
  <behavior>
    capture.js change:
    - At the end of step 3 (after the INSERT signal_capture and farmos_person resolution), call `extractionPipeline.enqueue({...captureCtx}).catch((e) => logger.warn('[capture] extraction enqueue failed: ' + e.message))`.
    - Gate by `if (extractionPipeline && farmosPerson && farmosPerson !== '(unassigned)')` -- never enqueue for unknown senders (matches CONTEXT D-02's "known farmer" precondition).
    - NEVER await -- fire-and-forget.
    - Pass extractionPipeline in as a constructor arg to createCapturePipeline.

    index.js change:
    - After `await extractionDb.initDb(pool)` (Plan 02 Task 2), construct `const extractionPipeline = createExtractionPipeline({pool, extractor, extractionDb, stateMachine, previewBuilder, config, logger, clock, outboundDispatcher})`.
    - `outboundDispatcher` is a stub in Plan 05: `{ dispatch: (effect, row) => logger.info('[extraction] dispatch stub: ' + effect) }`. Plan 06 replaces with the real outbound module.
    - Pass `extractionPipeline` into createCapturePipeline.
    - Wire `extractor` via existing llmClient OR by creating a sibling `createExtractor` from `./extraction/extractor`. Recommended: separate extractor instance reusing ANTHROPIC_API_KEY env -- keeps llm-client.js (the alerter-reply path) decoupled from extractor.js (the extraction path).

    Tests:
    - No new test file. Manual smoke: `cd src/agents/alerter && node -e "process.env.ANTHROPIC_API_KEY='sk-t'; process.env.SIGNAL_DEFAULT_RECIPIENT='+1'; process.env.TIMESCALE_HOST='nonexistent.local'; require('./src/index')"` -- expect logger output complaining about DB connection failing but no thrown error from module resolution / pipeline construction. The existing alerter has no index integration test; this is consistent.
  </behavior>
  <action>
    1. Edit `src/agents/alerter/src/capture.js`. Find the step-3 boundary (after signal_capture INSERT and farmos_person resolution). Add:
       ```js
       if (extractionPipeline && farmosPerson && farmosPerson !== '(unassigned)') {
         extractionPipeline.enqueue({
           captureId: id,
           sender: source,
           farmosPerson,
           text: text || null,
           transcripts: transcripts || [],
           attachmentPaths: attachmentPaths || [],
           replyTargetKind: replyTargetKind || 'dm',
           groupId: groupId || null,
           capturedAtMs: capturedAt,
         }).catch((e) => logger.warn('[capture] extraction enqueue failed: ' + e.message));
       }
       ```
       Add `extractionPipeline` to the createCapturePipeline constructor args alongside the existing dependencies.
    2. Edit `src/agents/alerter/src/index.js`. After the extractionDb.initDb wire-up (Plan 02 Task 2), construct extractor + extractionPipeline as described above. Thread extractionPipeline into createCapturePipeline call.
    3. Run `cd src/agents/alerter && npm test` -- ensure capture tests still pass. If capture.test.js exists and breaks because constructor signature changed, update the test fixtures to inject a stub `extractionPipeline = { enqueue: jest.fn().mockResolvedValue({ok:true}) }`. Commit: `feat(38-05): wire extraction pipeline into capture path`.

    No em-dashes.
  </action>
  <verify>
    <automated>cd src/agents/alerter && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `cd src/agents/alerter && npm test` exits 0 (full suite green).
    - `cd src/agents/alerter && grep "extractionPipeline.enqueue" src/capture.js` >= 1.
    - `cd src/agents/alerter && grep "extractionPipeline.enqueue" src/capture.js | grep ".catch"` succeeds (fire-and-forget pattern).
    - `cd src/agents/alerter && grep "createExtractionPipeline" src/index.js` >= 1.
    - Smoke boot: `cd src/agents/alerter && timeout 5 node -e "process.env.ANTHROPIC_API_KEY='sk-t'; process.env.SIGNAL_DEFAULT_RECIPIENT='+1'; require('./src/index')"` does not exit non-zero on module load.
    - No em-dashes in any file touched.
  </acceptance_criteria>
  <done>capture.js fires extraction jobs in fire-and-forget mode for known farmers. index.js boot constructs the full pipeline. Plan 06 replaces the outbound stub with real Signal sends.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| capture.js -> extraction pipeline | Trust boundary internal to alerter; sender E.164 already validated by Phase 37 routing. |
| pipeline -> Anthropic / Postgres | Both already threat-modeled in Plans 02 + 03; this plan only composes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-05-01 | DoS | extraction blocking capture path | mitigate | Fire-and-forget enqueue; receive-loop's 30s budget is never blocked. Test R7 verifies pipeline.enqueue cannot throw upward. |
| T-38-05-02 | Tampering | unknown sender ('(unassigned)') triggering extraction | mitigate | Gate `if (farmosPerson && farmosPerson !== '(unassigned)')` -- unknown senders never enter the pipeline. |
| T-38-05-03 | Tampering | replay attack same capture set re-extracts and double-writes | mitigate | computeDraftId(D-02a) is deterministic; partial unique index (D-02c) blocks duplicate in-flight; ON CONFLICT semantics on insertDraft return in_flight_conflict cleanly. |
| T-38-05-04 | Information disclosure | dispatch stub logs side-effect name | accept | Side-effect names are non-sensitive ('send_ask_back', etc.); Plan 06 will log dispatch results with structured fields. |
</threat_model>

<verification>
- `cd src/agents/alerter && npm test` green.
- Integration test exercises forceStartNewIfIdle path, continuity append path, 3-turn cap path.
- capture.js enqueue is fire-and-forget (grep confirms `.catch`).
- No em-dashes in any modified file.
</verification>

<success_criteria>
- pipeline.js + extraction/index.js barrel + 8+ integration tests green.
- capture.js step 3 enqueues extraction jobs.
- index.js boots the full pipeline.
- Outbound effects routed through injected dispatcher (replaced in Plan 06).
</success_criteria>

<output>
After completion, create `.planning/phases/38-extraction-pipeline/38-05-SUMMARY.md`.
</output>
