---
phase: 38-extraction-pipeline
plan: 08
type: execute
wave: 3
depends_on: [07]
status: superseded
superseded_by: 09
superseded_reason: "Plan 09 (2026-05-12 remediation) folds prod-corpus eval into the ship-gate denominator rather than running it as a separate advisory pass. Plan 08's scope is fully absorbed."
files_modified:
  - .planning/phases/38-extraction-pipeline/38-PRODLOG-REPORT.md
autonomous: false
requirements: [EXT-01, EXT-02, EXT-03, EXT-04, EXT-05]
must_haves:
  truths:
    - "Same harness as Plan 07 runs against the production-log corpus path supplied by Don Santiago at execution time"
    - "Produces an advisory report (NOT a ship-gate) -- the phase already passed/failed under Plan 07's mushdatadump verdict"
    - "If the production-log path is not yet known/available, this plan is marked deferred rather than blocking phase close"
  artifacts:
    - path: ".planning/phases/38-extraction-pipeline/38-PRODLOG-REPORT.md"
      provides: "Advisory eval report against production logs"
  key_links:
    - from: "npm run eval:extraction"
      to: ".planning/phases/38-extraction-pipeline/38-PRODLOG-REPORT.md"
      via: "EXTRACTION_FIXTURE_DIR override + report-path override"
      pattern: "EXTRACTION_FIXTURE_DIR"
---

<objective>
Run the Plan 07 harness against the production-log corpus once Don Santiago supplies the path. This is ADVISORY only -- a stress test on real, non-curated farmer messages. It does not gate phase close.

Purpose: Memory `project_phase38_production_logs_available` notes a production-log corpus exists; we want to know how the pipeline scores against it before the SHI-on-sawdust pilot (Phase 42).

Output: An advisory report at `.planning/phases/38-extraction-pipeline/38-PRODLOG-REPORT.md`, OR a deferred-item note if the path is not yet supplied.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/38-extraction-pipeline/38-07-PLAN.md

@src/agents/alerter/test/eval/extraction/mushdatadump.test.js
@src/agents/alerter/test/eval/extraction/jest.config.js
@src/agents/alerter/test/eval/extraction/report.js
</context>

<tasks>

<task type="checkpoint:human-action" gate="non-blocking">
  <name>Task 1: Ask Don Santiago for the production-log corpus path</name>
  <what-built>Nothing built yet. Plan 07 produced a passing mushdatadump report; this is the optional production-log smoke.</what-built>
  <how-to-verify>
    Don Santiago, please confirm one of:

    A. **Production-log path is available:** Reply with the absolute path on this box (e.g. `/mnt/slime-kingdom/shared/farmer-logs/` or similar). The executor will proceed to Task 2.

    B. **Production-log path is NOT yet available:** Reply `defer` and the executor will mark this plan deferred, add an entry to STATE.md "Pending Todos" tracking the need, and close Phase 38.

    If path is available, also confirm:
    - Does the corpus include a ground-truth CSV similar to mushdatadump?
    - If no ground-truth: the harness can still score schema-conformance + ask-back-rate + per-field confidence distribution (no exact-match metric), but exact-field-match is N/A. Note this in the advisory.
  </how-to-verify>
  <resume-signal>Type either an absolute path, or `defer`.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Run harness against production logs + write advisory report</name>
  <read_first>
    - .planning/phases/38-extraction-pipeline/38-07-PLAN.md (eval-harness invocation pattern)
    - src/agents/alerter/test/eval/extraction/mushdatadump.test.js (driver)
    - src/agents/alerter/test/eval/extraction/fixtures-loader.js (may need to extend if production corpus has a different layout)
  </read_first>
  <action>
    ONLY EXECUTE IF Task 1 returned an absolute path. If Task 1 returned `defer`, skip to Task 3.

    1. Verify the supplied path exists: `ls <path>` succeeds.
    2. Inspect layout. Two cases:
       - If it matches mushdatadump layout (`images/` + `ground-truth.csv`): run directly with `EXTRACTION_FIXTURE_DIR=<path>`.
       - If it has a different layout: add a minimal layout-detection branch in `fixtures-loader.js` keyed off whether `ground-truth.csv` exists; if not, fall back to a "schema-conformance + ask-back-rate only" mode that scores only those dimensions. Commit the layout extension separately: `feat(38-08): production-log layout in fixtures loader`.
    3. Override the report destination -- two options:
       - Simplest: hardcode a second env var `EVAL_REPORT_PATH` in `report.js` (default points to 38-EVAL-REPORT.md; if set, writes elsewhere). Add this env-var support if it doesn't already exist.
       - Run: `cd src/agents/alerter && EXTRACTION_FIXTURE_DIR=<path> EVAL_REPORT_PATH=$(realpath ../../.planning/phases/38-extraction-pipeline/38-PRODLOG-REPORT.md) npm run eval:extraction`
    4. Eyeball the resulting `.planning/phases/38-extraction-pipeline/38-PRODLOG-REPORT.md`. The verdict line is informational only -- if FAIL, do NOT block phase close. Capture the lowest dimension as a follow-up note.
    5. Commit the report: `docs(38): production-log advisory eval report`.

    No em-dashes; fmtNum on all numbers (the existing report.js handles this).
  </action>
  <verify>
    <automated>test -f .planning/phases/38-extraction-pipeline/38-PRODLOG-REPORT.md && grep -E "^## Verdict:" .planning/phases/38-extraction-pipeline/38-PRODLOG-REPORT.md</automated>
  </verify>
  <acceptance_criteria>
    - Report file exists with a `## Verdict:` line.
    - No em-dashes in report.
    - Don Santiago has reviewed the verdict and either flagged a follow-up (filed as a backlog item or appended to STATE.md Pending Todos) or noted "no action needed".
  </acceptance_criteria>
  <done>Advisory report committed; follow-up (if any) filed in STATE.md.</done>
</task>

<task type="auto">
  <name>Task 3: If deferred -- record deferral in STATE.md</name>
  <read_first>
    - .planning/STATE.md (Pending Todos section)
  </read_first>
  <action>
    ONLY EXECUTE IF Task 1 returned `defer`.

    1. Edit `.planning/STATE.md`. In the `## Pending Todos` section, add a line:
       ```
       - **Phase 38 prodlog advisory smoke:** deferred at phase close <YYYY-MM-DD> -- Don Santiago to surface production-log corpus path; run `cd src/agents/alerter && EXTRACTION_FIXTURE_DIR=<path> EVAL_REPORT_PATH=.../38-PRODLOG-REPORT.md npm run eval:extraction` when available.
       ```
    2. Commit: `docs(38): defer production-log advisory smoke -- pending corpus path`.
  </action>
  <verify>
    <automated>grep "prodlog advisory" .planning/STATE.md</automated>
  </verify>
  <acceptance_criteria>
    - STATE.md has the deferral note.
    - Commit landed.
    - No em-dashes.
  </acceptance_criteria>
  <done>Deferral tracked; phase close not blocked.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Eval harness -> production-log filesystem | Real farmer-message attachments may contain real PII. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-08-01 | Information disclosure | committing the PRODLOG-REPORT containing real farmer text/photos | mitigate | Report writer (existing report.js) only includes aggregate scores -- not raw fixture content. Don Santiago confirms before commit. |
| T-38-08-02 | Information disclosure | sending real farmer images to Anthropic API | accept | The live alerter ALREADY does this for every farmer message under Phase 25; this advisory adds no new exposure beyond rerunning historical traffic. |
</threat_model>

<verification>
- One of: report file committed OR deferral note committed.
- No em-dashes.
- Phase 38 close not blocked.
</verification>

<success_criteria>
- Path supplied + advisory report shipped, OR deferral recorded.
</success_criteria>

<output>
After completion, create `.planning/phases/38-extraction-pipeline/38-08-SUMMARY.md` noting which branch fired (advisory report committed vs deferred).
</output>
