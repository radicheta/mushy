---
phase: 13-farmos-daily-report
plan: 04
type: execute
wave: 2
depends_on: ["13-03"]
files_modified: []
autonomous: false
gap_closure: true
requirements:
  - FMOS-01
  - FMOS-02
  - FMOS-03

must_haves:
  truths:
    - "FC-1 asset in FarmOS shows Lab 1 as location and includes sensor metadata in notes"
    - "Vikki account has 'create log' permission so photo uploads succeed"
    - "After rebuild, daily report observation shows correct humidity (~94%) and attached photo"
  artifacts: []
  key_links: []
---

<objective>
Rebuild the farmos-agent and bridge containers with the code fixes from Plan 03, perform FarmOS admin actions (set FC-1 location, grant upload permission), trigger a corrected report, and verify end-to-end.

Purpose: Plan 03 fixes the code; this plan deploys the fixes and resolves the two FarmOS admin gaps (location/metadata and upload permission) that cannot be automated.

Output: A corrected observation in FarmOS with proper humidity values and attached camera snapshot.
</objective>

<execution_context>
@/home/santi/.claude/get-shit-done/workflows/execute-plan.md
@/home/santi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/13-farmos-daily-report/13-CONTEXT.md
@.planning/phases/13-farmos-daily-report/13-VERIFICATION.md
@.planning/phases/13-farmos-daily-report/13-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rebuild bridge and farmos-agent containers with fixes</name>
  <files></files>
  <read_first>
    /mnt/slime-kingdom/opt/mushy/.planning/phases/13-farmos-daily-report/13-03-SUMMARY.md
  </read_first>
  <action>
    Rebuild and restart both containers that received code fixes in Plan 03:

    ```bash
    cd /mnt/slime-kingdom/opt/mushy
    docker compose up -d --build bridge farmos-agent
    ```

    Wait for both containers to reach running state:
    ```bash
    docker compose ps bridge farmos-agent
    ```

    Verify the farmos-agent reaches active lifecycle state:
    ```bash
    docker compose logs farmos-agent --tail=20
    ```

    Expected: "configured" and "activated" messages in logs. If the agent fails to configure (DB connection, FarmOS auth), the logs will show the error. Do NOT proceed to checkpoint if configuration fails — debug and fix first.

    Then trigger a manual execute_report to produce a corrected observation for yesterday:
    ```bash
    docker compose exec farmos-agent python3 -c "
    from farmos_agent.farmos_agent_node import FarmOSAgent
    import rclpy
    rclpy.init()
    n = FarmOSAgent()
    n.on_configure(None)
    n.execute_report()
    n.destroy_node()
    rclpy.shutdown()
    "
    ```

    Note: If a duplicate observation already exists for yesterday's date, the agent will skip it (D-09 idempotency). In that case, the existing observation has wrong humidity values. The user will need to either:
    - Delete the old observation in FarmOS UI and re-trigger, OR
    - Accept that tomorrow's 06:00 report will be the first correct one

    Log the outcome either way.
  </action>
  <verify>
    <automated>cd /mnt/slime-kingdom/opt/mushy && docker compose ps bridge farmos-agent --format json 2>/dev/null | grep -c running || docker compose ps bridge farmos-agent 2>&1 | grep -c "Up"</automated>
  </verify>
  <done>
    Both bridge and farmos-agent containers rebuilt and running. Manual execute_report triggered (or skipped due to existing duplicate). Logs show successful lifecycle transitions.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: FarmOS admin actions and end-to-end verification</name>
  <files></files>
  <what-built>
    Plan 03 fixed: humidity units (no longer 100x), upload_photo auth (session.post), bridge staleness guard, exact match filter, test files committed. Task 1 above rebuilt containers. Three FarmOS admin actions remain that require human browser access.
  </what-built>
  <action>
    **FarmOS Admin Actions (user performs these in browser):**

    1. **Set FC-1 location (FMOS-01):**
       - Open http://10.68.155.50:8082/asset/28/edit as admin
       - In the Location field, set it to "Lab 1" (asset 26)
       - In the Notes field, add sensor description (e.g., "DHT22 + SCD41 sensors, hardware PWM fan, SSR humidifier")
       - Save

    2. **Grant upload permission (FMOS-02):**
       - Open http://10.68.155.50:8082/admin/people/permissions
       - Find Vikki's role
       - Enable "create log" permission (or the most specific permission that covers creating observation logs with file attachments)
       - Save permissions

    3. **Delete stale observation (if duplicate blocks new report):**
       - If the manual trigger in Task 1 logged "already exists — skipping", open http://10.68.155.50:8082/asset/28 and navigate to Logs
       - Find "FC-1 Daily Report {yesterday's date}" with wrong humidity (9671%)
       - Delete it
       - Then re-trigger: `docker compose exec farmos-agent python3 -c "from farmos_agent.farmos_agent_node import FarmOSAgent; import rclpy; rclpy.init(); n=FarmOSAgent(); n.on_configure(None); n.execute_report(); n.destroy_node(); rclpy.shutdown()"`

    **Verification (after admin actions):**

    4. Open http://10.68.155.50:8082/asset/28 — confirm Location shows "Lab 1", Notes has sensor description
    5. Check the Logs tab — find latest "FC-1 Daily Report" observation, verify humidity ~94% (NOT 9671%), temperature ~17C, CO2 ~476ppm
    6. Verify the observation has a camera snapshot attached (JPEG image visible)
    7. Verify container health: `docker compose ps` shows both bridge and farmos-agent as Up
  </action>
  <verify>
    <automated>cd /mnt/slime-kingdom/opt/mushy && docker compose logs farmos-agent --tail=5 2>&1 | grep -c "observation created\|report complete\|already exists"</automated>
  </verify>
  <done>
    FC-1 has location "Lab 1" and sensor notes. Observation has correct humidity (~94%), attached camera JPEG. Vikki has upload permission. Containers healthy, scheduler active for 06:00 daily run.
  </done>
  <resume-signal>Type "approved" if FC-1 has correct location/metadata, the observation shows correct humidity values with an attached photo, and containers are healthy. Otherwise describe what is still wrong.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| FarmOS admin UI | Human grants permissions through browser |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-13 | Elevation of Privilege | FarmOS Vikki role | mitigate | Grant minimum permission needed ("create log") — not full admin. Vikki should not be able to delete assets or modify farm settings. |
</threat_model>

<verification>
1. FC-1 asset at /asset/28 shows Lab 1 location and notes
2. Daily observation has humidity ~94% (not 9671%)
3. Observation has attached camera JPEG
4. `docker compose ps` shows bridge and farmos-agent running
5. `docker compose logs farmos-agent` shows successful report execution
</verification>

<success_criteria>
- FC-1 has location "Lab 1" and sensor notes in FarmOS (FMOS-01 complete)
- Daily observation has correct humidity values and attached camera snapshot (FMOS-02, FMOS-03 complete)
- Both containers healthy and scheduler active for tomorrow's 06:00 run
</success_criteria>

<output>
After completion, create `.planning/phases/13-farmos-daily-report/13-04-SUMMARY.md`
</output>
