---
phase: 37
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/agents/alerter/test/fixtures/envelopes/group-silent.json
  - src/agents/alerter/test/fixtures/envelopes/group-mention.json
  - src/agents/alerter/test/fixtures/envelopes/group-command.json
  - src/agents/alerter/test/fixtures/envelopes/group-reply-to-bot.json
  - src/agents/alerter/test/fixtures/envelopes/group-mention-and-command.json
  - src/agents/alerter/test/fixtures/envelopes/group-unknown-sender.json
  - .planning/phases/37-multi-farmer-routing/37-SMOKE.md
autonomous: false
requirements: [ROUTE-01, ROUTE-02, ROUTE-03]
tags: [signal, alerter, group, fixtures, smoke]

must_haves:
  truths:
    - "Live signal-cli on current deviceId can SEND to a group via POST /v2/send with recipients=['group.<base64>']"
    - "Live signal-cli RECEIVE envelope from the Mushroom Farm group includes dataMessage.groupInfo.groupId"
    - "Live @mention envelope from group includes dataMessage.mentions[] with mention.number matching bot phone (REST mode, not JSON-RPC — Risk #10/A2)"
    - "Live reply-to-bot envelope includes dataMessage.quote with either .author or .authorNumber set to bot phone (Risk #9/A1)"
    - "Six fixture envelopes exist on-disk and match the live shape (no fabricated fields)"
  artifacts:
    - path: "src/agents/alerter/test/fixtures/envelopes/group-silent.json"
      provides: "Group DM with no triggers (capture-only, reply_target_kind='none')"
      contains: "groupInfo"
    - path: "src/agents/alerter/test/fixtures/envelopes/group-mention.json"
      provides: "Group message @mentioning bot by phone"
      contains: "mentions"
    - path: "src/agents/alerter/test/fixtures/envelopes/group-command.json"
      provides: "Group message starting with command keyword"
      contains: "groupInfo"
    - path: "src/agents/alerter/test/fixtures/envelopes/group-reply-to-bot.json"
      provides: "Group message quoting a bot message"
      contains: "quote"
    - path: "src/agents/alerter/test/fixtures/envelopes/group-mention-and-command.json"
      provides: "Group message with BOTH mention AND command (D-09 dedupe target)"
      contains: "mentions"
    - path: "src/agents/alerter/test/fixtures/envelopes/group-unknown-sender.json"
      provides: "Group message from whitelisted-but-unmapped sender (D-12 sentinel target)"
      contains: "groupInfo"
    - path: ".planning/phases/37-multi-farmer-routing/37-SMOKE.md"
      provides: "Smoke probe outcome: SIGNAL_GROUP_ID value, send-probe HTTP status, captured envelopes, quote.author vs authorNumber observation"
  key_links:
    - from: "operator's signal-cli on fc1 / docker host"
      to: "Mushroom Farm group"
      via: "POST http://signal-cli:8080/v2/send with recipients=['group.<base64>']"
      pattern: "recipients.*group\\."
    - from: "Wave 1 plans (02/03)"
      to: "live envelope shape"
      via: "fixture files extracted from real receives, NOT hand-crafted"
      pattern: "envelope.dataMessage.groupInfo"
---

<objective>
Wave 0 fail-fast: run a live signal-cli smoke probe and capture six real group envelopes
as test fixtures BEFORE any code changes. De-risk three RESEARCH.md assumptions (A1: quote
field name; A2: mentions[] in REST mode; A3: deviceId=1 supports group sends).

Purpose: Three things must be verified live before the rest of the phase commits to its
shape. If A3 fails the phase is blocked; if A1/A2 surface unexpected field names the
trigger-matcher in 37-03 must adapt. Cheaper to discover here than in Wave 3.

Output: Six committed fixture envelopes + a SMOKE.md attesting the live group-send works
on current deviceId and documenting the actual quote/mentions field shapes seen.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/37-multi-farmer-routing/37-CONTEXT.md
@.planning/phases/37-multi-farmer-routing/37-RESEARCH.md
@.planning/phases/37-multi-farmer-routing/37-PATTERNS.md
@src/agents/alerter/test/fixtures/envelopes/text.json
@src/agents/alerter/test/fixtures/envelopes/snooze.json

<interfaces>
<!-- Existing DM fixture shape (extend this with groupInfo for group fixtures) -->
From src/agents/alerter/test/fixtures/envelopes/text.json:
```json
[
  { "envelope": {
      "source": "+59892893012",
      "sourceNumber": "+59892893012",
      "sourceUuid": "11111111-1111-1111-1111-111111111111",
      "timestamp": 1714240000000,
      "dataMessage": {
        "timestamp": 1714240000000,
        "message": "logged 3 jars in tent A",
        "expiresInSeconds": 0,
        "viewOnce": false,
        "attachments": []
      }
    }
  }
]
```

Group variant adds `dataMessage.groupInfo`:
```json
"groupInfo": { "groupId": "<base64>", "type": "DELIVER" }
```

Bot phone reference (for mention/quote matching): `config.signalSender` (E.164).
Operator must `echo $SIGNAL_SENDER` or read from `.env` for the BOT_PHONE constant in
fixtures. Use literal `+BOTPHONE_PLACEHOLDER` if BOT_PHONE has not been operator-attested
yet, and replace via search-replace once SMOKE.md captures it.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Live signal-cli group-send + receive smoke probe</name>
  <read_first>
    - .planning/phases/37-multi-farmer-routing/37-RESEARCH.md §Risks #4, #9, #10 + Assumptions Log (A1/A2/A3)
    - .planning/phases/37-multi-farmer-routing/37-CONTEXT.md §canonical_refs (Phase 33 D-05 constraint)
    - Memory: feedback_alerter_env_convention_bridge_http_url (use BRIDGE_HTTP_URL not bridge:8080 — N/A here but keeps the convention straight)
    - Memory: project_signal_cli_link_gotchas + project_signal_cli_primary_reregister_path
  </read_first>
  <what-built>
    Operator runs four live signal-cli probes against the production signal-cli container
    (post-PRE-01 — currently deviceId=1 per Phase 36) and pastes outcomes into 37-SMOKE.md:

    Probe A (group-send works on current deviceId — A3):
      curl -s -X POST http://localhost:8085/v2/send \
        -H 'Content-Type: application/json' \
        -d "{\"message\":\"Phase 37 smoke A — please ignore\",\"number\":\"$SIGNAL_SENDER\",\"recipients\":[\"group.$SIGNAL_GROUP_ID\"]}"
      # Expect: HTTP 201 with {"timestamp":N}. Anything else = phase BLOCKED.
      # SIGNAL_GROUP_ID obtained from: curl -s http://localhost:8085/v1/groups/$SIGNAL_SENDER | jq -r '.[0].internal_id'

    Probe B (receive a silent group message — captures groupInfo shape):
      Operator (or another farmer) sends a plain message to "Mushroom Farm" group
      from f2's phone. Then:
      curl -s "http://localhost:8085/v1/receive/$SIGNAL_SENDER?max_messages=10" > /tmp/p37-recv-silent.json
      jq '.[0]' /tmp/p37-recv-silent.json
      # Expect: envelope.dataMessage.groupInfo.groupId present, type=="DELIVER"

    Probe C (receive an @mention — A2 / Risk #10):
      f2 sends a message tagging the bot user in the group ("@<bot-display-name> test").
      curl -s "http://localhost:8085/v1/receive/$SIGNAL_SENDER" > /tmp/p37-recv-mention.json
      jq '.[0].envelope.dataMessage.mentions' /tmp/p37-recv-mention.json
      # Expect: [{name, number, uuid, start, length}]. If null/missing → A2 FAILS.

    Probe D (receive a reply-to-bot — A1 / Risk #9):
      Bot first posts a message to the group (use Probe A's send). f2 long-presses
      that bot message in Signal and replies "quote test".
      curl -s "http://localhost:8085/v1/receive/$SIGNAL_SENDER" > /tmp/p37-recv-quote.json
      jq '.[0].envelope.dataMessage.quote' /tmp/p37-recv-quote.json
      # Expect: { author OR authorNumber: "<bot E.164>", id, text, ... }
      # CAPTURE: which field name carries the phone — author vs authorNumber.
  </what-built>
  <how-to-verify>
    1. SSH to the host running the docker-compose stack (elder-plops).
    2. Confirm signal-cli is reachable: `curl -s http://localhost:8085/v1/about | head`
    3. Read `SIGNAL_SENDER` and `SIGNAL_GROUP_ID` from `.env` at repo root.
       If `SIGNAL_GROUP_ID` is unset: `curl -s http://localhost:8085/v1/groups/$SIGNAL_SENDER | jq` → grab `internal_id`.
    4. Run Probes A/B/C/D above in sequence.
    5. Save each `/tmp/p37-recv-*.json` for fixture extraction in Task 2.
    6. Author `.planning/phases/37-multi-farmer-routing/37-SMOKE.md` with:
       - Date / deviceId / signal-cli image tag (from `docker inspect signal-cli | jq .[].Config.Image`)
       - Probe A: HTTP status code + response body verbatim
       - Probe B/C/D: the raw envelope JSON (one per fixture)
       - The actual field name observed for quote phone (author vs authorNumber)
       - Bot phone value (mask all but last 4 digits — match `maskNumber` convention from signal.js)
       - VERDICT line: "Wave 0 gate: PASS" or "Wave 0 gate: FAIL — &lt;reason&gt;"
    7. If Probe A returns anything other than 2xx, STOP — file a follow-up backlog item, do not proceed to Task 2.
  </how-to-verify>
  <acceptance_criteria>
    - .planning/phases/37-multi-farmer-routing/37-SMOKE.md exists with all four probe outcomes recorded
    - Probe A status is 2xx (HTTP 201 expected per signal-cli-rest-api send response shape)
    - Probe B captures a real envelope showing dataMessage.groupInfo.groupId
    - Probe C either confirms mentions[] populated OR documents the gap with a deferred-item link
    - Probe D records which field carries the quote author phone (author vs authorNumber); both names go into the defensive matcher in Plan 03
    - Verdict line present
  </acceptance_criteria>
  <resume-signal>Type "smoke-pass" if VERDICT==PASS, or describe the failure mode</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Extract six group envelope fixtures from smoke captures</name>
  <read_first>
    - .planning/phases/37-multi-farmer-routing/37-SMOKE.md (just authored)
    - .planning/phases/37-multi-farmer-routing/37-PATTERNS.md §"Test fixtures" (the per-fixture additions list)
    - src/agents/alerter/test/fixtures/envelopes/text.json (DM template)
    - src/agents/alerter/test/fixtures/envelopes/snooze.json (DM command template)
  </read_first>
  <files>
    src/agents/alerter/test/fixtures/envelopes/group-silent.json,
    src/agents/alerter/test/fixtures/envelopes/group-mention.json,
    src/agents/alerter/test/fixtures/envelopes/group-command.json,
    src/agents/alerter/test/fixtures/envelopes/group-reply-to-bot.json,
    src/agents/alerter/test/fixtures/envelopes/group-mention-and-command.json,
    src/agents/alerter/test/fixtures/envelopes/group-unknown-sender.json
  </files>
  <action>
    Create six JSON fixtures using the Probe B/C/D captures as ground truth. Each fixture
    is a top-level JSON array containing one envelope (matches the existing text.json shape).

    Constants for fixture authoring:
      BOT_PHONE = value pasted in 37-SMOKE.md (the masked-disclosed bot E.164)
      F2_PHONE  = "+59892893012" (zoy — matches existing text.json source)
      F3_PHONE  = "+59891234567" (placeholder for a third whitelisted farmer)
      UNKNOWN   = "+15550009999" (NOT in any SIGNAL_FARMER_MAP — D-12 target)
      GROUP_ID  = the actual base64 from SMOKE.md Probe A (real but harmless in tests)
      QUOTE_AUTHOR_FIELD = "author" OR "authorNumber" — whichever SMOKE.md Probe D observed.
        If both → populate both fields with BOT_PHONE in the reply-to-bot fixture.

    Per fixture, all share this skeleton (extend text.json):
    ```json
    [
      { "envelope": {
          "source": "<sender>",
          "sourceNumber": "<sender>",
          "sourceUuid": "22222222-2222-2222-2222-222222222222",
          "timestamp": 1714240000000,
          "dataMessage": {
            "timestamp": 1714240000000,
            "message": "<varies per fixture>",
            "expiresInSeconds": 0,
            "viewOnce": false,
            "attachments": [],
            "groupInfo": { "groupId": "<GROUP_ID>", "type": "DELIVER" }
          }
        }
      }
    ]
    ```

    Per-fixture overrides:

    - group-silent.json: source=F2_PHONE, message="hey did anyone water tent B"
      (No mentions[], no command keyword, no quote — should trigger NO reply.)

    - group-mention.json: source=F2_PHONE, message="@bot status",
      dataMessage.mentions = [{name:"bot", number: BOT_PHONE, uuid:"33333333-3333-3333-3333-333333333333", start:0, length:4}]

    - group-command.json: source=F2_PHONE, message="mute"
      (No mentions, no quote — pure command keyword.)

    - group-reply-to-bot.json: source=F2_PHONE, message="quote test",
      dataMessage.quote = { "id": 1714240000000, "<QUOTE_AUTHOR_FIELD>": BOT_PHONE, "text": "alerts muted for 24h" }
      If SMOKE.md observed BOTH fields populated, include BOTH "author" and "authorNumber"
      each set to BOT_PHONE (defensive — A1 mitigation).

    - group-mention-and-command.json: source=F2_PHONE, message="@bot mute",
      dataMessage.mentions = [{name:"bot", number: BOT_PHONE, uuid:"...", start:0, length:4}]
      (BOTH triggers — feeds D-09 envelope-dedupe assertion in Plan 03 tests.)

    - group-unknown-sender.json: source=UNKNOWN, message="hello from a random number"
      groupInfo present. Note: this fixture assumes UNKNOWN has been added to the alerter
      whitelist for the test scenario (the fixture lives in test-land; the test harness
      controls whitelist via config injection — does NOT require real Signal whitelisting).

    Per D-04: bot's display name in mentions is NOT the matcher — phone number is. Do not
    add display-name-only mentions.

    Format JSON with 2-space indent, trailing newline (match text.json formatting).
  </action>
  <verify>
    <automated>cd /mnt/slime-kingdom/opt/mushy && for f in src/agents/alerter/test/fixtures/envelopes/group-silent.json src/agents/alerter/test/fixtures/envelopes/group-mention.json src/agents/alerter/test/fixtures/envelopes/group-command.json src/agents/alerter/test/fixtures/envelopes/group-reply-to-bot.json src/agents/alerter/test/fixtures/envelopes/group-mention-and-command.json src/agents/alerter/test/fixtures/envelopes/group-unknown-sender.json; do jq -e '.[0].envelope.dataMessage.groupInfo.groupId' "$f" >/dev/null || { echo "MISSING groupInfo in $f"; exit 1; }; done && echo "all 6 fixtures have groupInfo"</automated>
  </verify>
  <acceptance_criteria>
    - All six JSON files parse with `jq .`
    - All six contain `.[0].envelope.dataMessage.groupInfo.groupId`
    - group-mention.json contains `mentions[].number` equal to the BOT_PHONE constant used in fixtures
    - group-reply-to-bot.json contains `dataMessage.quote.<author|authorNumber>` set to BOT_PHONE
    - group-mention-and-command.json contains BOTH `mentions[]` AND a message starting with "mute"
    - group-unknown-sender.json source is NOT one of {F2_PHONE, F3_PHONE, BOT_PHONE}
  </acceptance_criteria>
  <done>Six fixture files committed; grep-style verify command above exits 0; downstream plans can import these fixtures by relative path</done>
</task>

<task type="auto">
  <name>Task 3: Verify jest framework is wired and existing fixtures still load</name>
  <read_first>
    - src/agents/alerter/package.json (jest config block)
    - src/agents/alerter/test/capture-db.test.js (existing test loading pattern)
  </read_first>
  <files>(no source edits — verification-only)</files>
  <action>
    Confirm the alerter jest harness still loads the existing fixtures AND the six new ones:

    1. cd src/agents/alerter
    2. npx jest --listTests | head -20
       (Confirms jest config resolves; no syntax error introduced.)
    3. npx jest 2>&1 | tail -30
       (Full current suite must remain GREEN. We've only added fixture files; no source
        changes yet, so existing tests should not regress. If a test reads the fixtures dir
        with a glob and fails on the unknown new files, surface the failure now — it will
        force a Plan 03 fixup. Most likely: tests reference specific filenames, no glob.)

    Append the runtime + result to 37-SMOKE.md under a new "## Jest baseline" heading
    (one-liner: "npx jest: X passed / Y total / Zs").
  </action>
  <verify>
    <automated>cd /mnt/slime-kingdom/opt/mushy/src/agents/alerter && npx jest 2>&1 | tail -5 | grep -E "Tests:.*passed" </automated>
  </verify>
  <acceptance_criteria>
    - `npx jest` exits 0 (full alerter suite green)
    - No test failed due to new fixture files
    - 37-SMOKE.md "## Jest baseline" line written
  </acceptance_criteria>
  <done>jest baseline confirmed green; fixtures coexist with existing tests; Wave 1 unblocked</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator's shell → signal-cli REST | Smoke probe sends real Signal messages to a real group; cleartext credentials live in `.env` |
| fixture file → repo | Fixture JSON committed to git contains a real (low-sensitivity) Signal group base64 ID + farmer E.164 phones |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-01-01 | Information disclosure | 37-SMOKE.md captures real envelope JSON | mitigate | mask all but last 4 digits of phones (reuse signal.js maskNumber convention); SMOKE.md is committed but contains no Signal session tokens |
| T-37-01-02 | Information disclosure | Fixture files commit real group base64 ID | accept | Group ID alone is not a credential; sending requires signal-cli session + bot phone identity. Low-value target. |
| T-37-01-03 | Tampering | Smoke probe sends a real message to the live group | accept | Probe messages are operator-prefixed "Phase 37 smoke" and explicitly disposable; farmers tolerate one round of test traffic per phase |
| T-37-01-04 | Repudiation | No audit trail of who triggered the probe | accept | This is a one-shot operator action with git history (SMOKE.md commit) attesting authorship |
</threat_model>

<verification>
Manual: 37-SMOKE.md verdict is PASS; six fixtures exist; full jest suite green.
</verification>

<success_criteria>
- A3 verified: live signal-cli group-send returns 2xx → phase unblocked
- A1/A2 documented: actual quote field name + mentions[] presence recorded for Plan 03 matcher logic
- Six fixture files committed and structurally validated
- Wave 1 (Plans 02 + 03) can proceed against real-shaped fixtures, not hand-crafted guesses
</success_criteria>

<output>
After completion, create `.planning/phases/37-multi-farmer-routing/37-01-SUMMARY.md` recording:
- Smoke verdict line copied from 37-SMOKE.md
- The actual `QUOTE_AUTHOR_FIELD` observed (this propagates to Plan 03's collectGroupTriggers)
- Jest baseline (pass count / runtime)
- Any deferred items (e.g. mentions[] gap observed → backlog 999.x to upgrade signal-cli-rest-api)
</output>
