---
phase: 37
plan: 04
type: execute
wave: 3
depends_on: ["37-02", "37-03"]
files_modified:
  - src/agents/alerter/src/index.js
  - docker-compose.override.yml
  - .planning/phases/37-multi-farmer-routing/37-RUNBOOK.md
autonomous: false
requirements: [ROUTE-01, ROUTE-02, ROUTE-03]
user_setup:
  - service: signal-cli
    why: "Operator must populate SIGNAL_GROUP_ID and SIGNAL_FARMER_MAP in repo-root .env"
    env_vars:
      - name: SIGNAL_GROUP_ID
        source: "curl http://localhost:8085/v1/groups/$SIGNAL_SENDER | jq -r '.[0].internal_id' — bare base64, no group. prefix"
      - name: SIGNAL_FARMER_MAP
        source: "Operator-authored — format +phone:slug,+phone:slug,+phone:slug for f1, zoy, f3"
    dashboard_config:
      - task: "Re-deploy alerter container after .env update"
        location: "elder-plops: docker compose up -d --build alerter"
tags: [compose, env, integration, runbook, integration-test]

must_haves:
  truths:
    - "docker-compose.override.yml alerter env block plumbs SIGNAL_GROUP_ID and SIGNAL_FARMER_MAP"
    - "alerter boot threads config.signalGroupId into createSignalClient as defaultTarget = {groupId} (D-04 flip)"
    - "alerter boot threads config.signalFarmerMap into createCapturePipeline"
    - "When SIGNAL_GROUP_ID is unset in .env, alerter falls back to legacy DM behavior (defaultTarget = signalRecipient phone) — back-compat preserved"
    - "37-RUNBOOK.md documents operator steps for obtaining SIGNAL_GROUP_ID, authoring SIGNAL_FARMER_MAP, and the deploy/verify sequence"
    - "Live integration test: f2 DMs bot → reply lands on f2 only (999.20 proof-of-fix)"
    - "Live integration test: heartbeat or rule send lands in 'Mushroom Farm' group (D-04 visibility)"
  artifacts:
    - path: "docker-compose.override.yml"
      provides: "Two new env lines under alerter (SIGNAL_GROUP_ID, SIGNAL_FARMER_MAP)"
    - path: "src/agents/alerter/src/index.js"
      provides: "Wire-up: defaultTarget construction + signalFarmerMap threading into pipeline factory"
    - path: ".planning/phases/37-multi-farmer-routing/37-RUNBOOK.md"
      provides: "Operator deploy + verification recipe"
  key_links:
    - from: ".env SIGNAL_GROUP_ID"
      to: "alerter signal.js defaultTarget = {groupId}"
      via: "docker-compose env → config.js → createSignalClient constructor"
      pattern: "SIGNAL_GROUP_ID"
    - from: ".env SIGNAL_FARMER_MAP"
      to: "capture.js farmosPerson resolution"
      via: "docker-compose env → config.js parseFarmerMap → createCapturePipeline"
      pattern: "SIGNAL_FARMER_MAP"
---

<objective>
Final wiring + integration: plumb the two new envs through docker-compose to the alerter
boot path, runbook the operator deploy, and run the 999.20 proof-of-fix live (f2 DM →
reply to f2 only) plus the D-04 group-default attestation (next heartbeat/alert lands
in group).

After this plan ships, Phase 37 is operationally complete: ROUTE-01/02/03 closed end-to-end
on the live stack.

Output: compose override line additions + index.js wire-up + RUNBOOK.md + live attestation
checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/37-multi-farmer-routing/37-CONTEXT.md
@.planning/phases/37-multi-farmer-routing/37-RESEARCH.md
@.planning/phases/37-multi-farmer-routing/37-PATTERNS.md
@.planning/phases/37-multi-farmer-routing/37-01-SUMMARY.md
@.planning/phases/37-multi-farmer-routing/37-02-SUMMARY.md
@.planning/phases/37-multi-farmer-routing/37-03-SUMMARY.md
@.planning/phases/37-multi-farmer-routing/37-SMOKE.md
@src/agents/alerter/src/index.js
@docker-compose.override.yml

<interfaces>
<!-- Current alerter env block in docker-compose.override.yml (around line 75-77) -->
```yaml
      - SIGNAL_API_URL=http://signal-cli:8080
      - SIGNAL_SENDER=${SIGNAL_SENDER}
      - SIGNAL_RECIPIENT=${SIGNAL_RECIPIENT}
      - SIGNAL_ADDITIONAL_SENDERS=${SIGNAL_ADDITIONAL_SENDERS}
```
(Phase 36 commit c8e9ac1 landed the SIGNAL_ADDITIONAL_SENDERS line. Add two more after it.)

<!-- index.js signalClient construction shape (Plan 02 made constructor accept defaultTarget) -->
```javascript
const signalClient = createSignalClient({
  apiUrl: config.signalApiUrl,
  sender: config.signalSender,
  recipient: config.signalRecipient,
  // ADD: defaultTarget = config.signalGroupId ? { groupId: config.signalGroupId } : config.signalRecipient
  // Logger / maxSendsPerHour args unchanged
});
```

<!-- createCapturePipeline call shape (Plan 03 added signalFarmerMap option) -->
```javascript
const pipeline = createCapturePipeline({
  pool, signalClient, transcribeClient, llmClient,
  captureHistory, sensorSnapshot,
  baseDir: config.captureBaseDir, logger, clock,
  // ADD: signalFarmerMap: config.signalFarmerMap,
});
```

<!-- Phase 36 c8e9ac1 commit pattern (the operator-facing convention to follow) -->
- Add env in docker-compose.override.yml alerter block
- Operator sets value in repo-root .env
- Compose down + up --build alerter
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: index.js wire-up + docker-compose.override.yml env plumbing</name>
  <read_first>
    - src/agents/alerter/src/index.js (full file, focus on createSignalClient + createCapturePipeline construction sites)
    - docker-compose.override.yml (alerter env block — find by `grep -n "alerter:" docker-compose.override.yml` then read ~30 lines after)
    - .planning/phases/37-multi-farmer-routing/37-PATTERNS.md §"index.js (boot wire-up)" + §"docker-compose.override.yml"
    - .planning/phases/37-multi-farmer-routing/37-CONTEXT.md D-04, D-05, D-11, D-16
    - Memory: feedback_alerter_env_convention_bridge_http_url (do NOT introduce BRIDGE_URL; existing BRIDGE_HTTP_URL convention untouched here)
  </read_first>
  <files>
    src/agents/alerter/src/index.js,
    docker-compose.override.yml
  </files>
  <action>
    PART A — src/agents/alerter/src/index.js:

    1. Find the `createSignalClient` invocation (near boot — likely top half of file).
       Modify to pass `defaultTarget`:
       ```javascript
       const signalClient = createSignalClient({
         apiUrl: config.signalApiUrl,
         sender: config.signalSender,
         recipient: config.signalRecipient,
         defaultTarget: config.signalGroupId
           ? { groupId: config.signalGroupId }
           : config.signalRecipient,
         maxSendsPerHour: config.alertMaxSendsPerHour,
         // keep all other existing args
       });
       ```
       This is the D-04 flip: when SIGNAL_GROUP_ID is set, the default target is the group;
       absent, fall through to phone (back-compat).

    2. Find the `createCapturePipeline` invocation. Add `signalFarmerMap`:
       ```javascript
       const pipeline = createCapturePipeline({
         // ... existing args ...
         signalFarmerMap: config.signalFarmerMap,
       });
       ```

    3. Add ONE boot-log line right after both construction sites:
       ```javascript
       logger.info(`[boot] signal defaultTarget = ${config.signalGroupId
         ? `group:${config.signalGroupId.slice(0, 8)}…`
         : 'DM:' + maskNumber(config.signalRecipient)}`);
       logger.info(`[boot] farmer-map entries = ${config.signalFarmerMap.size}`);
       ```
       (Operator can read these from `docker compose logs alerter` post-deploy to confirm
       env reached the runtime — guards against silent typos in .env.)

    4. NO CHANGE to applyEvent dispatch (D-02): heartbeat/snooze_ack/recovery/send actions
       inherit defaultTarget automatically. Add a code comment near applyEvent to call this
       out explicitly: `// D-04: non-reply sends inherit defaultTarget (group when SIGNAL_GROUP_ID set).`

    5. NO CHANGE to Phase 33 VPS heartbeat path. Confirm by grep: the alerter file should
       not contain anything matching `/vps|VPS|heartbeat.receiver/i` (that path lives in
       a different service per memory: project_phase33_shipped). If it does, leave untouched.

    PART B — docker-compose.override.yml:

    1. Locate the alerter env block (grep -n "alerter:" docker-compose.override.yml; read
       the next ~30 lines until the env: section ends).

    2. Immediately after the line `- SIGNAL_ADDITIONAL_SENDERS=${SIGNAL_ADDITIONAL_SENDERS}`
       (added in Phase 36 commit c8e9ac1), append:
       ```yaml
             - SIGNAL_GROUP_ID=${SIGNAL_GROUP_ID}
             - SIGNAL_FARMER_MAP=${SIGNAL_FARMER_MAP}
       ```
       Match indentation exactly with the surrounding env entries (likely 6 spaces).

    3. NO default value for either. Absence in `.env` → config.js reads `''` → signalGroupId=null,
       signalFarmerMap=empty Map → alerter behaves as pre-Phase-37 (back-compat).

    4. NO CHANGE to bridge service env or any other compose block. Phase 33 path is in a
       different file/service.
  </action>
  <verify>
    <automated>grep -n "SIGNAL_GROUP_ID=\${SIGNAL_GROUP_ID}" /mnt/slime-kingdom/opt/mushy/docker-compose.override.yml && grep -n "SIGNAL_FARMER_MAP=\${SIGNAL_FARMER_MAP}" /mnt/slime-kingdom/opt/mushy/docker-compose.override.yml && grep -n "defaultTarget: config.signalGroupId" /mnt/slime-kingdom/opt/mushy/src/agents/alerter/src/index.js && grep -n "signalFarmerMap: config.signalFarmerMap" /mnt/slime-kingdom/opt/mushy/src/agents/alerter/src/index.js && echo "wire-up grep OK"</automated>
  </verify>
  <acceptance_criteria>
    - Both env lines present in docker-compose.override.yml under alerter env block (verified by grep)
    - index.js threads defaultTarget into createSignalClient (verified by grep)
    - index.js threads signalFarmerMap into createCapturePipeline (verified by grep)
    - Two new boot-log lines exist for operator post-deploy verification
    - `cd src/agents/alerter && npx jest` still green (no regression from index.js wire-up — index.js itself isn't unit-tested but indirect coverage via Plan 02/03 tests with config injection)
    - `docker compose config` from repo root parses without error (compose-file syntax valid)
    - Phase 33 path NOT touched (no new vps/heartbeat-receiver references added)
  </acceptance_criteria>
  <done>Compose env plumbed; index.js wires Plan 02/03 outputs into runtime; back-compat preserved when envs unset</done>
</task>

<task type="auto">
  <name>Task 2: Author 37-RUNBOOK.md with operator deploy + verification recipe</name>
  <read_first>
    - .planning/phases/36-*/36-RUNBOOK.md (existing runbook structure — Phase 36 is the most recent template)
    - .planning/phases/37-multi-farmer-routing/37-SMOKE.md (the operator-validated SIGNAL_GROUP_ID value)
    - .planning/phases/37-multi-farmer-routing/37-CONTEXT.md D-11/D-16
    - Memory: project_signal_cli_link_gotchas, feedback_no_coauthor
  </read_first>
  <files>
    .planning/phases/37-multi-farmer-routing/37-RUNBOOK.md
  </files>
  <action>
    Create a concise (~80-120 line) runbook with the following sections:

    ## Phase 37 Runbook — Multi-farmer Routing

    ### 1. Prerequisites
    - Phase 36 shipped (signal-cli is primary, deviceId=1)
    - Plans 37-01..37-03 merged to main; alerter container rebuild pending
    - SMOKE.md verdict line = PASS

    ### 2. Obtain SIGNAL_GROUP_ID
    Step-by-step:
      a. From the host running signal-cli (elder-plops):
         `curl -s http://localhost:8085/v1/groups/$SIGNAL_SENDER | jq`
      b. Identify the "Mushroom Farm" entry by its `name` field.
      c. Copy the `internal_id` value (bare base64 — NO `group.` prefix; the alerter prepends it).
      d. Validate format: should match `^[A-Za-z0-9+/=]{20,}$`.

    Common pitfall: do NOT paste the `id` field if it's URL-form (`...=`) — internal_id is
    what signal-cli /v2/send expects.

    ### 3. Author SIGNAL_FARMER_MAP
    Format: `+phone:slug,+phone:slug,+phone:slug`
    - phones are E.164 (`+` prefix, country code, no spaces)
    - slugs are farmOS person slugs (read from farmOS people directory; see
      `/mnt/slime-kingdom/shared/farmos/.planning/notes/` for the agreed slug convention)
    - For v1.7: three entries — f1 (operator), zoy (f2), f3 (third farmer)
    - Whitespace around `:` and `,` is OK (parser trims)
    - Whitelisted but unmapped numbers → captured with farmos_person='(unassigned)'

    ### 4. Update .env at repo root
    Append (or update) these two lines:
    ```
    SIGNAL_GROUP_ID=<paste from step 2>
    SIGNAL_FARMER_MAP=+<f1>:f1,+<zoy>:zoy,+<f3>:f3
    ```
    (Phones must be unmasked in .env; this file is in .gitignore.)

    ### 5. Deploy alerter
    From repo root on elder-plops:
    ```
    docker compose up -d --build alerter
    docker compose logs --tail=50 alerter | grep "\\[boot\\]"
    ```
    Expected boot log lines (verify presence):
      `[boot] signal defaultTarget = group:<8chars>…`
      `[boot] farmer-map entries = 3`

    If `defaultTarget = DM:...` appears, SIGNAL_GROUP_ID didn't reach the runtime —
    re-check .env line + compose file env block.

    ### 6. Live verification — three attestations

    Attestation A — 999.20 proof-of-fix (ROUTE-01):
      - F2 (zoy) sends a Signal DM to the bot: `ping P37-<rand>`
      - Within 30s, F2 receives a reply
      - F1 (operator) confirms NO incoming Signal message during the same window
      - Pass criterion: reply visible on F2 phone, absent on F1 phone

    Attestation B — Group default visibility (D-04, ROUTE-02):
      - Trigger a heartbeat or wait for the next scheduled send (heartbeat cadence is per
        alerter config; check `docker compose logs alerter | grep heartbeat`)
      - Pass criterion: heartbeat message appears in the "Mushroom Farm" group thread,
        not as a DM to F1

    Attestation C — Unknown-sender capture (ROUTE-03):
      - Pick a whitelisted phone NOT in SIGNAL_FARMER_MAP (a fresh test number added to
        SIGNAL_ADDITIONAL_SENDERS for this purpose). DM the bot.
      - Verify the capture row: `psql -d mushy -c "SELECT sender, farmos_person, reply_target_kind FROM signal_capture ORDER BY captured_at DESC LIMIT 1"`
      - Pass criterion: farmos_person = `(unassigned)`, reply_target_kind = `dm`, AND a
        Signal reply landed on the test phone (not silently dropped)

    Attestation D — Group dedupe (D-09):
      - From F2 phone, send to group: `@<bot-display-name> mute` (must include both
        @mention AND command keyword)
      - Pass criterion: exactly ONE bot reply in the group thread (count visible bot
        messages from before/after); a single signal_capture row exists for the envelope

    ### 7. Rollback
    If any attestation fails:
    - Comment out (or remove) `SIGNAL_GROUP_ID=` line in .env
    - `docker compose up -d --build alerter` — alerter reverts to DM-only behavior
    - File deferred-item; do not block other phases

    ### 8. Phase 33 invariant check
    After deploy, send a test outage signal from VPS (operator-side procedure per
    33-RUNBOOK.md) and confirm the operator alert STILL lands as a direct DM, NOT in the
    group. (Phase 33 path bypasses alerter — verifies no regression.)

    Footer: no co-authored-by trailer per project convention.
  </action>
  <verify>
    <automated>test -f /mnt/slime-kingdom/opt/mushy/.planning/phases/37-multi-farmer-routing/37-RUNBOOK.md && grep -c "Attestation [A-D]" /mnt/slime-kingdom/opt/mushy/.planning/phases/37-multi-farmer-routing/37-RUNBOOK.md | awk '$1 >= 4 {exit 0} {exit 1}' && echo "runbook structurally OK"</automated>
  </verify>
  <acceptance_criteria>
    - File exists at the expected path
    - Has sections for prerequisites, obtain SIGNAL_GROUP_ID, author SIGNAL_FARMER_MAP, .env update, deploy, four live attestations, rollback, Phase 33 invariant
    - Four attestation procedures present (A/B/C/D) and each has a falsifiable pass criterion
    - No `Co-Authored-By` trailer
  </acceptance_criteria>
  <done>Operator has a complete, falsifiable deploy + verify recipe</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Live integration test — execute 37-RUNBOOK.md attestations A/B/C/D</name>
  <read_first>
    - .planning/phases/37-multi-farmer-routing/37-RUNBOOK.md (just authored)
    - .planning/phases/37-multi-farmer-routing/37-SMOKE.md (for SIGNAL_GROUP_ID value)
  </read_first>
  <what-built>
    Plans 37-01 through 37-04 Task 1+2 are all merged. The alerter has been rebuilt with
    the new envs in compose. Two `[boot]` log lines confirm SIGNAL_GROUP_ID + farmer-map
    reached the runtime. ROUTE-01/02/03 are live.
  </what-built>
  <how-to-verify>
    Operator (and at minimum F2/zoy) executes Attestation A through D from 37-RUNBOOK.md
    in order. For each:

    Attestation A (ROUTE-01 / 999.20 proof-of-fix):
      1. F2 DMs the bot from her phone: `ping P37-<3 digit rand>`
      2. Wait up to 60s.
      3. Confirm F2 receives a Signal reply.
      4. Operator confirms NO Signal message arrived on F1 phone during the window.
      5. Record outcome (PASS/FAIL) + the exact ping string in 37-04-SUMMARY.md.

    Attestation B (ROUTE-02 / D-04 group default):
      1. Either wait for next scheduled heartbeat OR trigger one via the existing
         force-heartbeat code path (check `docker compose logs alerter | grep heartbeat`).
      2. Confirm the heartbeat message appears in the "Mushroom Farm" group thread.
      3. Confirm operator's F1 DM thread did NOT receive it.
      4. Record outcome + timestamp.

    Attestation C (ROUTE-03 / unknown-sender capture):
      1. Identify a whitelisted phone NOT mapped in SIGNAL_FARMER_MAP (a real device the
         operator can use — could be a test SIM or a temporarily-added gumbald number).
      2. DM the bot from that phone: `hello P37-unmapped`
      3. Confirm a reply arrives back on the test phone.
      4. On elder-plops: `psql -h localhost -U postgres -d mushy -c "SELECT sender, farmos_person, reply_target_kind, captured_at FROM signal_capture ORDER BY captured_at DESC LIMIT 1"`
         (Substitute the actual DB connection per `docker compose exec timescale ...` if
         psql isn't on host.)
      5. Verify: sender matches the test phone, farmos_person='(unassigned)', reply_target_kind='dm'
      6. Record outcome + DB row.

    Attestation D (ROUTE-02 / D-09 envelope dedupe):
      1. F2 sends to the Mushroom Farm group: `@<bot-name> mute` (must include @mention AND command keyword in one message).
      2. Confirm the bot posts EXACTLY ONE reply/ack to the group (not two).
      3. On DB: `SELECT id, sender, group_id, reply_target_kind FROM signal_capture ORDER BY captured_at DESC LIMIT 1`
      4. Verify: exactly one row, group_id non-NULL, reply_target_kind='group'
      5. Record outcome.

    Attestation E (Phase 33 invariant non-regression):
      1. Operator simulates a VPS-side outage trigger per 33-RUNBOOK.md (e.g., stop the
         heartbeat sender on elder-plops for >3min and let VPS detect silence).
      2. Confirm: operator receives the outage alert as a DM on F1, NOT in the group.
      3. Record outcome.

    If A, B, C, D, AND E all pass: type "phase-37-attest-pass" — phase is complete pending
    /gsd-verify-work artifact generation.
    If ANY fails: paste the failure mode; revisit either runbook rollback (Section 7) or
    file a follow-up plan.
  </how-to-verify>
  <acceptance_criteria>
    - Attestation A: F2 reply received on F2; no F1 traffic during window
    - Attestation B: heartbeat lands in group; not in F1 DM
    - Attestation C: unmapped-sender row has farmos_person='(unassigned)' AND reply fires
    - Attestation D: exactly ONE bot reply for a mention+command envelope; one capture row
    - Attestation E: VPS outage path still goes direct-to-F1 (Phase 33 non-regression)
    - All five outcomes recorded in 37-04-SUMMARY.md
  </acceptance_criteria>
  <resume-signal>Type "phase-37-attest-pass" or describe the failure</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator .env → docker-compose → alerter runtime | SIGNAL_GROUP_ID typo silently routes ALL alerts to wrong group (or breaks signal-cli send entirely) |
| operator .env SIGNAL_FARMER_MAP → farmos_person attribution | Map typo silently mis-attributes capture rows; downstream Phase 38 extraction inherits the wrong farmer slug |
| Phase 33 VPS path (out of scope for this phase) | MUST remain direct-to-F1, not inherit group default |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-04-01 | Information disclosure | Wrong-group SIGNAL_GROUP_ID leaks farm data to unintended recipients | mitigate | Boot log line `[boot] signal defaultTarget = group:<8chars>…` lets operator visually verify against signal-cli --list-groups output; runbook step 5 makes this a required check before declaring deploy done |
| T-37-04-02 | Tampering | .env on host is operator-controlled; corruption changes routing globally | accept | .env is gitignored, host-local, owned by the operator account; same trust model as all other SIGNAL_* envs (ASVS L1) |
| T-37-04-03 | Spoofing | farmer-map entry with wrong slug attributes messages to wrong farmOS person | mitigate | Boot log `farmer-map entries = N` lets operator catch missing entries; runbook step 3 prescribes reading slug from farmOS people directory (single source of truth) |
| T-37-04-04 | DoS | Group thread becomes chatty; global rate-limit cap starves alert-class sends (RESEARCH Risk #1) | accept | Per-recipient buckets explicitly deferred (D-03); operator can bump ALERT_MAX_SENDS_PER_HOUR if observed; heartbeat already passes bypassCap=true |
| T-37-04-05 | Repudiation | Phase 33 VPS heartbeat regression — outage alert goes to group instead of F1 | mitigate | Attestation E in runbook is a non-regression check; Phase 33's path doesn't pass through alerter (different container) so structurally cannot regress, but verify anyway |
| T-37-04-06 | Elevation of privilege | New compose env lets operator (without code change) silently flip alert destination | accept | Operator already has full .env control; this is the intended config surface |
</threat_model>

<verification>
- Task 1 grep checks confirm both compose env lines + index.js wire-up
- Task 2 runbook structurally complete (4 attestations + Phase 33 check)
- Task 3 (checkpoint): all five attestations PASS as recorded in 37-04-SUMMARY.md
- Final: `cd src/agents/alerter && npx jest` green (full suite — last regression check)
</verification>

<success_criteria>
- ROUTE-01 attested live: F2 DM → reply to F2 only (999.20 retired)
- ROUTE-02 attested live: heartbeat lands in group; envelope dedupe (D-09) holds; silent group messages do not provoke replies
- ROUTE-03 attested live: known phone → slug; unknown → '(unassigned)' AND reply still fires; capture row exists for every received envelope
- D-04 attested: alerter-originated sends default to group when SIGNAL_GROUP_ID is set; fall back to DM when unset
- D-05 attested: Phase 33 VPS heartbeat path unaffected — operator-only DM preserved
</success_criteria>

<output>
After completion, create `.planning/phases/37-multi-farmer-routing/37-04-SUMMARY.md` recording:
- compose + index.js change line counts
- 37-RUNBOOK.md authoring confirmed
- Five attestation outcomes (A/B/C/D/E) with timestamps, exact ping strings used, and DB rows captured
- Any deferred items (e.g. SIGNAL_FARMER_MAP live-reload, multi-group support, LLM-classifier v2 group reply gate)
- Phase 37 closeout line: "ROUTE-01/02/03 closed live YYYY-MM-DD; 999.20 retired"
</output>
