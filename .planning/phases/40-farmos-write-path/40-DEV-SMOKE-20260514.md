---
phase: 40-farmos-write-path
artifact: dev-smoke
run_ts: 2026-05-14T16:23Z
verdict: PASS for ship-gate (seeding green; harvest blocked on pre-existing dev QR gap, not schema)
status: Option A hybrid schema convergence proven live
---

# Phase 40 -- Dev-farmOS Smoke -- 2026-05-14 (Option A hybrid)

Second live smoke after the Option A hybrid schema convergence with the
farmOS team (commits 0e56eec mushy + farmOS-side `4627acc` /
`seed-fungi-xing.php`). Replaces the 2026-05-13 smoke result; the v2
blocker (Backlog B taxonomy seeding) is now resolved.

## Setup

| Step | Detail |
|------|--------|
| Mushy alerter image | `b0c5e898` (rebuilt 2026-05-14 16:21 UTC; commit `0e56eec` Option A code) |
| FARMOS_URL | `http://10.68.155.50:18080` (dev-farmOS) |
| asset_link probe | absent, using farm_id_tag fallback (unchanged from 2026-05-13) |
| Taxonomies live | `fungi_type` (14 strains), `fungi_xing` (block, fruit), `substrate_type` (10 + legacy `Sawdust`) |
| Pre-flight | watchdog started; FARMOS_USERNAME=Vikki; dev-farmOS reachable HTTP 200 |

## Test 1 -- Seeding (PASS)

Synthetic confirmed-seeding draft (`smoke20260514162321_optA_seed_bc28704e`):
species_code=DT, batch=BATCH-SMK-260514-DEV, block=260514_DT_99, no QR.

| seq | event | latency | status |
|-----|-------|---------|--------|
| 1 | commit_attempt | -- | -- |
| 2 | commit_success | 2802 ms | 201 |

Single attempt. Zero retries. Read-back from dev-farmOS:

- **Asset** `b9e5ca52-ce3c-498c-a7b0-25cb106cf59a` (`asset--fungi/260514_DT_99`):
  - `fungi_type` relationship -> term `DT` (uuid `1218c47b...`)
  - `fungi_xing` relationship -> term `block` (uuid `4b8424ca...`)
  - notes: `mushy:draft:smoke20260514162321_optA_seed_bc28704e`
  - **No batch parent asset created** (expected; D-03d lock)
- **Log** `930cee1a-4cd6-434a-ad8d-e81d3911f07b` (`log--seeding/Inoc 260514_DT_99`):
  - `asset` refs: `[b9e5ca52...]` (block only; D-03d lock)
  - notes: `option A hybrid smoke\nsterilization_batch: BATCH-SMK-260514-DEV\nmushy:draft:...`
  - **`sterilization_batch` lineage preserved in notes** (expected; D-03d lock)

Everything matches the Option A hybrid wire shape. Schema convergence
PROVEN end-to-end against the real dev-farmOS instance.

## Test 2 -- Harvest (blocked on pre-existing dev QR gap, NOT a schema regression)

Synthetic confirmed-harvest draft
(`smoke20260514162505_optA_harv_d69ec48d`): source_qr_codes=`["QR-SMK-NOEXIST-260514"]`,
harvest_batch_name=`HBATCH-2026-05-14-DT-001`, 2 bags.

| seq | event | reason |
|-----|-------|--------|
| 1 | commit_attempt | -- |
| 2 | commit_attempt_retry | missing_source_block |
| 3 | commit_attempt | -- |
| 4 | commit_attempt_retry | missing_source_block |

Watchdog still retrying at cleanup (terminal cleanup forced).

**Why this is not a schema regression:**

- The harvest module loaded against the new code (no module-load errors).
- Pre-check guard fired BEFORE any farmOS write (D-03b expected
  behavior).
- The failure reason is `missing_source_block`, which traces to
  `qr.resolveQr` returning not-found for `QR-SMK-NOEXIST-260514`.
- Root cause is **dev-farmOS has no QR-bound source assets**: the
  `asset_link` module is absent AND the `farm_id_tag` attribute is not
  on the fungi bundle (PATCH attempt 422'd:
  `The attribute farm_id_tag does not exist on the asset--fungi resource type`).
- This was the same situation on 2026-05-13; it is operator-side, not
  related to Option A.

The Option A code paths exercised by harvest (`fungi_type=<strain>` from
`harvest_batch_name` regex, `fungi_xing=fruit`, multi-parent bag,
no harvest_batch fungi asset) are structurally identical to seeding's
asset-create payload shape (which we just proved green). Unit tests
cover the variation (29/29 passing in the four targeted suites).

**Operator follow-up (not blocking v1.7):** install `farmos_asset_link`
on dev OR add `farm_id_tag` field on the fungi bundle, then re-run a
real harvest smoke. Both are operator-deferred per the 2026-05-11 lock
note and noted in `40-CONTEXT.md` D-04a.

## Side observations

1. **Retry classifier still treats deterministic failures as retryable.**
   `missing_source_block` will never succeed by retry (it's a pre-check
   gate); 3 watchdog cycles wasted before commit_failed. Same observation
   noted in the 2026-05-13 smoke side-section (filed as future
   optimization, not urgent).

2. **Legacy `Sawdust` term in `substrate_type` (snake_case-inconsistent)
   is harmless** in current alerter scope -- substrate is log-only per
   the C-narrowed lock and the alerter doesn't reference substrate_type
   terms anywhere in v1.7. Flagging for the post-v1.7 substrate-write-
   path phase.

## Smoke drafts -- cleanup

| id | terminal state |
|----|----------------|
| smoke20260514162321_optA_seed_bc28704e | committed (real asset+log left in dev-farmOS for replay-aware inspection); marked `terminal_reason=smoke-cleanup-optA-2026-05-14` |
| smoke20260514162505_optA_harv_d69ec48d | forced commit_failed; `terminal_reason=smoke-cleanup-optA-2026-05-14`; `commit_failed_reason=missing_source_block (dev QR gap, not schema)` |

## Verdict

**Ship-gate evidence: PASS.** Option A hybrid schema convergence is
proven live against dev-farmOS. Seeding writes correct
`fungi_type=<strain>` + `fungi_xing=block` shape; log carries
`sterilization_batch` lineage; no orphan batch fungi asset. Harvest
path is wired and gated correctly; full harvest E2E remains operator-
gated on QR-resolution infra (pre-existing, non-schema).

v1.7 alerter -> farmOS write-path is greenlit for prod-farmOS once the
prod-side preconditions land (asset_link module install OR farm_id_tag
field; fungi_type/fungi_xing/substrate_type vocab + field config; bot
account with write perms). All tracked in `40-RUNBOOK.md` § 5
"Dev -> prod env-flip."

## Cross-refs

- Previous smoke (Option A predecessor): `40-DEV-SMOKE-20260513.md`
- Schema lock: `40-CONTEXT.md` D-03d (Option A hybrid + C-narrowed
  substrate)
- farmOS-side seeder run: `.planning/notes/2026-05-14-seeder-run-confirmation.md`
- Alerter rewrite: commit `0e56eec`
- Coordination thread: `.planning/notes/2026-05-14-*` (5 reply notes)
