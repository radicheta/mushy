- 25-04 discovery: `config.test.js Test A` fails when DASHBOARD_URL env var is exported by outer shell — test asserts default but `process.env.DASHBOARD_URL=http://100.96.10.66:8080/` leaks in. Pre-existing on main; not caused by this plan. Fix: scrub env in beforeEach or accept default-only assertion conditional on env-unset.
- 25-05 (D-03 narrowing): `degraded=true` not persisted on LLM-failure path. capture.js writes the row UPDATE only when `llmOk=true` (lines 149-158). UAT-5 evidence — 401 from Anthropic + fallback reply fired, but row's `degraded` column stayed false. Transcribe failures DO persist degraded=true (UAT-7 confirmed). Fix: add UPDATE in fallback branch.
- 25-05: `llm_session_tag` column never populated. LLM emits tag in reply prose ("inoc-2026-04-28") but no extraction. Either (a) JSON-mode/tool-use for structured field, or (b) regex `\binoc-\d{4}-\d{2}-\d{2}\b` post-compose. Blocks per-session analytics queries. Natural fit for Wave-5 farmOS event writes.
- 25-05: Multi-envelope context window. Signal/signal-cli splits attachments across envelopes when sent as a single burst (audio + photos). LLM sees one envelope at a time, undercounts attachments, comments per-envelope. Future: cross-envelope correlation keyed on `(sender, captured_at within ±5s)` before LLM compose.
- 25-05: HuggingFace cache not on a named volume (carried from 25-03 backlog) — cold-start cost ~5min for medium-model download on whisper-transcribe rebuild.
