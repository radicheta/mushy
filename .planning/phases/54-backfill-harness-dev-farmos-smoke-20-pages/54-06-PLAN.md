---
phase: 54-backfill-harness-dev-farmos-smoke-20-pages
plan: 06
type: execute
wave: 5
depends_on: [54-05]
files_modified:
  - .planning/phases/54-backfill-harness-dev-farmos-smoke-20-pages/54-CYCLE-2-RUNBOOK.md
  - .planning/phases/54-backfill-harness-dev-farmos-smoke-20-pages/54-CYCLE-2-RECEIPT.md
autonomous: false
requirements: [BACK-08]
tags: [v1.11, backfill, cycle-2, ship-gate, back-08-part3, phase-55-gate]

must_haves:
  truths:
    - "Cycle 2 runs ONLY after 54-CYCLE-1-RECEIPT.md verdict == SIGN-OFF."
    - "Cycle 2 RUNBOOK exists with the 20-page selection (8 fixtured + 12 fresh)."
    - "Operator runs harness against dev farmOS :18080 with --cycle=2 --limit=20."
    - "Run completes with duplicate_asset_count == 0; upsert_stability all stable."
    - "Farmer reviews receipt.md and signs off (or files findings)."
    - "Cycle 2 receipt explicitly states Phase 55 unlock status."
  artifacts:
    - path: .planning/phases/54-.../54-CYCLE-2-RUNBOOK.md
      provides: "Step-by-step operator instructions including --resume-from usage"
      min_lines: 70
    - path: .planning/phases/54-.../54-CYCLE-2-RECEIPT.md
      provides: "Operator + farmer attestation + Phase 55 unlock decision"
      min_lines: 25
  key_links:
    - from: 54-CYCLE-1-RECEIPT.md verdict==SIGN-OFF
      to: 54-CYCLE-2-RUNBOOK.md
      via: "Hard prereq check at top of Cycle-2 RUNBOOK"
      pattern: "SIGN-OFF"
---

<objective>
Cycle 2: 20-page validation run. Only unlocked after Cycle 1 sign-off (54-05). Same workflow at larger N validates that the receipt-review process scales without farmer-process friction. After Cycle 2 sign-off, Phase 55 (full-corpus backfill) unlocks.

Per CONTEXT D-09: Cycle 2 pulls additional images from the IMG_3775..IMG_3861 range. Page selection:
- All 8 already-fixtured pages from 53-04 (5 of which were Cycle 1's; reusing means observable rerun behavior + intra-cycle upsert stability ACROSS the Cycle 1→Cycle 2 boundary on dev farmOS — i.e. the second time IMG_3775 runs, the harness should hit Phase 51's upsert path and REUSE the asset UUIDs, not create duplicates).
- 12 additional pages drawn from IMG_3775..IMG_3861 chosen for: (a) parent-block re-reference density (pages that cite source codes already created by Cycle 1 — validates Phase 51 upsert across page boundaries); (b) date diversity (spread across Feb–Dec 2025 not already in the 8 fixtures).

The 8-fixture overlap is a deliberate stress test of Phase 51's upsert contract: zero new assets should be minted for those 8 pages on the second run.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/54-backfill-harness-dev-farmos-smoke-20-pages/54-CONTEXT.md
@.planning/phases/54-backfill-harness-dev-farmos-smoke-20-pages/54-CYCLE-1-RECEIPT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author 54-CYCLE-2-RUNBOOK.md</name>
  <files>.planning/phases/54-backfill-harness-dev-farmos-smoke-20-pages/54-CYCLE-2-RUNBOOK.md</files>
  <action>
    Create the runbook. Structure:

    ## Prerequisite

    - 54-CYCLE-1-RECEIPT.md MUST exist with verdict == SIGN-OFF. If not, abort.
    - Grep command: `grep -E '^- verdict: SIGN-OFF$' .planning/phases/54-backfill-harness-dev-farmos-smoke-20-pages/54-CYCLE-1-RECEIPT.md` MUST exit 0.

    ## Page Selection (20 pages)

    Tier A — re-run the 8 Cycle 1 + 53-04 fixtures (validates Phase 51 cross-cycle upsert stability):
    - IMG_3775, IMG_3776, IMG_3778, IMG_3782, IMG_3785, IMG_3800, IMG_3825, IMG_3830

    Tier B — 12 new pages from IMG_3775..IMG_3861 chosen by operator to:
    - cover dates not in Tier A (Mar, Jun, Jul, Sep, Oct, Dec 2025)
    - include >= 3 pages whose `source` field references parent-block codes that Tier A already created (verifiable by `awk -F, '$1=="<page-date>"' /mnt/slime-kingdom/shared/mushdatadump/mushroom_log.csv`)
    - skip operator-known-bad pages: IMG_3790 (CA3 strain regex fail), IMG_3810 (continuation/renumbering ambiguity), IMG_3820 (WEDGE substrate regex fail) — same skip list from 53-04 SUMMARY.

    Operator records the 12 chosen IMG_NNNN in this section of the runbook before execution.

    ## Steps

    1. Confirm Cycle 1 SIGN-OFF via the grep command above.
    2. Confirm the dev farmOS state from Cycle 1: assets from those 5 pages exist. Spot-check: `curl -s ... /api/asset/fungi?filter[name]=<block_name>` returns the Cycle 1 UUID.
    3. Backup dev farmOS DB (or note: corpus backup already at /mnt/slime-kingdom/shared/mushdatadump.backup-2026-05-24/ — harness is read-only on corpus; dev farmOS state is the only concern. Per D-09, accept the backup is the corpus anchor; dev farmOS is rebuild-from-scratch-acceptable.)
    4. Dry-run smoke: `cd src/agents/alerter && FARMOS_URL=http://10.68.155.50:18080 ... node scripts/backfill-notebook.js --bulk-backfill --farmer=santi --cycle=2 --limit=20 --dry-run`. Expect 20 pages listed.
    5. Real run: drop --dry-run. Note the new <runId>. The 20-page run is paid LLM ~$2 estimated.
    6. If the run interrupts (network blip, OOM, etc.), use `--resume-from=IMG_NNNN.jpg` to continue without re-spending tokens on completed pages. The runDir's responses.jsonl carries the resume cursor.
    7. Inspect `<runDir>/receipt.md`:
       - Aggregate: duplicate_asset_count MUST be 0.
       - Tier A re-run validation: for the 8 fixture pages, assets_reused count should be HIGH (most assets pre-exist from Cycle 1). assets_created on Tier A pages should be NEAR-ZERO. If non-zero, that's a Phase 51 contract regression — file as finding and BLOCK Phase 55.
       - Upsert stability: all stable.
       - CSV diff: >= 80% hit on pages with CSV coverage.
       - Unknown strain codes: review with farmer (could indicate new strain to add to memory).
    8. Inspect summaries.log: 20 pages worth, every draft has ok=true OR a documented reason.
    9. Inspect responses.jsonl: total_cost_usd reported in receipt aggregate matches sum of cost_estimate_usd per line.
    10. Send receipt.md to farmer for review.
    11. Author 54-CYCLE-2-RECEIPT.md per Task 2 below.

    ## Phase 55 unlock gate

    Cycle 2 sign-off + receipt's "Phase 55 unlock: YES" line is what unblocks the full-corpus backfill (Phase 55).
  </action>
  <verify>
    <automated>test -f .planning/phases/54-backfill-harness-dev-farmos-smoke-20-pages/54-CYCLE-2-RUNBOOK.md && grep -c 'Phase 55 unlock gate' .planning/phases/54-backfill-harness-dev-farmos-smoke-20-pages/54-CYCLE-2-RUNBOOK.md</automated>
  </verify>
  <done>RUNBOOK exists with the Cycle-1-sign-off prereq + 20-page tiered selection + Phase 55 unlock gate.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Operator executes Cycle 2 + farmer attests + Phase 55 unlock decision</name>
  <what-built>Operator runs the harness with --cycle=2 --limit=20 per the RUNBOOK. Farmer reviews receipt.md.</what-built>
  <how-to-verify>
    1. Verify Cycle 1 sign-off prereq.
    2. Follow 54-CYCLE-2-RUNBOOK.md steps 1-10.
    3. Author `.planning/phases/54-backfill-harness-dev-farmos-smoke-20-pages/54-CYCLE-2-RECEIPT.md`:

    ```
    # Cycle 2 Receipt (BACK-08 part 3)

    - run_id: <runId from runDir>
    - run_dir: .planning/backfill/2025-notebook/<runId>/
    - executed_at: <ISO>
    - operator: <name>
    - pages_processed: 20 (Tier A: 8 fixtures + Tier B: 12 fresh)
    - tier_b_pages: [list of IMG_NNNN]
    - drafts: <n>
    - assets_created: <n>
    - assets_reused: <n>  <-- should be HIGH for Tier A
    - duplicate_asset_count: 0  <-- MUST be 0
    - upsert_stability: all stable / N unstable
    - total_cost_usd: <n>
    - receipt_path: .planning/backfill/2025-notebook/<runId>/receipt.md

    ## Tier A Phase 51 contract validation

    For the 8 fixture pages (already in dev farmOS from Cycle 1):
    - assets_created on Tier A: <n>  (expected: near-zero)
    - assets_reused on Tier A:   <n>  (expected: ~= total Tier A entry count)
    - If assets_created > 0 on Tier A, document each new-creation event with rationale (legitimate new block? upsert bug?).

    ## Farmer Review (Santi)

    - reviewed_at: <ISO>
    - verdict: SIGN-OFF | FINDINGS-FILED
    - findings: [list any filed todos or "none"]

    ## Phase 55 unlock: YES | NO

    Rationale: <one sentence>
    ```

    Phase 55 unlocks ONLY when verdict == SIGN-OFF AND Phase 55 unlock == YES.
  </how-to-verify>
  <resume-signal>Reply "Cycle 2 signed off, Phase 55 unlocked" with the path to 54-CYCLE-2-RECEIPT.md, OR "Cycle 2 findings: <description>" to block Phase 55.</resume-signal>
</task>

</tasks>

<verification>
- 54-CYCLE-1-RECEIPT.md verdict SIGN-OFF (Cycle 2 prereq) — verified before Task 2 runs.
- 54-CYCLE-2-RUNBOOK.md exists with all 11 steps + Phase 55 unlock gate.
- 54-CYCLE-2-RECEIPT.md exists with farmer verdict + Phase 55 unlock decision.
- Underlying `<runDir>/receipt.md` shows duplicate_asset_count == 0, Tier A assets_created near-zero, upsert_stability all stable.
</verification>

<success_criteria>
- 20-page backfill executed end-to-end on dev farmOS.
- Phase 51 contract validated under the cross-cycle stress (Tier A re-run pattern).
- Farmer attested Phase 55 unlock decision (YES or NO with rationale).
- Phase 54 closes.
</success_criteria>

<output>
Create `.planning/phases/54-backfill-harness-dev-farmos-smoke-20-pages/54-06-SUMMARY.md` when done. Reference 54-CYCLE-2-RECEIPT.md and note the Phase 55 unlock status in the SUMMARY frontmatter `provides` field (e.g. `provides: [phase-55-unlocked]` or `provides: [phase-55-blocked-on-<finding>]`).
</output>
