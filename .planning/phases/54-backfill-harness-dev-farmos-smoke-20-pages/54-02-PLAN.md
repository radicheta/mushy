---
phase: 54-backfill-harness-dev-farmos-smoke-20-pages
plan: 02
type: execute
wave: 2
depends_on: [54-01]
files_modified:
  - src/agents/alerter/scripts/backfill-notebook.js
  - src/agents/alerter/scripts/backfill-notebook.test.js
  - src/agents/alerter/src/extraction/extraction-db.js
autonomous: true
requirements: [BACK-06]
tags: [v1.11, backfill, auto-confirm, santi-only, back-06]

must_haves:
  truths:
    - "When --bulk-backfill --farmer=santi is set, every draft created by the pipeline is auto-flipped to status='confirmed_by_farmer' (or the alerter's canonical confirmed state) before the dispatch loop moves to the next page."
    - "Each auto-confirmed draft produces one human-readable summary line written to <runDir>/summaries.log (NOT sent over Signal)."
    - "Without --bulk-backfill, the harness leaves drafts in their normal pending/awaiting_farmer status (no short-circuit applied)."
    - "The auto-confirm step is hard-gated to farmer==='santi'; Task 1's gate plus an in-loop assertion both block any other farmer."
    - "Summaries.log carries: page_path, captureId, draftId, log_type, asset/log count summary, ok/reason."
  artifacts:
    - path: src/agents/alerter/scripts/backfill-notebook.js
      provides: "Auto-confirm short-circuit + summary line emitter"
      min_lines: 280
    - path: src/agents/alerter/scripts/backfill-notebook.test.js
      provides: "Tests for auto-confirm gating + summary line format"
      min_lines: 140
  key_links:
    - from: src/agents/alerter/scripts/backfill-notebook.js
      to: src/agents/alerter/src/extraction/extraction-db.js
      via: "loadDraftsForCapture(pool, captureId) + flipDraftToConfirmed(pool, draftId)"
      pattern: "extraction-db|signal_draft"
    - from: src/agents/alerter/scripts/backfill-notebook.js
      to: src/agents/alerter/src/farmos/commits/commit-router.js
      via: "commit(client, draft) called per auto-confirmed draft"
      pattern: "commit-router|commit\\("
---

<objective>
Layer the `--bulk-backfill --farmer=santi` short-circuit onto the harness from 54-01: after each page's pipeline dispatch yields N drafts, the harness reads those drafts out of `signal_draft`, flips their status to `confirmed_by_farmer`, dispatches each through the farmOS commit-router against dev farmOS, and emits an audit-line per draft to `<runDir>/summaries.log`. Hard santi-only gate is enforced at both CLI parse (54-01) and inside the loop (defense-in-depth per T-54-02).

Purpose: Closes BACK-06. Honors `[[feedback_hard_rules_relaxed_when_farmer_is_santi]]` (Santi is the only farmer for which the explicit-YES gate may be short-circuited) AND `[[feedback_no_silent_failure_after_farmer_confirm]]` (summary line IS the audit; no silent writes).
Output: A backfill run produces real dev-farmOS asset+log creation per page; summaries.log lists every draft with its outcome.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/54-backfill-harness-dev-farmos-smoke-20-pages/54-CONTEXT.md
@src/agents/alerter/src/extraction/extraction-db.js
@src/agents/alerter/src/farmos/commits/commit-router.js
@src/agents/alerter/scripts/live-fire-52.js

<interfaces>
From src/agents/alerter/src/extraction/extraction-db.js:
- `updateDraftStatus(pool, id, newStatus, extras)` — whitelisted extras keys. Use to flip status.
- `getDraftById(pool, id)` — returns the row.
- Status values in DRAFT_STATUS enum (require from src/extraction/state-machine.js). Confirmed terminal status used by the live confirm flow is the canonical target.

From src/agents/alerter/src/farmos/commits/commit-router.js:
- `commit(client, draft, ctx)` returns `{ ok, asset_ids[], log_ids[], file_ids[], http_status, latency_ms, reason }`.
- `draft` shape: `{ id, log_type, draft_json, ... }` — match what Phase 39/45 ack-flow passes.

From src/agents/alerter/src/farmos/client.js:
- `createFarmosClient({ farmosUrl, username, password, logger })` returns the auth'd client.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: getDraftsForCapture helper + in-loop santi assertion</name>
  <files>src/agents/alerter/src/extraction/extraction-db.js, src/agents/alerter/scripts/backfill-notebook.js, src/agents/alerter/scripts/backfill-notebook.test.js</files>
  <behavior>
    - Add `getDraftsForCapture(pool, captureId)` to extraction-db.js returning all signal_draft rows whose source_capture_ids array contains captureId, ordered by created_at ASC.
    - Test (extraction-db): given 3 drafts with source_capture_ids @> ARRAY[captureId] and 2 unrelated drafts, helper returns exactly the 3.
    - In backfill-notebook.js add `assertSantiInLoop(opts)` called inside the dispatch loop body BEFORE any short-circuit action; throws if opts.farmer !== 'santi'. Defense-in-depth per T-54-02.
    - Test: monkey-patching opts.farmer between Task 1 (54-01) parse and the loop (simulating attacker mutation) still trips assertSantiInLoop and aborts the run with exit 4.
  </behavior>
  <action>
    Extend extraction-db.js with the new query. Use the existing UPDATE_EXTRAS_WHITELIST pattern as a stylistic reference. The new helper does not need whitelisting (read-only).

    In backfill-notebook.js add the in-loop guard and refactor the dispatch loop to: dispatch via pipeline.enqueue → fetch drafts via getDraftsForCapture → store on the per-page runSummary entry as `draftIds`. Auto-confirm itself lands in Task 2.
  </action>
  <verify>
    <automated>cd src/agents/alerter && npx jest src/extraction/extraction-db.test.js scripts/backfill-notebook.test.js --runInBand</automated>
  </verify>
  <done>extraction-db.test.js + backfill-notebook.test.js both green; in-loop santi assertion test passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: flipDraftToConfirmed + commit-router dispatch + summaries.log writer</name>
  <files>src/agents/alerter/scripts/backfill-notebook.js, src/agents/alerter/scripts/backfill-notebook.test.js</files>
  <behavior>
    - `flipDraftToConfirmed(pool, draftId)` calls updateDraftStatus(pool, draftId, 'confirmed_by_farmer', {needs_review_reason:'bulk_backfill_santi'}). Use the canonical confirmed-state constant from DRAFT_STATUS if it exists; fall back to the literal `'confirmed_by_farmer'` only if no constant.
    - For each draft from getDraftsForCapture: if --bulk-backfill, flipDraftToConfirmed → commitRouter.commit(client, draft, {auditLogger:noopAuditLogger}) → append a summary line to <runDir>/summaries.log.
    - Summary line format (one per draft, ASCII-only per `[[feedback_no_em_dashes_in_artifacts]]`):
      `<ISO> page=<basename> capture=<captureId> draft=<draftId> log_type=<type> ok=<true|false> assets=<n> logs=<n> [reason=<reason if !ok>]`
    - runDir = `.planning/backfill/2025-notebook/<runId>/`. Created with fs.mkdirSync({recursive:true}) at run start.
    - On --dry-run, do NOT flip or commit; still emit summary lines with ok='dry-run' and asset/log counts both 0.
    - Test: mocked pipeline emits 3 drafts; mocked commit-router returns ok with 2 assets/1 log; summaries.log has 3 well-formed lines.
    - Test: --dry-run produces 3 lines with ok='dry-run' and zero side effects (no commit-router calls, no flipDraftToConfirmed calls).
    - Test: without --bulk-backfill, drafts stay in pending status; commit-router NOT called.
    - Test: summary line contains no em-dashes; ASCII-only.
  </behavior>
  <action>
    Wire the createFarmosClient call once at run start (after prod-guard); pass through the loop. On commit-router failure (ok:false), summary line still emits with ok=false + reason; do NOT abort the run (continue to next draft) — per `[[feedback_no_silent_failure_after_farmer_confirm]]` the audit IS the receipt; aborting would lose visibility on subsequent pages.

    On `flipDraftToConfirmed` DB error, log warn and SKIP the commit-router call for that draft (summary line ok=false reason='draft_flip_failed: <msg>'). Do not block other drafts.

    Tests inject a mocked commit-router via require-cache or by passing it through createBackfillContext (extend the seam from 54-01). Use a tmp dir for runDir in tests.

    The canonical confirmed-state constant: check src/extraction/state-machine.js DRAFT_STATUS during execution; if there's no terminal-confirmed value, introduce one only if absolutely required (preferable: reuse whatever Phase 39/45 commit-watchdog reads). Document the chosen constant in the SUMMARY.
  </action>
  <verify>
    <automated>cd src/agents/alerter && npx jest scripts/backfill-notebook.test.js --runInBand</automated>
  </verify>
  <done>Auto-confirm short-circuit + commit dispatch + summaries.log all hermetically verified; --dry-run still safe.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Plan 01 → Plan 02 | Plan 02 trusts Plan 01's farmer-gate; re-asserts in-loop. |
| harness → signal_draft | Privileged status flip ('confirmed_by_farmer') bypassing Phase 39 confirm flow. |
| harness → dev farmOS commit-router | Real writes (asset / log POST) to dev farmOS. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-54-05 | Elevation of Privilege | Auto-confirm leaks to non-santi farmer via mid-loop mutation | mitigate | assertSantiInLoop runs every iteration (Task 1). Refusing on bulk_backfill_santi-only test fixture. |
| T-54-06 | Repudiation | Auto-confirmed write has no audit trail | mitigate | summaries.log line per draft; `needs_review_reason='bulk_backfill_santi'` stamped on the signal_draft row itself. |
| T-54-07 | Tampering | commit-router writes wrong farmOS instance | mitigate | Prod-guard from 54-01 already blocks; the client is created once at run start, no per-loop URL switch. |
| T-54-08 | Denial of Service | One bad page aborts the whole run | mitigate | Per-draft try/catch; one failure logs reason and continues. |
</threat_model>

<verification>
- `cd src/agents/alerter && npx jest scripts/backfill-notebook.test.js src/extraction/extraction-db.test.js --runInBand` — green.
- `cd src/agents/alerter && npm test` — no regressions vs Plan 01 baseline.
- Manual hermetic: run script with mocked commit-router; summaries.log has expected lines.
</verification>

<success_criteria>
- Bulk-backfill drafts auto-flip and commit on dev farmOS.
- Every draft has a summary line in summaries.log (audit, not silent).
- 54-03 can layer paid-LLM persistence; 54-04 can read summaries.log + runSummary[] to build the receipt.
</success_criteria>

<output>
Create `.planning/phases/54-backfill-harness-dev-farmos-smoke-20-pages/54-02-SUMMARY.md` when done.
</output>
