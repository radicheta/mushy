---
phase: 54-backfill-harness-dev-farmos-smoke-20-pages
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/agents/alerter/scripts/backfill-notebook.js
  - src/agents/alerter/scripts/backfill-notebook.test.js
autonomous: true
requirements: [BACK-05]
tags: [v1.11, backfill, harness, back-05]

must_haves:
  truths:
    - "Running `node scripts/backfill-notebook.js --help` prints the documented CLI surface (--bulk-backfill, --farmer, --cycle, --limit, --dry-run, --resume-from, --run-id)."
    - "Given --limit=N, the harness iterates exactly N JPEGs from /mnt/slime-kingdom/shared/mushdatadump/jpeg/ restricted to the IMG_3775..IMG_3861 range."
    - "For each page, the harness inserts a synthetic signal_capture row with corpus_context={default_year:2025, source:'paper_log'} and dispatches via the alerter extraction pipeline."
    - "The script aborts with exit 3 if FARMOS_URL contains ':8082' or 'prod' (dev-only prod-guard, mirrors live-fire-52.js)."
    - "The script aborts with exit 4 if --bulk-backfill is set without --farmer=santi (santi-only hard gate per D-06)."
  artifacts:
    - path: src/agents/alerter/scripts/backfill-notebook.js
      provides: "Notebook backfill harness CLI"
      min_lines: 200
    - path: src/agents/alerter/scripts/backfill-notebook.test.js
      provides: "Hermetic unit tests for CLI parsing, prod-guard, farmer-gate, page-range filter, synthetic capture shape"
      min_lines: 80
  key_links:
    - from: src/agents/alerter/scripts/backfill-notebook.js
      to: src/agents/alerter/src/extraction/pipeline.js
      via: "createExtractionPipeline().enqueue({sender, captureId, text, attachmentPaths, transcripts, corpusContext})"
      pattern: "createExtractionPipeline|enqueue\\("
    - from: src/agents/alerter/scripts/backfill-notebook.js
      to: /mnt/slime-kingdom/shared/mushdatadump/jpeg/
      via: "fs.readdir filtered to IMG_3775..IMG_3861, sorted ascending"
      pattern: "IMG_3(7[7-9][0-9]|8[0-5][0-9]|86[0-1])"
---

<objective>
Ship the core `scripts/backfill-notebook.js` harness: a node CLI that iterates 2025-notebook JPEGs from the corrected corpus path, builds synthetic `signal_capture` rows with corpus_context pinned to year=2025 + source=paper_log, and dispatches them through the live extraction pipeline. Includes the prod-guard, santi-only gate plumbing (the short-circuit logic itself lands in 54-02), CLI parsing, and page-range filtering.

Purpose: Closes BACK-05. Foundation that 54-02 (auto-confirm), 54-03 (LLM persistence), 54-04 (receipt) layer onto.
Output: Working `--dry-run` capable harness whose hermetic tests prove CLI shape and guards; real run still no-ops at the pipeline boundary because 54-02/03/04 haven't wired auto-confirm or persistence yet.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/54-backfill-harness-dev-farmos-smoke-20-pages/54-CONTEXT.md
@src/agents/alerter/src/extraction/pipeline.js
@src/agents/alerter/scripts/live-fire-52.js

<interfaces>
<!-- Contracts the harness consumes (extracted, do NOT re-explore). -->

From src/agents/alerter/src/extraction/pipeline.js:
- `createExtractionPipeline({ pool, logger, clock, extractor, extractionDb, stateMachine, outboundDispatcher, config })` returns `{ enqueue, handleStartingSeqReply }`.
- `enqueue(captureCtx)` where captureCtx = `{ sender, captureId, text?, transcripts?, attachmentPaths?, corpusContext? }`. Returns `{ ok, reason? }`. Never throws.
- Phase 53 BACK-01 plumbing already forwards `captureCtx.corpusContext` to `extractor.extract({..., corpusContext})`.

From src/agents/alerter/src/extraction/extraction-db.js:
- `insertDraft(pool, row)` — used internally by pipeline; the harness should NOT call directly.
- The harness's only DB touch on this plan is inserting the synthetic `signal_capture` row before pipeline dispatch (so the pipeline can resolve it).

From src/agents/alerter/scripts/live-fire-52.js (prod-guard template):
- Lower-cases FARMOS_URL, refuses if endswith ':8082', contains ':8082/', or contains 'prod' → exit 3.

From /mnt/slime-kingdom/shared/mushdatadump/HANDOFF.md:
- jpeg/ holds IMG_3775.jpg..IMG_3884.jpg. Only IMG_3775..IMG_3861 have CSV ground truth in mushroom_log.csv.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CLI skeleton + prod-guard + santi-only gate + page-range filter</name>
  <files>src/agents/alerter/scripts/backfill-notebook.js, src/agents/alerter/scripts/backfill-notebook.test.js</files>
  <behavior>
    - parseArgs(argv) → { bulkBackfill, farmer, cycle, limit, dryRun, resumeFrom, runId, corpusDir, help }. Defaults: corpusDir='/mnt/slime-kingdom/shared/mushdatadump/jpeg', cycle=1, limit=5, dryRun=false.
    - assertProdGuard(farmosUrl) throws/exits 3 when URL lowercases to endswith ':8082', contains ':8082/', or contains 'prod' (mirror live-fire-52.js).
    - assertFarmerGate(opts) exits 4 when bulkBackfill && farmer !== 'santi'. Logs the specific farmer that was attempted.
    - listCorpusPages(corpusDir) returns sorted IMG_3775..IMG_3861 JPEG paths only (regex filter; warn-and-skip on IMG_3862..IMG_3884 with reason="un-transcribed gap per HANDOFF.md").
    - selectPages(allPages, { limit, resumeFrom }) slices to limit; if resumeFrom set, starts at the page whose basename === resumeFrom.
    - Test: --help exits 0 and prints usage banner with all flags.
    - Test: FARMOS_URL='http://x.y:8082' → assertProdGuard throws Error('prod-guard').
    - Test: FARMOS_URL='http://x.y:18080' → assertProdGuard passes.
    - Test: bulkBackfill=true farmer='vikki' → assertFarmerGate throws Error('santi-only').
    - Test: bulkBackfill=true farmer='santi' → assertFarmerGate passes.
    - Test: listCorpusPages over a fake dir containing IMG_3775, IMG_3860, IMG_3861, IMG_3862, IMG_3884 returns only the first three.
    - Test: selectPages({limit:3}) over 8 returns first 3; with resumeFrom='IMG_3778.jpg' returns pages starting at that one.
  </behavior>
  <action>
    Author the script with a `main()` async fn that: (1) parseArgs from process.argv.slice(2); (2) if --help, print usage and exit 0; (3) assertProdGuard(process.env.FARMOS_URL); (4) assertFarmerGate(opts); (5) compute runId default = new Date().toISOString().replace(/[:.]/g,'-') if --run-id absent; (6) call listCorpusPages + selectPages; (7) log the selected pages and exit 0 if --dry-run is true. The pipeline dispatch loop body is a TODO marker for Task 2.

    Export the parse/assert/list/select helpers for tests. Wrap main() so direct `node backfill-notebook.js` triggers it; require-from-test does not.

    Tests use jest. Mock fs.readdirSync for listCorpusPages tests. Do NOT touch the real pipeline or DB in this task.

    Use exit codes: 0 ok, 1 unhandled exception, 3 prod-guard, 4 farmer-gate, 5 missing-env (FARMOS_URL/USERNAME/PASSWORD).

    Per D-09 corpus path is /mnt/slime-kingdom/shared/mushdatadump/jpeg/ (NOT mushdatadump-prod). Per BACK-05 synthetic capture corpus_context = {default_year:2025, source:'paper_log'} (wiring lands in Task 2).
  </action>
  <verify>
    <automated>cd src/agents/alerter && npx jest scripts/backfill-notebook.test.js --runInBand</automated>
  </verify>
  <done>All Task 1 behaviors pass; --help and --dry-run runs exit 0 without touching the pipeline.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Synthetic signal_capture row + pipeline.enqueue dispatch loop</name>
  <files>src/agents/alerter/scripts/backfill-notebook.js, src/agents/alerter/scripts/backfill-notebook.test.js</files>
  <behavior>
    - For each selected page, harness:
      (a) generates a deterministic captureId (e.g. `backfill-${runId}-${path.basename(page,'.jpg')}`).
      (b) INSERTs a synthetic signal_capture row with the captureId, sender_e164=process.env.BACKFILL_SENDER_E164 (default '+59891840205'), captured_at=now, body=null, attachment_paths=[absolute page path], corpus_context={default_year:2025, source:'paper_log'}.
      (c) calls pipeline.enqueue({ sender, captureId, attachmentPaths:[page], corpusContext:{default_year:2025, source:'paper_log'} }).
      (d) collects per-page result {captureId, pagePath, ok, reason, draftIds:[]} into a runSummary[].
    - On --dry-run: skip steps (b) and (c); still produce runSummary with ok:'dry-run'.
    - Test (hermetic): with a mocked pipeline whose enqueue returns {ok:true} and a mocked pg pool whose query records calls, processing 3 fake pages produces 3 INSERT INTO signal_capture calls each carrying the correct corpus_context JSON.
    - Test: --dry-run skips DB and pipeline entirely, runSummary length === selected pages.
    - Test: corpusContext is a literal {default_year:2025, source:'paper_log'} object on every enqueue call (not undefined, not null).
  </behavior>
  <action>
    Add `buildSyntheticCapture({ page, runId, sender })` returning the row shape.

    Add `insertSyntheticCapture(pool, row)` doing the SQL INSERT. Use the existing signal_capture column set; corpus_context is the JSONB column added in Phase 53 Plan 01. If unsure of full column list, run `grep -n "signal_capture" src/agents/alerter/src/**/*.js | head` during execution and mirror the canonical INSERT shape; minimally insert (id, sender_e164, captured_at, body, attachment_paths, corpus_context).

    Add the dispatch loop in `main()` after Task 1's selectPages. The loop is sequential (not parallel) — backfill must be observable and rate-controllable per locked decisions; no Promise.all batching.

    Wire pg.Pool with DATABASE_URL from env. Inject the pool into the pipeline factory using the same options the alerter's bootstrap uses (look at src/index.js for the canonical wiring). If the bootstrap is intricate, expose a `createBackfillContext({ pool, logger })` helper that builds the minimal pipeline factory args. Tests inject mocks via this seam.

    Auto-confirm short-circuit, paid-LLM persistence, and receipt are intentionally OUT of scope for this plan — they land in 54-02 / 54-03 / 54-04 respectively. Per-page result rows from this plan feed the receipt builder in 54-04.
  </action>
  <verify>
    <automated>cd src/agents/alerter && npx jest scripts/backfill-notebook.test.js --runInBand</automated>
  </verify>
  <done>Synthetic capture insert + pipeline dispatch loop hermetically verified; --dry-run runs end-to-end against the 8 already-fixtured pages without touching DB or pipeline.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator-CLI → harness | Operator-supplied flags / env vars (FARMOS_URL, FARMOS_USERNAME, DATABASE_URL, BACKFILL_SENDER_E164). |
| harness → dev farmOS | Synthetic captures land on dev farmOS via the pipeline; prod-guard is the only barrier against accidentally hitting :8082. |
| harness → corpus filesystem | Read-only access to /mnt/slime-kingdom/shared/mushdatadump/jpeg/. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-54-01 | Tampering | FARMOS_URL env mis-set to prod | mitigate | assertProdGuard exits 3 on `:8082` or `prod` substrings; test-covered. |
| T-54-02 | Elevation of Privilege | `--bulk-backfill --farmer=vikki` bypasses Vikki/Selina's confirm-required rule per `[[feedback_hard_rules_relaxed_when_farmer_is_santi]]` | mitigate | assertFarmerGate exits 4 on any farmer !== 'santi'; test-covered. |
| T-54-03 | Tampering | Corpus filesystem path traversal via --corpus-dir flag pointing at a non-corpus directory | accept | Operator-supplied; risk is "harmless garbage in, garbage out"; the page-range regex filter keeps non-IMG_NNNN files out of dispatch. |
| T-54-04 | Info Disclosure | Synthetic capture body/attachment_paths leak to logs in case of error | accept | Logger uses `console.warn` only; no farmer PII in 2025 notebook pages (paper-log only). |
| T-54-SC | Tampering | npm package installs | accept | No new npm packages introduced this plan; harness uses only existing alerter deps (fs/path/pg). |
</threat_model>

<verification>
- Hermetic: `cd src/agents/alerter && npx jest scripts/backfill-notebook.test.js --runInBand` — all tests pass.
- Full suite regression: `cd src/agents/alerter && npm test` — no new failures vs the 1151-pass baseline.
- Dry-run smoke: `cd src/agents/alerter && FARMOS_URL=http://10.68.155.50:18080 FARMOS_USERNAME=x FARMOS_PASSWORD=x node scripts/backfill-notebook.js --bulk-backfill --farmer=santi --cycle=1 --limit=5 --dry-run` — exits 0, prints 5 selected pages from IMG_3775..IMG_3861 range.
- Prod-guard smoke: same as above but FARMOS_URL=http://10.68.155.50:8082 → exits 3.
- Farmer-gate smoke: same but --farmer=vikki → exits 4.
</verification>

<success_criteria>
- `scripts/backfill-notebook.js` exists with the full CLI surface documented in --help.
- All hermetic tests green.
- Dry-run end-to-end works against the real corpus filesystem.
- 54-02 can layer auto-confirm short-circuit on top without restructuring the dispatch loop.
</success_criteria>

<output>
Create `.planning/phases/54-backfill-harness-dev-farmos-smoke-20-pages/54-01-SUMMARY.md` when done.
</output>
