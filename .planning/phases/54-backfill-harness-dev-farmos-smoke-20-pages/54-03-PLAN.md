---
phase: 54-backfill-harness-dev-farmos-smoke-20-pages
plan: 03
type: execute
wave: 3
depends_on: [54-01, 54-02]
files_modified:
  - src/agents/alerter/src/extraction/extractor.js
  - src/agents/alerter/src/extraction/extractor.test.js
  - src/agents/alerter/scripts/backfill-notebook.js
autonomous: true
requirements: [BACK-07]
tags: [v1.11, backfill, paid-llm, persistence, back-07]

must_haves:
  truths:
    - "Every paid Anthropic call made during a backfill run appends one JSONL line to <runDir>/responses.jsonl."
    - "Lines never overwrite prior runs (per-run-id directory; append-only file)."
    - "Each line carries: ISO timestamp, captureId, model, request_hash (sha256 of normalized request body), latency_ms, input_tokens, output_tokens, cost_estimate_usd, raw response object."
    - "The persistence sink is wired via dependency injection, not a hard import — leaves non-backfill code paths unaffected."
    - "When ANTHROPIC_API_KEY is absent OR --dry-run is set, no LLM calls happen and no JSONL is written."
  artifacts:
    - path: src/agents/alerter/scripts/backfill-notebook.js
      provides: "responses.jsonl writer + extractor instrumentation"
      min_lines: 320
  key_links:
    - from: src/agents/alerter/scripts/backfill-notebook.js
      to: src/agents/alerter/src/extraction/extractor.js
      via: "createExtractor({ onLlmCall })" or post-call observer hook
      pattern: "onLlmCall|observer|createExtractor"
---

<objective>
Wire append-only JSONL persistence of every paid LLM call into `<runDir>/responses.jsonl`. Honors `[[feedback_persist_paid_results_default]]` and `[[feedback_never_overwrite_paid_live_api_results]]`. Persistence is wired as a sidecar observer on the extractor: no behavior change for live captures, only the backfill harness installs the observer.

Purpose: Closes BACK-07. Provides the evidence trail farmer + operator review alongside the receipt (54-04) and summaries.log (54-02).
Output: After Cycle 1, `.planning/backfill/2025-notebook/<runId>/responses.jsonl` contains one well-formed line per Anthropic call.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/54-backfill-harness-dev-farmos-smoke-20-pages/54-CONTEXT.md
@src/agents/alerter/src/extraction/extractor.js

<interfaces>
From src/agents/alerter/src/extraction/extractor.js:
- Likely shape: `createExtractor({ client, model, ... })` returning `{ extract({captures, inFlightDraft, corpusContext}) }`. Verify during execution.
- Anthropic SDK call site emits `{ id, content, usage:{input_tokens, output_tokens}, model }`.
- Reference: 53-04 SUMMARY documents that EVAL_RUN_LIVE=1 already persists raw responses to `test/eval/ingestion/results/notebook-2025/<page-id>-<ISO>.json`. Mirror that pattern but in JSONL form for the harness.

From memory `[[feedback_persist_paid_results_default]]`:
- Per-call unique paths + append-only JSONL — never overwrite.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extractor observer hook (opt-in)</name>
  <files>src/agents/alerter/src/extraction/extractor.js, src/agents/alerter/src/extraction/extractor.test.js</files>
  <behavior>
    - createExtractor accepts an optional `onLlmCall(observation)` callback in its options.
    - observation = { ts, model, input_tokens, output_tokens, latency_ms, raw_response, request_hash, captureId? }.
    - request_hash = sha256(JSON.stringify({model, messages, tools, system})).
    - captureId is plumbed in via extract({captures}) — first captures[0].captureId.
    - When onLlmCall is absent (existing call sites), zero behavior change.
    - When onLlmCall throws, extractor logs warn and continues (observer must not break extraction).
    - Test: with a recording onLlmCall and a mocked Anthropic client, one extract() call produces one observation with all fields populated.
    - Test: onLlmCall throwing does not propagate; extract returns its normal result.
    - Test: without onLlmCall, existing extractor.test.js cases still pass (regression).
  </behavior>
  <action>
    Locate the actual Anthropic call site in extractor.js (likely a `await client.messages.create(...)`). Wrap it with a try/finally that captures latency_ms via Date.now() and emits the observation after the call resolves. Pass captureId through extract → call site.

    request_hash: use node's `crypto.createHash('sha256').update(JSON.stringify(...)).digest('hex').slice(0,16)`.

    Do NOT add observer to live capture call sites; only the harness wires it. Existing extractor consumers (pipeline.js, eval test harness) keep their current factory args.
  </action>
  <verify>
    <automated>cd src/agents/alerter && npx jest src/extraction/extractor.test.js --runInBand</automated>
  </verify>
  <done>Extractor instrumented with optional observer; backward-compatible.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: responses.jsonl writer wired in backfill harness</name>
  <files>src/agents/alerter/scripts/backfill-notebook.js, src/agents/alerter/scripts/backfill-notebook.test.js</files>
  <behavior>
    - On run start (after Task 1 from 54-01 setup), open `<runDir>/responses.jsonl` for append (`fs.openSync(path, 'a')`).
    - Build an `onLlmCall` callback that estimates cost_estimate_usd from input/output tokens using a const rate table (Sonnet 4.6 prices; document the rate in code comment with a 2026-05-24 date). Serializes the observation as one JSON line, writes via fs.writeSync(fd, line+'\n').
    - Pass `onLlmCall` to createExtractor when the harness builds its pipeline context.
    - On --dry-run: do NOT open the file; do not pass onLlmCall (extractor never gets a real client either).
    - On run end (success or failure), fs.closeSync(fd).
    - Test: hermetic run with a mocked extractor that fires onLlmCall twice produces exactly 2 lines in responses.jsonl, each a valid JSON object with required keys.
    - Test: --dry-run does not create responses.jsonl.
    - Test: re-running with the same --run-id REFUSES to start (exit 6 'run_id_exists') — prevents the rare manual collision that would clobber prior paid results per `[[feedback_never_overwrite_paid_live_api_results]]`.
  </behavior>
  <action>
    Add a `runIdExistsGuard(runDir)` helper checked immediately after runDir computation; exits 6 if the directory already contains responses.jsonl (allows recreating the dir if empty for retries).

    Cost rate constants (document the cited date 2026-05-24): SONNET_INPUT_USD_PER_MTOK = 3.00, SONNET_OUTPUT_USD_PER_MTOK = 15.00. Compute cost as `(in_tok/1e6)*3 + (out_tok/1e6)*15`. If model string contains 'haiku', use 0.80 / 4.00. Defaults are best-effort; rate-drift correction is a deferred concern.

    Per-line JSONL shape:
    ```
    {"ts":"2026-05-24T19-30-01Z","captureId":"backfill-...-IMG_3775","model":"claude-sonnet-4-6","input_tokens":1234,"output_tokens":456,"latency_ms":3210,"cost_estimate_usd":0.0102,"request_hash":"a1b2c3d4...","raw_response":{...}}
    ```

    No code blocks IN the JSONL file; the shape above is a reference for the test assertions and SUMMARY.md.
  </action>
  <verify>
    <automated>cd src/agents/alerter && npx jest scripts/backfill-notebook.test.js --runInBand</automated>
  </verify>
  <done>responses.jsonl writer hermetically verified; --dry-run skips it; run-id collision exits 6.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| extractor → observer | Observer is invoked with raw_response; observer must not mutate or block. |
| harness → filesystem | Append-only JSONL writes to `.planning/backfill/2025-notebook/<runId>/`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-54-09 | Repudiation | Lost LLM call evidence on crash | mitigate | fs.writeSync (synchronous) on each call; not buffered. fd opened with 'a' append flag. |
| T-54-10 | Tampering | Re-using a --run-id clobbers prior responses.jsonl | mitigate | runIdExistsGuard exits 6 (Task 2). Honors `[[feedback_never_overwrite_paid_live_api_results]]`. |
| T-54-11 | Info Disclosure | raw_response contains farmer-PII | accept | 2025 notebook paper-log is non-PII; backfill is santi-only; runDir is gitignored under .planning/backfill/ (verify path during execution). |
| T-54-12 | Denial of Service | Observer throw breaks extraction | mitigate | try/catch in extractor (Task 1); observer errors logged warn, never propagated. |
</threat_model>

<verification>
- `cd src/agents/alerter && npx jest src/extraction/extractor.test.js scripts/backfill-notebook.test.js --runInBand` — green.
- Full suite: `npm test` — no regressions.
- Confirm `.planning/backfill/` is in .gitignore (or add it as a precondition step in the SUMMARY).
</verification>

<success_criteria>
- Every paid LLM call during a backfill run leaves a verifiable JSONL receipt.
- Live capture paths (not backfill) are unchanged.
- Run-id collisions cannot clobber prior paid results.
</success_criteria>

<output>
Create `.planning/phases/54-backfill-harness-dev-farmos-smoke-20-pages/54-03-SUMMARY.md` when done.
</output>
