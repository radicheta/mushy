---
phase: 54-backfill-harness-dev-farmos-smoke-20-pages
plan: 04
type: execute
wave: 3
depends_on: [54-02, 54-03]
files_modified:
  - src/agents/alerter/scripts/backfill-notebook.js
  - src/agents/alerter/scripts/backfill-notebook.test.js
  - src/agents/alerter/scripts/build-backfill-receipt.js
  - src/agents/alerter/scripts/build-backfill-receipt.test.js
autonomous: true
requirements: [BACK-08]
tags: [v1.11, backfill, receipt, back-08-part1]

must_haves:
  truths:
    - "After every run (regardless of exit code), `<runDir>/receipt.md` exists summarizing per-page outcomes."
    - "receipt.md per-page rows include: source image filename, extracted event_date(s), draft count, asset UUIDs created/reused, seeding-log UUIDs created."
    - "receipt.md per-page section includes a CSV-diff block (hit/miss/extra row counts) when the page has a row in mushroom_log.csv ground truth (IMG_3775..IMG_3861)."
    - "receipt.md aggregate section reports: total drafts, total assets, total reused-assets, duplicate-asset count (MUST be 0), per-strain counts cross-checked against `mossrock_active_strain_codes` memory."
    - "receipt.md aggregate section reports intra-cycle upsert stability: count of parent block codes (source field in mushroom_log.csv) that appeared on >1 row; for each, asserts all references resolved to the SAME UUID (Phase 51 contract validation)."
    - "receipt.md is ASCII-only (no em-dashes per `[[feedback_no_em_dashes_in_artifacts]]`)."
  artifacts:
    - path: src/agents/alerter/scripts/build-backfill-receipt.js
      provides: "Receipt builder fed by runSummary[] + summaries.log + responses.jsonl + mushroom_log.csv"
      min_lines: 220
    - path: src/agents/alerter/scripts/build-backfill-receipt.test.js
      provides: "Hermetic tests covering per-page rows, aggregate section, diff calc, upsert-stability check"
      min_lines: 140
  key_links:
    - from: src/agents/alerter/scripts/backfill-notebook.js
      to: src/agents/alerter/scripts/build-backfill-receipt.js
      via: "buildReceipt({ runDir, runSummary, csvPath }) called in finally{} at run end"
      pattern: "buildReceipt|build-backfill-receipt"
    - from: src/agents/alerter/scripts/build-backfill-receipt.js
      to: /mnt/slime-kingdom/shared/mushdatadump/mushroom_log.csv
      via: "csv-parse readFile; group by page_date / image_id correspondence"
      pattern: "mushroom_log\\.csv"
---

<objective>
Build the receipt artifact that Cycle 1 farmer review depends on. Receipt is produced unconditionally at run-end (success or failure) from the in-memory runSummary[] + the on-disk summaries.log and responses.jsonl + the corpus CSV ground truth. It is the SINGLE document farmer reviews — dev-farmOS UI is too noisy for a 5-page check per locked decision D-08.

Purpose: Closes BACK-08 part 1 (receipt artifact). Cycle execution + farmer sign-off land in 54-05/06.
Output: `.planning/backfill/2025-notebook/<runId>/receipt.md` with per-page detail + aggregate + Phase 51 upsert-stability validation.

**BACK-08 stub-enrichment resolution:** The original CONTEXT.md flagged that the 4 May-22 ancestor stubs (`260304_SHI_5` etc.) are 2026-dated codes that cannot appear in 2025 notebook pages — they postdate the notebook. **Resolved as N/A** for the literal stub-enrichment check. **Substituted Phase-51 contract validation**: intra-cycle upsert stability on the 2025 corpus itself. The notebook has 829 rows where `source` is frequently re-referenced across pages (e.g. one mother-block spawning multiple children weeks apart). Each repeat reference must upsert to the SAME UUID. This validates the exact same Phase 51 invariant the original check targeted, on data that actually exists.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/54-backfill-harness-dev-farmos-smoke-20-pages/54-CONTEXT.md
@src/agents/alerter/scripts/backfill-notebook.js

<interfaces>
runSummary[] shape (from Plans 01+02):
- `{ page: 'IMG_3775.jpg', captureId, ok, reason?, draftIds: [uuid,...], commits: [{ draftId, log_type, ok, asset_ids:[uuid,...], log_ids:[uuid,...], reason? }, ...] }`

summaries.log line shape (Plan 02): see 54-02 spec.

responses.jsonl line shape (Plan 03): see 54-03 spec. (Receipt uses cost_estimate_usd aggregate only — sum + count.)

mushroom_log.csv (HANDOFF.md):
- Columns: `page_date,entry_num,strain,source,notes`. 829 rows Feb–Dec 2025.
- One IMG_NNNN ↔ N rows (typically 4–24). Image→date mapping is established per fixture in test/eval/ingestion/fixtures/notebook-2025/<page-id>/manifest.json.

Active strain codes (memory `[[project_mossrock_active_strain_codes]]`):
- SHI SH2 KOY MAI MALI KOS DT CAS CAZ WIN ALM MOR BP LIMA
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Per-page rendering + CSV diff calculator</name>
  <files>src/agents/alerter/scripts/build-backfill-receipt.js, src/agents/alerter/scripts/build-backfill-receipt.test.js</files>
  <behavior>
    - `renderPageSection(pageEntry, csvRowsForPage)` returns a markdown block:
      ```
      ### IMG_3775.jpg (2025-02-01)
      - drafts: 1 (uuid abc12345...)
      - commits: 1 ok, 0 fail
      - assets created: 24
      - assets reused: 0
      - logs created: 24
      - CSV diff: 24 hit / 0 miss / 0 extra
      ```
    - `computeCsvDiff({ csvRowsForPage, committedAssets })` returns {hit, miss, extra, missing_strain_codes, extra_strain_codes}. Match key = strain code (case-insensitive). Hit = strain present in both; Miss = in CSV not in commits; Extra = in commits not in CSV.
    - `loadCsvForPage(csvPath, pageDate)` returns CSV rows whose page_date matches.
    - Image→pageDate mapping: prefer fixture manifest.json if present at test/eval/ingestion/fixtures/notebook-2025/<page-id>/manifest.json; fallback to parsing pageDate from captureId or commit event_date.
    - Test: renderPageSection with 3 hits + 1 miss + 0 extras emits the block above with diff `3 hit / 1 miss / 0 extra` and includes the missing strain code in a sub-bullet.
    - Test: pages without CSV coverage (e.g. operator hand-curated extras) render `CSV diff: N/A (no ground truth)`.
  </behavior>
  <action>
    Use a minimal CSV parser (split on `,` is unsafe for notes fields; if csv-parse is in alerter deps use it; otherwise add a 20-line regex-based parser that handles double-quote escapes for the notes column).

    Tests use a fixture CSV stub in /tmp; do not touch /mnt corpus during tests.
  </action>
  <verify>
    <automated>cd src/agents/alerter && npx jest scripts/build-backfill-receipt.test.js --runInBand</automated>
  </verify>
  <done>Per-page rendering + CSV diff hermetically verified.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Aggregate section + Phase 51 upsert-stability check + writer</name>
  <files>src/agents/alerter/scripts/build-backfill-receipt.js, src/agents/alerter/scripts/build-backfill-receipt.test.js, src/agents/alerter/scripts/backfill-notebook.js</files>
  <behavior>
    - `computeAggregate(runSummary, csvRows)` returns:
      `{ pages, drafts, assets_created, assets_reused, duplicate_asset_count, logs_created, per_strain:{SHI:n, KOS:m, ...}, unknown_strain_codes:[], total_cost_usd, upsert_stability:{ checked:n, stable:n, unstable:[{block_name, uuids:[]}] } }`
    - `upsert_stability` check: collect (block_name → [uuid]) pairs across all commits; for each block_name that appears with multiple UUIDs, that's an instability (Phase 51 contract violation). `checked` = count of block_names appearing >= 2 times; `stable` = those resolving to exactly one uuid; `unstable` = the rest.
    - `unknown_strain_codes` = strain codes seen in CSV/commits not in the active strain codes list (sanity prompt for the farmer; do NOT auto-fail).
    - duplicate_asset_count: count of asset UUIDs that appear in commits[].asset_ids more than once across DIFFERENT block_names. (Same block_name repeats are expected reuse, not duplicates.)
    - `buildReceipt({ runDir, runSummary, csvPath, runId, cycleNumber })` writes runDir/receipt.md = header + per-cycle metadata + per-page sections + aggregate section. Returns the receipt path.
    - Receipt header includes: runId, cycleNumber, generation timestamp, dev-farmOS URL used, total elapsed seconds.
    - Test: aggregate over a 3-page runSummary correctly tallies drafts/assets/logs and per_strain counts.
    - Test: upsert_stability with 2 commits referencing block '0218-3' both returning the same UUID → stable:1, unstable:[]; with 2 different UUIDs → stable:0, unstable:[{block_name:'0218-3', uuids:[u1,u2]}].
    - Test: duplicate_asset_count counts cross-block_name UUID collisions only.
    - Test: receipt.md output contains no em-dashes (regex assertion).
    - In backfill-notebook.js, call buildReceipt in a try/finally that runs on success AND failure paths.
  </behavior>
  <action>
    Phase 51 upsert-stability is the load-bearing check per the BACK-08 N/A resolution; explain in the receipt header that it validates the same invariant the original stub-enrichment check would have validated.

    duplicate_asset_count MUST be 0 to pass farmer review — explicit pass/fail line in the aggregate section.

    Tests use fixture data; do not invoke real backfill.
  </action>
  <verify>
    <automated>cd src/agents/alerter && npx jest scripts/build-backfill-receipt.test.js scripts/backfill-notebook.test.js --runInBand</automated>
  </verify>
  <done>Receipt builder hermetically verified; harness wires it into run-end finally; sample receipt.md generated from a fixture runSummary inspected and ASCII-clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| harness → receipt | Receipt is operator+farmer-facing evidence; bugs here corrupt farmer judgement. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-54-13 | Repudiation | Run crashes before receipt emitted | mitigate | buildReceipt called from try/finally; runs even on uncaught exceptions. |
| T-54-14 | Tampering | CSV-diff mis-classes hits/misses | mitigate | Hermetic tests with fixture CSV cover hit/miss/extra paths. |
| T-54-15 | Info Disclosure | Receipt leaks farmer PII | accept | 2025 paper-log corpus is non-PII (strain codes + dates only). |
| T-54-16 | Spoofing | Receipt claims success when commits failed | mitigate | commits[].ok rolled up explicitly; aggregate ok-count and fail-count both rendered; duplicate_asset_count==0 is a hard PASS line. |
</threat_model>

<verification>
- `cd src/agents/alerter && npx jest scripts/build-backfill-receipt.test.js scripts/backfill-notebook.test.js --runInBand` — green.
- `npm test` — no regressions.
- Manual: run harness in --dry-run mode against the 8 already-fixtured pages and inspect the resulting receipt.md is well-formed even with zero commits.
</verification>

<success_criteria>
- Receipt.md is the SINGLE document farmer reads to attest Cycle 1 / Cycle 2.
- Phase 51 contract is validated via intra-cycle upsert stability (BACK-08 N/A resolution).
- All numbers are explicit; no farmer guessing.
</success_criteria>

<output>
Create `.planning/phases/54-backfill-harness-dev-farmos-smoke-20-pages/54-04-SUMMARY.md` when done. Include sample receipt.md output from --dry-run as evidence.
</output>
