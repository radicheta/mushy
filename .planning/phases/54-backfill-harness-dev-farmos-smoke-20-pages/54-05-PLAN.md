---
phase: 54-backfill-harness-dev-farmos-smoke-20-pages
plan: 05
type: execute
wave: 4
depends_on: [54-01, 54-02, 54-03, 54-04]
files_modified:
  - .planning/phases/54-backfill-harness-dev-farmos-smoke-20-pages/54-CYCLE-1-RUNBOOK.md
  - .planning/phases/54-backfill-harness-dev-farmos-smoke-20-pages/54-CYCLE-1-RECEIPT.md
autonomous: false
requirements: [BACK-08]
tags: [v1.11, backfill, cycle-1, ship-gate, farmer-checkpoint, back-08-part2]

must_haves:
  truths:
    - "Cycle 1 RUNBOOK with copy-paste commands exists at .planning/phases/54-.../54-CYCLE-1-RUNBOOK.md."
    - "Operator runs the harness end-to-end against dev farmOS :18080 with the 5 selected Cycle-1 pages."
    - "Run completes with duplicate_asset_count == 0 in the generated receipt."
    - "Farmer reviews receipt.md and explicitly signs off (or files findings to address before Cycle 2)."
    - "Cycle 1 RECEIPT artifact captures: operator run-id, receipt.md path, summary of farmer review, sign-off timestamp."
  artifacts:
    - path: .planning/phases/54-.../54-CYCLE-1-RUNBOOK.md
      provides: "Step-by-step operator instructions"
      min_lines: 60
    - path: .planning/phases/54-.../54-CYCLE-1-RECEIPT.md
      provides: "Operator + farmer attestation record"
      min_lines: 20
  key_links:
    - from: operator
      to: harness
      via: "node scripts/backfill-notebook.js --bulk-backfill --farmer=santi --cycle=1 --limit=5"
      pattern: "backfill-notebook\\.js"
    - from: farmer
      to: receipt
      via: "Signal/in-person review of <runDir>/receipt.md"
      pattern: "receipt\\.md"
---

<objective>
Execute Cycle 1: 5-page smoke run on dev farmOS, farmer-attested. This is the hard checkpoint that blocks Cycle 2 (54-06) until farmer signs off that the receipt-review workflow scales. Per CONTEXT D-04, Cycle 1's 5 pages are drawn from the 8 already-fixtured pages in `test/eval/ingestion/fixtures/notebook-2025/` — reuse, don't re-curate. Most have CSV ground truth so the per-page diff is meaningful.

Purpose: Closes BACK-08 part 2. Validates the harness end-to-end on real dev farmOS writes with real paid LLM calls before scaling to Cycle 2's 20 pages.
Output: A signed-off Cycle 1 receipt + a green light for Cycle 2.

Cycle 1 page selection (5 of the 8 53-04 fixtures, weighted for diversity):
1. IMG_3775 (2025-02-01, 24 entries, multi-strain, fully dated)
2. IMG_3778 (2025-02-20, 8 entries, small/sparse)
3. IMG_3785 (2025-05-27/28, 18 entries, YEAR-ABSENT two-date)
4. IMG_3800 (2025-08-06, 21 entries, YEAR-ABSENT single-strain bulk)
5. IMG_3830 (2025-11-17, 22 entries, YEAR-ABSENT page-truncation case)

This selection covers: fully-dated path (BACK-01 not exercised), year-absent path (BACK-01 IS exercised — 3 of 5), sparse/dense/bulk variety, and the entry-truncation edge case.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/54-backfill-harness-dev-farmos-smoke-20-pages/54-CONTEXT.md
@src/agents/alerter/test/eval/ingestion/fixtures/notebook-2025/README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author 54-CYCLE-1-RUNBOOK.md</name>
  <files>.planning/phases/54-backfill-harness-dev-farmos-smoke-20-pages/54-CYCLE-1-RUNBOOK.md</files>
  <action>
    Create the runbook with these numbered steps (concrete commands, copy-paste-able):

    1. Pre-flight check — confirm dev farmOS :18080 is reachable: `curl -s -o /dev/null -w '%{http_code}\\n' http://10.68.155.50:18080/jsonapi`. Expect 401 or 200.
    2. Confirm DATABASE_URL points at the alerter's dev DB; confirm `.planning/backfill/` is in .gitignore.
    3. Confirm `cd src/agents/alerter && npm test` is green (1151+ pass baseline post-Phase 53).
    4. Hand-author a `--cycle=1` page allowlist file at `/tmp/cycle-1-pages.txt` with the 5 IMG_NNNN basenames listed above (or pass `--pages=IMG_3775.jpg,IMG_3778.jpg,...` if the CLI supports comma-list — confirm during execution; if not, document a `--pages-file` flag follow-up here).
    5. Dry-run smoke first: `cd src/agents/alerter && FARMOS_URL=http://10.68.155.50:18080 FARMOS_USERNAME=mushy-bot FARMOS_PASSWORD=... node scripts/backfill-notebook.js --bulk-backfill --farmer=santi --cycle=1 --limit=5 --dry-run`. Expect: 5 pages listed, exit 0, no farmOS writes, no responses.jsonl.
    6. Real run: same command WITHOUT --dry-run + with ANTHROPIC_API_KEY exported.
    7. Inspect `<runDir>/receipt.md`. Verify: duplicate_asset_count == 0; per-page CSV diffs reasonable (>= 80% hit on the 4 fixtures with CSV coverage); upsert_stability all stable.
    8. Inspect `<runDir>/summaries.log` — one line per draft, no surprises.
    9. Inspect `<runDir>/responses.jsonl` — one line per LLM call; aggregate total_cost_usd from receipt under $0.50 expected.
    10. Send receipt.md (path + key numbers) to farmer (Santi) for review. Farmer responds with sign-off OR findings.
    11. If sign-off: proceed to Task 2 (RECEIPT). If findings: file as `.planning/todos/pending/2026-MM-DD-cycle-1-finding-<name>.md`, address before 54-06 unlocks.

    Hard cycle-2 gate: this RUNBOOK closes with `### DO NOT proceed to 54-06 (Cycle 2) without farmer sign-off on 54-CYCLE-1-RECEIPT.md.`
  </action>
  <verify>
    <automated>test -f .planning/phases/54-backfill-harness-dev-farmos-smoke-20-pages/54-CYCLE-1-RUNBOOK.md && grep -c 'DO NOT proceed to 54-06' .planning/phases/54-backfill-harness-dev-farmos-smoke-20-pages/54-CYCLE-1-RUNBOOK.md</automated>
  </verify>
  <done>RUNBOOK exists with 11 numbered steps + the hard cycle-2 gate.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Operator executes Cycle 1 + farmer attests on 54-CYCLE-1-RECEIPT.md</name>
  <what-built>Operator runs the harness end-to-end per the RUNBOOK. Farmer (Santi) reviews the generated receipt.md.</what-built>
  <how-to-verify>
    Follow .planning/phases/54-backfill-harness-dev-farmos-smoke-20-pages/54-CYCLE-1-RUNBOOK.md steps 1-10.

    After farmer review, author `.planning/phases/54-backfill-harness-dev-farmos-smoke-20-pages/54-CYCLE-1-RECEIPT.md` containing:

    ```
    # Cycle 1 Receipt (BACK-08 part 2)

    - run_id: <runId from runDir>
    - run_dir: .planning/backfill/2025-notebook/<runId>/
    - executed_at: <ISO timestamp>
    - operator: <name>
    - pages_processed: 5 (IMG_3775, IMG_3778, IMG_3785, IMG_3800, IMG_3830)
    - drafts: <n>
    - assets_created: <n>
    - logs_created: <n>
    - duplicate_asset_count: 0  <-- MUST be 0
    - total_cost_usd: <n>
    - receipt_path: .planning/backfill/2025-notebook/<runId>/receipt.md

    ## Farmer Review (Santi)

    - reviewed_at: <ISO>
    - verdict: SIGN-OFF | FINDINGS-FILED
    - findings: [list any filed todos or "none"]
    - cycle-2 unlock: YES | NO

    ## Notes

    <free-form observations from operator + farmer>
    ```

    Cycle 2 (54-06) unlocks ONLY when farmer verdict == SIGN-OFF.
  </how-to-verify>
  <resume-signal>Reply "Cycle 1 signed off" (with the path to 54-CYCLE-1-RECEIPT.md), or "Cycle 1 findings: <description>" to block 54-06.</resume-signal>
</task>

</tasks>

<verification>
- 54-CYCLE-1-RUNBOOK.md exists with the hard gate.
- 54-CYCLE-1-RECEIPT.md exists with farmer verdict SIGN-OFF (or operator reports findings blocking Cycle 2).
- Underlying `<runDir>/receipt.md` shows duplicate_asset_count == 0 and upsert_stability all stable.
</verification>

<success_criteria>
- Cycle 1 executed end-to-end on dev farmOS.
- Farmer attested the receipt-review workflow scales.
- Cycle 2 unblocked OR findings clearly filed.
</success_criteria>

<output>
Create `.planning/phases/54-backfill-harness-dev-farmos-smoke-20-pages/54-05-SUMMARY.md` when done. Reference 54-CYCLE-1-RECEIPT.md for the operator+farmer attestation.
</output>
