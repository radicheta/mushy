---
phase: 999.34-sht30-heater-cycle
plan: 03
type: execute
wave: 3
depends_on: [999.34-01, 999.34-02]
files_modified:
  - src/agents/alerter/src/rules.js
  - src/agents/alerter/src/state.js
  - src/agents/alerter/src/bridge-client.js
  - src/agents/alerter/test/rules.heater.test.js
  - src/chambers/fc-core/fc_core/fc_controller.py
  - src/chambers/fc-core/fc_core/test/test_controller.py
  - .planning/phases/999.34-sht30-heater-cycle/RUNBOOK.md
autonomous: false
requirements: [HEAT-02, HEAT-04, HEAT-06]
must_haves:
  truths:
    - "Alerter subscribes to fc1/sensor/sht30_heater_active via the WS bridge and tracks heaterActive + heaterRecoveryUntilMs"
    - "isRhOob, isSensorSilent (sensor='sht30'), and isSensorError rules return false when (heaterActive || now < heaterRecoveryUntilMs)"
    - "fc_controller mirrors fc_sensors heater pre/post RH + recovery_until into sensor_health KeyValues so a single topic carries the full contract"
    - "A long-run validation runbook exists at .planning/phases/999.34-sht30-heater-cycle/RUNBOOK.md describing the SHT30↔SCD41 RH-delta query, the 30-day cadence, and the rollback procedure (heater_cycle_enabled=false + restart fc-core)"
    - "Live deploy procedure documented and executed on fc1 via fc1/prod branch"
    - "First nightly cycle on production sensor observed: Bool toggles, no farmer alert at 03:01, controller duty stays at pre-cycle value, post-RH within 0.1% of pre-RH (HEAT-05)"
  artifacts:
    - path: src/agents/alerter/src/rules.js
      provides: "Heater-window guards on isRhOob, isSensorSilent, isSensorError"
    - path: src/agents/alerter/src/state.js
      provides: "heaterActive + heaterRecoveryUntilMs fields tracked from WS bridge"
    - path: src/agents/alerter/src/bridge-client.js
      provides: "Subscription to fc1/sensor/sht30_heater_active topic"
    - path: src/agents/alerter/test/rules.heater.test.js
      provides: "Vitest unit tests for alerter heater suppression"
    - path: src/chambers/fc-core/fc_core/fc_controller.py
      provides: "Sensor→controller pre/post RH + recovery_until mirror via fc1/sensor_health republish chain"
    - path: .planning/phases/999.34-sht30-heater-cycle/RUNBOOK.md
      provides: "30-day validation procedure, query, rollback steps, dashboard task"
  key_links:
    - from: "alerter rules.js"
      to: "heaterActive flag"
      via: "precondition check at top of isRhOob, isSensorSilent, isSensorError"
      pattern: "if \\(heaterActive \\|\\| .*recoveryUntil.*\\) return false"
    - from: "fc_sensors pre/post RH state"
      to: "fc_controller sensor_health KeyValues"
      via: "small republish bridge (sensor adds KVs to its own diag publish OR controller subscribes to a new diag topic from sensor)"
    - from: "Mission Control RH chart"
      to: "heater_active boolean"
      via: "bridge republish (existing WS bridge architecture; no new chart code in this plan)"
---

<objective>
Close the loop: alerter must NOT fire at 03:01 UYT when the heater pulses, the sensor_health KeyValues must carry full pre/post RH telemetry, and a long-run validation procedure (HEAT-06: 30-day SHT30↔SCD41 delta narrowing) must be documented and runnable. Final task is the fc1 deploy + first-cycle observation gate.

This is the **alerter coordination + telemetry + validation + deploy** plan. Without it, HEAT-04 (no 03:01 alert) is unmet and HEAT-06 (30-day proof) has no observable.

Out of scope (deferred): bridge-side Timescale skip-insert and Mission Control chart annotation — see Open Question 1 below; these compose with Phase 26 / 999.27 and need a discuss-phase decision before implementation. The sensor-side suppression in plan 01 means the bridge will already see a data gap (no slot-1 publishes), so the chart will show a gap by default — acceptable v1, formal annotation deferred.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/999.34-sht30-heater-cycle/RESEARCH.md
@.planning/phases/999.34-sht30-heater-cycle/999.34-01-SUMMARY.md
@.planning/phases/999.34-sht30-heater-cycle/999.34-02-SUMMARY.md
@src/agents/alerter/src/rules.js
@src/agents/alerter/src/state.js
@src/agents/alerter/src/bridge-client.js
@src/chambers/fc-core/fc_core/fc_controller.py
@src/chambers/fc-core/fc_core/fc_sensors.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Alerter heater suppression — RED tests + GREEN impl + container rebuild</name>
  <files>src/agents/alerter/src/rules.js, src/agents/alerter/src/state.js, src/agents/alerter/src/bridge-client.js, src/agents/alerter/test/rules.heater.test.js</files>
  <behavior>
    - test_rh_oob_suppressed_during_heater: isRhOob({rh: 80, target: 96, band: 1.5, heaterActive: true, ...}) returns false (would normally trigger).
    - test_rh_oob_suppressed_during_recovery: isRhOob with heaterActive=false but recoveryUntilMs > nowMs returns false.
    - test_rh_oob_fires_post_recovery: heaterActive=false, recoveryUntilMs in past, rh OOB → returns true (existing behavior preserved).
    - test_sensor_silent_suppressed_for_sht30: isSensorSilent({sensor:'sht30', lastSeenMs: now - 600_000, heaterActive: true, ...}) returns false.
    - test_sensor_silent_NOT_suppressed_for_scd41: isSensorSilent({sensor:'scd41', ..., heaterActive: true}) returns its normal value (heater suppression is sht30-only).
    - test_sensor_error_suppressed_during_window: isSensorError for sht30 returns false during heater window.
    - test_state_updates_from_bool_topic: state.js applyHeaterTopic({data: true}) sets state.heaterActive=true; applyHeaterTopic({data:false}) sets it false and records the falling edge timestamp; state.heaterRecoveryUntilMs is computed by mirroring from sensor_health KV when received (or set to now+180s as a fallback if KV is absent).
  </behavior>
  <action>
    1. Write the 7 failing vitest tests in `src/agents/alerter/test/rules.heater.test.js`.

    2. In `src/agents/alerter/src/state.js`, add fields:
       ```js
       heaterActive: false,
       heaterRecoveryUntilMs: 0,
       lastHeaterCycleIso: null,
       ```
       Add `applyHeaterTopic(msg)` that updates heaterActive from Bool messages.
       In the existing sensor_health DiagnosticStatus handler, also extract `heater_recovery_until_iso` and `last_heater_cycle_iso` KeyValues (parse ISO → ms).

    3. In `src/agents/alerter/src/bridge-client.js`, add a subscription to `fc1/sensor/sht30_heater_active` (Bool, TRANSIENT_LOCAL — bridge layer should already pass these through; verify via existing slot-1 freshness subscriptions established in Phase 26). Wire the callback to `state.applyHeaterTopic(msg)`.

    4. In `src/agents/alerter/src/rules.js`, add precondition guards at the top of:
       - `isRhOob`: `if (state.heaterActive || Date.now() < state.heaterRecoveryUntilMs) return false;`
       - `isSensorSilent`: same guard, but **only** when the rule is firing for sensor='sht30' (preserve scd41 staleness alarm).
       - `isSensorError`: same sht30-only guard.

    5. Run `cd src/agents/alerter && npm test` — RED first, then implement to GREEN.

    6. Rebuild the alerter container against the live compose stack at the repo root (per memory `feedback_verify_runtime_compose` — NOT src/docker-compose.yml):
       ```bash
       docker compose up -d --build bridge alerter  # if alerter is co-deployed in bridge container, rebuild bridge
       ```
       Verify by checking the running container logs include the new subscription line on startup (e.g., `subscribed to fc1/sensor/sht30_heater_active`).

    Commit message: `feat(999.34-03): alerter suppresses sht30 alarms during heater window`
  </action>
  <verify>
    <automated>cd /mnt/slime-kingdom/opt/mushy/src/agents/alerter && npm test 2>&1 | tail -30</automated>
  </verify>
  <done>7 alerter tests GREEN. Live alerter container rebuilt and running. Container logs show heater topic subscription on startup. No regression in existing alerter tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Mirror fc_sensors heater pre/post RH into fc_controller sensor_health KeyValues + RUNBOOK</name>
  <files>src/chambers/fc-core/fc_core/fc_controller.py, src/chambers/fc-core/fc_core/fc_sensors.py, src/chambers/fc-core/fc_core/test/test_controller.py, .planning/phases/999.34-sht30-heater-cycle/RUNBOOK.md</files>
  <behavior>
    - fc_sensors publishes its own small DiagnosticStatus on a new topic `fc1/sensor/sht30_heater_status` carrying KeyValues: heater_active, heater_recovery_until_iso, last_heater_cycle_iso, last_heater_pre_rh, last_heater_post_rh.
    - fc_controller subscribes to it and updates the corresponding self._last_heater_* fields (replacing the placeholder Nones from plan 02).
    - sensor_health published by fc_controller now contains real values for these KeyValues after a heater cycle.
    - RUNBOOK.md exists and contains: validation queries, 30-day cadence, rollback procedure, dashboard tasks for farmer-visible heater event log.
  </behavior>
  <action>
    1. In fc_sensors.py, add a publisher for `fc1/sensor/sht30_heater_status` (DiagnosticStatus, default QoS or TRANSIENT_LOCAL — match Phase 26 pattern, TRANSIENT_LOCAL is preferred so late-joiners snap). Publish on every state transition AND every successful capture of pre_rh/post_rh.

       Rationale for separate topic (vs piggybacking on the Bool): DiagnosticStatus carries the full record; Bool stays the lightweight machine-readable contract for alerter/controller. Both topics together ARE the contract.

    2. In fc_controller.py, subscribe to `fc1/sensor/sht30_heater_status` and on each message update:
       ```python
       self._heater_recovery_until_iso = kv['heater_recovery_until_iso']
       self._last_heater_cycle_iso = kv['last_heater_cycle_iso']
       self._last_heater_pre_rh = float(kv['last_heater_pre_rh']) if kv.get('last_heater_pre_rh') else None
       self._last_heater_post_rh = float(kv['last_heater_post_rh']) if kv.get('last_heater_post_rh') else None
       ```
       Then trigger _publish_sensor_health so downstream consumers get the update.

    3. Add unit test test_controller_mirrors_heater_status_kv: simulate the DiagnosticStatus message, assert sensor_health publish carries non-empty values for all 4 KeyValues.

    4. Write `.planning/phases/999.34-sht30-heater-cycle/RUNBOOK.md` with sections:

       ## 30-day Validation (HEAT-06)
       - Daily Timescale query (paste-ready):
         ```sql
         SELECT date_trunc('day', ts) AS day,
                avg(sht30_rh - scd41_rh) AS mean_delta,
                stddev(sht30_rh - scd41_rh) AS sd_delta
         FROM (
           SELECT ts, value AS sht30_rh
           FROM telemetry WHERE topic='fc1/humidity' AND frame_id='sht30'
         ) s JOIN (
           SELECT ts, value AS scd41_rh
           FROM telemetry WHERE topic='fc1/humidity_2' AND frame_id='scd41'
         ) c USING (ts)
         WHERE ts > now() - interval '30 days'
         GROUP BY 1 ORDER BY 1;
         ```
         (Adjust column names to actual schema; this is the shape, not literal — confirm against bridge insert path.)
       - Target: |mean_delta| < 1% sustained; trend should be flat-or-narrowing over 30 days. Widening = heater not clearing creep → bias to 2s pulse and re-evaluate.
       - Alternate sanity metric: per-cycle `last_heater_pre_rh - last_heater_post_rh`. On a non-condensed sensor: ≤ 0.1% (HEAT-05). On a condensed sensor: positive drop magnitude = "creep dose cleared."

       ## Mission Control dashboard task (deferred — captured here for the next planning cycle)
       - Add a "Heater cycle log" panel sourced from sensor_health KeyValue history showing last_heater_cycle_iso, last_heater_pre_rh, last_heater_post_rh.
       - Defer formal chart annotation until bridge-side Timescale skip-insert decision is made (Open Question 1 below).

       ## Rollback
       - Disable: `ros2 param set /fc_sensors heater_cycle_enabled false` (live).
       - Persist: edit `src/chambers/fc-core/config/fc_config.yaml` set `heater_cycle_enabled: false`, commit, push fc1/prod, run deploy.sh on fc1.
       - Verify: `ros2 topic echo /fc1/sensor/sht30_heater_active` should publish nothing further; controller _heater_active never goes true; alerter resumes pre-999.34 behavior.

       ## Operational notes
       - First nightly cycle observation: 03:00 local the day after deploy. Tail journalctl on fc1 for the cycle log lines.
       - Pulse-duration tuning: if 30-day delta widens, bump `heater_pulse_seconds` to 2.0 then 3.0 in successive 30-day windows.
       - Recovery-duration tuning: if first post-window control_loop tick shows >0.1 duty step within 10s of falling edge → `heater_recovery_seconds` is too short; increase to 240s.

    5. Run pytest + colcon build.

    Commit message: `feat(999.34-03): heater status mirror + 30-day validation runbook`
  </action>
  <verify>
    <automated>cd /mnt/slime-kingdom/opt/mushy && pytest src/chambers/fc-core/fc_core/test/ -x 2>&1 | tail -20 && colcon build --symlink-install --packages-select fc_core 2>&1 | tail -5 && test -f .planning/phases/999.34-sht30-heater-cycle/RUNBOOK.md && echo RUNBOOK_OK</automated>
  </verify>
  <done>fc-core tests GREEN. RUNBOOK.md exists with all 4 sections (validation, dashboard task, rollback, ops notes). sensor_health KeyValues populate with real values after a simulated heater cycle.</done>
</task>

<task type="auto">
  <name>Task 3: Deploy to fc1 and stage first-cycle observation</name>
  <files>(deploy only — no source changes)</files>
  <action>
    1. Diff repo vs fc1 systemd unit (per memory `feedback_diff_repo_vs_pi_systemd`):
       ```bash
       ssh santi@172.16.10.5 'cat /etc/systemd/system/fc-core.service' > /tmp/fc1-unit.txt
       diff /tmp/fc1-unit.txt scripts/pi-deploy/fc-core.service || echo "drift detected — investigate before deploy"
       ```

    2. Push to fc1/prod:
       ```bash
       git checkout fc1/prod
       git merge --ff-only main   # or whatever branch has the 999.34 commits
       git push origin fc1/prod
       ```

    3. On fc1, deploy:
       ```bash
       ssh santi@172.16.10.5 'cd ~/mushroom_farm_ws && ./deploy.sh'
       # deploy.sh pulls fc1/prod, runs colcon build --symlink-install, restarts fc-core.service
       ```

    4. Verify topic surface immediately post-restart:
       ```bash
       ssh santi@172.16.10.5 'source /opt/ros/jazzy/setup.bash && source ~/mushroom_farm_ws/install/setup.bash && ros2 topic list | grep heater'
       # expect: /fc1/sensor/sht30_heater_active and /fc1/sensor/sht30_heater_status
       ```

    5. Echo Bool initial state (should be false):
       ```bash
       ssh santi@172.16.10.5 'source /opt/ros/jazzy/setup.bash && source ~/mushroom_farm_ws/install/setup.bash && timeout 5 ros2 topic echo /fc1/sensor/sht30_heater_active --once'
       ```

    6. Smoke-test manual heater trigger by setting next_due_at to now (one-shot test, NOT a parameter override that persists):
       ```bash
       # Issue a one-shot trigger via service call OR temporarily set heater_schedule_local_hhmm to current time + 1 minute via ros2 param set, observe one cycle, then restore to 03:00.
       ```
       Watch for:
       - Bool topic publishes true → 1s later → false-but-recovery → 180s later → false-and-normal.
       - Controller log: "heater window open — holding last_duty=X.XX"
       - Controller log 181s later: "heater window closed — bumpless re-engage at duty=X.XX"
       - Alerter container logs: NO RH-OOB or sensor-silent alert during the window.
       - sensor_health post-window: KeyValue last_heater_pre_rh / last_heater_post_rh populated.
       - Restore heater_schedule_local_hhmm to "03:00".

    Commit message: `chore(999.34-03): deploy 999.34 to fc1`
  </action>
  <verify>Manual smoke-trigger above + checkpoint task below for the real 03:00 cycle observation.</verify>
  <done>Topics present on fc1; smoke-trigger cycle completes cleanly; no alerter messages during window; pre/post KVs populated.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: First nightly cycle on production sensor (03:00 next day)</name>
  <what-built>Full 999.34 stack live on fc1: scheduler, suppression, controller guard, alerter coordination, telemetry mirror.</what-built>
  <how-to-verify>
    Morning of 03:00 cycle (the day after deploy):
    1. No farmer Signal alert received between 02:55 and 03:10 local.
    2. journalctl on fc1: `journalctl -u fc-core --since="03:00" --until="03:05"` shows the cycle log lines (heater open, heater closed) with sane pre/post RH.
    3. Mission Control RH chart: small data gap visible at 03:00 (~3 min wide). Not a corrupt spike. Acceptable v1 — formal annotation deferred.
    4. Controller duty during window: confirm via `ros2 topic echo /fc1/actuators/humidifier_duty` history or Timescale that duty was held flat at the pre-cycle value.
    5. HEAT-05 spot-check: |last_heater_pre_rh - last_heater_post_rh| < 0.1% for first cycle (sensor likely not yet meaningfully condensed; this is the no-drift acceptance test).
    6. Note the date in RUNBOOK.md as Day 1 of the 30-day soak; set a calendar reminder for Day 30 to run the SHT30↔SCD41 delta query.
  </how-to-verify>
  <resume-signal>Type "first cycle clean" or describe any anomaly (alerter fired / duty spiked / heater stuck on / pre-post RH delta > 0.1%)</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ROS Bool topic → WS bridge → alerter | Alerter trusts bridge to deliver heater state changes; if bridge silently drops, alerter could fire false positives at 03:01 |
| fc1 systemd unit → repo | Drift between Pi unit and repo could mask a deploy regression |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-99934-10 | D (DoS) | Alerter fires false RH-OOB at 03:01 | mitigate | Plan 03 Task 1 — guard on every relevant rule; Task 4 checkpoint verifies on real cycle |
| T-99934-11 | I (info loss) | Alerter container restart loses heater state | mitigate | TRANSIENT_LOCAL Bool topic + sensor_health DiagnosticStatus replay; alerter snaps to current state on subscribe |
| T-99934-12 | T (tampering) | Stale fc1 systemd unit clobbered by deploy | mitigate | Diff-before-deploy step in Task 3 |
| T-99934-13 | E (silent failure) | 30-day delta widens but no one notices | accept | RUNBOOK calendar reminder + farmer dashboard task; will revisit at Day 30 checkpoint |
</threat_model>

<verification>
- npm test (alerter) GREEN.
- pytest fc-core GREEN.
- fc1 deploy successful; heater topics visible.
- Smoke-trigger cycle completes cleanly with no alerter alert.
- First real 03:00 cycle observed clean (human checkpoint).
</verification>

<success_criteria>
- HEAT-04: Alerter does NOT fire during heater window. (Verified by checkpoint.)
- HEAT-02: RH chart shows gap (not corrupt spike) — sensor-side suppression delivers this for free; chart annotation deferred.
- HEAT-05: Post-window RH within 0.1% of pre-window RH on first cycle. (Verified by checkpoint.)
- HEAT-06: Validation runbook + query in place; 30-day soak begins on Day 1.
- Rollback path documented and proven by `heater_cycle_enabled: false` mechanism.
</success_criteria>

<output>
`.planning/phases/999.34-sht30-heater-cycle/999.34-03-SUMMARY.md` documenting: live deploy date/time, first-cycle observation results (pre/post RH), any deviations, calendar reminder set for Day 30 SHT30↔SCD41 delta query.
</output>

<open_questions_deferred_to_discuss>
The following ROADMAP open questions are NOT resolved in this plan and should be raised at the next discuss-phase touchpoint (most likely as part of a 999.34 follow-up or before the Day-30 review):

1. **Bridge-side Timescale skip-insert vs insert-with-quality-flag during heater window.** Sensor-side suppression in plan 01 already gives a gap to slot-1 consumers, so Timescale already gets a gap by default. The question is whether bridge should additionally tag the gap with a quality marker for analytics. Default in this phase: do nothing (gap is honest). Revisit if 999.27 derivations need the disambiguation.

2. **Add an RH-stuck-condition trigger ("RH > 95.5% sustained > 24h") in this phase or defer.** Default: defer. Ship nightly fixed schedule first; condition-trigger only if 30-day delta evidence shows nightly is insufficient.

3. **Failure mode if `self.sht.heater = True` raises (retry vs wait-next-slot).** Plan 01 implements the recommended default (3 retries at 10-min intervals, then wait for next nightly slot). Confirm at first failure observation; tune if needed.

4. **Verify SHT30↔SCD41 thermal coupling on FC-1 mounting.** Default: log SCD41 readings during heater window and review at Day 30. If SCD41 shows a perturbation correlated with heater events, raise a follow-up phase to physically separate sensors or add a thermal shield.
</open_questions_deferred_to_discuss>
