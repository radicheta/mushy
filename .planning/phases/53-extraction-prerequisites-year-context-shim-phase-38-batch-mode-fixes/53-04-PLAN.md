---
phase: 53-extraction-prerequisites-year-context-shim-phase-38-batch-mode-fixes
plan: 04
type: execute
wave: 2
depends_on: [53-01, 53-02, 53-03]
files_modified:
  - src/agents/alerter/test/eval/ingestion/fixtures/notebook-2025/
  - src/agents/alerter/test/eval/ingestion/notebook-2025.test.js
  - src/agents/alerter/package.json
autonomous: false
requirements: [BACK-04]
phase54_gate: true

must_haves:
  truths:
    - "5-10 hand-labeled 2025 notebook pages from /mnt/mossrock/shared/mushdatadump-prod/ live under test/eval/ingestion/fixtures/notebook-2025/"
    - "Each fixture has: source image path, manifest.json (corpus_context={default_year:2025, source:'paper_log'} + capture_kind ground truth), ground-truth.json (expected drafts shape)"
    - "Hermetic mock-extractor eval (`npx jest notebook-2025.test.js`) PASSES in CI — exercises pipeline.enqueue with corpus_context plumbing (53-01) + small-N routing (53-02) + capture_kind classification (53-03)"
    - "Live-LLM eval (`EVAL_RUN_LIVE=1 npm run test:eval`) is wired but skipped in default CI — operator runs on demand because paid LLM cost (per D-BACK-04 and memory feedback_smoke_before_expensive_batch)"
    - "Phase 54 cannot start until this eval gate is GREEN — recorded in this plan's frontmatter (phase54_gate: true) and in the SUMMARY"
  artifacts:
    - path: "src/agents/alerter/test/eval/ingestion/fixtures/notebook-2025/"
      provides: "5-10 fixture subdirs, each with manifest.json + ground-truth.json + mock-extraction.json + source image symlink"
    - path: "src/agents/alerter/test/eval/ingestion/notebook-2025.test.js"
      provides: "Hermetic mock-extractor it.each over the corpus + EVAL_RUN_LIVE=1 branch wired but no-op by default"
    - path: "src/agents/alerter/package.json"
      provides: "Added `test:eval` script invoking jest on the notebook-2025 + sessions live-LLM branches with EVAL_RUN_LIVE=1"
  key_links:
    - from: "test/eval/ingestion/notebook-2025.test.js"
      to: "src/extraction/extractor.js (real createExtractor with mocked Anthropic client)"
      via: "createExtractor + makeMockAnthropicClient (pattern from sessions.test.js)"
      pattern: "createExtractor"
---

<objective>
Build the hermetic eval ship-gate for Phase 53. Hand-label 5-10 representative 2025-notebook pages from `/mnt/mossrock/shared/mushdatadump-prod/` and assert that the post-fix extractor produces correct outputs on each. This is the BACK-04 hard gate: Phase 54 (backfill smoke harness) CANNOT begin until this suite is green.

Per D-BACK-04 (locked):
- Corpus: 5-10 hand-labeled pages from `/mnt/mossrock/shared/mushdatadump-prod/` (memory `[[project_phase38_production_logs_available]]`).
- Labels: per-page expected extraction JSON (drafts[], confidence, capture_kind).
- Fixture location: `test/eval/ingestion/fixtures/notebook-2025/` (new subdir).
- Pass criteria: per-page extraction matches expected on `asset_ref`, `event_kind`, `qty`; free-text fields tolerant.
- CI hermetic by default (mock extractor); live-LLM run via `EVAL_RUN_LIVE=1 npm run test:eval` (paid; on-demand only).

Per CONTEXT critical lock: NO live-fire on dev farmOS — extraction-only changes, no write path touched.

Purpose: Proves end-to-end that 53-01 (corpus_context plumbing), 53-02 (routing heuristic), and 53-03 (capture_kind classifier) survive contact with real 2025 notebook scans before any paid batch run touches anything. Closes BACK-04. Gates Phase 54.

Output: 5-10 fixture subdirs + 1 hermetic test file + 1 npm script + 1 SUMMARY artifact noting the gate is green and Phase 54 may proceed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/53-extraction-prerequisites-year-context-shim-phase-38-batch-mode-fixes/53-CONTEXT.md
@src/agents/alerter/test/eval/ingestion/sessions.test.js
@src/agents/alerter/test/eval/ingestion/sessions-loader.js
</context>

<interfaces>
<!-- Pattern to mirror — sessions.test.js is the canonical eval-suite shape. -->

sessions.test.js (test/eval/ingestion/sessions.test.js:1-40):
- Loads a corpus via a loader module (sessions-loader.js)
- Filters by `manifest.regression_guard === true`
- `it.each(NAMED)` over the corpus
- Default hermetic: real `createExtractor` + mock Anthropic client returning the fixture's `mock-extraction.json` verbatim
- Live branch: `process.env.EVAL_RUN_LIVE === '1' && process.env.ANTHROPIC_API_KEY` — uses real Anthropic client (currently no-op in 49; we WILL wire it here)

Fixture layout (sessions/2026-05-22_inoc_santi/ etc.):
- `manifest.json` — `{ regression_guard: bool, name: string, ... }`
- `ground-truth.json` — expected fields on the produced draft(s)
- `mock-extraction.json` — raw Anthropic tool_use response to replay in hermetic mode
- source media (image/jpg) symlinked or copied from the mushdatadump corpus

For notebook-2025/, mirror this exactly but add to manifest:
```
{
  "name": "<page-id>",
  "year": 2025,
  "corpus_context": { "default_year": 2025, "source": "paper_log" },
  "expected_capture_kind": "paper_log" | "physical_object_photo",
  "regression_guard": true
}
```

Pipeline + extractor are the units under test — `extractor.extract({ captures, corpusContext: manifest.corpus_context })` returns an envelope; the test asserts `envelope.drafts` matches `ground-truth.json` on `asset_ref` / `event_kind` / `qty.value`, and `envelope.capture_kind === manifest.expected_capture_kind` (when 53-03 lands).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Hand-curate 5-10 fixture pages from mushdatadump-prod</name>
  <files>src/agents/alerter/test/eval/ingestion/fixtures/notebook-2025/</files>
  <action>
    Browse `/mnt/mossrock/shared/mushdatadump-prod/` for 2025-notebook pages (memory: extractor hallucinates years on undated pages — these are the target). Pick 5-10 pages that span the variety this phase aims to handle:
    - At least 3 true paper-log pages (multi-row notebook scans) — should classify capture_kind=paper_log AND route to runBatchMode under 53-02 heuristic.
    - At least 1 physical-object photo (a "DT tubs 0519 1 and 2"-shaped capture if findable) — should classify capture_kind=physical_object_photo AND route through small-N per-draft confirm under 53-02.
    - At least 1 page where the year is genuinely absent (the BACK-01 case) — should have all drafts use `event_date` starting with `2025-` because corpus_context.default_year=2025.

    For EACH page, create a subdir `test/eval/ingestion/fixtures/notebook-2025/<page-id>/` containing:
    - `image.jpg` — symlink to the source under mushdatadump-prod (do NOT copy giant binaries into git; symlink + .gitignore the binary tree OR commit small thumbnails — read memory feedback_persist_paid_results_default + check existing sessions/ fixture pattern for how big media is handled)
    - `manifest.json` — see <interfaces> above. Set `regression_guard:true`, `corpus_context:{default_year:2025, source:'paper_log'}`, `expected_capture_kind:'paper_log'|'physical_object_photo'`.
    - `ground-truth.json` — hand-labeled expected envelope: drafts[] with at minimum `{type, event_date, asset_ref OR groups[].parent.value, qty.value, event_kind}` per draft. Year on event_date MUST be 2025.
    - `mock-extraction.json` — a once-produced raw Anthropic tool_use response that matches ground truth. To produce this without burning paid tokens repeatedly: run the live extractor ONCE on the curated page (with EVAL_RUN_LIVE=1) and save the raw response. Per memory feedback_persist_paid_results_default — save with a unique per-call path; never overwrite. If a live LLM run is genuinely not feasible (no API key), hand-author a plausible mock-extraction.json matching the ground truth exactly (the hermetic test only verifies envelope-shape conformance, not LLM realism).

    Append fixture binaries / large media to .gitignore if the existing sessions/ fixtures do so; otherwise commit lean (under 100KB per page is ideal). Document curation choices in a `README.md` inside `notebook-2025/`.
  </action>
  <verify>
    <automated>ls src/agents/alerter/test/eval/ingestion/fixtures/notebook-2025/ | wc -l | awk '{exit ($1 >= 5 && $1 <= 11) ? 0 : 1}'</automated>
  </verify>
  <done>5-10 subdirs exist, each with the 4 artifacts (image, manifest, ground-truth, mock-extraction). README.md documents the curation rationale.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Hermetic eval test + npm test:eval script</name>
  <files>src/agents/alerter/test/eval/ingestion/notebook-2025.test.js, src/agents/alerter/test/eval/ingestion/notebook-2025-loader.js, src/agents/alerter/package.json</files>
  <behavior>
    - `notebook-2025-loader.js` exports `loadNotebook2025Corpus()` returning `[{name, manifest, groundTruth, mockExtraction, imagePath}]`. Mirrors `sessions-loader.js` shape.
    - `notebook-2025.test.js` iterates the corpus, for each entry:
        * Default (hermetic): instantiate real `createExtractor` with a mock Anthropic client that returns `mockExtraction` verbatim; call `extractor.extract({ captures:[{captureId:'fixture', text:null, images:[<loaded image>]}], corpusContext: manifest.corpus_context })`; assert returned envelope's drafts match `groundTruth.drafts` on `{type, event_date, asset_ref OR groups[].parent.value, qty.value, event_kind}` (tolerant on free-text); assert `envelope.capture_kind === manifest.expected_capture_kind` when 53-03 has shipped (skip assertion gracefully if capture_kind absent — both directions back-compat).
        * `EVAL_RUN_LIVE=1` branch: instantiate real Anthropic client (requires `ANTHROPIC_API_KEY`); call `extractor.extract` with the real image bytes; assert same shape conformance. Persist each live response to `test/eval/ingestion/results/notebook-2025/<page-id>-<ISO timestamp>.json` (per memory feedback_persist_paid_results_default — never overwrite).
    - `package.json` gains: `"test:eval": "EVAL_RUN_LIVE=1 jest test/eval/ingestion/notebook-2025.test.js test/eval/ingestion/sessions.test.js"` (composes with the existing live-eval branch in sessions.test.js).
    - All hermetic cases PASS — this is the BACK-04 ship-gate.
  </behavior>
  <action>
    Write `notebook-2025-loader.js` mirroring `sessions-loader.js` exactly (read fixtures/notebook-2025/*, load the 4 artifacts per subdir, return the array). Write `notebook-2025.test.js` mirroring `sessions.test.js`'s `it.each(NAMED)` + `makeMockAnthropicClient` pattern. Add the `test:eval` script to package.json. Run hermetic eval, fix any mock-extraction.json discrepancies until all cases green.

    The expensive `EVAL_RUN_LIVE=1` branch ships but is NOT run as part of this plan's ship-gate — the hermetic branch is the gate. Operator runs `npm run test:eval` on demand to spot-check that the prompt changes survive a real LLM call.
  </action>
  <verify>
    <automated>cd src/agents/alerter && npx jest test/eval/ingestion/notebook-2025.test.js</automated>
  </verify>
  <done>Hermetic suite green. Live-eval script present in package.json but skipped by default.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Operator confirms Phase 54 may proceed</name>
  <what-built>BACK-04 ship-gate (5-10 hand-labeled 2025 notebook fixtures + hermetic eval + on-demand live-LLM script). Phase 54 (backfill harness) is gated on this — per CONTEXT critical lock #7.</what-built>
  <how-to-verify>
    1. Inspect `test/eval/ingestion/fixtures/notebook-2025/README.md` — confirm the 5-10 curation choices are sensible (at least 3 paper-log pages, ≥1 physical-object photo, ≥1 undated-year page).
    2. Run `cd src/agents/alerter && npx jest test/eval/ingestion/notebook-2025.test.js` — expect full green.
    3. Optionally run `cd src/agents/alerter && npm run test:eval` once with a real ANTHROPIC_API_KEY to spot-check live-LLM behavior on the fixtures. Per memory feedback_smoke_before_expensive_batch this is the 5-10-item smoke before Phase 54 burns batch-LLM cost on the full mushdatadump-prod corpus. Results persisted under `test/eval/ingestion/results/notebook-2025/`.
    4. If live-eval surfaces a divergence between mocked and real LLM behavior, EITHER update the mock-extraction.json fixtures from the persisted live response (acceptable — that's curation refinement) OR file a follow-on todo if the divergence indicates a prompt bug.
    5. Confirm: "Phase 54 backfill harness is unblocked."
  </how-to-verify>
  <resume-signal>Type "Phase 54 unblocked" to proceed, or describe issues found</resume-signal>
</task>

</tasks>

<verification>
- Hermetic suite green by default.
- Live-LLM script wired but no-op in CI.
- Phase 54's PLAN.md (when authored) will frontmatter `depends_on: [53-04]` to make the gate explicit.
</verification>

<success_criteria>
- 5-10 fixtures exist under `test/eval/ingestion/fixtures/notebook-2025/`
- `cd src/agents/alerter && npx jest test/eval/ingestion/notebook-2025.test.js` — full green (the ship-gate)
- `npm run test:eval` script present in package.json (operator-on-demand)
- Operator-verified Task 3 checkpoint signals Phase 54 may begin
</success_criteria>

<output>
Create `.planning/phases/53-extraction-prerequisites-year-context-shim-phase-38-batch-mode-fixes/53-04-SUMMARY.md` when done. Include a top-line "BACK-04 ship-gate GREEN — Phase 54 unblocked" line for the orchestrator to pick up.
</output>
