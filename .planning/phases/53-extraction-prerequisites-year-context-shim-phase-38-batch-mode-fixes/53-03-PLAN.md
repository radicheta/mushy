---
phase: 53-extraction-prerequisites-year-context-shim-phase-38-batch-mode-fixes
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/agents/alerter/src/extraction/prompts/system.js
  - src/agents/alerter/src/extraction/schemas/index.js
  - src/agents/alerter/test/extraction/integration/capture-kind.test.js
autonomous: true
requirements: [BACK-03]

must_haves:
  truths:
    - "Extractor system prompt teaches the model to emit a top-level `capture_kind: 'paper_log' | 'physical_object_photo' | 'voice_note' | 'text' | null` field on the extraction envelope"
    - "Few-shot examples include at least one paper-log photo (positive) AND one physical-object photo with multi-bag caption (DT-tubs counter-example) so the model distinguishes them"
    - "Extraction schema accepts capture_kind as optional/nullable — drafts without the field validate exactly as today (back-compat)"
    - "When the extractor omits capture_kind, downstream routing (BACK-02) ignores it — routing decision is unchanged (the load-bearing fix is BACK-02)"
  artifacts:
    - path: "src/agents/alerter/src/extraction/prompts/system.js"
      provides: "Updated SYSTEM_PROMPT instructions for capture_kind + at least 2 few-shot pairs (1 paper-log, 1 physical-object-photo)"
      contains: "capture_kind"
    - path: "src/agents/alerter/src/extraction/schemas/index.js"
      provides: "Optional nullable capture_kind enum on the extraction envelope (NOT on individual draft entries)"
      contains: "capture_kind"
  key_links:
    - from: "src/agents/alerter/src/extraction/prompts/system.js (capture_kind few-shots)"
      to: "src/agents/alerter/src/extraction/schemas/index.js (envelope schema)"
      via: "extractor validates model output against schema — must accept the new optional field"
      pattern: "capture_kind"
---

<objective>
Per D-BACK-03 Option 1 (locked, prompt-only — NOT vision pre-classifier): extend the extractor system prompt with `capture_kind: 'paper_log' | 'physical_object_photo' | 'voice_note' | 'text'` few-shots so the extraction envelope carries this classification field for analytics + future routing refinement. Add the field as OPTIONAL/nullable on the schema so legacy callers and partial outputs keep validating.

Closes `2026-05-24-phase38-photo-vs-paper-log-classifier-too-eager.md`. The DT-tubs photo (`01KSCW771VB2FDWBPWNS4MEHAZ`) should classify as `physical_object_photo`; the May-22 paper-log photo at `mushdatadump-prod/2026-05-12_inoc_santi/XAbzzUidkLR3irhVmjea.jpg` should classify as `paper_log`.

Per D-BACK-03 back-compat lock: if the extractor omits the field, treat as `null` and DO NOT use as a routing input. Routing fix (BACK-02 in 53-02-PLAN.md) is the load-bearing piece; this classifier is supportive metadata for analytics + future refinement. No schema change to `signal_draft` — capture_kind rides inside the existing `extraction_envelope` JSON.

Purpose: Closes the eager-classifier todo and produces an analytics signal for "what % of captures are physical-object photos vs paper-log scans." Sets the stage for future per-capture-kind routing without locking it in today.

Output: Updated SYSTEM_PROMPT with 2 new few-shot pairs + schema accepts optional `capture_kind` + 1 integration test verifying classification on both fixture shapes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/53-extraction-prerequisites-year-context-shim-phase-38-batch-mode-fixes/53-CONTEXT.md
@.planning/todos/pending/2026-05-24-phase38-photo-vs-paper-log-classifier-too-eager.md
@src/agents/alerter/src/extraction/prompts/system.js
@src/agents/alerter/src/extraction/schemas/index.js
</context>

<interfaces>
<!-- Prompt structure (extracted from prompts/system.js). -->

The system prompt is composed of:
- A leading instruction block (lines ~80-160) — natural-language rules
- Few-shot tool_use/tool_result pairs (lines ~165-340) demonstrating outputs
- Each few-shot pair has: user content blocks (text + maybe image), then `tool_use` with `input` matching the extraction envelope schema, then a matching `tool_result` in the next user turn

Extraction envelope shape (schemas/index.js — verify exact path during execution):
- Top-level keys today: `drafts[]`, `continuity_decision`, optionally `draft` (legacy single), `per_field_confidence`
- BACK-03 adds: `capture_kind: 'paper_log' | 'physical_object_photo' | 'voice_note' | 'text' | null` (optional)

Few-shot pattern from system.js: existing examples use `tu_fewshot_1` / `tu_fewshot_2` / `tu_fewshot_3` tool_use_ids. Add `tu_fewshot_4` (paper-log positive) and `tu_fewshot_5` (physical-object-photo counter-example), each closed with a matching tool_result in the following user turn (Plan 07 Rule 1 bug fix at extractor.js:55-62 requires this — every tool_use needs an immediate tool_result).
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend extraction envelope schema with optional capture_kind enum</name>
  <files>src/agents/alerter/src/extraction/schemas/index.js, src/agents/alerter/test/extraction/schemas.test.js</files>
  <behavior>
    - Envelope schema (Zod or equivalent) accepts `capture_kind` as optional + nullable enum of the 4 values.
    - Existing envelopes (no capture_kind) validate without changes.
    - Envelope with `capture_kind: 'physical_object_photo'` validates.
    - Envelope with `capture_kind: 'banana'` (invalid value) fails validation.
    - The JSON schema emitted by zod-to-json-schema (consumed as Anthropic tool input_schema, see extractor.js Plan 07 fix at pipeline.js:200-ish) includes capture_kind as optional in the resulting tool spec.
  </behavior>
  <action>
    Locate the envelope schema in `src/agents/alerter/src/extraction/schemas/` (likely `index.js` — find via grep). Add `capture_kind: z.enum(['paper_log','physical_object_photo','voice_note','text']).nullable().optional()` to the top-level envelope object. If multiple envelope shapes exist (Plan 08 batch vs legacy single), add to BOTH or to the shared base — back-compat is non-negotiable. Add 3 schemas.test.js cases mirroring the behavior list above.
  </action>
  <verify>
    <automated>cd src/agents/alerter && npx jest test/extraction/schemas.test.js</automated>
  </verify>
  <done>Schema accepts/rejects per the cases; existing schema tests still pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add capture_kind instructions + 2 new few-shots to SYSTEM_PROMPT</name>
  <files>src/agents/alerter/src/extraction/prompts/system.js, src/agents/alerter/test/extraction/integration/capture-kind.test.js</files>
  <behavior>
    - SYSTEM_PROMPT gains 3-6 lines of instruction in the rules block explaining capture_kind values + when to use each. Key distinction: `paper_log` = handwritten/printed grid/rows OCR-able; `physical_object_photo` = photo of N physical bags/tubs/blocks of mushrooms (frame of objects, not a notebook page); `voice_note` = audio-derived caption-or-transcript; `text` = no media, text-only.
    - 2 new few-shot pairs added (with matching tool_result blocks per Plan 07 Rule 1):
        * `tu_fewshot_4`: paper-log photo example. Input: image of notebook page + caption. Output envelope: `capture_kind: 'paper_log'`, drafts.length >= 3 (genuine multi-row page).
        * `tu_fewshot_5`: physical-object-photo example explicitly modelled on the DT-tubs case. Input: image of 2 spawn-tubs + caption "DT tubs 0519 1 and 2". Output envelope: `capture_kind: 'physical_object_photo'`, drafts.length = 2, both per-draft confidence high (>= 0.8), asset_refs `260519_DT_1` / `260519_DT_2`.
    - Integration test (`capture-kind.test.js`) uses a mocked Anthropic client returning a fixed tool_use response — asserts the extractor packs `capture_kind` through to the returned envelope when present, and tolerates omission (returns the envelope without errors).
  </behavior>
  <action>
    Edit `prompts/system.js`:
    1. In the rules block (near lines 100-160 alongside the corpus_context rules), add instructions for capture_kind classification with the 4-value enum and a 1-sentence guideline per value.
    2. Append two new few-shot pairs after `tu_fewshot_3`. Follow the exact structural pattern of the existing pairs (text blocks → tool_use with `input_schema`-conformant input → tool_result in the next user turn with content `[{type:'text', text:'accepted'}]`). Reference the existing Plan 07 Rule 1 fix at extractor.js:55-62 — every tool_use MUST be immediately followed by a tool_result or Anthropic 400's.
    3. Use DT-tubs as the literal `tu_fewshot_5` content (caption "DT tubs 0519 1 and 2", capture_kind=physical_object_photo, 2 drafts with asset_refs 260519_DT_1 / _2). This makes the few-shot directly anti-correlate with the misclassification the todo flagged.

    Then add `test/extraction/integration/capture-kind.test.js` with:
    - One test: mocked Anthropic returns envelope with capture_kind=physical_object_photo → extractor.extract() result.drafts[0] envelope carries the field through.
    - One test: mocked Anthropic returns envelope WITHOUT capture_kind → extract() succeeds, capture_kind on returned envelope is null/undefined (back-compat).

    Do NOT modify pipeline.js routing — BACK-03 is back-compat / additive only (per D-BACK-03 lock).
  </action>
  <verify>
    <automated>cd src/agents/alerter && npx jest test/extraction/integration/capture-kind.test.js test/extraction/integration/</automated>
  </verify>
  <done>New integration tests green; existing extractor integration tests still pass; tu_fewshot_4 and tu_fewshot_5 each have matching tool_result blocks in the next user turn.</done>
</task>

</tasks>

<verification>
Full alerter test suite green. Particular care for any test that asserts on the exact text of SYSTEM_PROMPT or on the few-shot count — those will need adjustment to accommodate the additions.
</verification>

<success_criteria>
- `cd src/agents/alerter && npx jest` — full green
- Manual prompt audit: SYSTEM_PROMPT contains the capture_kind instruction block + 2 new few-shot pairs (tu_fewshot_4 paper-log, tu_fewshot_5 DT-tubs physical-object-photo)
- Live extractor accepts capture_kind in the envelope when the model emits it; tolerates absence (verified by hermetic 53-04 eval gate, separate plan)
</success_criteria>

<output>
Create `.planning/phases/53-extraction-prerequisites-year-context-shim-phase-38-batch-mode-fixes/53-03-SUMMARY.md` when done
</output>
