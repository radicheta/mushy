---
phase: 53-extraction-prerequisites-year-context-shim-phase-38-batch-mode-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/agents/alerter/src/capture-db.js
  - src/agents/alerter/src/capture.js
  - src/agents/alerter/src/extraction/pipeline.js
  - src/agents/alerter/test/capture-db.test.js
  - src/agents/alerter/test/extraction/pipeline.test.js
autonomous: true
requirements: [BACK-01]

must_haves:
  truths:
    - "A signal_capture row can carry a JSONB corpus_context (default null, back-compat)"
    - "When corpus_context is non-null on a capture, the extractor receives it as `corpusContext` and the model sees a `corpus_context: {...}` text block"
    - "Live captures with no corpus_context behave identically to today (null in, no extra prompt block)"
  artifacts:
    - path: "src/agents/alerter/src/capture-db.js"
      provides: "ADD COLUMN IF NOT EXISTS corpus_context jsonb on signal_capture; insertCapture accepts row.corpus_context"
      contains: "corpus_context"
    - path: "src/agents/alerter/src/extraction/pipeline.js"
      provides: "enqueue() reads captureCtx.corpusContext and forwards as extractor.extract({ ..., corpusContext })"
      contains: "corpusContext"
  key_links:
    - from: "src/agents/alerter/src/extraction/pipeline.js"
      to: "src/agents/alerter/src/extraction/extractor.js:67"
      via: "extract({ corpusContext }) -> JSON.stringify(corpusContext) prompt block"
      pattern: "corpusContext"
---

<objective>
Wire `corpus_context` end-to-end from `signal_capture` through `pipeline.enqueue()` to `extractor.extract()`. The extractor system prompt (`prompts/system.js` lines 117/134/174/239/335) and `extractor.buildInitialUserContent` (line 67) already consume `corpusContext` — this plan is pure plumbing: schema column + DB read/write + pipeline passthrough. Per D-BACK-01, the field is JSONB nullable, no farmer-facing surface; lives behind the bulk-backfill harness Phase 54 will build.

Purpose: Lets the Phase 54 backfill harness pin `{default_year: 2025, source: 'paper_log'}` on synthetic captures so the extractor stops hallucinating years on undated 2025 notebook pages. Closes BACK-01.

Output: A signal_capture row with non-null corpus_context produces an extractor call that includes the `corpus_context: {...}` prompt block; null behaves like today.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/53-extraction-prerequisites-year-context-shim-phase-38-batch-mode-fixes/53-CONTEXT.md
@src/agents/alerter/src/capture-db.js
@src/agents/alerter/src/capture.js
@src/agents/alerter/src/extraction/pipeline.js
@src/agents/alerter/src/extraction/extractor.js
</context>

<interfaces>
<!-- Extracted from codebase — executor should use these directly, no discovery needed. -->

Extractor signature (src/agents/alerter/src/extraction/extractor.js:116):
```
async extract({ captures, inFlightDraft, corpusContext, farmerCorrection } = {})
```
When `corpusContext` is a non-null object, `buildInitialUserContent` emits a text block
`corpus_context: ${JSON.stringify(corpusContext)}` (line 67). When falsy, no block emitted.

Schema pattern (capture-db.js:32-52):
```
await pool.query(`ALTER TABLE signal_capture ADD COLUMN IF NOT EXISTS <name> <type>`);
```
All recent columns added via additive ALTERs in the `initSchema`/migration function.

insertCapture signature (capture-db.js:73): row object with optional keys; column list explicit
in the INSERT — add `corpus_context` to both column list and VALUES, defaulting `row.corpus_context ?? null`.

Pipeline enqueue (pipeline.js:232, callsite capture.js:208): captureCtx is a plain object
built in capture.js; today does NOT carry corpus_context (live captures have none). Pipeline
must accept `captureCtx.corpusContext` (camelCase to match extractor) and pass it through to
`extractor.extract({ captures, inFlightDraft, corpusContext })` at pipeline.js:281.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add corpus_context JSONB column + insertCapture passthrough</name>
  <files>src/agents/alerter/src/capture-db.js, src/agents/alerter/test/capture-db.test.js</files>
  <behavior>
    - initSchema/migration runs the additive ALTER `ADD COLUMN IF NOT EXISTS corpus_context jsonb` (idempotent — second run is a no-op).
    - insertCapture(pool, {..., corpus_context: {default_year: 2025}}) persists the JSONB value; reading the row back yields the same object.
    - insertCapture(pool, {...}) without corpus_context writes NULL (back-compat with every existing caller).
    - Column is plain `jsonb` (not `jsonb NOT NULL`) — null is the default for all existing rows and all live captures.
  </behavior>
  <action>
    Append one ALTER line in the migration block alongside the existing `ADD COLUMN IF NOT EXISTS` family (per D-BACK-01 schema decision): `ALTER TABLE signal_capture ADD COLUMN IF NOT EXISTS corpus_context jsonb`. Extend `insertCapture` (capture-db.js:78) column list and VALUES with `corpus_context` bound to `row.corpus_context ?? null` — wrap with `JSON.stringify` only if pg requires it for jsonb binding (node-postgres handles js objects natively for jsonb; verify by test). Extend the existing capture-db.test.js with one round-trip case (insert with corpus_context, SELECT back, deep-equal) and one null-default case.
  </action>
  <verify>
    <automated>cd src/agents/alerter && npx jest test/capture-db.test.js</automated>
  </verify>
  <done>Both test cases pass; schema migration is idempotent on re-run.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Plumb corpus_context through capture.js -> pipeline.enqueue -> extractor.extract</name>
  <files>src/agents/alerter/src/capture.js, src/agents/alerter/src/extraction/pipeline.js, src/agents/alerter/test/extraction/pipeline.test.js</files>
  <behavior>
    - When capture.js receives a capture row with corpus_context set (Phase 54 harness will do this; live captures will not), it includes `corpusContext: <value>` in the captureCtx object passed to `extractionPipeline.enqueue`.
    - pipeline.enqueue passes `corpusContext: captureCtx.corpusContext || null` into extractor.extract().
    - With a mock extractor, a captureCtx with corpusContext={default_year:2025} causes extractor.extract to be called with corpusContext={default_year:2025}.
    - With no corpus_context on the captureCtx, extractor.extract is called with corpusContext absent/null (existing behavior — no regression to the 1000+ existing pipeline tests).
  </behavior>
  <action>
    In capture.js around line 208 (the `extractionPipeline.enqueue({...})` call), add a `corpusContext: corpusContext || null` field sourced from wherever the capture row is built (the row passed into `handle()` — surface `row.corpus_context` into a local `corpusContext` near where `farmosPerson` / `groupId` are destructured, mirroring the existing pattern). In pipeline.js:281, change `await extractor.extract({ captures, inFlightDraft: treatInFlight })` to `await extractor.extract({ captures, inFlightDraft: treatInFlight, corpusContext: captureCtx.corpusContext || null })`. Add 2 jest cases to pipeline.test.js asserting the mock extractor receives the value verbatim when set and null when omitted.
  </action>
  <verify>
    <automated>cd src/agents/alerter && npx jest test/extraction/pipeline.test.js test/capture.test.js</automated>
  </verify>
  <done>New passthrough cases green; existing pipeline + capture tests still pass.</done>
</task>

</tasks>

<verification>
Run the full alerter test suite — schema changes must not regress any neighbour. The added column is additive and nullable, so existing INSERTs continue to work without modification.
</verification>

<success_criteria>
- `cd src/agents/alerter && npx jest` — full green
- Manual: `psql $DATABASE_URL -c "\d signal_capture"` after migration shows `corpus_context | jsonb`
- A round-trip insert with `{corpus_context: {default_year: 2025, source: 'paper_log'}}` returns the same object
</success_criteria>

<output>
Create `.planning/phases/53-extraction-prerequisites-year-context-shim-phase-38-batch-mode-fixes/53-01-SUMMARY.md` when done
</output>
