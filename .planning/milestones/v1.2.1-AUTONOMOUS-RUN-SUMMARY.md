# v1.2.1 Autonomous Run — Saturday Morning Handoff

**Run window:** 2026-04-17 evening → 2026-04-18 early morning
**Authorization:** User-granted no-ask weekend scope ("surprise me with everything running")
**Milestone:** v1.2.1 — Hotfix: camera stall + sensor warmup

---

## TL;DR

- **Phase 14** (fc_camera idle-stall hotfix) — ✅ Shipped. Live soak passed: 9-second recovery vs the 8-hour stall that kicked this off yesterday.
- **Phase 15** (sensor warm-up grace period) — ✅ Shipped. Live soak passed: WARN→OK at 25s, zero spurious humidifier actuation during warm-up.
- **Phase 16** (system health panel — narrow) — ✅ Shipped automated gates. Two human-UAT items waiting for you to look at in the browser.

30 commits, all pushed to `origin/main`. No Co-Authored-By. No prod outages. fc1 is currently live with all three phases deployed.

---

## What shipped

### Phase 14 — camera stall fix
- Root cause confirmed live on fc1 (Path A — stale writer-local cache, fresh graph-level API)
- `fc_camera.py` gains a 1 Hz `_graph_poll` timer calling `node.count_subscribers()`
- 15/15 unit tests pass; `TestIdleToActiveRecovery` class added
- Bridge `/health` exposes `camera.last_frame_age_sec`
- Mission Control camera panel now has two status lights ("Feed live", "Camera subscribed") via a reusable `makeStatusLight` primitive
- Live 30-min soak on fc1: canonical-stall recovery in 9 seconds (hard SLA target was 10s)
- Full writeup: `.planning/phases/14-fc-camera-idle-stall-hotfix/14-SOAK-EVIDENCE.md`

### Phase 15 — sensor warmup grace
- `fc_controller.py` early-returns from control_loop during the first ~20 seconds
- New topic `/fc1/sensor_health` publishes `diagnostic_msgs/DiagnosticStatus` (TRANSIENT_LOCAL QoS) — state-change only, not per-tick noise
- `startup_grace_period: 20.0` in `fc_config.yaml`, overridable
- `package.xml` declares the new `diagnostic_msgs` dependency
- 29/29 controller tests pass; `TestStartupGracePeriod` class added
- `REQUIREMENTS.md` cleaned up — SENS-01 promoted out of "Out of Scope" and into active v1.2.1 (was double-listed — fixed)
- Live soak on fc1: WARN→OK transition at 25s post-restart; no humidifier actuation during grace

### Phase 16 — system health panel (narrow)
- Six-light horizontal strip in Mission Control as a new object under "Fruiting Chamber FC-1" → "System Health"
- Lights: Sensors, Camera feed, Humidifier, Bridge, Pi reachable, Grace
- Reuses Phase 14's `makeStatusLight` primitive verbatim
- Bridge gains WebSocket forwarding for `/fc1/sensor_health` and two new `/health` fields (`ros.connected`, `humidifier.last_msg_ts`)
- Automated smoke passed all 6 lights with live data

---

## What needs your eyeballs Saturday

### Phase 16 HUMAN-UAT items

File: `.planning/phases/16-system-health-panel/16-HUMAN-UAT.md`

1. **Visual smoke** — open Mission Control, expand "Fruiting Chamber FC-1", click "System Health". Confirm six lights render in one row with expected colors (Bridge/Pi/Humidifier/Sensors/Grace green, Camera grey since no one is viewing). No DevTools console errors.

2. **Decision: replay `sensor_health` on WS reconnect?** — Known gap: since `fc_controller` only publishes sensor_health on state transitions, a freshly-loaded MC page shows Sensors/Grace lights as **grey** until the next transition (typically within a minute). Two options:
   - (a) Ship as-is; grey-until-next-tick is acceptable; farmer learns the rhythm.
   - (b) Small bridge follow-up: cache the last sensor_health DiagnosticStatus and replay to new WebSocket clients on connect. ~20 LoC, ship in a decimal phase 16.1 if you want.

### Broader observations to consider

- The farmer's original camera-stall symptom ("i see black on MC") is now impossible to mistake for a frozen feed — the two lights in the camera panel will show red/stale before the image goes stale. Phase 14 closed the loop on both the bug AND the trust surface.
- Phase 12's idle-pulse-not-persisted design flaw (see `feedback_gap_over_noise.md` and `project_phase12_camera_stall.md` memories) is still open in 999.14 (camera history). Not urgent, but it's the other half of the camera trust story.
- During Phase 14's 30-min soak the Tailscale DERP relay hiccuped briefly (39s frame delivery gap at t=11min) — noted as data for future 4G/connectivity tuning. The current design handled it cleanly (brief grey, quick recovery).

---

## What was NOT done

- `/gsd:audit-milestone`, `/gsd:complete-milestone`, and `/gsd:cleanup` were skipped so you can decide whether to close v1.2.1 now or bundle Phase 16's human-UAT resolution + any Saturday-morning tweaks before archiving.
- Phase 999.14 (continuous camera persistence) was NOT touched — scope was deliberately kept to the v1.2.1 hotfix trio.
- No bridge `sensor_health` replay shim (see HUMAN-UAT #2).

---

## Commit landmarks (if you want to browse)

- `3e7d65c` — the 15-LoC fc_camera fix itself
- `1a984eb` — grace-gate logic in fc_controller
- `2188c11` — the six-light panel in plugin.js
- `4870c00`, `aae0586`, `f544053` — three phase-complete docs commits

30 commits total in the run, all on `main`, all pushed.

---

*Written 2026-04-18 during autonomous-run closeout. No Co-Authored-By.*
