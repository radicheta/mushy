---
milestone: v1.10
name: Order-Independent Writes (upsert-by-stable-identity)
audited: 2026-05-24
status: tech_debt
scores:
  requirements: 7/7
  phases: 1/1
  integration: n/a (single-phase milestone)
  flows: 1/1 (live-fire end-to-end)
gaps:
  requirements: []
  integration: []
  flows: []
tech_debt:
  - area: planning-bookkeeping
    items:
      - "REQUIREMENTS.md has no v1.10 section. UPSERT-01..07 are documented only in `.planning/phases/51-…/51-SPEC.md`. The 3-source cross-reference (VERIFICATION + SUMMARY + traceability) reduces to 2-source for this milestone."
      - "`.planning/phases/51-…/51-VALIDATION.md` frontmatter still reads `status: draft, nyquist_compliant: false, wave_0_complete: false` despite all UPSERT-01..07 verified and 1103/1103 hermetic tests green. Frontmatter not updated post-execution."
  - area: documented-deviations
    items:
      - "UPSERT-04 degraded from `412→retry→ConcurrencyExhausted` to soft-compare (revision_id pre/post). Planner-accepted per RESEARCH §3 (farmOS does not honor `If-Match`). Semantically equivalent: one-shot retry budget + structured non-throwing surface + `etag_source` audit field. Documented in VERIFICATION.md and VALIDATION.md."
      - "Live-fire log outcome reported `log.patched=11, log.created=0` rather than the SPEC's net-new expectation. This is correct B5-invariant convergence (one seeding log per child asset already existed on dev from a prior session). Strengthens rather than violates the zero-duplicate-POSTs criterion."
  - area: scope-boundaries-respected
    items:
      - "farm_group work not attempted (out of scope per SPEC)"
      - "2025-paper-scan ingestion not attempted (downstream consumer)"
      - "Observation-of-unknown-asset farmer-ack UX not wired (separate phase)"
      - "Non-`fungi`, non-`seeding` log paths untouched: commit-input, commit-activity, commit-observation, commit-harvest harvest-log still use `createLog`. Documented residual grep gate matches in VERIFICATION.md — explicitly out of scope per SPEC."
nyquist:
  compliant_phases: 0
  partial_phases: 1
  missing_phases: 0
  overall: PARTIAL
  notes: "Phase 51 VALIDATION.md exists but frontmatter never flipped to compliant. Empirical compliance is high — verifier confirmed all UPSERT-01..07 with concrete evidence — but the formal VALIDATION.md status field is stale. Run /gsd-validate-phase 51 to retro-audit and reconcile."
---

# Milestone v1.10 — Audit Report

**Milestone:** v1.10 Order-Independent Writes (upsert-by-stable-identity)
**Scope:** Phase 51 (single-phase milestone)
**Audited:** 2026-05-24
**Status:** `tech_debt` — no critical blockers, accumulated bookkeeping debt
**Score:** 7/7 requirements satisfied

---

## Per-Requirement Status

All 7 milestone-internal requirements (UPSERT-01..07, defined in `51-SPEC.md`) are satisfied per Phase 51's VERIFICATION.md. See `.planning/phases/51-order-independent-farmos-writes-upsert-by-stable-identity-se/VERIFICATION.md` for the canonical evidence trail.

| REQ | Status | Source | Evidence |
|-----|--------|--------|----------|
| UPSERT-01 `upsertFungiAsset` | ✓ satisfied | VERIFICATION.md §Per-Requirement Verdict | `assets.js:185-308`; commit callers migrated; 59 farmos tests green |
| UPSERT-02 `upsertLog` (seeding) | ✓ satisfied | VERIFICATION.md §Per-Requirement Verdict | `logs.js:149-319`; `LOG_STABLE_KEYS` table; live-fire `log.patched=11, log.created=0` |
| UPSERT-03 `_mergeAssetFields` | ✓ satisfied | VERIFICATION.md §Per-Requirement Verdict | `merge.js` 7-case rule table; 7 unit tests green |
| UPSERT-04 etag-guarded PATCH | ✓ satisfied (DEGRADED, planner-accepted) | VERIFICATION.md §Deviations | Soft-compare retry; `etag_source` audit field |
| UPSERT-05 stub-detection contract | ✓ satisfied | VERIFICATION.md §Per-Requirement Verdict | `isStubAsset` + `STUB_BACKFILL_MARKER`; 6 unit tests + property test 2 |
| UPSERT-06 hermetic property tests | ✓ satisfied | VERIFICATION.md §Per-Requirement Verdict | `upsert-property.test.js` 3 properties × 20 perms; all green |
| UPSERT-07 live-fire ship-gate | ✓ satisfied | `.planning/notes/2026-05-24-phase-51-live-fire.md` | dev farmOS 2026-05-24: 16 assets PATCHed / 0 created, 11 logs PATCHed / 0 created, zero duplicate UUIDs, 11/11 lineage walks green |

**Score: 7/7**

---

## Phase Status

| Phase | VERIFICATION.md | Status | Verdict |
|-------|-----------------|--------|---------|
| 51-order-independent-farmos-writes-upsert-by-stable-identity-se | ✓ present | passed | SHIP |

---

## Cross-Phase Integration

**Not applicable** — v1.10 contains only one phase. No cross-phase wiring to verify within this milestone.

The upsert layer's integration with upstream phases (commit-paths added/modified in v1.7/v1.8/v1.9) was verified within Phase 51 itself via:
- **Grep gate:** zero `createFungiAsset|resolveOrCreateAsset` references remain in `src/agents/alerter/src/farmos/commits/`
- **Key Links (Wiring) table** in VERIFICATION.md lines 80-93 enumerates each commit-path → upsert-primitive call site
- **Live-fire end-to-end:** replays the May-22 inoc session through `commit-seeding-session.js` → `upsertFungiAsset` + `upsertLog` against real dev farmOS

---

## E2E Flows

| Flow | Status | Evidence |
|------|--------|----------|
| Multi-parent inoc session → multiple seeding events arriving in arbitrary order → final farmOS state is byte-equivalent | ✓ passes | Property test 1: 20 random permutations of 3 inoc events byte-equivalent |
| Pre-stubbed ancestor + later real inoc → existing stub enriched in place, no duplicate mint | ✓ passes | Property test 2 + live-fire post-flight: 4 stub UUIDs byte-identical pre/post |
| Conflicting scalar (e.g. fungi_type SHI vs KOY) → structured conflict surfaced, no silent overwrite | ✓ passes | Property test 3 + `assets.test.js` "hit-with-conflicts path" |
| End-to-end live-fire against dev farmOS | ✓ passes | `scripts/live-fire-51.js` 2026-05-24: 27 audit events captured, PASS verdict |

---

## Tech Debt Summary

### Planning bookkeeping (non-blocking)

1. **REQUIREMENTS.md has no v1.10 section.** UPSERT-01..07 live only in `51-SPEC.md`. This milestone slipped past the requirements-table discipline that prior milestones (v1.7, v1.9) followed. For traceability hygiene, either:
   - Append a `# Milestone v1.10 Requirements` section to `REQUIREMENTS.md` before archival, OR
   - Accept SPEC-as-source-of-truth for single-phase milestones and document this convention in `PROJECT.md`.

2. **VALIDATION.md frontmatter stale.** Phase 51's `51-VALIDATION.md` still reads `status: draft, nyquist_compliant: false, wave_0_complete: false`. Empirical compliance is high (VERIFICATION.md confirms all sampling-rate cadences were respected and the live-fire receipt is committed), but the field-level flag was never flipped post-execution. Run `/gsd-validate-phase 51` to retro-reconcile, or update the frontmatter directly.

### Documented deviations (planner-accepted, no action required)

- **UPSERT-04 → soft-compare** (RESEARCH §3 finding #2; farmOS does not honor `If-Match`). Semantic equivalent preserved; `etag_source` audit field surfaces the degradation per write.
- **Live-fire log outcome strengthens (not weakens) the SPEC.** `log.patched=11` reflects pre-existing dev seeding logs from prior session — correct B5-invariant convergence.
- **Notes-field representation locked** to `\n---\n`-separated `plain_text` in CONTEXT.md; Wave-0 probe attests byte-fidelity through Drupal.

### Boundaries respected (out-of-scope tracking)

- Non-`fungi`/non-`seeding` log paths still use `createLog` (POST-only). Explicitly out of scope per SPEC UPSERT-02. Future milestone may migrate observation/activity/input/harvest logs if order-dependence pressure emerges in those paths.
- `farm_group` work, 2025-paper-scan ingestion, and observation-of-unknown-asset farmer-ack UX are downstream consumers / sibling milestones.

---

## Nyquist Coverage

| Phase | VALIDATION.md | Compliant | Action |
|-------|---------------|-----------|--------|
| 51 | exists | partial (stale frontmatter; empirical compliance high) | `/gsd-validate-phase 51` to reconcile, OR manually flip frontmatter post-archival |

---

## Final Verdict

**`tech_debt` — milestone met its definition of done; bookkeeping cleanup recommended before archival.**

The order-independence claim is proven by pure-function tests, property tests, and live-fire empirical convergence. The 2025-paper-scan backfill (the future write path that motivated this phase) now has a layer that will enrich the 4 in-prod stubs in place rather than minting parallel duplicates. The phase is ready to ship; the milestone is ready to archive.

Tech debt is bookkeeping (stale frontmatter, missing REQUIREMENTS.md section) — none of it blocks merge or production behavior. Resolve before or after archival as preferred.

---

*Audited: 2026-05-24T19:00:00Z*
*Auditor: Claude (gsd-audit-milestone)*
