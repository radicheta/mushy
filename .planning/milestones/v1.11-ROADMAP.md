# v1.11 — Extraction prereqs + 2025-paper backfill (ARCHIVED)

**Status:** ✅ Shipped 2026-06-14
**Phases:** 53, 54, 54.1, 55, 55B (5 phases, 19 plans, all complete)
**Git range:** `2f23b9e` (53-01, 2026-05-18) → `c073a62` (55B complete, 2026-06-14)

> Archived at v1.11 close. The live ROADMAP.md retains a collapsed one-line entry +
> `<details>` summary; this file holds the full phase detail.

## v1.11 2025-Notebook Backfill — PLANNED 2026-05-24

**Driver:** Phase 51 upsert layer (shipped 2026-05-24) structurally unblocks running the `/mnt/mossrock/shared/mushdatadump-prod/` 2025 paper-log corpus through the extraction+confirm+write pipeline without create-collisions against the May-22 stubs or any future captures. Today's prod write minted 4 stubs (`260304_SHI_5`, `260118_SHI_23`, `260118_SHI_26`, `260118_KOY_12`) carrying `STUB - awaits 2025-paper-scan backfill` markers — v1.11 is what makes them get *enriched* in place rather than just reused.

**Strategic role:** Generates the high-volume confirm corpus that v1.13 auto-commit narrowing structurally requires. Also closes the year-hallucination issue and the two open Phase 38 batch-mode findings filed 2026-05-24.

**Honors:** smoke-before-expensive-batch (5-10 pages first), persist-paid-results-default (per-call unique paths + JSONL append), real-data-before-ship-gate (real notebook scans are the ship gate), hard-rules-relaxed-when-farmer-is-santi (bulk-backfill auto-confirm under `--farmer=santi`).

### Phase 53: Extraction prerequisites — year-context shim + Phase 38 batch-mode fixes

**Goal:** Close the three known extraction bugs that would corrupt a notebook backfill before any batch run touches farmOS.

- [x] 53-01 — BACK-01 corpus_context JSONB column + pipeline/extractor plumbing (`bf721e2` + `9d25ec7`)
- [x] 53-02 — BACK-02 small-N multi-draft routing heuristic + DT-tubs regression (`9835caf`)
- [x] 53-03 — BACK-03 capture_kind prompt-only classifier + envelope schema + 2 few-shots (`52f0874` + `673c413`)
- [x] 53-04 — BACK-04 hermetic eval gate on hand-labeled 2025 pages; 8/8 green; Phase 54 unblocked (`a2467ea` + `cc95c8d`)

### Phase 54: Backfill harness + dev-farmOS smoke (≤20 pages)

**Goal:** A scripted harness ingests N notebook pages, runs them through extraction → confirm (auto-YES under `--bulk-backfill --farmer=santi`) → upsert into dev farmOS, with paid-LLM results persisted per-call. Corpus path corrected to `/mnt/slime-kingdom/shared/mushdatadump/jpeg/` (IMG_3775..IMG_3861).

- [x] 54-01 — backfill-notebook.js CLI core, prod-guard, santi-gate, page-range filter, synthetic-capture dispatch (BACK-05) (`bfcde26`)
- [x] 54-02 — bulk-backfill short-circuit + commit-router dispatch + summaries.log audit (BACK-06) (`99e3f98`)
- [x] 54-03 — paid-LLM observer hook + append-only responses.jsonl per run-id (BACK-07) (`f4923e4`)
- [x] 54-04 — receipt.md builder + Phase 51 upsert-stability check (BACK-08 part 1) (`31b31bf`)
- [x] 54-05 — Cycle 1 (5 pages) operator runbook + farmer checkpoint; SIGN-OFF completed 2026-06-07 (`31b31bf`)
- [x] 54-06 — Cycle 2 (20 pages) operator runbook + Phase 55 unlock decision (`31b31bf`)

### Phase 54.1: Strain-confirm before mint (INSERTED)

**Goal:** When extraction yields a fungi_type strain code that is NOT an exact match to the curated active strain set, hold it for a farmer double-check before minting a taxonomy term. Replaces the blind auto-mint that polluted dev farmOS with extraction variants (LIM/SHIITAKE/OYS for LIMA/SHI/POY).

- [x] 54.1-01 — Strain resolver: exact-match against curated 14-set (config.strains); KNOWN vs UNKNOWN, no fuzzy auto-resolve
- [x] 54.1-02 — Backfill path: hold unknown-strain drafts as needs_review + batched Signal confirm + follow-up mint/remap/commit
- [x] 54.1-03 — Live-capture path: per-encounter strain ask-back via Phase 39 confirm-loop

### Phase 55: Full corpus run + receipt

**Goal:** Tooling to run the full 2025 notebook corpus to dev farmOS, generate a receipt of every asset/log created or patched, decide whether to promote any subset to prod. (The run itself stays parked behind GA2 / Cycle-2 farmer sign-off.)

- [x] 55-01 — harness --all-pages + build-backfill-receipt (buildUuidJsonl/computePerShapeStats) + notes copy-out (BACK-09, BACK-10)
- [x] 55-02 — 55-FULL-CORPUS-RUNBOOK.md (GA1 isolation + smoke-before-full + crash recovery) + 55-PROMOTION-DECISION.md dev-only default (BACK-11)

### Phase 55B: Fidelity / corpus-unblock (cross-check + F1/F2 session reconcile)

**Goal:** Land the blockers before the parked full-corpus run is safe. (1) Commit-time fidelity cross-check that HOLDS every entry not exact-verified against the per-page CSV reading (needs_review, never hard-reject). (2) F1+F2 reconcile surface: backfill per-block logs/assets group under the inoc-session group asset with source notebook page image(s) attached, so a human reconciles a session 1:1 against the physical notebook.

- [x] 55B-01 — Wave 0: RED test scaffolds (fidelity/aggregate/image) + A1 dev smoke probe (FIDELITY-01/02, SESSION-01/02)
- [x] 55B-02 — Commit-time CSV fidelity hold gate (buildCsvBudget + 3-branch hold) (FIDELITY-01/02); gate-wiring fix `96d1cd0`
- [x] 55B-03 — Session routing (aggregateSeedingDraftsToSessionJson) + page-image attach via field-scoped route (SESSION-01/02/03); A1' `bbd9212`
- [x] 55B-04 — 5-page GA1-isolated re-smoke runbook + live gate (IMG_3776 mode-2 regression guard) (SMOKE-01, SESSION-03)

## Milestone Summary

**Delivered:** The 2025-notebook backfill pipeline end-to-end — extraction bug fixes,
a santi-gated bulk-backfill harness with persisted paid-LLM results, strain-confirm before
mint, full-corpus tooling + receipt, and a commit-time CSV fidelity cross-check whose live
re-smoke proved the 2026-06-07 POY-as-KOY silent misattribution is now caught and held.

**Key decisions:**
- BACK-08 stub-enrichment sub-clause resolved N/A (May-22 stubs postdate the 2025 notebook); substituted intra-cycle upsert-stability against the Phase 51 contract.
- A1 PATCH-associates-files route falsified live; replaced by field-scoped `/api/asset/group/{uuid}/image` (A1', A1-SMOKE PASS).
- Fidelity gate HOLDS (needs_review), never hard-rejects — the ground-truth CSV is a fallible 2nd reading.
- 55B-04 checkpoint closed on the hard-gate PASS; live F2 reconcile (D-03) carried as a tracked human-needed follow-on.

**Tech debt / carried forward:**
- Full 73-page corpus run parked (Phase-55/GA2, Cycle-2 farmer sign-off).
- Live F2 reconcile D-03 (`55B-HUMAN-UAT.md`).
- Strain gate unwired in the backfill driver (CR-02/WR-06); needs a curated-strain source decision.
- Session `qty>1` whole-page rollback (CR-01) — fix before full run.
- No `55B-SECURITY.md` (security_enforcement on) — secure-phase not run.

**Verification:** 55B `human_needed` (12/13 must-haves, 1 override); alerter Jest suite 1397 pass / 9 skipped.
