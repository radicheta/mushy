---
phase: 30
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/mission-control/bridge/src/control_param.js
  - src/mission-control/bridge/src/control_persist.js
  - src/mission-control/bridge/test/control_param.test.js
  - src/mission-control/bridge/test/control_persist.test.js
autonomous: true
requirements:
  - SCHED-01
tags:
  - bridge
  - allowlist
  - validator
  - jest
must_haves:
  truths:
    - "control_param.js ALLOWLIST contains a `schedule_windows` entry of type T_STRING with a validate function that mirrors fc_core/scheduler.py rules: parse JSON, require array, each element has {start,end,mode} with HH:MM regex and `mode` ∈ DECLARED_MODES."
    - "Bridge POST /control/param with `param='schedule_windows', value='[]'` returns ok=true and round-trips to fc_controller via SetParameters as a STRING."
    - "Bridge POST /control/param with malformed JSON / missing key / bad HH:MM / unknown mode returns 400 with rejected_param='schedule_windows' and a reason that names the failure (atomicity: whole batch rejected)."
    - "Bridge POST /control/persist with `schedule_windows` writes the value flat into `fc_controller.ros__parameters.schedule_windows` of `runtime_overrides.yaml` (Phase 28 D-17 mergeOverlay path — JSON string preserved verbatim)."
    - "Existing Phase 28/29 allowlist behavior unchanged (no entries removed, all prior tests still pass)."
  artifacts:
    - path: src/mission-control/bridge/src/control_param.js
      provides: "schedule_windows allowlist entry + validator helper"
      contains: "schedule_windows"
    - path: src/mission-control/bridge/src/control_persist.js
      provides: "schedule_windows reachability via shared cp.validate (no separate code path needed — control_persist already calls cp.validate)"
      contains: "schedule_windows"
    - path: src/mission-control/bridge/test/control_param.test.js
      provides: "Jest tests covering accept + 5 reject paths for schedule_windows"
      contains: "schedule_windows"
    - path: src/mission-control/bridge/test/control_persist.test.js
      provides: "Jest test confirming schedule_windows persists into runtime_overrides.yaml as a flat string under fc_controller.ros__parameters"
      contains: "schedule_windows"
  key_links:
    - from: control_param.js ALLOWLIST IIFE
      to: new entry `a['schedule_windows'] = entrySchedule()` with type T_STRING
      via: helper function `entrySchedule()` returning `{ type: T_STRING, validate(v) }`
      pattern: "schedule_windows"
    - from: control_persist.js handler
      to: cp.validate('schedule_windows', value)
      via: existing `for (const { param, value } of entries) { const v = cp.validate(...) }` loop — no edit required, allowlist extension is sufficient
      pattern: cp.validate
---

<objective>
Extend the bridge allowlist (Phase 28 D-17) to permit `schedule_windows` writes via Layer 1 (`POST /control/param`) and Layer 2 (`POST /control/persist`). Add JS-side validation that mirrors the rclpy validator from Plan 30-01 (defense in depth — T-28-09 pattern). Mirror the Phase 28-05/28-06 test shapes for the new param.

Purpose: closes the runtime-edit half of SCHED-01. With this plan landed, farmOS can POST a schedule JSON to the bridge and have it (a) hot-applied via SetParameters and (b) persisted into `runtime_overrides.yaml` for restart survival — same two-layer pattern Phase 28 established for mode/band edits.

Output: bridge image rebuilds with the new allowlist entry; jest GREEN; no regressions in existing 156-test suite.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@./CLAUDE.md
@.planning/phases/30-time-of-day-mode-scheduling/30-CONTEXT.md
@.planning/phases/28-mode-primitive-2-baseline-modes-fruiting-pinning-runtime-con/28-CONTEXT.md

<interfaces>
<!-- control_param.js existing structure (verified by reading the file 2026-05-08): -->
<!-- Line 17:    const ALLOWED_NODES = new Set(['fc_controller']); -->
<!-- Lines 20-23: T_BOOL=1, T_INTEGER=2, T_DOUBLE=3, T_STRING=4 -->
<!-- Line 26:    const DECLARED_MODES = ['fruiting', 'pinning'];   // EXPORT — reuse for schedule mode validation -->
<!-- Lines 87-126: const ALLOWLIST = (() => { ... })();  Add new key inside this IIFE. -->
<!-- Lines 128-132: function validate(param, value) { return ALLOWLIST[param].validate(value); } -->
<!-- Lines 134-144: function toParamValue — already handles T_STRING via {type: T_STRING, string_value: String(value)} -->
<!-- Line 248: module.exports = { ALLOWLIST, DECLARED_MODES, MAX_PARAMS_PER_REQUEST, validate, toParamValue, makeHandler }; -->

<!-- control_persist.js relies on control_param.js's validate via `cp.validate(...)` (line 72). -->
<!-- mergeOverlay (lines 27-36) writes flat dotted keys under fc_controller.ros__parameters — schedule_windows lands as a top-level key (no dotted path), which is exactly what we want. -->

<!-- Test patterns (verified by reading control_param.test.js head): -->
<!-- describe('validate', ...) with test.each blocks; expect(validate(p,v)).toEqual({ ok: true }) | expect(r.ok).toBe(false) -->

<!-- New helper to add to control_param.js (insert before the ALLOWLIST IIFE): -->
const HHMM_RE = /^([01][0-9]|2[0-3]):[0-5][0-9]$/;
function entrySchedule() {
    return {
        type: T_STRING,
        validate: (v) => {
            if (typeof v !== 'string') {
                return { ok: false, reason: 'schedule_windows must be a JSON-encoded string' };
            }
            let parsed;
            try { parsed = JSON.parse(v); }
            catch (e) { return { ok: false, reason: `schedule_windows: invalid JSON: ${e.message}` }; }
            if (!Array.isArray(parsed)) {
                return { ok: false, reason: 'schedule_windows: must be a JSON array' };
            }
            for (let i = 0; i < parsed.length; i++) {
                const w = parsed[i];
                if (!w || typeof w !== 'object' || Array.isArray(w)) {
                    return { ok: false, reason: `schedule_windows[${i}]: must be an object` };
                }
                for (const k of ['start', 'end', 'mode']) {
                    if (!(k in w)) {
                        return { ok: false, reason: `schedule_windows[${i}]: missing key ${k}` };
                    }
                }
                if (!HHMM_RE.test(w.start)) {
                    return { ok: false, reason: `schedule_windows[${i}].start: must be HH:MM (got ${JSON.stringify(w.start)})` };
                }
                if (!HHMM_RE.test(w.end)) {
                    return { ok: false, reason: `schedule_windows[${i}].end: must be HH:MM (got ${JSON.stringify(w.end)})` };
                }
                if (!DECLARED_MODES.includes(w.mode)) {
                    return { ok: false, reason: `schedule_windows[${i}].mode: ${JSON.stringify(w.mode)} not in ${JSON.stringify(DECLARED_MODES)}` };
                }
            }
            return { ok: true };
        },
    };
}
// Then inside the ALLOWLIST IIFE, after a['active_mode'] = ...:
a['schedule_windows'] = entrySchedule();
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add schedule_windows allowlist + jest tests in control_param</name>
  <files>src/mission-control/bridge/src/control_param.js, src/mission-control/bridge/test/control_param.test.js</files>
  <read_first>
    - src/mission-control/bridge/src/control_param.js (full — confirm helper-fn placement and IIFE shape)
    - src/mission-control/bridge/test/control_param.test.js (existing test naming, describe/test.each shape, mock setRequestImpl pattern at top of file)
    - .planning/phases/30-time-of-day-mode-scheduling/30-CONTEXT.md (D-01..D-04, D-13, D-15)
    - .planning/phases/28-mode-primitive-2-baseline-modes-fruiting-pinning-runtime-con/28-05-SUMMARY.md (Phase 28-05 outcome — confirm assertion patterns)
  </read_first>
  <behavior>
    Tests added to control_param.test.js (in the existing `describe('validate', ...)` block, plus a new `describe('schedule_windows', ...)` block):
    - test('schedule_windows=[] empty array → ok'): expect(validate('schedule_windows','[]')).toEqual({ ok: true }).
    - test('schedule_windows valid two-window → ok'): valid two-window JSON with fruiting+pinning → ok.
    - test('schedule_windows wraparound window → ok'): `[{"start":"22:00","end":"06:00","mode":"pinning"}]` → ok (bridge does NOT reject end<start; that's a valid wraparound per D-02).
    - test('schedule_windows malformed JSON → reject'): value='{not json' → ok=false, reason matches /JSON/.
    - test('schedule_windows not an array → reject'): value='{"start":"06:00"}' → ok=false, reason matches /array/.
    - test('schedule_windows missing key → reject'): value='[{"start":"06:00","end":"22:00"}]' → ok=false, reason matches /missing key mode/.
    - test('schedule_windows bad HH:MM → reject'): value='[{"start":"6:00","end":"22:00","mode":"fruiting"}]' → ok=false, reason matches /HH:MM/.
    - test('schedule_windows bad minutes → reject'): value='[{"start":"06:60","end":"22:00","mode":"fruiting"}]' → ok=false.
    - test('schedule_windows hour out of range → reject'): value='[{"start":"24:00","end":"22:00","mode":"fruiting"}]' → ok=false.
    - test('schedule_windows unknown mode → reject'): value='[{"start":"06:00","end":"22:00","mode":"composting"}]' → ok=false, reason matches /declared/ or /not in/.
    - test('schedule_windows non-string value → reject'): value=42 (raw number) → ok=false.
    - test('schedule_windows element not an object → reject'): value='["fruiting"]' → ok=false.

    Plus a handler-level integration test mirroring the active_mode pattern in the existing suite:
    - test('POST /control/param schedule_windows=[] forwards as STRING via SetParameters'): mock the rclnodejs sendRequest to capture the request payload; assert `parameters[0].name==='schedule_windows'`, `parameters[0].value.type===T_STRING`, `parameters[0].value.string_value==='[]'`, response 200 + `applied[0].param==='schedule_windows'`.
  </behavior>
  <action>
    1. **RED:** Add the 12 validate tests + 1 handler test to `control_param.test.js`. Run `npm test -- control_param` → expect new tests to fail (validator not yet declared, returns "param schedule_windows not allowlisted").

    2. **GREEN:** In `src/mission-control/bridge/src/control_param.js`:
       - Above the `ALLOWLIST = (() => {...})()` IIFE, insert the `HHMM_RE` constant + `entrySchedule()` helper (full body shown in `<interfaces>` above).
       - Inside the IIFE, immediately after `a['active_mode'] = {...}` (around line 90-95), add `a['schedule_windows'] = entrySchedule();`.
       - No changes to `toParamValue`, `validate`, `makeHandler` — they're type-agnostic.
       - No `module.exports` changes needed (consumers use `cp.validate` / `ALLOWLIST` indirectly).

    3. Run `npm test -- control_param` again. Expect 12 validator + 1 handler test PASS plus all existing tests PASS (no regressions).

    4. Confirm DECLARED_MODES is in scope inside `entrySchedule()` — it's a module-level const declared on line 26 above the helper, so closure access is automatic.
  </action>
  <verify>
    <automated>cd src/mission-control/bridge && npm test -- --testPathPattern=control_param</automated>
  </verify>
  <done>
    All control_param.test.js tests PASS (existing + 13 new). `grep -c "schedule_windows" src/mission-control/bridge/src/control_param.js` ≥ 2 (helper export-ish + ALLOWLIST entry).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Confirm Layer 2 persist round-trips schedule_windows + jest test</name>
  <files>src/mission-control/bridge/test/control_persist.test.js</files>
  <read_first>
    - src/mission-control/bridge/src/control_persist.js (full — verify cp.validate is the only allowlist gate and mergeOverlay treats top-level keys correctly)
    - src/mission-control/bridge/test/control_persist.test.js (existing test patterns + the in-memory transport mock used by other tests)
    - .planning/phases/28-mode-primitive-2-baseline-modes-fruiting-pinning-runtime-con/28-06-SUMMARY.md (Phase 28-06 outcome — overlay layout)
  </read_first>
  <behavior>
    - test('POST /control/persist schedule_windows writes flat key to fc_controller.ros__parameters'): use the in-memory transport mock from existing tests; POST `{node:'fc_controller', param:'schedule_windows', value:'[{"start":"06:00","end":"22:00","mode":"fruiting"}]'}`; expect 200; expect transport.atomicWrite called once with a YAML body that, when parsed, has `parsed.fc_controller.ros__parameters.schedule_windows === '[{"start":"06:00","end":"22:00","mode":"fruiting"}]'` (the JSON STRING preserved verbatim, NOT pre-parsed into a YAML list).
    - test('POST /control/persist schedule_windows malformed JSON → 400 (no overlay write)'): value='{not json'; expect 400; expect transport.atomicWrite NOT called.
    - test('POST /control/persist schedule_windows merges with existing overlay'): seed transport with an existing overlay containing `modes.fruiting.target_humidity: 0.96`; POST schedule_windows; assert merged overlay still has the prior key AND the new key.
  </behavior>
  <action>
    1. **RED:** Add the 3 tests above. Run `npm test -- control_persist` → expect (a) the malformed-JSON test to already pass (cp.validate gates it) and (b) the round-trip + merge tests to pass once Task 1 lands the allowlist entry. If they don't all pass after Task 1, debug — control_persist.js requires no further code changes per the `<interfaces>` analysis.

    2. **GREEN:** Run `npm test -- control_persist`. Expect all new tests + existing tests PASS.

    3. Run the FULL bridge test suite: `npm test`. Expect ZERO regressions vs. baseline (Phase 29 = 156 tests; this plan adds 16 new = 172 expected).
  </action>
  <verify>
    <automated>cd src/mission-control/bridge && npm test</automated>
  </verify>
  <done>
    Full bridge jest suite PASS, including 13 new control_param tests + 3 new control_persist tests. No regressions. `grep "schedule_windows" src/mission-control/bridge/test/control_persist.test.js` returns the new test names.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| farmOS HTTP client → bridge `/control/param` | Untrusted input; allowlist + validator gate it. |
| farmOS HTTP client → bridge `/control/persist` | Untrusted input; same allowlist (cp.validate) + fc_buffer path allowlist (Phase 28 T-28-20). |
| bridge → fc_controller (SetParameters) | Defense-in-depth — Plan 30-01 validator re-checks (T-28-09 pattern). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-06 | Tampering | bridge POST schedule_windows | mitigate | `entrySchedule()` validator parses JSON, asserts array, validates HH:MM regex per element, validates `mode ∈ DECLARED_MODES`. Defense in depth with controller-side validator. Covered by 12 explicit reject-tests. |
| T-30-07 | Information disclosure | error reasons leak schedule structure | accept | Validator reasons include the exact rejected value (e.g. `"6:00"`); same disclosure pattern as Phase 28 (`band_low must be in [0,1] (got 1.5)`). farmOS is internal-only — no PII. |
| T-30-08 | DoS | giant schedule JSON consumed by SetParameters | mitigate | MAX_PARAMS_PER_REQUEST=20 already caps the batch size at the bridge (line 31). The schedule JSON itself rides as a single STRING param value — JSON.parse cost on a sane farmOS submission (<10 windows) is microseconds. If a future attacker submits a 100MB JSON string, Express body-parser limit (default 100kb) gates it before this code runs. No new mitigation required. |
| T-30-09 | Tampering | overlay file written by fc_buffer | accept | Phase 28 T-28-20 path-allowlist on fc_buffer side already restricts `/control/persist` writes to `/var/lib/fc-core/runtime_overrides.yaml`. New param key inherits this. |
</threat_model>

<verification>
1. `npm test -- --testPathPattern=control_param` PASS (13 new tests).
2. `npm test -- --testPathPattern=control_persist` PASS (3 new tests).
3. `npm test` full suite — zero regressions (>= 172 PASS).
4. Static check: `grep "schedule_windows" src/mission-control/bridge/src/control_param.js` returns at least 2 lines (helper + ALLOWLIST entry).
5. Manual smoke (deferred to Plan 30-03): `curl -X POST localhost:3000/control/param -d '{"node":"fc_controller","param":"schedule_windows","value":"[]"}'` returns 200 with `{ok: true}`.
</verification>

<success_criteria>
- [ ] control_param.js ALLOWLIST contains `schedule_windows` entry typed as T_STRING with full-shape validation.
- [ ] control_persist.js requires no code changes (existing cp.validate pathway covers the new key).
- [ ] All 13 new control_param tests PASS, all 3 new control_persist tests PASS.
- [ ] Full bridge jest suite GREEN with no regressions.
- [ ] Validator messages include the offending value (debuggability).
- [ ] Plan 30-03 can issue a single Layer 1 + Layer 2 curl pair against the live bridge to deploy a schedule.
</success_criteria>

<output>
After completion, create `.planning/phases/30-time-of-day-mode-scheduling/30-02-SUMMARY.md`
</output>
