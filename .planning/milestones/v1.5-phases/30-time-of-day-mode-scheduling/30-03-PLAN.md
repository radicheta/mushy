---
phase: 30
plan: 03
type: execute
wave: 2
depends_on:
  - "30-01"
  - "30-02"
files_modified:
  - src/chambers/fc-core/config/fc_config.yaml
autonomous: false
requirements:
  - SCHED-01
  - SCHED-02
  - SCHED-03
tags:
  - config-yaml
  - deploy
  - smoke
  - checkpoint
must_haves:
  truths:
    - "fc_config.yaml under fc_controller.ros__parameters declares `schedule_windows: \"[]\"` as the explicit default — preserves SCHED-03 backward compat with a documented config line (not a hidden code default)."
    - "fc-core systemd unit on fc1 runs the rebuilt code and a fresh `ros2 param get /fc_controller schedule_windows` returns `'[]'` (the JSON string)."
    - "Layer 1 hot edit smoke proves end-to-end: POST schedule_windows to bridge → ros2 param shows new value within ~1s → /fc1/control/current_mode publishes a `source='scheduler'` transition at the next 30s tick (or sooner if startup-aligned)."
    - "Layer 2 persist smoke proves end-to-end: POST /control/persist with schedule_windows → `/var/lib/fc-core/runtime_overrides.yaml` on fc1 contains the value → restart fc-core → param survives."
    - "Farmer-facing rollback recipe documented in summary: `ros2 param set /fc_controller schedule_windows '[]'` (Layer 1) + bridge persist call to clear (Layer 2)."
  artifacts:
    - path: src/chambers/fc-core/config/fc_config.yaml
      provides: "schedule_windows default declaration under fc_controller.ros__parameters"
      contains: "schedule_windows"
    - path: .planning/phases/30-time-of-day-mode-scheduling/30-03-SUMMARY.md
      provides: "deploy log + smoke evidence + farmer attestation status + rollback recipe"
      contains: "deploy"
  key_links:
    - from: fc_config.yaml fc_controller.ros__parameters block
      to: declare_parameters('schedule_windows', '[]') decl in fc_controller.py (Plan 30-01)
      via: "ROS2 standard fc_config.yaml → declare_parameters bind"
      pattern: "schedule_windows"
    - from: live bridge POST /control/param
      to: live fc_controller SetParameters service
      via: "Phase 28 D-17 Layer 1 hot path"
      pattern: "/control/param"
---

<objective>
Wire the new param into `fc_config.yaml` (so the default is documented + visible to farmers in the same place every other tunable lives), deploy the rebuilt fc-core + bridge to fc1, and prove end-to-end that the scheduler works on real hardware.

Purpose: closes the deployment half of SCHED-01/02/03. This is the only plan that touches the live system; it carries the human checkpoint that gates the phase as "shipped".

Output: fc1 runs Phase 30 code; smoke evidence captured in summary; farmer attestation pending or PASS.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@./CLAUDE.md
@.planning/phases/30-time-of-day-mode-scheduling/30-CONTEXT.md
@.planning/phases/30-time-of-day-mode-scheduling/30-01-PLAN.md
@.planning/phases/30-time-of-day-mode-scheduling/30-02-PLAN.md
@src/chambers/fc-core/config/fc_config.yaml
@.planning/phases/28-mode-primitive-2-baseline-modes-fruiting-pinning-runtime-con/28-07-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add schedule_windows: "[]" default to fc_config.yaml</name>
  <files>src/chambers/fc-core/config/fc_config.yaml</files>
  <read_first>
    - src/chambers/fc-core/config/fc_config.yaml (full — find the fc_controller.ros__parameters block; insert AFTER the modes.* block and BEFORE the alerter block, so the schedule sits adjacent to the data it references)
    - .planning/phases/30-time-of-day-mode-scheduling/30-CONTEXT.md (D-01, D-05)
  </read_first>
  <action>
    In `src/chambers/fc-core/config/fc_config.yaml`, inside the `fc_controller.ros__parameters:` block, immediately after the existing `modes.pinning.t_target: .nan` line and BEFORE the `# Phase 29 ALRT-10 (D-08) — tuned values` comment, insert:

    ```yaml
        # Phase 30 SCHED-01/03: time-of-day mode schedule.
        # JSON-encoded list of {start, end, mode} windows with HH:MM local times.
        # Empty array = scheduling disabled (single-mode fallback per SCHED-03).
        # Edit live via POST /control/param; persist via POST /control/persist.
        # Wraparound supported (end<start = crosses midnight); half-open [start,end).
        schedule_windows: "[]"
    ```

    Indentation MUST match the surrounding lines (4 spaces of indent inside `fc_controller.ros__parameters:`). Do NOT touch any other key.

    After the edit, validate the YAML parses: `python -c "import yaml; yaml.safe_load(open('src/chambers/fc-core/config/fc_config.yaml'))"` returns no error.
  </action>
  <verify>
    <automated>cd /mnt/slime-kingdom/opt/mushy && python -c "import yaml; d=yaml.safe_load(open('src/chambers/fc-core/config/fc_config.yaml')); assert d['fc_controller']['ros__parameters']['schedule_windows'] == '[]', 'schedule_windows default is not the empty-array string'; print('OK')"</automated>
  </verify>
  <done>
    fc_config.yaml parses; `schedule_windows: "[]"` is present under `fc_controller.ros__parameters`. No other config keys changed (`git diff src/chambers/fc-core/config/fc_config.yaml` shows only the inserted block).
  </done>
</task>

<task type="auto">
  <name>Task 2: Build + deploy to fc1 + verify scheduler is live (no schedule active)</name>
  <files></files>
  <read_first>
    - .planning/phases/28-mode-primitive-2-baseline-modes-fruiting-pinning-runtime-con/28-07-SUMMARY.md (deploy procedure used for Phase 28; SSH path is `ssh ubuntu@172.16.10.5` per memory `feedback_ssh_tailscale`)
    - .planning/phases/29-alerter-mode-awareness-cooldown-tuning/29-07-SUMMARY.md (most recent live deploy — confirm same recipe still applies)
    - CLAUDE.md (deploy method: edit→commit→push fc1/prod→deploy.sh; boot-time fc-update.service also pulls)
    - Memory: `feedback_deploy_method` (Pi deploy is git, branch fc1/prod) — DO NOT scp; commit + push fc1/prod, then run deploy.sh on fc1
    - Memory: `feedback_diff_repo_vs_pi_systemd` — diff repo vs. /etc/systemd before clobbering hand-fixes (this task does NOT touch systemd, but be vigilant if the deploy.sh script touches them)
    - Memory: `feedback_fc1_remote_action_preflight_protocol` — gating checklist for fc1 remote actions
  </read_first>
  <action>
    Local machine steps:
    1. `cd /mnt/slime-kingdom/opt/mushy && git status` — confirm only Phase 30 files modified (30-01/02/03 PLAN.md, scheduler.py, fc_controller.py, fc_config.yaml, jest + pytest changes).
    2. `colcon build --packages-select fc_core --symlink-install` (local sanity build — must succeed before push).
    3. Run the full pytest + jest suites locally one more time (`pytest src/chambers/fc-core/fc_core/test/` + `cd src/mission-control/bridge && npm test`) — both GREEN.
    4. Commit + push to `fc1/prod`:
       ```bash
       git add src/chambers/fc-core/fc_core/scheduler.py src/chambers/fc-core/fc_core/fc_controller.py src/chambers/fc-core/fc_core/test/ src/chambers/fc-core/config/fc_config.yaml src/mission-control/bridge/src/control_param.js src/mission-control/bridge/test/control_param.test.js src/mission-control/bridge/test/control_persist.test.js
       git commit -m "feat(phase-30): time-of-day mode scheduling — controller scheduler + bridge allowlist + yaml default"
       git push origin fc1/prod   # only if main and fc1/prod track each other; otherwise push to main and let fc-update.service pull
       ```
    5. Bridge rebuild: from repo root, `docker compose up -d --build bridge` (live stack on elder-plops; per CLAUDE.md "always pass --build"). Wait for bridge container to come up; confirm `docker logs --tail 50 mushy-bridge | grep -i "listening\|ready\|started"`.

    fc1 steps (via `ssh ubuntu@172.16.10.5`):
    6. Pre-deploy preflight (per memory `feedback_fc1_remote_action_preflight_protocol`): confirm `tailscale status | head` healthy, `ip route show default` includes wg0, `systemctl is-active fc-core` is `active`. If any fails, STOP and surface to operator before proceeding.
    7. `cd ~/mushroom_farm_ws && git pull` → expect to fast-forward to the new commit. Confirm `git log -1 --oneline` shows the Phase 30 commit hash.
    8. `bash scripts/pi-deploy/deploy.sh` (or whatever the standard pi-deploy entry is — verify via memory `feedback_deploy_method` + by listing `scripts/pi-deploy/`). Wait for completion.
    9. `sudo systemctl restart fc-core` (only if deploy.sh doesn't already restart it). Wait 30s, then `journalctl -u fc-core -n 100 --no-pager` and confirm:
       - "Fruiting Chamber Controller Node Started" line present.
       - No tracebacks or "schedule_windows" rejection at startup.
       - Look for any `[scheduler]` log lines — empty schedule should produce zero transition logs (gap-debounce code path doesn't fire on empty schedule per Plan 30-01 logic).
    10. Verify the new param is live: `ros2 param get /fc_controller schedule_windows` → returns `'[]'`.
    11. Verify mode topic still publishes: `ros2 topic echo /fc1/control/current_mode --once` → expect `name: 'fruiting'`, `source: 'config_default'`.
  </action>
  <verify>
    <automated>ssh ubuntu@172.16.10.5 "ros2 param get /fc_controller schedule_windows" 2>&1 | grep -F "'[]'"</automated>
  </verify>
  <done>
    fc1 runs the new code; `ros2 param get /fc_controller schedule_windows` returns `'[]'`; fc-core unit healthy; current_mode still publishes fruiting; no validator reject errors in journalctl.
  </done>
</task>

<task type="auto">
  <name>Task 3: End-to-end smoke — Layer 1 hot apply + Layer 2 persist + restart survival</name>
  <files></files>
  <read_first>
    - .planning/phases/30-time-of-day-mode-scheduling/30-CONTEXT.md (D-09 startup alignment, D-10 manual override semantics, D-13/D-14 layer paths)
    - bridge URL: from `docker-compose.yml`/`docker-compose.override.yml` at repo root, the bridge typically binds 8080 and exposes `/control/*` — verify via `curl http://localhost:8080/health` from elder-plops before testing.
  </read_first>
  <action>
    Setup: pick a "now-relative" schedule that will provoke a visible transition shortly. Capture current time on fc1: `ssh ubuntu@172.16.10.5 'date "+%H:%M"'` → `HH:MM`. Build a 2-window schedule that puts the chamber into `pinning` for 2 minutes from now, then back to `fruiting`. (Use `pinning` since it is the existing declared baseline mode and farmer-acknowledged passive — this avoids surprising the chamber.)

    Example: if now is 14:23, schedule = `[{"start":"14:25","end":"14:27","mode":"pinning"},{"start":"14:27","end":"14:25","mode":"fruiting"}]`. The second window wraps around to cover the rest of the day. Adjust HH:MM to whatever is "two minutes from now" at execution time.

    **Layer 1 hot apply smoke:**
    1. `curl -sS -X POST -H 'content-type: application/json' http://elder-plops:8080/control/param -d "{\"node\":\"fc_controller\",\"param\":\"schedule_windows\",\"value\":\"<JSON-here-properly-escaped>\"}"` → expect HTTP 200, body `{ok:true, applied:[{param:'schedule_windows', value:'[...]'}]}`.
    2. Verify on fc1 within 5s: `ssh ubuntu@172.16.10.5 "ros2 param get /fc_controller schedule_windows"` shows the new value.
    3. Wait until the start of the first window (~2 min) + up to 30s scheduler tick. Watch `ros2 topic echo /fc1/control/current_mode` (or its JSON sibling `/fc1/control/current_mode_json`) on fc1 — expect a message with `name: 'pinning'` and `source: 'scheduler'`.
    4. After ~2 more minutes, expect a second transition with `name: 'fruiting'`, `source: 'scheduler'`.
    5. Capture journalctl: `journalctl -u fc-core --since "5 min ago" | grep "\\[scheduler\\]"` — expect at least 2 transition INFO lines + zero ERROR lines.

    **Layer 2 persist smoke:**
    6. Reset to empty schedule for the persistence test: `curl -sS -X POST .../control/param -d '{"node":"fc_controller","param":"schedule_windows","value":"[]"}'` → 200.
    7. Now POST a schedule via `/control/persist` (writes through to runtime_overrides.yaml AND applies live):
       ```
       curl -sS -X POST -H 'content-type: application/json' http://elder-plops:8080/control/persist \
         -d '{"node":"fc_controller","param":"schedule_windows","value":"[{\"start\":\"00:00\",\"end\":\"06:00\",\"mode\":\"pinning\"},{\"start\":\"06:00\",\"end\":\"24:00\",\"mode\":\"fruiting\"}]"}'
       ```
       → expect 200, body `{ok:true, persisted:[...], path:'/var/lib/fc-core/runtime_overrides.yaml'}`.
    8. Verify on fc1: `ssh ubuntu@172.16.10.5 "sudo cat /var/lib/fc-core/runtime_overrides.yaml"` — expect the file to contain `schedule_windows:` with the JSON string verbatim under `fc_controller.ros__parameters`.
    9. **Restart survival check:** `ssh ubuntu@172.16.10.5 "sudo systemctl restart fc-core" && sleep 30 && ros2 param get /fc_controller schedule_windows` — expect the persisted JSON, NOT `'[]'` (overlay survived).
    10. Restore baseline: POST `schedule_windows: "[]"` via `/control/persist` to clear the overlay (and Layer 1 to clear hot value).

    **Capture evidence:** save the curl outputs + journalctl excerpt + ros2 param outputs + topic echo to `.planning/phases/30-time-of-day-mode-scheduling/30-03-SMOKE.md`. Include exact timestamps so the farmer attestation has a chronology to align against.
  </action>
  <verify>
    <automated>test -f .planning/phases/30-time-of-day-mode-scheduling/30-03-SMOKE.md && grep -q "Layer 1" .planning/phases/30-time-of-day-mode-scheduling/30-03-SMOKE.md && grep -q "Layer 2" .planning/phases/30-time-of-day-mode-scheduling/30-03-SMOKE.md && grep -q "restart" .planning/phases/30-time-of-day-mode-scheduling/30-03-SMOKE.md</automated>
  </verify>
  <done>
    Both layers proven on real hardware. SMOKE.md captures HTTP responses, ros2 param values before/after, current_mode topic transitions with `source='scheduler'`, journalctl `[scheduler] transition` lines, and the post-restart param survival proof. Baseline (`schedule_windows: "[]"`) restored before completing the task so the chamber is left in single-mode operation pending farmer attestation.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Farmer attestation — schedule fires + chamber survives</name>
  <files>.planning/phases/30-time-of-day-mode-scheduling/30-03-SUMMARY.md</files>
  <action>
    Present farmer (or operator) with the SMOKE.md evidence + the rollback recipe. Wait for one of three replies:

    1. Confirm the chamber has been in baseline (`schedule_windows: "[]"`) for at least 1 hour without unexpected mode swaps. Check Mission Control's mode display (or `ros2 topic echo /fc1/control/current_mode`).
    2. Optional but recommended — push a real overnight schedule via farmOS (or the same curl pattern from SMOKE.md):
       ```
       [{"start":"06:00","end":"22:00","mode":"fruiting"},{"start":"22:00","end":"06:00","mode":"pinning"}]
       ```
       and observe the next 22:00 transition log + Mission Control mode change. If the farmer prefers to wait until they have a use case, leave the schedule at `[]` and gate the attestation on the smoke evidence in 30-03-SMOKE.md.
    3. Confirm Phase 29 alerter behavior is unchanged in baseline — no new alerts caused by Phase 30 deploy.

    Record the response verbatim in 30-03-SUMMARY.md under a `## Farmer Attestation` section, with timestamp + reply.

    Acceptable replies:
      - "approved" — Phase 30 ships; schedule stays at `[]` until farmer chooses to use it.
      - "approved with schedule" — farmer set up a real schedule and observed at least one boundary transition.
      - "issues: <description>" — open a follow-up backlog item; do NOT mark phase complete.
  </action>
  <verify>
    <automated>grep -q "## Farmer Attestation" .planning/phases/30-time-of-day-mode-scheduling/30-03-SUMMARY.md</automated>
  </verify>
  <done>30-03-SUMMARY.md contains a `## Farmer Attestation` section with the farmer's reply and a timestamp.</done>
  <resume-signal>Type "approved", "approved with schedule", or "issues: ..."</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo → fc1 git pull | Trusted (SSH-protected fc1/prod branch). |
| operator curl → bridge HTTP | Untrusted; gated by Plan 30-02 allowlist + 30-01 validator (defense in depth). |
| fc1 systemd → fc-core unit | Trusted; not modified by this plan. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-10 | Tampering | fc_config.yaml schedule_windows default | mitigate | Default value `"[]"` is a known-valid empty-array string per Plan 30-01 validator. YAML parse smoke (Task 1 verify) and ros2 param round-trip smoke (Task 2 verify) both prove no rejection at boot. |
| T-30-11 | Denial of Service | accidentally schedule that conflicts with humidifier safety | accept | Mode definitions themselves (`fruiting`, `pinning`) are pre-validated band/defend-side combos from Phase 28; switching between them at boundaries cannot violate any safety invariant the controller already enforces. The PID bumpless re-engage primitive prevents duty kicks during transitions (Phase 28 D-12). |
| T-30-12 | Repudiation | unclear which mode swap was scheduler vs. farmer | mitigate | `current_mode.source` field disambiguates (D-12). SMOKE.md captures the topic echo lines proving `source='scheduler'`. |
| T-30-13 | Operational | farmer pushes a typo'd schedule and chamber drifts | mitigate | Validator rejects malformed values at both bridge AND controller. If farmer manages to push a *valid* but undesired schedule, the rollback recipe (`schedule_windows: "[]"` via Layer 1 + Layer 2) restores baseline in one curl pair. Documented in 30-03-SUMMARY.md per `<must_haves>`. |
</threat_model>

<verification>
1. `python -c "import yaml; ..."` parse check on fc_config.yaml passes (Task 1 verify).
2. fc1 deploy: `ros2 param get /fc_controller schedule_windows` returns `'[]'` post-restart (Task 2 verify).
3. SMOKE.md exists and documents Layer 1 + Layer 2 + restart survival (Task 3 verify).
4. Farmer attestation: "approved" / "approved with schedule" / "issues: ..." (Task 4 checkpoint).
5. Manual sanity: Mission Control mode display still functional, no new alerter alarms triggered by deploy.
</verification>

<success_criteria>
- [ ] fc_config.yaml documents `schedule_windows: "[]"` default in the fc_controller block.
- [ ] fc1 runs the rebuilt code; new param visible via ros2 param.
- [ ] Live Layer 1 + Layer 2 smoke evidence captured in 30-03-SMOKE.md, including topic echo with `source='scheduler'`.
- [ ] Restart survival proven (overlay file survives, controller picks up persisted schedule on boot).
- [ ] Baseline restored to `schedule_windows: "[]"` before farmer attestation so chamber is in single-mode operation by default.
- [ ] Farmer attestation captured in 30-03-SUMMARY.md.
- [ ] Rollback recipe documented in 30-03-SUMMARY.md.
</success_criteria>

<output>
After completion, create `.planning/phases/30-time-of-day-mode-scheduling/30-03-SUMMARY.md`. Also create `.planning/phases/30-time-of-day-mode-scheduling/30-03-SMOKE.md` with the raw smoke-test evidence captured during Task 3.
</output>
