---
phase: 27
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - src/chambers/fc-core/fc_core/test/conftest.py
  - src/chambers/fc-core/fc_core/test/test_pid_kernel.py
  - src/chambers/fc-core/fc_core/test/test_pwm_driver.py
  - src/chambers/fc-core/fc_core/test/test_controller.py
  - src/chambers/fc-core/fc_core/vendor/__init__.py
  - src/chambers/fc-core/fc_core/vendor/simple_pid/__init__.py
  - src/chambers/fc-core/fc_core/vendor/simple_pid/pid.py
  - src/chambers/fc-core/config/fc_config.yaml
  - docs/OPERATIONS.md
  - docs/pi-setup/dev-workflow.md
autonomous: true
requirements: [HUMID-01, HUMID-02, HUMID-03, HUMID-04]
requirements_addressed: [HUMID-01, HUMID-02, HUMID-03, HUMID-04]
tags: [ros2, pid, pwm, humidity, fc_core]

must_haves:
  truths:
    - "RED test scaffolds exist for PID kernel, slow-PWM driver, and refactored controller — all importable, all failing for the right reason (missing fc_pwm_driver.py / missing PID code)"
    - "simple-pid is vendored in-repo so fc1 deploy does not require PyPI access"
    - "min_dwell_time is gone from active config + active code paths in fc_core; historical .planning/* and .planning/milestones/v1.0-* remain untouched"
    - "fc_config.yaml carries every new pid_*/pwm_* param with calibration-derived defaults"
  artifacts:
    - path: "src/chambers/fc-core/fc_core/vendor/simple_pid/pid.py"
      provides: "Vendored simple-pid PID class with set_auto_mode bumpless API"
    - path: "src/chambers/fc-core/fc_core/test/test_pid_kernel.py"
      provides: "Pure-math PID unit tests (bumpless preload, saturation, anti-windup)"
    - path: "src/chambers/fc-core/fc_core/test/test_pwm_driver.py"
      provides: "Slow-PWM windowing tests (120s window, 10s min pulse, rolling cap, defensive OFF)"
    - path: "src/chambers/fc-core/config/fc_config.yaml"
      provides: "PID + slow-PWM ROS param defaults; min_dwell_time removed"
  key_links:
    - from: "test_pid_kernel.py"
      to: "fc_core.vendor.simple_pid"
      via: "from fc_core.vendor.simple_pid import PID"
      pattern: "from fc_core.vendor.simple_pid import PID"
    - from: "fc_config.yaml"
      to: "fc_controller.py + fc_pwm_driver.py declare_parameters"
      via: "ROS2 param load at node start"
      pattern: "pid_kp:|pwm_window_seconds:|min_pulse_seconds:"

user_setup: []
---

<objective>
Lay down the Wave 0 foundation: failing test scaffolds for PID kernel, slow-PWM driver, and refactored controller; vendor simple-pid into the repo (offline-friendly per fc1 SSH-DERP unreliability); add all new pid_*/pwm_* params to fc_config.yaml with calibration-derived defaults; remove `min_dwell_time` from the active sweep (config + docs only — controller refactor lands in Plan 03).

Purpose: Wave 1 (fc_pwm_driver) and Wave 2 (controller refactor) need the test files to already exist so they can write code RED→GREEN. Vendoring simple-pid up front decouples the PID work from network reachability on the Pi. Putting the min_dwell_time sweep first prevents stale references from leaking into later plans.

Output: Test scaffolding (failing for the right reason), vendored simple-pid library, updated config YAML, doc sweep for min_dwell_time.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/27-pid-time-proportional-duty-cycle-primitive/27-CONTEXT.md
@.planning/phases/27-pid-time-proportional-duty-cycle-primitive/27-RESEARCH.md
@.planning/phases/27-pid-time-proportional-duty-cycle-primitive/27-PATTERNS.md
@.planning/phases/27-pid-time-proportional-duty-cycle-primitive/27-VALIDATION.md
@.planning/phases/999.9-pid-time-proportional-humidity-control/CALIBRATION-FINDINGS-2026-04-11.md
@src/chambers/fc-core/fc_core/test/test_controller.py
@src/chambers/fc-core/config/fc_config.yaml

<interfaces>
<!-- Existing pytest fixture pattern from test_controller.py:13-25 -->
```python
import rclpy
from rclpy.qos import DurabilityPolicy
from unittest.mock import MagicMock, patch
import pytest

_ROS_TIME = rclpy.time.ClockType.ROS_TIME

def _mock_clock_at(nanoseconds):
    mock_clock = MagicMock()
    mock_clock.now.return_value = rclpy.time.Time(
        nanoseconds=nanoseconds, clock_type=_ROS_TIME
    )
    return mock_clock

@pytest.fixture
def ros_context():
    rclpy.init()
    yield
    rclpy.shutdown()
```

<!-- simple-pid 2.0.0 public API (from PyPI, MIT licensed) -->
```python
class PID:
    def __init__(self, Kp=1.0, Ki=0.0, Kd=0.0, setpoint=0.0,
                 sample_time=0.01, output_limits=(None, None),
                 auto_mode=True, proportional_on_measurement=False,
                 differential_on_measurement=True,
                 error_map=None, time_fn=None, starting_output=0.0): ...
    def __call__(self, input_, dt=None) -> float: ...
    def set_auto_mode(self, enabled: bool, last_output: float | None = None) -> None: ...
    @property
    def components(self) -> tuple[float, float, float]: ...  # (P, I, D)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Vendor simple-pid + add new params to fc_config.yaml + remove min_dwell_time from active config and docs</name>
  <files>src/chambers/fc-core/fc_core/vendor/__init__.py, src/chambers/fc-core/fc_core/vendor/simple_pid/__init__.py, src/chambers/fc-core/fc_core/vendor/simple_pid/pid.py, src/chambers/fc-core/config/fc_config.yaml, docs/OPERATIONS.md, docs/pi-setup/dev-workflow.md</files>

  <read_first>
    - .planning/phases/27-pid-time-proportional-duty-cycle-primitive/27-RESEARCH.md (§Standard Stack, §Discretion-Default Recommendations, §Runtime State Inventory `min_dwell_time` removal sweep table)
    - .planning/phases/27-pid-time-proportional-duty-cycle-primitive/27-CONTEXT.md (D-08, D-11, D-12, D-15)
    - src/chambers/fc-core/config/fc_config.yaml (current `min_dwell_time: 180.0` line and surrounding humidity section)
    - docs/OPERATIONS.md (locate current `min_dwell_time` reference around line 47)
    - docs/pi-setup/dev-workflow.md (locate current `min_dwell_time` reference around line 135)
  </read_first>

  <action>
1. Vendor `simple-pid` 2.0.0 (MIT licensed) verbatim into `src/chambers/fc-core/fc_core/vendor/simple_pid/`:
   - Create `src/chambers/fc-core/fc_core/vendor/__init__.py` with content `# Vendored third-party packages — do not modify` (one line, plus blank line).
   - Create `src/chambers/fc-core/fc_core/vendor/simple_pid/__init__.py` exposing `from .pid import PID` only.
   - Create `src/chambers/fc-core/fc_core/vendor/simple_pid/pid.py` containing the simple-pid 2.0.0 PID class verbatim from https://github.com/m-lundberg/simple-pid/blob/v2.0.0/simple_pid/pid.py. Preserve the upstream MIT license header. Do NOT modify class behavior.
   - If unable to fetch upstream (offline), reproduce the canonical PID class with this exact public API (signature shown in `<interfaces>`): `__init__(Kp, Ki, Kd, setpoint=0.0, sample_time=0.01, output_limits=(None, None), auto_mode=True, proportional_on_measurement=False, differential_on_measurement=True, ...)`, `__call__(input_, dt=None) -> float`, `set_auto_mode(enabled, last_output=None)`, and `components` property returning `(P, I, D)` tuple. The `set_auto_mode(True, last_output=X)` path MUST back-compute the integrator so the very next `__call__` (with steady error) returns approximately `X` — this is the bumpless preload contract D-06 depends on.

2. Edit `src/chambers/fc-core/config/fc_config.yaml`:
   - Delete the line `min_dwell_time: 180.0` (and any inline comment on the same line) per D-15.
   - Under the same `fc_controller` parameters block, add the new params verbatim (match existing inline-`#` comment style and indentation):
     ```yaml
         # PID gains (Phase 27 — Skogestad SIMC defaults from 2026-04-11 calibration; error in %RH, output 0.0–1.0)
         pid_kp: 0.5
         pid_ki: 0.002
         pid_kd: 4.0
         pid_derivative_filter_tau: 10.0
         pid_setpoint_ramp_seconds: 30.0  # D-07
         bypass_threshold: 0.025           # D-10 — Mode C entry when |error| > 2.5% RH
         # Slow-PWM (fc_pwm_driver — Phase 27)
         pwm_window_seconds: 120.0         # D-08 LOCKED
         min_pulse_seconds: 10.0           # D-11 LOCKED — empirical fog-visibility floor
         max_duty_5min_avg: 0.40           # D-12 — rolling 5-min duty cap
         duty_topic_timeout_seconds: 5.0   # defensive OFF if duty subscription silent
     ```
   - Use the values from the recommendation block VERBATIM. Do not round, scale, or reinterpret.

3. `docs/OPERATIONS.md`: locate the `min_dwell_time` row (around line 47) and remove it. If it sits in a markdown table, remove the entire table row and re-balance the table. Do NOT touch any other rows.

4. `docs/pi-setup/dev-workflow.md`: locate the `min_dwell_time` reference (around line 135) and remove the line. Preserve surrounding paragraph flow.

5. DO NOT touch:
   - `src/chambers/fc-core/fc_core/fc_controller.py` (Plan 03 owns this)
   - `.planning/research/ARCHITECTURE.md`, `.planning/milestones/v1.0-*`, or any `.planning/phases/15-*`, `.planning/phases/17-*` reference (historical — they describe v1.0/Phase-15/Phase-17 state truthfully; let history stand per RESEARCH §Runtime State Inventory).
   - Test files (handled by Tasks 2 + 3).

6. Run `ament_flake8 src/chambers/fc-core/fc_core/vendor/` to confirm vendored code passes lint (it should; if not, vendor with `# noqa` headers — the upstream code is the source of truth).
  </action>

  <verify>
    <automated>cd /mnt/slime-kingdom/opt/mushy &amp;&amp; python -c "from src.chambers.fc_core.fc_core.vendor.simple_pid import PID; p = PID(0.5, 0.002, 4.0, output_limits=(0.0, 1.0), auto_mode=False, sample_time=None); p.set_auto_mode(True, last_output=0.15); v = p(0.0, dt=1.0); assert 0.10 &lt; v &lt; 0.20, f'bumpless preload broken: got {v}'; print('OK', v)" &amp;&amp; ! grep -n "min_dwell_time" src/chambers/fc-core/config/fc_config.yaml docs/OPERATIONS.md docs/pi-setup/dev-workflow.md &amp;&amp; grep -q "pid_kp: 0.5" src/chambers/fc-core/config/fc_config.yaml &amp;&amp; grep -q "pwm_window_seconds: 120" src/chambers/fc-core/config/fc_config.yaml &amp;&amp; grep -q "min_pulse_seconds: 10" src/chambers/fc-core/config/fc_config.yaml &amp;&amp; grep -q "bypass_threshold: 0.025" src/chambers/fc-core/config/fc_config.yaml</automated>
  </verify>

  <acceptance_criteria>
    - File exists: `src/chambers/fc-core/fc_core/vendor/simple_pid/pid.py` with a `class PID:` definition and `def set_auto_mode` method.
    - `from fc_core.vendor.simple_pid import PID` succeeds (after colcon build) — verified by Python import check above.
    - `grep -c "min_dwell_time" src/chambers/fc-core/config/fc_config.yaml` returns `0`.
    - `grep -c "min_dwell_time" docs/OPERATIONS.md` returns `0`.
    - `grep -c "min_dwell_time" docs/pi-setup/dev-workflow.md` returns `0`.
    - `grep -c "min_dwell_time" .planning/research/ARCHITECTURE.md` is UNCHANGED (historical, must not be edited) — verify with `git diff .planning/research/ARCHITECTURE.md` produces no output.
    - `grep -E "^\s*pid_kp: 0\.5$" src/chambers/fc-core/config/fc_config.yaml` matches.
    - `grep -E "^\s*pwm_window_seconds: 120\.0$" src/chambers/fc-core/config/fc_config.yaml` matches.
    - `grep -E "^\s*min_pulse_seconds: 10\.0$" src/chambers/fc-core/config/fc_config.yaml` matches.
    - `grep -E "^\s*max_duty_5min_avg: 0\.40$" src/chambers/fc-core/config/fc_config.yaml` matches.
    - `grep -E "^\s*duty_topic_timeout_seconds: 5\.0$" src/chambers/fc-core/config/fc_config.yaml` matches.
    - `grep -E "^\s*bypass_threshold: 0\.025" src/chambers/fc-core/config/fc_config.yaml` matches.
    - The bumpless smoke check above prints `OK 0.15…` (value within 0.10–0.20).
  </acceptance_criteria>

  <done>simple-pid vendored under fc_core.vendor.simple_pid and importable; min_dwell_time gone from active config + docs; new pid_*/pwm_* params present with locked defaults.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RED test scaffold for PID kernel + slow-PWM driver (test_pid_kernel.py + test_pwm_driver.py + conftest.py)</name>
  <files>src/chambers/fc-core/fc_core/test/conftest.py, src/chambers/fc-core/fc_core/test/test_pid_kernel.py, src/chambers/fc-core/fc_core/test/test_pwm_driver.py</files>

  <read_first>
    - src/chambers/fc-core/fc_core/test/test_controller.py (lines 13-25 fixture, 185-242 mock-clock dwell tests, 371-388 publish-capture, 558-564 QoS assertion)
    - .planning/phases/27-pid-time-proportional-duty-cycle-primitive/27-PATTERNS.md (§test_pwm_driver.py + §test_pid_kernel.py sections)
    - .planning/phases/27-pid-time-proportional-duty-cycle-primitive/27-RESEARCH.md (§Validation Architecture Phase Requirements → Test Map)
    - .planning/phases/27-pid-time-proportional-duty-cycle-primitive/27-VALIDATION.md (Wave 0 Requirements + Per-Task Verification Map)
    - .planning/phases/27-pid-time-proportional-duty-cycle-primitive/27-CONTEXT.md (D-06 bumpless, D-09 Mode C, D-11 min pulse, D-12 rolling cap, D-13 stale=duty=0)
  </read_first>

  <behavior>
    `test_pid_kernel.py` (pure-math, no rclpy):
    - test_bumpless_preload_returns_seed: PID(0.5, 0.002, 4.0, output_limits=(0,1), auto_mode=False, sample_time=None) ; pid.set_auto_mode(True, last_output=0.15) ; pid(0.0, dt=1.0) returns float in [0.10, 0.20].
    - test_output_clamps_at_one: large positive error (e.g. error_pct=10.0) drives output to exactly 1.0 (saturation).
    - test_output_clamps_at_zero: large negative error drives output to exactly 0.0.
    - test_anti_windup_clamping_on_saturation: feed error_pct=10.0 for 100 ticks at dt=1.0 ; pid.components[1] (integral) does not grow without bound — assert |components[1]| < 1.0/0.002 + epsilon (Ki * I cannot exceed output_limits range when clamped).
    - test_d_on_measurement_no_kick_on_setpoint_change: holding measurement constant, change setpoint suddenly — `pid()` output rises smoothly (no derivative spike) because differential_on_measurement=True.
    - test_disengage_freezes_integrator: engage, run for 10 ticks at error_pct=1.0, snapshot integral; set_auto_mode(False); run 10 more ticks; integral unchanged.

    `test_pwm_driver.py` (ROS node tests; uses `ros_context` fixture from conftest):
    - test_pwm_driver_initialization: SlowPwmDriver instantiates, declares params humidifier_pin/pwm_window_seconds/min_pulse_seconds/max_duty_5min_avg/actuator_simulation_mode/duty_topic_timeout_seconds.
    - test_window_on_then_off: with actuator_simulation_mode=True, latest_duty=0.5, mock-clock at window-start, tick → relay HIGH; mock-clock at window-start + 60s → relay HIGH (still inside on_seconds=60); mock-clock at +61s → relay LOW.
    - test_min_pulse_skip: latest_duty=0.05 (60s × 0.05 = 6s < 10s min_pulse) → entire window emits OFF.
    - test_min_pulse_passes_at_floor: latest_duty=0.0833333 (10s exactly) → emits ON for ~10s then OFF.
    - test_rolling_max_cap_engages: feed latest_duty=1.0 over many windows so 5-min average ≥ 0.40 ; assert subsequent emitted on_seconds is reduced so rolling avg stays ≤ 0.40.
    - test_duty_silence_forces_off: no _duty_callback ever fires → tick → relay LOW; or callback fired then 6s pass with no new msg → relay LOW.
    - test_bool_published_on_edge_only: capture published Bool messages; over 120s steady-state at duty=0.5, expect exactly 2 publications (one OFF→ON edge, one ON→OFF edge) — NOT 120 publications.
    - test_duty_subscription_qos_transient_local: assert SlowPwmDriver._duty_sub.qos_profile.durability == DurabilityPolicy.TRANSIENT_LOCAL (Pitfall 5).
    - test_humidifier_pub_qos_transient_local: assert state_pub.qos_profile.durability == TRANSIENT_LOCAL (Phase 04 ACTR-03 contract preserved).
    - test_clamps_negative_duty_to_zero: _duty_callback(Float32(data=-0.5)) → latest_duty == 0.0.
    - test_clamps_above_one_to_one: _duty_callback(Float32(data=1.5)) → latest_duty == 1.0.
  </behavior>

  <action>
1. Create `src/chambers/fc-core/fc_core/test/conftest.py` containing only the shared fixtures used across the new test files. Move/duplicate from `test_controller.py:13-25`:
```python
import rclpy
from unittest.mock import MagicMock
import pytest

_ROS_TIME = rclpy.time.ClockType.ROS_TIME

def _mock_clock_at(nanoseconds):
    mock_clock = MagicMock()
    mock_clock.now.return_value = rclpy.time.Time(
        nanoseconds=nanoseconds, clock_type=_ROS_TIME
    )
    return mock_clock

@pytest.fixture
def ros_context():
    rclpy.init()
    yield
    rclpy.shutdown()
```
Export `_mock_clock_at` as a plain helper (not a fixture) so test files import it via `from .conftest import _mock_clock_at` or `from conftest import _mock_clock_at` (depending on pytest rootdir setup — match what test_controller.py does today).

2. Create `src/chambers/fc-core/fc_core/test/test_pid_kernel.py`. Imports: `from fc_core.vendor.simple_pid import PID`, `import pytest`. Implement the 6 tests under `<behavior>` above. NO `ros_context` fixture (pure math). Use explicit `dt=1.0` on every `pid(error)` call (per Pitfall 6 — sample_time=None on construction).

3. Create `src/chambers/fc-core/fc_core/test/test_pwm_driver.py`. Imports:
   ```python
   from fc_core.fc_pwm_driver import SlowPwmDriver  # WILL fail to import — Wave 1 creates this
   from std_msgs.msg import Float32, Bool
   from rclpy.qos import DurabilityPolicy
   from .conftest import _mock_clock_at  # or `from conftest import _mock_clock_at` matching repo style
   from unittest.mock import patch
   import pytest
   ```
   Implement the 11 tests under `<behavior>` above. Each test instantiates `SlowPwmDriver()` inside `ros_context`, sets `actuator_simulation_mode=True` via `node.set_parameters([Parameter('actuator_simulation_mode', value=True)])` (mirror test_controller.py:43-46 pattern). Use mock-clock pattern for window-rollover tests:
   ```python
   with patch.object(node, 'get_clock', return_value=_mock_clock_at(0)):
       node._duty_callback(Float32(data=0.5))  # also stamps _last_duty_msg_ts
       node._tick()
       assert node._current_state == True
   with patch.object(node, 'get_clock', return_value=_mock_clock_at(int(61e9))):
       node._tick()
       assert node._current_state == False
   ```
   For `test_bool_published_on_edge_only`, use the publish-capture trick from `test_controller.py:371-388`:
   ```python
   published = []
   node._state_pub.publish = lambda msg: published.append(msg.data)
   ```

4. Tests MUST run RED at this stage. `test_pid_kernel.py` tests should PASS already (vendored simple-pid is functional). `test_pwm_driver.py` MUST fail-on-import because `fc_core.fc_pwm_driver` doesn't exist yet — that is the correct RED state for Wave 1 to turn green.

5. Add file-level docstring to each new test file referencing its requirement coverage:
   - `test_pid_kernel.py`: `"""HUMID-03: PID gain math (bumpless, anti-windup, D-on-measurement)."""`
   - `test_pwm_driver.py`: `"""HUMID-02: Slow-PWM windowing (D-08 120s, D-11 10s min pulse, D-12 rolling cap, defensive OFF)."""`

6. DO NOT modify `test_controller.py` in this task — Task 3 owns it.
  </action>

  <verify>
    <automated>cd /mnt/slime-kingdom/opt/mushy/src/chambers/fc-core &amp;&amp; python -m pytest fc_core/test/test_pid_kernel.py -x --timeout=10 2&gt;&amp;1 | tail -20 &amp;&amp; python -m pytest fc_core/test/test_pwm_driver.py --collect-only 2&gt;&amp;1 | grep -E "ImportError|ModuleNotFoundError|fc_pwm_driver" &amp;&amp; test -f fc_core/test/conftest.py &amp;&amp; test -f fc_core/test/test_pid_kernel.py &amp;&amp; test -f fc_core/test/test_pwm_driver.py</automated>
  </verify>

  <acceptance_criteria>
    - File exists: `src/chambers/fc-core/fc_core/test/conftest.py` with `def _mock_clock_at` and `@pytest.fixture def ros_context`.
    - File exists: `src/chambers/fc-core/fc_core/test/test_pid_kernel.py` with at least 6 `def test_*` functions.
    - File exists: `src/chambers/fc-core/fc_core/test/test_pwm_driver.py` with at least 11 `def test_*` functions.
    - `pytest src/chambers/fc-core/fc_core/test/test_pid_kernel.py -x` — ALL 6 tests PASS (vendored simple-pid is functional).
    - `pytest src/chambers/fc-core/fc_core/test/test_pwm_driver.py --collect-only` — collection FAILS with `ModuleNotFoundError: No module named 'fc_core.fc_pwm_driver'` (correct RED state for Wave 1).
    - `grep -c "DurabilityPolicy.TRANSIENT_LOCAL" src/chambers/fc-core/fc_core/test/test_pwm_driver.py` >= 2 (one assertion for sub QoS, one for pub QoS — Pitfall 5).
    - `grep -q "min_pulse_seconds" src/chambers/fc-core/fc_core/test/test_pwm_driver.py` matches.
    - `grep -q "max_duty_5min_avg" src/chambers/fc-core/fc_core/test/test_pwm_driver.py` matches.
    - `grep -q "set_auto_mode(True, last_output=0.15" src/chambers/fc-core/fc_core/test/test_pid_kernel.py` matches (D-06 bumpless contract test).
    - `grep -q "differential_on_measurement" src/chambers/fc-core/fc_core/test/test_pid_kernel.py` matches.
  </acceptance_criteria>

  <done>RED test scaffold lands: PID kernel tests pass against vendored simple-pid; PWM driver tests are RED via missing-module import (Wave 1 will turn green by creating fc_pwm_driver.py).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Refactor test_controller.py — delete dwell tests, add RED test stubs for PID + Mode C + ramp + duty publishing</name>
  <files>src/chambers/fc-core/fc_core/test/test_controller.py</files>

  <read_first>
    - src/chambers/fc-core/fc_core/test/test_controller.py (current full file — identify dwell-related tests: test_humidity_control:62-87, test_new_params_declared:155-160 min_dwell line, test_none_humidity_safe_state:163-181, test_dwell_*:185-266, test_safe_state_updates_dwell_toggle:329-347, test_humidifier_state_published:371-388, test_warmup_grace_blocks_actuation:402-440 if it touches humidifier_state)
    - .planning/phases/27-pid-time-proportional-duty-cycle-primitive/27-PATTERNS.md (§test_controller.py MODIFY section — exact list of deletes, keeps, adds)
    - .planning/phases/27-pid-time-proportional-duty-cycle-primitive/27-RESEARCH.md (§Validation Architecture Phase Requirements → Test Map — HUMID-01 + HUMID-03 rows)
    - .planning/phases/27-pid-time-proportional-duty-cycle-primitive/27-CONTEXT.md (D-06, D-07, D-09, D-13, D-14)
  </read_first>

  <behavior>
    DELETE:
    - test_humidity_control (lines ~62-87) — bang-bang ON/OFF assertions on `node.humidifier_state` (attribute moves to fc_pwm_driver; controller no longer tracks it)
    - test_dwell_time_blocks_toggle (~185-204)
    - test_dwell_time_allows_toggle_after_wait (~207-226)
    - test_dwell_time_first_toggle_always_allowed (~229-242)
    - test_dwell_time_applies_both_directions (~245-266)
    - test_safe_state_updates_dwell_toggle (~329-347)
    - test_humidifier_state_published (~371-388) — replaced by duty-published variant below
    - In test_new_params_declared (~155-160): delete the `min_dwell_time == 300.0` assertion line; keep the rest of the test if it asserts other params, or delete the whole test if `min_dwell_time` was the only assertion. (Inspect first.)

    REWRITE:
    - test_none_humidity_safe_state, test_none_temp_safe_state (~163-181): change assertions from `node.humidifier_state == False` to capturing the published `Float32` on `_duty_pub` and asserting last value == 0.0.
    - test_sensor_staleness (~269-285), test_safe_state_recovery (~288-309), test_fresh_data_not_stale (~350-368): same pattern — assert duty published == 0.0 on stale, ≥ 0.0 (and engaged) on recovery.
    - Any test_warmup_*  (~402-555) that reads `node.humidifier_state`: switch to duty-publish capture (`duty_published[-1] == 0.0` during grace).

    ADD (RED stubs — will fail until Plan 03 lands):
    - test_duty_published_each_tick: with fresh humidity at exactly target, control_loop() runs N times, _duty_pub captures exactly N publications.
    - test_duty_qos_transient_local: assert `node._duty_pub.qos_profile.durability == DurabilityPolicy.TRANSIENT_LOCAL`.
    - test_humidifier_state_pub_removed: assert `not hasattr(node, 'humidifier_state_pub')` AND `not hasattr(node, '_set_humidifier_with_dwell')` — Pitfall 4 prevention.
    - test_pid_params_declared: assert pid_kp/pid_ki/pid_kd/pid_setpoint_ramp_seconds/bypass_threshold/pid_derivative_filter_tau are declared with the expected defaults from fc_config.yaml.
    - test_pid_gains_live_reload: change pid_kp via set_parameters mid-run, assert next tick's duty reflects new Kp.
    - test_grace_forces_duty_zero: during grace_active, duty published == 0.0 regardless of error.
    - test_stale_forces_duty_zero: D-13 — stale = True forces duty=0.0 even mid-ramp.
    - test_mode_c_entry: error_pct > bypass_threshold*100 → duty == 1.0.
    - test_mode_c_exit_bumpless: after Mode C, when error returns inside threshold, PID re-engages with last_output=1.0 (smooth handoff, no zero blip).
    - test_setpoint_ramp_slews_effective: change target_humidity, observe `_effective_setpoint` slews over `pid_setpoint_ramp_seconds` rather than stepping.
    - test_bumpless_preload_on_grace_clear: grace clears, first PID tick produces duty ≈ 0.15 (not 0, not 1).
    - test_min_dwell_time_param_removed: assert `'min_dwell_time' not in [p.name for p in node.list_parameters([], 10).names]` (param NOT declared anymore — D-15 enforcement).

    KEEP UNCHANGED:
    - test_controller_initialization, test_temperature_control, test_light_control
    - test_humidity_spike_rejection, test_humidity_median_partial_buffer, test_humidity_buffer_fifo
    - test_sht30/scd41_* (~572-631) — Phase 26 D-03 unchanged
    - test_sensor_health_* (~484-564) — Phase 16 contract unchanged
    - test_staleness_log_deduplication (~312-326) — log dedupe pattern still applies
  </behavior>

  <action>
1. Read full `test_controller.py`. Identify exact line ranges for each test in the DELETE list above by their `def test_<name>` headers — RESEARCH/PATTERNS line numbers are approximate.

2. DELETE the listed tests outright. Preserve all imports needed by remaining tests.

3. REWRITE the listed tests to capture published `Float32` duty messages instead of reading `node.humidifier_state`. Pattern (mirroring test_controller.py existing publish-capture):
   ```python
   duty_published = []
   node._duty_pub.publish = lambda msg: duty_published.append(msg.data)
   # ... drive the test ...
   assert duty_published[-1] == 0.0
   ```

4. ADD the 12 RED test stubs from the `<behavior>` ADD list. Each stub must have a real assertion — no `pass` placeholders. The tests will fail on import or attribute-access until Plan 03 implements:
   - `node._duty_pub` (publisher attribute)
   - `node._effective_setpoint` (ramp state)
   - `node._pid` and `node._pid_engaged` (PID state)
   - Removal of `node.humidifier_state_pub` and `node._set_humidifier_with_dwell`
   - Removal of `min_dwell_time` from declare_parameters

5. Add file-level docstring update at top: `"""HUMID-01 + HUMID-03: PID-driven duty publishing, Mode C, ramp, bumpless transfer (Phase 27)."""`

6. Imports — ensure these are present at file top:
   ```python
   from std_msgs.msg import Float32  # NEW for duty pub assertions
   from rclpy.qos import DurabilityPolicy
   ```
   And remove unused imports if any tests' deletion orphans them.

7. DO NOT touch any other file in this task.
  </action>

  <verify>
    <automated>cd /mnt/slime-kingdom/opt/mushy &amp;&amp; ! grep -E "test_dwell_time|test_safe_state_updates_dwell_toggle|test_humidity_control\b" src/chambers/fc-core/fc_core/test/test_controller.py &amp;&amp; grep -c "def test_" src/chambers/fc-core/fc_core/test/test_controller.py &amp;&amp; grep -q "test_duty_published_each_tick\|test_duty_qos_transient_local\|test_mode_c_entry\|test_setpoint_ramp\|test_bumpless_preload_on_grace_clear\|test_min_dwell_time_param_removed\|test_humidifier_state_pub_removed" src/chambers/fc-core/fc_core/test/test_controller.py &amp;&amp; cd src/chambers/fc-core &amp;&amp; python -m pytest fc_core/test/test_controller.py --collect-only 2&gt;&amp;1 | tail -5</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "def test_dwell_time" src/chambers/fc-core/fc_core/test/test_controller.py` returns `0`.
    - `grep -c "def test_safe_state_updates_dwell_toggle" src/chambers/fc-core/fc_core/test/test_controller.py` returns `0`.
    - `grep -c "def test_humidity_control\b" src/chambers/fc-core/fc_core/test/test_controller.py` returns `0`.
    - `grep -c "def test_humidifier_state_published\b" src/chambers/fc-core/fc_core/test/test_controller.py` returns `0`.
    - `grep -c "def test_duty_published_each_tick" src/chambers/fc-core/fc_core/test/test_controller.py` returns `1`.
    - `grep -c "def test_duty_qos_transient_local" src/chambers/fc-core/fc_core/test/test_controller.py` returns `1`.
    - `grep -c "def test_mode_c_entry" src/chambers/fc-core/fc_core/test/test_controller.py` returns `1`.
    - `grep -c "def test_mode_c_exit_bumpless" src/chambers/fc-core/fc_core/test/test_controller.py` returns `1`.
    - `grep -c "def test_setpoint_ramp" src/chambers/fc-core/fc_core/test/test_controller.py` returns `1` (or more).
    - `grep -c "def test_bumpless_preload_on_grace_clear" src/chambers/fc-core/fc_core/test/test_controller.py` returns `1`.
    - `grep -c "def test_min_dwell_time_param_removed" src/chambers/fc-core/fc_core/test/test_controller.py` returns `1`.
    - `grep -c "def test_humidifier_state_pub_removed" src/chambers/fc-core/fc_core/test/test_controller.py` returns `1`.
    - `grep -c "def test_pid_params_declared" src/chambers/fc-core/fc_core/test/test_controller.py` returns `1`.
    - `grep -c "def test_grace_forces_duty_zero" src/chambers/fc-core/fc_core/test/test_controller.py` returns `1`.
    - `grep -c "def test_stale_forces_duty_zero" src/chambers/fc-core/fc_core/test/test_controller.py` returns `1`.
    - `pytest src/chambers/fc-core/fc_core/test/test_controller.py --collect-only` succeeds (file is syntactically valid and importable). Some tests will be RED — that is expected; this task only requires collection to succeed.
    - `grep -q "min_dwell_time" src/chambers/fc-core/fc_core/test/test_controller.py` returns no matches OR matches only inside the `test_min_dwell_time_param_removed` assertion (not in `declare_parameters` setups).
  </acceptance_criteria>

  <done>test_controller.py refactored: dwell tests deleted, safe-state tests rewritten to assert duty=0.0, 12 new RED stubs added covering HUMID-01 + HUMID-03 + Pitfalls 4 + 7. File collects cleanly under pytest.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ROS topic ↔ ROS topic (intra-fc-core) | `fc1/actuators/humidifier_duty` Float32 boundary between fc_controller (publisher) and fc_pwm_driver (subscriber). Both nodes run in the same systemd unit on the same Pi under the same user. No new external trust boundary. |
| Vendored code | `simple-pid` MIT-licensed code copied verbatim into the repo. Trust boundary = code review of the vendored copy at vendor time; no runtime fetch. |

## STRIDE Threat Register (ASVS L1)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-01-01 | T (Tampering) | Vendored simple-pid copy | mitigate | Vendor code is read-only by convention; `vendor/__init__.py` carries a comment marking it as not-to-modify. ament_flake8 catches accidental edits via lint. Acceptable: developer error here only affects PID math; sensor-stale and grace gating remain in fc_controller and force duty=0. |
| T-27-01-02 | I (Information disclosure) | Test fixtures | accept | Tests run only in CI / dev shell; no secrets. |
| T-27-01-03 | D (Denial of service) | min_dwell_time docs sweep | accept | Doc edits do not affect runtime; no DoS surface. |
| T-27-01-04 | E (Elevation of privilege) | Vendored code | accept | simple-pid is pure-Python computation, no I/O, no eval/exec on dynamic input. |

**Conclusion:** Phase 27 Plan 01 introduces no new external trust boundary. Existing GPIO/topic boundary is unchanged from Phase 04. Threat surface is internal-only and bounded by ROS2 system topology already covered by Phase 06 (Tailscale) and existing fc-core systemd containment.
</threat_model>

<verification>
- All three new/modified test files importable under pytest (`--collect-only` succeeds for test_pid_kernel.py and test_controller.py; test_pwm_driver.py collection RED on missing fc_pwm_driver module — expected).
- `pytest src/chambers/fc-core/fc_core/test/test_pid_kernel.py -x` passes all 6 tests.
- `grep -E "min_dwell_time" src/chambers/fc-core/config/fc_config.yaml docs/OPERATIONS.md docs/pi-setup/dev-workflow.md` returns nothing.
- `grep -E "min_dwell_time" .planning/research/ARCHITECTURE.md .planning/milestones/v1.0-*.md .planning/phases/15-*/15-01-PLAN.md` is UNCHANGED (historical files preserved — verify with `git diff`).
- New params (pid_kp/pid_ki/pid_kd/pid_setpoint_ramp_seconds/bypass_threshold/pwm_window_seconds/min_pulse_seconds/max_duty_5min_avg/duty_topic_timeout_seconds/pid_derivative_filter_tau) all present in fc_config.yaml with verbatim defaults.
</verification>

<success_criteria>
- Vendored simple-pid importable as `from fc_core.vendor.simple_pid import PID` (after colcon build).
- 6/6 PID kernel tests GREEN.
- test_pwm_driver.py RED on missing `fc_core.fc_pwm_driver` module (expected).
- test_controller.py refactored, all 12 new RED stubs present and syntactically valid.
- `min_dwell_time` removed from active config + docs; historical .planning/* untouched.
- All 10 new pid_*/pwm_* params present in fc_config.yaml with locked defaults.
</success_criteria>

<output>
After completion, create `.planning/phases/27-pid-time-proportional-duty-cycle-primitive/27-01-SUMMARY.md`.
</output>