# Requirements — v1.5 (Analog Humidity Control & Condensation/Evaporation Forcing) — ARCHIVED 2026-05-09

> Shipped 2026-05-09. v1.5.0.1 (resilience hotfix) shipped 2026-05-07; see `v1.5.0.1-REQUIREMENTS.md`.

---

## v1.5 — Analog Humidity Control & Condensation/Evaporation Forcing

### HUMID — Analog Humidity Control

- [x] **HUMID-01** — Controller publishes a 0–100% duty cycle setpoint each control tick (replaces bang-bang). **Validated** 2026-05-02 (Phase 27).
- [x] **HUMID-02** — Slow-PWM actuator translates duty cycle into time-proportional on/off windows; window length per calibration findings. **Validated** 2026-05-02.
- [x] **HUMID-03** — PID gains tunable as ROS params; defaults from 2026-04-11 system-ID data. **Validated** 2026-05-02.
- [x] **HUMID-04** — Operating band tightens from interim ±1% to PID-tracked tolerance verifiable on 2-hour soak. **Validated** 2026-05-02 (farmer-attested).

### MODE — Mode Primitive + Runtime Config Delivery (incorporates SEED-001)

- [x] **MODE-01** — Mode registry: `(target_humidity, band_low, band_high, defend_side, T_target_optional)` flattened into dotted-key ROS2 params. Schema reconciled with SEED-004 at Phase 28 discuss-phase. **Validated** 2026-05-07/08.
- [x] **MODE-02** — Two baseline modes shipped: `fruiting` and `pinning`, with per-mode targets/bands chosen with farmer. **Validated** 2026-05-08.
- [x] **MODE-03** — Farmer can switch active mode via ROS service call; takes effect on next control tick. **Validated** live this session 2026-05-09 (`active_mode=fruiting` reset via `/control/param`).
- [x] **MODE-04** — Controller publishes `current_mode` topic; downstream consumers read live mode without restart. **Validated** via bridge `[experiment_event]` + `current_mode` broadcast logs.
- [x] **MODE-05** — Mode definitions runtime-tunable without deploy: edit-and-reload via Layer 1 (`/control/param`) + Layer 2 (`/control/persist` → `runtime_overrides.yaml`). **Validated** in Phase 30-03 smoke test.

### ALRT — Alerter Mode Awareness + Cooldown Tuning

- [x] **ALRT-08** — Alerter reads RH target and band from `current_mode` instead of static env vars. Closes backlog 999.22. **Validated** 2026-05-08 (Phase 29).
- [x] **ALRT-09** — Sweep `config.js` for env-hidden farmer-meaningful knobs (heartbeat hour, humidifier-stuck threshold, RH OOB grace, pi/sensor offline minutes); routed through dynamic source as ALRT-08. **Validated** 2026-05-08.
- [ ] **ALRT-10** — Alert cooldown thresholds tuned based on Phase 17's ≥2 weeks of live data. **DEFERRED** — calendar-gated; tracked as backlog 999.20. Rolls to v1.6 / next milestone with sufficient observation data.

### SCHED — Time-Of-Day Mode Scheduling

- [x] **SCHED-01** — Declarative schedule definition (JSON-encoded `[{start, end, mode}]`) maps time windows to mode names. **Validated** 2026-05-09 via Phase 30-03 smoke (boundary transition `fruiting → pinning at 22:47`).
- [x] **SCHED-02** — Scheduler issues mode-switch service calls at window boundaries. Closes backlog 999.23 groundwork. **Validated** 2026-05-09.
- [x] **SCHED-03** — Default profile is constant-single-mode (no schedule); backward compatible. **Validated** — `schedule_windows: "[]"` default in fc_config.yaml; baseline restored cleanly post-smoke.

### EXPT — Experimental Forcing Modes

- [x] **EXPT-01** — `force-condensation` mode: 100% duty for N minutes, auto-reverts to prior mode on timeout. **Validated** 2026-05-09 via direct bridge curl (DB row 7: 1.00min auto-revert, end_reason=`timeout`). Farmer Signal command path requires signal-cli primary re-registration (deferred to v1.6 / signal-cli reinit prerequisite).
- [x] **EXPT-02** — `force-evaporation` mode: 0% duty for N minutes, auto-reverts. Same trigger surface. **Validated** 2026-05-09 via saturday lab visit (DB row id=4: timeout, 1.00min). Same Signal-path caveat as EXPT-01.
- [x] **EXPT-03** — Both modes log start/end + measured RH delta to TimescaleDB. **Validated** 2026-05-09 — `fc_experiments` table populated with 7 closed experiments (start/end/baseline_rh/final_rh/delta_rh/end_reason).

---

## Outcomes Summary

**Score:** 17/17 mapped, 16 satisfied, 1 deferred (ALRT-10 calendar-gated).

**Adjustments during execution:**
- MODE-01 wording originally bundled `(target_RH, band, duty-cycle behavior)`; reconciled with SEED-004 schema `(target_humidity, band_low, band_high, defend_side, T_target_optional)` at Phase 28 discuss-phase 2026-05-07. Old wording retired.
- EXPT-01/02 farmer Signal trigger surface validated as code-correct but blocked on pre-existing infrastructure (signal-cli registered as deviceId=2 linked-secondary; cannot use `/v1/receive`). Re-registration carried as prerequisite for any future Signal-driven UAT (memory `project_signal_cli_primary_reregister_path`).

---

## Future Requirements (deferred from v1.5 scope)

- **Mode editor UI in farmer app** — graphical mode definition + schedule editor; v1.6 candidate, depends on farmer-app maturity (999.11).
- **Per-mode PID gains** — different modes carry different gains for stage-aware tuning; baseline v1.5 ships one PID gain set across all modes.
- **Stage-triggered mode transitions** — farmer flags "spawn-run start" → schedule advances; v1.6 candidate, depends on farmOS event integration.
- **Mode-aware sensor selection** — trust SHT30 RH during incubation, SCD41 RH during fruiting; deferred until SCD41 RH clipping is investigated (Phase 26 known issue).
- **Schedule gap-mode default** — observation from 30-03 smoke: scheduler keeps current mode in gaps between windows (CONTEXT D-08 design); a future "default_mode_in_gap" parameter would let operator declare the fall-through (currently must define a covering window).
- **MC widget for active experiment state** — bridge broadcasts `current_mode` (source='experiment') and `experiment_event` topics but no MC widget renders these; cosmetic gap from Phase 31 (file as 999.x).

## Out of Scope

- **Temperature control** — still no actuator in scope.
- **Multi-chamber mode coordination** — single-chamber until multi-chamber milestone.
- **PID auto-tuning** — manual tuning from calibration data is sufficient for v1.5.
- **Phase 19 (FarmOS admin actions)** — deferred to v1.6; still Zoy/farm-team gated.

## Tech Debt Carried Forward

- **ALRT-10** alert cooldown tuning (calendar-gated, ≥2 weeks observation data).
- **signal-cli primary re-registration** — prerequisite for Signal-driven Phase 31 UAT (Tests 3-5).
- **Phase 28 + Phase 30 missing VERIFICATION.md** — workflow gap, not code; functional evidence in respective SUMMARY.md / 30-03-SMOKE.md.
- **Phase 31 Tests 6 (boot-recovery D-09 LOAD-BEARING) and 7 (scheduler suppression)** — runnable any time via direct bridge curl + ssh restart; unit-test-covered.
- **humidity_target restart re-init** — `_effective_setpoint` initializes to nominal `target_humidity` (0.96) instead of defended band edge (0.945); transient cosmetic overshoot post-restart. User opted to ignore.
- **Humidifier-stuck-3h+ at 96.7% RH** — relates to backlog 999.29 (recovery-aware PWM duty cap auto-lift when RH error is large).

## Traceability

| REQ-ID | Phase | Final Status |
|--------|-------|--------------|
| HUMID-01..04 | 27 | ✓ Complete (farmer-attested 2026-05-02) |
| MODE-01..05 | 28 | ✓ Complete (shipped 2026-05-07/08) |
| ALRT-08, ALRT-09 | 29 | ✓ Complete (shipped 2026-05-08) |
| ALRT-10 | 29 | ⊘ Deferred (calendar-gated) |
| SCHED-01..03 | 30 | ✓ Complete (smoke PASSED 2026-05-09) |
| EXPT-01..03 | 31 | ✓ Complete (bridge curl path proven 2026-05-09; Signal blocked on signal-cli reinit) |
