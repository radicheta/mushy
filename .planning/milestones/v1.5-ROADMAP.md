# v1.5 — Analog Humidity Control & Condensation/Evaporation Forcing — ARCHIVED 2026-05-09

**Shipped:** 2026-05-09 (9-day milestone, 2026-05-01 → 2026-05-09)
**Phases:** 27, 28, 29, 30, 31 (5 phases, 26 plans)
**Status:** SHIPPED with TECH DEBT (16/17 requirements satisfied; ALRT-10 calendar-deferred)
**Audit:** `.planning/v1.5-MILESTONE-AUDIT.md` → `tech_debt` (no critical blockers)

> Sister archive: `v1.5.0.1-ROADMAP.md` (resilience hotfix from 2026-05-02 incident; Phases 27.1 + 27.2 shipped 2026-05-07).

## Stats

- **Commits:** 175 in v1.5 range (`ae18915..HEAD`)
- **Files changed:** 59
- **LOC:** +10,543 / -362 (net +10,181)
- **Plans:** 26/26 complete (5 phases)
- **Timeline:** 9 days

## Key Accomplishments

1. **PID + slow-PWM humidity control** (Phase 27, shipped 2026-05-02, farmer-attested HUMID-04). Replaced bang-bang controller with PID emitting 0–100% duty cycle on `fc1/actuators/humidifier_duty`; slow-PWM actuator translates to time-proportional on/off windows. Closed the structural ±2% RH ceiling proven 2026-04-11.

2. **Mode primitive + 2 baseline modes + runtime config delivery** (Phase 28, shipped 2026-05-07/08). Wrapped PID in named-mode abstraction; `fruiting` and `pinning` baselines; `current_mode` topic; Layer 1 (`/control/param`) + Layer 2 (`/control/persist` → `runtime_overrides.yaml`) for live tuning without redeploy. Closed SEED-001 (config-edit-requires-redeploy pain). Schema reconciled with SEED-004 (`target_humidity, band_low, band_high, defend_side`).

3. **Alerter mode awareness + cooldown tuning** (Phase 29, shipped 2026-05-08). Alerter consumes `current_mode` topic instead of static env vars. Closed backlog 999.22 (RH target/band hidden in `.env`). Swept `config.js` for env-hidden farmer-meaningful knobs (heartbeat hour, humidifier-stuck threshold, RH OOB grace, pi/sensor offline minutes) and routed through dynamic source.

4. **Time-of-day mode scheduling** (Phase 30, shipped 2026-05-09). Declarative `schedule_windows` JSON config; scheduler issues mode switches at window boundaries; persisted via Layer 2; survives fc-core restart. Closed backlog 999.23. Side-finding: scheduler holds current mode in gaps between windows (CONTEXT D-08 design).

5. **Experimental forcing modes** (Phase 31, shipped 2026-05-09). `force-condensation` (100% duty) and `force-evaporation` (0% duty) timed override modes with auto-revert. `fc_experiments` TimescaleDB table logs start/end + measured RH delta for farmer review. Bridge HTTP path proven E2E (DB row 7); Signal command path code-correct but blocked on pre-existing signal-cli linked-secondary limitation (not a Phase 31 regression).

## Phases

### Phase 27: PID + time-proportional duty-cycle primitive — SHIPPED 2026-05-02

**Goal:** Replace bang-bang humidifier control with PID + slow-PWM. Closed structural ±2% RH ceiling. Acceptance: ±0.5% RH over 2h farmer-attested soak (HUMID-04).

**Requirements:** HUMID-01..04 ✓ (farmer-attested 2026-05-02)

**Plans:** 5/5
- 27-01: Wave 0 — vendor simple-pid, RED tests, dwell sweep, fc_config.yaml params
- 27-02: Wave 1 — fc_pwm_driver node (slow-PWM + GPIO27 ownership) + launch wiring
- 27-03: Wave 2 — fc_controller PID refactor (Mode C bypass + ramp + bumpless transfer)
- 27-04: Wave 3 — bridge subscription on humidifier_duty
- 27-05: Wave 4 — deploy + 2h HUMID-04 soak

### Phase 28: Mode primitive + 2 baseline modes + runtime config delivery — SHIPPED 2026-05-07/08

**Goal:** Wrap PID primitive in named-mode abstraction. Ship `fruiting` and `pinning`. Live mode-switching via ROS service. `current_mode` topic. Runtime config path (no deploy cycle). Closes SEED-001; reconciles MODE-01 schema with SEED-004.

**Requirements:** MODE-01..05 ✓

**Plans:** 7/7
- 28-01: fc_msgs skeleton (Mode.msg + SetMode.srv) + Wave 0 spike (rclnodejs SetParameters shape, fc_buffer HTTP relay path)
- 28-02: fc_config.yaml modes block (D-05 fruiting / D-06 pinning) + fc_core depends on fc_msgs
- 28-03: fc_controller.py — ModeView + _resolve_active_mode + band-aware error projection + ramp-to-defended-edge + nearest-defended-edge bypass
- 28-04: current_mode topic + on_set_parameters_callback validator + set_mode service + bumpless mode-swap
- 28-05: bridge POST /control/param (Layer 1) with allowlist + batched-coupled-band edits — control_param.js
- 28-06: bridge POST /control/persist (Layer 2) atomic overlay write to runtime_overrides.yaml via fc_buffer HTTP — control_persist.js
- 28-07: fc.launch.py conditional overlay load + deploy.sh fixes + fc1 integration verification

**Tests:** 86 pytest + 156 jest GREEN.

### Phase 29: Alerter mode awareness + cooldown tuning — SHIPPED 2026-05-08

**Goal:** Alerter consumes live mode/band state from controller. Closes backlog 999.22 (env-hidden ops thresholds). Carries Phase 20 (alert cooldown tuning) from v1.3.

**Requirements:** ALRT-08, ALRT-09 ✓; ALRT-10 ⊘ deferred (calendar-gated, backlog 999.20)

**Plans:** 7/7
- 29-01: Wave 0 — bridge param-set allowlist extension + jest fixtures
- 29-02: Wave 1 — bridge ROS subs + WS broadcast + on-connect replay (3 new topics)
- 29-03: Wave 1 — fc_controller publishers + Tier B/C param decls + validator extension + fc_config.yaml seed
- 29-04: Wave 2 — alerter state.js + index.js routing + config.js fields
- 29-05: Wave 2 — rules.js freshness gating + offline-blindness (closes 999.39) + message.js pi last-known summary
- 29-06: Wave 3 — cooldown tuning analysis + commit tuned defaults
- 29-07: Wave 4 — deploy fc1+bridge+alerter, on-host smoke, mark 999.22 + 999.39 resolved

### Phase 30: Time-of-day mode scheduling — SHIPPED 2026-05-09

**Goal:** Declarative schedule drives mode switches at window boundaries. Closes backlog 999.23. Default profile = constant-single-mode (backward compatible).

**Requirements:** SCHED-01..03 ✓ (Layer 1+2 smoke PASSED; farmer attestation of 30-03-SMOKE.md pending)

**Plans:** 3/3
- 30-01: Wave 1 — scheduler.py pure helpers + fc_controller integration (schedule_windows param decl, _validate_params arm, 30s timer, startup alignment, in-process mode swap with source='scheduler')
- 30-02: Wave 1 — bridge allowlist extension (control_param.js entrySchedule helper)
- 30-03: Wave 2 — fc_config.yaml schedule_windows: "[]" default, deploy verify, end-to-end Layer 1+Layer 2 smoke (boundary transition + restart survival), farmer attestation checkpoint

**Smoke evidence:** `.planning/phases/30-time-of-day-mode-scheduling/30-03-SMOKE.md`. Boundary transition `fruiting → pinning at 22:47` confirmed; Layer 2 persist + restart survival verified via `ros2 param get`.

**Side-finding:** Scheduler keeps current mode in gaps between windows (CONTEXT D-08 design — no auto-revert to config_default). One-shot windows leave mode "stuck" until next match, manual reset, or restart. Worth surfacing in farmer-facing docs.

### Phase 31: Experimental forcing modes — SHIPPED 2026-05-09

**Goal:** Two timed override modes the farmer can trigger for short experiments. `force-condensation` (100% duty for N minutes) and `force-evaporation` (0% duty for N minutes), both with auto-revert. Start/end + measured RH delta logged to TimescaleDB.

**Requirements:** EXPT-01..03 ✓ (bridge curl path PROVEN E2E; Signal command path blocked on pre-existing signal-cli deviceId=2 — not a Phase 31 regression)

**Plans:** 4/4
- 31-01: Foundation — StartExperiment / CancelExperiment srv defs + force-* mode config in fc_config.yaml
- 31-02: Controller wiring — force-mode short-circuit (duty=1.0/0.0) + experiment services + 1Hz TTL timer auto-revert + ActiveExperiment dataclass + scheduler suppression + BOOT-RECOVERY at __init__
- 31-03: Bridge wiring — POST /control/experiment + /control/cancel-experiment + GET state + DB migration (`fc_experiments` table) + experiment_event topic subscriber INSERT/UPDATE flow
- 31-04: Signal command parser + receive-loop dispatch (`/force-condensation`, `/force-evaporation`, `/cancel-experiment`)

**Acceptance basis:** DB row 7 closed lifecycle (1.00min auto-revert, end_reason='timeout', baseline 96.7% → final 96.65%, Δ=-0.05); fc1 controller logs `[experiment] started` + `[experiment] auto-revert`; bridge logs `[experiment_event] started ... ended`. Saturday lab visit produced 6 prior experiment rows covering force-condensation, force-evaporation, timeout, cancelled outcomes.

**Bug A fixed mid-UAT** (commit `6640af2`): alerter `BRIDGE_HTTP_URL` convention. Phase 31-04 introduced `BRIDGE_URL` defaulting to `http://bridge:8080`, unreachable from alerter (compose-network) since bridge runs in host-network. Fixed to read `BRIDGE_HTTP_URL || BRIDGE_URL || legacy fallback`.

**Bug B pre-existing** (not Phase 31 regression): signal-cli registered as deviceId=2 (linked-secondary); `/v1/receive` returns empty arrays; Signal DataMessages deliver only to primary. Tracked in memory `project_signal_cli_primary_reregister_path`. Phase 25 spike planned re-registration via 4G router SIM SMS; never executed.

**Tech debt carried:** Tests 6 (boot-recovery D-09 LOAD-BEARING) + 7 (scheduler suppression) deferred but unit-test-covered. fc_experiments id=2 NULL/orphan row from saturday lab (low-pri).

## Cross-Phase Integration (verified live this session)

- Phase 28 → 29 → 30 → 31 chain exercised consecutively: pinning window fired at 22:47 (Phase 30 scheduler), manually reverted at 22:51 (Phase 28 set_mode service), schedule persisted for 09:00 tomorrow (Phase 28 Layer 2), fc-core restart loaded persisted schedule, baseline restored.
- Bridge HTTP allowlist: `schedule_windows` + `active_mode` + Phase 31 experiment endpoints all routed through `control_param.js` + `control_experiment.js` + `control_persist.js` without conflict.
- TimescaleDB telemetry: `humidity_target` / `pid_output` / `humidifier_duty` persisting at ~1Hz; `fc_experiments` table populated correctly with NULL-safe deltas.
- Alerter ↔ bridge: receive-loop dispatch path repaired (BRIDGE_HTTP_URL fix); outbound Signal alerts unaffected.

## Tech Debt Carried Forward

- **ALRT-10** alert cooldown tuning (calendar-gated, ≥2 weeks observation data).
- **signal-cli primary re-registration** — prerequisite for Signal-driven Phase 31 UAT.
- **Phase 28 + 30 missing VERIFICATION.md** — workflow gap, not code; functional evidence in SUMMARY.md + SMOKE.md.
- **Phase 31 boot-recovery D-09 + scheduler suppression live attestation** — runnable via direct bridge curl + ssh restart.
- **humidity_target post-restart re-init bug** — transient cosmetic overshoot, opt-ignore.
- **Humidifier-stuck-3h+ at 96.7% RH** — relates to backlog 999.29 (recovery-aware PWM duty cap).
- **MC widget for active experiment state** — file as 999.x.

## What Got Validated by Operating in Production

- 9-day cadence (2026-05-01 → 2026-05-09) — including a 5-day v1.5.0.1 detour from the 2026-05-02 blackout incident — produced a working PID + mode + scheduler + experiment stack on real hardware, attested by the farmer for HUMID-04 and operating continuously through fc-core restarts and DDS transport switches.
- Live exercise of Phase 28→31 cross-phase wiring proved the integration without a separate integration phase.

## Next Milestone Seeds

- **Mode editor UI in farmer app** (depends on 999.11 farmer-app maturity)
- **Schedule gap-mode default** (default_mode_in_gap parameter from 30-03 side-finding)
- **MC active-experiment widget** (cosmetic gap from Phase 31)
- **Per-mode PID gains** (stage-aware tuning)
- **Stage-triggered mode transitions** (depends on farmOS event integration)
- **signal-cli primary re-registration** (unlocks Signal-driven Phase 31 UAT + future bidirectional commands)
