---
phase: 21
plan: 04
type: execute
wave: 3
depends_on: [21-02, 21-03]
files_modified:
  - src/mission-control/bridge/src/index.js
  - src/mission-control/frontend/plugins/fruiting-chamber/plugin.js
autonomous: false
requirements: [D-06b]
user_setup: []

must_haves:
  truths:
    - "GET /health returns a new `snapshots` object containing `last_24h` (integer count) and `oldest_at` (ISO string or null)"
    - "/health also exposes flat aliases `snapshots_last_24h` and `oldest_snapshot_at` for consumers using that shape"
    - "/health continues to return 200 even if the snapshots sub-query fails (swallowed error; fields null)"
    - "Phase 16 system-health panel renders a new 'Snapshots' status light fed by /health.snapshots.last_24h"
    - "Farmer sees the chip green when last_24h >= floor (200), red when 0, grey when unknown or bridge unreachable"
  artifacts:
    - path: "src/mission-control/bridge/src/index.js"
      provides: "Extended async /health handler with snapshots: {last_24h, oldest_at} + flat aliases"
      contains: "snapshots_last_24h"
    - path: "src/mission-control/frontend/plugins/fruiting-chamber/plugin.js"
      provides: "New 'Snapshots' status light wired into pollHealth"
      contains: "lights.snapshots"
  key_links:
    - from: "app.get('/health')"
      to: "snapshots hypertable"
      via: "two aggregate queries (COUNT 24h, MIN captured_at)"
      pattern: "FROM snapshots"
    - from: "plugin.js pollHealth()"
      to: "/health.snapshots"
      via: "fetch().then(data) block"
      pattern: "data.snapshots"
---

<objective>
Expose D-06b so the farmer has a regression signal. `/health` gains `snapshots.last_24h` and `snapshots.oldest_at`. The Phase 16 system-health panel gets a new "Snapshots" chip that drops to red when the pipeline stops producing rows.

Purpose: The invariant change in Plan 02 is silent — without surfacing `snapshots_last_24h`, a broken persister would only be discovered when Phase 22 tries to scrub. This chip turns the pipeline into an always-visible health signal.

Output: Extended /health JSON + new health-panel chip. Ends with a human checkpoint because rendering requires a browser.
</objective>

<execution_context>
@/home/santi/.claude/get-shit-done/workflows/execute-plan.md
@/home/santi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/21-camera-history-continuous-persistence/21-CONTEXT.md
@.planning/phases/21-camera-history-continuous-persistence/21-RESEARCH.md
@.planning/phases/21-camera-history-continuous-persistence/21-02-SUMMARY.md
@.planning/phases/21-camera-history-continuous-persistence/21-03-SUMMARY.md
@src/mission-control/bridge/src/index.js
@src/mission-control/frontend/plugins/fruiting-chamber/plugin.js

<interfaces>
Current /health handler (index.js:170) is SYNCHRONOUS. Must change to `async` to await DB aggregates. Express 5 handles async handlers natively.

Phase 16 plugin (plugin.js:493-632) renders 6 lights via `makeStatusLight(container, name)`.
Polling loop at lines 577-627 inside `pollHealth()`. Lights expose `setGreen(tooltip)`, `setRed(tooltip)`, `setGrey(tooltip)`.

Farmer chip floor for 24h count:
- 5-min cadence x 288 ticks/day = 288 expected
- Floor 200 (generous — absorbs bridge restarts + stall recovery)
- Red at 0 (pipeline broken)
- Grey between 1..199 with count-in-tooltip (degraded but not dead)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Extend /health with snapshots.last_24h and snapshots.oldest_at</name>
  <files>src/mission-control/bridge/src/index.js</files>
  <read_first>
    - src/mission-control/bridge/src/index.js lines 170-190 (current synchronous /health handler)
    - .planning/phases/21-camera-history-continuous-persistence/21-RESEARCH.md Pattern 6
    - .planning/phases/21-camera-history-continuous-persistence/21-CONTEXT.md D-06b
  </read_first>
  <action>
    Replace the existing /health handler in `src/mission-control/bridge/src/index.js` at line 170 with an async version. Keep every existing field (status, db, ros, camera, humidifier) byte-for-byte and ADD a new top-level `snapshots` object plus flat aliases.

    Exact new handler:

    ```
    app.get('/health', async (req, res) => {
        const lastFrameAgeSec = lastFrameTime === null
            ? null
            : Math.round((Date.now() - lastFrameTime) / 1000);

        let snapshotsLast24h = null;
        let oldestSnapshotAt = null;
        if (dbReady) {
            try {
                const [countRow, oldestRow] = await Promise.all([
                    pool.query("SELECT COUNT(*)::int AS n FROM snapshots WHERE captured_at > NOW() - INTERVAL '24 hours'"),
                    pool.query("SELECT MIN(captured_at) AS oldest FROM snapshots")
                ]);
                snapshotsLast24h = countRow.rows[0].n;
                oldestSnapshotAt = oldestRow.rows[0].oldest === null
                    ? null
                    : oldestRow.rows[0].oldest.toISOString();
            } catch (e) {
                console.error('[health] snapshots stats failed:', e.message);
            }
        }

        res.json({
            status: 'ok',
            db: dbReady,
            ros: { connected: rosReady },
            camera: {
                lastFrame: lastFrameTime,
                last_frame_age_sec: lastFrameAgeSec,
                clients: mjpegClients.size,
                subscribed: cameraSubscription !== null
            },
            humidifier: { last_msg_ts: humidifierLastMsgTs },
            snapshots: { last_24h: snapshotsLast24h, oldest_at: oldestSnapshotAt },
            snapshots_last_24h: snapshotsLast24h,
            oldest_snapshot_at: oldestSnapshotAt
        });
    });
    ```

    Do NOT touch /farmer/summary — it is owned by Phase 18 and has its own consumer.
  </action>
  <verify>
    <automated>grep -q "app.get('/health', async" src/mission-control/bridge/src/index.js &amp;&amp; grep -q "snapshots_last_24h" src/mission-control/bridge/src/index.js &amp;&amp; grep -q "oldest_snapshot_at" src/mission-control/bridge/src/index.js &amp;&amp; node --check src/mission-control/bridge/src/index.js</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "app.get\('/health', async" src/mission-control/bridge/src/index.js
    - grep -q "SELECT COUNT" src/mission-control/bridge/src/index.js AND grep -q "FROM snapshots" src/mission-control/bridge/src/index.js
    - grep -q "SELECT MIN(captured_at)" src/mission-control/bridge/src/index.js
    - grep -q "snapshots_last_24h" src/mission-control/bridge/src/index.js
    - grep -q "oldest_snapshot_at" src/mission-control/bridge/src/index.js
    - node --check src/mission-control/bridge/src/index.js
    - After docker compose up -d --build bridge: curl http://localhost:8081/health shows both nested snapshots object and flat aliases
    - With timescale stopped, /health still returns 200 and snapshots.last_24h is null (no 500)
  </acceptance_criteria>
  <done>/health returns nested `snapshots` object and flat aliases; graceful on DB failure; no other /health fields altered.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add "Snapshots" status light to Phase 16 panel</name>
  <files>src/mission-control/frontend/plugins/fruiting-chamber/plugin.js</files>
  <read_first>
    - src/mission-control/frontend/plugins/fruiting-chamber/plugin.js lines 560-630 (makeStatusLight block + pollHealth handler)
    - src/mission-control/frontend/plugins/fruiting-chamber/plugin.js lines 112-114 (setGreen/setRed/setGrey convention)
    - .planning/phases/21-camera-history-continuous-persistence/21-RESEARCH.md Pattern 6 (frontend plugin diff notes)
    - Plan 04 Task 1 /health shape
  </read_first>
  <action>
    In `src/mission-control/frontend/plugins/fruiting-chamber/plugin.js`:

    1. Add the light in the `show:` function immediately after `lights.grace = makeStatusLight(container, 'Grace');` (around line 569):

    ```
    lights.snapshots  = makeStatusLight(container, 'Snapshots');
    ```

    It is auto-picked up by the existing `Object.keys(lights).forEach(...).setGrey('waiting for first /health response')` loop at line 571.

    2. Inside `pollHealth()`, after the Humidifier block and before `updateSensorsAndGraceLights();` (insert around line 617), add:

    ```
    var snap = data.snapshots || {};
    var last24 = (typeof snap.last_24h === 'number') ? snap.last_24h : null;
    var oldest = snap.oldest_at || null;
    if (last24 === null) {
        lights.snapshots.setGrey('snapshots stats unavailable (DB down?)');
    } else if (last24 === 0) {
        lights.snapshots.setRed('0 snapshots in last 24h — persister broken');
    } else if (last24 < 200) {
        lights.snapshots.setGrey(last24 + ' snapshots in last 24h (degraded; oldest ' + (oldest || 'unknown') + ')');
    } else {
        lights.snapshots.setGreen(last24 + ' snapshots in last 24h (oldest ' + (oldest || 'none') + ')');
    }
    ```

    3. In the `.catch` block around line 621, after `lights.humidifier.setGrey('bridge unreachable');`, add:

    ```
    if (lights.snapshots) lights.snapshots.setGrey('bridge unreachable');
    ```

    Do NOT change the WebSocket logic or the makeStatusLight helper.
  </action>
  <verify>
    <automated>grep -q "lights.snapshots" src/mission-control/frontend/plugins/fruiting-chamber/plugin.js &amp;&amp; grep -q "'Snapshots'" src/mission-control/frontend/plugins/fruiting-chamber/plugin.js &amp;&amp; grep -q "data.snapshots" src/mission-control/frontend/plugins/fruiting-chamber/plugin.js &amp;&amp; node --check src/mission-control/frontend/plugins/fruiting-chamber/plugin.js</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "lights.snapshots  = makeStatusLight(container, 'Snapshots')" src/mission-control/frontend/plugins/fruiting-chamber/plugin.js
    - grep -q "data.snapshots" src/mission-control/frontend/plugins/fruiting-chamber/plugin.js
    - grep -q "0 snapshots in last 24h" src/mission-control/frontend/plugins/fruiting-chamber/plugin.js
    - node --check src/mission-control/frontend/plugins/fruiting-chamber/plugin.js
    - After docker compose up -d --build openmct bridge, browser load of Mission Control shows 7 lights (6 existing + new 'Snapshots')
  </acceptance_criteria>
  <done>Snapshots light added in show, wired in pollHealth with green/red/grey thresholds, grey fall-through on bridge unreachable.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verification — farmer sees the Snapshots chip behave</name>
  <files>N/A (human UAT of Plan 04 Tasks 1 and 2)</files>
  <action>Claude runs the automated stack checks below, then hands off to the human for browser-based UI confirmation. This is a human-verify checkpoint — no code changes in this task.</action>
  <verify>See how-to-verify block.</verify>
  <done>Human types "approved" after observing the Snapshots chip in Mission Control.</done>
  <what-built>
    - /health JSON includes snapshots.last_24h (count) + snapshots.oldest_at (ISO) plus flat aliases
    - Phase 16 panel renders a new 'Snapshots' chip with green/red/grey thresholds
    - Pipeline is fully continuous end-to-end: Plan 02 invariant + Plan 03 retention + /camera/history + Plan 04 health chip
  </what-built>
  <how-to-verify>
    Claude MUST run these automated checks itself (per memory `feedback_run_verifications_yourself`) before stalling the human:

    1. From repo root:
       - `docker compose up -d --build bridge openmct`
       - `docker compose ps` — bridge and openmct are running and healthy
    2. Wait ~15 min (3 tick cycles at 5 min cadence). Then:
       - `curl -s http://localhost:8081/health | python3 -m json.tool` — confirm `snapshots.last_24h >= 3` and `snapshots.oldest_at` is non-null
       - `docker compose exec -T timescale psql -U postgres -t -c "SELECT source, count(*) FROM snapshots GROUP BY source;"` — at least one `idle` row
       - `curl -s "http://localhost:8081/camera/history?from=$(( $(date +%s%3N) - 86400000 ))&to=$(date +%s%3N)" | python3 -c "import json,sys; d=json.load(sys.stdin); print('count', d['count'], 'has_more', d['has_more'])"` — non-empty count
       - `bash scripts/verify/phase-21-smoke.sh` — exits clean
    3. Only if all automated checks pass, hand off to the human:

    Human verification steps:

    1. Open Mission Control at `http://10.68.155.50:8080` (or whichever CORS_ORIGIN is configured).
    2. Navigate to the system-health panel (same view that shows the other 6 lights from Phase 16).
    3. Expected: a 7th chip labeled "Snapshots" in the strip.
    4. Expected state:
       - If it has been running for less than 24h with >=200 snapshots persisted: green, tooltip shows "N snapshots in last 24h (oldest ISO-timestamp)"
       - If this is a fresh install with <200 snapshots so far: grey, tooltip shows the actual count
       - Red should only appear if the pipeline is broken (count 0)
    5. Optional negative test: `docker compose stop bridge` → chip drops to grey "bridge unreachable" within ~2s polling. Then `docker compose start bridge` → chip recovers.
  </how-to-verify>
  <resume-signal>Type "approved" once the Snapshots chip renders and matches the expected tooltip in Mission Control, or describe any UI issue (wrong color, missing tooltip, wrong count).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP client -> /health | Already public on LAN/Tailscale; no auth. New snapshots fields are read-only aggregate counts — no PII. |
| browser (MC) -> /health | Same origin via existing CORS allowlist (index.js:155-166). No new route, no new origin. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-15 | DoS | /health aggregate queries slow under load | mitigate | Two COUNT/MIN queries against an indexed hypertable; at Phase 21 volumes (<100k rows) both return in <5ms. If latency becomes a concern, planner can cache with 5s TTL — deferred. |
| T-21-16 | DoS | /health 5xx on DB failure | mitigate | try/catch swallows sub-query errors; /health returns 200 with null fields (Pattern 6 rationale). Acceptance test explicitly verifies this. |
| T-21-17 | Info Disclosure | `oldest_snapshot_at` leaks install-age | accept | Install age is already inferable from git log / telemetry oldest row. No new disclosure surface. |
</threat_model>

<verification>
- curl /health shows new snapshots fields and flat aliases
- Forced DB-down test returns 200 with null snapshots fields
- Browser renders 7 lights on the health panel strip
- Snapshots chip responds to bridge stop/start within 2s
- Plan 01's smoke script still exits clean
</verification>

<success_criteria>
- /health returns nested snapshots object AND flat aliases
- /health gracefully degrades when DB is unreachable
- Phase 16 panel shows a 7th chip with correct green/red/grey thresholds
- Human has visually confirmed the chip on Mission Control
</success_criteria>

<output>
Create `.planning/phases/21-camera-history-continuous-persistence/21-04-SUMMARY.md` per the standard summary template.
</output>
