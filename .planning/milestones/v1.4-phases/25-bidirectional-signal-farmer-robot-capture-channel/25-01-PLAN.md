---
phase: 25
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - docker-compose.override.yml
  - src/agents/alerter/test/fixtures/envelopes/text.json
  - src/agents/alerter/test/fixtures/envelopes/audio.json
  - src/agents/alerter/test/fixtures/envelopes/photo-batch.json
  - src/agents/alerter/test/fixtures/envelopes/snooze.json
  - src/agents/alerter/test/capture.test.js
  - src/agents/alerter/test/transcribe-client.test.js
  - src/agents/alerter/test/llm-client.test.js
  - src/agents/alerter/test/helpers/fake-whisper-server.js
  - scripts/wave0-signal-receive-smoke.sh
autonomous: false
requirements: [R1]
user_setup:
  - service: signal-cli
    why: "Primary re-registration of +59891840205 requires SMS verification; SMS code only retrievable from Huawei B310s-518 admin UI"
    env_vars:
      - name: ANTHROPIC_API_KEY
        source: "Anthropic Console -> API Keys (Wave 0 confirms it is added to repo-root .env before Wave 3)"
    dashboard_config:
      - task: "Read Signal verification SMS from Huawei admin UI"
        location: "http://192.168.8.1 (LAN of B310s-518); admin password in /tmp/huawei on elder-plops; per spike 001 README"
must_haves:
  truths:
    - "MODE=normal flag is set in docker-compose.override.yml for signal-cli service"
    - "+59891840205 is registered as PRIMARY (device_id=1) on signal-cli"
    - "GET /v1/receive/+59891840205 returns HTTP 200 with empty array (or envelope list) — NOT HTTP 400"
    - "Farmer identity +59892893012 is trusted via trust_all_known_keys"
    - "Outbound /v2/send still works after MODE flip (regression check)"
    - "Wave 0 test fixtures exist for downstream waves to consume"
  artifacts:
    - path: "docker-compose.override.yml"
      provides: "MODE=normal for signal-cli service"
      contains: "MODE=normal"
    - path: "src/agents/alerter/test/fixtures/envelopes/text.json"
      provides: "fixture for text-only envelope"
    - path: "src/agents/alerter/test/fixtures/envelopes/audio.json"
      provides: "fixture for audio-attachment envelope"
    - path: "src/agents/alerter/test/fixtures/envelopes/photo-batch.json"
      provides: "fixture for 3-photo batch envelope"
    - path: "src/agents/alerter/test/fixtures/envelopes/snooze.json"
      provides: "fixtures for mute/snooze/quiet keyword envelopes"
    - path: "src/agents/alerter/test/capture.test.js"
      provides: "RED skeleton test for capture pipeline (R2,R6)"
    - path: "src/agents/alerter/test/transcribe-client.test.js"
      provides: "RED skeleton test for transcribe-client (R3)"
    - path: "src/agents/alerter/test/llm-client.test.js"
      provides: "RED skeleton test for llm-client (R5,R6)"
    - path: "src/agents/alerter/test/helpers/fake-whisper-server.js"
      provides: "fake whisper HTTP server harness mirroring fake-signal-server"
    - path: "scripts/wave0-signal-receive-smoke.sh"
      provides: "smoke verifier for /v1/receive HTTP-200 + /v2/send round-trip"
  key_links:
    - from: "docker-compose.override.yml"
      to: "signal-cli container env"
      via: "MODE=normal env var"
      pattern: "MODE=normal"
    - from: "scripts/wave0-signal-receive-smoke.sh"
      to: "/v1/receive/+59891840205"
      via: "curl HTTP GET"
      pattern: "v1/receive"
---

<objective>
Unblock the signal-cli receive pipe so downstream waves (capture persistence, whisper, LLM) can plug into a working envelope source. Three things must change in lockstep: (a) flip MODE=json-rpc → MODE=normal in `docker-compose.override.yml` so `/v1/receive` is HTTP not WebSocket (Pitfall 1); (b) re-register `+59891840205` as PRIMARY device via SMS verification through B310s-518 (linked-secondary mode cannot use /v1/receive — Pitfall 2); (c) restore farmer identity trust after the rebuild (Pitfall 3). Also produce all Wave 0 test fixtures and skeleton tests so Waves 1–4 are TDD-ready (no scavenger hunts mid-implementation).

Purpose: R1 is load-bearing. Without /v1/receive returning envelopes, R2–R7 cannot be implemented or verified. The pre-gate spike (001) proved SMS roundtrip on B310s-518 — this plan executes the resulting recipe.

Output: Live signal-cli pipe + 4 envelope fixtures + 3 skeleton tests + fake whisper helper + receive smoke script + outbound send regression-checked.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/25-bidirectional-signal-farmer-robot-capture-channel/25-SPEC.md
@.planning/phases/25-bidirectional-signal-farmer-robot-capture-channel/25-CONTEXT.md
@.planning/phases/25-bidirectional-signal-farmer-robot-capture-channel/25-RESEARCH.md
@.planning/phases/25-bidirectional-signal-farmer-robot-capture-channel/25-PATTERNS.md
@.planning/spikes/001-huawei-router-sms-roundtrip/README.md

<interfaces>
<!-- Existing signal.js receive() and send() signatures the smoke script + tests will hit -->

From src/agents/alerter/src/signal.js (current — Wave 1 will modify):
```javascript
// receive() — currently hits /v1/receive/{sender}?timeout=1&ignore_attachments=true
async function receive({ timeoutSec = 1 } = {}) { ... }
// send() — hits /v2/send
async function send(body, { bypassCap = false } = {}) { ... }
// accounts() — hits /v1/accounts
async function accounts() { ... }
```

From src/agents/alerter/test/helpers/fake-signal-server.js:
- exports `start({ port = 0 })` returning `{ url, sent, received, statusOverride, delayMs, close }`
- Mirror this exact shape for fake-whisper-server.js

Signal-cli REST endpoints used in this plan (LIVE, not stubs):
- GET  http://localhost:8080/v1/accounts                                  — list accounts + device_id
- POST http://localhost:8080/v1/register/{number}                         — start registration
- POST http://localhost:8080/v1/register/{number}/verify/{code}           — submit SMS code
- GET  http://localhost:8080/v1/receive/{number}?timeout=1                — pull envelopes (200 = ok, 400 = WS-only-mode or linked-secondary)
- PUT  http://localhost:8080/v1/identities/{ours}/trust/{theirs}          — body {"trust_all_known_keys": true}
- POST http://localhost:8080/v2/send                                      — outbound regression check
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Flip MODE=normal + primary re-register +59891840205 + trust farmer + receive smoke</name>
  <read_first>
    - .planning/phases/25-bidirectional-signal-farmer-robot-capture-channel/25-RESEARCH.md (Pitfall 1, 2, 3, 7; Runtime State Inventory)
    - .planning/spikes/001-huawei-router-sms-roundtrip/README.md (B310s-518 admin UI recipe; password is in /tmp/huawei on elder-plops, NOT the typo'd Shiitake1! in the README body)
    - docker-compose.override.yml (current MODE=json-rpc on signal-cli service, line ~31)
  </read_first>
  <files>docker-compose.override.yml</files>
  <action>
Sequence (operator-driven; Claude prepares the diffs and helper commands, operator executes the SMS-verification step).

STEP A — flip MODE in docker-compose.override.yml (autonomous diff Claude can write):
  Change `signal-cli.environment.MODE=json-rpc` → `MODE=normal`.
  Keep volume `signal-cli-data:/home/.local/share/signal-cli` and the `signal-net` network unchanged.

STEP B — recreate signal-cli with the new MODE (operator runs):
  cd /mnt/slime-kingdom/opt/mushy
  docker compose up -d --force-recreate signal-cli
  docker compose logs --tail 30 signal-cli   # confirm "started" + no startup error

STEP C — verify nvidia-container-toolkit is present on host (Pitfall 7 — fails fast for Wave 2):
  docker info 2>/dev/null | grep -i 'Runtimes:.*nvidia' || echo "MISSING — install: sudo apt install nvidia-container-toolkit && sudo systemctl restart docker"
  (NOT blocking for this plan; just record presence in summary so Wave 2 doesn't re-discover.)

STEP D — primary re-registration of +59891840205 (HUMAN STEP — Claude cannot read the SMS):
  # If the existing account is linked-secondary, signal-cli register will replace it.
  curl -s -X POST 'http://localhost:8080/v1/register/+59891840205' -H 'Content-Type: application/json' -d '{"use_voice": false, "captcha": ""}'
  # ^ if this fails with "captcha required", operator obtains a captcha token from https://signalcaptchas.org/registration/generate.html and re-runs with it.
  # Operator opens http://192.168.8.1 (B310s-518 admin UI; password from /tmp/huawei on elder-plops, value: Shiitake1 — NOT Shiitake1!), reads SMS inbox for the verification code (6 digits).
  curl -s -X POST 'http://localhost:8080/v1/register/+59891840205/verify/{CODE}' -H 'Content-Type: application/json' -d '{}'
  curl -s 'http://localhost:8080/v1/accounts' | jq    # expect device_id: 1 for +59891840205

STEP E — restore farmer trust (Pitfall 3; per memory project_signal_cli_rebuild_breaks_trust):
  curl -s -X PUT 'http://localhost:8080/v1/identities/+59891840205/trust/+59892893012' \
    -H 'Content-Type: application/json' \
    -d '{"trust_all_known_keys": true}' | jq

STEP F — outbound regression (A1 verification — MODE=normal must not break /v2/send):
  curl -s -X POST 'http://localhost:8080/v2/send' \
    -H 'Content-Type: application/json' \
    -d '{"message":"phase 25 wave 0 smoke — please ignore","number":"+59891840205","recipients":["+59892893012"]}' | jq
  # Operator confirms farmer received the SMS-style Signal message.

STEP G — receive smoke:
  curl -sS -o /tmp/recv.json -w "%{http_code}\n" 'http://localhost:8080/v1/receive/+59891840205?timeout=1'
  # MUST print 200 (not 400). cat /tmp/recv.json — empty array or envelope list, both acceptable.
  # If still 400: stop. Either MODE flip not applied, or account is still linked-secondary. Re-check device_id in step D.

STEP H — round-trip receive: operator sends a Signal message from the farmer's phone (+59892893012) → poll /v1/receive again → expect envelopes[0].envelope.source == "+59892893012" and dataMessage.message present.

Capture all curl responses into the SUMMARY (mask numbers per maskNumber pattern in src/agents/alerter/src/config.js lines 52-55 when paraphrasing).
  </action>
  <acceptance_criteria>
    - `grep -n 'MODE=normal' docker-compose.override.yml` returns one match on the signal-cli service block
    - `grep -n 'MODE=json-rpc' docker-compose.override.yml` returns ZERO matches
    - `curl -s http://localhost:8080/v1/accounts | jq -r '.[] | select(.number=="+59891840205") | .device_id'` returns `1`
    - `curl -s -o /dev/null -w "%{http_code}" http://localhost:8080/v1/receive/+59891840205?timeout=1` returns `200`
    - `curl -s -X PUT http://localhost:8080/v1/identities/+59891840205/trust/+59892893012 -H 'Content-Type: application/json' -d '{"trust_all_known_keys": true}'` returns 2xx (no error body)
    - `curl -s -X POST http://localhost:8080/v2/send -H 'Content-Type: application/json' -d '{"message":"smoke","number":"+59891840205","recipients":["+59892893012"]}' | jq -r '.timestamp'` returns a numeric value
    - Operator sends a Signal message from farmer's phone → next /v1/receive call returns at least one envelope with `.envelope.source == "+59892893012"`
  </acceptance_criteria>
  <what-built>MODE=normal flag in compose; primary registration of +59891840205; farmer trust restored; receive pipe verified end-to-end</what-built>
  <how-to-verify>
    Operator runs the curl commands in STEP D–H above. Reports the device_id from /v1/accounts, the HTTP code from /v1/receive, and confirms the farmer-side SMS roundtrip succeeded.
  </how-to-verify>
  <resume-signal>Type "approved" with the device_id and HTTP code captured, OR describe the failure (which step, error body)</resume-signal>
  <verify>
    <automated>bash scripts/wave0-signal-receive-smoke.sh</automated>
  </verify>
  <done>
    - MODE=normal in compose; /v1/receive returns 200; farmer trust restored; outbound /v2/send still works; round-trip envelope observed.
    - Device-id, HTTP code, and timestamp values recorded in plan SUMMARY.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create envelope fixtures + receive smoke script + fake whisper helper</name>
  <read_first>
    - src/agents/alerter/test/helpers/fake-signal-server.js (factory shape to mirror)
    - src/agents/alerter/test/receive-loop.test.js (current envelope shape — `env.envelope.source`, `env.envelope.dataMessage.message`, `dataMessage.attachments`)
    - .planning/phases/25-bidirectional-signal-farmer-robot-capture-channel/25-RESEARCH.md (Wave 0 Gaps section)
    - .planning/phases/25-bidirectional-signal-farmer-robot-capture-channel/25-PATTERNS.md (fake-server harness pattern)
  </read_first>
  <files>
    src/agents/alerter/test/fixtures/envelopes/text.json,
    src/agents/alerter/test/fixtures/envelopes/audio.json,
    src/agents/alerter/test/fixtures/envelopes/photo-batch.json,
    src/agents/alerter/test/fixtures/envelopes/snooze.json,
    src/agents/alerter/test/helpers/fake-whisper-server.js,
    scripts/wave0-signal-receive-smoke.sh
  </files>
  <action>
1. Create the 4 envelope fixtures using the exact shape signal-cli REST API returns (verified by reading src/agents/alerter/src/receive-loop.js dispatch logic). Each fixture is a JSON array of one or more envelope objects.

   `text.json` — a single text-only envelope:
   ```json
   [
     { "envelope": {
         "source": "+59892893012",
         "sourceNumber": "+59892893012",
         "sourceUuid": "11111111-1111-1111-1111-111111111111",
         "timestamp": 1714240000000,
         "dataMessage": {
           "timestamp": 1714240000000,
           "message": "logged 3 jars in tent A",
           "expiresInSeconds": 0,
           "viewOnce": false,
           "attachments": []
         }
       }
     }
   ]
   ```

   `audio.json` — one envelope with one audio attachment (per signal-cli envelope shape — `attachments[].id`, `contentType`, `filename`, `size`):
   ```json
   [
     { "envelope": {
         "source": "+59892893012", "sourceNumber": "+59892893012",
         "sourceUuid": "11111111-1111-1111-1111-111111111111",
         "timestamp": 1714240100000,
         "dataMessage": {
           "timestamp": 1714240100000, "message": null,
           "attachments": [
             { "contentType": "audio/aac", "filename": null, "id": "att-audio-001",
               "size": 102400, "width": 0, "height": 0, "voiceNote": true }
           ]
         }
       }
     }
   ]
   ```

   `photo-batch.json` — one envelope with three image attachments (photo batch in a single Signal message):
   ```json
   [
     { "envelope": {
         "source": "+59892893012", "sourceNumber": "+59892893012",
         "sourceUuid": "11111111-1111-1111-1111-111111111111",
         "timestamp": 1714240200000,
         "dataMessage": {
           "timestamp": 1714240200000, "message": "tray 4",
           "attachments": [
             { "contentType": "image/jpeg", "filename": "IMG_001.jpg", "id": "att-img-001", "size": 204800, "width": 1080, "height": 1920 },
             { "contentType": "image/jpeg", "filename": "IMG_002.jpg", "id": "att-img-002", "size": 204800, "width": 1080, "height": 1920 },
             { "contentType": "image/jpeg", "filename": "IMG_003.jpg", "id": "att-img-003", "size": 204800, "width": 1080, "height": 1920 }
           ]
         }
       }
     }
   ]
   ```

   `snooze.json` — three envelopes covering the simple-grammar fast-path keywords (`mute`, `snooze`, `quiet`) plus one strict legacy form (`snooze rh 4h`):
   ```json
   [
     { "envelope": { "source": "+59892893012", "timestamp": 1714240300000, "dataMessage": { "timestamp": 1714240300000, "message": "mute", "attachments": [] } } },
     { "envelope": { "source": "+59892893012", "timestamp": 1714240310000, "dataMessage": { "timestamp": 1714240310000, "message": "snooze", "attachments": [] } } },
     { "envelope": { "source": "+59892893012", "timestamp": 1714240320000, "dataMessage": { "timestamp": 1714240320000, "message": "quiet", "attachments": [] } } },
     { "envelope": { "source": "+59892893012", "timestamp": 1714240330000, "dataMessage": { "timestamp": 1714240330000, "message": "snooze rh 4h", "attachments": [] } } }
   ]
   ```

2. Create `src/agents/alerter/test/helpers/fake-whisper-server.js`. MIRROR the exact factory shape of `fake-signal-server.js` (read it first):
   - Exports `start({ port = 0 } = {})` returning `Promise<{ url, requests, statusOverride, delayMs, transcribeResponse, close }>`.
   - Server handles `POST /transcribe` (200 → returns `transcribeResponse` || `{text:'hello world',duration_ms:1234,language:'en',language_probability:0.98}`; or `statusOverride` if set; or sleeps `delayMs` first to simulate timeout).
   - Also handles `GET /health` → 200 `{ok:true,model_loaded:true}`.
   - `requests[]` records every body received (so capture.test.js can assert audio_path is forwarded).

3. Create `scripts/wave0-signal-receive-smoke.sh` (executable, `chmod +x`):
   ```bash
   #!/usr/bin/env bash
   set -euo pipefail
   API="${SIGNAL_API_URL:-http://localhost:8080}"
   NUM="${SIGNAL_SENDER:-+59891840205}"
   FARMER="${SIGNAL_RECIPIENT:-+59892893012}"

   echo "==> /v1/accounts"
   DEV_ID=$(curl -fsS "$API/v1/accounts" | jq -r --arg n "$NUM" '.[] | select(.number==$n) | .device_id')
   if [[ "$DEV_ID" != "1" ]]; then echo "FAIL: device_id=$DEV_ID (expected 1 — primary)"; exit 1; fi
   echo "ok device_id=1"

   echo "==> /v1/receive HTTP code"
   CODE=$(curl -sS -o /tmp/wave0-recv.json -w "%{http_code}" "$API/v1/receive/$NUM?timeout=1")
   if [[ "$CODE" != "200" ]]; then echo "FAIL: /v1/receive returned $CODE (expected 200; if 400 — MODE not flipped or still linked-secondary)"; exit 1; fi
   echo "ok /v1/receive=200"

   echo "==> /v2/send regression"
   TS=$(curl -fsS -X POST "$API/v2/send" -H 'Content-Type: application/json' \
     -d "{\"message\":\"wave0-smoke\",\"number\":\"$NUM\",\"recipients\":[\"$FARMER\"]}" | jq -r '.timestamp')
   if [[ -z "$TS" || "$TS" == "null" ]]; then echo "FAIL: /v2/send did not return timestamp"; exit 1; fi
   echo "ok /v2/send timestamp=$TS"

   echo "PASS"
   ```
  </action>
  <acceptance_criteria>
    - `jq -e '.[0].envelope.dataMessage.message' src/agents/alerter/test/fixtures/envelopes/text.json` returns a non-null string
    - `jq -e '.[0].envelope.dataMessage.attachments | length' src/agents/alerter/test/fixtures/envelopes/photo-batch.json` returns `3`
    - `jq -e '.[0].envelope.dataMessage.attachments[0].voiceNote' src/agents/alerter/test/fixtures/envelopes/audio.json` returns `true`
    - `jq '. | length' src/agents/alerter/test/fixtures/envelopes/snooze.json` returns `4`
    - `grep -n 'POST /transcribe' src/agents/alerter/test/helpers/fake-whisper-server.js` returns 1+ matches AND `grep -n 'function start' src/agents/alerter/test/helpers/fake-whisper-server.js` returns a match
    - `bash -n scripts/wave0-signal-receive-smoke.sh` exits 0 (syntax valid)
    - `test -x scripts/wave0-signal-receive-smoke.sh` (executable bit set)
    - After Task 1 completes, running `bash scripts/wave0-signal-receive-smoke.sh` prints "PASS"
  </acceptance_criteria>
  <verify>
    <automated>jq -e '.[0].envelope.source' src/agents/alerter/test/fixtures/envelopes/text.json && jq -e '.[0].envelope.dataMessage.attachments | length == 3' src/agents/alerter/test/fixtures/envelopes/photo-batch.json && bash -n scripts/wave0-signal-receive-smoke.sh && bash scripts/wave0-signal-receive-smoke.sh</automated>
  </verify>
  <done>4 fixtures present + valid JSON; fake-whisper-server matches fake-signal-server factory shape; smoke script PASSes against live signal-cli.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: RED skeleton tests for capture, transcribe-client, llm-client</name>
  <read_first>
    - src/agents/alerter/test/receive-loop.test.js (test driver pattern — describe/beforeEach/afterEach/test)
    - src/agents/alerter/test/snooze.test.js (current shape — Wave 4 will extend)
    - src/agents/alerter/test/helpers/fake-signal-server.js (harness)
    - .planning/phases/25-bidirectional-signal-farmer-robot-capture-channel/25-PATTERNS.md (test analog block; mock SDK pattern)
    - .planning/phases/25-bidirectional-signal-farmer-robot-capture-channel/25-RESEARCH.md (Phase Requirements → Test Map)
  </read_first>
  <files>
    src/agents/alerter/test/capture.test.js,
    src/agents/alerter/test/transcribe-client.test.js,
    src/agents/alerter/test/llm-client.test.js
  </files>
  <behavior>
    transcribe-client.test.js (tests against fake-whisper-server):
      - Test 1: success → returns `{ ok: true, text, duration_ms, language }` matching server canned response
      - Test 2: server returns 500 → returns `{ ok: false, reason }` (does NOT throw)
      - Test 3: server delay > timeoutMs → returns `{ ok: false, reason: /abort|timeout/i }` (does NOT throw)
      - Test 4: POST body contains `audio_path` field equal to the value passed in

    llm-client.test.js (jest.mock('@anthropic-ai/sdk')):
      - Test 1: client.messages.create returns a stub message → `compose()` returns `{ ok: true, text: '<stub>' }`
      - Test 2: client.messages.create throws → `compose()` returns `{ ok: false, reason }` (does NOT throw)
      - Test 3: prompt assembly — assert `messages.create` was called with `model: 'claude-sonnet-4-6'`, `max_tokens: 150`, and the user content includes the rolling-history block, sensor-snapshot block, and current-message block (assert via mock call args)

    capture.test.js (mock pool, inject fake transcribeClient/llmClient/signalClient):
      - Test 1: text-only envelope → `pool.query` called with INSERT into signal_capture; row has `message_type='text'`, `raw_text=<envelope.dataMessage.message>`, `attachment_paths=[]`
      - Test 2: audio envelope → ULID generated; attachment downloaded (signalClient.fetchAttachment called with envelope.dataMessage.attachments[0].id); file written under temp baseDir; transcribeClient called; row has `transcript=<text>`
      - Test 3: photo-batch envelope → 3 attachments downloaded; row has `attachment_paths.length === 3`
      - Test 4 (R6 — degraded transcribe): transcribeClient returns `{ ok: false }` → row written with `degraded=true`, transcript=null; signalClient.send called with a degraded reply mentioning attachment count + timestamp; NO throw escapes
      - Test 5 (R6 — degraded LLM): llmClient.compose returns `{ ok: false }` → row has `llm_reply=null`; signalClient.send called with a fallback receipt naming raw counts; NO throw escapes
      - Test 6: any internal throw (mock pool.query rejects) is caught — `handle()` resolves (does not reject); `logger.warn` called

    All three test files are RED today (subjects under test do not exist). They will turn GREEN in Waves 1–4. Per Nyquist: tests scaffold first.
  </behavior>
  <action>
1. Write `src/agents/alerter/test/transcribe-client.test.js` mirroring `receive-loop.test.js` driver shape. Import `fake-whisper-server.js` from helpers. Subject under test: `require('../src/transcribe-client').createTranscribeClient` (will not resolve until Wave 2). Wrap require inside the test file behind a `try { ... } catch { describe.skip(...) }` IIFE so the test file is loadable today; remove the skip-guard in Wave 2 by replacing it with a direct `require`. Mark each test with the relevant requirement in the test name (e.g. `'(R3) returns ok=false on 500 without throwing'`).

2. Write `src/agents/alerter/test/llm-client.test.js`. At top: `jest.mock('@anthropic-ai/sdk', () => { /* default export class with messages.create as a jest.fn() */ })`. Subject under test: `require('../src/llm-client').createLlmClient` (will not resolve until Wave 3). Same skip-guard idiom. The mock factory should expose the jest.fn() for assertion access via `require('@anthropic-ai/sdk')`.

3. Write `src/agents/alerter/test/capture.test.js`. Mock pool: `{ query: jest.fn().mockResolvedValue({ rows: [], rowCount: 1 }) }`. Inject fake clients via constructor: `createCapturePipeline({ pool, signalClient: { fetchAttachment: jest.fn().mockResolvedValue(Buffer.from('AAA')), send: jest.fn().mockResolvedValue({ ok: true }) }, transcribeClient: { transcribe: jest.fn().mockResolvedValue({ ok: true, text: 'foo', duration_ms: 100, language: 'en' }) }, llmClient: { compose: jest.fn().mockResolvedValue({ ok: true, text: 'logged inoc-2026-04-27' }) }, captureHistory: { selectRecentBySender: jest.fn().mockResolvedValue([]) }, sensorSnapshot: jest.fn().mockResolvedValue({ sensors:{ humidity:90, temperature:22, co2:600 } }), baseDir: tmpDir, logger })`. Use `os.tmpdir() + '/capture-test-' + Date.now()` for baseDir; cleanup with `rimraf` or `fs.rm({recursive:true})`. Same skip-guard idiom (subject under test does not exist yet).

4. Each test file MUST set `nyquist`-friendly named tests: include the requirement ID in parentheses at the start of the test name so grep-by-requirement works (`'(R3) ...'`, `'(R5) ...'`, `'(R2) ...'`).
  </action>
  <acceptance_criteria>
    - `cd src/agents/alerter && npx jest --listTests test/capture.test.js test/transcribe-client.test.js test/llm-client.test.js` lists all 3 files (no parse errors)
    - `grep -c '(R2)' src/agents/alerter/test/capture.test.js` >= 3
    - `grep -c '(R6)' src/agents/alerter/test/capture.test.js` >= 2
    - `grep -c '(R3)' src/agents/alerter/test/transcribe-client.test.js` >= 3
    - `grep -c '(R5)' src/agents/alerter/test/llm-client.test.js` >= 1
    - `grep -nE "describe\\.skip|it\\.skip|test\\.skip" src/agents/alerter/test/capture.test.js src/agents/alerter/test/transcribe-client.test.js src/agents/alerter/test/llm-client.test.js` returns lines (skip-guard present today; Wave 1–4 removes them)
    - `cd src/agents/alerter && npm test 2>&1 | grep -E 'Tests:|FAIL'` does NOT include any new red failures attributable to these files (skipped is acceptable)
    - `grep -n "claude-sonnet-4-6" src/agents/alerter/test/llm-client.test.js` returns 1+ match (model assertion)
    - `grep -n "max_tokens" src/agents/alerter/test/llm-client.test.js` shows 150
  </acceptance_criteria>
  <verify>
    <automated>cd src/agents/alerter && npx jest --listTests test/capture.test.js test/transcribe-client.test.js test/llm-client.test.js && npm test 2>&1 | tail -20</automated>
  </verify>
  <done>3 RED-skeleton test files committed; jest can parse them; existing alerter test suite still passes (no regressions); skip-guards documented for Wave 1–4 to remove.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| signal-cli REST API ↔ alerter | local-only HTTP on signal-net bridge (or localhost in normal mode); no auth; trust-on-network |
| Huawei B310s-518 admin UI ↔ operator | LAN-only HTTP; password in plaintext on host (/tmp/huawei) |
| Re-registration → Signal infrastructure | one-shot; uses farmer-known phone number; SMS code is the only secret |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-01-01 | Information Disclosure | Huawei admin password handling | mitigate | Password loaded from `/tmp/huawei` (not committed); never paste inline in commit messages or SUMMARY; add to `.gitignore` if path moves under repo |
| T-25-01-02 | Tampering | SMS verification code interception | accept | Local LAN only; B310s-518 admin UI is on the closed 192.168.8.0/24 the SIM router serves; physical access required |
| T-25-01-03 | Spoofing | re-registration of someone else's number | accept | Number is the bot's own +59891840205; ownership proven by SMS receipt on the SIM |
| T-25-01-04 | Information Disclosure | Phone numbers in logs / SUMMARY | mitigate | Reuse existing `maskNumber()` from `src/agents/alerter/src/config.js:52-55` for any post-task SUMMARY output |
| T-25-01-05 | DoS | Wrong MODE flip breaks outbound /v2/send (alerter regression) | mitigate | STEP F outbound regression is a HARD acceptance criterion; rollback path = revert MODE in compose + recreate |
| T-25-01-06 | Repudiation | Trust-all-known-keys silently accepts a new device key | accept | Single-farmer trust model; MITM on Signal protocol is out of scope (Signal handles E2E); recovery is manual re-trust if farmer's keys legitimately rotate |

</threat_model>

<verification>
- `bash scripts/wave0-signal-receive-smoke.sh` returns "PASS" on elder-plops
- `cd src/agents/alerter && npm test` passes (no regressions)
- All 4 envelope fixtures parse as valid JSON with expected shapes
- nvidia-container-toolkit presence recorded in SUMMARY (informational; Wave 2 acts on it)
</verification>

<success_criteria>
- /v1/receive returns 200 (not 400) for +59891840205 in production
- Outbound /v2/send still works after MODE flip
- Round-trip envelope from farmer's phone observed end-to-end
- Wave 0 fixtures + skeleton tests committed; Waves 1–4 unblocked
</success_criteria>

<output>
After completion, create `.planning/phases/25-bidirectional-signal-farmer-robot-capture-channel/25-01-SUMMARY.md` capturing: device_id, /v1/receive HTTP code, /v2/send timestamp, round-trip envelope source, nvidia-container-toolkit presence, list of created files.
</output>
