# Requirements: v1.11 — Extraction prereqs + 2025-paper backfill (ARCHIVED)

> **Archive note (2026-06-14):** This file was reconstructed at v1.11 close from the
> phase ROADMAP blocks and PLAN frontmatter (Phases 53, 54, 54.1, 55, 55B). The live
> `.planning/REQUIREMENTS.md` at the time of this close still held stale v1.7/v1.9
> content (never reset for v1.8/v1.10/v1.11) and was intentionally left untouched for a
> separate reconciliation. This archive is the authoritative v1.11 requirements record.

**Milestone goal:** Run the `mushdatadump` 2025 paper-log corpus through the now-unblocked
extraction → confirm → upsert pipeline, after closing the extraction bugs that would
corrupt a backfill, with a commit-time fidelity cross-check and a human-reconcilable
session surface as the safety floor before the (still-parked) full-corpus run.

**Phases:** 53 (extraction prereqs), 54 (backfill harness + smoke), 54.1 (strain-confirm
before mint), 55 (full-corpus tooling + receipt), 55B (fidelity cross-check + F1/F2
session reconcile).

---

## Requirements (all complete)

### BACK — Backfill pipeline (Phases 53, 54, 55)

- [x] **BACK-01**: `corpus_context` extension pins `year=2025` so the extractor stops hallucinating years on undated notebook pages. (Phase 53, `bf721e2`+`9d25ec7`)
- [x] **BACK-02**: Phase 38 batch-mode no longer misroutes small multi-draft captures (drafts>5 OR conf<0.7 -> batch; else N per-draft confirms). (Phase 53, `9835caf`)
- [x] **BACK-03**: Photo-vs-paper-log classifier eagerness reined in via prompt-only capture_kind classifier + 2 few-shots. (Phase 53, `52f0874`+`673c413`)
- [x] **BACK-04**: Hermetic eval on hand-labeled 2025 notebook pages PASSES before any batch run (8/8 fixtures green). (Phase 53, `a2467ea`+`cc95c8d`)
- [x] **BACK-05**: `scripts/backfill-notebook.js` iterates corpus pages, builds synthetic `signal_capture` rows with `corpus_context={year:2025, source:'paper_log'}`, dispatches through the normal pipeline. Prod-guard + santi-gate + page-range filter. (Phase 54, `bfcde26`)
- [x] **BACK-06**: Bulk-backfill mode short-circuits the CONF-01 YES requirement for `farmer=santi` only; every auto-confirmed draft still emits a farmer-facing audit summary. (Phase 54, `99e3f98`)
- [x] **BACK-07**: Paid LLM responses persisted append-only per-call to `.planning/backfill/<run-id>/responses.jsonl`. (Phase 54, `f4923e4`)
- [x] **BACK-08**: Smoke-run upsert-stability against the Phase 51 contract (stub-enrichment sub-clause resolved N/A; substituted intra-cycle upsert-stability); Cycle 1 farmer SIGN-OFF completed 2026-06-07. (Phase 54, `31b31bf`)
- [x] **BACK-09**: Full-corpus harness (`--all-pages`) + receipt builder (`buildUuidJsonl`/`computePerShapeStats`) + `.planning/notes/` copy-out. (Phase 55, 55-01) -- tooling shipped; the *run itself* stays parked behind GA2/farmer Cycle-2 sign-off.
- [x] **BACK-10**: Per-shape confirm-accuracy stats computed; bulk-backfill auto-YES tagged as non-signal for v1.13 (v1.13 needs human-YES). (Phase 55, 55-01)
- [x] **BACK-11**: Prod-promotion decision documented (default dev-only; prod write only on per-session-class operator opt-in). (Phase 55, 55-02; `55-PROMOTION-DECISION.md`)

### STRAIN — Strain-confirm before mint (Phase 54.1)

- [x] **STRAIN-01**: Strain resolver exact-matches an extraction code against the curated 14-set (`config.strains`); KNOWN vs UNKNOWN, no fuzzy auto-resolve. (54.1-01)
- [x] **STRAIN-02**: Backfill path holds unknown-strain drafts as `needs_review` + one batched Signal message + follow-up confirmed-mint/remap/commit pass. (54.1-02)
- [x] **STRAIN-03**: Live-capture path per-encounter strain ask-back via the Phase 39 confirm-loop; YES authorizes the per-draft mint, correction remaps to canonical. (54.1-03)

  > Design + scope locked in `.planning/todos/pending/2026-05-25-strain-confirm-before-mint.md`. Detection matches the curated 14-code set, not live farmOS terms. Builds on `ensureFungiTypeUuid` (`c2af701`).

### FIDELITY — Commit-time CSV cross-check (Phase 55B)

- [x] **FIDELITY-01**: Commit-time CSV fidelity cross-check HOLDS every entry not exact-verified against the per-page CSV reading (`needs_review`, never hard-reject -- CSV is a fallible 2nd interpretation). `buildCsvBudget`/`consumeCsvBudget` + 3-branch hold (`fidelity_cross_check_no_csv`/`_unverified`/`_nonseeding`). (55B-02)
- [x] **FIDELITY-02**: The gate is wired into the live backfill driver (`backfill-notebook.js` call site) -- the run-1 silent no-op (inputs never supplied) was caught and fixed; re-smoke holds IMG_3776 POY (not committed as KOY). (55B-02, gate-wiring `96d1cd0`)

### SESSION — F1/F2 reconcile surface (Phase 55B)

- [x] **SESSION-01**: Backfill per-block seeding drafts aggregate into ONE per-page `seeding_session` (Phase 52 group-asset mechanism); `aggregateSeedingDraftsToSessionJson`. (55B-03)
- [x] **SESSION-02**: Source notebook page image(s) attached to the session group asset (1..N pages; the inoc session, not the page, is the unit), via the field-scoped `/api/asset/group/{uuid}/image` route. (55B-03; A1' route `bbd9212`, A1-SMOKE PASS)
- [x] **SESSION-03**: Held (needs_review) drafts produce no member, so a human reconciles a session 1:1 against the attached page image (held blocks visibly absent). Code path verified; the live F2 reconcile against dev farmOS is carried as a tracked human-needed follow-on (`55B-HUMAN-UAT.md`, D-03). (55B-03/55B-04)

### SMOKE — Live ship-gate (Phase 55B)

- [x] **SMOKE-01**: 5-page GA1-isolated re-smoke (throwaway pg :5433) green against isolated dev; receipt shows held entries; IMG_3776 POY held (not KOY) is the hard pass/fail. Hard gate PASS recorded in `55B-RE-SMOKE.md` run 3 (`re-smoke-55b-1781472903`): 33 confirmed / 31 held / duplicate_asset_count 0. (55B-04)

---

## Traceability

| REQ-ID | Phase | Status |
|---|---|---|
| BACK-01 | 53 | Complete |
| BACK-02 | 53 | Complete |
| BACK-03 | 53 | Complete |
| BACK-04 | 53 | Complete |
| BACK-05 | 54 | Complete |
| BACK-06 | 54 | Complete |
| BACK-07 | 54 | Complete |
| BACK-08 | 54 | Complete |
| BACK-09 | 55 | Complete (tooling; full run parked GA2) |
| BACK-10 | 55 | Complete |
| BACK-11 | 55 | Complete |
| STRAIN-01 | 54.1 | Complete |
| STRAIN-02 | 54.1 | Complete |
| STRAIN-03 | 54.1 | Complete |
| FIDELITY-01 | 55B | Complete |
| FIDELITY-02 | 55B | Complete |
| SESSION-01 | 55B | Complete |
| SESSION-02 | 55B | Complete |
| SESSION-03 | 55B | Complete (live F2 reconcile -> human-needed, D-03) |
| SMOKE-01 | 55B | Complete |

## Carried forward (not gating v1.11; owned by the parked full-corpus run / v1.13)

- **Full 73-page corpus run** stays parked -- Phase-55/GA2-owned, gated on Cycle-2 farmer sign-off. Not unblocked by the 55B re-smoke alone.
- **Live F2 reconcile (D-03)** -- `55B-HUMAN-UAT.md`; confirm page images on session group assets + held blocks absent from members in dev farmOS.
- **Strain gate unwired in the backfill driver** (`curatedStrains` not passed at the `backfill-notebook.js` call site) -- code review CR-02/WR-06; needs a curated-strain source decision.
- **Session `qty>1` whole-page rollback** (code review CR-01) -- fix before the full-corpus run.
