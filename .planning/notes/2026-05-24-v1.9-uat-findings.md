# 2026-05-24 v1.9 Live-Fire UAT Findings (Santi)

**Context:** v1.9 (Phases 47-50) shipped 2026-05-23 with all hermetic ship-gates green. First live-farmer UAT with Santi the evening of 2026-05-23 -> 2026-05-24. Goal was Option 3: alerter rebuild + small smoke capture + (if green) May-22 reprocess via `49-SHIP-GATE.md`.

Outcome: smoke surfaced 3 real bugs (1 critical, 1 high, 1 medium) plus 1 deeper design gap. May-22 reprocess deferred. v1.9 hermetic posture unchanged; only Phase 38 + Phase 50 changed in-flight (hotfixes).

## Timeline (UTC)

| Time | Event |
|---|---|
| 23:48 | Alerter rebuilt with v1.9 commits (5h-old image replaced) |
| 23:48 | Schema migrations ran cleanly on boot: 4 new columns (signal_outbound.signal_msg_ts + idx, signal_capture.{signal_msg_ts, quote_msg_ts, quote_author_e164}) |
| 23:51-23:53 | Santi sends "DT tubs 0519 1 and 2" -> bot replies with numbered ask-back pointing to 5 stale `commit_failed` drafts from May 13-21. Capture never persists (Bug #1) |
| ~00:30 | Hotfix #1 shipped (`fix(50): stale commit_failed drafts no longer trap fresh captures`, commit `370c6ed`). Alerter redeployed. |
| 11:34 | Santi retries: image + caption "DT tubs 0519 1 and 2" -> extractor produces 2 well-formed observation drafts (`bb34475403…` DT_1, `ccd52457c2…` DT_2), both auto-marked `needs_review` with `batch_mode_low_conf`. Operator-channel ping fires to Santi's own phone: "Hey Don Santiago, paper-log scan from +59892893012: 2 drafts (0 clean, 2 need review). IDs: bb34475403, ccd52457c2." (Bug #2 + Bug #3) |
| 11:34-11:35 | Santi's follow-up text replies ("Just an observation", "Colonizing nicely. Warmer temp better than cold") capture cleanly. Hotfix #1 validated under load. |
| ~12:00 | Hotfix #2 shipped (`fix(38): trinity-skip operator-channel pings when operator==sender`, commit `6b4cbde`). Alerter redeployed. |
| ~12:30 | Plan B (flip drafts to awaiting_farmer + commit through alerter) blocked: `FARMOS_INTEGRATION=0` + mushy-bot lacks read perms on `asset/fungi` in prod farmOS. Plan A (manual via farmOS UI) the only path tonight. |

## Bugs surfaced

### #1 (CRITICAL, FIXED) - Stale commit_failed drafts trapped every fresh capture

**File:** `fix(50): stale commit_failed drafts no longer trap fresh captures` (commit `370c6ed`).

`findActiveDraftsForSender` (Phase 50-04) included EVERY `commit_failed` draft regardless of age. Santi had 5 stale ack-debt drafts from May 13-21. When he sent a fresh capture, the parser greedily classified the text as EDIT (parser.parseReply default), the receive-loop saw `activeDrafts.length > 1`, fired numbered ask-back, and `continue`d -- skipping `insertCapture` entirely. Fresh captures vanished.

Hotfix: age out `commit_failed` drafts >6h in `findActiveDraftsForSender`. `awaiting_farmer` never ages out. EDIT-by-quote on older `commit_failed` still works via Phase 50 quote-resolution (bypasses this gate). Regression test added.

**Validation:** post-hotfix, Santi's "DT tubs 0519 1 and 2" + 2 follow-up texts all captured cleanly.

### #2 (HIGH, FIXED) - Operator-channel ping leaked to farmer surface (trinity-bleed)

**File:** `fix(38): trinity-skip operator-channel pings when operator==sender` (commit `6b4cbde`).

Phase 38 Plan-08's `sendBatchReviewSummary` (multi-draft paper-log scan) and `sendNeedsReviewPing` (ask-back cap hit) target `operatorRecipient` (env: `SIGNAL_RECIPIENT`). The radicheta/santi/farmer-1 trinity (per `[[user_santi_radicheta_farmer1_trinity]]`) means operator-recipient IS Santi's own Signal number. While Santi-as-farmer was in active conversation, he got an operator-debug ping addressed to "Don Santiago" containing his own phone number written back + opaque 10-char draft-id hex fragments + internal `clean/needs_review` mechanics.

Hotfix: skip operator-channel ping when `operatorRecipient === senderE164`. Other-farmer cases (Vikki, Selina) still ping Santi-as-operator normally. Regression tests added.

### #3 (MEDIUM, BACKLOG) - Phase 38 batch-mode routes 2-draft captures into needs_review queue

**File:** `.planning/todos/pending/2026-05-24-phase38-batch-mode-misroutes-small-multi-draft-captures.md`

`drafts.length > 1` automatically routes through Plan 08 batch-mode review queue, which auto-marks every draft `needs_review` with reason `batch_mode_low_conf` and suppresses the per-draft `confirm_prompt`. Conflates true paper-log scans (10+ rows, low confidence) with multi-event single messages (2-3 tubs, high confidence). Result: tonight's two well-formed DT drafts (per-field confidence 0.8-0.9) sit in limbo, never previewed to farmer, never committed.

Proposed fix: heuristic at routing seam -- `drafts.length > 5 OR min(per-draft confidence) < 0.7` -> review queue; else -> N normal `confirm_prompt`s through Phase 39. Eval fixture: tonight's `01KSCW771VB2FDWBPWNS4MEHAZ` capture preserved as named regression case.

### #4 (LOW, BACKLOG) - Photo-vs-paper-log classifier too eager

**File:** `.planning/todos/pending/2026-05-24-phase38-photo-vs-paper-log-classifier-too-eager.md`

Extractor treats any multi-bag-visible photo as a paper-log scan, even when the photo is of physical tubs/bags with a single caption (not a notebook page). Data quality recovered fine (`asset_ref=260519_DT_1`/`_2` correctly inferred from caption), but the `capture_kind` classification is wrong. Lower priority once #3 routing bug is fixed.

## Deeper design gap surfaced: observation-of-unknown-asset

Santi's framing during triage: **"if the farmer is recording an observation, the system replying 'that asset does not exist' would be comically wrong."** When a farmer reports an observation against asset X and X is missing from farmOS, the farmer just told us X exists in the physical world; the system's job is to reconcile, not to reject.

Today's `commit-observation.js:22-24` returns `observation_requires_target` (commit_failed) if `resolveQr` finds zero assets. This is correct for absurd inputs (typos, hallucinated IDs) but wrong for the "I haven't logged this block yet, observing it for the first time" case -- which is the production-normal case when a new batch enters the farm.

Backfill design captured separately at `.planning/todos/pending/2026-05-24-observation-of-unknown-asset-should-backfill-not-fail.md`. Likely a Phase 52+ deliverable; needs farmer-confirm UX for the auto-create (double-check the inferred ID against fuzzy-match neighbors before minting).

## What worked

- v1.9 schema migrations ran cleanly idempotent on first prod boot. No data loss, no manual ops.
- Phase 50 quote-thread persistence: all 4 ask-backs and the operator-leak ping carry non-null `signal_msg_ts` (`1779580292807`, `1779580381657`, `1779580411677`, `1779580441673`, `1779622462273`).
- Phase 50 numbered ask-back rendering: format itself is fine. The failure mode was the staleness gate, not the rendering.
- Hotfix turnaround: 30 min for #1 (with regression test + rebuild + redeploy), 30 min for #2.

## What didn't work (initially) — and how the run actually closed

- Initial Plan-B attempt (flip drafts, FARMOS_INTEGRATION=1, let alerter commit) blocked on two pre-existing gaps:
  - mushy-bot lacked fungi perms on the `farm_manager` role in farmOS prod.
  - basic_auth Drupal module was not enabled, so JSON:API treated Basic-Auth headers as anonymous.
- Both were resolved in-session:
  - Enabled `basic_auth` module via `drush pm:enable basic_auth -y`.
  - Granted `farm_manager` the full fungi asset + taxonomy term permissions via `drush role:perm:add` (note: should be ported to `config/sync/user.role.farm_manager.yml` permanently so the next `drush cache:rebuild`/config-import doesn't revert it -- captured as todo).
- DT_1 + DT_2 assets minted via direct JSON:API POST (since the alerter's commit-observation path doesn't backfill missing assets, per the principle todo).
- 2 stuck drafts flipped from `needs_review` -> `confirmed`, commit-watchdog picked them up next tick, both committed cleanly (HTTP 201) and the observation logs are now attached to the correct fungi assets in farmOS prod.
- Surfaced bug #5 (ack-renderer dead code): the success-ack said "saved as a general farm note since I couldn't match a specific block" even though the block WAS matched. Hotfix shipped (`53eb652`).

## State after UAT (final)

- v1.9 milestone unchanged on disk (still `[x]` SHIPPED).
- Three hotfix commits on `main`, all with regression tests:
  - `370c6ed` -- Phase 50 staleness gate (stale `commit_failed` no longer traps fresh captures)
  - `6b4cbde` -- Phase 38 trinity-skip (no operator-channel chatter when sender is Santi)
  - `53eb652` -- Phase 45 ack-renderer reads draft_json.{asset_ref,qr_codes,...} when row.target absent
- Three backlog items filed:
  - `2026-05-24-phase38-batch-mode-misroutes-small-multi-draft-captures.md`
  - `2026-05-24-phase38-photo-vs-paper-log-classifier-too-eager.md`
  - `2026-05-24-observation-of-unknown-asset-should-backfill-not-fail.md`
- One config-drift gap to close: port the runtime `drush role:perm:add` grant to `config/sync/user.role.farm_manager.yml` in the farmos shared repo before any `drush config:import` runs (the role config is otherwise authoritative and would revert).
- 2 observation logs landed in prod farmOS:
  - log `00634854-5eb4-405b-96cc-37982b4184cc` attached to `260519_DT_1`
  - log `cea082cf-e5a3-4ca6-89bc-707357db6f73` attached to `260519_DT_2`
- The 2 originating drafts (`bb34475403…`, `ccd52457c2…`) are now `committed`; no discard cleanup needed.

## What didn't reach scope

- The v1.9 May-22 ship-gate reprocess (`49-SHIP-GATE.md`) -- still pending; targets farmOS **dev**, not prod, by design. Tonight's commits write to prod (since that's where the DT_1/DT_2 blocks live physically). The ship-gate path is unchanged.

## Lessons

1. **Stale-state ageing is a routing concern, not just a UI concern.** Phase 50's ambiguity calc assumed `commit_failed` was "actively waiting for farmer action"; in practice it's a long-tail of operator-bound debt. Active-set queries need explicit time windows.
2. **Trinity-bleed is a real production concern, not an edge case.** The same human IS the operator + farmer-1 + project owner. Any operator-channel send needs to ask "what role is this person in right now?" before firing.
3. **Real-data UAT > curated eval, every time.** All four bugs were latent in hermetic-green code paths. Tonight's 90-min smoke surfaced more than 800 hermetic tests did, exactly as `[[feedback_real_data_before_ship_gate_pass]]` predicts.
4. **Observation-of-unknown-asset is the canonical reason FARMOS_INTEGRATION=0 is the right default today.** The bot can't fail-loudly to the farmer ("that asset doesn't exist") without losing trust; until the auto-backfill flow lands, write-through-to-farmOS prod is a regression risk, not a feature.

## State after UAT

- Phase 48/49/50 ROADMAP entries: all still `[x]` SHIPPED. v1.9 milestone state unchanged on disk.
- Two new hotfix commits land on `main` (`370c6ed` + `6b4cbde`), both with regression tests.
- Three new backlog items filed under `.planning/todos/pending/`:
  - `2026-05-24-phase38-batch-mode-misroutes-small-multi-draft-captures.md`
  - `2026-05-24-phase38-photo-vs-paper-log-classifier-too-eager.md`
  - `2026-05-24-observation-of-unknown-asset-should-backfill-not-fail.md`

## Loose ends (not blocking)

- [ ] **Port runtime perm-grant to farmos repo config.** Tonight's `drush role:perm:add` on `farm_manager` (fungi asset CRUD + fungi_type/fungi_xing term edit) is in-memory only -- next `drush config:import` will revert it because `config/sync/user.role.farm_manager.yml` is authoritative. Add the perms list to that YAML and commit on the farmos repo. Also persist the `basic_auth` module enable in farmos repo's enabled-modules config so it survives a fresh import.
- [ ] **New `tub` fungi_xing term should be added to farmos repo config sync** if it's not already there -- otherwise a fresh `drush config:import` would remove it. (`fungi_xing` is a vocabulary, the term is content not config -- usually safe -- but worth confirming.)
- [ ] **Phase 38 batch-mode routing fix** (backlog `2026-05-24-phase38-batch-mode-misroutes-small-multi-draft-captures.md`). Captures like tonight's "DT tubs 0519 1 and 2" should produce N per-draft confirm_prompts via Phase 39, not auto-mark needs_review. Until this lands, `needs_review` drafts need manual `status='confirmed'` flips for the alerter to pick them up.
- [ ] **Observation-of-unknown-asset backfill flow** (backlog `2026-05-24-observation-of-unknown-asset-should-backfill-not-fail.md`). Tonight the assets were minted by hand via direct API; the principle says future runs should mint-with-farmer-confirm automatically.
