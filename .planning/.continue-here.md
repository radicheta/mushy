---
context: between-phases
milestone: v1.7
last_phase_closed: 38
next_phase: 39
status: paused
last_updated: 2026-05-13T01:42:00Z
---

# Resume here -- v1.7 milestone, between Phase 38 (just closed) and Phase 39

## Critical Anti-Patterns (do NOT repeat these)

| Pattern | What happened | Severity | Structural mitigation |
|---|---|---|---|
| Curated-only ship-gate eval | Plan 07 PASS on 2 curated photo-only fixtures missed every production-shape bug | blocking | Ship-gate harness now MUST include >=1 real-session fixture from `/mnt/mossrock/shared/mushdatadump-prod/`. See `[[feedback_real_data_before_ship_gate_pass]]`. |
| Harness bypasses production code path | Eval harness called `readImageToBase64` directly while live pipeline silently dropped images. PASS on a path that did not exist in production. | blocking | Eval harness now imports `loadImageBlocks` from `src/agents/alerter/src/extraction/pipeline.js`. Any future code-path divergence between harness and live alerter MUST be caught the same way. |
| Fake-green healthcheck | Whisper `/health` checked only that FastAPI was up. GPU drifted under the container for 5 days and every `/transcribe` returned 500 silently. | blocking | `/health` now does eager startup probe (model load + 1s synthetic transcribe); returns 503 with reason on cuda failure. See `src/whisper-transcribe/main.py`. |
| Full-batch run before smoke | Burned $5.65 on a 96-fixture FAIL caused by a single-line maxTokens bug that a $0.51 10-fixture smoke would have caught immediately. | blocking | Run smoke (5-10 items) under identical config before ANY paid full-corpus / render / sim / long-compute run. See `[[feedback_smoke_before_expensive_batch]]`. |
| Overwriting intermediate artifacts | Was about to let the run-2 PASS overwrite the run-1 FAIL report. Don Santiago flagged: keep the paper trail. | advisory | Copy current output to `<name>-<state>.<ext>` BEFORE re-running with different params. See `[[feedback_keep_paper_trail_of_intermediates]]`. |

**All four blocking patterns are now structurally addressed in code. The advisory one is a behavioral rule for future sessions.**

<current_state>
Phase 38 (Extraction Pipeline) was reopened mid-session 2026-05-12 after the first
real Signal inoc session failed 0/4 through the live alerter. Five bugs root-caused,
all fixed in code. Plan 09 (remediation) shipped end-to-end and Don Santiago attested
PASS on a 96-fixture ship-gate (95 curated + 1 real prod session) at 95.8% schema
conformance.

Phase 38 is now REALLY closed (commit b8847e8). Ready to resume autonomous v1.7
close starting at Phase 39.
</current_state>

<completed_work>

This session (2026-05-12):

- Phase 38 closed on Plan 07 PASS (commit 30c409c) -- then retracted same hour
- Phase 38 reopened (4fb2541) after prod inoc session 0/4 in live alerter
- Whisper GPU drift recovered (container restart); HF model cache persisted to named volume
- Pipeline image-wire bug fixed (a04a6bc) -- attachmentPaths now properly loaded to base64
- Replay harness written (d5b853f) for offline pipeline-fix validation
- Plan 09 filed (683c642) with 6 tasks
- Plan 09 Tasks 1-4 executed (2d2e64b): deep whisper /health, harness-pipeline parity,
  prod-session fixtures, species vocab additions (winecap->WIN etc)
- Plan 09 5th bug found mid-eval: extractor maxTokens 2048 truncating multi-event pages;
  bumped to 16384 (76df06f)
- Plan 09 re-eval PASS at 95.8% (commit included in b8847e8)
- Phase 38 RE-CLOSED on Plan 09 attestation (b8847e8)
- 3 new memory rules written: smoke-before-expensive, keep-paper-trail, real-data-ship-gate
</completed_work>

<remaining_work>

- Phase 39: Farmer Confirmation Loop -- discuss -> plan -> execute (NEW; no plans yet)
- Phase 40: FarmOS Write Path
- Phase 41: Ingestion Harness
- Phase 42: SHI-on-Sawdust Pilot

User's autonomous scope choice (still valid): "All the way to v1.7 close (39 -> 42)".
</remaining_work>

<decisions_made>

- Real prod session fixture mandatory in ship-gate denominator going forward
- extractor maxTokens 2048 -> 16384 (default); fixed both harness and live alerter
- whisper /transcribe gets vad_filter=True + condition_on_previous_text=False (kills
  end-of-audio counting hallucinations; 4x faster too)
- Plan 08 superseded by Plan 09 (prod-corpus eval folded into ship-gate)
- Corpus dir convention: `/mnt/mossrock/shared/mushdatadump-prod/<YYYY-MM-DD>_<session-tag>/`
- Eval harness routes image loading through `pipeline.loadImageBlocks` (exported)
- HF model cache lives on named docker volume `whisper-hf-cache`
- Compose healthcheck start_period bumped to 120s to fit cold model load
</decisions_made>

<blockers>
None blocking. Only "human action pending" is user signal to resume autonomous run.
</blockers>

## Required Reading (in order)

1. `.planning/STATE.md` -- current position + Phase 38 close audit
2. `.planning/ROADMAP.md` (Phase 39 section, lines 191-212) -- Phase 39 goal + success criteria
3. `.planning/phases/38-extraction-pipeline/38-EVAL-REPORT.md` -- the ship-gate verdict the milestone was re-grounded on
4. `/mnt/mossrock/shared/mushdatadump-prod/2026-05-12_inoc_santi/MANIFEST.md` -- the prod fixture format that future Phase 41 ingestion harness will key on
5. Memory: `feedback_real_data_before_ship_gate_pass`, `feedback_smoke_before_expensive_batch`, `feedback_keep_paper_trail_of_intermediates` -- three rules learned this session

## Infrastructure State

- `mushy-alerter-1`: live with image-wire fix + maxTokens=16384 (rebuilt 2026-05-12 ~21:00)
- `mushy-whisper-transcribe-1`: live with deep /health + VAD + persisted HF cache (rebuilt ~20:50)
- `mushy-timescale-1`: 1 draft sitting in `signal_draft` status=awaiting_farmer; not going anywhere until Phase 39 ships
- `mushy-signal-cli-1`: live; primary +59891840205
- Prod corpus dir `/mnt/mossrock/shared/mushdatadump-prod/` (NFS, group-writable, currently holds the 2026-05-12_inoc_santi/ session)

## What runs in farmOS right now?

NOTHING from this pipeline. Zero farmOS write code exists in the alerter (greps for
`/api/log`, `/api/asset`, `farmosClient`, etc. all return empty). The Phase 38 pipeline
only produces drafts in Timescale's signal_draft table. Phase 39 confirms them, Phase 40
writes to farmOS -- both not built yet. Raw data is safely staged.

<context>
Don Santiago is taking a context-clear break before resuming the autonomous run.
The session was long and information-dense; clearing context is the right move.
On resume, the natural next move is /gsd-autonomous --from 39 (scope already
agreed: through Phase 42). Phase 39 is fresh (no CONTEXT.md, no plans) so
autonomous will run discuss -> plan -> execute.

Phase 39 is the confirmation loop. The interesting design space is what farmer
UX looks like for confirming/editing/rejecting a 30-draft extraction from a
single logsheet photo without spamming them with 30 separate ask-back messages.
A summary preview + single YES/EDIT/NO probably; details for discuss-phase.

One open observation from this session, not blocking: when the alerter pipeline
returns ok:false (extraction failed), it still sends the farmer 4 auto-replies
in degraded state. Phase 39 design should address "degraded mode" outbound UX
rather than auto-responding as if extraction succeeded.
</context>

<next_action>
1. Read this file + the items in Required Reading
2. Confirm with Don Santiago that autonomous v1.7 close (Phases 39 -> 42) should resume
3. Run /gsd-autonomous --from 39
4. Phase 39 will start with discuss-phase (no CONTEXT.md exists yet)
</next_action>
