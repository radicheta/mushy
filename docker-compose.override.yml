version: '3.8'

services:
  openmct:
    network_mode: "host"
    extra_hosts:
      - "host.docker.internal:host-gateway"

  life_support:
    network_mode: "host"
    extra_hosts:
      - "host.docker.internal:host-gateway" 